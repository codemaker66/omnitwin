# E2E Triage - 2026-04-27 Audit Finding

Triaged: 2026-04-29.
Task: T-084.

## Summary

Current command:

```powershell
pnpm --filter @omnitwin/web e2e -- --reporter=list
```

Result on 2026-04-29: 64 tests discovered, 32 passed, 29 failed, 3 skipped.

The audit record said 28 failures. Current main has 29 failures. The extra failure is not a new class of defect; it falls into the same existing E2E reliability/product-gap clusters.

Before the successful full run, localhost:5173 was occupied by a stale Vite/Node process returning HTTP 500. Stopping that process and using the fresh Vite server restored a valid test run.

## Resolution

T-086 fixed the current failure set on 2026-04-29.

Final verification:

```powershell
pnpm --filter @omnitwin/web lint
pnpm --filter @omnitwin/web typecheck
pnpm --filter @omnitwin/web e2e -- --reporter=list --workers=1
```

Result: 64 tests discovered, 61 passed, 3 skipped, 0 failed.

Changes made:

- Hallkeeper now fetches `/v2` before `/progress`, so progress failures cannot mask a 403/404 from the authoritative sheet endpoint.
- Hallkeeper normalizes optional `instructions` through `EventInstructionsSchema` and tolerates legacy rows missing `positions` or `notes`.
- Editor vertical toolbar scrolls on shorter desktop viewports, keeping the Sign In button reachable.
- Dashboard route admits the documented `staff` role.
- Review API schemas now match the shared API client's already-unwrapped `{ data }` payload boundary.
- Planner submit and staff review E2E fixtures now mock the authenticated config/review contracts with schema-valid UUIDs and complete review snapshots.
- The Hallkeeper lazy-load assertion now targets actual route/page chunks instead of shared domain modules.
- Editor interaction tests run serially to avoid local WebGL/dev-server contention.

## Classification

| Cluster | Count | Classification | Evidence | T-086 action |
|---|---:|---|---|---|
| Editor catalogue interactions time out under full parallel run | 2 | Flaky/test-infra | `clearing search restores all catalogue items` and `clicking a collapsed category header expands it again` both pass with `--workers=1` in 7-8 seconds. | Do not change catalogue behavior. Run heavy WebGL E2E serially or mark this describe block serial/increase timeout. |
| Editor Sign In button is outside the viewport | 3 | Real product bug | `page.getByRole("button", { name: "Sign In" }).click()` sees the button but Playwright cannot click it because it is outside the viewport. Screenshot shows the left toolbar overflowing below the screen. | Fix editor toolbar layout so every command remains reachable at the Playwright desktop viewport and likely smaller laptop heights. |
| Hallkeeper normal render crashes | 15 | Real product bug plus stale fixture | Page crashes with `Cannot read properties of undefined (reading 'accessibility')` from `HallkeeperPage.tsx`; `HallkeeperSheetV2.instructions` is nullable, and the test fixture omits it entirely. | Make `HallkeeperPage` null-safe for `data.instructions`; update E2E fixture to include schema-current `instructions: null` and `approval: null`. |
| Hallkeeper 403/404 error states are masked | 2 | Real product bug plus incomplete test mock | 404 test renders generic `Failed to fetch` because `/progress` is unmocked and the page fetches `/v2` and `/progress` in one `Promise.all`; the progress failure masks the primary `/v2` status. | Mock `/progress` in error-state tests and harden `HallkeeperPage` so a progress fetch failure cannot hide a sheet 403/404. |
| Hallkeeper lazy-load performance assertion | 1 | Stale/brittle test | Test flags shared dev-server modules such as `packages/types/dist/hallkeeper-v2.js` as "hallkeeper chunk" loads. That is not equivalent to loading `HallkeeperPage` or a production route chunk. | Rewrite assertion against production build chunks or filter for actual page/dashboard chunks, not shared type/domain modules. |
| Planner submit-for-review tests never reach editor | 3 | Stale/incomplete test fixture | Snapshot shows `Failed to load layout / Network error`; tests mock review endpoints but not the authenticated configuration load path. The panel also intentionally hides when there are no objects. | Mock the config load path used for authenticated planners, set `isPublicPreview: false`, and include at least one placed object before expecting `submit-for-review-button`. |
| Staff review tests hit route 403 | 3 | Product-policy mismatch / likely real auth bug | Snapshot shows `403` with `Your role: staff`. `USER_ROLES` includes `staff`, and the review UI/API comments describe staff review flow, but `/dashboard` allows only `admin`, `hallkeeper`, and `planner`. | Decide policy. If staff are meant to review, add `staff` to dashboard allowed roles. If not, change the tests to seed an allowed role and rename the flow. |

## Failed Tests By Cluster

### Flaky/test-infra: editor catalogue parallel timeout

- `e2e/editor-interactions.spec.ts:108` - clearing search restores all catalogue items
- `e2e/editor-interactions.spec.ts:128` - clicking a collapsed category header expands it again

Targeted rerun:

```powershell
pnpm --filter @omnitwin/web e2e -- e2e/editor-interactions.spec.ts -g "clearing search restores all catalogue items|clicking a collapsed category header expands it again|Sign In button opens the auth modal" --reporter=list --workers=1
```

Result: the two catalogue tests pass serially; Sign In still fails.

### Real product bug: editor Sign In off-screen

- `e2e/editor-interactions.spec.ts:161` - Sign In button opens the auth modal
- `e2e/editor-interactions.spec.ts:167` - auth modal closes when Escape is pressed
- `e2e/editor-interactions.spec.ts:177` - auth modal closes when the backdrop is clicked

All three fail at the first click on the Sign In toolbar button.

### Real product bug plus stale fixture: Hallkeeper normal render

- `e2e/hallkeeper.spec.ts:178` - displays the venue name in the page header
- `e2e/hallkeeper.spec.ts:182` - renders the config name as the page h1
- `e2e/hallkeeper.spec.ts:188` - displays the space name in the header meta chips
- `e2e/hallkeeper.spec.ts:192` - displays the guest count in the header
- `e2e/hallkeeper.spec.ts:197` - displays the room dimensions chip in the header
- `e2e/hallkeeper.spec.ts:209` - renders a phase heading for every phase in the payload
- `e2e/hallkeeper.spec.ts:215` - renders item rows with their quantities
- `e2e/hallkeeper.spec.ts:221` - accessory rows with afterDepth > 0 show an after badge
- `e2e/hallkeeper.spec.ts:227` - zone subheaders are rendered under each phase
- `e2e/hallkeeper.spec.ts:233` - clicking a row toggles its checkbox
- `e2e/hallkeeper.spec.ts:244` - Download PDF button is present
- `e2e/hallkeeper.spec.ts:248` - Print button is present
- `e2e/hallkeeper.spec.ts:256` - floor plan placeholder is shown when diagramUrl is null
- `e2e/hallkeeper.spec.ts:326` - renders the approval banner when approval is populated
- `e2e/hallkeeper.spec.ts:356` - does NOT render the approval banner when approval is null

Targeted rerun confirms the page-level crash:

```text
Unexpected Application Error!
Cannot read properties of undefined (reading 'accessibility')
```

### Real product bug plus incomplete mock: Hallkeeper error states

- `e2e/hallkeeper.spec.ts:280` - shows configuration-not-found message when the API returns 404
- `e2e/hallkeeper.spec.ts:294` - shows permission-denied message when the API returns 403

The 404 targeted rerun rendered generic `Failed to fetch`, not `Configuration not found.`.

### Stale/brittle test: code splitting assertion

- `e2e/performance.spec.ts:87` - code splitting works - hallkeeper chunk loads lazily

Failure was caused by shared dev-server module URLs:

```text
packages/types/dist/hallkeeper-v2.js
packages/types/dist/hallkeeper-accessories.js
packages/types/dist/hallkeeper-instructions.js
```

These names are domain modules, not proof that the Hallkeeper page chunk loaded.

### Stale/incomplete fixture: planner submit-for-review

- `e2e/planner-submit.spec.ts:87` - draft config surfaces the Submit for Approval button
- `e2e/planner-submit.spec.ts:97` - clicking Submit hits the submit endpoint and flips UI to locked
- `e2e/planner-submit.spec.ts:114` - changes_requested state also surfaces the submit button

Targeted rerun snapshot:

```text
Failed to load layout
Network error - check your connection
```

### Product-policy mismatch: staff review dashboard

- `e2e/staff-review.spec.ts:95` - dashboard Reviews tab shows the submitted config in the pending list
- `e2e/staff-review.spec.ts:107` - opening a pending review shows its detail + available actions
- `e2e/staff-review.spec.ts:125` - clicking Approve hits the approve endpoint

Targeted rerun snapshot:

```text
403
You don't have permission to access this page.
Your role: staff
```

## Recommended T-086 Order

1. Fix Hallkeeper null-safety and `/progress` masking. This addresses 17 failures and protects the real page from nullable `instructions`.
2. Fix the editor toolbar overflow. This addresses 3 failures and an actual user-reachability bug.
3. Decide the staff-dashboard policy. If staff should review, add `staff` to `/dashboard`; otherwise update tests to seed an allowed review role.
4. Refresh planner submit fixtures so they mock the real authenticated config path and include placed objects.
5. Rewrite the lazy-load assertion so it measures route chunks rather than shared dev modules.
6. Make WebGL-heavy editor interaction tests serial or raise timeout for local runs; no product behavior change needed for the two catalogue tests.

## Skipped Tests

The 3 skipped tests are intentional SpacePicker/config-creation tests in `public-config-flow.spec.ts`. They are skipped after the rebrand because `/editor` now renders the public landing page and the planner happy path moved to `/plan`.
