# Focused Hallkeeper release — live verification

## Delivered identity

Web source `91220e00072700143f9c73dfa151bb47b17917d6` was published by normal non-force push from `54f4dc5a`, preserving the copy cleanup, its `9138d287` closeout and the current email-verification behavior. Vercel Production deployment `6315921182` succeeded. The authenticated provider overview at `HdXYNY6A2B1nLUmp4edKNK272gtZ` showed Ready Latest, Production, `venviewer.com` and the exact commit link. Public entry is `/assets/index-C-XTXi4z.js`; public stylesheet is `/assets/index-DNroCu9R.css`. API remains ready at `7f2a701b4dade7d9d6048a82511ffa83cf4ff821`; this change contains no API, shared types, migrations, dependency or lockfile delta.

Exact clean-source production-config build passed. The combined source passed 55 focused tests, full web/e2e TypeScript and affected lint. Local rendered desktop/mobile interactions and the full four-page paper output passed. CI `34160437128` had passed Build, Typecheck, Lint and Security Audit at the live checkpoint; Test and four E2E shards were still running. A full CI green result is not claimed.

## Actual signed-in production observations

- [Approved presentation sheet](https://venviewer.com/hallkeeper/3b18bfc3-4a40-4723-8130-13134c80e16e?eventId=fa64096b-4bbe-4d76-87a8-2b938343d863) renders the focused ivory workspace, room photograph and six coloured views. The actual ASCII demo title, approved v1/Blake Faraway provenance and zero supplied guests are unchanged. The local fixture title's replacement diamonds were fixture-only encoding, not production data.
- Verified event context resolves the planned 16:00 on 7 September 2026 and the 14:00 setup, 16:00 demonstration and 17:00 reset running order. Planned times are labelled; no execution is inferred.
- 288 manifest items remain distinct from 162 saved footprints. Paging from checks 1–5 to 6–6 and choosing a saved table marker return the correct setup row without checking it. All 43 real setup rows remain unchecked throughout.
- Preparation shows the supplied operations information and honestly reports absent planner contacts. Checks, Hosting, Reset and Handback open their real content. Handback links to the verified v1 pack and reports its 53 still-open operations tasks. No operational writes, approvals, messages, invitations or venue data changes were performed.
- The [six-room library](https://venviewer.com/hallkeeper/rooms) exposes separate North and South Gallery entries. North displays the faithful left-room original detail; South displays its cleaned plan and switches to its original. Grand Hall's cleaned reference opens inside the sheet. All 11 published PNG assets return HTTP 200 with `image/png`.
- Download PDF completes its request and displays `PDF download started.` The browser Print action caused a CDP timeout; its native preview was not available through the tab's accessibility tree. The browser tool explicitly blocked the downloads-page navigation, and no workaround was attempted. Saved-file completion and native live print-preview contents are therefore unverified. The actual local print mirror was rendered and visually checked across four pages, retaining all 43 fixture rows and 162 footprints.

## Monitoring and evidence

All 15 bounded unauthenticated GET/HEAD smoke checks passed. The existing `final-config.json` was rotated only after qualification, preserving its previous bytes and all protected origins, route declarations and content-type assertions. The promoted configuration is byte-identical to its passing candidate. `VenviewerSmokeMON` remains Ready; its before/after XML is identical and its schedule/action/principal/settings were not edited.

Build, publication, provider and static-asset receipts are at `D:/claude/venviewer-hallkeeper-focus-evidence-20260907/`. Smoke rotation and public HTML identity evidence are at `D:/claude/demo-smoke-readonly-20260906/hallkeeper-rotation-91220e00-20260907/`. Actual production screenshots/accessibility trees were captured inline in the originating task. Local screenshots and `full-sheet-final.pdf` remain under the isolated worktree's `output/playwright/hallkeeper-focus/`.

Five cleaned images are visual references with exact originals retained. North's generated variants remain rejected for geometric drift. No room dimensions, calibrated boundaries or saved event geometry were altered. The source annotations remain annotations, not a newly measured plan. Full supplier/cover/escalation automation and founder aesthetic acceptance are not established by this release.
