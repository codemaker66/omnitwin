# D-004 — Projective texturing as base renderer

Status: Superseded by D-004a
Date: 2026-04-23
Authors: Blake
Supersedes: none
Superseded by: D-004a

## Context

Photographic free-fly mode needs a base photometric layer that looks like the actual venue at near range. Panoramas projected onto aligned geometry deliver this: walls and floors look like the actual venue. Google Street View has used this technique for 20+ years; it is well-trodden. Gaussian splats alone degrade at close range (floaters, blur); projection gives near-photo fidelity in the same regions the splat is weakest. This matches the D-003 philosophy of "each asset does what it's best at."

## Decision

In photographic free-fly mode, the OBJ mesh is rendered using panoramas projected onto it as view-dependent textures, blended per pixel from the N (typically 3–5) nearest panoramas. The OBJ's own baked textures are never used.

## Consequences

- v1 projection blender is a fragment shader using distance + angular + normal weighting (heuristic; full spec in `docs/architecture/GENJUTSU.md` when written).
- v2 projection blender is a learned neural blender (R&D Direction 3; activated post-launch once data exists).
- Depth maps per panorama must be pre-computed server-side and stored on R2 per AssetVersion (for occlusion testing in the shader).
- Target cost: ~3ms per frame on M2 MacBook.
- Quality bar "Photographic Everywhere": if the composite looks worse than a photograph from the same viewpoint, the projection blender or alignment is broken.

## Alternatives considered

- **Gaussian splats alone (no projective base)** — rejected. Splats degrade at close range with floaters and blur, exactly the regions where projection is sharpest.
- **OBJ baked textures** — rejected implicitly. The OBJ's own baked textures cannot match the photographic fidelity of view-dependent panoramic projection.

## References

- Original location: `docs/decisions/ADR-004-projective-texturing-base.md` (migrated 2026-04-25)
- Internal: D-002, D-003, D-005, D-004a (supersedes this)
- External: Google Street View projective texturing prior art
