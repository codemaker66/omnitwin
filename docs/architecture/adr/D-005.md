# D-005 — Splat cropped to reflective surfaces only

Status: Superseded by D-005a
Date: 2026-04-23
Authors: Blake
Supersedes: none
Superseded by: D-005a

## Context

Splats excel at view-dependent appearance (spherical harmonics); they are unnecessary for matte surfaces where projection is cheaper and sharper. Cropping the splat to reflective surfaces only reduces splat file size from ~200MB to ~5–10MB per venue. Smaller splat means faster load, lower VRAM, better mobile support. Cropping also avoids splat vs projection z-fighting on matte surfaces.

## Decision

Production splats are cropped to reflective/transparent surfaces only (chandeliers, mirrors, glass, gilt). Matte surfaces are handled by projective texturing (D-004). The full uncropped splat is archival on R2; it is never rendered in production.

## Consequences

- Cropping is a manual SuperSplat lasso step per venue (post-training).
- SPZ is the production splat format (D-001); cropped splat exports as `.spz`.
- AssetVersion records both archival and cropped splat as separate versions.
- If a reflective surface is missed during cropping, that surface renders as projection only (flat appearance) — visible bug, fixable by re-cropping.

## Alternatives considered

The original ADR did not enumerate alternatives in detail. The implicit rejected alternative is **uncropped splat throughout**: rejected on file size (~200MB vs ~5–10MB), VRAM cost, and z-fighting with the projective texturing base on matte surfaces.

## References

- Original location: `docs/decisions/ADR-005-splat-cropped-to-reflective.md` (migrated 2026-04-25)
- Internal: D-001, D-003, D-004, D-005a (supersedes this)
- External: SuperSplat cropping tool, SPZ format spec
