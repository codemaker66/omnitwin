# The work ladder: what the worker model does next, card by card

**For:** the Opus 5 worker sessions (plentiful usage), designed by the Fable 5.1 session (scarce) on Friday 2026-09-04, 18:40 BST · **Instruction (Blake, verbatim):** "construct a ./goal to efficiently use up the usage in pushing our project closer to the end goal of our grand ambitious, design the tasks and goals using the advance intelligence of claude 5.1 and have the opus 5 worker carry it out" · **Governs with:** GOAL.md (read first), docs/plan/13 (the fused-twin plan), docs/plan/12 (Monday's demo)

Every card below is a bounded piece of work with its design decisions already taken, a measured definition of done, the commands that verify it, and the things it must not do. Work the cards in order unless a card says it may run in parallel. When a card is done, mark it in section 3 of GOAL.md with the date, the commit and one line of evidence, and append the session log. Do not re-litigate a decision written here; if the code or a measurement contradicts one, stop, write the contradiction down (the Blake Clause), and take the next card.

## REVISED 2026-09-04, late evening: the ladder after two second opinions

Blake asked for a second opinion before any pod time. ChatGPT reviewed the brief (docs/reports/second-opinion-chatgpt-2026-09-04.md, kept verbatim) and three independent critics of my own attacked the plan for return on investment, for 2025-26 state of the art, and for engineering risk. Where at least two of the four agreed, or one proved it in the code, the ladder changes. The sections below this one are the original cards and remain the detailed specification; where this section contradicts them, this section wins.

**What every reviewer agreed on.** The order was wrong: four cards of instruments, transfer and registration before anything a visitor could see, while the one defect Blake named in words (the floor) waited a week. The photographed floor needs no registration and no GPU, so it goes first. An opaque plane under the intact splat is not enough: floor Gaussians in front of it still haze it, so the floor band must be erased (a SplatEditSdf BOX slab, opacity 0, over about y in [-0.10, +0.06] m in the floor footprint) and the patch judged in motion as well as stills, because a polished floor's reflections are view-dependent and a static atlas freezes them. The planner's ladder does not ship on Saturday: Monday is protected by pre-warming the demo laptop's browser cache on Sunday (open the walk and the planner once; the tiles then come from disk), and T-581 moves to Tuesday. Card W7's flags were wrong in the code: in gsplat 1.5.3 only `--steps-scaler` moves MCMC's refinement window and the SH schedule (`adjust_steps`, trainer lines 190-205), so `--max-steps 300000` alone refines for 25k steps and coasts for 275k; the run uses the 30k base with `--steps-scaler`, and the iteration rate is measured before any duration is quoted. The court as written would score pose error, not beauty: the pinhole frames carry a median 3.2 degrees of roll (p90 10.8) and the walk's camera has none, so the court gets a full pose (a quaternion, not yaw and pitch), excludes each judged frame and its neighbours within two seconds from every training set and every bake, and reports raw and colour-fitted scores separately. Masks belong in the loss (the operator, the fisheye rims: 8-58 % of the virtual views are black), not painted black in the images. Training samples by place and source, not by file count: virtual views are 83 % of the image list and swamp a uniform draw. And nothing is paid for until the frame round-trip is proven free: the zone test's PLY on the volume, un-normalised with the saved transform INCLUDING the rotation of the spherical-harmonic coefficients, converted to SOG and mounted in the walk, must land on the vendor build at a court pose.

**The idea ChatGPT missed and two critics found: warm-start from the vendor's own splat.** The served Grand Hall is 6.02 M LiDAR-fused Gaussians already in the frame we train in, with walls, gilding and chandeliers Blake already likes. Training from 2.48 M SfM points throws that away. Decode the finest level (the census script has the decoder; extend it to SH1-3), initialise the trainer from it, and refine with our 41,737 posed photographs. Two more from the same critics: a dense per-view depth term from the vendor splat's own expected depth plus the y = 0 floor plane, which needs no registration and replaces the 10-13 sparse points per image the depth loss had; and gsplat 1.5.3's bilateral grid as the appearance model across passes. Each is an A/B on the zone for about a dollar before it is trusted.

**One more free check ChatGPT found.** LCC Studio 2.3 exports "Creator Data": undistorted PortalCam images, people and equipment masks, COLMAP camera parameters and an aligned LAS cloud, all in the served frame. The installation here is 2.0.0028, so it is one free upgrade away. Blake upgrades and exports the Grand Hall; the agent compares a zone against our package. If the vendor's poses and masks are as good or better, the bridge's uncertainty leaves the programme.

**The ladder, revised (the cards below keep their detail; this is the order and the changes):**

- V0 Protect Monday (Saturday, before 18:00): as W0, minus the Saturday planner push. Add: Sunday, pre-warm the demo laptop (open /room/grand-hall and /plan?space=grand-hall once each; confirm in DevTools that tile rows read "disk cache"). Blake's owner action for the week, minutes and free: the R2 custom domain (Cloudflare dashboard), which lets tiles skip the Vercel proxy that misses its cache on every request.
- V1 The floor patch, offline on a branch this weekend (was W5): the SDF erase slab; a 2 x 2 m patch from the posed XGRIDS pinholes chosen per texel by incidence angle, projected resolution, sharpness and exposure, blended by a robust median with a 5 px feather (never a plain average of misregistered frames); compare 2.5, 5 and 10 mm per texel; judge four candidates at three oblique poses and in a walking clip: vendor floor, plane under intact splats, plane with the slab, and (once V6 registers it) the Matterport mesh region. If the plane-with-slab wins, bake the whole floor; ship behind `?splat=floor:bake`; Blake decides.
- V2 The court, made valid (was W1): the `pose=` parameter carries a quaternion; frames judged are excluded, with their two-second neighbours, from every training split and bake source; raw and colour-fitted scores reported separately; the floor region by the plane mask, walls and ceiling by segmentation where cheap; six stills plus a walking clip; PSNR 50 stays a re-photograph target and is never quoted as a promise.
- V3 Free proofs before any pod hour (new): (a) the round-trip on the zone PLY already on the volume (pull it, ~200 MB, no compute): un-normalise with SH rotation, SOG, mount in the walk, compare at a court pose against the vendor build; (b) LCC Studio 2.3 Creator Data for the Grand Hall against our package on the zone (Blake installs; the agent compares poses by reprojection and masks by eye).
- V4 Delivery, week one (was W6): a lighter first rung (the level-1 tile with its SH1-3 bands stripped, about 3-4 MB); the coarser served level for medium and low tiers (one line per the ladder report) with `?splat=tier:medium` measured; re-chunking to 4 MB only after those, measured against the 15 % bar.
- V5 Training on the H100, proven on the zone first (was W7; the recipe changes): warm-start from the decoded vendor Gaussians (A/B against SfM init at 7k zone steps, ~$1: the warm start must be within 2 dB of the vendor at step 0 or the decoder is wrong); masks in the loss; dense depth from the vendor splat plus the floor plane (A/B against sparse, ~$1); the bilateral grid (A/B, ~$1); a source-balanced curated subset of 1,000-3,000 sharp frames, full-resolution pinholes; `--steps-scaler` on the 30k base, cap 3 M rising to 5 M with `--opacity-reg 0.01 --scale-reg 0.01` if the name-board band asks for it; the iteration rate measured in the first 2,000 steps before the duration is quoted; evaluations on a fixed small held-out set that excludes court frames. Then the whole-hall run, exported through the proven round-trip, judged at the court. Budget as before: $25 a run, two runs without asking.
- V6 Registrations, only when a verdict asks for them (were W3 and W4): Bright Walls when the walls at the court need more evidence; the E57 and the 148 panoramas when the floor or the walls want the Matterport source. Register the served Gaussians' centres, not proxy meshes; accept on a photographic gate (median reprojection of the source's own sparse points into twenty GH_2 pinholes, 1-2 px at evaluation resolution, tails disclosed), not on ICP RMSE alone; verify five panoramas across the room before trusting the sweep-to-file mapping (149 sweeps, 148 files).
- V7 The other rooms, with the recipe that won.

**Corrections to what the programme has been saying.** "No indoor radiance field exceeds 30 dB" is false: the original 3DGS paper reports 30.6 on room, 30.3 on kitchen and 32.0 on bonsai; those are dense orbits of small scenes and do not predict this hall, but the ceiling is not universal. The first run's 0.17 passes is the figure at 7k steps; a completed 30k run would have been 0.72 passes, and the zone test shows trainability without isolating starvation as the only cause (coverage, sampling and the cap changed too). The brief mixed MB and MiB; the reports now say which.

**Pod time.** Nothing starts the pod before V3 is done. The first pod session is V5's zone A/Bs, about an hour.

## ROUND TWO 2026-09-05, 13:10: the protocols after ChatGPT's second reply

Blake sent ChatGPT the six decisions the REVISED section rests on (the brief and the reply are both verbatim in docs/reports/second-opinion-chatgpt-round-two-2026-09-05.md) and pasted the reply at 13:07. Before anything here was written, every claim the reply makes about gsplat 1.5.3 was checked against the installed 1.5.3 package (C:\Users\blake\AppData\Roaming\Python\Python313\site-packages\gsplat) and the pinned examples at tag v1.5.3, by two independent readers plus one for the depth definitions and one for the vendor documents; the verdicts are Part 3 of that report and are cited below as "(checked)". Where this section contradicts the REVISED section above, this section wins; the REVISED section still wins over the original cards further down. The court keeps its standing as an instrument: ChatGPT agreed (its first reply had already asked for six viewpoints, a larger validation set and walking clips), so "court-lite" is closed.

**Two corrections that apply to every card.**

1. A round trip compares the same model before and after each conversion. Comparing our zone model with the vendor's build measures two reconstructions, not the pipeline. Three pairs, each at identical cameras and, as far as possible, identical raster settings: (i) the zone model rendered by the trainer versus its exported, un-normalised PLY rendered by Spark; (ii) that PLY versus its SOG, both in Spark; (iii) the vendor's finest tile versus the same tile decoded and re-encoded by our tools, both in Spark. Each residual has to be explained (the SH basis and its rotation, the raster convention, the camera projection, colour handling) before the pipeline is trusted. The raster convention is already known to move the number: Codex's T-592 lane measured Spark's default antialiased opacity compensation against a classic renderer on the recovered Brush model at 22.9 dB of agreement, 30.1 dB once Spark was set to the classic convention (preBlurAmount 0.3, blurAmount 0, focalAdjustment 1; docs/reports/t592-grand-hall-source-comparison-2026-09-05.md). gsplat's rasteriser is "classic" by default and applies the compensation only under "antialiased" (rendering.py lines 129 and 472 in the installed 1.5.3); the vendor's SOGs declare `antialias: false`; the walk sets no Spark blur property. So every comparison names the convention on both sides, and V5 trains classic unless a measured pair says otherwise.
2. Warm-starting from the vendor's Gaussians changes what "held out" means. A photograph we keep out of our refinement may still have trained the vendor's model. Label such frames "excluded from refinement" (the court, V5's evaluation set) unless their absence from the vendor's training is known; they still judge the product. A truly unseen number needs photographs no stage has used, which only V2's own re-photographs are.

**V3 amended: the free proofs.** (a) The round trip is the three same-model pairs above, not "against the vendor build". (b) Creator Data is compared by matched observations, not by projecting one cloud into the other's frames: point identifiers are model-local, so an arbitrary projected point is not a correspondence. The protocol: match 30 to 50 physical exposures by timestamp and camera identity across the zone and both sensor types, on the original images or one shared rectification; fit one rigid transform on a subset of the matched camera centres and rotations and report the translation and rotation residuals on the rest, with scale checked separately and a few anchors elsewhere in the hall before any whole-hall switch; build a shared set of observed feature tracks, triangulate from designated training views only, project into withheld observations of the same tracks, and report median, p90 and p95 pixel error, track coverage and failure rate, never refitting a test pose to its own observations; inspect the masks on matched people, equipment and thin architecture, converting polarity deliberately (XGRIDS marks invalid pixels white, COLMAP excludes black; checked against both documents); confirm the winner with the same small image set and render budget. Decision rule, declared now: switch to the vendor's inputs if the cross-validated error falls by 20 % without worse tails or lost coverage and the browser comparison agrees; a tie goes to the simpler supported export; a lower self-reprojection residual alone switches nothing. Correction to the paragraph above: XGRIDS documents the exported images, poses and LAS as aligned with each other; it does not promise the old GH_2 served frame, so the transform is fitted and reported, never assumed.

**V4 amended: delivery.** The SH strip is measured, not estimated. The coarse tile 0_0.sog is 7,226,379 bytes; its higher-order SH members are 2,120,476 (centroids) and 701,684 (labels); repacked without them it is 4,397,245 bytes, 39 % smaller (ChatGPT's in-memory repack; the member sizes re-listed here on the staged file). At 20 Mbps that is 1.13 s of ideal transfer saved (2.89 s to 1.76 s), not half the first view. Keep the existing webp members and never re-encode them; drop the shN metadata consistently; a runtime SH-degree cap saves no bytes and only previews the look. Judge full-SH coarse against SH0 coarse through turns and through the hand-over to the fine level: gilding, chandelier highlights, window brightness, colour popping. If the bands' absence reads poorly, the second experiment is an SH0 colour refit against representative full-SH views with geometry and opacity fixed, which still cannot keep view dependence. Coarser served levels for weak tiers are previews and performance controls, not final representations: plan 16's parity requirement (and the goals deck's flag, reduced tiers interim only) means they ship labelled interim, and the parity test at the declared display and zoom is part of the card, alongside visible-region detail selection and less redundant work.

**V5 amended: training, one ingredient at a time.** Warm-starting is a hypothesis that may save a great deal; a six-million import, a renderer conversion and new losses are not yet a recipe, so nothing becomes default before its own A/B.

- 5a The import path, laptop work, own code, test-first. Decode the finest level into gsplat's parameterisation with fresh optimiser states: positions; scales stored as log-scales and opacities as logits, applied once, never twice; the quaternion order; the SH basis and its rotation; the coordinate transform. Never let the SfM initialiser overwrite the imported values. Before any of it runs on the pod, its copy of simple_trainer.py is diffed against tag v1.5.3 (the laptop checkout at C:/Users/blake/gsplat carries only a Windows patch for the fused_ssim and tensorboard imports; the pinned blob is what Part 3 quotes). Three 1.5.3 facts govern the code: the SH degree used at a step is the schedule's, so the imported SH1-3 must be forced active from step 0 or the model renders SH0 (checked); `cap_max` caps additions and never prunes an oversize start, so the run keeps the exact 6,019,684 and caps additions at that count (checked); MCMC injects position noise every step, outside the refinement window, so freezing geometry means disabling the noise or bypassing the strategy callback, not just stopping relocation (checked). The stock `--ckpt` path loads, evaluates and stops, so it is not a resume (checked). The step-0 gate I proposed ("within 2 dB of the vendor or the decoder is wrong") is withdrawn: 2 dB permits 58 % more squared error and equal scores hide different defects. Instead, render the imported model in gsplat and the vendor tile in Spark at identical cameras and convention and explain the difference before any training step.
- 5b SH-only refinement, the first pod experiment (zone, under a dollar): freeze cameras, positions, scales, rotations and opacity; no relocation, no noise; optimise SH alone for 500 to 1,000 updates on the curated subset (next paragraph) at one tenth of the pinned learning rates (SH0 2.5e-4, higher bands 1.25e-5; the pinned values are checked in Part 3); opacity and scale regularisation off; export at 0, 100, 500 and 1,000 and inspect the same regions in Spark under the same convention.
- 5c Only if 5b helps: unfreeze geometry cautiously at lower learning rates for 1,000 to 3,000 updates, still without redistribution; then test redistribution and pruning separately. Remove only confirmed invalid or out-of-room content before the baseline; low opacity alone does not make a splat disposable. Keep the floor-erased variant distinct from the unmodified warm start.
- 5d Depth, three otherwise identical short zone runs: A, RGB plus the existing trustworthy sparse depth; B, A plus the visible-floor plane; C, B plus weak, frozen, confidence-masked vendor depth. The vendor model is a frozen teacher that may stop good geometry from deteriorating; it cannot say which vendor geometry is wrong, so it never replaces the measured sparse depth and is never regenerated from the student. gsplat's expected depth is the alpha-normalised mean of camera Z, not a first surface (checked), so matching the floor's mean depth does not make a thin, sharp floor: keep opacity, depth spread and multi-view consistency where available, and exclude glass, crystal, mirrors, the known-bad floor regions and depth edges. The y = 0 plane is a planarity assumption whose datum came from the vendor slab (T-578), so it is not independent evidence: transform it into the normalised training frame and apply it only to bounded, visible floor, excluding furniture, occlusion and grazing rays. Normalise the measured and teacher losses separately; a teacher coefficient one tenth of the sparse one is the first trial once their units are comparable. Judge on exported image quality and retained measured depth, not agreement with the teacher. The sparse-depth flag reads per-image points, not dense maps (checked): add a masked path, or sample valid teacher pixels explicitly, keeping them distinct from measured samples, and re-check the external-depth indexing and scaling issues round one raised before reusing that adapter. Nobody precomputes 41,737 depth maps for this.
- 5e The bilateral grid, A/B with `app_opt` off so the export stays ordinary SH Gaussians: identical initialisation, images, sampling order and budget, grid off against on; judge the raw SOG in Spark and in motion, because the per-image grid does not export and any gain must show in the plain splat; the corrected-training and cc_* scores are diagnostics, not what a visitor sees; keep a declared reference appearance and watch for the scene and the grid trading arbitrary colour shifts. The checkpoint does not serialise the grid (checked), so save grid and optimiser states explicitly if continuation matters. `app_opt` is a different thing: its stock PLY export bakes the network's appearance into DC colour and drops the higher bands (checked). PPISP and per-image embeddings wait until this comparison leaves a problem.
- Order: 5a, 5b, 5c, 5d, 5e, then the whole-hall run through the proven round trip, judged at the court. The budget lines stand ($25 a run, two runs without asking; nothing starts the pod before V3).

**The first selection (new; feeds V5 and the court).** From 7,178 pinholes and 34,559 virtual views, target 2,400 images as soft quotas: 1,800 pinholes and about 600 virtual views, a 75:25 mix against the list's 17:83, a testable choice rather than an optimum. Build the exclusion set first: every sensor image and derived crop of a judged exposure plus its two-second neighbourhood, and near-duplicate poses beyond that where the rig paused or revisited; fix the validation set before any result is seen. Normalise sharpness within each physical camera and each virtual-crop family (raw Laplacian values do not compare across optics and resolutions); reject corrupt, badly masked or blurred redundant views, but keep uniquely informative coverage for explicit review rather than silently opening holes. Bin camera centres into 0.75 m cubes and, within each, viewing direction into eight 45-degree azimuth sectors and three elevation bands (below -30 degrees, -30 to +30, above +30). Fill the pinhole quota by rounds over occupied bins, sharpest eligible first, suppressing candidates within about 15 cm and 8 degrees of a chosen comparable view; where two sensors see the same region prefer the better image and keep both only where coverage differs. Fill the virtual quota from the gaps the pinholes leave (the dome, downward floor coverage, directions the forward cameras miss), normally at most two crops per original fisheye before another position, never five near-identical derivatives because they are separate files. Inspect a coverage contact sheet and a pose-and-direction map, explicitly checking the floor, both long walls, both ends, the dome, the chandeliers and the lettering; relax the virtual quota toward 800 to 900 only for missing coverage, taking the budget from redundant pinholes, and keep fewer than 600 if they add nothing. During training sample by source class, then by occupied bin, never uniformly over files, and avoid drawing siblings of one exposure together. Write a manifest with id, parent id, split, bin and selection reason. If the sparse model can serve as proxy geometry, add visible-surface coverage as a refinement: positions, directions and sharpness alone cannot certify that every important surface is seen. Compare the 2,400 selection with one alternative mix at equal elapsed compute before expanding it. The tool lives beside the package tooling (tools/xgrids-xbag), test-first, and its output goes under D:\claude\colmap-gh\hall-t502\selections\.

**The sequence, as ChatGPT recommends and the ladder adopts.** V1 the floor patch, with V3(a)'s same-model pairs alongside (both free); V3(b) Creator Data and the selection (laptop); V5a then V5b, the SH-only refinement (the first pod hour); then 5d and 5e as separate comparisons. The pod stays stopped until V3 is done; 5a and the selection are laptop work.

## 0. The protocol for every session

1. Read CLAUDE.md, .claude/AI_INTEGRITY_RULES.md, GOAL.md, then this file. Say which card you are taking and what its done-when is.
2. Test first. Typecheck, tests and lint before any commit. `git commit -- <explicit paths>`; never stage everything. Commit messages say what changed and why.
3. Measure before claiming. Load times come from `window.__roomWalk` (firstView, complete) via the instrument at D:\claude\fused-twin-2026-09-04\ladder-load.mjs or the harness packages/web/scripts/splat-drag-budget.mjs; never from screenshots (they stall the GPU during a load) and never from Playwright's response event (it fires on headers). The embedded Browser pane cannot stream splats; use Playwright from packages/web. Never edit packages/web source while a harness or e2e run is in progress.
4. **The freeze.** Nothing is pushed to master from Saturday 2026-09-05 18:00 BST until Monday 2026-09-07 18:00 BST, and no `railway up` at any point without Blake. During the freeze, work on a branch or on offline tooling and merge on Tuesday.
5. **Money.** The RunPod H100 (pod trmciz4jo6yf6m, $3.49/h) starts only for a card that names it, and is stopped at the end of every session that started it (`POST /pods/<id>/stop` through the REST API with the key in C:\Users\blake\deploy-secrets\runpod-api-key.txt; the start/stop scripts of 2026-09-04 in D:\claude\colmap-gh are the pattern). Log every start and stop with the reason in D:\claude\colmap-gh\autostart.log. Cap: $60 across this ladder without asking Blake; a single training run is capped at $25.
6. **Escalate, do not guess**, when: a change to production is needed inside the freeze; any spend beyond the caps; a registration will not converge to the acceptance below; the court says a candidate is worse than the vendor build; anything touches the production database; a card's inputs are not where this file says they are.
7. Secrets stay in C:\Users\blake\deploy-secrets and packages/api/.env and are never printed. Bulk output goes under D:\claude\<card>\, never C:. Python wants C:/ paths; tsc needs NODE_OPTIONS=--max-old-space-size=8192; repo files are often CRLF (line-ending-safe anchors); pnpm needs C:\Users\blake\AppData\Roaming\npm on PATH; a Node script outside packages/web imports Playwright by absolute file URL.
8. Write to Blake the way he asked: the answer first, plain prose, short unless the length is earned.

## 1. Decided by the Fable session, not to be re-derived

- The blur on the floor is content (794,351 translucent 2 cm discs), so the floor needs another source; no renderer setting fixes it.
- Every XGRIDS level is the whole room at one density; serving is one level plus the sky, never a stack; the coarsest level is the first view.
- A Spark mesh cannot load hidden and be revealed (unsorted blobs); opacity 0 is the same trap. One SparkRenderer host per scene, on a mount of its own.
- The first training run starved (0.17 passes over 41,737 images); the package's geometry is right; the MCMC noise theory was refuted by the zone test. Train normalised, save `Parser.transform`, invert on export, at least seven passes over the images, `--disable-video`, a held-out set of a few hundred views.
- PSNR 50 is a fixed-viewpoint re-photograph target, not a novel-view number; the court (docs/plan/13 §6) is the judge of every beauty claim.
- Bright Walls is training evidence once registered, not a second splat to blend. Panoramas enter as pinhole crops, never as panoramas.
- The room's floor is the served Gaussians' densest slab (T-578, done); the manifest is regenerated by `lcc2 stage` then `lcc2 lod`.

## 2. The cards

### W0 · Protect Monday (Saturday, finished before 18:00 BST)

**Why.** Everything shipped on 2026-09-04 (the ladder, the datum, supersampling, the wheel, the poller, the hung-tile fix) is on production untested by a second pair of eyes, and the demo is Monday.

**Do.**
(a) Resume the verification workflow that the session restart killed: `Workflow({scriptPath: "C:\Users\blake\.claude\projects\C--Users-blake-omnitwin2\dbedc34c-b506-4496-9e76-a176257a072b\workflows\scripts\ladder-verification-sweep-wf_3e54f1d1-c96.js", resumeFromRunId: "wf_3e54f1d1-c96"})`. Cached agents replay; read every verdict. If the Workflow tool is unavailable, do its four checks by hand: the ladder on every walkable room at 20 Mbps, the invisible-load audit of every SparkSplatLayer consumer, the planner's walk toggle, and the adversarial review of RoomSplatScene's delivery state machine.
(b) T-580 (/living-hall and /fresh draw the Reception Room three times: keep one level in RECEPTION_TILE_MANIFEST) is user-reachable; fix it test-first if it is under an hour.
(c) T-581 (the planner and the captures console never got the ladder) decides itself by measurement: run the planner on the Grand Hall at 20 Mbps (`/plan?space=grand-hall`, the Act 2 route) and time the first splat pixels from the page. If they take longer than 15 s, give CockpitSplatLayer the ladder on Saturday, test-first, following RoomSplatScene's delivery state machine; if not, it waits for Tuesday. Either way the planner keeps exactly one renderer host.
(d) The demo smoke: a script `packages/web/scripts/demo-smoke.mjs` that, against https://venviewer.com, checks the homepage answers 200 and carries "Walk the whole building", the Grand Hall walk reaches `__roomWalk.firstView` inside 15 s at an emulated 20 Mbps and `complete` without page errors, `/plan?space=grand-hall` creates a public draft (the URL becomes `/plan/<code>`), `/venues/trades-hall/twin` renders a viewpoint, and the API's health route answers. It prints one line per check and exits non-zero on any failure. Run it Saturday, Sunday and Monday at 08:00 BST (a `/loop` or a scheduled run) and push-notify Blake only on failure.
(e) Any blocker or major from (a) is fixed test-first and deployed before 18:00 Saturday; anything smaller is a task row for Tuesday.

**Done when.** The workflow's verdicts are recorded in docs/sessions/2026-09-05.md with what was fixed and what was deferred; the smoke script exists, passes against production, and has run on Saturday morning; CI's four gating jobs are green on the last Saturday push.

**Forbidden.** Pushing after 18:00 Saturday. Touching the Diary, bookings or production data (Saturday's rehearsal is Blake's, signed in).

---

### W1 · The court: the fixed-viewpoint measurement that judges every beauty claim (Saturday to Sunday, offline, may run alongside W0)

**Why.** Nothing about "beautiful" can be decided without it: the fused-twin plan (13 §6) makes the court the gate for the floor bake, the training runs and the per-room rollout, and "PSNR 50" only means anything here.

**Design (decided).**
- Ground truth is a real photograph from a known pose: the undistorted XGRIDS pinhole frames in D:\claude\colmap-gh\hall\images with poses from the refined model D:\claude\colmap-gh\hall\sparse\4-final (read with pycolmap; the SLAM frame in metres). Six viewpoints: the pinhole frame nearest the walk spawn, and five more spread over the walk box at roughly eye height facing along the long axis and toward each side wall, chosen by their poses, preferring camera_3 (the sharper pinhole) and frames the package kept (not blur-dropped).
- The render is the live viewer at exactly that pose. Add a DEV-and-court-only URL parameter to the room walk, `?pose=x,y,z,yaw,pitch` in scene metres and radians, parsed in RoomWalkPage and passed to InteriorCamera as the spawn (test-first: a unit test that the parameter overrides the bundle's spawn and is ignored when malformed; it must not change the spawn for anyone who does not pass it). A pose in the SLAM frame maps to the scene frame with the manifest transform (rotation [-π/2, 0, 0], then position; the census script tools/xgrids-lcc2/scripts/sog-floor-census.py shows the arithmetic).
- Render at the frame's own intrinsics: the walk's FOV is 48° vertical at 1600×900; the pinhole frame's vertical FOV comes from its focal length (fy ≈ 1931 px on 3000 px height ≈ 75.6°). Rather than change the viewer's FOV, crop the ground-truth frame to the viewer's FOV around its principal point and resample it to 1600×900; document the crop.
- Screenshot only after `complete` (never during a load), at DPR 1 with the settled profile, then metrics in Python: PSNR and SSIM (scikit-image) and LPIPS (the `lpips` package, AlexNet, pinned), over the whole frame and over three regions (floor band, wall band, ceiling band) so the floor's failure shows on its own. Output D:\claude\court\<candidate>\<viewpoint>.json and a side-by-side PNG (photo | render | abs-diff).
- Candidates are named: the vendor build is `gh2-vendor`; later `gh2-floorbake`, `fused-run1`, and so on. The tool takes a candidate name and a base URL so a preview deployment or a local dev server can be judged.

**Do.** The `pose=` parameter (tests first, on a branch during the freeze, merged Tuesday); `tools/court/` (Python: viewpoint selection, ground-truth crop, metrics, side-by-sides) and `packages/web/scripts/court-render.mjs` (Playwright: open the walk at each pose, wait for complete, screenshot); a README with the exact commands; the baseline run for `gh2-vendor` on the local dev server (the tiles are identical to production, verified 2026-09-04); a report docs/reports/court-baseline-2026-09-06.md with the six rows and the region numbers.

**Done when.** Six viewpoints have photo, render, diff and numbers for `gh2-vendor`; the report states the baseline honestly (expect whole-frame PSNR in the low 20s and the floor band the worst); re-running the tool reproduces the numbers to within 0.1 dB (rendering was measured deterministic to a handful of pixels).

**Forbidden.** Using the spawn instead of the photographed pose; screenshots during a load; any change to the walk's behaviour for visitors without the parameter.

---

### W2 · The sources land on the pod (after the R2 upload completes; one pod hour, cap $5)

**Why.** The Trades Hall upload (bucket r2f:venviewer-foundry-sources, prefixes raw/panoramas-8k, raw/panoramas-8k-output, raw/matterpak, raw/panoramas-small, derived, vendor/lcc2-grand-hall-variations, vendor/splat-outputs, raw/matterport/cloud_0.e57, raw/xgrids-captures; about 157 GB) removes the 7.5 Mbps home uplink from every later card: the pod pulls from R2 at datacenter speed.

**Do.** Confirm the upload is complete (the watch log D:\claude\colmap-gh\r2-tradeshall-watch.log plateaus near 157 GB; the kit's own UPLOAD-LOG.txt on the drive says STEP 3 finished with failures 0 when Blake brings the drive home). Start the pod; install rclone on the pod if absent; configure it from C:\Users\blake\deploy-secrets\r2-foundry-sources.txt without printing the token; `rclone copy` each prefix to /workspace/omnitwin/sources/<prefix> with `--size-only --transfers 8`; then `rclone check --size-only` for every prefix and record the counts and bytes; stop the pod. Write docs/operations/r2-ingest-2026-09-0N.md with the per-prefix counts, bytes, minutes and cost, and an entry in state/capture_log.json if its schema fits (read it first; do not invent a schema).

**Done when.** Every prefix checks clean on the volume, the pod is EXITED, the record is written.

**Forbidden.** Leaving the pod running after the copy; printing the token; starting any training.

---

### W3 · Bright Walls registered to the served frame (local, no pod; Sunday to Monday, may run alongside W1)

**Why.** Bright Walls (vendor build "Grand_Hall_Small", 4.98 M splats, D:\venviewer data\Gaussian splat outputs from remote pc\scan_output_{1,2,3}_GH_SMALL, mesh Grand_Hall_Small.obj) is a second pass over the same hall in its own SLAM frame. Registered, it is more evidence for the fused training and an A/B for the walls; unregistered it is nothing.

**Design (decided).** Open3D in Python (pip install open3d). Source: the Bright Walls mesh vertices. Target: the GH_2 mesh vertices (C:\GRAND_HALL_BIG_MODEL_VARIATIONS\scans_BIG_MODEL_TH_GH_2\mesh-files\Grand_Hall.obj) cropped with the Grand Hall roomCropM from tools/xgrids-lcc2/src/capture-sources.ts. Voxel downsample both at 5 cm; FPFH features; RANSAC global registration; refine with point-to-plane ICP at 2 cm then 1 cm. Accept when ICP fitness ≥ 0.6 and inlier RMSE ≤ 3 cm; otherwise try the three GH_SMALL variants and report the best; if none passes, escalate with the numbers.

**Do.** `tools/registration/register_meshes.py` (arguments: source, target, crop, out) writing D:\claude\registrations\brightwalls-to-gh2.json ({T_target_from_source 4x4, fitness, rmse, voxel sizes, date, inputs with sha256}) and a top-view overlay PNG (target grey, transformed source in colour) plus a side-elevation overlay; a unit test on synthetic data (a box cloud, a known rigid transform recovered to 1 mm / 0.1°). Record the result in docs/reports/registrations-2026-09-0N.md.

**Done when.** The JSON exists with fitness and RMSE inside acceptance, the overlays show the two halls coincident to the eye, the synthetic test is green.

---

### W4 · The Matterport E57 registered to the served frame, and its 49 sweep poses with it (local or pod; after W3, same tool)

**Why.** The E57 (F:\E57\cloud_0.e57, 20.5 GB, hall = sweeps 0-48; also raw/matterport/cloud_0.e57 in R2 after W2) carries the LiDAR floor and the tripod positions of the 148 8K panoramas (F:\downloads (some very important)\TH Panoramic, 8192×4096; Blake: "a splat using only the panoramic images ... gave us clearer walls"). Registering the E57 to GH_2 puts every panorama's tripod position into the served frame, which is what the floor bake and the fused training need.

**Design (decided).** Read sweeps 0-48 with pye57 (pip), keep every 10th point, register with the same tool as W3 (source: E57 cloud; target: GH_2 mesh vertices cropped). Then write the 49 sweep origins transformed into the scene frame to D:\claude\registrations\matterport-sweeps-in-gh2.json. Verify on one panorama by eye: choose a sweep, render the viewer at its transformed position at eye height facing the fireplace, and put the corresponding 8K panorama's forward cube face beside it; the fireplace and the name boards must sit within a few percent of the frame width of each other.

**Done when.** Registration inside acceptance, the 49 origins written, the one-panorama check passed and pictured in the registrations report.

---

### W5 · The floor from photographs, behind a flag (after W1 and W3; Tuesday onward; the first beauty change)

**Why.** Blake: the Matterport mesh "had much clearer view of the floor". The floor is the worst region in the court's baseline and it is content, so the fix is a photographed floor under the room, judged at the court, shipped behind a flag until it wins.

**Design (decided).**
- Build (Python, tools/floorbake/): the floor plane is scene y = 0 after T-578. Orthomosaic at 5 mm/px over the walk footprint (about 2,020 × 3,980 px for 10.1 × 19.9 m) by projecting the undistorted XGRIDS pinhole frames (D:\claude\colmap-gh\hall\images with poses from sparse/4-final, mapped into the scene frame) onto the plane. Per output pixel choose the source frame with the smallest incidence angle among those within 4 m, take its colour, and blend seams with a 5 px feather; leave pixels no frame sees transparent. Write the PNG and a JSON with the plane's placement (origin, size, pixel pitch) to D:\claude\floorbake\gh2-v1\. Panoramas join only after W4 (their tripod height sees the floor at a kinder angle); keep that as v2.
- Runtime (web, test-first): a `FloorBake` component that mounts a three.js PlaneGeometry with MeshBasicMaterial from the atlas at the plane's placement, rendered before the splat layer (Spark's splat material is depthTest true / depthWrite false, so the atlas occludes the floor Gaussians beneath it and nothing else). Behind `?splat=floor:bake` on the room walk; off by default; the atlas is staged next to the room's tiles under `floor/` and described in the manifest by a new optional `floor` descriptor (generated types extended, staging tool extended, tests).
- Judge at the court: candidate `gh2-floorbake`; the floor band's PSNR/SSIM/LPIPS and Laplacian sharpness against `gh2-vendor`, whole frame unchanged elsewhere. Also the harness: frame rate unchanged within 5 %, bytes added stated.

**Done when.** The atlas exists and is pictured; the flag shows it in the walk; the court's floor band improves on every viewpoint and the whole frame does not regress; the numbers are in docs/reports/floorbake-2026-09-NN.md. The flag stays off in production until Blake looks and decides.

---

### W6 · Delivery levers, measured (Tuesday onward, parallel with W5)

(a) **Re-chunk tiles to at most 4 MB** so Spark's four-worker pool streams instead of arriving in three waves (measured cause of the 13-17 s blank before the ladder). Extend `lcc2 stage` to split each finest-level tile into ≤ 4 MB parts if the SOG container allows it, else split at staging by re-encoding with Spark's tooling (D:\claude\spark-src); the manifest lists the parts; the viewer mounts parts as tiles; publish to R2 with packages/api/src/scripts/publish-splat-tiles.ts. Measure at 20 Mbps before and after with the instrument: done when time-to-complete falls by 15 % or more with first view unchanged and fps unchanged; if not, revert and write down why.
(b) **A coarser sharp layer for weak tiers.** The profile's medium and low tiers get finest-1 and finest-2 as their sharp level (the levels are complete rooms); measure with `?splat=tier:medium` on the 4090 (budget behaviour only; a real mid laptop remains Blake's to supply) and record the bytes and first view per tier. Test-first in splat-runtime-profile.ts and room-splat-bundles.ts.
(c) **T-583**: CI's e2e job dies at its own 30-minute timeout, so it gates nothing. Measure the suite's duration locally, shard it across two or three jobs in .github/workflows/ci.yml, and confirm a green E2E run on master.

---

### W7 · The fused training, run 1 (after W1, W3, W4; pod, cap $25 per run, two runs without asking)

**Why.** This is the "super-combined-something-new": one training over the XGRIDS frames, judged at the court against the vendor build.

**Design (decided).** gsplat 1.5.3 on the pod's venv (the 2026-09-04 scripts in D:\claude\colmap-gh: diag-sweep.sh for PATH, the JIT warm and the detached launch pattern). Dataset: the T-502 package at /workspace/omnitwin/hall-t502 (41,737 images; if the pod lost it, rebuild from R2's raw/xgrids-captures with tools/xgrids-xbag, or copy from D:\claude\colmap-gh\hall-t502 — 102 GB, only if R2 has it). Flags: `mcmc --data-factor 2 --sh-degree 3 --antialiased --strategy.cap-max 3000000 --depth-loss --depth-lambda 0.02 --disable-video --test-every 200 --max-steps 300000 --eval-steps 100000 200000 300000 --save-steps 300000 --ply-steps 300000 --save-ply` and NO `--no-normalize-world-space`. Before launch, a 30-line Python step instantiates gsplat's `Parser` on the dataset with normalize=True and writes its `transform` (4×4) to the run directory as transform.json; after export, apply its inverse to the PLY (means, scales, rotations) to return to the SLAM frame, then to the scene frame with the manifest transform. Run 1 is XGRIDS-only (the package as it is); run 2 adds the Bright Walls frames once W3's transform places them (extract with tools/xgrids-xbag, keyframe 1 in 4, package with the same tool, merge the COLMAP models by the registration). At 45 it/s, 300k steps is under two hours; evaluations at 200 views take about two minutes each.

**Do.** Launch, watch the held-out PSNR at 100k/200k/300k (write each to state/training_runs.jsonl as the first rows, defining the shape: run id, dataset, flags, steps, held-out metrics, cost, artefacts), export the PLY, un-normalise, convert to SOG per tier (Spark's tooling or @playcanvas/splat-transform pinned), stage as candidate `fused-run1` behind `?twin=fused` on a Vercel preview deployment (not production), and judge at the court against `gh2-vendor`. Stop the pod.

**Done when.** state/training_runs.jsonl has the run; the court has `fused-run1` with six viewpoints; docs/reports/fused-run1-2026-09-NN.md states where it beats the vendor build and where it loses; Blake has a preview URL and the decision is his.

---

### W8 · The other seven rooms

Only after W5 or W7 has produced a Grand Hall that Blake prefers at the court. Then, per room in the order of docs/plan/12's demo value (Reception Room, Saloon, then the galleries): the court's viewpoints, the floor bake, and — where a room has its own XGRIDS raw capture in R2 — the training. Write the per-room recipe once, as tools/court/README.md's second half, and keep every number in one table in docs/reports/rooms-court-2026-09.md.

## 3. Standing rows to keep honest

T-576 (the Monday demo) closes on Monday evening with what Elaine saw and what she asked for. T-579 (the ladder) is done; T-580, T-581, T-583 are in W0 and W6. Open a task row for every card when you start it (T-584 onward), and close it with the evidence line.
