# Grand Hall route arrival

The old route fallback showed a generic box, implementation wording and an operational-review chip while JavaScript loaded. The new fallback gives an explicitly requested Grand Hall a full-screen composition using the existing venue image, labelled as event-styling inspiration. Other or unknown rooms use a neutral background without claiming a Grand Hall identity. The shared Activity indicator is the only animation. The page keeps a home/venue link, has one polite work status, uses no synthetic progress, and disappears when Suspense resolves without waiting for the image or a timer.

No new image was generated or edited. The existing `grand-hall-room-1120.webp` and its 480px derivative are already in the public venue image ladder. The 1120px file is 228,806 bytes. The separate `walk-poster-1120.webp` was inspected and rejected for this use because it shows another room. The styling photograph is not presented as the currently saved furniture arrangement, an interactive rendering or reconstruction evidence.

Verification: 13 tests across the new route and existing Activity suites pass, including explicit/unknown room identity, optional image failure with escape/status preservation, no invented progress and immediate handover after a deferred route resolves. Web/E2E TypeScript and changed-file lint pass. The initial test lint failure for an async callback without await was corrected and retained in the tool receipt. Independent source review found no material routing, ownership or accessibility issue.

Actual visible browser port5208 was reloaded with cache disabled, 120ms network latency and 256,000 bytes/second download throttling. `D:/claude/reference-viewer-20260906/arrival-route-slow-initial.jpg` shows the actual pending route at the current 1016×571 CSS viewport. It was shown to Blake as an iteration, not accepted design. Once the route loaded, the arrival component disappeared and the actual planner mounted. This development-server exercise is not a production load-speed benchmark.

The in-app browser's device emulation retains its host zoom/backing scale. Requested 390px initially produced 309 CSS pixels, below the app's existing 320px body minimum; that capture is retained as a failed target setup. Correcting the requested dimensions produced measured390×844 CSS with scrollWidth 390 and DPR1.26. Under emulated reduced motion, all sampled shared particles had `animation-name:none`. Its captured image was cropped by the browser's emulation/backing-scale mismatch; `arrival-route-narrow-final.png` is not a successful full mobile visual receipt despite its filename. `arrival-route-narrow-final.json` preserves the measured DOM result. A proper full mobile visual recheck remains required. Cache, network, media and device overrides were reset and the browser was closed before further edits.

The full mobile visual recheck subsequently passed using the already installed Playwright Chromium in a fresh context. `D:/claude/reference-viewer-20260906/arrival-chromium-mobile.png` is an inspected complete 390×844 image: scrollWidth 390, one polite status, readable heading/footer and reduced-motion particles with no animation. The corresponding desktop image is 1672×941. The harness held the actual editor module request to inspect Suspense, then released it and observed immediate handover; it did not add a wait to application code. The mobile image was shown to Blake. This is desktop browser responsive evidence, not physical-phone qualification.

The same harness's later reflection A/B readiness assertion was inadequate: its recorded images had only 408,192 furniture triangles and no painted capture, despite a transient resolved store value. Its raw `passed` flag does not qualify a full-scene comparison. The failed readiness evidence and SwiftShader renderer identity are preserved; later visible in-app-browser comparisons inspect actual painted geometry instead.

Combined candidate `b0477037` passes 76 targeted tests, four CPU source tests, full web/E2E types, changed-file lint and a 29.48s test-mode build. Its actual integrated browser traversal also displayed both explicit Grand Hall and generic workspace arrivals. The route image does not solve capture-streaming time, photographic reconstruction, furniture materials or founder aesthetic acceptance. It adds no artificial wait and does not block an already usable workspace during refresh.
