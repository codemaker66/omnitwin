# Hallkeeper saved furniture footprints — 6 September 2026

The hallkeeper web page passed only operational manifest rows into its SVG. The demo's 18 table-and-chair bundles therefore produced 18 position dots, although the saved `floorPlan` contained all 162 furniture objects and the approved PDF already rendered them.

The page now passes that saved field to the renderer. The existing runtime schema validates it; every saved catalogue box or cylinder is drawn using its recorded dimensions, scale and Three Y rotation. The saved polygon and all furniture share one uniform projection, including off-room objects. Current room dimensions, live catalogue assets and inferred chair rings do not alter this drawing. These are catalogue footprints, not photographs or surveyed collision results.

Explicit manifest position IDs retain row highlighting and click/keyboard navigation. One object may link to multiple phase rows; an already-highlighted row remains the click destination. Bundled chairs have no recorded chair-to-row association in this contract, so they remain visible without inventing that link. Missing or null floor-plan fields preserve legacy markers. A saved empty layout stays empty; malformed saved geometry displays an unavailable state rather than substituting plausible markers.

Four new behavior regressions failed before the change. Afterward, **26 tests across three focused suites passed**, covering the 18-table/144-chair case, the actual HallkeeperPage fetch boundary with no writes, explicit and multiple-phase row links, legacy/empty/invalid behavior, actual Three transform comparisons, non-centred nonrectangular outlines and retained off-room furniture. Web and E2E TypeScript, changed-file ESLint and `git diff --check` passed. The local Vite test-mode build passed (3,404 modules). The final test-only lint cleanup was rechecked with its seven-case component suite.

Independent source review found no material blocker; it did not repeat tests or exercise a browser. Work is isolated at `D:/claude/venviewer-hallkeeper-footprints-20260906`, from `beb1492cb6fa46e42be1e56a5e79e1dae39dbcee`. Its unchanged types package was built privately so runtime schema exports match source; no shared installed dependency or original asset was changed. Root owns actual approved-sheet browser qualification. No browser, production write, deployment or photographic/performance acceptance is claimed here.
