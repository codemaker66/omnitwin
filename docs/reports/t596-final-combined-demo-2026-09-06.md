# Final combined local demo qualification — 6 September 2026

Application source: `b04770376069bdb76bffd202a4c51556ee9c160a`, branch
`codex/t599-reference-integration`, in
`D:/claude/venviewer-t599-reference-integration-20260906`.
The application combines the reference viewer, frozen-layout/approval fixes,
event-linked bootstrap, one-gesture Undo, captured tour, shared route arrival and
optional panorama lighting/reflections with T599's correctness/dependency work.
It is local and has not been published or accepted aesthetically.

## Source and service checks

The private dependency install avoids the main and earlier candidate's junctions.
Its base passed 5,463 web tests, 2,807 API tests with 34 skips, 13 explicit PostgreSQL
atomicity cases, and all workspace types/lint/builds. The final frontend additions
passed 76 targeted tests, four CPU source tests, web/E2E types, changed-file lint
and a 29.48-second test-mode build. Backend source did not change after its full
qualification. Builds used the CI placeholder publishable key and do not qualify
real-account sign-in or a production artifact.

The coordinator closed the earlier browser before switching the three owned
loopback Vites and controlled API to this private source. DB 54339, bridge 54331 and
demo passthrough 5204 remained unchanged. The API retains its synthetic test
identity, isolated database and blocked external delivery. No migration, reseed,
production write or draft clearing occurred. Exact PIDs, commands, startup
provenance and HTTP checks are in
`D:/claude/venviewer-final-demo-20260906/switch-after.json` and `README.md`.

## Actual combined browser

The visible in-app Chromium tab exercised the following against the switched
application. Its source stayed frozen until the browser closed.

- The linked event offers its four actual saved configurations. Selecting the
  approved dinner opens its saved 162-object plan.
- The frozen dinner shows 162 footprints against its frozen room outline, without
  current capture or fog. The explicit room-flip gap has no geometry. Exiting
  restores the live room and the exact arrival camera position/quaternion.
- The hallkeeper sheet renders 162 footprints and 18 table-to-manifest links. One
  actual link highlights its matching footprint; all six operation checkboxes
  remain false. The event operations page shows the correct event, phases and
  nine pending tasks; no task was falsely completed.
- The public saved layout remains revision 10 with 162 objects. The previously
  recoverable anonymous draft remains dirty. Model/Capture and 2D/3D transitions
  preserve all editor objects byte-for-byte; 2D counts 144 actual chairs and 18
  tables. A fresh 3D renderer reaches 12,469,192 painted triangles with capture
  progress hidden, at a 2560×1440 native buffer.
- The optional environment is withdrawn in Model and recreated with a new UUID
  in Capture and the replacement 3D renderer. This does not accept its appearance.

Evidence is `D:/claude/venviewer-final-demo-20260906/browser-*`. The collected tab
error/warning log is empty. The proxy logs contain 48 GETs and no non-read request:
40 local API responses (39 HTTP 200, one expected absent-mission 404), and eight
anonymous public-demo responses (three 200, five protected schedule/runtime 401).
These expected responses remain recorded. The browser was closed after checking
the final furnished scene; no app source was edited during inspection.

## Remaining delivery work

The subsequent interactive fallback found and repaired Capture floor placement
and covered seating-dialog controls; see the [actual placement/save/reopen
report](t596-capture-placement-2026-09-06.md). Release packaging is prepared
separately. This bounded traversal does not qualify a complete real-account
enquiry/review/email journey. The intended owner identity was repaired earlier,
but the new local build does not use production Clerk credentials. The current
founder release instruction now authorizes the narrow T601 combined release for
the confirmed Monday 7 September, 16:00 BST demo after its gates. The task Build
next-gen venue platform is the sole production release owner. This earlier
bounded local traversal alone does not satisfy those release gates.

The existing pale floor, proxy furniture, unaccepted registration and photographic
reconstruction remain quality gaps. The direct offline input validator passed,
but Foundry admission, calibration, executable training and paid-run readiness
are distinct. Physical-device 60fps, photographic PSNR50+ and founder sublime
acceptance remain open. No new paid run or deployment occurred.
