# Frozen phase scene and integrated viewer qualification — 6 September 2026

The actual browser rehearsal showed today's captured room and live clearance overlays behind historical phases and even behind a room-flip gap. This change draws the immutable room polygon and furniture in their shared canonical coordinate frame, withdraws the current capture and editing overlays, and restores the live interior camera on exit. Current decoded splats stay mounted under a hidden ancestor; Spark's installed renderer uses visible traversal, so returning does not reload them. Frozen preview uses its own floor and full-height boundary without current ornament, wall visibility, section clipping or atmospheric fog.

The frozen camera introduced by `99be4373` measures visible dock rectangles, fits the exact polygon and height, cancels queued live navigation, and restores lens/pose/target. Independent review caught and corrected bottom-caption classification and live owner changes during suspension. Expanded reference timeline raises both preview panels and fixed actions; its caption sits above the timeline. Review status during preview is explicitly labelled as the saved plan's status, not approval of the displayed historical phase.

## Evidence

Before the final fog/status copy changes, the actual visible in-app browser on port5206 opened four authenticated linked layout choices, then the saved dinner configuration, arrival/ceremony furniture, dinner and the room-flip gap. No public draft creation was attempted. Source diagnostic confirmed capture ancestor hidden and frozen room present during dinner; the gap had no frozen room or visible captured source. Exit restored exactly position `[0.4,1.6,7.5]`, original quaternion, FOV55 and no projection offset. Expanded panels/actions cleared the timeline. Saved fixture data was not mutated.

This browser's measured viewport was1016x571 at DPR2.51999998 with a2560x1440 framebuffer, not the earlier1672x941/DPR1 lighting comparison. Evidence: `D:/claude/reference-viewer-20260906/frozen-dinner-corrected-2053.jpg`, `frozen-gap-corrected-2054.jpg`. The canonical fixture already has a centred outline, so its actual furniture offset iszero; the asymmetric lower-left-origin regression independently verifies the required nonzero offset and avoids a mirrored floor.

The same browser exposed a cold-idle tour failure: the first demand-loop delta consumed the entire tour duration and the live Walk handoff briefly restored an old orbit pose. The failed16second trace contains3818rAFsamples but only8distinct render-counter values and two brief camera changes; it is NOT an FPS benchmark. `captured-tour-observation-2056.json` is retained. Separate fix `97903033` now starts timing at the first rendered tour frame and prevents the orbit flash; browser requalification is pending.

Integration initially failed nine hallkeeper regressions because this private web package still resolved an old shared types build in main. Its local dependency junction was preserved under `types-main-prior-20260906` and replaced with this private checkout's unchanged types package; the current private build exports the frozen floor-plan schema. No main dependency or source was changed. All three local Vite servers were restarted with fresh dependency optimization. After correction154tests across10integrated suites, web/E2E types and test-mode build passed (3408modules,33.53seconds). The later pointer-remap and cold-idle fixes receive their own integrated checks and browser evidence; the count here does not qualify their later additions.

## Limits

The floor/boundary view represents the saved planning geometry, not a reconstructed photographic historical room. Source-aligned immutable capture resolution, photographic overhead fidelity, source registration, PSNR50+, furnished physical-device60fps and founder aesthetic acceptance remain open. The initial integrated tour trace failed and is not reused as performance evidence. Local synthetic owner authentication, disabled external delivery and read-only browser requests do not qualify the complete live sign-in/send/review journey. Nothing was deployed and no paid compute was started.
