# Frozen phase scene and integrated viewer qualification — 6 September 2026

The actual browser rehearsal showed today's captured room and live clearance overlays behind historical phases and even behind a room-flip gap. This change draws the immutable room polygon and furniture in their shared canonical coordinate frame, withdraws the current capture and editing overlays, and restores the live interior camera on exit. Current decoded splats stay mounted under a hidden ancestor; Spark's installed renderer uses visible traversal, so returning does not reload them. Frozen preview uses its own floor and full-height boundary without current ornament, wall visibility, section clipping or atmospheric fog.

The frozen camera introduced by `99be4373` measures visible dock rectangles, fits the exact polygon and height, cancels queued live navigation, and restores lens/pose/target. Independent review caught and corrected bottom-caption classification and live owner changes during suspension. Expanded reference timeline raises both preview panels and fixed actions; its caption sits above the timeline. Review status during preview is explicitly labelled as the saved plan's status, not approval of the displayed historical phase.

## Evidence

Before the final fog/status copy changes, the actual visible in-app browser on port5206 opened four authenticated linked layout choices, then the saved dinner configuration, arrival/ceremony furniture, dinner and the room-flip gap. No public draft creation was attempted. Source diagnostic confirmed capture ancestor hidden and frozen room present during dinner; the gap had no frozen room or visible captured source. Exit restored exactly position `[0.4,1.6,7.5]`, original quaternion, FOV55 and no projection offset. Expanded panels/actions cleared the timeline. Saved fixture data was not mutated.

This browser's measured viewport was1016x571 at DPR2.51999998 with a2560x1440 framebuffer, not the earlier1672x941/DPR1 lighting comparison. Evidence: `D:/claude/reference-viewer-20260906/frozen-dinner-corrected-2053.jpg`, `frozen-gap-corrected-2054.jpg`. The canonical fixture already has a centred outline, so its actual furniture offset iszero; the asymmetric lower-left-origin regression independently verifies the required nonzero offset and avoids a mirrored floor.

The same browser exposed a cold-idle tour failure: the first demand-loop delta consumed the entire tour duration and the live Walk handoff briefly restored an old orbit pose. The failed16second trace contains3818rAFsamples but only8distinct render-counter values and two brief camera changes; it is NOT an FPS benchmark. `captured-tour-observation-2056.json` is retained. Separate fix `97903033` now starts timing at the first rendered tour frame and prevents the orbit flash; browser requalification is pending.

Integration initially failed nine hallkeeper regressions because this private web package still resolved an old shared types build in main. Its local dependency junction was preserved under `types-main-prior-20260906` and replaced with this private checkout's unchanged types package; the current private build exports the frozen floor-plan schema. No main dependency or source was changed. All three local Vite servers were restarted with fresh dependency optimization. After correction154tests across10integrated suites, web/E2E types and test-mode build passed (3408modules,33.53seconds). The later pointer-remap and cold-idle fixes receive their own integrated checks and browser evidence; the count here does not qualify their later additions.

## Final browser qualification at `2e55e209`

The visible in-app browser rechecked the frozen candidate on 6 September, finishing at 20:21:30 UTC. The actual Diary booking → Open the plan link now presents all four linked configurations. Dinner shows the exact frozen room and 162 objects with fog absent and “Saved plan: Approved”; the room-flip gap hides both current capture and frozen geometry. Exit restores the original interior pose, quaternion, FOV55 and null projection offset. At the measured 1016×571 CSS viewport, DPR2.51999998 and native2560×1440 buffer, the expanded timeline and caption remain clear of the side controls. The full-height preview is necessarily small in that available space and is not the requested photographic overhead presentation.

The cold-idle tour now passes the actual motion check. Its 18-second trace records 1,317 camera-changing rendered observations over 11,949.8ms, with median8.3ms and p95 16.7ms between those observations. Camera x remains0.4m, y1.6m and z stays within2.5–7.5m; no old orbit pose appears, and the final position/quaternion exactly match arrival. This is approximately110 observed changing rendered frames/second on NVIDIA RTX4090 through ANGLE/D3D11, Chromium152, with18tables/144chairs and a native2560×1440 buffer. A local reconstruction validator concurrently consumed about half a CPU core on average and retained about8.65GB private memory. These observations do not prove physical presentation cadence, cold load speed, a statistically stable performance benchmark, or office/phone performance. The earlier failed trace is preserved.

The actual approved hallkeeper SVG contains162 independent saved footprints and18 table links. Clicking one table highlights its real manifest row; clicking the six-table row highlights six table footprints. The144 independent chair footprints are not guessed interactive bundle links. The approval fixture and API were read-only throughout this browser traversal.

On the existing recoverable public draft at5208, an actual3D pointer gesture with pauses moved exactly9objects in one group by the same delta;153objects remained unchanged. History changed0→1, then one actual Undo restored all162 editor objects, all placement objects and selection byte-for-byte, with history0 and one Redo. The draft was already dirty and remains dirty; this is not a clean server reopen or successful save. Both attempted autosaves received the wrapper's deliberate405 before forwarding; visible error text now includes “This comparison opens the saved demo read-only.” The copy has an extra full stop, a minor presentation follow-up. This existing-ID browser check does not separately exercise an in-flight INSERT acknowledgement; that race has source regression coverage.

Evidence under `D:/claude/reference-viewer-20260906/`: `frozen-dinner-final-2120.jpg`, `frozen-gap-final-2120.json`, `captured-tour-active-final.jpg`, `captured-tour-final-{trace,summary,hardware}.json`, `hallkeeper-final-footprints.{json,jpg}`, `diary-plan-corridor-final.json`, `drag-final-{before,moved,undo}-full.json`, `drag-final-summary.json`, `drag-final-readable-failure.jpg` and both request logs. The `2120` filenames are labels, not authoritative capture timestamps; JSON/provider timestamps establish timing. Final E2E TypeScript and test-mode build passed on unchanged2e55e209, build28.32s with no reported errors/warnings. The later integrated pointer/tour source batch passed121tests and web types. All root browser tabs were closed before subsequent edits.

## Remaining limits

The floor/boundary view represents saved planning geometry. Source-aligned immutable capture resolution, photographic overhead fidelity, source registration, PSNR50+, furnished physical-device60fps and founder aesthetic acceptance remain open. Orbiting above the current captured room exposes rough scan roof surfaces; this is a retained visual failure, not an accepted overhead view. Local synthetic owner authentication, disabled external delivery and read-only browser requests do not qualify the complete live sign-in/send/review journey. Nothing was deployed and no paid compute was started.
