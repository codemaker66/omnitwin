# Session log — 2026-07-14

## T-508 · Derivative V1 live verifier and authority-none output bundle

- Added journaled migration `0056_foundry_derivative_execution_barrier.sql`.
  Legacy V0 now rejects every geometry stage and every immutable worker binding
  classified `deterministic_transformation` at execution, attempt,
  submit/checkpoint preparation, command creation, claim and invocation.
  Poll, reconcile, stop and forensic completion remain available for containment.
- Added strict V1 contracts and journaled migration
  `0057_foundry_derivative_execution_candidates.sql`. Append-only authenticated
  registry attestations and revocations bind the exact accepted 0055 review,
  approval and custody evidence. Atomic one-time candidate reservations bind the
  exact base V0 subject, singleton worker/stage/asset binding, complete ordered
  restriction lineage and quarantine-only output policy.
- Added platform-admin attestation, revocation and candidate-reservation service
  paths with strict public inputs and readback validation. Every candidate is
  permanently `authority='none'`, `execution_eligible=false`,
  `dispatch_enabled=false`; its reservation receipt explicitly records
  `executionActivationRecorded=false`.
- The independent audit's V0 alias/wrapper downgrade was closed with
  geometry-wide classification plus positive immutable operation-class checks.
  The database-clock chronology gap and consumption-style vocabulary were also
  corrected. Final re-audit found no P0/P1 regression.
- Fresh PostgreSQL 16.14 replayed all 56 ordered SQL migrations in individual
  transactions. Live probes denied exact normalization, geometry alias/wrapper,
  malformed geometry and unrelated-payload jobs bound to an immutable
  `deterministic_transformation` worker; the unrelated control remained allowed.
  All six 0056 and twelve 0057 triggers were enabled, and the inspected runtime
  and candidate tables contained zero rows after rollback-only probing.
- The hardened live 0057 verifier replayed the complete successful source graph
  against the pinned `linux/amd64` PostgreSQL image
  `postgres@sha256:57c72fd2a128e416c7fcc499958864df5301e940bca0a56f58fddf30ffc07777`
  on the local Windows `npipe://` daemon, with deterministic final-postmaster
  readiness. Seven races were observed waiting through both `pg_stat_activity`
  and `pg_locks` before the controlling transaction was released: double
  reservation ended with one commit and one `23505`; candidate-first
  attestation revocation, policy revocation and policy-generation replacement
  each allowed both historical records to commit; the three authority-first
  orders rejected the candidate with `23514`.
- The verifier also recomputed strict attestation-revocation request and
  material digest parity, asserted all twelve runtime side-effect tables stayed empty, and
  checked cleanup. Its independent final P0/P1/P2 re-audit was clean. Fixture
  SHA-256 is
  `dbdd676451bc3350a82cb53adf057f543f0fd34dba002a4d3404162098c6f28a`;
  verifier SHA-256 is
  `862f2ecc5a8a1172cb999575f4b63dc3d90ab65a2810ecd4dd93509a0e099a15`.
- Final verification passed: focused API 12 files / 110 tests; focused V1 types
  10/10; complete shared types 90 files / 2,074 tests; complete API 130 files /
  2,477 tests; API and types typechecks and builds; targeted ESLint;
  SQL/TypeScript digest parity; and a clean stale consumption-vocabulary scan
  over the V1 boundary.
- Migration SHA-256 values are
  `3075ba5895283dd6a15407e4aa3edb44073fe7125a69a541d125579efef7a78d`
  for 0056 and
  `10fc023060ecd1228421243272d584dcb1b2bd8bd277622d9f66c5cc27ba1c6e`
  for 0057. Frozen 0053–0055 hashes remained unchanged.
- Added strict authority-none derivative-normalization invocation, report and
  artifact-index contracts plus a test-only bundle writer. The exact output set
  is `normalized.glb`, `normalization-report.json` and `artifact-index.json`, with
  the index written last by the reviewed code path. The bundle binds the complete
  candidate and reservation, base subject, worker/stage/asset binding and ordered
  restriction lineage; its public verifier independently rereads every byte and
  reruns the GLB normalization proof. Activation is absent, every authority check
  is false, and release, signing, publication, redistribution, runtime promotion
  and measured-geometry flags remain false. The production entrypoint fails
  before accessing options; the test writer is not exported from the package root.
- Output-bundle QA was clean through P0–P3, with 27/27 focused tests and the full
  Reconstruction Foundry suite at 197 passed / 1 skipped. SHA-256 values are
  contract `0b244a1f0c9a72e98c2e2150637fa0d071f4ed6a81b1e5cd3c44b57fdba9067f`,
  bundle `53182c0841fe4753bb2aa1eca9cc6bec7b64db9b7bf353033e894c73dd3133f4`,
  tests `c7794cf2f9068d14eefe94bdba953e98e003a4a68b6642a25725ac2d31b13884`
  and root index `5f5f067a4c4d232f82c510c3418a57608acad01dd651fe1d9a5d2e911b8364b6`.
- The index-last/fsync chronology is code-reviewed, not independently
  artifact-proven. Local same-user custody, Windows sandboxing, immutable-store
  custody, authenticated executor identity and live-authority-at-output-commit
  remain unresolved, so this is not sealed production custody or activation.
  The repaired activation contract at SHA-256
  `8442babfb2a636c508016d4bdaca169b70b94b2c8f650d7cb6f44b52d3149788`
  received independent GO only to implement a generation-1-disabled 0058-A
  substrate at that checkpoint; the later implementation finding recorded
  below invalidated that GO. Enabled authority and 0059 remain NO-GO. T-508
  remains in progress. No execution activation, runtime
  execution, provider call, production database apply, object-store write,
  signing, publication or promotion occurred.

## T-508 · Second-amended activation contract and disabled 0058-A draft

- The first activation-contract re-audit initially cleared inert 0058 design,
  then disposable implementation invalidated that GO: its trailing-slash
  reservation prefix could not equal an insertable 0053 JobSpec prefix because
  unchanged `foundry_is_safe_relative_path` rejects the terminal empty segment.
- The second-amended contract keeps 0053 untouched and uses one safe-relative
  no-leading/no-trailing-slash job/reservation prefix with object key
  `output_prefix || "/normalized.glb"`. The exact amended artifact is 69,012
  bytes, 1,162 lines, SHA-256
  `1f593314fd45850950afe168548ac1eefebabea2e0754ee05101cc21c4371659`.
  A separate reviewer found no new design P0–P2 and cleared continued inert
  implementation only; the catalog-test P3 requiring `pg_policy` and
  `pg_attribute` coverage remains.
- Produced a bounded disabled migration draft,
  `0058_foundry_derivative_activation_disabled.sql`, 262,281 bytes / 5,789
  lines, SHA-256
  `1655b8ff5022377f28f7ef1f73aa5ca0e75e0ddc14218339b341b81a33c5506e`.
  That exact hash replayed every journaled migration 0000–0058 transactionally
  on pinned networkless PostgreSQL 16.14 and left exactly generation 1,
  `disabled_sentinel`, enabled false. Frozen 0053–0057 hashes remained unchanged.
- Replay success was not accepted as implementation completion. The final
  non-author audit reported P0 none and P1 findings: caller-constructible GLB/
  worker-manifest custody facts without authenticated runner/verifier evidence;
  absent closed `SECURITY DEFINER` mutation/claim/redemption/containment/custody
  APIs while direct DML is revoked; containment that is not source-scoped and
  reverse-closed; and custody reuse of a dispatch/pricing authority horizon.
  The author conservatively treated the first two trust-boundary gaps as P0;
  both classifications produce the same NO-GO.
- No focused 0058 semantic/adversarial suite, `pg_policy`/`pg_attribute`
  catalog coverage, Drizzle schema parity or activation service was added.
  Independent verdict: GO only for the inert disabled-sentinel installation;
  spec-complete 0058-A and 0059 remain NO-GO. No execution, provider call,
  credential use, production DB, object-store mutation, spend, signing,
  publication or promotion occurred.

## T-509 · Authority-none full-factor provider recommendation

- Added a deterministic recommendation request/result contract and compiler
  over the exact PlanOnly request and dossier. Every candidate records input
  size, RAM, VRAM, expected duration, privacy, queue state, required software/
  worker-image compatibility, cost and operator preference.
- Missing, future-observed, expired, incompatible, unknown, over-capacity,
  PlanOnly-blocked or digest-conflicting evidence is a hard eligibility block.
  The explicit soft-criterion priority uses integer/fixed-point values; an exact
  tie returns `no_recommendation` rather than falling back to a route name.
- The first independent audit returned NO-GO after reproducing a self-consistent,
  re-digested candidate-cost substitution that changed the winner. The repaired
  validator now derives local/remote economics, budget, storage profile and the
  canonical plan-only JobSpec from the exact bound request; ranking uses that
  request-derived cost.
- A separate final reviewer replayed the original winner-changing attack and
  independently varied route, budget, stage, output prefix and storage. All
  remained PlanOnly-schema-valid after re-digesting but were rejected at the
  recommendation boundary. Final P0–P3 verdict: clean GO for recommendation-
  only V0 and explicit NO-GO for execution or activation authority.
- Verification passed 18/18 focused tests; the full Reconstruction Foundry
  suite passed 215 with one existing skip; typecheck, ESLint and build passed.
  Frozen SHA-256 values are module
  `f30095cb40df138c52d99f479c8a0e22ce01f9a6b5898b6ee76295a5589b1747`,
  tests `886a4050e60050d353fcee5c196d3f569e61e43ee57546eb1fcf7c2d94c25586`,
  specification
  `6b81074afa0d36692fa939eddc0c4a7f6b0f0a18ef63cf76b7681038d0856e30`
  and root index
  `95b35dc7fedbf3106c57abd05dfb4012e7dc8761975fcde5b1ec82e0a6966cf0`.
- The result is always `authority='none'`; execution, dispatch, provider
  invocation, network, object-storage mutation, spend, signing, publication and
  promotion capabilities are literal false. Evidence is unauthenticated caller
  input, nothing is reserved, and manifest-dependent input/rights facts are not
  independently recompiled because the manifest is digest-bound but not
  embedded. No external or paid action occurred.

## T-500 · Native E57 computer-vision direction and geometry audit

- Confirmed a complete native six-JPEG rig for E57 scans 122–144: 23 stations,
  138 embedded 4096×4096 images. The source remained read-only at 20,518,437,888
  bytes, SHA-256
  `975039d11fc04ca681f038e499f358124bbcab178ad5ce6324fa912212729cdd`.
- The point-colour reprojection audit passed all 138 images with one fixed,
  proper, non-mirrored mapping. NCC minimum/median/maximum was
  0.959714/0.984797/0.997269 and the lead over the next adequately covered
  direction was 0.104339/0.571526/0.798487. This is strong internal colour
  consistency, not independent geometry, because point colour may derive from
  the same JPEGs.
- Added a colour-blind, same-E57 cross-modality geometry-edge audit that requests
  only XYZ plus organized row/column fields (`colors=False`, `intensity=False`). It tests 48 independently
  rasterized rotations/mirrorings and gives every candidate the same ±4-pixel
  local shifts. The 240-translation exact-location result is diagnostic-only and
  cannot approve continuous calibration or veto discrete orientation.
- Preserved the failed v1 development report unchanged. Review found that exact
  pixel location and cube direction had been conflated and that the old 24-cell
  count did not measure spatial distribution. Version 2 openly records the
  post-development change, uses 12 cells / 3 rows / 3 columns / all 4 quadrants,
  and keeps the exact-location calculation as `UNIQUE`, `NONUNIQUE` or
  `UNASSESSABLE` only.
- The revised v2 method passed 42/42 development images across scans
  122,124,126,130,134,140,144. The implementation, tests, thresholds, source,
  reports, exact 16-scan split and output name were recorded in an unsigned,
  authority-none protocol that declares they were frozen before held-out access.
  Those receipts do not authenticate the actor or chronology, prove unique
  execution, or exclude earlier access. The full Reception audit suite passed
  108/108 tests, and an independent design review found no P0/P1/P2 issue.
- The recorded report declares one exact held-out command run over scans
  123,125,127,128,129,131,132,133,135,136,137,138,139,141,142,143: 96 images.
  The overall result was `REJECT_GEOMETRY_MISMATCH`: 82 passes, 6 direct direction
  mismatches, 4 ambiguous margins and 4 insufficient distributed-coverage
  results. The fixed mapping ranked first on 90/96 images before and after equal
  local shifts. Only scans 125,129,133,135,143 passed all six faces.
- Seven of the 14 non-passes were downward-looking `Skybox 5`, including four of
  the six mismatches. A later predeclared circular-region overlay test contained
  zero geometry-edge pixels in all seven failures and six matched controls, so
  its inside rates were null and it neither confirmed nor rejected the blurred
  tripod/nadir-patch hypothesis. Other failures span Skybox 1–4, so the issue is
  not limited to the downward face.
- An independent read-only result audit recomputed both digests, all 96 face
  decisions, station and overall aggregation, ranks, margins, candidate lists,
  coverage summaries, receipts and frozen implementation hashes. It found no
  inconsistency and approved the report as a valid negative result—not the
  mapping or training workflow.
- Produced an authority-none exploratory package for the 14 non-passing faces
  and 14 deterministic same-face controls (27 unique faces): 27 exact native
  JPEG copies plus 27 generated diagnostic PNG overlays. Thirteen paired
  unmatched-edge deltas were positive and one was negative; mean delta was
  `0.177608746714`. This selected comparison is descriptive, not independent or
  causal. The predeclared Skybox-5 circle contained zero geometry-edge pixels
  in all seven failures and six controls, so its region result was null.
- Added a separate create-only private pair-sheet exporter that pins the exact
  E57, frozen protocol and held-back report, then reconstructs only the 44
  already-selected masks needed for 14 failure-versus-pass boards. It read 13
  scans and 22 photos with `colors=False` and `intensity=False`; it did not call
  the full audit, recompute ranks, change thresholds or alter a frozen decision.
  The 19-file bundle manifest payload SHA-256 is
  `26b6e44992a79fd484410156fa4b2158ca37c06ba81b48c044769d9c259b44e1`.
  Its venue pixels remain local, private and unpublished.
- Computer-vision measurement and side-by-side review classified all 14 cases:
  four lack geometry-edge support in one image quarter; seven downward views
  combine repeating floorboards with a large soft central area; and three are
  dominated by repeated translucent curtain folds. The soft centre also
  appears in passing controls, so its source cannot be identified and it is not
  sufficient by itself. No case supports changing the fixed mapping or a
  coherent global calibration correction. The frozen negative remains
  unchanged. The detailed record is
  `docs/reports/reception-room-e57-geometry-edge-visual-diagnosis-2026-07-14.md`.
- Pair-sheet exporter SHA-256 is
  `616a1447acacfa2324039c4a5aeb5ec2f18d4caa464e5d9f0fc27fefcd80d86d`;
  test SHA-256 is
  `4f3e9468c8303509d75fe15ed2a634bee51a27daaad1ae2da8665d9f2f962476`.
  Focused exporter tests passed 35/35. A fresh full Reception HD run completed
  160 tests: 159 passed and one Windows privilege-dependent symlink test was
  skipped; direct reparse-attribute coverage passed.
- Added a separate stdlib-only post-render verifier. It pins the protocol,
  manifest, held-out report and renderer identities; enforces the exact 55-file
  set; rehashes all retained artifacts; and rechecks source links, dimensions,
  records, statuses, pairs and arithmetic. A fresh run returned
  `PASS_SEALED_PACKAGE_INTEGRITY`, payload
  `dc7f4655b6baf69914a8efbd8cff53d3d836b5623a35780261fc47d01faca1e5`;
  verifier SHA-256 is
  `36b262a4a3427608be22cd50138e9cb28d70acf7eb5b758166a67fbc3e465895`.
  The v1 masks were not retained, the source E57 was descriptor-bound rather
  than rehashed, and the unsigned receipts do not prove creator, time,
  immutability or unique execution. The hardened verifier additionally rejects
  lexical reparse paths and performs two full package snapshots plus final
  re-enumeration; it still cannot prevent post-return swaps or authenticate
  itself. Focused verifier tests passed 10 with one privilege-dependent Windows
  symlink test skipped; the combined renderer/verifier slice passed 17 with
  that one skip.
- A separate post-hardening read-only audit matched the verifier and test hashes,
  reran the 10-pass/1-skipped verifier suite and the CLI, reproduced the payload
  plus artifact/directory receipts, and found no P0–P3 issue in the scoped
  package-integrity claim. The skipped Windows symlink-creation case remains an
  explicit privilege limitation; direct reparse-attribute coverage passed.
- Recorded whole-file SHA-256 values:
  - failed v1 development:
    `d8307d8547ba2bce44f87a3173497a83762f98c994e13af272d95c21a24f941a`;
  - passing v2 development:
    `96a9fd87a9a78a68b4ebe3f699f313e1f56d985ce40856532198b40a29435389`;
  - frozen v2 protocol:
    `7212244f38a4678cd3e3b60a491c6b2154390d253d9eaa22e0255e16e8cd78d9`;
  - failed one-shot v2 held-out report:
    `ed744eba259c5a40c886af65d8fccf54c36a60ee2e5890449dee0c23f50794a0`.
- No training, known-pose materialization, continuous or metric calibration,
  source mutation, upload, paid compute, publication or rights approval occurred.
  T-500 remains in progress. The 14-case post-hoc visual diagnosis is complete.
  Still required are full-resolution privacy review with explicit person,
  tripod and nadir masks, Reception room/crop confirmation, genuinely fresh
  independent geometry controls under an authenticated protocol, and written
  E57 processing/derivative/training rights. The local exact-package delivery
  step described below is complete; the live Stage-0 A/B still requires an
  approved migration/deployment and two private registrations.

## T-500 · Private exact-package Stage-0 delivery path

- Added a signed-in platform-admin page at
  `/admin/runtime-package-previews/<PACKAGE_UUID>/view`. It requests exactly the
  UUID in the address and never falls back to a different private, public or
  development package. If the exact package fails, the page says so, shows the
  normal photograph and states that nothing was substituted.
- Added ordered visual-member receipts to the immutable package manifest. Each
  receipt binds the member ID, filename, extension, size, byte SHA-256 and a
  SHA-256 of its server-only storage identity. The API recalculates the package
  digest and checks current database rows on every read.
- Every private byte request repeats platform-admin authorization and exact
  membership checks. The API reads the entire protected R2 object into a
  bounded verifier and checks its SHA-256 before returning any byte to Spark.
  The browser receives no R2 key, storage URL, signed URL or credential.
- Resource use is bounded to 16 MiB per member and four retained verified
  transfers, with one preallocated buffer per transfer and a 60-second upstream
  deadline. Browser close, route change, unmount and Spark parse failure cancel
  the relevant work.
- The public latest-package, public room-visual and public runtime-byte routes
  remain `published`-only. A seeded route-level regression proves that an
  `internal_ready` package is unavailable from all three.
- The private selector accepts two separate exact profiles. Quality pins four
  SOG leaves and Mobile pins four SPZ leaves. Both profiles require the ordered
  asset IDs, filenames, extensions, byte sizes, SHA-256 values, hashed storage
  identities and complete hierarchy basis; correct filenames alone are not
  enough.
- Added a separate create-only Mobile preflight. A real read-only run checked
  the actual `Reception Room Mobile.lcc2`, validated all declared SPZ
  containers, reproduced authoritative receipt
  `c897dd55fd8efc5397a76d96572a654058defd232f10767b1827fe684e7b6357`,
  checked all seven existing asset records in the configured database and
  produced payload digest
  `9d35c8cf339e618e68349d637199f9200019dd4fcabee6ffb6be72172f88dc93`.
  Both Quality and Mobile preflights now perform a read-only check for migration
  0052's three columns, four constraints and four safety triggers. The configured
  database has none of them, so the payloads validate but both reports say
  `databaseReady: false` and block any create request.
  It made no write and did not download protected R2 bytes.
- Focused verification passed 53 shared-type, 127 API and 62 web tests, all
  relevant type-checks and targeted lint. The final full shared-types run passed
  2,078 tests across 90 files and the final full web run passed 3,121 across 260
  files. The final full API run passed 2,508 individual tests across 127 files;
  six tests failed across six other files because their fixed migration-journal
  positions do not yet include existing migration 0058. The API build passed.
  The web production build correctly stopped because no live Clerk publishable
  key was supplied; no safety gate or credential requirement was bypassed.
  Independent reviews found three low-priority issues and all were corrected.
- A read-only migration-tail check found nine pending migrations (`0050`–
  `0058`), including required immutable-revision migration 0052, and returned
  `safeToApplyProduction: false`. Do not run either preflight with `--apply` and
  do not cherry-pick 0052.
- No package registration, upload, deployment, public-pointer change, signed-in
  live stream, raw-source mutation, training or paid compute occurred. The
  exact next step is a separate review and authorization of the complete safe
  migration/deployment plan. After deployment, rerun both dry runs, register
  Quality and Mobile as separate `internal_ready` candidates, and compare their
  exact private URLs on the same devices, native DPR, Spark settings and saved
  camera stations. T-500 remains in progress.

## T-514 · Config B local contract preflight

- Repaired and tested the Config B checker; no training was run or enabled.
- The lightweight command now provides help and an authority-none CPU
  preflight without importing the upstream trainer or contacting a provider.
- The checker enforces the exact frozen Config B values, compiles their audited
  target argv and records the exact gsplat 1.5.3 archive, licence, API and
  source-closure identities. Runtime acceptance of that argv remains unproved.
- A deterministic synthetic fixture checks two pinhole cameras, three source
  images, exact factor-2 runtime images, two training images, one held-out image
  and two training-only sparse-depth priors.
- Negative tests reject altered configuration, unsafe source locks,
  unsupported cameras, invalid images, wrong split rules, held-out leakage,
  bad depth data, invalid ICP results, cross-dataset paths, empty projections
  and attempts to reuse an output.
- The final focused suite passed 74/74. Two fresh same-host runs produced
  byte-identical authority-none receipts with decision
  `contract_valid_runtime_blocked`. The source closure is not vendored or
  verified, and the runtime remains blocked.
- The E57 depth projector matches the pinned pye57 and pycolmap object shapes
  in focused tests, selects training views only, requires a rigid finite ICP
  result and writes create-only outputs. It has not completed a real
  E57/Open3D/RunPod end-to-end run.
- Distorted-camera training is not proved: the checked external-depth path
  accepts only `SIMPLE_PINHOLE` and `PINHOLE`. External depth is not yet wired
  into the training loss.
- The SSIM fallback matches the expected callable interface in focused tests;
  numerical and performance equivalence with the fused CUDA package is not
  claimed.
- `execute` and the legacy RunPod runner still fail closed with exit 78. No
  provider, GPU, training, upload, object-store, signing, registration or
  release action occurred.
- T-514 is complete as a non-training contract proof. T-502 remains not started
  behind T-501, T-505 and the real runtime, evaluation and D-014 requirements.

## T-515 · Reception E57 ↔ XGRIDS computer-vision preflight

- Added the read-only, fail-closed geometry preflight and diagnostic path. It
  pins the exact 20.5 GB/149-scan staged E57, Quality SH3 XGRIDS PLY and pose
  file, rejects the unrelated nine-scan lobby and freezes separate fit,
  validation and untouched test scan roles.
- The real unrestricted fit found a genuine structural match but failed the
  proposed bar: combined RMSE 421.8 mm, p95 646.9 mm and minimum directional
  50 mm overlap 9.85%. A separately optimized mirror scored slightly better.
- T-515 completed as an authority-none negative diagnostic. It created no
  transform, approval, runtime change, training action or publication.

## T-516 · Upright Reception computer-vision overlay

- Added a +Z-up, yaw-only, fixed-scale candidate and exactly eight private
  geometry views: full context, cropped top, two sides, oblique, both distance
  directions and four-candidate comparison. Scan 122 is boundary context only;
  test-scan geometry 126/129/141 remains unread and unscored.
- The upright fit rotates 167.6884°. Its broad outline is consistent with the
  same room, but this is not formal identity proof. In the diagnostic crop,
  RMSE is 319.6/334.5 mm, p95 is 668.0/826.0 mm and only 14.47%/18.36% is
  within 50 mm. A mirror still wins the reverse-direction comparison.
- Rebuilt the package as V2 after independent review found crowded labels on
  the two widest views. V2 keeps one-metre grid lines, uses readable 5/10 m
  labels and leaves all metrics, transforms, scan roles, crop and sampling
  unchanged. Six of eight PNGs remain byte-identical to V1.
- Canonical manifest SHA-256:
  `d0b9a1e5199c4ea565ea94210c7db98b7b3b1a2447c911c7c3d2b156e392ffc9`;
  internal receipt:
  `9aed42dd70c9783e45723c0b612bba22c22a87a0e7400f0b270a55a7e1b1bcaf`.
- Verification passed: focused 20/20, full Reception 222 tests with two
  environment-dependent Windows symlink skips, compilation, diff check,
  external manifest verification and independent all-view review with no
  remaining P0–P3 issue.
- T-516 is done. T-505 is blocked on a reviewed crop/identity, independent
  physical controls or calibration, rights and an untouched one-shot test;
  T-502, runtime use, registration and publication remain blocked.

## T-507/T-486 · Current gate and offline-package revalidation

- Re-read the complete controlling attachment (28,777 bytes, SHA-256
  `a0dbe2ef903f9e43aa2cf53d771e8cd7aa71c813af6821dc4b019f4ef3d3a4ac`)
  and re-audited the full objective rather than treating the activation-contract
  slice as the whole programme.
- The original T-507 13-file output is currently available and byte-identical
  to the copy in the T-486 r2 dossier. Its output-index file SHA-256 is
  `5ce95707ed8111df82e32fd7997cbcf862bec2974cf544734f5098cfe0540773`.
  This corrects the continuation handoff's earlier availability statement; it
  does not rerun T-507 or upgrade any evidence class.
- The T-486 r2 offline preflight is exactly 22 files: 21 indexed artifacts plus
  `package-manifest.json`. All indexed sizes and SHA-256 values match. The
  manifest SHA-256 is
  `3fba49c89207b6b78fbba067436c6a2a993efada113c9c0267cb625ea06203fd`
  and the independently recomputed canonical package digest is
  `d2411e3f5a0ab3206c1dc174c71005130e5cd26df6369ddd2ec6e10b5eb9a85b`.
  Schema validation and negative parses against the online T-486 review,
  evidence-registration, TransformArtifact and signing inputs pass. The
  package remains `offline_unsigned_preflight`, authority `none`, evidence
  review blocked, public approval unavailable offline and signing not ready.
- Independent surveyed control remains absent: zero fit controls and zero blind
  checks exist versus the requested minimum eight and six. The 5.757 mm frozen
  holdout is same-lineage self-consistency, not survey accuracy. The proposed
  COLMAP-to-E57 transform is also not load-bearing for the already E57-native
  149-node release.
- The internal decision B remains digest-bound but covers only sweeps
  000/010/020/040 and excludes 049. It is not an authenticated venue-authority
  attestation and does not classify all 0–48 candidates or all 149 release
  nodes. The later bare `b` supplied in this thread was not accepted as a new
  attestation.
- Current primary-source licence review confirms that permissive application
  code does not clear model/data/source rights. Exact status was refreshed for
  COLMAP/GLOMAP, gsplat, Splat Analyzer, MeshCoder, Mip-Splatting, Three
  Meshlets, NeuWorld and XGRIDS. Matterport contract/order use, venue-reference
  imagery and XGRIDS remain written-decision gates. MeshCoder's released
  weights/data, Mip-Splatting code, the Three Meshlets demo assets, NeuWorld
  placeholder material and XGRIDS payload/SDK binaries remain ineligible for a
  production package under current evidence. Independent audit found no P0–P2
  factual defect in the refreshed matrix.
- No surveyed-accuracy, rights, identity, transform-review, evidence
  registration, signing, publication or promotion gate was claimed closed.

## T-508 · Test-only local GLB end-to-end conformance

- Added a build-excluded fixture and a cross-package integration starting with
  a real universal intake receipt, explicit authority-none admission review and
  verified local stage for one bounded GLB.
- The integration genuinely exercises `executeNextFoundryProviderCommand` and
  the existing local CPU adapter. Only inside the injected test backend does it
  call the existing test-only lossless normalizer and authority-none output
  writer. Terminal poll uses the public verifier over exactly
  `normalized.glb`, `normalization-report.json` and `artifact-index.json`.
- Exact execution/attempt/fence, staging/manifest/admission, stage,
  worker-profile, source size/hash, sandbox and output-prefix bindings are
  checked. Replaying the same durable marker returns the same output without a
  second write. Pre-backend cancellation produces no output. Wrong staging
  digest, worker profile or fence, post-stage source drift, and tampering of
  each of the three output artifacts are rejected.
- The report remains authority `none`; activation is absent; all release,
  redistribution, registration, signing, publication, promotion and measured-
  geometry capabilities are false. Test-only functions remain absent from the
  package root and excluded from production builds.
- Verification passed: conformance 10/10; adjacent local-adapter/executor slice
  71/71; normalization/staging/output focus 38/38; complete Reconstruction
  Foundry 215 passed with one expected Windows symlink skip; Reconstruction
  Foundry and API typechecks, targeted lint and both builds passed. A separate
  adversarial P0–P3 review was clean.
- File SHA-256 values: conformance
  `c298a04c765d855252750120e7360462a6e69a747aab79a71ef891b7030e8d22`;
  fixture
  `c7fce6e45f7588e354cb303ad52b42ca19b484f73f4b42eebdb48906b783c368`;
  refactored output test
  `76e2dc3d8bc6c51a91a0cb883e3e711524e50e984d16057d54feb661bf591e8a`.
- This is not a production execution proof. The store and sandbox backend are
  in-memory test harnesses; no live database admission/activation, OS sandbox,
  provider, object store, signing, registration or publication occurred.
  Production normalizer/output entrypoints and generation 1 remain disabled.
