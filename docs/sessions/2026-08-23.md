# Session log — 2026-08-23

## T-541 · Grand Hall visual super-pipeline vertical slice

The owner directed the project to continue from T-540 using the newer export
set at `C:\GRAND_HALL_BIG_MODEL_VARIATIONS`. The current scope remains one
individual room: the Trades Hall Grand Hall. The exterior facade, neighbouring
rooms, and the multi-room building layouts shown in earlier concepts are not
part of this room runtime.

The owner stated that the project owns the XGRIDS capture, captured it, and has
written authority to combine and disseminate it with proprietary, open-source,
generative, Matterport, and other provider technologies for this project. This
session log records that instruction; it is not a substitute for a separately
registered rights-evidence artifact or capture-session identity.

The architectural correction remains binding: Grand Hall pixels and structural
behavior must come from supplied data. Windows, doors, openings, wall geometry,
floor regions, and dark central-floor areas must not be invented to resemble a
concept image. Generative enhancement is deferred until deterministic source
lineage establishes where any fidelity loss occurs.

T-541's local vertical slice is **complete** after the corrective camera,
depth-sort, lineage, full-regression, and project-record audit. It does not
close T-540 or T-091, and it does not promote any runtime package, transform,
structural mesh, diagnostic benchmark result, or public evidence.

## Directive and intended vertical slice

The smallest coherent slice is:

1. preserve T-540's exact, authenticated, capture-only SOG frontier;
2. introduce one typed `RoomScene` composition contract that keeps captured
   appearance, reconstructed structure, planning data, cinematic derivatives,
   and debug witnesses separate by purpose and provenance;
3. use supplied real-data derivatives for diagnostic room structure and camera
   framing without allowing them to create visible Grand Hall architecture;
4. preserve “The Room Resolves” as an honest load-state experience, with a real
   source-envelope diagnostic witness able to carry the early diagnostic phase
   rather than a fabricated rectangular room;
5. add a deterministic fixed-camera lineage harness executable for local SOG
   and SPZ fixtures, with reserved evidence slots for external native LCC and
   authenticated current-runtime rendering; and
6. keep enhancement or generated-cinematic work inactive until the lineage run
   identifies a real, evidenced problem.

The dependency graph records `T-116 → T-541`, `T-498 → T-541`, and
`T-540 → T-541`: T-116 supplies the `TransformArtifactV0` contract that this
slice can reference, not a reviewed Grand Hall transform; the current manifest
still has `transformArtifacts: []`. The existing Room Resolves experience is
the presentation foundation, and the exact protected Grand Hall capture
remains the visual source of truth. `T-542` owns the reviewed room-local
transform and structural-authority work.

## Evidence baseline carried into this session

The exact SOG visual frontier remains the T-540 decision:

- decision ID `grand-hall-big-model-sog-fine-v1`;
- source manifest SHA-256
  `927a92699de222e99d2684ca2567a35ab1e523a036461e6e01236b7b77b7f659`;
- frontier receipt
  `sha256:8e7514e75aa19345dda1955f2cee3f9369339c553c2711c084cd04be4c9c1352`;
- eleven ordered highest-detail, non-environment SOG leaf members;
- 6,019,684 Gaussians and 106,479,738 bytes; and
- `env.sog` and ancestor LOD nodes excluded from simultaneous mounting.

The newer source root also contains SPZ, native LCC, OBJ, PLY, pose, and vendor
files named `collision.lci`; their bytes are present, but their semantics,
parser, and application frame are unvalidated. The verified inventory and benchmark contract are recorded
in `docs/reports/visual-lineage-benchmark.md`. The combined `.ply` is a triangle
mesh, not a Gaussian PLY, so the requested Gaussian-PLY comparison remains
unavailable unless a distinct eligible artifact is supplied or derived through
an evidenced conversion path.

Before the final local diagnostics described below:

- no native-LCC matched-camera screenshot had been captured;
- no immutable benchmark camera fixture had been reviewed;
- no SOG, SPZ, or current-runtime benchmark screenshot was accepted as
  evidence;
- no browser/GPU timing or frame distribution was accepted as evidence; and
- no representation had received a comparative fidelity verdict.

A first fresh-server local diagnostic completed SOG and SPZ at an exterior
inspection camera (`0` warm-up / `1` timed frame each). Both routes passed receipt,
decoded-count, active-splat, settled-sort, camera, actual colour-pipeline,
non-background-pixel, source-state, and schema gates. The exact records and
PNGs are under `docs/evidence/grand-hall-lineage/2026-08-23`. Codex visual
inspection flags each image as a distorted compact mass, not a trustworthy
Grand Hall view; both records remain `not_reviewed` pending Blake or another
human reviewer inspecting these exact hashes. SOG used 6,019,684 decoded/active splats and
106,479,738 source bytes; SPZ used the same count and 178,415,360 runtime-hashed
bytes, but its export lineage is not independently proven. SwiftShader needed
about 79.2 s and 77.8 s respectively for the single timed render request. This
diagnostic proved ingestion/render presence and exposed an unacceptable local
presentation; at that point it did not identify whether the dominant loss was
source, frontier composition/registration, decode, or renderer. One earlier atomic-
output attempt failed on a temporary filename before publication and retained
no final evidence pair.

### Corrective camera and depth-sort continuation

The compact mass was then isolated without changing source bytes. The original
camera inverse-mapped to a point about 22.4 m beyond the captured long-axis
bound and 8.17 m above the captured top. It was an external whole-frontier
sphere fit looking through an interior-trained volumetric shell. Independent
hierarchy and decode checks confirmed that the eleven members are the complete
finest frontier, share the identity LCC2 transform, occupy their declared
global bounds, and align with the matching SPZ positions. Adding ancestors or
`env.sog` would duplicate data or add a kilometre-scale far-field shell.

The local lineage and exact runtime now start from transformed source pose
19,890 at world position
`[-0.0342618693,2.1560615354,8.0151048418]`, looking horizontally toward the
q05/q95 pose-envelope centre. The position is exact source-trajectory data;
the look direction and FOV 60 remain inspection choices because the source
rotation convention and optical intrinsics are not native-viewer validated.
Orbit/dollhouse coordinates are clamped to the captured pose envelope. The
supplied LCC2 also declares depth sorting, so both paths now set Spark
`sortRadial: false` explicitly.

A second local Playwright run retained new v1 SOG and SPZ PNG/JSON pairs next
to the superseded exterior-camera v0 history. It passed 2/2 in 4.2 minutes with all 6,019,684
splats active, settled depth sorting, matching camera/projection, no context
loss, and schema-valid atomic records. SOG screenshot SHA-256 is
`sha256:83bc8c80a0ce0e00d31a8b05c52473cc5292e17c51f2f86ee7edd9b907527a59`;
SPZ screenshot SHA-256 is
`sha256:da894224fddabc4aaf04d152fe6bdb4e9900e4899950eae726ab30cf867f2fa3`.
Codex inspection finds both room-wide views coherent, including the supplied
windows, curtains, doors, timber panelling, portraits, chandeliers, decorated
ceiling and continuous timber floor; no invented central dark floor is
present. Both remain `diagnostic` / `not_reviewed` pending Blake or another
human reviewer dispositioning those exact hashes.

## Supplied OBJ and source-envelope diagnostic boundary

The supplied representative mesh is:

`C:\GRAND_HALL_BIG_MODEL_VARIATIONS\scans_BIG_MODEL_TH_GH_2\mesh-files\Grand_Hall.obj`

- SHA-256
  `ba5aa3d2c244acca3937505a17b34fb7f437ef5f59b7a85e7e691a2b2bcd47b6`;
- 34,040 vertices and 59,763 triangle faces; and
- source bounds min `[-31.858929, -23.662237, -6.327585]`, max
  `[3.825, 4.925, 8.617472]`.

The OBJ is a real XGRIDS reconstruction output, but “real source” does not mean
“reviewed room shell.” Its bounds extend materially beyond the selected SOG
frontier, it has no reviewed Grand-Hall-only connected-component decision, and
it has no registered room transform, floor semantic, safe spawn, portal set,
or collision acceptance record. Any OBJ-to-GLB form must retain its source and
derivative hashes and remain `RECONSTRUCTED`.

Likewise, a source-envelope proxy derived from the exact SOG AABB is only a
diagnostic witness for bounds, source arrival, or cutaway development. An AABB
does not describe the actual floor, walls, windows, doors, openings, alcoves,
or collision surface. It must not be labelled or used as a reviewed room shell,
walkable boundary, safe-spawn authority, or collision mesh. It must not become
visible architecture in the captured Grand Hall presentation.

The native `collision.lci` bytes are present but their format and application
frame have not been validated. They are unavailable as collision authority.
Diagnostic near-horizontal OBJ triangle concentrations suggest a possible main
floor around source Z `-2.20`, while the current inspection transform places
the minimum selected SOG Z `-2.8431265...` at world Y=0. That observation is a
floor-review lead only; it is not a measured floor datum.

## Fixed-camera lineage boundary

Every eligible comparison must freeze and record:

- source/master/runtime identity and all ordered asset hashes and byte counts;
- camera position, quaternion, projection, vertical FOV, near/far, aspect,
  viewport, output resolution, DPR, and complete transform lineage;
- Three, Spark, browser, OS, WebGL, GPU, colour, tone-mapping, antialias, blur,
  focal-adjustment, sort, and splat-budget settings;
- fetch, verification, decode, upload, first-pixel, stable-frame, and frame-time
  fields when the run actually measures them; and
- lossless screenshot dimensions, bytes, and SHA-256.

Native LCC remains an external reference unless a supported in-app reader is
introduced. It is eligible only when the native viewer can reproduce the same
numeric camera or a documented mapping with a measured residual. Supplied SOG
and current Venviewer runtime share the same visual bytes; that comparison can
isolate renderer/runtime behavior but is not an independent reconstruction
comparison. Cross-GPU pixel equality is not an acceptance gate.

## Actions deliberately not taken

This directive did not authorize, and this documentation work did not perform:

- a push, merge, pull request, branch replacement, or worktree reset;
- deployment, private-storage mutation, intake activation, package activation,
  database mutation, or public exposure;
- creation or rotation of provider credentials or API keys;
- generative modelling, image generation, video generation, inpainting, or
  architectural completion; or
- promotion of the OBJ, source envelope, `.lci`, or any derived proxy to
  reviewed shell, measured geometry, or collision authority.

No generative provider key is needed for the current lineage and structural
diagnostic slice. If a later evidence-backed enhancement phase is authorized,
its provider inputs and outputs must be separately identified, badged, and
kept out of operational geometry.

## Remaining post-slice gates

T-541's local vertical slice is complete. The broader controlled benchmark and
production path remain gated by:

- a native-viewer-validated optical camera convention and FOV for a genuinely
  matched cross-renderer comparison; the current camera has an exact captured
  trajectory position but inspection-only look direction and FOV;
- eligible representations must have complete run records or explicit
  unavailable dispositions;
- screenshot and timing evidence, if produced, must be hash-addressed and
  reviewed without overstating cross-renderer parity;
- the supplied structural mesh needs a human-reviewed room crop and registered
  transform under T-542 before it can become a room-shell candidate;
- floor, safe-spawn, boundary, portal, and collision behavior need separate
  evidence before walk/escape-prevention claims; and
- deployment, intake, activation, and any authenticated staging QA remain
  separate operator actions under the T-540 runbook and explicit authority;
- a production web build requires a real live `VITE_CLERK_PUBLISHABLE_KEY`;
  the key must be supplied through the provider secret manager and must not be
  faked, copied from a test environment, or bypassed; and
- a later staging run requires the selected target/API origin, split runtime-
  read and intake-put storage credentials, an admin token, and explicit
  permission for authenticated browser/WebGL QA. No generative provider key is
  needed for the current source-faithful slice.

## Verification results

Final local verification after the evidence-schema hardening:

- `pnpm --filter @omnitwin/types typecheck` — passed;
- `pnpm --filter @omnitwin/types lint` — passed;
- `pnpm --filter @omnitwin/types test` — 97 files / 2,156 tests passed, with
  no type errors;
- `pnpm --filter @omnitwin/types build` — passed;
- `pnpm --filter @omnitwin/web typecheck` — application and E2E TypeScript
  both passed;
- `pnpm --filter @omnitwin/web lint` — passed;
- `pnpm --filter @omnitwin/web test` — final rerun passed 334 files / 4,142
  tests. The first full run exposed eleven `App.test.tsx` failures caused by a
  stale hook mock omitting the new nullable exact-runtime key; the one-line
  mock-contract correction passed its focused 11/11 rerun before the final
  complete suite;
- `pnpm --filter @omnitwin/web exec vite build --mode development` — passed,
  transforming 3,331 modules in 20.31 seconds;
- `pnpm --filter @omnitwin/web build` — correctly refused to start without a
  real live `VITE_CLERK_PUBLISHABLE_KEY`; no placeholder or bypass was used;
- the local lineage Playwright invocation with
  `GRAND_HALL_LINEAGE_ROOT=C:\GRAND_HALL_BIG_MODEL_VARIATIONS`, the evidence
  directory under this session, `0` warm-up frames, `1` sampled frame,
  `E2E_BASE_URL=http://127.0.0.1:5189`, and
  `E2E_REUSE_EXISTING_SERVER=false` passed 2/2 in 6.9 minutes; and
- both retained JSON records parse through the final built
  `VisualLineageBenchmarkV0Schema` as `diagnostic`, `not_reviewed`, and
  `sparkRuntimeState.dirty=false`; and
- `git diff --check` — passed, with only the repository's Windows line-ending
  conversion warnings.

Corrective camera/depth-sort verification then passed:

- focused navigation, lineage, runtime-camera, and exact-layer regression
  bundle: 4 files / 38 tests;
- web application plus E2E TypeScript: passed;
- web lint: passed;
- complete web suite: 334 files / 4,142 tests;
- development Vite build: 3,331 modules in 21.19 seconds;
- corrected local SOG/SPZ lineage: 2/2 in 4.2 minutes at 0 warm-up / 1
  sampled frame, with 6,019,684 active splats, `sortRadial: false`, settled
  Spark state, no context loss, and atomic v1 evidence pairs; and
- all four retained historical/corrected JSON records parse through the built
  `VisualLineageBenchmarkV0Schema` as `diagnostic`, `not_reviewed`, and
  `sparkRuntimeState.dirty=false`;
- final `git diff --check`: passed, with line-ending warnings only.

This verification is local. It does not replace the outstanding authenticated
staging/package QA, native-LCC matched-camera comparison, hardware-GPU
performance pass, structural review, or Blake's visual disposition of the two
corrected v1 diagnostic PNG hashes.
