# Session — 2026-08-04

## Scope

Continued the active OmniTwin Foundry goal after Blake reported that Claude had
upgraded the project. This was ordinary local reconstruction/product work;
cybersecurity, activation, credentials and provider execution were out of
scope.

## Upgrade re-baseline

- Confirmed the 2026-07-28 checked-in/untracked documentation was primarily an
  external-evidence refresh, not a reconstruction run.
- Found Claude's untracked `tools/foundry-room-shape` implementation and its
  preserved 6,526,772-point Grand Hall cache and first real refusal.
- Found the separate `worktree-twin-nadir-fill` Floor Atlas lane. Its 14 focused
  commits are absent from the active branch. The committed lane is promising,
  but the current uncommitted alignment refinement is not safe to integrate:
  39/40 focused tests pass, a coarse-grid phase error invents per-source height
  offsets on clean inputs, and the final three Floor Atlas commits alone omit
  their `nadir_fill` prerequisite. Integration remains a separate clean-branch
  task, not an active-tree cherry-pick.
- Corrected the evidence refresh's resolution error: documented API 2048px
  cube faces correspond to nominal 8192×4096/~33.6 MP, while the local E57's
  4096px faces correspond to nominal 16384×8192/~134.2 MP. E57 does not share
  the API ceiling; owned photography remains a separately controlled quality
  and provenance option rather than the only route past it.
- Restored the truncated citation tail and Shepherd protocol in
  `docs/state/tasks.md`.

## T-545 completed

Hardened the authority-none Grand Hall room-shape proposer and froze an honest
real refusal pack. Key corrections include blocking unadjudicated outboard
mass, actual-support plane sampling, plane-to-plane height splay, strict PLY
payload handling, independent comparison digest verification, deterministic
diagnostics and a dependency-free run verifier. The proposal now explicitly
says it is not directly compatible with the existing Potree-specific V0 human
review artifact.

The exact real replay remains `unmeasurable`:

- `WALL_NOT_FOUND:y_max`
- `OUTBOARD_MASS_UNADJUDICATED:y_min`

Diagnostic-only values are x-plane pair 21.152 ± 0.038 m and height 6.744 ±
0.046 m. No advertised dimension was used or compared, and no crop or mesh was
created.

Verification:

- 40/40 focused tests pass.
- Five Python entry points compile.
- Two real replays produced byte-identical 26,877-byte proposal files at
  SHA-256 `55baf588528473ab3baef67a56ba1ce33d2d4ec0b0949f1ae55662d640fd11bc`.
- Full input/output rehash returned `PASS_ROOM_SHAPE_RUN_INTEGRITY`.
- Proposal digest:
  `7b850efa32104b8df6c35c40c29e0ec7af30acd861ee49e5ea5b83541644b1dd`.
- Receipt digest:
  `0da7325bd480a7bf23901f8690c9de0d3e9cfc1227c622582ecf9ec403fdbab7`.

Evidence:

- `docs/reports/grand-hall-room-shape-proposer-v1-2026-08-04.md`
- `docs/operations/grand-hall-room-shape-proposal-v1-2026-08-04.json`
- `docs/operations/grand-hall-room-shape-proposal-v1-2026-08-04-receipt.json`
- `docs/operations/grand-hall-room-shape-proposal-v1-2026-08-04-top-view.svg`

## Next ordinary slice

Create a durable sweep-tagged version of the cached cloud, adjudicate outboard
mass with opening-aware ray evidence, diagnose the missing positive-y boundary
and add an explicit E57 room-shape human-review adapter. Only a complete,
human-accepted envelope should feed bounded Poisson meshing. Separately,
integrate the 14-commit Floor Atlas lane from a clean branch after repairing
its alignment phase error and adding dependency/evidence closure.
