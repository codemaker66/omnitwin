# Session log — 2026-07-01 (ran into the early hours of 07-02)

## The Rite — landing page redesign

Blake's brief: completely redesign the venviewer.com landing page to embody
the philosophy of the sublime (Burke, Kant, Schopenhauer) — a total breath of
fresh air for July 2026, jaw-dropping, joyful, with genius-level care in tiny
details.

### Decisions (brainstormed, approved by Blake)

- Audience stays **Trades Hall bookers**; the venue is the protagonist.
- The embedded mini-planner preview was **cut** — full cinematic page.
- Medium: **scroll-cinema + photography + a ~2 KB raw-WebGL flame** — no
  Three.js on the route; bundle discipline holds.
- Boldness: **full ritual** (entry beat, carried light, optional room tone).
- Chosen direction: **"The Rite"** — a three-act dramaturgy: darkness
  (Burke) → magnitude (Kant) → contemplation (Schopenhauer) → the Return.
- Spec: `docs/superpowers/specs/2026-07-01-landing-rite-redesign-design.md`
  (committed 8507a1b4).

### Built

- `packages/web/src/pages/landing/` — 15 focused units: rite-copy (all copy
  as data, chapters derived from `publicRoomSelectionCards` so nothing can
  drift), rite-motion (pure springs + scroll-act math + Glasgow dusk
  heuristic), hooks (cursor light, scroll conductor, Web-Audio room tone,
  reduced motion, seen-latch), FlameCanvas (WebGL1 flame that gutters with
  cursor speed and rights itself on an underdamped spring), four act
  components + nav, rite.css (~900 lines: lighting-design palette, wick
  scrollbar, candle-glow focus rings, print fact-sheet, first-class static
  variant).
- Old `LandingPage.tsx` (1,873 lines) + `LandingPage.css` (2,593) deleted;
  shell rebuilt at 150 lines. Route surface unchanged (`/`, `/landing`,
  `/editor`), `#contact` anchor and "Powered by Venviewer" preserved.
- Eight-room index closes Act III (Deacon Convener's, Lady Convener's,
  North/South Galleries not orphaned); exterior photograph grounds the
  Return, preserving the e2e photo-set contract.
- E2E: 5 specs updated to the new h1/selectors; obsolete
  `landing-mobile-planner.spec.ts` replaced by `landing-rite-responsive.spec.ts`;
  new act-by-act screenshot harness `scripts/rite-visual-check.mjs`.
- Tests: `LandingPage.test.tsx` rewritten (claim guard sweeps the whole
  rendered page + `allRiteCopy()`); new `rite-motion.test.ts`,
  `rite-copy.test.ts`.

### Verified

- `tsc --noEmit` clean · eslint clean · **2,643 tests / 205 files green** ·
  vite build green. Landing chunk **22.7 kB / 8.22 kB gzip** (budget: 60 kB).
- Three visual passes via the new harness (12 captures each, 0 console
  errors). Fixed from screenshots: **CTA ignite black-on-black** (missing
  stacking context → `isolation: isolate`), **flame not rendering**
  (StrictMode double-invoke + `loseContext()` in cleanup handed the remount a
  dead context — removed loseContext), fragment feathering, dark-line pacing
  (34→62vh), chapter-margin legibility (text-shadow + corner scrim), mobile
  tone-toggle overlap.

### Gotcha worth remembering

React StrictMode + WebGL: never `loseContext()` in an effect cleanup that can
re-run — `getContext()` returns the same (dead) context on the second
invoke and the canvas is unrecoverable without the restore-event dance.

### Deviations from spec (documented)

- View Transitions API unused — the final architecture is scroll-native
  (sticky stages); there are no discrete DOM swaps for it to animate.
- AVIF: no AVIF assets exist in the repo; ships the existing optimized JPEGs
  (spec said "AVIF where available").
- Room-tone is Web-Audio synthesis (no 200 KB asset) — better than spec.

### Remaining / follow-ups

- Playwright e2e suites updated but not executed this session (unit/visual
  verified; run `landing-rite-responsive.spec.ts` + the T-469 public pass
  before the next deploy).
- `docs/state/tasks.md` work-log entry added alongside the commit.
