# Session — 2026-09-01

## T-564 pose-blind E57 cubeface camera recovery

- Added an exact, source-backed camera-orientation solver for native E57 scans
  40–47. It scores all 48 signed-axis bases for each of the 48 exact T559 JPEGs
  against a deterministic sample of scanner-local coloured E57 points. Stored
  Image2D poses are absent from the scoring API and are not read.
- Built a dedicated, byte-attested CPython 3.12.12 runtime with exact NumPy,
  OpenCV, pye57 and pyquaternion wheels. Its 930-file tree, all four installed
  distribution trees, six critical native binaries, Python executable, OpenCV
  build information, one-thread setting and disabled OpenCL state are frozen.
- Independent review found and closed three defects before the method commit:
  forged nested candidate/pose records could pass the result validator; a
  Pillow fallback produced decoder-dependent real-test bytes; and a final
  directory could be replaced after no-replace rename but before final
  verification. Each defect now has an adversarial regression.
- Committed the reviewed method locally as
  `a0b41caa338e4cbe8d62c99ecf22de0bda2eea6d`. The normal focused suite ran 34
  tests with the real-source test skipped; the isolated hermetic suite ran all
  34 tests, including exact scan 40, and passed.
- Ran the full receipt-last build and a separate full recomputation check. Both
  rehashed the exact 20,518,437,888-byte E57 and regenerated three fresh
  same-process reader attempts over all eight scans. The published 744,483-byte
  result is
  `sha256:27d247086fbbf85e3ec53397dd8fa79616d7ceb0a9618345d57055c0e44e71bd`;
  the 1,187-byte receipt is
  `sha256:5ae120e1c37641c58f83e38e6075ac06fd0dae9e9edba038726f4416b378c22f`.
- All 48 faces selected the identical proper six-face basis. Minimum winning
  NCC is `0.9826238541917383`, maximum RGB MAE is
  `2.5589983358427766`, and minimum winner/runner NCC margin is
  `0.24957572866243183`.
- This is a meaningful camera-recovery result, not room or reconstruction
  acceptance. External panorama correspondence/orientation, qualified human
  Grand Hall scope review, masks, E57-to-XGRIDS registration, training input,
  captured-master selection, provider input, runtime admission, staging,
  publication and production trust remain false or pending. No source was
  mutated; no push, upload, provider run, deployment or production action
  occurred.

## T-565 external panorama-to-E57 orientation recovery

- Added a source-only, deterministic five-fold spherical orientation solver
  for exact external panorama pixels and exact native E57 cubefaces. The
  reviewed path binds every match identity, persists train/query pixels and
  fold membership, tests both chirality conventions, requires unanimous fold
  selection and broad multi-face support, and composes only proper physical
  rotations. It does not use a deforming warp or generated pixels.
- Added fail-closed builder validation for exact T559/T560/T561/T564 inputs,
  filtered Git source identity, candidate-pose recomputation, authority guards,
  no-replace receipt-last publication, and zero-write full recomputation check.
  The focused suites pass 15 mathematical and 7 builder-contract tests.
- The first real attempt published nothing: it exposed float32 keypoint
  trigonometry at a stricter float64 unit-ray gate. The identical spherical
  convention now recomputes from source pixels in float64, with a regression
  test. The reviewed method is frozen locally at
  `c0f03f9b1c49fa9645bae2430fdc1bc00c317b51`.
- The real build and an independent fresh-process check both passed. The local
  pack contains 12 files / 109,200,016 bytes. Its 4,256,392-byte result is
  `sha256:e191fe09cd2ca033d362a7a6fde154f1c5a02fe5d3c10978a16662e3e4da3325`;
  its 3,359-byte receipt is
  `sha256:658326d6af128514ea02665ab94ab0cfe4581739c5fd4cb2f74670469de3251f`.
- The eight primary sweep 041–048 / scan 40–47 candidates retain 6,187 / 6,343
  held-out inliers (`97.54059593252404%`). All 40 primary folds independently
  select the reflected scanner-Y display convention. Held-out all-match p95 is
  `0.5033705618769961°`–`0.7624861670195795°`; all eight candidate E57 camera
  matrices are proper to `1.1102230246251565e-15` determinant error.
- Visual inspection finds rigid source agreement on the ceiling, chandeliers,
  portraits, fireplace, windows, panelling, doors and floor. Sweep 047 / scan
  010 is retained visibly as a genuine differently lit revisit with 351 / 400
  held-out inliers and greater parallax; it receives no physical E57 pose.
  Sweep 047 / scan 046 remains the stronger primary with 736 / 744 held-out
  inliers.
- Every correspondence, station, orientation, all-pixel room membership and
  mask decision remains independently human-pending. Authority is `none`; no
  generated content, source mutation, push, upload, provider run, staging,
  deployment, publication, or production action occurred.
