# Session log — 2026-06-07

Request (Blake): "push the project forward to be as most impressive as possible."

Branch: `feat/runtime-asset-provenance` (off `master`). Not pushed/merged.

## Multi-agent reconciliation (Codex)

Codex landed a comprehensive, room-scoped runtime-asset registry in the working
tree (uncommitted, intentional), superseding the T-423 groundwork: CaptureSession
+ RoomManifest + RuntimePackage, asset kinds (splat/mesh/point_cloud/…),
`staged|usable|rejected|archived` asset lifecycle, `draft|internal_ready|published|archived`
package lifecycle, strict `RuntimePackageManifestJson`, R2 object-key validation,
master-scan support, and `/admin/assets/*` endpoints + the AWS g6e XGRIDS
processing runbook.

- Committed it as the canonical base (`2b1e3b4`) after getting the tree green.
- Fixed 3 type-stale test fixtures in `asset-version.test.ts` (types) — manifest
  needed `pointCloudAssetVersionId`, RoomManifest needed `primaryCaptureSource`,
  AssetVersion needed `externalUrl`/`notes`, and package fixtures used asset-status
  values where package status is required.
- The committed web runtime-asset layer typechecked under `tsconfig.build` (tests
  excluded) and passed runtime tests (esbuild strips types) but failed `tsc --noEmit`.
  Fixed in `ed34565`: `evidenceStatusLabel` non-exhaustive after the evidence enum
  gained `rejected`; `usablePackageUrl` gated on package status `usable` (an asset
  status) instead of `internal_ready|published`; web runtime-asset test fixtures
  aligned with the room-scoped schema.

## Completed this session — T-424

Live capacity & comfort spatial intelligence in the planner HUD (the venue's #1
sales question, computed from real geometry, independent of the runtime-asset area):

- `packages/web/src/lib/layout-capacity.ts` — pure, tested engine: real floor area
  × published per-guest space standards by layout style → comfortable/tight
  capacity, m²/guest, comfort band, `inferSeatingStyle`. Replaces the hardcoded
  `COMFORTABLE_CAPACITY=200` magic number.
- `PlannerSpatialHud.tsx` — shows computed comfortable capacity, comfort band,
  m²/guest, and a SAFE disclosure ("Planning-grade estimate · not a legal or fire
  capacity · human review required").
- SAFE language only — explicitly not a legal occupancy or fire-capacity figure.
- Tests: layout-capacity 9 (incl. zero/negative/NaN + unsafe-phrase guards),
  PlannerSpatialHud +1.

Verified: full typecheck, lint, test (web 1785 / api 1594), build all green.

## Not done / follow-ups

- **T-091 / T-091A still not-started** — no real captured asset loads in runtime.
- `docs/diagrams/task-graph.md` regeneration for T-424 deferred (no generator).
- Branch `feat/runtime-asset-provenance` now carries: T-423 groundwork → Codex's
  superseding registry (`2b1e3b4`) → resolution type fix (`ed34565`) → capacity
  feature. Ready for Blake's review/merge.

## Acquisition-grade review — T-425

Request (Blake): acquisition-grade deep review of the current monorepo, grounded
in code evidence, with a prioritized executable backlog. Guardrails observed:
no T-091/T-091A completion, no fake asset claims, no runtime/product code
changes, no push.

Baseline verification:

- `pnpm typecheck` passed.
- `pnpm lint` passed.
- `pnpm -r --workspace-concurrency=1 run test` passed in the prescribed
  serialized workspace mode. Package counts confirmed separately: types
  32 files / 1274 tests; api 53 files / 1594 tests; web 97 files / 1785 tests.
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm build` passed. Build output
  kept Spark lazy/manual-chunked: `spark-*.js` about 4.9 MB raw / 1.75 MB gzip,
  separate from `three-*.js` about 936 KB raw / 254 KB gzip.

Review findings recorded in final chat report. Highest concrete P0 found:
dashboard enquiry and approved-enquiry email layout links still target the
retired `/editor/:configurationId` URL shape while the router now exposes
`/plan` and `/plan/:code`.

New backlog rows added in `docs/state/tasks.md`:

- T-425 done: acquisition-grade review/backlog refresh.
- T-426 through T-440 not-started: stale planner links, proposal/quote domain,
  CRM pipeline, client-facing planning-grade capacity, evidence pack runtime,
  Truth Mode runtime backing, Reception Room runtime coverage, processing-job
  operator workflow, runtime-asset migration application, supplier coordination,
  event-day mobile workflow, post-event actuals, Guest Flow Replay engine spike,
  accessibility gates, and release operations proof pack.

Notes:

- `state/asset_versions.json`, `state/capture_log.json`, and
  `state/training_runs.jsonl` still contain no real captured runtime asset
  evidence; T-091/T-091A remain blocked on real asset processing/registration
  and human verification.
- An additional isolated web count probe returned nonzero once despite
  reporting all 97 files / 1785 tests passed, because Vitest surfaced an
  unhandled-error warning tied to the R3F-in-DOM Grand Hall test harness.
  Targeted `GrandHallRoom` passed afterwards but still emits a large React/R3F
  warning stream. Treat as test-harness debt, not a shipped feature regression.
- `docs/diagrams/task-graph.md` regeneration for T-425/T-426..T-440 deferred:
  no generator/script was found in the repo.

## Completed this session — T-426

Fixed the stale enquiry layout-link path found during the acquisition-grade
review:

- `EnquiriesView` now opens saved layouts through `/plan/:configurationId`.
- Approved-enquiry email URL generation now uses `/plan/:configurationId`.
- Email fixtures were updated to match the live planner route.
- Regression tests added for the dashboard link and approved-email URL builder.

Verified:

- Focused web dashboard tests passed: 2 files / 74 tests.
- Focused API enquiry/email tests passed: 4 files / 56 tests.
- `pnpm typecheck` passed.
- `pnpm lint` passed.
- `pnpm -r --workspace-concurrency=1 run test` passed: types 32 files /
  1274 tests, api 53 files / 1595 tests, web suite passed with the existing
  R3F DOM-harness warning stream.
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm build` passed.

Notes:

- No runtime asset pipeline, training, RunPod, Docker, XGRIDS, public
  marketing copy, or asset-state files were changed.
- T-091/T-091A remain blocked/not-started: local state still has no real
  captured runtime asset evidence.
- `docs/diagrams/task-graph.md` regeneration remains deferred because no
  generator/script was found.

## T-441 — Fix hallkeeper offline progress sync (silent data loss + poison messages)

Request (Blake): "find something to improve to s+ tier engineering standard."

Squad role: offline / data-integrity reliability specialist.
Branch: `fix/hallkeeper-offline-sync-dataloss` (off `master` @ `2b84c7b`). Not pushed.

### What was wrong (verified against current code)

The hallkeeper tablet queues failed checkbox PATCHes to IndexedDB and replays
them on reconnect. Two real defects in that replay path:

1. **Silent data loss.** The flush built its "server" oracle from the optimistic
   local `checks` state — which already reflects every queued toggle — so
   `opsStillNeedingReplay` saw server == desired for every op, classified them all
   as already-converged, and `ackProgress`'d (deleted) them **without ever issuing
   the PATCH**. On reconnect the pending badge drained to 0 (looked synced) while
   the server never received the offline edits — the exact failure the queue exists
   to prevent.
2. **Poison messages.** Terminal 4xx replies (deleted config, revoked access, bad
   row) were treated as retriable, so such an op re-fired on every mount + `online`
   event forever and the badge never cleared.

The server `PATCH /hallkeeper/:configId/progress` is a non-idempotent toggle
(`packages/api/src/routes/hallkeeper-sheet.ts:229`), which is why blind replay is
unsafe and the authoritative-oracle approach is required.

### Fix (web-only)

- `packages/web/src/lib/progress-sync-queue.ts`: added pure, fully-tested helpers
  `partitionReplay` (replay vs already-converged against authoritative server
  state), `isReplayStatusTerminal` (4xx except 408/429 → terminal), and
  `resolveReplayDisposition` (ack on success/terminal-drop, keep on
  network/5xx/408/429).
- `packages/web/src/pages/HallkeeperPage.tsx` flush: fetches **authoritative**
  server progress as the oracle and bails (keeps the queue) if it can't read it;
  drops already-satisfied ops with no network call; re-issues exactly one toggle
  for the rest; acks/keeps per disposition; `flushInFlightRef` guards concurrent
  flushes; effect deps narrowed to `[configId]`.
- `handleToggle`: reverts the optimistic UI on a terminal rejection instead of
  queueing a doomed op.

### Verified

- web typecheck ✓ · lint ✓ · full suite **97 files / 1797 tests** ✓ · production build ✓
- progress-sync-queue tests: +12 (18 total)

### Unverified — please check

- The page-level flush orchestration (fetch + IDB wiring) is covered by
  typecheck/build + review, not by an automated component test; the pure decision
  logic underneath it is fully unit-tested. A `HallkeeperPage` flush integration
  test with mocked `fetch` + IDB would close that gap.

### Follow-up

- **T-442 closed below**: the progress PATCH now has a backward-compatible
  idempotent set-state path, so queued replay no longer depends on a
  read-then-toggle window.

## T-442 — Idempotent hallkeeper progress PATCH

Request: continue the S+ engineering sweep by closing the T-441 residual replay
TOCTOU.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Fix

- `PATCH /hallkeeper/:configId/progress` accepts optional `checked: boolean`.
- `{ rowKey, checked }` is now idempotent set-state:
  - `checked: true` inserts the row with `onConflictDoNothing` against the
    unique `(config_id,row_key)` constraint so duplicate/lost/concurrent replay
    converges instead of producing a duplicate-key 500.
  - `checked: false` deletes directly by `(config_id,row_key)`, safely no-oping
    when already absent.
- `{ rowKey }` still preserves the legacy toggle path for older clients.
- `HallkeeperPage` already sends `checked: desiredChecked` for live toggles and
  queued replay; the Hallkeeper E2E mock now asserts that the browser sends a
  boolean `checked` field.
- `packages/api/src/lib/hallkeeper-progress.ts` keeps the pure
  `resolveProgressMutation` / `checkedStateAfter` decision logic tested without
  a database.

### Verified

- `pnpm --filter @omnitwin/api typecheck`
- `pnpm --filter @omnitwin/api lint`
- `pnpm --filter @omnitwin/api test -- hallkeeper-progress hallkeeper-sheet-v2`
  — 40 focused tests passed.
- `pnpm --filter @omnitwin/web test -- progress-sync-queue` — 18 tests passed.
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm --filter @omnitwin/web build`
- `pnpm --filter @omnitwin/api build`
- `pnpm --filter @omnitwin/api test` — 54 files / 1607 tests passed.
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm --filter @omnitwin/web e2e -- e2e/hallkeeper.spec.ts --workers=1`
  — 18 passed.

### Unverified — please check

- `pnpm --filter @omnitwin/api test:integration` was not run in this pass; the
  repo's default API test command excludes `integration.test.ts` and a live test
  database was not configured for this continuation.

### Remaining work

- No known remaining T-441/T-442 data-loss path after this patch. Broader S+
  codebase hardening continues under the active thread goal.

## T-443 — Retail client route, copy, and handoff polish sweep

Request (Blake): "are you able to do your best to fix everything" after the
retail-client end-to-end audit.

Squad role: Frontender + Tester + Architect.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Fix only the retail/staff journey defects found in the audit: broken public
  route aliases, unsupported public real-room wording, stale planner handoff
  links, raw catalogue slugs in enquiry success copy, and inconsistent product
  shell/casing.
- Do not touch T-091/T-091A evidence state, do not fake a real asset, and do
  not add public safety/production claims.

### Fix

- `LandingPage` public wording now describes a planning draft instead of implying
  verified real-room runtime evidence.
- `PricingPage` CTAs now render as real React Router links to `/register?...`
  instead of button-driven `window.location.href`; negative heading letter spacing
  in the touched pricing page was normalised to `0`.
- Router/legal/auth entry points now cover `/onboard` (query-preserving redirect
  to `/register`), `/legal/privacy`, `/legal/terms`, `/legal/accessibility`, and
  `/accessibility`.
- `LoginPage` / `RegisterPage` now have Venviewer-branded shells plus visible
  Clerk loading frames, so a slow or dummy local Clerk key no longer looks like
  an empty form column.
- Staff/client handoff links and API/email-generated review links now use
  `/plan/:id` instead of stale `/editor/:id` URLs.
- `GuestEnquiryModal` resolves catalogue item ids as well as slugs, preventing
  raw values such as `round-table-6ft` from leaking into success copy.
- Added regression guards for unsupported public wording, stale `/onboard?` /
  `/editor` links, email preview URLs, dashboard handoff URLs, legal aliases, and
  the route redirect source.

### Verified

- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web test -- auth-components PricingPage LandingPage public-claim-guard save-send-panel dashboard`
- `pnpm --filter @omnitwin/web build`
- Playwright smoke on `http://127.0.0.1:4174`: landing, pricing, legal aliases,
  login, register, `/onboard?tier=pro&cycle=annual`, mobile landing/pricing, and
  pricing CTA to `/register?tier=pro&cycle=annual`; no framework overlay, blank
  route, relevant console/page error, stale `/onboard?` link, or unsupported
  claim hit.
- `pnpm lint`
- `pnpm typecheck`
- `pnpm --filter @omnitwin/web test` — 97 files / 1801 tests passed.
- `pnpm --filter @omnitwin/api test` — 54 files / 1606 tests passed.
- `pnpm build`
- `pnpm --filter @omnitwin/web e2e -- e2e/landing-mobile-planner.spec.ts e2e/public-config-flow.spec.ts e2e/planner-submit.spec.ts e2e/staff-review.spec.ts e2e/hallkeeper.spec.ts e2e/navigation.spec.ts e2e/private-brief.spec.ts --workers=1` — 47 passed / 3 skipped.
- Source sweeps found only historical docs, negative tests, comments, and the
  dedicated Spark fixture page for the audited phrases/fixture markers.

### Unverified — please check

- The real Clerk hosted form with the configured deployment publishable key.
  Local smoke used `pk_test_dummy`, so the safe loading frame rendered correctly,
  but a real-key staging/live pass is still needed to prove the hosted form itself
  appears and completes account creation.
- In-app Browser MCP was unavailable (`Browser is not available: iab`), so the
  rendered route validation used Playwright screenshots instead.
- No push was performed.

### Remaining work

- **T-091 / T-091A still not-started** — no real captured Trades Hall asset loads
  in runtime and no room asset evidence was claimed or faked.
- T-441/T-442 are closed locally; broader S+ hardening continues.

## T-092 — Frontend Sentry foundation with guarded source-map upload

Request (Blake): continue doing whatever is needed to raise the codebase quality.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add the missing web-side observability foundation without requiring or handling
  real Sentry credentials locally.
- Do not change public claims, T-091/T-091A evidence state, runtime assets, or
  deployment state.

### Fix

- Added `@sentry/react` and `@sentry/vite-plugin` to `@omnitwin/web`.
- Added optional browser Sentry runtime init gated by `VITE_SENTRY_DSN`; when the
  DSN is absent, Sentry is a no-op and the SDK is not dynamically imported.
- Wired `AppErrorBoundary.componentDidCatch` to the observability layer while
  preserving the same user-facing recovery UI.
- Added PII controls for Sentry events: request payloads, headers, cookies,
  query strings, users, and extras are stripped before send.
- Added Vite build support for hidden source maps and Sentry upload only when
  all three server-side build credentials are present:
  `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, and `SENTRY_PROJECT`.
- Added env typings and local/production env templates for browser DSN, release,
  environment, trace sample rate, and source-map upload credentials.
- Updated task state honestly: T-060 and T-092 are `blocked`, not `done`, until
  the real EU-region Sentry project, production env, upload token, and alert
  rules are configured and verified.

### Verified

- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web test -- sentry error-boundary vite-config-env bundle-splitting`
  — 34 focused tests passed.
- `pnpm --filter @omnitwin/web lint`
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm --filter @omnitwin/web build`
- `pnpm --filter @omnitwin/web test` — 98 files / 1811 tests passed.
- `pnpm lint`
- `pnpm typecheck`
- `pnpm --filter @omnitwin/api test` — 54 files / 1607 tests passed.
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm build`
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy VITE_SENTRY_DSN=https://public@example.ingest.sentry.io/1 VITE_SENTRY_ENVIRONMENT=production VITE_SENTRY_RELEASE=local-test VITE_SENTRY_TRACES_SAMPLE_RATE=0 pnpm --filter @omnitwin/web build`
  — verified the dynamic Sentry runtime path compiles when a browser DSN is set.
- Source sweep over touched Sentry files found no `as unknown as`, `as any`, or
  explicit `any` annotations. Public-source unsafe-claim sweep found only an
  existing internal comment in `layout-capacity.ts` saying capacity is not a
  survey-grade measurement.

### Unverified — please check

- Real Sentry EU-region project/account exists.
- Production `VITE_SENTRY_DSN`, `VITE_SENTRY_ENVIRONMENT`, `VITE_SENTRY_RELEASE`,
  and `VITE_SENTRY_TRACES_SAMPLE_RATE` are configured.
- Build-only `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, and `SENTRY_PROJECT` are
  configured in deployment, not exposed as `VITE_*`.
- Source-map upload has been proven in a real deployment.
- Alert rules exist: unhandled production exception = immediate alert; sustained
  error rate > 1% over 5 minutes = warning.

### Remaining work

- T-060/T-092 remain blocked on external Sentry configuration and live alert
  verification.
- Full web test output still contains pre-existing React/Three DOM-casing
  warnings from mocked R3F components; tests pass, but this noise remains a
  cleanup target.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-200 — Deployable public claim-lint scanner

Request (Blake): continue doing whatever is needed to raise the codebase quality.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Expand the existing public-claim guard into a broader source-time scanner for
  deployable static assets and customer-facing surfaces.
- Do not weaken internal architecture docs, change T-091/T-091A evidence state,
  or add public claims.

### Fix

- Expanded `packages/web/src/__tests__/public-claim-guard.test.ts` from a small
  Landing/Pricing/README check into a production-oriented claim scanner covering:
  `packages/web/public`, route/page components, auth/dashboard/editor/hallkeeper/
  shared/truth UI surfaces, router/app metadata, customer-facing web copy helpers,
  API email templates, and hallkeeper PDF/export copy.
- Added explicit forbidden/evidence-required phrase families with per-pattern
  remediation guidance.
- Added an allowlist structure requiring exact file, phrase family, substring,
  owner, reason, and expiry. It is currently empty.
- Excluded tests and internal docs so research/ADR language does not become
  timid, while customer-facing source remains guarded.
- The new scanner caught strong Truth Mode wording: `Survey grade`. Replaced the
  bundled UI label with `Survey evidence tier` in
  `packages/web/src/lib/truth-mode-summary.ts`, and pinned it in
  `TruthModeIndicator` tests.
- Marked T-200 done. T-199 remains future work for the formal typed authoring
  taxonomy; the scanner owns a pragmatic v1 phrase taxonomy.

### Verified

- `pnpm --filter @omnitwin/web test -- public-claim-guard TruthModeIndicator`
  — 12 focused tests passed.
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm --filter @omnitwin/web build`
- Direct phrase sweep over current source/build output only found negative tests
  and internal comments outside the scanner's customer-facing surface set.

### Unverified — please check

- This is a source-time scanner. Generated `dist/` is rebuilt during verification
  but intentionally not used as the unit-test scan source because stale local
  dist output can predate the current source.

### Remaining work

- T-199 remains: formal typed public-copy claim taxonomy and authoring workflow.
- T-201/T-211/T-213 remain for internal-doc allowlist policy, exposure-tier
  artifact scanning, and full exposure/claim integration.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-444 — Dependency audit pins and Vitest 4 migration

Request (Blake): continue doing whatever is needed to raise the codebase quality.

Squad role: Security reviewer + TypeScript reviewer + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Remove current moderate+ npm audit advisories.
- Keep the existing Windows/V8 heap protections in Vitest config.
- Do not change product behavior, public claims, T-091/T-091A state, or runtime
  asset evidence.

### Fix

- Upgraded/pinned `react-router-dom` to the patched 7.17 line.
- Pinned `vitest` to `4.1.8` in web/API/types because the types package uses
  Vitest typecheck.
- Added root overrides for patched `js-cookie`, `ws`, and `brace-expansion`.
- Migrated Vitest 4 config away from removed `test.poolOptions` to top-level
  `execArgv`, `maxWorkers`, and `fileParallelism`.
- Re-typed Vitest mock/spies in API and web tests so no unsafe mock typing is
  introduced.

### Verified

- `pnpm install`
- `pnpm audit --audit-level=moderate` — no known vulnerabilities.
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
- E2E chunks: route/public/hallkeeper/trades visual, editor/mobile shell,
  Spark/Truth smoke, editor, performance/planner/staff review.

### Unverified — please check

- Live deployment not pushed or checked.
- Deprecated subdependency warnings from `react-email` remain non-audit warnings
  and are not fixed in this pass.

### Remaining work

- Keep monitoring audit drift in CI/deploy.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-445 — Runtime room registry alignment and browser URL hardening

Request (Blake): continue doing whatever is needed to raise the codebase quality.

Squad role: Runtime reviewer + TypeScript reviewer + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Remove drift between the shared Trades Hall runtime-room registry and the web
  `/dev/trades-hall-visual` room selector.
- Align browser Spark URL validation with the API/types fixture/demo rejection
  contract.
- Keep arbitrary manual Spark URLs out of production builds; live runtime
  loading should come from registered RuntimePackages.
- Do not register a real asset, change runtime evidence state, or mark
  T-091/T-091A done.

### Fix

- Changed `packages/web/src/lib/runtime-package-resolution.ts` to derive
  supported Trades Hall runtime rooms from `@omnitwin/types` instead of a stale
  local list.
- Added regression coverage that every shared Trades Hall runtime room can be
  selected by query param, including Reception Room.
- Changed `packages/web/src/lib/runtime-visual-asset.ts` to reuse
  `FORBIDDEN_ASSET_FIXTURE_MARKERS`, so generic `fixture` and `demo` URLs are
  rejected before Spark mounts them.
- Gated `splatUrl` query-string overrides to local dev builds in
  `TradesHallVisualPage`; production builds reject the manual URL and keep the
  registered-package/procedural fallback path.
- Added route/component tests for Reception Room and URL parser tests for
  generic fixture/demo paths and production manual-URL rejection.
- Added `E2E_WEB_SERVER=preview` support to `packages/web/playwright.config.ts`.
- Added a preview-only Playwright regression for production manual-URL rejection;
  it skips during normal dev-server E2E unless the production-preview env is set.

### Verified

- `pnpm --filter @omnitwin/web test -- runtime-package-resolution runtime-visual-asset TradesHallVisualPage`
  — 32 focused tests passed.
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm --filter @omnitwin/web build`
- `E2E_WEB_SERVER=preview E2E_EXPECT_PRODUCTION_MANUAL_URL_DISABLED=true pnpm --filter @omnitwin/web e2e -- e2e/trades-hall-visual.spec.ts -g "rejects manual splatUrl overrides in production builds" --workers=1`
  — 1 passed.
- `pnpm --filter @omnitwin/web e2e -- e2e/trades-hall-visual.spec.ts --workers=1`
  — 2 passed / 1 skipped.
- Built `dist/assets/TradesHallVisualPage-*.js` contains the disabled manual URL
  message and the production constant folded to `false` for the manual override.

### Unverified — please check

- Full repo test/build was not rerun after this small web-only patch; the prior
  full repo passes remain the broader baseline.
- Live deployment not pushed or checked.

### Remaining work

- Real AssetVersion/RuntimePackage registration is still blocked on real R2 keys
  and SHA-256 evidence.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-446 — Full local verification sweep after hardening

Request (Blake): continue doing whatever is needed to raise the codebase quality.

Squad role: Release verifier + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Re-run the full local repository gates after the dependency audit, Sentry
  foundation, retail polish, runtime registry, and production manual-URL
  hardening work.
- Keep this as verification only: no push, no real asset registration, no live
  deployment claim, and no T-091/T-091A status change.

### Verified

- `pnpm audit --audit-level=moderate` — no known vulnerabilities.
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
  - types: 32 files / 1274 tests.
  - API: 54 files / 1607 tests.
  - web: 98 files / 1817 tests.
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_dummy pnpm build`
- `E2E_WEB_SERVER=preview E2E_EXPECT_PRODUCTION_MANUAL_URL_DISABLED=true pnpm --filter @omnitwin/web e2e -- e2e/trades-hall-visual.spec.ts -g "rejects manual splatUrl overrides in production builds" --workers=1`
  — 1 passed.
- `pnpm --filter @omnitwin/web e2e -- e2e/trades-hall-visual.spec.ts --workers=1`
  — 2 passed / 1 skipped.
- `git diff --check` — no whitespace errors; CRLF warnings only.

### Unverified — please check

- Live deployment not pushed or checked.
- API integration suite remains outside the default API test command and was not
  run against a live database in this pass.
- Real Clerk/Sentry production env remains externally configured, not proven
  locally.

### Remaining work

- Split the large dirty tree into safe commits.
- Run a release gate on the final commit stack before any push.
- Register real AssetVersion/RuntimePackage entries only after real R2 keys and
  SHA-256 evidence exist.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-095 — Venue-scoped planner routing

Request (Blake): continue the S+ engineering sweep by fixing real acquisition
gaps where possible.

Squad role: Architect + TypeScript reviewer + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Fix the existing `/v/:venueSlug/plan` route so it actually opens the requested
  venue instead of silently using `venues[0]`.
- Keep `/plan` as the single-tenant shortcut.
- Do not change billing, CRM, simulation, asset evidence, public claims, or
  T-091/T-091A status.

### Fix

- Added `packages/web/src/lib/planner-venue-resolution.ts`, a pure resolver for
  planner venue selection:
  - `/plan` resolves to the first active venue.
  - `/v/:venueSlug/plan` resolves only an exact active slug.
  - unknown explicit slugs return `not_found`.
  - scoped non-admin users with a different `venueId` return `forbidden`.
  - admins and anonymous/public users can open active venue-scoped planner links.
- Updated `EditorPage` bootstrap to:
  - read `venueSlug` from `useParams`;
  - wait for auth resolution on venue-scoped URLs before auto-creating a layout;
  - list spaces for the resolved venue, not the first venue;
  - show explicit safe states for empty venue list, unknown venue, scoped-user
    mismatch, and network failure;
  - keep bad explicit slugs from falling back to Trades Hall.
- Updated the router comment so it no longer promises silent fallback.
- Added unit/component/browser coverage:
  - pure resolver tests for default, explicit second venue, unknown slug,
    empty list, scoped-user forbidden, and admin access;
  - `EditorPage` tests proving the explicit venue path creates against the
    second venue and refuses not-found/forbidden cases;
  - Playwright navigation tests for two-venue create, not-found, and forbidden.

### Verified

- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web test -- planner-venue-resolution EditorPage.venue-routing save-send-panel`
  — 39 focused tests passed.
- `pnpm --filter @omnitwin/web e2e -- e2e/navigation.spec.ts --workers=1`
  — 7 browser tests passed.
- `$env:VITE_CLERK_PUBLISHABLE_KEY='pk_test_dummy'; pnpm --filter @omnitwin/web build`
- Source audit over touched files found no `any`, `as any`, `as unknown as`,
  TODO/FIXME, `console.log`, or unsafe claim phrases.

### Unverified — please check

- Full repo test/build was not rerun after this narrow web route patch.
- Live deployment not pushed or checked.
- The client-side forbidden guard does not replace server-side venue ownership
  checks on authenticated mutation APIs; it prevents this public bootstrap route
  from creating a layout under the wrong venue from a scoped user's session.

### Remaining work

- T-094 Stripe/onboarding remains open.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-096 — Editor optimistic concurrency

Request (Blake): continue the S+ engineering sweep by fixing real acquisition
gaps where possible.

Squad role: API reviewer + TypeScript reviewer + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Close the audited `editor-store.ts` full-sync last-writer-wins gap.
- Add revisions, server-side optimistic concurrency, conflict UI, and persisted
  layout history for the public/authenticated batch save path used by the
  planner.
- Do not touch public copy, CRM, proposals, simulation, billing, training,
  runtime asset evidence, or T-091/T-091A status.

### Fix

- Added `configurations.revision` and a new
  `configuration_layout_revisions` history table via
  `packages/api/drizzle/0025_configuration_revisions.sql`.
- Added `configurationRevisionEtag` and `revisionConflictBody` in
  `packages/api/src/lib/configuration-revision.ts`.
- Updated public and authenticated full-sync batch saves to require
  `expectedRevision`, reject stale requests with typed `REVISION_CONFLICT`
  409 responses, atomically advance revisions before object writes, return
  `{ objects, revision }`, and persist revision-history payloads.
- Added ETags on configuration create/read/update responses.
- Updated the web configuration API schemas to require configuration revisions,
  validate batch-save envelopes, and parse typed revision conflicts.
- Updated `editor-store.ts`, `anonymous-planner-draft.ts`, `EditorPage`,
  `MobilePlannerTopBar`, and `VerticalToolbox` so stale saves do not clear the
  local dirty layout, anonymous drafts are only restored when their
  `baseRevision` matches the server revision, and the user gets an explicit
  reload path.
- Added schema/migration, route-source, API response-validation,
  anonymous-draft stale-revision, and editor-store stale-save regression
  coverage.

### Verified

- `pnpm --filter @omnitwin/api typecheck`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/api test -- schema public-config placed-objects`
  — 66 tests passed.
- `pnpm --filter @omnitwin/web test -- editor-store response-validation save-send-panel editor-bridge localstorage-cap EditorPage.venue-routing`
  — 98 tests passed.
- `pnpm --filter @omnitwin/web test -- editor-store anonymous-planner-draft response-validation`
  — 54 tests passed after adding draft `baseRevision` rejection.
- `pnpm --filter @omnitwin/web test`
  — 100 files / 1834 tests passed.
- `pnpm --filter @omnitwin/api lint`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/api test`
  — 54 files / 1615 tests passed.
- `$env:VITE_CLERK_PUBLISHABLE_KEY='pk_test_dummy'; pnpm --filter @omnitwin/web build`
- `pnpm --filter @omnitwin/web test -- TradesHallVisualPage runtime-visual-asset spark-stack bundle-splitting`
  — 36 tests passed.
- `git diff --check` — no whitespace errors; CRLF warnings only.

### Unverified — please check

- API integration suite was updated for the new contract but was not run
  against a live database in this pass.
- Live deployment not pushed or checked.

### Remaining work

- Legacy direct single-object mutation routes still do not use revision
  preconditions; the audited overwrite path was the editor full-sync batch
  save.
- T-094 Stripe/onboarding remains open.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-098 — Dependency reproducibility

Request (Blake): continue the S+ engineering sweep by closing remaining audit
quality gaps.

Squad role: Release engineering reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Close the audit finding for dependency reproducibility.
- Pin direct workspace dependencies exactly, keep internal workspace links as
  `workspace:*`, document the update policy, and add a regression guard.
- Do not introduce product features, public copy, runtime asset claims, package
  renames, or T-091/T-091A status changes.

### Fix

- Changed direct dependency specifiers in `packages/api/package.json`,
  `packages/types/package.json`, and `packages/web/package.json` from semver
  ranges to exact versions.
- Regenerated `pnpm-lock.yaml` so importer specifiers match the exact pins.
- Added `packages/types/src/__tests__/dependency-pins.test.ts`:
  - rejects direct dependency specifiers that are not exact `x.y.z` versions or
    internal `workspace:*` links;
  - pins the Spark/Three/R3F/drei renderer matrix;
  - pins Clerk and Vite/Vitest build/auth tooling.
- Added `docs/operations/dependency-update-policy.md` with the exact-pin rule,
  critical dependency sets, normal upgrade flow, and emergency CVE override
  flow.
- Removed three now-linted redundant `as const` assertions in the types package.
- Updated the Spark dependency test from caret expectations to exact-pin
  expectations.
- Fixed one nullable test readout in `PolygonEditor.test.tsx` exposed by the
  exact TypeScript pin.
- Added an explicit lint exception for React Router's intentional thrown
  `Response` rethrow in `resolve-loader.ts`.

### Verified

- `pnpm install`
- `pnpm --filter @omnitwin/types test -- dependency-pins`
  — 3 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types test -- dependency-pins residual-radiance runtime-venue-manifest url-identifiers`
  — 62 tests passed.
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web test -- spark-stack PolygonEditor`
  — 22 tests passed.
- `pnpm audit --audit-level=moderate`
  — exited 0; one low-severity advisory remains below the configured threshold.
- `pnpm typecheck`
- `pnpm test`
  — types 33 files / 1277 tests, API 54 files / 1615 tests, web 100 files /
  1834 tests.
- `pnpm lint`
- `$env:VITE_CLERK_PUBLISHABLE_KEY='pk_test_dummy'; pnpm build`
- `pnpm --filter @omnitwin/web test -- TradesHallVisualPage runtime-visual-asset spark-stack bundle-splitting`
  — 36 tests passed.
- Source sweep found no remaining direct package semver ranges.

### Unverified — please check

- The low-severity audit item was not remediated because the required gate is
  `--audit-level=moderate`; it remains below the current release threshold.
- Live deployment not pushed or checked.

### Remaining work

- T-094 Stripe/onboarding remains open.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-085 — Current deploy flow documentation

Request: continue the S+ engineering sweep by closing unblocked operational
quality gaps.

Squad role: Release engineering reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Document the current deploy flow and gating gaps honestly.
- Do not implement deploy orchestration; T-093 owns that.
- Do not touch product runtime code, training, runtime assets, public claims, or
  T-091/T-091A state.

### Fix

- Added `docs/operations/deploy-flow-current.md`.
- Documented repo-proven deploy evidence:
  `packages/web/vercel.json`, `railway.json`, `Dockerfile`, GitHub CI,
  `.github/workflows/deploy.yml`, manual database workflows, and API health
  probes.
- Separated repo-controlled gates from Vercel/Railway dashboard behavior that
  the repo cannot prove.
- Listed current gaps: no single release controller, no repo proof that provider
  deploys wait for CI/migrations, no provider polling, no shared release ID,
  one configured Railway replica, and manual rollback.
- Added pre-push and post-deploy verification checklists.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` to mark T-085
  done while leaving T-093 open.

### Verified

- `rg` sweep over the new doc and touched docs for unsupported runtime-asset
  claims.
- `git diff --check` passed with CRLF warnings only.

### Unverified — please check

- The actual Vercel and Railway dashboard auto-deploy settings were not checked
  from this local pass.
- No live deployment was pushed or verified.

### Remaining work

- T-093 remains open for real gated deploy orchestration with release IDs,
  migration locking, provider polling, live smoke checks, and rollback records.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-099 — Per-package README onboarding docs

Request: continue the S+ engineering sweep by closing unblocked local quality
gaps.

Squad role: Developer-experience reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Address the legitimate T-099 scope: package-level READMEs for onboarding and
  hiring readiness.
- Leave the low-priority corporate-internal audit memo files out of scope.
- Do not change product runtime behavior, training, runtime assets, public
  claims, or T-091/T-091A state.

### Fix

- Added `packages/api/README.md` with API ownership, non-ownership, commands,
  env, migration, and server contract rules.
- Added `packages/types/README.md` with shared contract ownership, schema rules,
  and when to change the package first.
- Added `packages/web/README.md` with route/UI ownership, browser commands,
  runtime-asset guardrails, public-copy guardrails, and environment notes.
- Updated root `README.md` to link the package docs, remove stale test counts,
  and replace misleading single-tenant wording with application-workspace
  wording.
- Added `packages/types/src/__tests__/package-readmes.test.ts` to pin package
  README presence, required boundary/gate sections, and absence of unsupported
  claim phrases.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` to mark T-099
  done.

### Verified

- `pnpm --filter @omnitwin/types test -- package-readmes` — 2 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/web test -- public-claim-guard` — 4 tests passed.
- `git diff --check` passed with CRLF warnings only.
- Added-line unsafe-phrase sweep found no new hits.

### Remaining work

- Corporate-internal audit memo files remain intentionally deferred/retired for
  solo-founder stage, per the T-099 scope note.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-284 — Artifact type vocabulary

Request: continue the S+ engineering sweep with unblocked local foundation work.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add the metadata-only Artifact Registry type vocabulary from VAR-001.
- Do not implement a registry service, database schema, storage migration,
  runtime loader, public copy, or package rename.
- Do not change runtime asset evidence or T-091/T-091A state.

### Fix

- Added `packages/types/src/artifact-type.ts` with `ARTIFACT_TYPES`,
  `ArtifactTypeSchema`, and `ArtifactType`.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/artifact-type.test.ts` to pin:
  - the initial VAR-001 artifact families;
  - unique lower-snake-case metadata shape;
  - explicit OpenUSD/KHR/C2PA/DSSE families rather than generic export/blob
    categories;
  - rejection of file formats, claim states, exposure tiers, provider names,
    and runtime status terms as artifact types.
- Marked T-284 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- artifact-type` — 4 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`
- `git diff --check` passed with CRLF warnings only.
- Added-line unsafe-phrase sweep found no new hits.

### Remaining work

- T-285 closed below with the actual artifact manifest v0 shape.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-285 — Artifact manifest v0 schema

Request: continue the S+ engineering sweep with unblocked local foundation work.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add the v0 metadata schema for Artifact Registry manifests.
- Keep this as schema design only: no registry service, DB schema, storage
  migration, runtime loader, public copy, or package rename.
- Do not change runtime asset evidence or T-091/T-091A state.

### Fix

- Added `packages/types/src/artifact-manifest.ts` with:
  - `ARTIFACT_MANIFEST_V0_SCHEMA_VERSION`;
  - exposure tiers;
  - freshness states;
  - export-safety states;
  - creator/reference vocabularies;
  - sha256 content-hash schema;
  - runtime-compatibility schema;
  - strict `ArtifactManifestV0Schema` fields for artifact identity, type,
    purpose, source inputs, content hash, creator, exposure, freshness,
    associated claims/evidence, runtime compatibility, export safety, and known
    limitations.
- Exported the manifest vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/artifact-manifest.test.ts` covering:
  full-manifest parsing, every registered artifact type, required source inputs,
  runtime compatibility and limitations, sha256 shape, strict unknown-field
  rejection, and unsafe exposure/export-safety combinations.
- Marked T-285 done in `docs/state/tasks.md`.
- Added unblocked notes to T-286 and T-287. T-288+ still have separate
  dependencies.

### Verified

- `pnpm --filter @omnitwin/types test -- artifact-manifest artifact-type` — 12
  tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-286 and T-287 are now ready for policy/doctrine work.
- T-210 remains broader exposure metadata integration and is not closed by this
  manifest foundation.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-286 — Artifact freshness policy

Request: continue the S+ engineering sweep with unblocked local foundation work.

Squad role: Architecture reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Define the policy for `ArtifactManifestV0.freshnessState`.
- Keep this doctrine-only: no registry service, DB schema, runtime loader,
  public copy, stale-detection implementation, or claim graph.
- Do not change runtime asset evidence or T-091/T-091A state.

### Fix

- Added `docs/architecture/artifact-freshness-policy.md`.
- Defined every v0 freshness state:
  `current`, `partial`, `stale`, `superseded`, `expired`, `not_checked`,
  `degraded_evidence`, `requires_human_review`, and `unsupported_request`.
- Set missing/ambiguous freshness to fail closed as `not_checked`.
- Added trigger mapping for capture, transform, layout, policy, data
  sufficiency, runtime compatibility, toolchain/license, and claim lifecycle
  changes.
- Defined transition rules, purpose-specific interpretation, source-input
  attribution, runtime compatibility decay, claim/copy implications, and
  review-record fields.
- Marked T-286 done in `docs/state/tasks.md`.

### Verified

- `git diff --check` passed with CRLF warnings only.
- Added-line unsafe-phrase sweep found no new hits.

### Remaining work

- T-287 remains ready for artifact exposure-tier policy.
- T-289 remains blocked by T-287/T-288 and is not unblocked by T-286 alone.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-287 — Artifact exposure-tier policy

Request: continue the S+ engineering sweep with unblocked local foundation work.

Squad role: Architecture reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Define how Artifact Registry records use `exposureTier` and `exportSafety`.
- Keep this doctrine-only: no access-control implementation, route changes,
  object-storage migration, registry service, DB schema, public copy, or runtime
  loader.
- Do not change runtime asset evidence or T-091/T-091A state.

### Fix

- Added `docs/architecture/artifact-exposure-tier-policy.md`.
- Set missing exposure metadata to fail closed as `internal_only`.
- Defined registry meaning and minimum controls for:
  `internal_only`, `partner_preview`, `authenticated_client`, `investor_demo`,
  `expert_review`, `public_marketing`, and `published_case_study`.
- Defined `exportSafety` states and promotion rules.
- Added tier-specific controls, storage/routing fail-closed rules,
  runtime-package exposure rules, future scanner checks, and review-record
  fields.
- Marked T-287 done in `docs/state/tasks.md`.

### Verified

- `git diff --check` passed with CRLF warnings only.
- Added-line unsafe-phrase sweep found no new hits.

### Remaining work

- T-288 remains blocked by T-263.
- T-289 remains blocked by T-288.
- T-210 remains broader exposure metadata integration and is not closed by this
  policy.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-193 — Claim lifecycle typed vocabulary

Request: continue the S+ engineering sweep with unblocked local foundation work.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata-only shared TypeScript vocabulary for Venue Claim Lifecycle.
- Keep lifecycle separate from Truth Mode source/verification states and Layout
  Proof Object claim statuses.
- Do not add DB schema, runtime UI, public copy, package rename, or runtime
  asset evidence changes.

### Fix

- Added `packages/types/src/claim-lifecycle.ts` with:
  - claim lifecycle states;
  - lifecycle events;
  - staleness triggers;
  - regeneration decisions;
  - actor categories;
  - source categories;
  - Zod schemas and inferred types for each vocabulary.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/claim-lifecycle.test.ts` covering pinned
  values, event/state separation, trigger/event separation, actor/source
  separation, metadata-only string shape, and separation from Truth Mode and
  Layout Proof Object axes.
- Marked T-193 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- claim-lifecycle truth-mode layout-proof-object`
  — 28 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-194 remains deferred for an actual staleness evaluator.
- T-196/T-197 remain deferred UI/workflow follow-ups.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-219 — Purpose-fit evidence vocabulary

Request: continue the S+ engineering sweep with unblocked local foundation work.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata-only shared vocabulary for Purpose-Fit Evidence.
- Keep purpose-fit separate from generic Truth Mode confidence tiers, Layout
  Proof claim statuses, and artifact exposure tiers.
- Do not add runtime UI, validators, DB schema, public copy, package rename, or
  runtime asset evidence changes.

### Fix

- Added `packages/types/src/purpose-fit.ts` with:
  - purpose categories;
  - purpose-fit statuses;
  - purpose evidence requirement refs;
  - purpose-specific confidence labels;
  - Zod schemas and inferred types for each vocabulary.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/purpose-fit.test.ts` covering pinned
  doctrine categories, status separation from Layout Proof claim statuses,
  confidence separation from Truth Mode confidence tiers, category separation
  from artifact exposure tiers, evidence requirement refs, and metadata-only
  string shape.
- Marked T-219 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- purpose-fit truth-mode layout-proof-object artifact-manifest`
  — 34 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-270 remains blocked by T-188 and T-256.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-188 — Assumption Ledger typed vocabulary

Request: continue the S+ engineering sweep by unblocking data-sufficiency
foundation work.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata-only shared vocabulary for Assumption Ledger.
- Keep assumption sources/assurance separate from Truth Mode source/confidence
  axes and Layout Proof claim statuses.
- Do not add validators, simulators, database schema, runtime UI, public copy,
  package rename, or runtime asset evidence changes.

### Fix

- Added `packages/types/src/assumption-ledger.ts` with:
  - assumption categories;
  - assumption sources;
  - assumption assurance bands;
  - stale triggers;
  - review requirements;
  - Zod schemas and inferred types for each vocabulary.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/assumption-ledger.test.ts` covering pinned
  doctrine values, source separation from Truth Mode source states, assurance
  separation from Truth Mode confidence tiers, stale-trigger separation from
  Layout Proof claim statuses, and metadata-only string shape.
- Marked T-188 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- assumption-ledger truth-mode layout-proof-object`
  — 26 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-256 remains the other prerequisite before T-270 can start.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-256 — Review gate typed vocabulary

Request: continue the S+ engineering sweep by unblocking data-sufficiency
foundation work.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata-only shared vocabulary for Review Gate Engine.
- Keep gate lifecycle/status tokens separate from Layout Proof claim statuses and
  Truth Mode source/verification axes.
- Do not add runtime UI, validator implementation, public copy, package rename,
  or runtime asset evidence changes.

### Fix

- Added `packages/types/src/review-gate.ts` with:
  - review reasons;
  - reviewer roles;
  - required-data categories;
  - blocking modes;
  - gate lifecycle statuses;
  - message-key families;
  - Zod schemas and inferred types for each vocabulary.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/review-gate.test.ts` covering pinned
  doctrine values, blocking/lifecycle separation from Layout Proof statuses,
  lifecycle separation from Truth Mode source/verification states,
  message-key families, and metadata-only string shape.
- Marked T-256 done in `docs/state/tasks.md`.
- Added the ready-to-start note to T-270 because T-188 and T-256 are now done.

### Verified

- `pnpm --filter @omnitwin/types test -- review-gate layout-proof-object truth-mode`
  — 27 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-270 is now ready.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-270 — Data sufficiency outcome vocabulary

Request: continue the S+ engineering sweep by adding the shared metadata
vocabulary from the Data Sufficiency Contract.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata-only shared vocabulary for DSC-001.
- Cover the surfaces named by doctrine: validator kernel, Layout Evidence Pack,
  Guest Flow Replay, Lighting Context, and Truth Mode.
- Keep data sufficiency outcomes separate from verdicts and trust/source-state
  labels.
- Do not add validator behavior, runtime UI, public copy, package rename, or
  runtime asset evidence changes.

### Fix

- Added `packages/types/src/data-sufficiency.ts` with:
  - the four data sufficiency outcomes;
  - emitting surfaces;
  - required-input categories from the doctrine examples;
  - message-key families;
  - Zod schemas and inferred types for each vocabulary.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/data-sufficiency.test.ts` covering pinned
  outcomes, internal surfaces, required input categories, rejection of
  `pass`/`fail`/`warn`, intentional overlap with Layout Proof `not_checked` and
  `requires_human_review`, separation from Truth Mode source/verification states,
  message-key families, and metadata-only string shape.
- Marked T-270 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- data-sufficiency layout-proof-object truth-mode review-gate`
  — 34 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-271 can now define the witness fields that consume this vocabulary.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-331 — Human Review Overlay schema

Request: continue the S+ engineering sweep with the next unblocked
proof-carrying stack foundation task.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add a strict metadata schema for human review overlays.
- Preserve the doctrine boundary that review overlays sit on top of machine
  witnesses and do not mutate replayable witness output.
- Connect review overlays to reviewer roles, claim lifecycle actor categories,
  affected claims/evidence, and lifecycle effect metadata.
- Do not add review queue behavior, database schema, validator code, runtime UI,
  public copy, package rename, or runtime asset evidence changes.

### Fix

- Added `packages/types/src/human-review-overlay.ts` with:
  - schema version;
  - review decisions;
  - reason categories;
  - scopes;
  - reference types;
  - lifecycle effects;
  - `HumanReviewReferenceSchema`;
  - strict `HumanReviewOverlaySchema`.
- The overlay records reviewer role, reviewer actor category, optional reviewer
  ID, decision, reason, review timestamp, scope, expiry, source witness refs,
  affected claims/evidence, lifecycle effect, replacement link, review note, and
  an explicit never-mutate witness policy.
- Added schema refinements so claim decisions must name affected claims, gate
  decisions must cite a review gate, and expiry must be later than review time.
- Exported the schema and vocabularies from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/human-review-overlay.test.ts` covering
  pinned vocabularies, strict parsing, required source witness refs, mutation
  field rejection, claim/gate decision requirements, expiry ordering, separation
  from lifecycle events and Layout Proof claim statuses, and metadata-only
  string shape.
- Marked T-331 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- human-review-overlay claim-lifecycle review-gate layout-proof-object`
  — 33 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- Follow-on stack schema tasks can now reference Human Review Overlay records
  instead of inventing ad hoc review fields.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-303 — Flow Zone type vocabulary

Request: continue the S+ engineering sweep with the next unblocked
metadata-only operational-geometry foundation task.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add typed metadata vocabulary for Flow Zone Authoring Layer kinds.
- Include customer visibility and downstream consumer classifications.
- Keep the work out of editor/runtime/compiler/simulator implementation.
- Do not add database schema, public copy, package rename, or runtime asset
  evidence changes.

### Fix

- Added `packages/types/src/flow-zone.ts` with:
  - `FLOW_ZONE_KINDS`;
  - `FLOW_ZONE_CUSTOMER_VISIBILITY_LEVELS`;
  - `FLOW_ZONE_CONSUMERS`;
  - total kind-to-visibility mapping;
  - total kind-to-consumer mapping;
  - Zod schemas and inferred types.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/flow-zone.test.ts` covering pinned
  FZAL-001 kinds, customer visibility levels, consumer classifications, total
  mapping coverage, separation from Layout Proof verdict statuses and distinct
  Guest Flow scenario labels, and metadata-only string shape.
- Marked T-303 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- flow-zone crowd-simulation-replay layout-proof-object`
  — 24 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-304/T-305 can now import shared zone kinds and mappings instead of
  re-declaring them.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-317 — Approximation vocabulary

Request: continue the S+ engineering sweep with the next unblocked
metadata-only operational evidence foundation task.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add typed metadata vocabulary for the Geometry Approximation Policy.
- Keep proof geometry approximation kinds separate from render/runtime layer
  names and Layout Proof claim statuses.
- Do not add a proof footprint generator, geometry library, validator behavior,
  runtime UI, public copy, package rename, or runtime asset evidence changes.

### Fix

- Added `packages/types/src/geometry-approximation.ts` with:
  - `GEOMETRY_APPROXIMATION_KINDS`;
  - `GEOMETRY_APPROXIMATION_METADATA_FIELDS`;
  - `GEOMETRY_APPROXIMATION_PURPOSES`;
  - `GEOMETRY_APPROXIMATION_CONSERVATISM_DIRECTIONS`;
  - Zod schemas and inferred types.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/geometry-approximation.test.ts` covering
  pinned GAP-001 kinds, required metadata field names, purpose categories,
  conservatism direction labels, separation from runtime visual layer names and
  Layout Proof claim statuses, and metadata-only string shape.
- Marked T-317 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- geometry-approximation runtime-venue-manifest layout-proof-object`
  — 42 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-318/T-320 can now import shared approximation vocabulary instead of
  re-declaring proof geometry labels.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-336 — 2.5D Event Object Semantics vocabulary

Request: continue the S+ engineering sweep with the next unblocked shared
contract needed before regulatory trigger tags.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata vocabulary for operational event-object semantics beyond visual
  mesh representation.
- Include footprint, height, top elevation, load estimate, heat output,
  temporary-structure status, stage/platform status, rigging requirement,
  floor-loading relevance, and human-review triggers.
- Keep the schema fail-closed when risk-relevant metadata appears without an
  explicit review trigger.
- Do not add editor behavior, compiler code, validator code, database schema,
  runtime UI, public copy, package rename, or runtime asset evidence changes.

### Fix

- Added `packages/types/src/event-object-semantics.ts` with:
  - `EVENT_OBJECT_OPERATIONAL_SCHEMA_VERSION`;
  - semantic metadata fields;
  - footprint kinds;
  - temporary-structure statuses;
  - stage/platform statuses;
  - rigging requirements;
  - floor-loading relevance;
  - heat-output relevance;
  - human-review triggers;
  - strict `EventObjectOperationalSemanticsSchema`.
- Added schema refinements so unsupported operational footprints, temporary
  structures, stage/platform use, rigging, floor-loading relevance, and
  heat-output relevance require explicit human-review trigger metadata.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/event-object-semantics.test.ts` covering
  pinned metadata fields, all vocabularies, basic parse behavior, fail-closed
  review-trigger requirements, visual-layer substitution rejection, strict
  unknown-field rejection, and metadata-only string shape.
- Marked T-336 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- event-object-semantics geometry-approximation runtime-venue-manifest layout-proof-object`
  — 49 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-337 can now add regulatory trigger tags on top of typed event-object
  semantics.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-337 — Regulatory Trigger Tags vocabulary

Request: continue the S+ engineering sweep with the follow-on metadata
vocabulary unlocked by T-336.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata-only regulatory trigger tags.
- Map every tag to Review Gate reason metadata and Event Object human-review
  trigger metadata.
- Preserve the rule that tags create review gates and review metadata, not
  software certification.
- Do not add editor behavior, validator behavior, database schema, runtime UI,
  public copy, package rename, or runtime asset evidence changes.

### Fix

- Added `packages/types/src/regulatory-trigger-tags.ts` with:
  - `REGULATORY_TRIGGER_TAGS`;
  - `REGULATORY_TRIGGER_REVIEW_OUTCOMES`;
  - total tag-to-review-gate-reason mapping;
  - total tag-to-event-object-human-review-trigger mapping;
  - Zod schemas and inferred types.
- Exported the vocabulary from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/regulatory-trigger-tags.test.ts` covering
  pinned tags, safe review outcomes, total mapping coverage, mapping values
  against Review Gate and Event Object schemas, rejection of pass/fail and
  approval-status leakage, and metadata-only string shape.
- Marked T-337 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- regulatory-trigger-tags event-object-semantics review-gate layout-proof-object`
  — 29 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- Future authoring/validator work can now cite shared regulatory tags and review
  mappings instead of inventing local strings.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-346 — Verified Tool Capability Registry

Request: continue the S+ engineering sweep with the next unblocked toolchain
governance foundation.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add a metadata schema for the Verified Tool Capability Registry.
- Track initial external tool IDs, capability status, usage mode, purpose,
  evidence refs, license-review status, fixture refs, installed version, and
  public-exposure allowance.
- Make `verified` status require evidence; do not claim that any real external
  tool has now been approved.
- Do not install dependencies, change package versions, add runtime code,
  integrate simulators, provide legal advice, change public copy, rename
  packages, or change runtime asset evidence state.

### Fix

- Added `packages/types/src/tool-capability-registry.ts` with:
  - `TOOL_CAPABILITY_REGISTRY_SCHEMA_VERSION`;
  - initial tool IDs for Spark, JuPedSim, Frosting, MILo, RVO2, SPZ, PlayCanvas
    splat-transform, Recast/Detour, Vadere, Pathfinder, MassMotion, and
    AnyLogic;
  - capability statuses;
  - usage modes;
  - purposes;
  - evidence-ref types;
  - license-review statuses;
  - `ToolCapabilityEvidenceRefSchema`;
  - strict `ToolCapabilityRecordSchema`.
- Added schema refinements so `verified` requires installed version,
  verification fixture ref, verification timestamp, and verification fixture
  evidence.
- Blocked public exposure for unverified, false, research-only, blocked,
  pending-review, benchmark-only, and commercial-license-needed states.
- Exported the registry schema from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/tool-capability-registry.test.ts`
  covering pinned tool IDs, status/usage/purpose/evidence/license vocabularies,
  separation from Artifact export-safety states, verified-evidence requirements,
  public-exposure blocking, benchmark simulator boundaries, and metadata-only
  string shape.
- Marked T-346 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- tool-capability-registry artifact-manifest`
  — 17 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- Actual tool verification still requires real fixture records, official-source
  license review, installed version capture, and evidence refs per tool.
- T-347 can now reference the registry schema when defining bridge
  verification records.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-237 — Scenario Template schema

Request: continue the S+ engineering sweep with the next unblocked Guest Flow
Replay contract.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata schema for reusable Guest Flow Replay Scenario Templates.
- Compose existing Crowd Simulation Replay, Assumption Ledger, Flow Zone, Data
  Sufficiency, and Layout Proof vocabularies.
- Keep templates separate from Scenario Instances and from actual simulator
  execution.
- Do not add simulator code, dependencies, database schema, replay UI, public
  copy, package rename, or runtime asset evidence changes.

### Fix

- Added `packages/types/src/scenario-template.ts` with:
  - `SCENARIO_TEMPLATE_SCHEMA_VERSION`;
  - required geometry classes;
  - route-input requirements;
  - endpoint semantics;
  - measurement units;
  - witness integration rules;
  - human-review triggers;
  - nested flow-zone, assumption, agent-profile, and measurement schemas;
  - strict `ScenarioTemplateV0Schema`.
- The schema records stable template identity/version, display name, venue
  applicability, flow category, required geometry classes, required flow zones,
  required assumptions, default profile mix, spawn/destination semantics,
  route-input requirement, measurement definitions, output metrics, witness
  claim families, witness integration rules, limitations, and review triggers.
- Added refinements so bar-queue templates require queue and wait-service flow
  zones, connector/navmesh requirements declare review behavior, and unsupported
  v0 route-input requests declare an unsupported-request review trigger.
- Exported the schema from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/scenario-template.test.ts` covering
  pinned vocabularies, reusable bar-queue parsing, queue/wait-service
  requirements, connector/navmesh review behavior, scenario-instance field
  rejection, status/string separation, and metadata-only string shape.
- Marked T-237 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- scenario-template crowd-simulation-replay assumption-ledger flow-zone layout-proof-object`
  — 37 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-238 can now define Scenario Instance records against this template schema.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-171 — Canonical layout snapshot v0 implementation

Request: continue the S+ engineering sweep by unblocking Scenario Instance and
future evidence-pack work.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add shared TypeScript/Zod schemas for Canonical Layout Snapshot v0.
- Add a deterministic local canonical JSON helper and domain-separated SHA-256
  digest helper.
- Keep the helper browser-safe for `@omnitwin/types`; no Node `crypto` import.
- Preserve polygon vertex order while sorting only documented unordered arrays.
- Reject temporary planner IDs and digest self-inclusion.
- Do not add validators, solver behavior, database schema, proof signing,
  runtime/editor behavior, public copy, package rename, or runtime asset
  evidence changes.

### Fix

- Added `packages/types/src/canonical-layout-snapshot.ts` with:
  - schema/domain constants;
  - Canonical JSON value schema;
  - source-state, assumption-source, room-geometry-source, and generator-type
    vocabularies;
  - units and tolerance schemas;
  - placed-object asset snapshot schema;
  - event metadata schema;
  - scenario assumption schema;
  - venue/runtime reference schema;
  - policy bundle reference schema;
  - generator provenance schema;
  - strict `CanonicalLayoutSnapshotV0Schema`;
  - `CANONICAL_LAYOUT_SNAPSHOT_V0_FIXTURE`;
  - `normalizeCanonicalLayoutSnapshot`;
  - `stableCanonicalJson`;
  - `canonicalLayoutSnapshotJson`;
  - browser-safe `sha256Hex`;
  - domain-separated `canonicalLayoutSnapshotDigest`.
- Normalization sorts objects by `objectId`, sorts scenario assumptions by
  category/source/stable value, rounds position to millimetres, rotation to
  five decimals, and scale to three decimals.
- Floor-plan outline array order is preserved because polygon order is
  geometry, not metadata ordering.
- Exported the schema and helpers from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/canonical-layout-snapshot.test.ts`
  covering fixture parsing, canonical JSON key sorting and array order,
  SHA-256 known vectors, pose normalization, stable digest across object and
  assumption input ordering, polygon-order digest sensitivity, temporary
  planner-state rejection, local ID rejection, and digest self-inclusion
  rejection.
- Marked T-171 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- canonical-layout-snapshot layout-proof-object configuration space`
  — 277 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-238 can now define Scenario Instance records using snapshot digests.
- Future API/web work still needs an adapter that builds snapshots only from
  saved persisted configuration rows; T-171 only adds the shared contract and
  deterministic helpers.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-238 — Scenario Instance schema

Request: continue the S+ engineering sweep by defining the Scenario Instance
contract now that T-171 and T-237 are complete.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata schema for specific Guest Flow Replay Scenario Instances.
- Cite Scenario Template identity/version, canonical layout snapshot digest,
  runtime package hash, policy bundle, assumption refs, route/navmesh model,
  simulator metadata, seed policy, artifacts, metrics, witness ref, lifecycle,
  and staleness triggers.
- Keep Scenario Instances separate from reusable Scenario Templates.
- Do not add simulator code, database tables, runtime replay UI, public copy,
  package rename, or runtime asset evidence changes.

### Fix

- Added `packages/types/src/scenario-instance.ts` with:
  - `SCENARIO_INSTANCE_SCHEMA_VERSION`;
  - route-model type vocabulary;
  - seed-policy vocabulary;
  - artifact-ref type vocabulary;
  - staleness-trigger vocabulary;
  - assumption, policy-bundle, route-model, simulator, seed, artifact, and
    metric-summary schemas;
  - strict `ScenarioInstanceV0Schema`.
- Added refinements so simulation/metrics/replay lifecycle states require
  simulator metadata, metrics-ready/replay-ready states require metrics,
  replay-ready states require trajectory artifacts, witness refs must use
  `witness_block`, and unsupported route models must emit `unsupported_request`.
- Exported the schema from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/scenario-instance.test.ts` covering pinned
  vocabularies, replay-ready parsing, lifecycle/simulator requirements,
  metrics/trajectory requirements, seed-policy rules, witness ref typing,
  unsupported-route data sufficiency, template-only field rejection, invalid
  hash rejection, and metadata-only string shape.
- Marked T-238 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- scenario-instance scenario-template canonical-layout-snapshot crowd-simulation-replay data-sufficiency`
  — 37 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-243 can now define the `.venreplay.zip` artifact schema using Scenario
  Template and Scenario Instance records.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-243 — `.venreplay` v0 artifact schema

Request: continue the S+ engineering sweep by defining the portable replay
artifact contract now that `.venreplay.zip` doctrine and Scenario Instance
records exist.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add a metadata-only v0 schema for `.venreplay.zip` manifests.
- Cover required artifact contents, scenario template/instance references,
  layout/runtime hashes, policy bundle digest, simulator metadata, seed policy,
  assumptions, limitations, file hashes, CSV contracts, GeoJSON contracts,
  metrics shape, witness compatibility, and exposure tier.
- Do not add a zip writer/parser, simulator execution, browser replay loader,
  database schema, API/runtime code, public copy, or runtime asset evidence
  changes.

### Fix

- Added `packages/types/src/venreplay-artifact.ts` with:
  - `VENREPLAY_ARTIFACT_SCHEMA_VERSION`;
  - portable required/optional file paths and file roles;
  - internal/review/client exposure tier vocabulary for v0;
  - CSV contract and required-column vocabularies;
  - GeoJSON feature contract vocabularies;
  - witness compatibility requirements;
  - file hash, metrics shape, CSV contract, GeoJSON contract, and strict
    `VenreplayManifestV0Schema` schemas;
  - `venreplaySeedCount` helper.
- Added refinements rejecting missing, duplicated, or mislabelled file hashes;
  required-flag mismatches; non-replay-ready scenario instances; missing witness
  refs; scenario/template/layout/runtime/policy/simulator/seed mismatches;
  assumption drift; incomplete CSV, GeoJSON, and witness contracts; missing
  metric coverage; and public-facing exposure tiers for v0.
- Exported the schema and vocabularies from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/venreplay-artifact.test.ts` covering
  vocabulary pinning, valid manifest parsing, required file handling,
  non-portable path rejection, scenario identity checks, replay/witness/simulator
  requirements, seed consistency, assumption exactness, contract completeness,
  strict metadata shape, and v0 exposure gating.
- Marked T-243 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- venreplay-artifact`
  — 9 tests passed.
- `pnpm --filter @omnitwin/types test -- venreplay-artifact scenario-instance scenario-template crowd-simulation-replay artifact-manifest data-sufficiency`
  — 47 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types test`
  — 51 files and 1400 tests passed.
- `pnpm --filter @omnitwin/types build`

### Remaining work

- T-244 still needs deterministic logical artifact digest policy.
- T-246 still needs full artifact validation against actual file contents.
- Browser replay loading remains deferred to T-245.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-244 — `.venreplay` hash policy

Request: continue the S+ engineering sweep by implementing the deterministic
logical digest policy for `.venreplay.zip` artifacts.

Squad role: Shared-contract reviewer + TypeScript reviewer.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Define and implement the v0 logical artifact digest material for portable
  replay artifacts.
- Reuse the existing canonical JSON and SHA-256 helpers from Canonical Layout
  Snapshot.
- Prove zip timestamps, compression choice, platform metadata, central-directory
  ordering, raw manifest serialization hash churn, and file-list ordering do not
  affect logical replay identity.
- Do not add a zip parser/writer, simulator code, browser replay loader,
  database schema, API/runtime code, public copy, or runtime asset evidence
  changes.

### Fix

- Extended `packages/types/src/venreplay-artifact.ts` with:
  - `VENREPLAY_LOGICAL_DIGEST_POLICY_VERSION`;
  - `VENREPLAY_LOGICAL_DIGEST_DOMAIN_PREFIX`;
  - payload file-hash schema;
  - manifest digest identity schema;
  - logical digest material schema;
  - manifest identity normalization;
  - ordered payload file-hash extraction;
  - canonical digest JSON generation;
  - `venreplayLogicalArtifactDigest`.
- Logical digest material now includes the parsed manifest identity excluding
  `fileHashes`, normalized semantically unordered replay arrays, and
  path-ordered payload file hashes.
- Corrected the self-referential manifest hash trap: `manifest.json` is a
  required archive file, but it is not allowed inside its own `fileHashes`.
  Validators compute and report its raw hash separately.
- Updated `docs/architecture/venreplay-artifact.md` with the exact T-244 digest
  rule.
- Exported the digest constants, schemas, helpers, and types from
  `packages/types/src/index.ts`.
- Extended `packages/types/src/__tests__/venreplay-artifact.test.ts` with
  digest-policy regressions for ordered payload hash material, stable digest
  across manifest/file-list ordering, zip metadata neutrality, raw manifest-hash
  neutrality, and digest changes for payload or replay identity changes.
- Marked T-244 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- venreplay-artifact`
  — 13 tests passed.
- `pnpm --filter @omnitwin/types test -- venreplay-artifact canonical-layout-snapshot scenario-instance scenario-template crowd-simulation-replay`
  — 43 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`
- `pnpm --filter @omnitwin/types test`
  — 51 files and 1404 tests passed.

### Remaining work

- T-246 can now add full replay artifact validation against actual file contents.
- T-245 browser replay loading remains deferred.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-246 — Replay artifact validator

Request: continue the S+ engineering sweep by adding an in-memory validator for
extracted `.venreplay.zip` artifact contents.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Validate extracted archive entries without adding a zip parser/writer or
  runtime/browser loader.
- Verify required files, allowed paths, payload hashes and byte sizes, manifest
  shape, scenario consistency, witness consistency, assumptions, limitations,
  logical artifact digest availability, and unsupported public-claim wording.
- Keep the implementation in the shared type layer for future API, worker, and
  browser consumers.
- Do not add simulator code, database schema, API routes, runtime code, public
  copy, public claims, package rename, or runtime asset evidence changes.

### Fix

- Added `packages/types/src/venreplay-validator.ts` with:
  - `VENREPLAY_WITNESS_SCHEMA_VERSION`;
  - structured text/JSON file vocabularies;
  - validation issue-code vocabulary;
  - `VenreplayWitnessV0Schema`;
  - in-memory artifact file/result types;
  - `validateVenreplayArtifact`.
- The validator rejects unknown paths, duplicate files, missing required files,
  invalid UTF-8 structured text, malformed JSON, invalid manifest/scenario/
  witness schemas, payload hash mismatches, byte-size mismatches, missing hash
  entries for present payload files, scenario drift, witness drift, and
  unsupported public-claim wording in portable structured files.
- The validator computes and reports the raw `manifest.json` hash and byte size
  separately, then computes the logical artifact digest from the parsed manifest.
- Corrected `packages/types/src/venreplay-artifact.ts` so
  `manifest.json.fileHashes` covers payload files only; `manifest.json` remains
  a required archive file but cannot be listed in its own hash list.
- Exported the validator API from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/venreplay-validator.test.ts` with
  self-contained payloads whose manifest hashes are generated from actual file
  contents.
- Updated `docs/architecture/venreplay-artifact.md` and `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- venreplay-artifact venreplay-validator`
  — 23 tests passed.
- `pnpm --filter @omnitwin/types test -- venreplay-artifact venreplay-validator canonical-layout-snapshot scenario-instance scenario-template crowd-simulation-replay`
  — 53 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`
- `pnpm --filter @omnitwin/types test`
  — 52 files and 1414 tests passed.

### Remaining work

- T-245 browser replay loading remains deferred.
- T-247 can now add a tiny deterministic fixture.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-247 — Replay artifact fixture

Request: continue the S+ engineering sweep by adding a tiny deterministic
`.venreplay` fixture for tests and future browser-loader work.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Create a clearly synthetic/internal replay fixture with minimal geometry,
  three agents, short trajectories, metrics, bottleneck geometry, witness
  material, manifest, extracted files, and deterministic zip bytes.
- Keep it in `@omnitwin/types` so API, worker, web, and future loader tests can
  import one canonical fixture.
- Do not add real venue evidence, simulator execution, browser replay loading,
  database schema, API/runtime code, public copy, public claims, package rename,
  or runtime asset evidence changes.

### Fix

- Added `packages/types/src/venreplay-fixture.ts` with:
  - `VENREPLAY_SYNTHETIC_FIXTURE_ID`;
  - `.venreplay.zip` fixture filename;
  - fixed generated timestamp;
  - fixed DOS timestamp fields for zip metadata;
  - synthetic scenario instance;
  - synthetic witness;
  - payload files;
  - manifest;
  - extracted artifact files;
  - raw manifest hash/size;
  - logical artifact digest;
  - deterministic stored zip bytes;
  - zip hash;
  - `VENREPLAY_SYNTHETIC_FIXTURE`.
- The fixture includes `geometry.geojson`, `scenario.json`, `agents.csv`,
  `trajectory.csv`, `metrics.json`, `bottlenecks.geojson`, `witness.json`, and
  generated `manifest.json`.
- The zip bytes are generated without a production zip dependency: path-sorted
  stored entries, CRC-32, fixed timestamp fields, central directory, and end
  record.
- Exported the fixture API from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/venreplay-fixture.test.ts` covering
  synthetic/internal metadata, minimum file contents, validator success,
  manifest self-hash exclusion, deterministic zip structure, zip hash, fixed
  zip timestamps, and unsupported public-claim wording absence.
- Marked T-247 done in `docs/state/tasks.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- venreplay-fixture venreplay-artifact venreplay-validator`
  — 29 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types test -- venreplay-fixture venreplay-artifact venreplay-validator canonical-layout-snapshot scenario-instance scenario-template crowd-simulation-replay`
  — 59 tests passed.
- `pnpm --filter @omnitwin/types build`
- `pnpm --filter @omnitwin/types test`
  — 53 files and 1420 tests passed.

### Remaining work

- T-245 browser replay loading remains deferred.
- T-248 replay evidence integration remains deferred.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-252 — Fixed-light calibration-frame checklist

Request: continue the S+ engineering sweep by taking the next unblocked
real-capture foundation item.

Squad role: Documenter + Tester + Architect.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Create the fixed-light calibration-frame checklist required by the
  Photometric Chain-of-Custody doctrine.
- Keep it documentation/checklist only.
- Do not implement capture tooling, residual training, runtime loading, public
  copy, public claims, package rename, or T-091/T-091A state changes.

### Fix

- Added `docs/operations/fixed-light-calibration-frame-checklist.md`.
- The checklist covers capture session header, lighting-state lock, camera
  setting lock, start/end grey-card and ColorChecker calibration frames,
  flicker test, main capture discipline, raw-file preservation, frame-role
  review, excluded-frame reasons, operator/reviewer signoff, stop conditions
  before training, and handoff to raw hashes, train/holdout/challenge splits,
  residual capture manifest, Appearance Capture QA Pack, and future
  AssetVersion/RuntimePackage registration.
- Marked T-252 done in `docs/state/tasks.md` and added a task-graph note for
  the T-249 through T-254 cluster.

### Verified

- Architecture context checked: Photometric Chain-of-Custody, Residual Radiance
  Layer, RRL-001 fixed-light zone plan, D-014, D-024, and audit headings.
- Checklist remains internal/operator wording and does not claim any room asset
  is processed, verified, signed, or loaded.

### Remaining work

- T-250 remains blocked/not ready because it depends on T-147, which is still
  deferred.
- T-253 raw-file hash tool and T-251 holdout split script remain future work.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-297 — Operational GeoJSON schema

Request: continue the S+ engineering sweep by taking the next unblocked typed
foundation item after T-252.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add a shared schema-level operational GeoJSON contract for the Operational
  Geometry Compiler track.
- Cover room boundaries, walkable areas, obstacles, furniture footprints,
  clearance envelopes, doors, portals, connectors, queue/spawn/goal zones,
  staff/service zones, heritage-restricted zones, accessibility connectors, and
  unknown/unverified areas.
- Include coordinate frame, units, stable feature IDs, provenance/source refs,
  purpose, policy/assumption refs, object refs, connected feature refs,
  approximation metadata, and data sufficiency fields.
- Do not implement compiler behavior, solver behavior, simulation, runtime UI,
  public copy, package rename, or T-091/T-091A state changes.

### Fix

- Added `packages/types/src/operational-geometry.ts` with:
  - `OPERATIONAL_GEOMETRY_SCHEMA_VERSION`;
  - operational feature class, purpose, source-kind, GeoJSON geometry-type, and
    data-sufficiency status vocabularies;
  - strict 2D GeoJSON geometry schemas for Point, MultiPoint, LineString,
    MultiLineString, Polygon, and MultiPolygon;
  - reference, data-sufficiency, feature-property, feature, collection metadata,
    and `OperationalGeoJsonFeatureCollectionV0Schema` schemas;
  - class-to-geometry-type validation and refinements for ID drift, duplicate
    IDs, missing connector links, missing object refs, unknown-area sufficiency,
    and collection/feature unit/frame mismatch.
- Exported the contract from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/operational-geometry.test.ts` covering
  vocabulary pinning, valid collection parsing, visual-layer/status separation,
  invalid geometry/class combinations, unsupported GeoJSON geometry collections,
  polygon closure, duplicate IDs, missing connector/object refs, and hidden data
  insufficiency rejection.
- Marked T-297 done in `docs/state/tasks.md` and added a task-graph note for
  the T-296 through T-301 cluster.

### Verified

- `pnpm --filter @omnitwin/types test -- operational-geometry`
  — 10 tests passed.
- `pnpm --filter @omnitwin/types test -- operational-geometry data-sufficiency geometry-approximation flow-zone event-object-semantics`
  — 37 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`
- `pnpm --filter @omnitwin/types test`
  — 54 files / 1430 tests passed.
- `pnpm typecheck`
- Diff whitespace and unsafe-claim sweeps passed.

### Remaining work

- T-298 walkable-area compiler spike, T-299 geometry hash, T-300 polygon
  validity tests, and T-301 operational geometry data-sufficiency checks remain
  future tasks.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.

## T-299 — Operational geometry hash

Request: continue the S+ engineering sweep by implementing the next unblocked
Operational Geometry Compiler foundation item after T-297.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Define the shared operational geometry hash policy over valid
  `OperationalGeoJsonFeatureCollectionV0` material.
- Include compiler version, layout snapshot digest, canonical room geometry
  refs, furniture footprint refs, venue zones, portals/connectors, policy refs,
  scenario assumptions, source refs, data sufficiency, geometry, and
  deterministic output ordering.
- Keep this metadata/schema only: no compiler, solver, runtime UI, simulation
  dependency, public copy, package rename, or T-091/T-091A state change.

### Fix

- Added `OPERATIONAL_GEOMETRY_HASH_POLICY_VERSION` and
  `OPERATIONAL_GEOMETRY_HASH_DOMAIN_PREFIX` in
  `packages/types/src/operational-geometry.ts`.
- Added `OperationalGeometryHashFeatureSchema` and
  `OperationalGeometryHashMaterialV0Schema`.
- Added `operationalGeometryHashMaterial`, `operationalGeometryHashJson`, and
  `operationalGeometryHash`.
- The material builder parses the operational GeoJSON contract, sorts set-like
  arrays and features deterministically, dedupes references, keeps geometry
  coordinate order identity-bearing, and excludes `generatedAt` plus operator
  notes from the hash identity.
- Exported the hash policy surface from `packages/types/src/index.ts`.
- Extended `packages/types/src/__tests__/operational-geometry.test.ts` with
  regressions for material shape, stable ordering, ignored annotations, and
  drift across compiler version, layout hash, policy refs, assumption refs,
  source object refs, and geometry.
- Marked T-299 done in `docs/state/tasks.md` and updated the task-graph note
  for the T-296 through T-301 cluster.

### Verified

- `pnpm --filter @omnitwin/types test -- operational-geometry`
  — 14 tests passed.

### Remaining work

- T-298 walkable-area compiler spike, T-300 polygon validity tests, and T-301
  operational geometry data-sufficiency checks remain future tasks.
- **T-091 / T-091A remain not-started** — no real captured room asset is loaded
  in runtime and no evidence claim was changed.
