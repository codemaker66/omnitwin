# Session log — 2026-06-11

Request (Blake): resolve the tasks.md/session-log merge conflicts, verify/apply
migration 0024 (T-434), then build the T-427 proposal/quote schema layer
(phase 1, schema only — no routes, no UI).

Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

## Merge conflict resolution — docs source of truth repaired

`docs/state/tasks.md` and `docs/sessions/2026-06-07.md` carried unresolved
`<<<<<<< ours` / `=======` / `>>>>>>> theirs` conflict blocks (no `MERGE_HEAD`
existed — only stale unmerged index entries). Both were union-merged, keeping
every row/section from both sides:

- `tasks.md`: markers at lines 89/99/116. "ours" = T-441–T-449 (9 rows),
  "theirs" = T-425–T-440 (16 rows). Merged in ascending task-ID order
  (T-425…T-449 after T-424). Verified all 25 rows present exactly once, zero
  duplicates.
- `2026-06-07.md`: markers at lines 55/2194/2271. Shared 54-line header, then
  "theirs" (T-425 acquisition review, 76 lines) placed before "ours"
  (T-441–T-449 strand, 2138 lines) for task-ID order, one blank line at the
  seam. Both "T-091/T-091A remain not-started" closers preserved.
- Byte-careful: CRLF endings and no-BOM UTF-8 preserved on both files.
- Verified repo-wide: zero conflict-marker lines remain (the `^<<<<<<<`,
  `^>>>>>>>`, and `^=======$` patterns all return no matches).
- Both files staged (`git add`), clearing the `UU` index state. Not committed.

## T-434 — runtime-asset migration 0024 applied and verified

Target: the configured database from `packages/api/.env` `DATABASE_URL` —
Neon host `ep-dawn-glitter-abx2ux6f-pooler.eu-west-2.aws.neon.tech`, db
`neondb` (eu-west-2). The Neon MCP server is installed but unauthenticated and
its OAuth flow needs an interactive browser, so verification ran over the same
`DATABASE_URL` the API itself uses, via a new read-only operator script
`packages/api/src/scripts/verify-migration-0024.ts`
(`pnpm --filter @omnitwin/api tsx src/scripts/verify-migration-0024.ts`).

**Before:** 0/5 runtime-asset tables existed; `drizzle.__drizzle_migrations`
showed 22 applied (latest 2026-05-02) versus 23 entries in the local journal —
exactly `0024_runtime_assets` pending. **Not applied.**

**Apply:** `pnpm --filter @omnitwin/api db:migrate` (the canonical journal-
driven runner, same command the deploy workflow uses) → "migrations applied
successfully".

**After (catalog evidence, machine checked):**

- tables present 5/5: `asset_versions`, `capture_sessions`, `processing_jobs`,
  `room_manifests`, `runtime_packages`
- drizzle journal: 23 applied, latest entry 2026-05-03 (journal `when` of
  `0024_runtime_assets`)
- constraints: capture_sessions 5 (incl. `capture_sessions_capture_source_check`,
  `capture_sessions_status_check`, both slug-shape CHECKs); asset_versions 16
  (incl. `asset_versions_r2_key_unique`, `asset_versions_external_url_unique`,
  `asset_versions_storage_ref_required`, `asset_versions_no_fixture_keys`,
  `asset_versions_sha256_check`, `asset_versions_file_ext_check`, kind/source/
  evidence/runtime-status CHECKs, capture-session FK); room_manifests 5 (incl.
  `room_manifests_venue_room_unique`, `room_manifests_alignment_status_check`);
  runtime_packages 11 (incl. `runtime_packages_manifest_shape`,
  `runtime_packages_loadable_primary_visual_check`, four asset FKs);
  processing_jobs 7 (incl. `processing_jobs_processor_check`,
  `processing_jobs_status_check`, source-asset FK)
- 20 indexes across the five tables
- row counts: all five tables **0 rows** — no AssetVersion rows registered, no
  fixture or demo data, exactly as instructed

T-091 / T-091A remain not-started and untouched: the registry is live but
empty; no real captured Trades Hall asset exists, loads, or is claimed.

**⚠ Flag — unjournaled migration 0025 (pre-existing, out of scope, needs a
decision):** `packages/api/drizzle/0025_configuration_revisions.sql` exists on
disk but has NO entry in `drizzle/meta/_journal.json`, and the deploy workflow
applies migrations via journal-driven `drizzle-kit migrate`
(`.github/workflows/deploy.yml` "Apply pending Drizzle migrations"), so 0025
can never ship through deploys. Confirmed against the configured DB:
`configurations.revision` column and `configuration_layout_revisions` table
are both ABSENT. Current HEAD's batch-save path requires `revision`
(commit `8e4f1f5`, 2026-06-07), so when this branch merges and deploys,
planner saves would fail against this database. Recommended follow-up task:
journal (idx after 0026) and apply 0025 — not done in this session because the
instruction was scoped to 0024 with no scope creep.

## T-427 phase 1 — proposal/quote schema layer (schema only)

First-class proposal/quote domain objects, schema layer only — no API routes,
no UI, no public copy. House patterns followed throughout: status history,
exact minor-unit money, venue scoping, soft delete.

- `packages/types/src/proposal.ts` — 8-status proposal vocabulary
  (draft/sent/changes_requested/accepted/declined/expired/withdrawn/archived)
  and 6-status quote vocabulary with transition matrices + helpers mirroring
  the enquiry house pattern; `MinorUnitAmountSchema` (integer pence, £1M
  ceiling matching pricing MAX_PRICE); line-item and quote schemas with
  refinements enforcing lineTotal = unit × quantity and subtotal = Σ line
  totals exactly; `ProposalVersionPayloadSchema` with a claim guard rejecting
  unsupported certainty phrases ("fire approved", "certified safe", "legally
  compliant", "survey-grade", "approved for occupancy", "guaranteed
  accessible", "production ready", "photoreal digital twin") in client-facing
  text at the schema boundary; `proposalVersionPayloadDigest` — domain-prefixed
  SHA-256 over stable canonical JSON, reusing the canonical-layout-snapshot
  hash utilities. Exports wired into the types index.
- `packages/api/src/db/schema.ts` — five new tables: `proposals` (venue FK,
  enquiry/configuration links, share_code nanoid-6, current_version,
  soft delete), `proposal_versions` (immutable payload snapshots, gapless
  unique version, 64-hex source_hash — the sheet-snapshot pattern),
  `proposal_status_history` (the enquiry history pattern), `quotes`
  (integer minor-unit subtotal/total, GBP, superseded_by self-reference),
  `quote_line_items` (exact-total CHECK, pricing-rule provenance link).
- `packages/api/drizzle/0026_proposals_quotes.sql` — idempotent 0024-style
  migration with 18 named constraints (incl. `quote_line_items_total_exact`
  enforcing exact money at the DB level) + journal entry idx 23, so local
  `db:migrate` and journal-driven deploys will apply it. NOT yet applied to
  the database in this session.
- Tests: `packages/types/src/__tests__/proposal.test.ts` (vocabularies,
  transitions, money bounds, exact-total refinements, claim guard incl.
  case-insensitivity, digest stability/key-order independence, row schemas)
  and `packages/api/src/__tests__/proposals-schema.test.ts` (table/column
  contracts, integer-only money columns, types↔SQL status CHECK drift guards,
  named-constraint and index presence, journal registration with unique
  ascending idx).
- Also fixed three `@typescript-eslint/no-base-to-string` lint errors in
  `verify-migration-0024.ts` (typed `displayScalar` formatter) and re-ran the
  script against the DB to confirm identical output.

Verified: types build + lint + full suite (58 files / 1529 tests, zero type
errors); api typecheck + lint + full suite (57 files / 1659 tests); operator
script re-run green. Suite totals include the parallel T-330 event-phase-graph
strand present in the working tree.

Remaining for T-427 (phase 2+): API routes with state-machine enforcement and
venue scoping, share-link client proposal page, client comments,
approve/request-changes flow, capacity intelligence integration (T-429), and
applying migration 0026. tasks.md updated: T-427 → in-progress, T-434 → done.

## T-330 — Event Phase Graph foundation

Implemented the first real Event Phase Graph foundation without simulation or
unsafe claims:

- Added shared strict Zod/TypeScript contracts in
  `packages/types/src/event-phase-graph.ts` for events, event phases, event
  scenarios, layout variants, event configuration links, phase layout snapshots,
  status vocabularies, graph payloads, and the default Arrival/Ceremony/Room
  Flip/Dinner/Speeches/Bar Queue/Dancing/Breakdown template.
- Added Drizzle schema and migration `0027_event_phase_graph.sql` for
  `events`, `event_phases`, `event_scenarios`, `layout_variants`,
  `event_configuration_links`, and `phase_layout_snapshots`.
- Added API routes for event create/read/update, phase create/update, scenario
  create, layout variant create, and phase graph readback.
- Updated `/dev/trades-hall-visual` so Event Phase Graph reads runtime event
  phase data when `eventId` is present and falls back only to the internal demo
  fixture otherwise.
- Kept density and staff conflict output as explicit not-checked planning
  placeholders; no safety/compliance/simulation claims were added.

Verified:

- `pnpm --filter @omnitwin/types exec vitest run src/__tests__/event-phase-graph.test.ts`
- `pnpm --filter @omnitwin/api exec vitest run src/__tests__/events.test.ts src/__tests__/schema.test.ts`
- `pnpm --filter @omnitwin/web exec vitest run src/__tests__/TradesHallVisualPage.test.tsx`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/api typecheck`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/api exec vitest run src/__tests__/routes-inventory.test.ts`
- Product-code unsafe wording scan over the changed event phase graph files:
  no matches.

Not performed: no database migration was applied to a live/staging database in
this slice, no simulation engine was built, and no public/customer claim was
changed.

## 0025 journal gap resolved — migrations 0025/0026/0027 applied

The flag raised under T-434 is closed. `0025_configuration_revisions.sql` was
already fully idempotent (IF NOT EXISTS / guarded constraints), so no SQL
changes were needed — only journal registration:

- `drizzle/meta/_journal.json` now registers 0025 at idx 23
  (`when` 2026-06-10, between 0024 and 0026) and renumbers
  0026_proposals_quotes → idx 24, 0027_event_phase_graph → idx 25.
  Renumbering is safe: none of the three had been applied via journal in any
  environment (the DB's last applied row was 0024, and these entries exist
  only in this uncommitted working tree).
- `proposals-schema.test.ts` journal assertions converted from a hard-coded
  idx to order-based guards (0024 < 0025 < 0026) so the 0025 registration is
  itself test-enforced.
- Applied via `pnpm --filter @omnitwin/api db:migrate` → verified
  (read-only catalog check, then temp script deleted): all 12 tables from the
  three migrations present (`configuration_layout_revisions`, the five
  proposal/quote tables, the six event-phase-graph tables),
  `configurations.revision` column present, journal rows applied = 26.

The production landmine — journal-driven deploys never applying 0025 while
HEAD's batch-save requires `revision` — no longer exists.

## T-427 phase 2 — proposal/quote API routes

Schema layer (phase 1) now has its API. No UI yet; SAFE language throughout.

- `packages/api/src/state-machines/proposal.ts` — role policy layered over
  the @omnitwin/types structural matrices, mirroring state-machines/enquiry.ts:
  staff/admin drive the sales lifecycle (send, withdraw, expire, supersede,
  archive); client/planner may only accept / decline / request changes on a
  sent proposal; admin retains the house any-transition override. Exposed
  drift-guard helpers prove every role-policy key is structurally legal.
- `packages/api/src/routes/proposals.ts` — `/proposals` CRUD (venue-scoped:
  admin anywhere, staff own venue; reads via house canAccessResource), soft
  delete with accepted-proposal lock, `POST /:id/transition` with state-machine
  enforcement + `proposal_status_history` audit row + sent_at coherence
  (including admin-override backfill so DB CHECKs hold) + first-send share-code
  minting via the house `generateUniqueShortCode`, `GET /:id/history`,
  `GET /:id/available-transitions`, and immutable version snapshots:
  `POST /:id/versions` (claim-guarded payload → 400 on unsupported certainty
  wording, server-side `proposalVersionPayloadDigest` source hash, atomic
  version-number claim) + `GET /:id/versions/latest` / `/:version`.
  Send is blocked (422 `PROPOSAL_HAS_NO_VERSION`) until a version exists.
- Public share link: `GET /public/proposals/:shareCode` — no auth (the code is
  the capability), only client-visible statuses resolve (draft/withdrawn/
  archived 404 indistinguishably), response is the client-safe shape only
  (title, status, sentAt, venue name, clientMessage, capacityNote, quote
  snapshot — no internal IDs or layout references).
- `packages/api/src/routes/quotes.ts` — `/quotes` CRUD with totals computed
  exclusively server-side via the exact minor-unit engine (`multiplyMinor` /
  `sumMinor`, ceiling check 422 `QUOTE_TOTAL_EXCEEDS_LIMIT`), quote + line
  items inserted in one transaction, draft-only edits/deletes (issued quotes
  are commercial records — supersede instead), and transition handling where
  `superseded` requires a same-venue, non-self successor reference.
- Registered in `packages/api/src/index.ts` under `/proposals`, `/quotes`,
  and `/public`.
- Tests: `packages/api/src/__tests__/proposals-routes.test.ts` — state-machine
  role matrix + structural drift guards, 401/400/403 boundaries, venue-scoping
  rejections (staff of venue B cannot create for venue A), claim-guard
  rejection of "fire approved" wording at the route boundary, share-code shape
  validation, and source-grep guards (money engine usage, no parseFloat/
  toFixed, share-code/digest/history provenance).

Verified: api typecheck clean, lint clean, full suite 58 files / 1683 tests
green (includes the parallel T-330 strand). tasks.md T-427 note updated.

Remaining for T-427 (phase 3): client proposal web page on the share link,
client comments, approve/request-changes UI, capacity intelligence surfacing
(T-429), and proposal/quote staff UI in the dashboard.

## T-427 phase 3 — client-facing proposal page (verification completed 2026-06-12)

The share link now has its page. Client-safe and SAFE-language by
construction; no internal IDs, staff vocabulary, or certainty claims render.

- `packages/api/src/routes/proposals.ts` — added
  `POST /public/proposals/:shareCode/respond` (`action: accept |
  request_changes`, optional note ≤1000 chars). The share code is the
  capability; the endpoint drives the SAME state machine as the
  authenticated transition route under the "client" role (sent →
  accepted | changes_requested only), writes a `proposal_status_history`
  row with `changedBy: null` (anonymous share-link response) carrying the
  client's note to staff via `/:id/history`, and answers 404
  indistinguishably for unknown/draft/withdrawn/archived codes and 422
  when the proposal is not awaiting a response.
- `packages/web/src/api/proposals.ts` — boundary-validated client
  (`PublicProposalSchema` reusing `QuoteSnapshotSchema` from
  @omnitwin/types, T-422 house rule); `getPublicProposal` +
  `respondToProposal` (public POST via skipAuth).
- `packages/web/src/pages/ProposalPage.tsx` — `/proposal/:shareCode`
  (lazy route in `router.tsx`, public): graphite/gold/cream presentation,
  loading frame, plain-English unavailable state, client message, quote
  table formatted from integer minor units via Intl.NumberFormat (money
  stays integers in transit), capacity note labelled "planning estimate",
  status banners for accepted/changes_requested/declined/expired, Accept +
  Request-changes actions only while status is `sent` (request-changes
  requires a non-empty note; double-submit guarded), retryable error
  message on failed responses, and a standing footer disclosure: figures
  are planning estimates, human-reviewed, never a safety/occupancy/
  compliance determination.
- Tests: `packages/web/src/__tests__/ProposalPage.test.tsx` (7 — render
  states, money formatting, accept flow, request-changes note gating,
  action hiding for non-actionable statuses, retryable error),
  `packages/web/src/api/__tests__/proposals.test.ts` (schema boundary),
  and respond-endpoint cases added to
  `packages/api/src/__tests__/proposals-routes.test.ts`. Mid-suite fix:
  three ProposalPage tests failed because RTL auto-cleanup is not wired
  globally in this suite's environment, so successive renders accumulated
  in document.body and "absent button" assertions matched the previous
  test's tree — fixed with an explicit `afterEach(cleanup)`.

Verified (2026-06-12): web typecheck + lint clean, full web suite 119
files / 2058 tests, production build green; api typecheck + lint clean,
full api suite 65 files / 1749 tests (suite totals include the parallel
strand's evidence-runtime / ops / revenue / integrations modules present
in the working tree).

Remaining for T-427 (phase 4): staff proposal/quote authoring UI in the
dashboard, a real client comments thread (beyond the single
request-changes note), layout visuals on the client page, capacity
intelligence surfacing (T-429), and E2E coverage of the share-link flow.
