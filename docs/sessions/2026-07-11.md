# Session log — 2026-07-11

## Trades House Craft quiz iPhone fit (T-483)

- Preserved the approved source-faithful opening composition and its centred
  `max-width: 520px` portrait frame.
- Added compact result sizing for portrait iPhones and a short-height pass for
  the answer screens and iPhone SE result. All supplied copy, the introduction
  action, retake action, visitor-leaflet link, building mark, and signoff remain
  visible; no page-level clipping is used.
- Added Playwright coverage for `375x667`, `390x844`, `393x852`, and `430x932`.
  Each case walks the full nine-question path, checks the question-five omen,
  reaches the longest Hammermen result, and asserts zero horizontal and
  vertical document overflow. The existing `2048x1200` portrait-frame
  regression remains green.
- Verified the development build and live preview, 5 responsive Playwright
  cases, and 3 focused Vitest files / 11 tests. Visual checks covered the
  accepted intro, iPhone SE question, and iPhone SE result.
- Bound the local Vite preview to `0.0.0.0:4174`. Both localhost and the active
  Wi-Fi address return HTTP 200 for the exact quiz route; the existing Windows
  Firewall rule allows `C:\Program Files\nodejs\node.exe` inbound on the active
  Public profile. A physical iPhone request was not observable from the local
  workspace, so same-Wi-Fi reachability remains the user-side check.
- The package-wide web typecheck is not attributed to T-483 in this shared
  worktree: concurrent untracked Reconstruction Foundry code imports ten
  `@omnitwin/types` exports that do not yet exist. The Craft quiz development
  build, focused unit tests, and its browser regression all pass.

## T-494 · CARD A1 (G1a) — Reception Room runtime default-on (Wave A session)

- Executed `docs/plan/cards/wave-A.md` CARD A1 per the card protocol. Blake
  Clause flag raised at session start: the requested docs-only commit of
  `docs/plan/` + CLAUDE.md was a no-op — both were already committed in
  `020fdb82` ("feat: ship venue operating system release"); no history rewrite
  was attempted.
- `/plan` anonymous bootstrap now defaults to the Reception Room
  (`EditorPage.tsx`: `reception-room` → legacy `grand-hall` → first space),
  with the opening heading derived from the requested room instead of a
  hardcoded "Grand Hall" claim.
- Planner chip is data-driven end to end: new `plannerRuntimeChipLabel()` +
  exported `CAPTURED_LAYER_FALLBACK_STATUS` ("Captured visual layer not yet
  available — planning on reviewed geometry") in
  `runtime-package-resolution.ts`; the hook and the cockpit-store default both
  consume it. Loaded states keep the existing evidence labels ("Runtime asset
  loaded, not yet verified/signed." etc.) — the card's "or current state"
  branch.
- Found and fixed a chip-honesty defect: `CockpitTopBar.css` display-hid the
  runtime cell below 1501px, so the honesty chip vanished on ordinary laptops.
  It now truncates (full label in `title`), and a new guardrail in
  `sspp-performance-budget.test.ts` fails any future display-hiding rule.
- New e2e `plan-room-runtime-default.spec.ts` (production preview build, API
  stubbed): 404-stub → atelier fallback chip over the procedural room,
  interactive in 792–910 ms locally (01 §21.1 budget < 1.5 s; reference-laptop
  measurement still owed); resolved package → the real 63 MB / 7-chunk
  reception SOG set streams into the planner with the evidence chip visible.
  Screenshots: full-page + canvas crops for both states (Playwright report
  attachments).
- Honest caveat: the calibrated Reception Room view transform targets the
  registered lcc2 SPZ chunk set; the locally-available twin-sourced SOG chunks
  therefore render misaligned in the loaded e2e evidence. Signed room-local
  alignment remains its own open gate (transform note already says so).
- Verified: full web suite 244 files / 2954 tests, typecheck, lint clean on
  all touched files (pre-existing foundry `FoundryReleaseDetail.tsx` lint error
  left to its own workstream), production build, CARD A1 e2e 2/2, legacy
  Grand Hall fallback e2e 1/1. No T-453 bypass; the API exposure gate still
  owns what `/assets/runtime-packages/latest` returns.
- typescript-reviewer pass (mandatory): zero critical/high; one MEDIUM confirmed
  and fixed same-session — the pre-resolution loading heading could name
  "Reception Room" for a venue whose space list lacks it (fallback chain would
  open Grand Hall while the heading claimed otherwise). The heading now stays
  generic until the bootstrap resolves the room from the venue's own space
  list, then names that actual space; two regression tests pin both states.
  Reviewer LOW nit (tests assert copy as literals rather than importing the
  constant) kept deliberately — copy regressions should fail tests.
  Observation surfaced for later: the repo has no eslint-plugin-react-hooks,
  and `eslint src/` never lints `e2e/` (pre-existing, repo-wide).
  Post-fix gates rerun: 69 focused tests green, typecheck, lint, rebuild,
  CARD A1 e2e 2/2 (fallback interactive 779 ms on the final build).
