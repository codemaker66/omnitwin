# Session log — 2026-07-12

## T-498 · CARD A2 (G1b) — Resolve-over-blueprint load ("the room resolves")

- Executed `docs/plan/cards/wave-A.md` CARD A2 on Blake's explicit go-ahead
  (override of the one-card-per-session protocol; CARD A1/T-494 shipped
  earlier in the same conversation as commit `88861196`).
- The choreography: blueprint ink paints first (new `InkArchitectureLayer` —
  floor/ceiling loops + corner verticals in the floor-grid ink, pure
  `buildInkSegments` builder, section-plane-clipped, seeds C2); each captured
  chunk develops in as it decodes (`RevealingSplatChunk`, identity-stable
  Spark callbacks, multiplied by the existing Mesh↔Splat↔Hybrid dissolve);
  the ink recedes `1 − loaded/total` and persists honestly over failed
  chunks; a quiet caption reports real chunk arrivals (no fabricated MB, no
  spinner anywhere); the stage exposes `data-resolve-phase`.
- Pure layers: `lib/room-resolve-model.ts` (phase machine incl. failure
  settlement, caption copy, ink target, 02 §6 motion constants),
  `hooks/use-chunk-arrivals.ts` (dedupe, partial-overlap retention, failure
  tracking), cockpit-store `roomResolve` slice.
- typescript-reviewer verdict Block → all findings fixed same-session:
  HIGH unwired `onError` (dead chunk wedged "developing" forever — failures
  now settle the phase); MEDIUM stale ink-ease start on LineSegments
  recreation + missing `roomGeometry` useMemo; MEDIUM arrivals reset
  dropping still-mounted chunks on partial-overlap key changes.
- Verified: 6 test files / 58 tests + sspp/claim-guard 12, typecheck, lint,
  production build, full web unit suite exit 0. E2E: A2 3/3 (50 Mbps CDP
  throttle, mid-stream interactivity, no-progressbar sweep, 404 fallback,
  reduced-motion) and A1 2/2 re-passed post shared-fixture refactor;
  develop-start/2 s/complete screenshots preserved.
- Environmental finding (honest): this workstation's driver reports "GPU
  stall due to ReadPixels — High" with 2M splats resident; back-to-back
  heavy tests in one Chromium can stall injected JS for minutes or drop the
  WebGL context. Both spec files are serial-mode; heavy scenarios should run
  in separate invocations on CI. Also observed (pre-existing, now visible):
  the local-preview Clerk-failure flip remounts the planner tree ~15 s in,
  which this choreography renders as an honest re-develop from cache.
- Follow-ups queued in the T-498 tasks.md note: caption vs floating-widget
  avoid-zones, CI browser recycling, ease-constant unification + fake-timer
  reveal-loop unit test, reference-laptop §21.1/§13 timings, a task for the
  Clerk-remount defect.
