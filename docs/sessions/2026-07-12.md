# Session log — 2026-07-12

## T-498 · CARD A2 (G1b) — Resolve-over-blueprint load ("the room resolves")

- Executed `docs/plan/cards/wave-A.md` CARD A2 on Blake's explicit go-ahead
  (override of the one-card-per-session protocol; CARD A1/T-494 shipped
  earlier in the same conversation as commit `88861196`).
- The choreography: blueprint ink paints first (new `InkArchitectureLayer` —
  floor/ceiling loops + corner verticals in the floor-grid ink, pure
  `buildInkSegments` builder, section-plane-clipped, seeds C2); each captured
  chunk develops in as it decodes (`RevealingSplatChunk`, identity-stable
  Spark callbacks, multiplied by the existing Mesh↔Splat↔Hybrid dissolve);
  the ink recedes `1 − loaded/total` and persists honestly over failed
  chunks; a quiet caption reports real chunk arrivals (no fabricated MB, no
  spinner anywhere); the stage exposes `data-resolve-phase`.
- Pure layers: `lib/room-resolve-model.ts` (phase machine incl. failure
  settlement, caption copy, ink target, 02 §6 motion constants),
  `hooks/use-chunk-arrivals.ts` (dedupe, partial-overlap retention, failure
  tracking), cockpit-store `roomResolve` slice.
- typescript-reviewer verdict Block → all findings fixed same-session:
  HIGH unwired `onError` (dead chunk wedged "developing" forever — failures
  now settle the phase); MEDIUM stale ink-ease start on LineSegments
  recreation + missing `roomGeometry` useMemo; MEDIUM arrivals reset
  dropping still-mounted chunks on partial-overlap key changes.
- Verified: 6 test files / 58 tests + sspp/claim-guard 12, typecheck, lint,
  production build, full web unit suite exit 0. E2E: A2 3/3 (50 Mbps CDP
  throttle, mid-stream interactivity, no-progressbar sweep, 404 fallback,
  reduced-motion) and A1 2/2 re-passed post shared-fixture refactor;
  develop-start/2 s/complete screenshots preserved.
- Environmental finding (honest): this workstation's driver reports "GPU
  stall due to ReadPixels — High" with 2M splats resident; back-to-back
  heavy tests in one Chromium can stall injected JS for minutes or drop the
  WebGL context. Both spec files are serial-mode; heavy scenarios should run
  in separate invocations on CI. Also observed (pre-existing, now visible):
  the local-preview Clerk-failure flip remounts the planner tree ~15 s in,
  which this choreography renders as an honest re-develop from cache.
- Follow-ups queued in the T-498 tasks.md note: caption vs floating-widget
  avoid-zones, CI browser recycling, ease-constant unification + fake-timer
  reveal-loop unit test, reference-laptop §21.1/§13 timings, a task for the
  Clerk-remount defect.

## T-499 · Splat quality independence analysis

Blake asked whether we can build our own "HD Enhancement" (fusing our
high-resolution photos with our PortalCam scans) instead of paying XGRIDS for
it, plus a first-principles synthesis of an external ChatGPT technology
review. Session ran as ARCHITECT with the MR_GENJUTSU council lens; five
verification passes and an adversarial red-team pass on the deliverable
(15 findings, all incorporated).

- **Delivered:** `docs/strategy/splat-quality-independence.md` — verified
  facts, four-lever first-principles frame, Reception Room four-view
  diagnosis protocol, the own-HD-enhancement architecture (bridge →
  registration → existing Config B trainer), tech-list verification table
  with licenses and D-012 provenance classes, staged independence ladder
  with evidence gates, epistemic map, recommended program.
- **Findings that matter:** (1) trainer never executed — `training_runs.jsonl`
  empty, T-001 blocked ~11 weeks on owner-side RunPod console setup
  (surfaced per surveillance rule). (2) LCC Premium $2,500/yr; no public
  $800 price; PLY export below Premium; LCC/LCC2 open-sourced;
  splat-transform (MIT) reads both — the real lock is the raw
  pre-reconstruction container only. (3) Mip-Splatting repo is Inria
  non-commercial; Config B already uses gsplat's Apache-2.0 `antialiased`
  (clean); T-501 audits and renames doc references. (4) ChatGPT review:
  structurally sound, convergent with our ADRs; silent on licensing.
- **Blake's answers (question tool):** repo strategy doc; full raw
  PortalCam projects on disk; stance **full independence** — doc §8 stages
  it behind evidence gates, Blake Clause flagged explicitly.
- **Tasks:** T-499 done. Added T-500–T-504 with Depends linkage; task
  graph updated.

### Same-day amendment (E57/Unreal lane + Splat Analyzer correction)

Blake relayed a ChatGPT review of the analysis requesting six amendments.
Executed as doc/audit only (no bridge code, no push, no T-091/T-091A change):

- **Splat Analyzer verdict corrected — the earlier NOT-FOUND was wrong.**
  `github.com/nigelhartman/splat_analyzer` fetched directly and verified:
  MIT, PLY/SPZ, OWLv2 open-vocab detection → fused 3D boxes, README claims
  XGRIDS-tested. Reclassified as a machine-detected semantic helper (human
  review required, never measurement authority). Failure mode logged in
  `.claude/AI_INTEGRITY_RULES.md` (negative-existence claims).
- **Unreal/E57 tooling repo audit:** confirmed — pye57, Open3D (pod),
  CloudCompare 2.14.beta, Blender, PDAL (doctrine, not on workstation),
  RealityScan as typed candidate only. Absent — any Unreal artifact,
  Autodesk ReCap, MeshLab.
- **Doc amendments:** §5b Lane M (E57 toolchain), §5c parallel mesh lane
  (deterministic baseline first per D-024; Unreal/Nanite QA-only), §5d
  tooling audit, residual-report spec (mean/median/RMSE/p95/overlap/
  control-point error + fixed views) replacing any universal pre-committed
  ICP gate.
- **Tasks:** T-505 added (one-room E57↔XGRIDS alignment proof); T-502
  gated on it.

*(Note added 2026-07-16: these two sections were briefly mis-filed and
re-dated during a resumed session, then restored here; see
`docs/sessions/2026-07-16.md` for the correction record.)*
