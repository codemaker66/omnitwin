# Session log — 2026-07-12

## T-498 · CARD A2 (G1b) — Resolve-over-blueprint load ("the room resolves")

- Executed `docs/plan/cards/wave-A.md` CARD A2 on Blake's explicit go-ahead
  (override of the one-card-per-session protocol; CARD A1/T-494 shipped
  earlier in the same conversation as commit `88861196`).
- The choreography: blueprint ink paints first (new `InkArchitectureLayer` —
  floor/ceiling loops + corner verticals in the floor-grid ink, pure
  `buildInkSegments` builder, section-plane-clipped, seeds C2); each captured
  chunk develops in as it decodes (`RevealingSplatChunk`, identity-stable
  Spark callbacks, multiplied by the existing Mesh↔Splat↔Hybrid dissolve);
  the ink recedes `1 − loaded/total` and persists honestly over failed
  chunks; a quiet caption reports real chunk arrivals (no fabricated MB, no
  spinner anywhere); the stage exposes `data-resolve-phase`.
- Pure layers: `lib/room-resolve-model.ts` (phase machine incl. failure
  settlement, caption copy, ink target, 02 §6 motion constants),
  `hooks/use-chunk-arrivals.ts` (dedupe, partial-overlap retention, failure
  tracking), cockpit-store `roomResolve` slice.
- typescript-reviewer verdict Block → all findings fixed same-session:
  HIGH unwired `onError` (dead chunk wedged "developing" forever — failures
  now settle the phase); MEDIUM stale ink-ease start on LineSegments
  recreation + missing `roomGeometry` useMemo; MEDIUM arrivals reset
  dropping still-mounted chunks on partial-overlap key changes.
- Verified: 6 test files / 58 tests + sspp/claim-guard 12, typecheck, lint,
  production build, full web unit suite exit 0. E2E: A2 3/3 (50 Mbps CDP
  throttle, mid-stream interactivity, no-progressbar sweep, 404 fallback,
  reduced-motion) and A1 2/2 re-passed post shared-fixture refactor;
  develop-start/2 s/complete screenshots preserved.
- Environmental finding (honest): this workstation's driver reports "GPU
  stall due to ReadPixels — High" with 2M splats resident; back-to-back
  heavy tests in one Chromium can stall injected JS for minutes or drop the
  WebGL context. Both spec files are serial-mode; heavy scenarios should run
  in separate invocations on CI. Also observed (pre-existing, now visible):
  the local-preview Clerk-failure flip remounts the planner tree ~15 s in,
  which this choreography renders as an honest re-develop from cache.
- Follow-ups queued in the T-498 tasks.md note: caption vs floating-widget
  avoid-zones, CI browser recycling, ease-constant unification + fake-timer
  reveal-loop unit test, reference-laptop §21.1/§13 timings, a task for the
  Clerk-remount defect.

## T-499 · Splat quality independence analysis

Blake asked whether we can build our own "HD Enhancement" (fusing our
high-resolution photos with our PortalCam scans) instead of paying XGRIDS for
it, plus a first-principles synthesis of an external ChatGPT technology
review. Session ran as ARCHITECT with the MR_GENJUTSU council lens; five
verification passes (ADR recon, pipeline/state recon, XGRIDS ecosystem,
technology-list verification, build-stack licensing) and an adversarial
red-team pass on the deliverable (15 findings, all incorporated).

- **Delivered:** `docs/strategy/splat-quality-independence.md` — verified
  facts, four-lever first-principles frame, Reception Room four-view
  diagnosis protocol, the own-HD-enhancement architecture (bridge →
  registration → existing Config B trainer), tech-list verification table
  with licenses and D-012 provenance classes, staged independence ladder
  with evidence gates, epistemic map, recommended program.
- **Findings that matter:** (1) The in-house trainer has never executed —
  `training_runs.jsonl` empty, **T-001 blocked ~11 weeks on owner-side
  RunPod console setup; surveillance rule: surfaced as the highest-leverage
  hour in the backlog.** (2) Verified pricing: LCC Premium $2,500/yr; no
  public $800 price exists; PLY export sits below Premium; XGRIDS
  open-sourced LCC/LCC2 and splat-transform (MIT) reads both — the real
  lock is the raw pre-reconstruction container only. (3) License catch:
  Mip-Splatting repo is Inria non-commercial; Config B already uses
  gsplat's Apache-2.0 `antialiased` (clean), but T-001/infra docs name
  "Mip-Splatting" — T-501 audits and renames. (4) ChatGPT review: program
  structure sound and convergent with our ADRs; its "Splat Analyzer" NOT
  FOUND (treat as hallucinated), three-meshlets/NeuWorld have no usable
  code, and it was silent on licensing.
- **Blake's answers (question tool):** repo strategy doc; full raw
  PortalCam projects on disk; stance **full independence** — doc §8 stages
  it behind evidence gates, Blake Clause flagged explicitly.
- **Tasks:** T-499 done (this analysis). Added T-500 (Phase-0 audit +
  four-view diagnosis, Tier 0, gate G-A), T-501 (XGRIDS→COLMAP bridge +
  Inria-license bootstrap audit, Tier 2), T-502 (own-retrain baseline vs
  LCC2, gate G-B), T-503 (DSLR HD pass, gate G-C), T-504 (ArtiFixer
  imagination-mode spike, Tier 6, D-012-gated), all with Depends linkage.
  `docs/diagrams/task-graph.md` regenerated (A1/C subgraphs + edges).
  T-500 is ready to start now; T-501+ wait on T-001.
- **Deviations logged honestly:** `docs/state/tasks.md` (377 KB) exceeds
  the single-pass read limit; shepherd compliance was achieved via
  full-structure read + targeted greps + delegated scan, not a literal
  full read. Analysis-only session: no production code, no tests (Law 4
  not triggered), no build card consumed.

## T-495/T-496/T-497 · Diary Slice 3 — drawer, conversion, live channel

- Shipped on `feature/diary-p0-slice-3` (from slice-2 HEAD; never pushed):
  `4ecfa1e2` plan (architecture §11 + task rows) · `08f6b632` API
  (migration `0051_diary_enquiry_link` + `POST /bookings/from-enquiry` +
  typed `diary.changed` on the house event bus + `/ws/diary` hub) ·
  `6fb9e161` web (BookingDrawer create/edit/convert over the shared Zod
  schemas, tray "Open enquiries" with Pencil in…, `useDiaryLive`
  backoff/refetch-on-reconnect, presence chip) · `67861909` post-review
  hardening.
- Architectural call flagged per the Blake Clause: Canon §9's command
  envelopes are NOT built this slice — mutations stay REST (each runs in a
  transaction with the exclusion constraint as arbiter) and the ws carries
  events + presence only; every mutation emits after commit. Documented as
  the explicit remaining §9 step in architecture §11.
- typescript-reviewer verdict Block → all findings fixed same-session
  (`67861909`): P1 unguarded async DB call in `/ws/diary` auth (failure now
  sends AUTH_FAILED + closes — never a stranded socket or unhandled
  rejection); P1 BookingDrawer stale form on retarget (drawer keyed by an
  open-nonce, remounts fresh); P2s in-flight-auth frame ordering,
  close-during-auth join guard, useDiaryLive disposed guard, busy-guarded
  Escape/Close; P3s heartbeat unref + error frame on unknown post-auth
  messages. 5 regression tests/pins added.
- Gates green: types 1,815 · api 109 files / 2,178 · web 255 files / 3,072;
  ESLint all three packages; api tsc build; web Vite build. Migrations
  0050/0051 remain written-not-applied (owner-gated); live-browser pass of
  /diary and a Playwright spec remain open items.
- Report: `docs/reports/slice-3-report.md`. Tasks T-495/496/497 → done.
