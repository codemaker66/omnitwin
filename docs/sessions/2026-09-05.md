# Session log — 2026-09-05 (Saturday)

## 09:30–10:00, the Fable thread resumes after a crash

Blake: "we crashed can you resume where we left off". Re-established the state rather than assuming it.

**Where things stand.** origin/master is 34dd4de4 (the revised ladder) with Lint, Typecheck, Build and Test green. The local head 150e9d3d (the goals deck, plans 15–16, T-584/586/587, by another session) was one commit ahead and unpushed; this thread's push carries it. The goals deck (goals/00–10, README, EXECUTION.md) is the runnable form of plan 15 and says GOAL.md and the ladder in docs/plan/14 remain the execution order for the captured rooms and Monday, so the two do not collide. The Codex execution board's first batch is complete: T-581 (the planner's coarse-first ladder) is done on `codex/t581-planner-ladder` at 6dad07fe with 5,142 web tests green and local browser acceptance, production qualification later; the sublime study and the inventory domain also landed on their branches. `codex/floor-atlas-v1` is July's Matterport floor-orthophoto tooling for the Twin (tools/twin-forge/e57-scripts/floor_atlas.py), not the splat floor patch; the V1 goal handed to Blake last night is not duplicated, and has been amended to reuse that tool's fusion and coverage discipline rather than write a second one.

**The upload.** 276.0 GiB in 28,937 objects, nothing in flight, silent since 21:37 last night. Every prefix on the kit's list received files and the last folder took its one large file; finished or stopped cannot be told apart from outside, and the kit's UPLOAD-LOG.txt on the drive is the word. The 64 MiB speed-probe file and 0.27 GiB of orphaned multipart parts under raw/matterpak/cloud.xyz are debris for card W2.

**The demo smoke.** Three runs so far: 18:16 ok (first view 11.3 s), 22:22 FAIL (20.5 s; Codex's run under SwiftShader software rendering, which slows the coarse decode; not a production regression), 08:30 today ok (7.9 s, complete 61.2 s, no errors). No schedule existed, so the card's 08:00 runs are now two Windows scheduled tasks, VenviewerSmokeSUN and VenviewerSmokeMON, each running D:\claude\demo-smoke\run-smoke.cmd at 08:00 and appending to D:\claude\demo-smoke\morning.log. Remove after Monday: `schtasks /delete /f /tn VenviewerSmokeSUN` and the same for MON. A scheduled task cannot page a phone; whoever is awake Sunday and Monday morning reads morning.log or runs the smoke by hand.

**Still Blake's.** Paste ChatGPT's second-round reply if there is one; upgrade LCC Studio to 2.3 and export Creator Data for the Grand Hall; set the R2 custom domain; pre-warm the demo laptop on Sunday; run the V1 goal in a fresh session when ready. The pod stays stopped until V3's free proofs.
