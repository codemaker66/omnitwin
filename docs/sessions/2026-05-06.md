# 2026-05-06 Session Log

## T-381 — Opposite-wall Door Chair Rail Interruption

- Fixed the Grand Hall procedural placeholder line Blake flagged across the three opposite-wall double doors.
- Split the lower-wall brass chair rail into separate segments around each ornate door opening instead of rendering one continuous rail through the door leaves and frames.
- Added pure geometry regression coverage for the three door-center gaps and a source guard for the named interrupted rail mesh.

## Verification

- `pnpm --filter @omnitwin/web test -- GrandHallRoom`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes

- No public copy, backend behavior, auth/upload/security logic, T-091A loader, real asset claim, or package naming changed.

## T-382 — Disable Native Planner Chrome Highlighting

- Added a scoped planner-shell CSS rule to prevent accidental browser text selection across the 3D planner toolbar, catalogue, panels, and interactive chrome.
- Kept controls focusable and the furniture search field typable, while suppressing native blue drag-selection over product UI.
- Added editor E2E coverage that drag-selecting across the furniture catalogue leaves `window.getSelection()` empty and that catalogue/search chrome computes to `user-select: none`.

## Verification

- `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes

- No public copy, backend behavior, auth/upload/security logic, T-091A loader, real asset claim, or package naming changed.

## T-383 — Toolbar Caption Fit

- Fixed the desktop active-tool caption fit issue Blake flagged on the Furniture button while the catalogue panel is open.
- Widened the desktop toolbar button within the existing left rail and tightened long-caption tracking so `Furniture` no longer clips.
- Added editor E2E coverage that the active Add Furniture caption stays fully inside its button bounds.

## Verification

- `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes

- No public copy, backend behavior, auth/upload/security logic, T-091A loader, real asset claim, or package naming changed.

## T-384 — Internal Spark Visual Route

- Added an internal lazy route at `/dev/trades-hall-visual` for manually loading a future real Gaussian splat URL into the existing web runtime.
- Added `SparkSplatLayer` around Spark 2.0 `SplatMesh` with URL loading, visibility/opacity/transform props, load/error callbacks, and unmount disposal.
- Added `runtime-visual-asset` parsing so internal tests accept plausible `.ply`, `.spz`, `.splat`, `.ksplat`, `.rad`, and `.radc` URLs while rejecting fixture-only Spark sources such as `textSplats`.
- Kept the route labelled as an internal visual-layer test and “not yet verified/signed”; no real asset, fake splat, public copy, T-091/T-091A completion, backend ingestion, auth/upload/security logic, or package rename changed.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` for T-384. T-091 remains not-started until real training output exists and loads.
