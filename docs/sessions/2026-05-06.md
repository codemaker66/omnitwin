# 2026-05-06 Session Log

## T-384 — Internal Spark Visual Route

- Added an internal lazy route at `/dev/trades-hall-visual` for manually loading a future real Gaussian splat URL into the existing web runtime.
- Added `SparkSplatLayer` around Spark 2.0 `SplatMesh` with URL loading, visibility/opacity/transform props, load/error callbacks, and unmount disposal.
- Added `runtime-visual-asset` parsing so internal tests accept plausible `.ply`, `.spz`, `.splat`, `.ksplat`, `.rad`, and `.radc` URLs while rejecting fixture-only Spark sources such as `textSplats`.
- Kept the route labelled as an internal visual-layer test and “not yet verified/signed”; no real asset, fake splat, public copy, T-091/T-091A completion, backend ingestion, auth/upload/security logic, or package rename changed.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` for T-384. T-091 remains not-started until real training output exists and loads.
