# Session log — 2026-07-16

## T-500 · PortalCam raw-project audit + objective runtime diagnosis (audit half)

Resumed conversation (earlier turns of this same conversation ran on
2026-07-12 and produced T-499 + T-500–T-505; sessions on 07-13/14/15
independently advanced T-506–T-516). This session mounted four F: trees and
executed T-500's audit half. Full evidence:
`docs/operations/portalcam-raw-audit-2026-07-16.md`.

- **Frames go/no-go: NO at the raw layer.** The Reception Room project is 12
  files; everything sensor-raw sits in one 8.70 GB proprietary `XBAG`
  `.xbin`. Factory calibration YAMLs exist in `lixel.zip` but are
  password-encrypted. Findings independently converge with the T-506
  lawful-raw-boundary ruling (read after the fact — this session ran
  read-only; one standard unzip attempt failed on password before the
  boundary note was seen, disclosed in the report and the T-500 row).
- **Trajectory is open twice over:** `poses.csv` (10 Hz) in the project and
  `poses.json` (4,529 @ 5 Hz) in the LCC2 export — the pose half of the
  T-501 bridge is confirmed feasible.
- **LCC2 runtime dissected (objective half of the four-view diagnosis):**
  each SOG tile is a ZIP of webp planes; SH3 present but palettized to a
  65,536-entry k-means codebook per tile (`flash-kmeans-sog`);
  `antialias:false` (Spark blur compensation must match); tiles sum
  3,494,926 gaussians (mixed-depth, overlap suspected) vs 2,002,122 built;
  built model 336.7 MB vs 63.5 MB shipped (5.3× post-build compression);
  `hdImageCount: 0`.
- **Program consequence:** warm-start-from-master-PLY + DSLR supervision is
  the primary HD lane; scanner-frame retrain stays Stage 3. New cheap probe:
  LCC Studio's C: workspace may cache extracted frames.
- **T-505 unblock candidate:** every raw project ships a vendor-produced
  room-local metric potree preview cloud (Reception: 175,237 pts, explicit
  bbox ≈ 14.96×16.05×3.90 m) — offered as the human-reviewable room-only
  crop/ICP seed the blocked T-505 workflow asks for; Grand Hall (the
  T-506-selected pilot) carries the same artifact.
- **Device record:** PortalCam SN A25AAA663D, firmware V3.2.1_20250829
  (v3.2.2 available since 2025-10-31 — update before next capture),
  algorithm v2.1.2 beta, LiDAR RoboSense Airy. `capture_log.json` enriched
  with these fields. No `.uproject` anywhere on the four F: mounts.
- **Strategy doc updated:** §5 T-500 field-results addendum; ArtiFixer §6
  row corrected per the T-506 licence ruling (code Apache-2.0, released
  checkpoint NVIDIA R&D non-commercial — cannot enter the commercial
  product).
- **T-500 stays in-progress** — remaining owner/GUI items: LCC tier + $800
  reconcile, quality-scale ceiling check, master-PLY export, four-view
  screenshots, Spark knob pin, LCC-workspace frame-cache probe.

### Date-correction record

This resumed session initially assumed its earlier turns were also
2026-07-16 and bulk re-dated the 07-12 artifacts forward; the concurrent
T-506+ work (dated 07-13/14) proved the earlier turns were genuinely 07-12.
All dates were unwound surgically: creation dates back to 07-12, today's
audit dated 07-16, and the two mis-moved sections restored to
`docs/sessions/2026-07-12.md`. Failure mode logged append-only in
`.claude/AI_INTEGRITY_RULES.md` ("Resumed-conversation date drift").

## T-517 · CARD A3 (G2a) — House token layer

- Executed `docs/plan/cards/wave-A.md` CARD A3 on Blake's instruction. New
  `packages/web/src/styles/house-tokens.css`: canonical `--house-*` names at
  02-DESIGN-LANGUAGE §3's exact BOH values (bg/0-2, hairline ×2, text/1-3,
  brass, six status hues), ghost-material constants (0.32 fill / 1px stroke /
  3s ±4% breath), motion tiers (instant 100ms / deliberate 240ms / cinematic
  650ms). No Tailwind (06 §3.3).
- Blake Clause resolution, documented in the file header: 02's values differ
  visibly from every current `--vv-*` value, so "alias vv onto House" and
  "zero visual regression" could not both hold literally. Sub-threshold
  tokens (`--vv-ink`→bg-0, `--vv-ink-2`→bg-1, `--vv-cream`→text/1; Δ ≤ 3 per
  channel) alias House canon; high-delta tokens (gold, muted, cyan, danger,
  success, focus, panel ×2) stay literal with per-token MIGRATION notes.
  global.css's :root token block moved wholesale into house-tokens.css
  (single source; @import at the top of global.css).
- Consumption proof: `.cockpit-shell` (PlannerCockpit.css) now consumes
  `var(--house-bg-0)` / `var(--house-text-1)` — the second replacing its
  previously-undefined `--vv-cinema-black` reference. App.css's scoped
  `--vv-cinema-*` subsystem deliberately left alone.
- Contrast audit shipped as `src/__tests__/house-tokens.test.ts` (9 tests):
  WCAG math (sRGB 0.04045 with the erratum documented), text/1+2 ≥ 4.5:1 and
  text/3 + brass + all six status hues ≥ 3:1 against every BOH bg, canon
  byte-equality with 02, legacy literal/alias split pinned, status-grey
  structurally pinned to text/3, global.css ownership move asserted,
  cockpit consumption asserted.
- typescript-reviewer verdict: Warning (no CRITICAL/HIGH; transcription
  verified byte-exact against 02 §3–§6, alias deltas re-derived by hand).
  Both MEDIUMs + three actionable LOWs fixed same-session (dead
  --vv-cinema-black alias removed + header corrected; status-grey added to
  the hue audit + equality pin; erratum comment; last-wins token parsing;
  uniform rgba guards).
- Verified: token gate 9/9, eslint, typecheck, full web unit suite exit 0,
  golden-route pixel-diff suites — dashboard 8/8 and public-acquisition 4/4
  PASS (zero visual regression). Operational suite fails 2/2 runs at the
  asset-registry seeded-copy assertion — attributed with evidence to the
  parallel session's uncommitted runtime-resolver retirement (their modified
  use-room-runtime-splat.ts / runtime-packages.ts / runtime-package-
  resolution.ts; a CSS token cannot time out a text query). Flagged to that
  workstream; also their new vite.config VITE_API_URL production assert now
  gates local preview/e2e invocations.
- Docs rode uncommitted by design: tasks.md carried the parallel session's
  in-flight 45+/53− rewrite, so this card's T-517 entries were added
  additively and left for the ledger's active owner to commit; the A3 code
  commit is deliberately code/test/css-only.

## T-518 · Diary Slice 4 — prove it live (dev DB + two-coordinator e2e)

- Blake's instruction: apply migrations 0050/0051 to a dev database, seed the
  Trades Hall week, prove the whole Diary live with a Playwright spec.
- **Production-safety call first:** packages/api/.env points at the
  `ep-dawn-glitter` Neon project — docs/PRODUCTION.md names that as the
  production project, so it was never touched. Built an ISOLATED local stack
  instead: portable PostgreSQL 16.6 binaries in `.dev-db/` (gitignored;
  Docker Desktop's engine dies into a GUI error dialog on this machine —
  logged, deferred to Blake) + `infra/dev-db/` (compose for the Docker path,
  `neon-ws-bridge.mjs` byte-shovel proxy for the serverless driver,
  `provision-clerk-test-users.mjs`). `db/client.ts` gained a
  localhost-gated neonConfig branch; `pg` devDep so drizzle-kit migrates
  over TCP.
- **Firsts:** the ENTIRE migration journal (0001→0058, incl. the other
  session's uncommitted 0049/0052–0058) applied cleanly to real Postgres for
  the first time ever; btree_gist + `bookings_ink_no_overlap` verified by a
  live 23P01; the full Trades Hall seed ran through the bridge (22 bookings,
  week of Mon 14 Sep 2026 + two `+clerk_test` staff coordinators, clerkId
  NULL for the seed-user JIT link).
- **e2e:** `packages/web/e2e/diary-live.spec.ts` + `support/diary-live.ts` —
  NO stubs, NO `__OMNITWIN_E2E__` bypass: real Clerk dev-instance sign-in
  (password + 424242 second factor), two browser contexts. Five scenarios:
  live calendar render, drawer create, public enquiry → "Pencil in…",
  two-coordinator ink race (Postgres arbitrates; drawer shows the
  slot-taken copy), ws presence `Live · 2` + no-reload cross-browser
  updates. Green ×4 consecutive runs (self-healing leftover-ink sweep for
  crashed prior runs).
- **Live-run discoveries, all fixed with tests:**
  1. Drawer silently dead for non-hold creates — stale hold defaults
     (rank "1", owner) rode into the payload; schema rejected on fields the
     user couldn't see. `formToCreatePayload` now expresses only the visible
     form; new `hiddenFieldError` surfaces slotless errors as the form-level
     error (never a silent submit again).
  2. Clerk claims gap: dev-instance session tokens carry NO email claim, so
     the fail-closed email gate 403'd every real token (this is also the
     long-standing "local Clerk never works" mystery, incl. the
     pk_live_localbuildcheck Vite bake — the OTHER root cause found: any
     shell env pk_live key outranks .env's pk_test in
     resolveWebClerkPublishableKey). New `middleware/clerk-email.ts`:
     Backend-API verified-email fallback (fail-closed, claims-verdict
     preserving, TTL-cached); wired into `authenticate` + `resolveWsUser`.
  3. Unhandled idle db-client 'error' killed the API process mid-e2e —
     `pool.on("error")` guard in client.ts (crash → transient 500, pool
     self-heals; the alternating e2e flake vanished with it).
  4. Thrown 500s were invisible without Sentry — error-normalizer now
     `request.log.error`s every ≥500.
- Gates: api typecheck/lint/139 files/2,575 tests; web typecheck/lint/3,140
  tests; full workspace build (under the parallel session's new
  HTTPS-VITE_API_URL production gate, satisfied inline); e2e 5/5 ×4.
- Riders: `packages/api/package.json` (+pg devDep) and `pnpm-lock.yaml`
  touch the parallel session's modified files — one-line additive.
  `docs/state/tasks.md` T-518 row added additively; the in-flight ledger
  rewrite stays with its active owner, so tasks.md is NOT committed here.
  Resend fired real (rejected) notification attempts during e2e — dev key
  only sends to the owner address; noted as expected noise.
- Report: `docs/reports/slice-4-report.md`. Reviews: typescript-reviewer +
  security-reviewer (results in the report).

## T-519 · CARD A4 (G2b) — Chip grammar (the canonical EvidenceChip)

- One chip component now speaks the evidence language everywhere it was
  adopted: `components/evidence/EvidenceChip` + pure `lib/evidence-chip-model`
  (four 01 §9 states exactly; provenance badges operator / machine-checked /
  AI violet spark / simulated cyan flask; House status hues from CARD A3;
  hue + icon + label always, never color alone).
- Honest-mapping decisions, documented in code + tests: rejected evidence →
  Review required (never Current, not Missing); verification `expired` →
  Stale; the TruthModeIndicator's 11-value source-tag vocabulary deliberately
  NOT forced into four states (would overstate claims) — its Verification row
  carries the canonical chip instead, driven by `verificationState`.
- Swapped surfaces: router LoadingFallback (SAFE wording kept verbatim as the
  chip detail), cockpit truth rail (ad-hoc tone pills + non-House colors
  deleted; grid slot class retained), TruthModeIndicator Verification row.
- Reviewer verdict Block → both findings fixed same-session: HIGH — static
  chips carried role="status" + tabIndex (7 live regions + 7 dead tab stops
  in the rail); now static chips stay out of the tab order and live regions,
  interactive chips are real buttons with the visible ring (contract pinned
  in tests — this is the honest reading of the card's "keyboard-focusable"
  DoD). MEDIUM — tone mapping converted to a tsc-exhaustive switch.
- Verified: 27 focused tests + full web suite exit 0, typecheck, lint,
  production build, live keyboard probe on the built /dev/evidence-chips
  fixture (Tab → button chip only; 0 static tab stops; 0 live regions);
  fixture + focus-ring screenshots in the session scratchpad.
- Follow-ups: eager-bundle size guard for the router-embedded chip; lucide
  2px vs 02 §9's 1.5px stroke (pre-existing repo-wide); remaining
  vv-status-chip call sites (EditorPage blocker, ProtectedRoute ×2,
  DashboardLayout) for the consolidation sweep alongside A3's literal
  migrations. Ledger note added additively — tasks.md still carries the
  diary workstream's in-flight rewrite, so docs ride uncommitted again.

## T-520 · Diary Slice 5 — production rollout pack

- Blake's instruction: the owner-run migration script + checklist for
  0050/0051, dev-instance Clerk claims verification, and the Board's
  first-run onboarding — with plain-terms narration throughout.
- **Rollout script** `packages/api/src/scripts/apply-diary-rollout.ts`
  (house apply-migration-* convention, hardened): dry-run by default,
  --apply gate, --accept-cursor-jump acknowledgment, per-migration
  transactions, ledger rows byte-identical to drizzle's migrator (sha256 +
  journal `when`; comparison semantics verified against
  drizzle-orm/pg-core/dialect.js — cursor is newest-created_at, hence the
  0049-stranding consequence, guarded + documented). REHEARSED on a
  disposable local DB brought to production-like state (temp-journal
  truncation to ≤0048 + drizzle's own migrator): dry-run report, refusal
  exit-1, apply, "Nothing to do" rerun, byte-perfect ledger rows, live
  23P01 probe. Rehearsal DB dropped after.
- **Runbook** `docs/operations/diary-production-rollout-runbook.md`: Neon
  backup-branch step, real rehearsal outputs as expected-output samples,
  ledger drift stop-conditions, cursor section (Foundry-owner heads-up),
  exact rollback SQL enumerated from 0050/0051's object inventory,
  post-apply smoke, "what this does NOT do".
- **Clerk claims verifier** `infra/dev-db/verify-clerk-claims.mjs`: real
  sign-in, captures the actual bearer token from /auth/me, grades claims
  against the middleware's accepted names (must-match note to
  auth-email.ts). Baseline run: FAIL as expected (default tokens, claims =
  azp/exp/fva/iat/iss/nbf/sid/sts/sub/v) with dashboard guidance — copy
  production's Claims JSON; email shortcode
  {{user.primary_email_address}} confirmed against Clerk docs (Context7),
  verified-flag shortcode deliberately not guessed.
- **First-run welcome** (T-520): `lib/welcome.ts` (per-user localStorage
  gating, throw-safe, TDD 3 tests) + `WelcomePanel.tsx` (modal, Escape +
  single-control tab trap, preventScroll focus — plain autoFocus scrolled
  the title out of view on the first live screenshot; scrollable body with
  the button always visible after the second) + board-copy `welcome`
  section (claim-guard swept) + header "How the Diary works" reopen.
  Live-screenshot verified over the seeded board. e2e support dismisses
  the welcome as a real first-run user would; live spec 5/5 with it.
- **T-number collision handled:** T-519 was claimed mid-session by the
  parallel A4 card (EvidenceChip) — this slice renumbered to T-520 and
  swept all file references.
- Gates: diary units 94/94; api 141 files / 2,592 (lint clean; the only
  typecheck errors in the tree are the Foundry tools package's in-flight
  V0→V1 renames — theirs, flagged); web 3,159/3,160 passed where the one
  failure (later two: EvidenceChip, living-hall) sits in parallel
  sessions' actively-edited areas — diary scope fully green; web build
  green; live diary e2e 5/5. Reviews: typescript-reviewer +
  security-reviewer on the slice diff (outcomes in the report).
- Riders: tasks.md T-520 row additive; session log + ledger remain
  uncommitted with the ledger owner, as before.

### T-519 addendum — shared-tree commit incident (recorded per AI_INTEGRITY_RULES)

- Incident 1: the CARD A4 commit (`4b69c39e`) accidentally swept the Living
  Hall workstream's uncommitted router hunks (two lazy route registrations)
  because `git add` staged the whole shared `router.tsx`. Those routes
  reference `LivingHallRuntimePreviewPage.tsx` / `LivingHallLocalPreflightPage.tsx`,
  which are still UNTRACKED — so a clean checkout of any commit from
  `4b69c39e` to the current tip fails to build until that workstream commits
  its pages. FLAG to the Living Hall session: please commit those two pages
  (or tell me to revert the two route registrations forward).
- Incident 2: my attempted repair (`commit --amend` to unsweep the hunks)
  fired while HEAD had ALREADY moved — the diary session had landed T-520
  between my commit and the amend, so the amend rewrote THEIR commit.
  Detected immediately; fully unwound via `git reset --soft HEAD@{1}` +
  index/working-tree restore. Final state verified: all commits at original
  hashes (999cc945 / 588f98a2 / 513cb154 / 4b69c39e), nothing staged, working
  tree byte-identical for the shared router.
- Lesson (added to memory): in this multi-session tree, NEVER `--amend` and
  never assume HEAD is yours — re-verify `git log -1` immediately before any
  history-affecting command, and stage shared files hunk-wise (`git diff` +
  `git apply --cached`) rather than whole-file `git add` when foreign hunks
  are known to be present.

## T-521 · Wave A closure pass

- Consolidation sweep + the G1/G2 acceptance check; full scorecard at
  `docs/reports/wave-a-closure-2026-07-16.md`. G1 functionally closed (three
  named caveats with owners); G2 closed for tokens + chip grammar with one
  small open item (scene eases onto the House motion tokens). Every
  non-migration is documented with its reason; the high-delta palette flip
  (gold→brass et al.) is explicitly parked as Blake's decision.
- Cross-workstream flags raised: the dashboard pixel suite is currently
  broken by the parallel removal of `#dashboard-main` (their in-flight
  dashboard edits); the branch tip remains not fresh-checkout-buildable
  until the Living Hall pages are committed.

## T-522 · G4 opened — the Action log programme

- Palette decision sheet delivered to Blake (scratchpad/palette/
  palette-decision.png): every parked colour today-vs-House-canon with real
  UI mocks; his call, doesn't block anything.
- G4 audit: editor-history.ts (T-447) already records invertible field-level
  deltas — the Action log is an EXTENSION (envelope + append-only log +
  coverage + persistence), not a rewrite. Programme with four one-session
  slices at docs/plan/g4-action-log-program.md; slice 1 next session.
- LEDGER FLAG: docs/state/tasks.md was reset in the working tree today
  (back to near slice-3-era HEAD + one stray row) — both the diary
  session's day of uncommitted ledger notes AND this session's additive
  T-517/T-519/T-521 entries were wiped. Nothing is lost permanently (this
  file, the committed reports, and commit messages carry everything);
  re-add rows once the diary workstream's restructure settles. T-numbers
  follow commit messages: next free is T-522.

## T-523 · Diary Slice 6 — the merge-and-deploy pack

(Two reconcile commits + the checklist briefly said T-522 before the G4
session claimed it; commits are immutable — this slice is T-523.)

- **Reconciled `feature/diary-p0-slice-3` with origin/master** (merge
  1d360e3b): homepage-series conflicts resolved (index.html = master title
  + branch favicon; dollhouse-cutaway = branch superset; session log
  unioned; task-graph ours + stash-restored overlay). Post-merge gates all
  green: web 267 files / 3,188 tests, api 143 / 2,622 (+5 skipped), both
  builds, live diary e2e 5/5.
- **Found master CI RED** (stale route-lazy pins vs the Wave-A cockpitImport
  refactor; `gh`: CI=failure, Deploy=skipped at 1ecbec23) → widened the two
  pin regexes (c89dd43c). Merging this branch turns master's CI green again.
- **Journal coherence** (a0830f72): the committed `_journal.json` referenced
  untracked `0049_reconstruction_foundry` — a fresh-checkout migrate would
  crash. Committed blob now lists only tracked files (working tree kept
  intact for the Foundry sessions). Invariant: never commit a journal entry
  without its .sql.
- **Deploy checklist** `docs/operations/diary-deploy-checklist.md`: Vercel
  deploys on push WITHOUT waiting for CI → migrations must run FIRST
  (`db:migrate` applies 0044→0051 in journal order; production ledger
  verified at 0043). Slice-5 runbook demoted to state-report/emergency tool
  (its `--apply` would strand 0044–0048 below the drizzle cursor).
- **Production smoke suite** `packages/web/e2e/production-smoke.spec.ts`
  (4dc4dfbd): PROD_SMOKE=1-gated; health surfaces, front door, signed-in
  Diary (lanes/presence/claim-safe line/welcome), opt-in write probe
  (labelled house block landed then released). Storage-state path for
  production's email-code challenge; SMOKE_TEST_OTP only for dev-instance
  rehearsals. Dress-rehearsed 4/4 green against the local stack after three
  live fixes: welcome-overlay race (settle board THEN dismiss), regex-unsafe
  title (escape before RegExp), OTP challenge path.
- Local dev DB cleaned of the one stray probe row a failed rehearsal run
  left behind (released trace row kept by design).
- typescript-reviewer verdict on the slice: SHIP, zero P0s. Implemented
  literally: P1 write-probe residue safety (run-unique title token,
  create→release try/finally with best-effort retry + loud manual-cleanup
  log, retries:0) — re-rehearsed 4/4; P1 e2e-outside-static-analysis
  tracked as T-524; P2 header failure-residue doc fixed; P2 static-import
  negative assertions completed for all 9 lazy pages (13/13); speculative
  P2 sign-in race accepted with rationale (fails loudly downstream).
  Full table in docs/reports/slice-6-report.md §5a.
