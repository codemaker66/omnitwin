# Session log — 2026-07-10

## Verified venue operating-loop programme

User instruction: use the mixed capture material under `F:\E57`
non-destructively, then implement all three proposed ambitious features. The
programme closed T-160 and T-480 through T-482 while preserving the accepted
evidence boundaries around T-091, T-100/T-103, and deferred multiplayer T-105.

### Capture-to-Truth factory (T-480)

- Added shared capture-intake and stage-manifest contracts plus the
  `tools/capture-factory` inspect/stage CLI. Classification is conservative:
  magic-derived ASTM E57 is primary capture, original MatterPak material is a
  vendor control, known generated/edited trees are reference-only or excluded,
  and unknown provenance is never guessed into the truth-source stage.
- Staging is required to be disjoint from the source. Copies stream through a
  digest-addressed partial, pass exact byte/SHA-256 verification, and promote by
  atomic rename. Matching final files are reused; conflicting files fail closed
  instead of being overwritten. Inspection and stage ledgers are immutable.
- Added an environment-configured, platform-admin-only API status boundary and
  `/dev/capture-intake` operator surface. Its fast status check validates both
  ledgers, copy-plan consistency, containment, regular-file targets, and byte
  sizes; it does not pretend that this replaces the sealed factory hash pass.

#### Real Trades Hall evidence

- Read-only source: `F:\E57` — 9,364 files, 64 directories,
  89,587,926,317 bytes.
- Classification: one high-confidence primary capture, 155 vendor controls,
  451 reference-only files, and 8,757 exclusions. No duplicate hash group was
  present in the selected source set.
- Primary source: `cloud_0.e57`, 20,518,437,888 bytes, SHA-256
  `975039d11fc04ca681f038e499f358124bbcab178ad5ce6324fa912212729cdd`.
  Its ASTM E57 1.0 physical length matches the file; the container records 149
  Matterport Pro3 sweeps, 965,520,000 structured points, and 894 embedded
  4096×4096 JPEG blobs with intact SOI/EOI boundaries. These are container and
  identity facts, not a survey-accuracy claim.
- The original 155-file MatterPak subset totals 1,759,056,988 bytes; its sorted
  hash-index digest is
  `06ff00550ff72865e26d32aa768121262d2237b07db712901a1e19e007436e06`.
  The later `TH_OBJ_RC_ALIGNED.obj` remained excluded as an edited rigidly
  transformed derivative, not an independent source mesh.
- Verified stage:
  `F:\VenviewerCaptureStaging\trades-hall-2026-07-10` — 156 files and
  22,277,494,876 payload bytes, with zero remaining partials.
- Copy-plan SHA-256:
  `d9a75df3ffaf2706d97f454cbfae9a5c47ce0719c83af7f56da391ce0def3729`.
- Inspection-ledger SHA-256:
  `368a4fc7799470feadac5820485854b9093c8b7de2f5ab2fc2288f2777c815c8`.
- Stage-manifest SHA-256:
  `c044823c232dae518df84140c90004a1c17dc682c84885d6f36848933d72ddff`.
- The real operator read returned `staged`, `consistent`, and
  `intake_verified`. Reconstruction, coordinate-control review, visual/runtime
  QA, signing, registry promotion, and public exposure remain open; T-091 was
  not advanced.

### Deterministic validator and Proof-Carrying Event Architect (T-160/T-481)

- Added a pure validator over normalized canonical snapshots. Its bounded v0
  checks are snapshot/policy identity, conservative oriented-footprint room
  containment, chair-first represented seating, convex primary-furniture
  clearance, and exact GBP minor-unit budget comparison. Stable rule/message
  keys, actual facts, object references, policy/snapshot/context identities,
  review gates, and proof hashes remain free of wall time, randomness,
  telemetry, background fetches, or AI/free-text authority.
- Added the pure Event Architect v0.2 engine with three explicit strategies:
  Comfort first, Balanced, and Capacity first. Dinner-round and theatre
  generators use canonical asset IDs/dimensions, content-derived IDs, explicit
  spacing assumptions, polygon-fit placement, exact price-book inputs, causal
  repair hints, and canonical snapshot/proof digests.
- Each candidate now includes deterministic seeded Guest Flow Replay evidence.
  The sidecar records its input/artifact/navmesh hashes, fallback grid-navmesh
  provenance, route-conflict metrics, limitations, and the literal disclosure
  `Simulated guest flow - planning support`. Assumed doors and the simplified
  crowd model never become validator passes. `humanReviewRequired` is always
  true and the Ops gate requires surveyed door positions, a reviewed route
  model, and venue operations sign-off.
- Migration `0047_event_architect_proof.sql` and the API persist the run, all
  three private draft configurations, real-metre placed objects and initial
  revisions, canonical snapshots, validator runs, candidates, selection, and
  idempotency state transactionally. Browser input is limited to the venue,
  room, structured brief, and idempotency key; geometry, policy, time,
  catalogue data, and actor identity are resolved server-side.
- The protected Event Architect page provides an accessible brief, three
  responsive SVG plans, factual witness/review/repair panels, simulated-flow
  disclosures, missing-pricing gates, persisted-run loading, retry-safe
  idempotency, exact candidate selection, and `Open in planner` handoff.
  Selection leaves the configuration in `draft` / review `draft`; configuration
  approval, event binding, and every open Ops gate still have to occur elsewhere.
- No egress, fire, accessibility-route, occupancy, legal, statutory, or
  certification determination was added. AI emphasis is hash-only provenance
  and never a checked fact.

### Persisted 4D Event Mission Control (T-482)

- Added migration `0046_event_mission_control.sql`, shared baseline/event/
  projection/replay contracts, and explicit mission, phase, task, and incident
  transition state machines.
- Starting a mission freezes the latest handoff into a canonical baseline and
  baseline hash. Phase, task, and incident rows are revision-CAS projections;
  the append-only event stream owns unique per-mission sequence and
  actor/idempotency identities. Acknowledgements target events in the same
  mission. Spatial anchors remain scoped to mission/event/phase/task identity.
- Authenticated read/write routes enforce event/venue authority. Commands are
  idempotent, stale revisions return explicit conflicts, only one phase can be
  active, closed missions reject mutation, timelines page from a cursor, and
  deterministic replay reconstructs planned-versus-actual state through any
  sequence.
- Presence uses expiring database heartbeats and is deliberately excluded from
  replay authority. It is advisory collaboration visibility, not a fabricated
  WebSocket or completion of deferred multiplayer T-105.
- Mission Control is integrated into Event Day Ops for launch, live phase/task
  transitions, incidents, acknowledgements, presence, timeline navigation,
  historical replay, return-to-live, and explicit completion. While a mission
  is live, the older pre-mission checklist stops offering a second task writer.
  Completed missions retain their replay.

### Browser-discovered fixes

- Mission Control could mutate its timeline ref before deciding where to place
  the range cursor. A new live run could therefore show `Live edge` while the
  control remained at sequence `#0`. The loader now captures
  `previousLatestSequence` before state/ref mutation; the regression pins the
  first live cursor value at `1`.
- Overlapping pages returned by timeline polling could render duplicate mission
  events and duplicate React keys. `mergeTimelineEvents` now merges by event ID
  in a `Map` and sorts by sequence; the overlap regression proves one rendered
  event per ID.
- Full-suite shutdown tracing found that root-scoped `@fastify/websocket`
  registration inherited one upstream `preClose` hook across 54 Fastify plugin
  contexts. The websocket plugin and its sole auto-save route now live in one
  encapsulated scope, reducing shutdown to one owner without suppressing
  listener limits; a lifecycle regression keeps the HTTP fallback route and
  rejects `MaxListenersExceededWarning` recurrence.
- Web tests now fail after every case that attempts an unstubbed request to the
  localhost API. This exposed one venue-routing test that mounted the real
  desktop cockpit and fetched Truth Mode data; the test now isolates the
  cockpit dependency. The complete web run no longer emits a late
  `ECONNREFUSED` aggregate or allows caught background traffic to pass silently.

### Continuation: migration-tail release safety

- Added a read-only, atomic-report migration-tail verifier and `db:verify-tail`
  command. It checks journal order and semantic prefix parity, required schema
  objects, affected-row counts, and the explicit 0044 approval digest without
  printing credentials or row data. The production deployment workflow now
  runs this gate before `db:migrate`.
- Read-only queries against the configured Neon database found 42 of 46 local
  migrations applied. The exact pending tail is 0044 through 0047;
  `event_missions` and `event_architect_runs` do not yet exist, and
  `placed_objects.coordinate_space` is absent. The only non-zero affected-table
  counts are one `proposal_versions` row, eight `event_phases`, and one
  `events` row; scenario/phase cross-event mismatches are zero.
- Migration 0044 remains a deliberate production gate because it takes
  `ACCESS EXCLUSIVE` locks and performs coordinate/provenance/write-token data
  changes. While it is pending, the protected environment must provide the
  exact canonical SQL approval digest
  `c1114fa53a21c9f9b30bfc5973d6ab769d4265152eaf3e289597975260537146`.
  No migration was applied and no database write was performed.
- The historical 0025 journal hash difference was reproduced as CRLF/LF-only;
  normalized local SQL matches the stored hash. `drizzle-kit check` and a
  2,290-line schema export both pass. Durable evidence is recorded in
  `docs/operations/migration-tail-readiness-2026-07-10.json` and
  `docs/operations/migration-tail-0044-0047-readiness-2026-07-10.md`.

### Continuation: reconstruction preflight

- Added a verified-stage Python guard used by pose, image, panorama, and GLB
  tooling. It validates the immutable stage ledger and payload hashes, rejects
  traversal, link/reparse inputs, source/output overlap, stale panorama
  evidence, and unsafe legacy defaults. Historical extraction scripts with
  hard-coded source paths now fail closed.
- A full 20.5 GB E57 hash recheck preceded extraction of all 149 scan poses to
  `F:\VenviewerReconstructionWork\trades-hall-2026-07-10\staged-e57-poses`.
  The canonical pose digest is
  `fe3b9000eda4737af038e01e811e57bffa7fae07290a938c1ef75875c9df82e3`;
  the data3D GUID digest is
  `f723f0e172946cfd57f9bf5b0bc678005b364b49cf7b3750949d15b687fc8bbb`.
  An existing ignored Twin manifest matches all 149 poses with zero
  quaternion/translation delta, but remains untrusted for imagery, mesh, and QA.
- Blender produced two byte-identical vendor-reference GLB exports outside the
  stage. The candidate is 141,636,656 bytes, SHA-256
  `dd7fcbd3800a6c1af18bd9121433d31ec89f9efd477acfe3a00ce31652d082f2`,
  resolves 144/144 diffuse textures, and has maximum source/import/round-trip
  bounds delta `4.65393066306774e-7 m`. It remains explicitly unsigned,
  unreviewed, vendor-control-only, and blocked from public/runtime promotion.
- T-118 remains open: no full staged-E57 mesh, point-to-mesh residual, bounded
  Poisson/BPA/dual comparison, or visual/runtime acceptance exists. The exact
  next experiment and tool gaps are recorded in
  `docs/operations/trades-hall-reconstruction-preflight-2026-07-10.md`.

### Continuation: Ops authority integration

- Event Architect evidence is now Zod-validated at the Ops boundary. Its
  immutable guest-flow gate blocks compilation with machine-readable resolution
  `reviewed_evidence_artifact_required`; no current button or human action is
  presented as being able to clear a gate for which no authoritative persisted
  evidence artifact yet exists.
- Event-bound handoff compilation now requires an explicit same-venue
  event/configuration link. Cross-venue layout-variant links fail before insert.
  Mission start rechecks the approved snapshot identity and exact hash, pack
  configuration, event/configuration venue and link, and compiled/exported pack
  state before accepting the immutable baseline.
- The desktop Ops Lens exposes only allowlisted actionable precondition errors;
  other failures keep generic safe wording. Browser QA confirmed the exact gate
  disclosure, no console/page errors, and no horizontal overflow.
- During the shared-worktree continuation, an incomplete Living Hall interaction
  edit surfaced in the full gates. Its scene-failure path now clears sandbox
  state, pointer ownership is opt-in only while the WebGL scene is active, and
  regressions cover the no-WebGL and CSS ownership boundaries.

### Continuation: persisted Event Architect Ops review authority

- Added migration `0048_event_architect_ops_reviews.sql` and a matching
  Drizzle table for append-only reviewed-evidence artifacts. Database triggers
  reject updates and deletes; composite candidate/run scope, venue,
  configuration, reviewer, idempotency, digest-shape, witness-count, and
  validity constraints fail closed.
- A review records exactly one surveyed-door, reviewed-route-model, and venue
  operations sign-off witness. Each witness carries a source label, controlled
  reference, observation time, and SHA-256 content digest. The artifact also
  binds reviewer identity/derived authority, decision, note, expiry, request
  digest, snapshot digest, validator proof digest, and simulated-flow artifact
  hash into a server-generated artifact digest and general audit event.
- Only the selected candidate can be reviewed. Venue staff, hallkeepers,
  administrators, and Venviewer platform administrators can append decisions;
  planners have read-only status access. Idempotent retries return the original
  artifact while reuse with a different envelope is rejected.
- Ops Compiler now joins the current review artifact at its existing hard gate.
  A current approval proceeds to the separate approved-snapshot and event-link
  checks; absence, rejection, expiry, malformed candidate evidence, or any
  digest mismatch remains blocking. The simulated guest-flow payload still
  literally declares human review required and cannot approve itself.
- The selected-candidate Event Architect UI exposes the effective gate,
  immutable history metadata, three evidence-witness inputs, decision, note,
  and one-hour-to-90-day validity. No production database migration or evidence
  approval was performed.
- Verification for this continuation: strict types/API/web typecheck, affected
  lint, 10 type-contract tests, 27 API/migration/Ops tests, 7 Event Architect UI
  tests, `drizzle-kit check`, and `git diff --check` all pass.

### Continuation: supplied Trades House collateral

- Imported only the 27 curated assets from the user-supplied archive. The
  generated `support.js` evaluator and 118 unreferenced draft uploads were not
  shipped. Source identities and the unresolved copy/image-rights approvals are
  recorded in `docs/operations/trades-house-leaflet-source-2026-07-10.md`.
- Added public lazy routes for the responsive two-sided leaflet and the native
  strict TypeScript/React nine-question Craft experience. The deterministic
  model scores all fourteen Crafts, preserves answer-order tie-breaking, shows
  accessible progress and results, and creates the supplied introduction email
  handoff. Both printed QR codes retain the source destination rather than being
  silently retargeted.
- Browser comparison at the supplied 430 by 900 viewport covered the intro,
  question, and Hammermen result states. A ref-driven walkthrough answered all
  nine questions and verified Wrights and Masons as the two runner-up affinities.
  The leaflet viewport matched the supplied A4 landscape sheets; a full-page
  screenshot compositor defect was isolated from the normal viewport and PDF
  renders.
- A user corrective screenshot exposed an unintended desktop-only three-column
  grid in the first implementation: at 2048px the quiz shell measured 2048px
  wide and auto-placed the second Craft rail and intro into the wrong columns.
  The supplied corrective HTML proves there is no breakpoint; the source is
  always a centred portrait canvas capped at 520px. The desktop branch and
  full-bleed hall-photo treatment were suppressed for screen rendering, the
  absolute rails/unclipped drape/source sizing were restored, and the missing
  achievement halo and result medallion details were added. The new browser
  regression first failed at 2048px, then passed with a 520px shell and exact
  centring. Source-versus-route screenshots at 823 by 763 and 430 by 900 were
  visually inspected side by side; at the user's 2553 by 1201 viewport the
  shell measured 520px with a 0px centring delta. The nine-answer browser flow
  still resolved to Hammermen with Wrights and Masons affinities.
- Generated `output/pdf/trades-house-glasgow-leaflet.pdf` and verified it as two
  tagged A4 landscape pages (841.92 by 594.96 pt). Both Poppler renders were
  inspected with no clipping, overlap, black tiles, or unreadable text. The
  artifact remains a browser-print preview without bleed, crop marks, CMYK,
  fold compensation, or commercial prepress approval.
- Final T-483 gates: 3 focused files / 11 tests passed, strict web typecheck
  passed, task-scoped ESLint passed, and the development-mode Vite build passed.
  The corrective wide-screen Playwright regression also passed, including the
  single-column question-option geometry, and the final full web lint passed
  after the concurrent T-484 work landed. The production build continues to
  fail closed without the existing T-470 `pk_live_...` Clerk key.

### Verification status

- Full workspace typecheck: passed after the Event Architect real-metre
  coordinate-space insert was pinned to the shared typed constant.
- Full workspace lint: passed.
- Full unit matrix: 432 files / 6,761 tests passed — types 79 files / 1,745
  tests; API 99 / 2,051; web 243 / 2,929; Capture Factory 3 / 10; Twin Forge
  8 / 26.
- Production builds passed for the three build-bearing projects (types, API,
  and web) using the repository-documented local-validation Clerk key; Capture
  Factory and Twin Forge have no `build` script and passed their strict
  typecheck and test gates instead.
- `pnpm audit --audit-level high`: no known vulnerabilities.
- `git diff --check`: passed with line-ending warnings only.
- Reconstruction Python guard matrix: 9 cases, with 8 passed and one real
  Windows symlink-creation case skipped for missing process privilege; the
  deterministic mocked link/reparse regression passed.
- Focused types matrix: 15 tests passed, including 9 Event Architect engine
  cases for determinism, canonical digests, geometry, pricing, prompt isolation,
  simulated guest-flow hashes/assumptions, and blocking review gates.
- Focused Event Architect web/API-client matrix: 30 tests passed.
- Focused Event Architect API integration: 3 tests passed.
- Focused Mission Control component matrix: 4 tests passed, including the two
  browser-discovered timeline regressions.
- The real Capture Factory stage and idempotent reread completed with all 156
  selected targets verified and no remaining partial files.
- The first complete suite caught an authority-disclosure wording regression.
  The copy was corrected to the explicit boundary `not safety, occupancy,
  accessibility-route, or statutory determinations`; the final 432-file /
  6,761-test matrix was then rerun and passed without the prior WebSocket
  listener or localhost network warnings.

No source-drive or verified-stage mutation, production database write,
package-wide codename rename, fake realtime layer, public capture promotion,
T-091 closure, T-105 closure, or unsafe evidence claim was introduced.

### Continuation: release CI portability

- Repaired the Linux-only capture-control unit assertion by normalising path
  separators before checking the generated report path.
- Updated stale browser assertions for the current planner command deck,
  private-brief landing copy, compact Truth Mode disclosure, Event Day focus
  cycle, and Mission Control controls. Production-live route tests now run only
  when an explicit HTTPS `E2E_BASE_URL` is provided, rather than falsely
  exercising production expectations against localhost.
- The Trades Hall visual-shell suite now deterministically mocks its two
  unauthenticated adjunct endpoints. Playwright runs in the venue timezone so
  visual/date assertions do not depend on the CI host timezone.
- Added 22 reviewed Chromium Linux visual baselines and regenerated the current
  Windows Event Day reference. The full 22-scenario visual/performance slice
  passes on Windows and in an isolated Ubuntu/Chromium environment under the
  pinned `Europe/London` timezone. The generated Linux files were copied back
  with byte-identical SHA-256 verification. Focused editor, keyboard, Truth
  Mode, Trades Hall visual-shell, private-brief, and Twin browser checks also
  pass.
- Made the proposal error-state capture wait for `document.fonts.ready` before
  calculating its bottom scroll position. This removes a real 36-pixel
  Windows race caused by late Inter/Playfair font metrics without weakening
  the visual-difference threshold; repeated no-update runs pass on Windows and
  Ubuntu Chromium.

### Continuation: Trades Hall camera-facing dollhouse section (T-484)

- Added a venue-scoped vertical cutaway plane that follows the orbit camera and
  removes the camera-facing scan shell in Dollhouse mode. It deliberately uses
  only the horizontal camera vector, so elevated orbit angles cannot slice the
  floor, ceiling, or dome.
- The cached GLTF hierarchy, geometry, textures, and source materials remain
  untouched. The presentation clone owns only its cloned materials and
  disposes them on teardown; invalid inputs fail safe to a non-clipping plane.
- Scan-node dots use the same plane for rendering and hit eligibility, so a dot
  hidden behind the section cannot remain an invisible interaction target.
- The feature is gated to the real `trades-hall` venue slug. A real-browser run
  loaded the configured Twin bundle, selected Dollhouse, rendered the exposed
  Grand Hall interior, and reported no console or page errors. Walk and Plan
  behaviour remain unchanged. This is visual presentation, not evidence
  signing, runtime promotion, measurement certification, or T-091 closure.
- A nearest-capture triangle-winding experiment met its numerical invariant but
  failed the visual gate by opening holes in the dome and far wall. That path,
  its mesh-only refresh command, and its regression fixtures were removed. The
  ignored local bundle was restored to the unchanged production GLB (7,158,232
  bytes, SHA-256 `bed9002f096cd2ed32f30d0e869bcfed359cb93798daac3338e2f4046ca4c431`)
  and re-verified at 149 nodes / 349 edges before final browser QA.
- A final four-heading real-bundle tuning pass set the venue-scoped section
  inset to 3.5 metres and added `packages/web/scripts/twin-cutaway-check.mjs`
  as a focused repeatable browser gate. All four reviewed headings retained a
  readable interior cutaway with no application console or page errors.
- Final T-484 gates: 22 focused tests; all 19 Twin unit files / 143 tests; strict
  web typecheck and full web lint; all 8 Twin Forge files / 26 tests plus its
  typecheck and lint; and Playwright CLI checks of the exact real-bundle route
  at boot, three orbit headings, Plan, Walk, and a visible-dot dive. The final
  direct Dollhouse load emitted only the development React DevTools info line.
  Production deployment was not performed in this workspace session.
