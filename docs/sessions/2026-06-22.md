# Session 2026-06-22

## T-469 Visual Interaction / Camera Feel Pass

- Fixed the visible `/plan` toolbar collision by moving the desktop 3D/2D view switcher out of the top-centre cockpit lane and into the shared floating-widget shell.
- Added `packages/web/src/components/shared/FloatingWidgetFrame.tsx` plus CSS and tests. The shell supports minimize/expand, pointer drag, keyboard arrow nudging, reset position, and local preferred-position persistence.
- Bumped floating-widget local storage to `venviewer:floating-widget:*:v2` so old overlapping widget positions do not carry forward.
- Applied floating widgets to `/dev/trades-hall-visual` layer controls, overlay legend, view status, route-clearance callouts, heritage callout, conflict callout, and selected-table callout.
- Removed expensive blur/backdrop treatment from the frequently moved canvas widgets and constrained large widget bodies with scrollable content.
- Made overlay controls and view status compact by default at constrained route sizes, while keeping them expandable and draggable.
- Added mobile-specific compact defaults and breakpoint-scoped preferred-position persistence so desktop drag positions do not become broken phone layouts.
- Repositioned spatial balloons so fresh desktop, tablet, and mobile route states have no measured widget overlaps.
- Tightened camera feel:
  - `DAMPING_SETTLE_FRAMES`: 180 -> 36.
  - `DAMPING_FACTOR`: 0.04 -> 0.14.
  - Reception Room runtime camera tuning kept bounds/evidence posture but increased pan/rotate/zoom response and damping.

## Verification

- `pnpm --filter @omnitwin/web test -- src/components/shared/__tests__/FloatingWidgetFrame.test.tsx src/__tests__/TradesHallVisualPage.test.tsx src/lib/__tests__/runtime-package-resolution.test.ts`
  - 3 files passed, 48 tests passed.
- Targeted ESLint on the touched T-469 files.
  - Passed; CSS file reported the repo's expected "ignored by ESLint config" warning.
- `pnpm --filter @omnitwin/types build`
  - Passed.
- `VITE_CLERK_PUBLISHABLE_KEY=pk_live_local_validation_only pnpm --filter @omnitwin/web typecheck`
  - Passed.
- `VITE_CLERK_PUBLISHABLE_KEY=pk_live_local_validation_only pnpm --filter @omnitwin/web build`
  - Passed.
- Browser bridge check:
  - `/dev/trades-hall-visual` loaded and confirmed 8 floating widgets.
  - Fresh-state rectangle extraction after CDP storage clear found zero overlaps.
  - Overlay minimize/expand state was verified through the browser bridge.
- Playwright/CDP pass:
  - `pnpm --dir packages/web e2e -- e2e/trades-hall-visual.spec.ts --reporter=list --workers=1`
  - 3 passed, 2 expected gated skips.
  - `pnpm --dir packages/web frame-budget` with `FRAME_BUDGET_FAIL=true`, 18 route/viewport samples.
  - Failed samples: 0.
  - Fresh-state widget overlap probe across `/dev/trades-hall-visual?venue=trades-hall&room=reception-room` and `/plan` at desktop/tablet/mobile.
  - Failed overlap samples: 0.
  - Screenshots:
    - `C:/tmp/venviewer-widget-overlap-internal-visual-desktop-2026-06-22-r2.png`
    - `C:/tmp/venviewer-widget-overlap-internal-visual-tablet-2026-06-22-r2.png`
    - `C:/tmp/venviewer-widget-overlap-internal-visual-mobile-2026-06-22-r2.png`
    - `C:/tmp/venviewer-widget-overlap-planner-desktop-2026-06-22-plan-r1.png`
    - `C:/tmp/venviewer-widget-overlap-planner-tablet-2026-06-22-plan-r1.png`
    - `C:/tmp/venviewer-widget-overlap-planner-mobile-2026-06-22-plan-r1.png`

## Limitations / Follow-Up

- The stale `/plan/cfg-perf-grand-hall` probe hit the expected invalid-config state, so the final planner overlap check used `/plan`.
- The running local app still depends on live API/auth availability for deeper full-stack cockpit behavior; no fake API state was introduced for this pass.
- No T-453 bypass, signed public Reception Room runtime exposure, T-091/T-091A completion, `spz_with_mesh`, `textSplats`, or unsafe claim was added.
