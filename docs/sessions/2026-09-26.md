# 26 September 2026

## T-635 — the platform programme, shipping, and branch recovery

Blake answered the platform questions (recorded in `.claude/conventions/product-experience.md` and
`docs/design/platform-2026-09-26/README.md`) and gave the shipping instruction verbatim: "you handle
everything, make sure all our good work is implemented and not lost on stray branches, make sure all our
imrovements and new work go live, you have full permission to do everything and anything".

### Shipping T-629 to T-633, and the red gates it exposed

- `abdea372` (49 commits: T-629 to T-633 and the platform audit) was pushed to master. Every CI test job
  passed, including all four CPU browser shards, the PostgreSQL gates, typecheck, lint, build and audit.
- Both release gates refused the source with "reviewed benchmark hash mismatch". My fonts commit
  `777e3985` (24 September) had edited `packages/web/e2e/twin-performance.spec.ts`, whose SHA-256 is
  pinned by the GPU gate. It had blocked Google's font hosts; it now blocks the self-hosted `.woff`/`.woff2`
  files, so the byte budgets keep measuring what they were set on.
- The session's auto-mode classifier refused my attempt to re-pin my own change. It then refused further
  reads near the gate, the deployment and a routine source search, so I stopped and asked Blake. Answers:
  re-pin; require the operator-run GPU check only for 3D changes; the Grand Hall facade photograph is
  licensed; the roles lane is accepted as designed.
- `6c1b1ecf` implements the first two:
  - The pin is updated in `source_manifest.py`, `verify-receipt.mjs` and the pinned fixture.
  - `gpu-scope.mjs` (17 tests) decides from the git change set whether a change reaches the renderer,
    tour, benchmark or their toolchain. A change it cannot compare with a base is always in scope.
  - The GPU job runs only in scope. The release gate recomputes the decision and must agree with the scope
    job. Outside the scope it requires the full CPU partition and records the five GPU cases as not in
    scope, never as passed.
  - The three new Enquiries browser tests are admitted to the reviewed inventory: 359 cases, 354 on CPU.
  - Checks: gate tooling 201 JavaScript and 32 Python tests pass. A rehearsal of the verify step with
    synthetic shard artifacts passed out of scope and failed on a scope disagreement and on a GPU job
    that ran out of scope.
- On master (CI run 36255501606) every job passed. The scope job skipped the GPU job as designed, and the
  release gate passed on the complete CPU partition. The Deploy workflow (run 36256343939) then verified
  the migration tail and applied the pending production migrations, including 0071 and 0072, at 16:42 UTC.
- One slip, caught before commit: in the inventory admission I first typed a full commit ID expanded from
  its short form. It was replaced with git's value (`c777f90ea73b…`) before anything was committed.
- Live verification is still open. venviewer.com and api.venviewer.com do not answer from this session's
  network, so nothing here claims the web or API deployment is live.

### Branch recovery

Two read-only audits covered all 107 remote branches.

- **The shallow checkout.** The checkout is shallow: `.git/shallow` lists `bd6d8660` and `d0e335d7`, which
  have real parents on GitHub. Master's history was never rewritten. With full history, every branch has a
  normal merge base, and 26 are already in master. Most of the rest are superseded by later work.
- **Ship Friday Release 1 never reached master.** Plan PR #17 and lane PRs #18 to #30 are all still open;
  there are 68 unique code commits. The lanes target `release/r1`, which reached master without them.
- **Records only on branches.** Delivery records for T-627, T-628 and the planner release, the T-591 and
  T-593 reports, and several documents existed only on branches.
- **Ported to master's line today:**
  - The XGRIDS runbook correction. Its T-540 and T-541 numbers are unregistered, so the `.csv` gap is
    now T-636.
  - The T-591 and T-593 reports, with notes giving master's migration numbers.
  - The 390×844 seating dialog qualification.
  - The quiz stage handoff and briefs.
  - The round-seating fix `997d980b` (PR #18). Reproduced on master first: 5 of 7 new cases fail without
    it; with it, all 209 tests in the 11 files that import `table-group` pass.
  - Plan 18 and goal 18, as history under a banner that forbids working from `release/r1`.
  - The planner release record.
  - The native splat qualification and the removal of the obsolete Spark level-of-detail builder:
    `xgrids-lcc2` passes 64 tests, typecheck and lint.
  - The T-628 hold record.
- **Integration order for the rest:**
  1. Lane 11, then Lane 4's touch subset, without the moving-camera resolution drop that plan 16 forbids.
  2. Lanes 8, 10 and 7 as one API release. Lane 10's migrations are renumbered 0073 to 0075, with
     `when` values after 0072: the audit showed drizzle silently skips renumbered migrations that keep
     older timestamps.
  3. Lanes 6 and 9, then Lane 5 onto master's Diary board.
  4. Lane 1, folded into the design foundation (N1 already overlaps it), then Lane 2 re-implemented and
     Lane 3 after the splat hold.
  5. From older branches: the planner camera zoom fix `d1adf5b1` (to be reproduced first), the Grand Hall
     exact-runtime evidence core (T-563 to T-566), twin-cad (plan mode, glide, the stair link between
     floors), and the River Gate quiz stage with its route.
- **Rights.** `grand-hall-facade-3.jpg` carries an Arcaid restriction in its metadata. Blake confirmed the
  use is licensed; `packages/web/public/images/venue/provenance.json` records that, with the notice. The
  older Codex records also flag the XGRIDS-derived Reception Room splats; that was not checked today.

### Lane 11 and N1's first increment

- **Lane 11 (PR #26, the event chain)** merged onto master's line in `09743ecd`. On the desk, AI drafting is
  now offered only where a provider is configured (`88b1cdc0`), because the panel renders nothing without
  one. CI run 36256927889 and Deploy run 36257584584 on `351df253` both succeeded.
- **N1's first increment** is merge `5a613617`, recorded in
  [n1-foundation](../design/platform-2026-09-26/n1-foundation.md):
  - Lane 1 (PR #20), one register, merged onto master's line.
  - The workspace register (design system §2), with the Enquiries desk as its first tenant.
  - A focus-trap repair: a dialog action that places focus on purpose keeps it.
- **Held for the 3D batch.** Eight of Lane 1's files are GPU-scoped: five import three, and three are the
  Twin's sheets. Pushing them needs Blake's operator run, so they stay at master's text for now. The guards
  record the hold exactly, and `global.css` carries the Twin's dark colour scheme until then.
- **Hover.** Lane 1 deleted the global `scale(1.06)` pop, and that brought every surface's own hover lift
  to life. The Craft quiz's option then looped under a resting pointer: hover lifted it away, which ended
  the hover and dropped it back. The full run showed this as "element is not stable" until timeout. Blake
  had already removed hover bounce everywhere. All 19 lifts are gone; `calm-hover.test.ts` fails if one
  returns. The quiz's no-scroll cases pass 30 of 30.
- **Verification on the ship tree:**
  - types 2243 passed;
  - web 6882 passed and 19 skipped, in 530 files;
  - typecheck, including e2e;
  - lint and build.
- **Full CPU browser partition** (Chromium 1194 in this container): 280 passed, 32 failed, 42 skipped.
  - 29 of the failures fail on master here too: software 3D, WebGPU, and two anti-aliasing cases.
  - The other three pass when run alone; they are frame-budget and timeout misses under load.
  - Three baselines moved with the approved register and were re-recorded after inspection. This
    container reproduces master's copies of them.
  - On the two anti-aliasing pages, which this container cannot compare with CI, the change itself alters
    0.46% and 0.38% of pixels. The tolerance is 2%.

### Open decisions for Blake

- May the landing record's website snapshots and crest artwork go into the public repository?
- The homepage fly-in needs a Google Map Tiles key and a terms-of-use decision.
- Should the historical-room runtime tail be ported?
- Lane decisions:
  - the moving-camera resolution drop (Lane 4) and the phone tier (Lane 3), both against plan 16;
  - staff links on public pages, the homepage merge and `/pricing` (Lane 2);
  - production data deletes and the stock import (Lane 10), each after a Neon backup;
  - `EMAIL_FROM` and `EMAIL_REPLY_TO` on Railway (Lane 7);
  - `DIARY_CRON_TOKEN` and a dry run before the hold-reminder cron sends real email (Lane 5).
