# 2026-07-22

## T-528 · Local PostgreSQL verifier follow-up

Continued the room-layout timeline PostgreSQL proof by repairing the operator
verifier's local connection path.

- Extracted `configureNeonDriverForDatabaseUrl` from the API database client's
  existing locality branch. Both `createDb` and the migration-tail verifier now
  configure the process-global Neon WebSocket driver immediately before pool
  construction. Localhost continues through the dev proxy on port 54331;
  non-local URLs restore the production defaults.
- Extended the existing local database tests to cover the complete proxy
  configuration, remote-default restoration, and cleanup after every test.
  The readiness suite also pins configuration before verifier pool creation.
- Updated the opt-in PostgreSQL proof to locate migration 0060 by its immutable
  `1784383200000` ledger timestamp and SQL hash. This keeps the proof exact now
  that migration 0061 legitimately follows it.
- The first rehearsal database used a safe but unsupported name; the test's
  fail-closed prefix guard rejected it, and the cleanup block confirmed zero
  databases remained. The rerun used the required
  `omnitwin_timeline_0060_*` prefix.
- On that fresh PostgreSQL database, both migration passes succeeded. The
  read-only verifier reported 60 applied / 60 local migrations, zero pending
  entries, matching timestamps and hashes, complete prerequisites, and
  `catalogPreflightPassed=true`. The real Fastify/auth freeze and timeline
  suite passed 3/3, including migration-0060 metadata, concurrent freeze
  serialization, canonical timeline retrieval, and RESTRICT deletion guards.
- Final verification: focused driver/readiness tests 18/18; opt-in PostgreSQL
  proof 3/3; API typecheck and lint clean; `git diff --check` clean apart from
  repository line-ending notices. The disposable database was dropped and
  confirmed absent. Shared `omnitwin_dev` and production were not targeted.

## T-542A - Windows Picker / Node Path-Reopen Preview V0

Completed the bounded picker/path-reopen sub-slice while keeping parent T-542
and the wider Foundry goal active. Genuine Windows OS drag-and-drop is not yet
implemented.

- The process-owned Windows helper now uses the Common Item Dialog to choose
  multiple files, a folder and an output folder. The browser accepts no source,
  workspace, helper or filter paths. Node.js reopens the selected canonical
  paths locally after selection; this slice makes no retained-handle native
  byte-custody claim.
- The local controller keeps a neutral multi-source basket, inspects each root
  and creates one T-541 child workspace per selected root. A durable top-level
  collection index binds terminal children, and the reopen verifier checks all
  stored child workspaces. Remove/Clear are deliberately absent until helper
  lifecycle reconciliation exists; restart the session to change the list.
- Browser view/action/report DTOs are path-free. Neutral File/Folder labels,
  progress, picker cancellation/failure, durable success, partial failure,
  all-failed, index-failed and cancelled outcomes remain distinct. Success is
  shown only after a report confirms a stored collection index and at least one
  stored child. Truth remains pending review throughout.
- The generated local flow selected two files (89-byte OBJ and 154-byte PLY)
  plus one folder containing one nested 85-byte text file: 3 roots, 3 files and
  328 bytes. All three children stored, source/copied SHA-256 values matched,
  collection index SHA-256 was
  `d8c95b5fbd12d6e0a5ef90cde02f90ed3a5d48b13dff016fc803e2112fda0cd3`,
  and reopen verification returned `storedChildrenVerified=3`.
- Child workspace SHA-256 values were
  `0c1fe98982008077969727a9d0026a725af2c4472734d757a46b38ee20f6b074`,
  `e161d88badf287e64e0bfba9d526e4dd8779cf078482d7d82038b8dea78fe129`
  and `80644cb06557f205d2df811cf0b98c77980908ef9b5651c168192d75ce7d4c47`.
  The public report contained no path, SHA-256 or digest fields.
- Desktop Browser QA reported 1265/1265 client/scroll width. A 390x844 mobile
  override reported 375/375, three neutral rows and visible durable success and
  report controls. Warnings/errors were zero. Final listeners and helper
  processes were both zero.
- A native folder-root defect was found and fixed. Rust helper tests are
  121/121; TypeScript protocol/bridge/adapter/intake tests are 112/112. The
  rebuilt debug helper is 2,275,840 bytes at SHA-256
  `3bdd27002c73475c2e31a1f81ed47256beb7e65c969fdc31c2892b88fb7ea856`.
- Final focused tools are 47/47 with typecheck, targeted lint and diff check
  green. Full tools are 57 files passed/1 failed and 928 tests passed/1 failed;
  the only failure is the pre-existing unrelated untracked
  `captured-quality-comparison.d.ts` release fixture. The known tools build
  remains at 9 TS6059 plus 9 TS6307 diagnostics, none naming T-542. Core is 68
  files, 797 passed/1 skipped, with typecheck/build green; its two lint parser
  errors are unrelated generated declarations.

No venue/user source, reconstruction, registration, training, enhancement,
cloud, credential, spend, signing, publication, deployment or cybersecurity
work occurred. Exact evidence is in
`docs/reports/omnitwin-windows-picker-path-reopen-preview-v0-evidence-2026-07-22.json`.
Next is T-542B: a genuine Windows OS drop target feeding the same process-owned
basket and path-free browser, still without reconstruction or cloud work.

## T-542B · Windows Explorer Drop / Node Path-Reopen Preview V0 — implementation verified, real gesture pending

Implemented the genuine helper-owned Windows drop-target path while keeping
T-542B, parent T-542 and the wider Foundry goal in progress until one real
Explorer gesture proves the complete flow.

- The Rust helper build identifier is now
  `venviewer-windows-source-helper/0.2.0`. A dedicated OLE STA owns a visible,
  topmost Windows panel and message loop. Both the top-level panel and its
  instruction child are registered as `IDropTarget` surfaces and revoked before
  teardown, so the child text does not form a drop hole.
- The target accepts only `CF_HDROP` through `TYMED_HGLOBAL` when Copy is
  offered. Accepted drops return Copy; Move-only or invalid offers return None.
  One `drop_sources` command can carry a mixed file/folder gesture and returns
  `drop_sources_ok` with acquisition evidence
  `windows_native_drop_cfhdrop_then_handle_open`.
- All roots in one native command are opened and validated before the helper
  extends its retained source list. The browser basket also waits for complete
  batch limit/conflict validation before appending anything. If later basket
  validation rejects a helper-selected batch, the visible list remains
  unchanged and the controller closes the helper; no rollback-and-continue
  claim is made for that same helper session.
- Closing the panel or pressing Escape is a distinct `drop_cancelled` outcome
  with no selections. A real cancellation observation closed the panel, left
  the browser basket unchanged and rendered neutral copy.
- The browser has an **Open drop area** action only. It does not register HTML5
  drop/drag listeners, use `DataTransfer`, accept a file input or receive file
  paths. The loopback response boundary now enforces exact public-field and
  schema-version allowlists, generated `File N`/`Folder N` labels and rejection
  of basename, relative and absolute locator strings. The separate Windows
  panel owns the gesture; Node retains the explicit path-reopen boundary. This
  remains `ordinary_windows_native_selection_node_path_reopen_preview`, not
  native retained-handle byte custody.
- The existing local intake flow preserves one T-541 child workspace per root,
  a durable top-level collection index and pending-review truth. Those
  persistence facts are implemented but are not yet claimed as verified
  through a real Explorer drop.
- App shutdown now keeps an expired session in stopping state and retries an
  unconfirmed close with bounded exponential delay. Browser and programmatic
  stops share one attempt, close the controller/helper before the listener and
  remain retryable after failure. CLI `SIGINT`/`SIGTERM` requests the same
  confirmed stop, including a signal received during startup or a retry after
  an unconfirmed attempt.
- The final lifecycle/privacy integration gate passed 46/46 focused tests, plus
  typecheck, focused lint and diff check. Signal delivery used deterministic
  injected `SIGINT`/`SIGTERM` events with deferred shutdown; an isolated real
  Windows console/ConPTY signal-delivery smoke was not run.
- Final integrated gates are T-542-focused 9 files / 210 tests passed. Full
  tools are 57 files passed / 1 failed and 949/950 tests; the sole failure is
  the pre-existing unrelated `captured-quality-comparison.d.ts` Windows
  release-fixture check in the shared dirty worktree. Core is 68 files, 799
  passed / 1 skipped, with typecheck and build green. Core lint remains
  qualified only by the two known generated-declaration project-service errors.
- Debug builds contain path-free OLE stage telemetry only. The optimized
  release binary contains no `omnitwin_drop_trace` marker. Native verification
  is 136/136 tests, with format, all-target Clippy under `-D warnings` and the
  optimized release build green. The release helper is 778,752 bytes at SHA-256
  `6d82ceb864e69b6d8d29ffef1dc1fa038a92ae484f3cccabcb19c8464129d9c0`.

A generated non-venue Explorer fixture is ready: two file roots plus one folder
root, totalling 3 roots / 3 files / 187 bytes. Its three hashes were rechecked
and the output parent still contains only its preparation marker. Calibrated
Computer Use pointer drags emitted zero OLE telemetry: the automation moved the
pointer but did not initiate or reach Explorer OLE. That is an automation
limitation, not proof of either acceptance or rejection by the helper.

The required real mixed Explorer gesture is therefore still pending. Do not
mark T-542B or T-542 done yet. After that gesture, verify three neutral browser
rows, exact copied hashes, one durable T-541 child per root, collection reopen
with three verified children, unchanged originals, final browser console/layout
and zero listener/helper processes. The integrated code gates are already
recorded above; the physical gesture and its resulting local evidence remain
the completion boundary.

No venue/user source, reconstruction, registration, training, enhancement,
cloud, credential, spend, signing, publication, deployment or cybersecurity
work occurred. Qualified evidence is in
`docs/reports/omnitwin-windows-explorer-drop-path-reopen-preview-v0-evidence-2026-07-22.json`.

## Grand Hall ordinary provenance intake templates — blank user handoff

Added a bounded, user-completable intake pack for the three external records
that cannot be manufactured by local engineering:

- `docs/operations/grand-hall-source-processing-rights-intake-template-v0.json`
  has separate blank Matterport, XGRIDS and venue/commissioned-content records,
  purpose-by-purpose decision fields, exact asset/supporting-record shapes and
  seven fixed-false guardrails.
- `docs/operations/grand-hall-venue-review-and-release-scope-intake-template-v0.json`
  records the reviewer's identity, relationship to the venue, basis of venue
  authority, knowledge basis, exact reviewed evidence and one unchosen
  whole-release or bounded-Grand-Hall scope. The existing phase-1 operator
  records are digest-bound context only; every acceptance and release field
  remains blank or false.
- `docs/operations/grand-hall-independent-survey-control-intake-template-v0.json`
  predeclares exactly eight fit-control slots and six blind-check slots. Every
  slot requests coordinates, uncertainty or covariance, instrument record,
  operator, observation time, raw-observation digest and target-photo digest;
  all six blind checks are fixed `usedForFit: false`.
- `docs/operations/grand-hall-ordinary-provenance-intake-guide-2026-07-22.md`
  gives plain completion instructions, recommends the bounded-Grand-Hall
  preparation route and preserves the distinction between intake evidence and
  a later review decision.

Validation passed for all three JSON documents: 3/3 parse, 3/3 are
`blank_user_completion_required`, authority is none and `unsigned` is true;
three rights-source records are blank; all 21 guardrails are false; survey
counts are 8 fit / 6 blind with 14 unique slot IDs and exact role/use flags; no
out-of-scope terminology was introduced. The referenced decade-30 pack digest
was rechecked. The existing T-486 r2 preflight was not modified or copied: its
22-file exact tree and all 21 indexed members still verify, and its manifest
SHA-256 remains
`3fba49c89207b6b78fbba067436c6a2a993efada113c9c0267cb625ea06203fd`.

No source right, venue decision, survey observation, transform review, release
scope, Scene Authority Map, T-486 review input, approval or publication request
was created. The next human action is to copy the needed templates to dated
filenames, complete them with cited supporting records and return the copies
for local validation. Only then may engineering prepare a new, separately
named, unsigned preflight while preserving r2 unchanged.

### Independent-review hardening

The intake contracts were tightened after an independent shape review:

- every source family now records local processing, derivative creation,
  derivative internal use, derivative commercial use, public display, public
  twin publication, original-source redistribution, derived-output
  redistribution, cloud processing and model training as separate decisions;
- the release-scope record now binds a frozen inventory by reference, SHA-256,
  observed count and exact 149-node ID list. A bounded scope must provide
  disjoint included/excluded lists whose exhaustive union is that inventory,
  with recomputed counts and `allReleaseNodesPartitioned` evidence;
- each of the 8 fit and 6 blind survey slots is now a paired source-side and
  independent-survey observation with explicit frame IDs, both coordinate
  triples, observation references/digests and deterministic point-pair
  evidence. The record also freezes the pre-fit plan identity, digest and time,
  records independence provenance and same-lineage exclusions, and records the
  sealed blind-bundle custody/release chronology; and
- the guide now states that all 21 guardrails remain immutable false,
  `unsigned` and all 14 `usedForFit` values are fixed, and the only three
  mutable booleans are the exact classification/partition completion fields in
  the venue template.

Post-hardening validation passed: all 3 JSON files parse and retain neutral
blank state; all required separate rights fields exist in all 3 source records;
all paired-control fields exist and remain blank in 14 unique slots; the
pre-fit slot lists exactly match those slots; the frozen 149-node inventory and
partition contract are present; the three-booleans-only contract matches the
guide; all 21 guardrails remain false; and the four pack files contain no
out-of-scope terminology. The decade-30 context digest remains
`12a54cfbe587270105f44cfbdc0582d855b8d9b270e231e3ebf1f5d860798e7e`.
T-486 r2 was rechecked without modification: 22 files, all 21 indexed
members exact, manifest SHA-256
`3fba49c89207b6b78fbba067436c6a2a993efada113c9c0267cb625ea06203fd`
and package digest
`d2411e3f5a0ab3206c1dc174c71005130e5cd26df6369ddd2ec6e10b5eb9a85b`.

## T-507/T-486 · July-19 diagnostic scope correction

A bounded audit found that the July-19 one-off fit runner admitted adjacent-space
sweep 49 and moved the frozen T-507 holdout from sweep 44 to sweep 45. The runner
now selects the exact 0–48 scope before partitioning, uses the frozen holdout
`[5, 15, 25, 35, 44]` and fails closed if any in-scope sweep is missing or
duplicated. Fifty correspondences are discovered, 49 enter the bounded
diagnostic, 44 enter the fit and five remain held out; sweep 49 enters neither
set.

The generated edge target is now `e57-global-diagnostic`, with an explicit
same-lineage E57 scan-pose label. It is authority none, not independent survey
control, not a reviewed transform and not the governing release transform. The
corrected result has scale 1.7362021512; fit median/RMSE/p95/max are
6.336/11.021/16.961/45.905 mm, and holdout median/RMSE/p95/max are
3.831/5.757/9.679/9.679 mm.

Before regeneration, both exact local inputs were rehashed read-only:

- `F:/E57/cloud_0.e57`: 20,518,437,888 bytes, SHA-256
  `975039d11fc04ca681f038e499f358124bbcab178ad5ce6324fa912212729cdd`;
- `F:/E57/colmap_v2/sparse/0/images.bin`: 43,053,735 bytes, SHA-256
  `6621fcb62dc5d68653df92dd5a3da2b6428a3817488a868cd86504fe048615b4`.

Only the July-19 residual report and proposed transform were regenerated. Their
new exact receipts are respectively 3,423 bytes / SHA-256
`54d670f2bb0799591e6ad4722612ba42a42127ebdb5f03cf9d93b311ecc4a779`
and 762 bytes / SHA-256
`e07d2fba336502395900c2a953732e226346f52347a221cf829c1bdd1ac86075`.
The original T-507 output remains exactly 13 files / 2,888,304 bytes with
deterministic inventory SHA-256
`9ec3584eccb692279966f8de184eadb16519f97cf5b581ecb38d4af587f5fbcf`.
T-486 r2 remains exactly 22 files / 4,727,280 bytes with deterministic inventory
SHA-256
`349f8b9fb7cfe07d012210914f7cad350015a3eedca3cbc6245c26342333595b`;
its package manifest remains
`3fba49c89207b6b78fbba067436c6a2a993efada113c9c0267cb625ea06203fd`.

Focused verification passed: 7/7 regression tests, focused lint, deterministic
regeneration, semantic/diff checks and a direct strict TypeScript check of both
changed source files.
The package-wide typecheck was also attempted but remains qualified by the
unrelated untracked `local-native-collection-analysis.test.ts`, whose imported
implementation is absent and which already has its own implicit-any/assertion
typing errors in the shared dirty worktree. That user work was not changed.

This correction adds no survey, rights decision, release approval, signing,
publication or deployment work. T-486 remains an offline in-progress preflight;
the external independent-survey, rights and release-review gates remain open.

## T-544 · Durable Collection → Foundry Workbench Bridge V0

Completed the bounded process-only bridge from a durable native-intake
collection into the existing local Foundry inspection chain. This work was
initially discussed under the next task label, but the ledger already assigns
T-543 to the Reception source-photo diagnostic, so the bridge is recorded as
T-544 without changing that historical task.

- The T-542 controller now retains a process-only collection root plus exact
  index digest only after durable index publication. Neither value can be
  supplied by HTTP, and the root is never a browser field.
- The tolerant analysis reopen checks the exact caller digest, then reverifies
  every stored T-541 child independently. One damaged child becomes a neutral
  item failure while later intact children continue.
- Each verified copied payload runs through the existing V8 intake receipt,
  Source Facts, readiness and operator-checklist components. The resulting
  receipt must equal the T-541 receipt, and the whole child is reverified again
  after V8 before any result is published. The private copied-payload and child
  workspace locations never enter a public DTO.
- Public start/status/cancel/report values use only generated labels, artifact
  state/digest summaries, sorted unique fixed blocker codes and stable fixed
  next actions. Every item remains `needs_operator_review`; XBIN is
  `opaque_reference_only` with `XBIN_OFFICIAL_EXPORT_ONLY` and
  `OBTAIN_OFFICIAL_EXPORT`. A collection entry without a stored copy is
  `COPIED_PAYLOAD_NOT_STORED` with `RESTART_LOCAL_INTAKE`, distinct from a
  copied payload that fails T-541 verification. Cancelled or bounded-inspection
  failures direct the operator to `RESTART_LOCAL_SESSION`, because the
  controller is deliberately one-shot.
- Cancellation aborts the V8 pass, but T-541 verification has no AbortSignal.
  View/report/UI therefore pin
  `between_bounded_verification_steps` and say that the current verification
  step may finish before cancellation settles.
- The loopback app now has exact-empty-body analysis start/status/cancel/report
  routes, closes analysis before the intake/helper, and rejects private locator
  fields at the same strict serializer. The Step 3 UI renders neutral copied
  payload rows without HTML injection or browser drag-and-drop.

Generated mixed E57, OBJ, GLB, Gaussian PLY, JPEG, MP4 and XBIN fixtures pass.
A real default-core regression creates two independent T-541 children (5-byte
OBJ plus 9-byte GLB), completes both through V8 and post-verification, then
mutates one copied payload and proves only that child fails while the later
child completes; both originals remain unchanged. Wrong index digest,
after-V8 mutation, cancellation, path/basename leakage, truth-pending and
forbidden-action regressions also pass.

Final root-integrated focused verification is 5 files / 55 tests passed: the
four T-544 files plus the Grand Hall fit regression. The tools package
typecheck, targeted lint and scoped diff check pass. The whole tools suite is
qualified at 58 files passed / 1 failed and 958/959 tests: the sole known
unrelated failure is `build-windows-x64-production-release.test.ts`, caused by
the pre-existing
`packages/reconstruction-foundry/src/captured-quality-comparison.d.ts` emitted
sibling. This supersedes the earlier same-session observation that the
then-untracked analysis test had no implementation: the implementation now
exists and the package typecheck is green. Manual visual browser QA was not
performed in the initial slice. A subsequent manual pass found that Step 3
action feedback was being left in the distant Step 1 status and that polling
erased the report-open message. The bounded remediation gives Step 3 its own
start/cancel/report feedback, clears stale Step 1 feedback and preserves the
report-open status while the report remains visible. The four focused files now
pass 48/48 with package typecheck, targeted lint and diff checks green. Fresh
QA at 1280x900, 390x844 and 320x720 held the report-open message beyond two
polls, kept Step 1 hidden and empty, retained report focus, found no
viewport/report overflow and recorded zero console warnings/errors. The
upstream T-542B human Explorer gesture remains pending. No admission, plan
compilation, reconstruction, worker/provider dispatch, training, enhancement,
cloud, rights evaluation, signing or publication occurred.

Evidence:
`docs/reports/omnitwin-durable-collection-workbench-bridge-v0-evidence-2026-07-22.json`.
