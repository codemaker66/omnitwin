# 2026-06-25 Session

## T-470 Clerk OAuth Consent Route

- Reviewed Clerk's custom OAuth consent release notes and current guide for the new `@clerk/react` `<OAuthConsent />` surface.
- Upgraded `@clerk/react` from `6.4.5` to `6.11.0` so the web app can import `OAuthConsent`, `Show`, and `useOAuthConsent`.
- Added `packages/web/src/pages/OAuthConsentPage.tsx` and `OAuthConsentPage.css`.
- Added `/oauth-consent` to `packages/web/src/router.tsx` behind `ClerkRouteProvider`.
- Kept the page focused: no app navigation, no account menu, no custom grant/token approval logic, and no hiding of Clerk's requester/scope/deny/redirect safety UI.
- Added strict-origin referrer policy handling for the consent route.
- Added visible loading, signed-out, and Clerk failure states. The failure state was added after production-preview browser QA found the consent shell could otherwise go blank when Clerk rejects a `pk_live_` key on `127.0.0.1`.
- Added regression coverage in `packages/web/src/__tests__/auth-components.test.ts`.

Verification:

- `pnpm --filter @omnitwin/web exec vitest run src/__tests__/auth-components.test.ts` passed: 16/16.
- `pnpm --filter @omnitwin/web typecheck` passed.
- `pnpm --filter @omnitwin/web lint` passed.
- `pnpm audit --audit-level=moderate` reported no moderate-or-higher advisories; two low-severity advisories remain.
- `pnpm --filter @omnitwin/web build` passed with pulled production `VITE_CLERK_PUBLISHABLE_KEY` from `.vercel/.env.production.local`.
- In-app browser production-preview QA for `http://127.0.0.1:5189/oauth-consent` passed desktop and mobile layout checks. Local preview correctly logs Clerk's live-key domain-origin rejection for localhost; the UI now renders a visible `Consent screen unavailable` alert rather than an empty panel.

Remaining:

- Deploy the route after review.
- In Clerk Dashboard, configure the custom OAuth consent path to the deployed `https://venviewer.com/oauth-consent` URL before any OAuth application relies on it.
- Verify the route in a real OAuth authorization flow on the production domain; direct localhost preview cannot prove the approved production Clerk consent component because live keys are domain-bound to `venviewer.com`.

## T-470 Production Auth Presentation Hardening

- Diagnosed the user-reported Google error as a real provider-configuration problem, not a styling issue: Clerk was exposing a Google social button before Production Google OAuth credentials were verified, which led Google to reject the request with `missing client_id`.
- Added a shared Venviewer Clerk appearance module and wired it through `ClerkRouteProvider`, `LoginPage`, `RegisterPage`, `AuthModal`, and `UserMenu`.
- Hid unverified Clerk social sign-in by default behind `VITE_CLERK_GOOGLE_SIGN_IN_ENABLED`. The button can come back only after Clerk Production has real Google OAuth credentials and the Vercel flag is deliberately set to `true`.
- Replaced the remaining white inline auth modal and guest sign-in button styling with graphite/gold/cyan CSS.
- Added visible `ClerkFailed` alert states to login/register so a future Clerk domain/configuration failure does not leave a blank or misleading auth panel.
- Added regression coverage that asserts the provider and route-level forms use the Venviewer appearance and suppress unverified social sign-in.

Verification:

- `pnpm --filter @omnitwin/web exec vitest run src/__tests__/auth-components.test.ts` passed: 18/18.
- `pnpm --filter @omnitwin/web lint` passed.
- `pnpm --filter @omnitwin/web typecheck` passed.
- `pnpm --filter @omnitwin/web build` passed with pulled production `VITE_CLERK_PUBLISHABLE_KEY` confirmed as `pk_live_`.
- Pushed `bee627a` to `origin/master`; Vercel produced Ready production deployment `dpl_B3JNBYqSVJP8vnHUqvW2TwPc8vhi` aliased to `https://venviewer.com`.
- Full production-live route matrix against `https://venviewer.com` passed: 5/5 tests. Report: `artifacts/t469-production-live-route-pass-2026-06-21/report.json`.
- Direct live `/login` probe captured `artifacts/t470-live-login-auth-hardening.png`: no `OMNITWIN`, no `Development mode`, no console errors, no visible Google button, graphite/gold Clerk card rendered.

Remaining:

- Configure Clerk Dashboard -> Configure -> Paths -> custom OAuth consent URL to `https://venviewer.com/oauth-consent`.
- Keep Google social sign-in hidden until Clerk Production has valid Google OAuth credentials and a real Google sign-in test passes.

## T-470 Google Social Sign-In Enablement

- Owner saved Clerk Production Google OAuth credentials after creating a Google Cloud OAuth web client for Venviewer.
- Added Vercel Production `VITE_CLERK_GOOGLE_SIGN_IN_ENABLED=true` as a non-sensitive build-time flag.
- Pulled Vercel Production env and verified the flag resolves to `true`.
- Ran a Google-enabled production web build locally with `VITE_CLERK_PUBLISHABLE_KEY=pk_live_...` and `VITE_CLERK_GOOGLE_SIGN_IN_ENABLED=true`; build passed.
- Pushed `bafdf01` to trigger Vercel production deployment `dpl_2k4bXKKEt7HvWndFGG7KX3g6E1M3`.
- Verified live `https://venviewer.com/login`: one visible Google button, no `OMNITWIN`, no `Development mode`, and no console/page errors. Screenshot: `artifacts/t470-live-google-enabled-login.png`.
- Clicked through the live Google button far enough to reach `accounts.google.com` with a real `client_id` and no `missing client_id` error.
- Re-ran the full production-live route matrix against `https://venviewer.com`; 5/5 tests passed after Google was enabled.

Remaining:

- Rotate the Google client secret before real customer launch because it appeared in setup screenshots.
