# Session log — 2026-07-18

## T-527 · Diary Slice 7 — first-live-week operations (Diary lane)

Blake overrode the deploy-parking ("run Diary Slice 7") — right call: the
slice's substance is deploy-independent and everything was dress-rehearsed
locally like Slices 5/6. Commits `818140dd` (drift merge), `00caf4f4`,
`5e2c9a58`, `b4117e9d`.

- **Drift merge first** (standing §2 pattern): master@7f085484 (the Answers
  questionnaire + "the ring" homepage polish) — 10 files, zero overlap,
  clean.
- **Hold-reminder delivery job** — the Slice-1 P1 debt closed. Design in
  architecture doc §12; the load-bearing choices: NO new schema (idempotency
  keys ride `email_sends`' UNIQUE constraint — restart-proof, cron-race-proof),
  keys carry the decision's venue-local day (a MOVED decision date earns
  fresh T-7/3/1 instead of deduping against the old date — caught during the
  CLI rehearsal, fixed same hour), 24h freshness window (skip, never send
  late), house cleanup convention for scheduling (admin endpoint + cron CLI,
  no in-process timers — the CLI exists because Clerk admins can't be crons).
- **TDD throughout:** 16 service tests (due windows, DST fold, London-vs-UTC
  midnight key stamping, moved-date fresh-key regression, failure isolation)
  + 5 route-contract tests (isolated file so its service mock can't touch the
  shared admin harness).
- **Live dress rehearsals** on the seeded local week (stack revived after
  the reboot: PG needed its `-o "-p 54329"` start option — the port is
  runtime, not in postgresql.conf): service pass — 23 scanned, nudged hold
  due at T-7, dev-mode send recorded, second pass idempotent (exactly one
  email_sends row), seed restored; CLI pass — dry-run clean, exit codes
  proven.
- **typescript-reviewer: FIX-FIRST → all findings implemented literally.**
  P1s: the cron script missed the no-console carve-out (run-cleanup
  precedent); users join → LEFT (ownerless holds now visible in `scanned`;
  the null-guards became reachable); two regression tests for the exact
  key fix. P2s: `formatToParts`-built day (locale-ordering-independent
  inside a UNIQUE key), key derivation inside the failure isolation,
  unreachable-owner branch logs + counts failed, ownerName wired as a
  salutation (was fetched-but-dead). 21/21 after; lint + build clean.
- **Operations pack** `docs/operations/diary-first-week-operations.md`:
  smoke cadence + append-only results log + per-surface triage tree +
  reminder cron recipe with rehearsed safety properties; checklist §6
  points at it.
- **Union-suite attribution:** 5 Foundry migration-schema tests fail in the
  worktree ONLY because the G4 session's uncommitted `0059_action_log`
  journal entry shifts their tail-relative offsets (`at(-7)`); the
  committed tree (57 entries) is what they expect and was fresh-checkout
  green this morning. Heads-up for the Foundry authors: tail-relative
  journal assertions break every time anyone appends a migration.
- Also cleared a phantom: 8 pre-disconnect api build errors were stale
  tsbuildinfo/node_modules state — 0 after clearing incremental state.

## G4 Slice 3 (T-522) — persistence + the audit read model

- Both slice-3 preconditions cleared first (pollution guard is PRE-parse —
  zod's record parser eats __proto__ keys via plain assignment; bounded
  ingestion measures depth iteratively BEFORE any recursive path). Then:
  action_log table (0059; client-uuid pk for idempotent retries, server
  ordinal as the only read order, recorded_ts vs received_at distinct,
  submitted_by = the authenticated principal), bounded batch ingest + paged
  audit read at /configurations/:configId/actions, the web flusher on
  save/config boundaries, and verifyReplayable gating slice-4 replay.
- Reviewer Block earned its keep a third time: a fold landing during an
  in-flight flush stranded the cursor in a stale index frame (silent audit
  orphaning at routine cap usage — reproduced against the real modules);
  fixed structurally with id-resolved acks (markSentThrough). Plus read-side
  depth bounding (RangeError at ~1000 levels), intra-batch duplicate-id
  rejection, and submitted_by end-to-end. Full record in
  docs/plan/g4-action-log-program.md "Slice 3 — SHIPPED".
- My-scope gates green: types 2135 tests + build; api slice suites 24 +
  typecheck + lint; web slice suites 128 + lint; zero typecheck errors in
  my files. Foreign in-flight flags (not mine to fix): api lint red on the
  diary lane's run-hold-reminders.ts console statements; web typecheck +
  5 suite failures in the room-layout-timeline lane's uncommitted work.

## T-508 · Ordinary point PLY Source Facts V6 local super-app slice

Completed one bounded ordinary offline source-understanding slice. The broader
OmniTwin Foundry goal remains active; no source was changed, reconstructed,
uploaded, admitted, approved or published.

- Preserved immutable V1-V5 Source Facts meanings and introduced separate V6
  Source Facts, Source Readiness and Operator Evidence schemas and digest
  domains. Caller-supplied V5/V6 issuance remains outside the package root.
- Kept Gaussian inspection first. An established or explicit Gaussian outcome
  cannot fall through to ordinary points. The V6 point target is case-sensitive
  PLY 1.0 `binary_little_endian`, exactly one positive vertex element, unique
  fixed-width scalar declarations, required `x`/`y`/`z` names and exact
  header-plus-count-times-stride EOF arithmetic.
- Published limits: 128 GiB source, 1 MiB header, 64 KiB line, 64 elements,
  4,096 properties, 256 comments/`obj_info`, 100 million vertices and 32 KiB
  stride. The schema also proves the header byte count lies between what the
  recorded declarations could minimally and maximally encode.
- No scalar value is decoded. Ten mandatory unknowns retain attribute values,
  property semantics, physical bounds/completeness, units/scale, frame/CRS/
  axes, geometry role, accuracy/uncertainty, registration, provenance/capture
  class and rights. Readiness and checklist schemas reject omission.
- Audit fixes closed success/failure code and coverage substitution, classic-
  Gaussian masquerade, impossible small/large headers, full property-line
  overflow, omitted point unknowns and receipted point candidates downgraded
  outside V6.
- Bounded inventory: 199 PLY files (8 ordinary, 64 mesh, 127 Gaussian), five
  XYZ and no LAS/LAZ/PTS/PCD. The 1.61 GB headerless XYZ candidate did not get a
  complete row pass. XYZ, LAS/LAZ and decoded-value/bounds work remain distinct
  later profiles.
- Real positive: derived COLMAP `fused.ply`, 23,248 bytes, SHA-256
  `434571ab5a2355fe42ee45bbe9f8a6258fca3ec732ec16075a6ac808ac52c7e5`,
  852 vertices, 244-byte CRLF header, 27-byte stride and XYZ/normals/RGB
  declarations. Chain:
  `15895f3b… → bc792ad7… → 1fb58d77… → d60549ac…`.
- Real controls: mesh `0_19.ply` returned
  `POINT_PLY_EXTRA_ELEMENT_UNSUPPORTED`; `export_05000.ply` remained inherited
  Gaussian with no ordinary point facts. All three sources repeated byte-
  identically with unchanged size, mtime and SHA-256.
- Rendered QA at 1280×720 and 390×844 showed all nine property rows, no page
  horizontal overflow and zero console warning/error. Wide evidence tables
  remain panel-local scrollers. The three V6 JSON downloads were found and
  hashed on disk; the loopback listener was stopped.
- Gates: focused V6 23/23; frozen V1-V5 compatibility 57/57; full core 49 files,
  610 passed and one existing skip; core lint/typecheck/build passed; focused
  app 46/46 and CLI lint/typecheck/build passed. The broad CLI suite remains
  red only in concurrently changing unrelated offline-preview tests plus one
  full-suite-only 20-second 500-file timeout; every implicated file passes
  alone and no deterministic V6 failure was found.
- Independent consistency review found no remaining P0-P2 correctness issue.
  Evidence:
  `docs/reports/ordinary-point-ply-source-facts-v6-evidence-2026-07-18.json`;
  report:
  `docs/reports/ordinary-point-ply-source-facts-v6-report-2026-07-18.md`.

Next: do not widen V6. Choose a later XYZ, LAS/LAZ or decoded-value profile only
after a complete real-input pass supports explicit byte, line, count,
cancellation and exact identity limits. T-507/T-486 remain relevant only for
authority claims. T-508 and the broader `/goal` remain active.

## T-528 · Room layout day/week timeline

Built the user-requested room-layout timeline into the existing planner rather
than creating a detached mock screen.

- Added an authenticated room-scoped timeline API with strict eight-day and
  frame-density bounds, half-open phase overlap, effective snapshot ranking,
  canonical payload/hash validation, lifecycle truth, and explicit room-flip
  gaps.
- Added a proof-carrying phase keyframe producer. The request carries only
  identities; the server verifies event/phase/venue/room/configuration access,
  canonical snapshot freshness, every persisted object transform and asset,
  real-metre coordinates, and the attached validator proof before appending a
  deterministic frozen row with predecessor, proof, actor, and canonical
  lineage. Migration `0060` uses durable `ON DELETE RESTRICT` references.
- Added the planner dock: venue-local 04:00→04:00 Day ownership (including
  after-midnight breakdown), civil Week, real wall-clock phase blocks, overlap
  lanes, lifecycle labels, canonical thumbnails, play/scrub/`[`/`]`/Space
  controls, tumbling snapshot metrics, reduced-motion crossfade, collapse and
  explicit Exit Preview.
- Preview state is isolated from the editor/autosave/undo stores. Saved
  furniture remains warm but hidden; mutation tools and the right dock are
  replaced by a clear preview lock; topbar and minimap consume preview state.
- Visual QA at 1536×1024 caught an implicit CSS grid-column defect that had
  crushed the phase track and filmstrip; all timeline layers now span the full
  dock. Canonical thumbnail bounds include object extents so unusual but valid
  coordinate envelopes remain visible.
- The initial 500-object same-event morph exposed long main-thread stalls from
  reconciling one React/Drei child per instance. The timeline path now updates
  raw instanced matrices in batches. Browser measurements with full motion
  enabled recorded p95 13.9 ms and no >16.7 ms frames in the first 1.4-second
  500-object transition; warmed 113↔500 runs held about 8.05 ms average with
  one >16.7 ms frame across both samples. No browser warning/error remained.
- Final gates: web focused 81/81, API focused 53/53, shared types 6/6, full web
  source+e2e typecheck, API typecheck, shared-types build, and targeted lint
  passed. Production web build was intentionally not bypassed: the repository
  requires a real `pk_live_...` Clerk publishable key.
- Follow-up isolated PostgreSQL proof applied migration `0060` twice to a
  disposable PostgreSQL 16 database and retained the exact 59-entry migration
  ledger. The real Fastify/auth path raced two freeze requests: one created the
  immutable row, the advisory-lock follower returned `already_current`, and
  the timeline returned the same canonical frozen keyframe. Actor, canonical,
  and proof deletes each failed on the expected new RESTRICT constraint. The
  reusable opt-in regression is
  `packages/api/src/__tests__/phase-layout-snapshot-postgres.test.ts`. Shared
  `omnitwin_dev` remained at its pre-run 57 migrations, production was not
  modified, and the disposable database was dropped after the final pass.

Design QA: `design-qa.md`. Local preview:
`http://localhost:5173/plan/11111111-1111-4111-8111-111111111111?eventId=99999999-9999-4999-8999-999999999999`.

## T-529 · Reception captured-quality regression-triage comparison and local app surface

Completed one bounded authority-none slice under the active T-508 offline
super-app path. No cybersecurity, credential, cloud, signing or publication
work was needed.

- Froze the exact four-file Quality SOG profile (2,002,009 Gaussians), four-file
  Mobile SPZ profile (1,978,258), six same-centre review cameras, 1200×900
  renderer profile, two repeats and six pair-metric groups in a strict shared
  report contract. The contract can issue only `regression_triage_not_acceptance`,
  `authority:none` and `winner:not_selected`.
- Added the process runner, fail-closed controller, loopback HTTP routes, CLI
  configuration and responsive local-app panel. Start/status/cancel/report
  flows are token/origin/request/digest bound. Malformed progress, stale output,
  pre-existing/fake/symlink directories and cancellation races are rejected.
- The first full attempt reached Windows finalization then correctly retained
  no success after `EPERM`; watchdog shutdown and bounded rename retry repaired
  that path. The second uninterrupted run atomically retained 24/24 captures,
  six scored view pairs and a verified V0 report. Later cancellation-only
  hardening passed 19 deterministic tests but was not given another full run.
- Real evidence: all eight sources matched before/after and on final rehash;
  24 PNGs matched 24 manifest entries; 12/12 repeat pairs were byte-identical;
  no capture mismatch, external request or console error was recorded. Ranges
  were MAE 0.028078–0.044548, SSIM 0.939355–0.963672 and PSNR
  25.698124–29.266582. Every triage verdict remains `review`; no winner or
  physical/source-quality claim is made.
- All six Quality/Mobile pairs were visually checked. Desktop 1280×720 and
  mobile 390×844 loopback QA had no page overflow or console warning/error. A
  real UI cancellation stopped during capture, retained no report, left its
  output root empty and closed port 43722.
- Final focused gates: 99/99 tests; core/tool/web typechecks; targeted
  TypeScript lint; core build and runner syntax checks passed. The tool build
  retains its package-wide sibling-source/`rootDir` diagnostics (nine sibling
  imports, including this one); the production web build retains the required
  live-Clerk-key release gate. Neither was bypassed.

Evidence:
`docs/reports/reception-room-captured-quality-comparison-v0-evidence-2026-07-18.json`;
report:
`docs/reports/reception-room-captured-quality-comparison-v0-report-2026-07-18.md`.

T-529 is done for this bounded slice. T-508 and the broader `/goal` remain
active. Next: a distinct XGRIDS/Potree 2.0 Source Facts V7 profile from
`metadata.json`, `hierarchy.bin` and `octree.bin`, driven by one known-good
Reception bundle plus real count/byte-range mismatch negatives. T-507/T-486
survey, rights and identity inputs remain prerequisites only for authority or
release claims.

## T-530 · XGRIDS/Potree v2 Source Facts V7 exact-bundle profile

Completed the next bounded authority-none source-understanding slice under the
active T-508 offline super-app path. Cybersecurity, credentials, cloud,
signing, deployment and publication remained out of scope.

- Preserved the exact V6 Source Facts, Readiness and Checklist artifacts and
  added separate V7 digest domains for exact Potree bundle overlays.
- Bound co-located `metadata.json`, `hierarchy.bin` and `octree.bin` receipt
  identities as one bundle, allowing ancillary files but never treating them as
  bundle members. Partial two-of-three candidates fail closed.
- Added bounded duplicate-key-safe metadata parsing, bounded 22-byte hierarchy
  capture/traversal, official-viewer proxy target replacement, sequential
  bundle processing and streamed octree hashing. Point counts, byte sizes,
  reachable nodes, disjointness, gaps and full byte coverage are schema-bound;
  no point or attribute value is decoded.
- Replayed the exact Reception source twice. Receipt SHA is `40ea026b…`, V7
  facts SHA `8a29ba1a…`, bundle SHA `f226739d…`; 175,237 × 14 equals all
  2,453,318 octree bytes across 93 reachable/logical nodes. The exact three
  member hashes remained unchanged.
- Retained three compatibility observations: declared depth 0 versus observed
  3, 13 leaf rows with child masks, and omitted optional attribute histograms.
  No proxies exist in this bundle. All ten decoded/semantic/physical/frame/
  provenance/accuracy/registration/fidelity/rights unknowns remain explicit.
- Four other primary room bundles stop at
  `POTREE_V2_OCTREE_LENGTH_MISMATCH`; five stop at
  `POTREE_V2_HIERARCHY_UNREACHABLE_BYTES`. These classifications do not prove
  truncation, general corruption or a vendor-viewer failure.
- Added one path-specific Readiness refinement over three inherited paths and
  ten exact bundle-digest Checklist requests. Requests remain unperformed work,
  not approval or permission.
- Added the V7 loopback product surface and three canonical downloads. Desktop
  1280×720 and mobile 390×844 QA showed no page-level overflow, dialog/error
  overlay or warning/error log. All three controls reported exact response
  fingerprints; separate HTTP checks returned 200, exact filenames and V7
  schemas. The listener stopped and port 41773 closed.
- Independent audit caught a self-redigested failure-code/category substitution
  in the exported wrapper schema. The schema now binds non-established codes to
  the frozen registry, rejects cancellation as an issued artifact and has a
  three-case regression test.
- Final focused gates at this checkpoint: V7 23/23; local app 60/60; core and
  CLI typechecks; core lint/build and targeted CLI lint all pass. The clean
  post-hardening full core recount is recorded in the evidence manifest.

Evidence:
`docs/reports/xgrids-potree-v2-source-facts-v7-evidence-2026-07-18.json`;
report:
`docs/reports/xgrids-potree-v2-source-facts-v7-report-2026-07-18.md`.

T-530 is done only for this bounded slice. T-508 and the broader `/goal` remain
active. A distinct later V8 may add bounded point-value decoding and local
preview evidence, but must not reinterpret values as units, frame, physical
meaning, completeness, accuracy, registration, provenance, rights or authority.
T-507/T-486 remain later authority/release prerequisites only.

## T-531 · XGRIDS/Potree v2 point-values V8 diagnostic preview

Completed the bounded point-value continuation under the active T-508 offline
super-app path. Cybersecurity, credentials, cloud, signing, deployment and
publication remained out of scope.

- Preserved the exact V7 Source Facts, Readiness and Checklist artifacts and
  exact Reception receipt/member/bundle identities. V8 adds separate facts,
  readiness and effective-checklist digest domains.
- Decoded every one of 175,237 established 14-byte records. All 525,711
  coordinate components are finite; declared-range and derived-node-bound
  violation counts are zero. Raw/decoded extrema and seven exact quantiles per
  component are retained without inferring units, axes, frame or physical
  meaning.
- Bound intensity to byte offset 12 and the semantically opaque `lcc prediction`
  byte to offset 13. Exact histograms give intensity 1–255 / sum 6,967,027 / 186
  distinct and opaque byte 20–100 / sum 14,415,430 / 79 distinct.
- Performed the exact deep profile because Reception is below the 500,000-point
  threshold: 168,929 unique positions, 6,308 duplicate-position record excess,
  12 repeated positions, maximum multiplicity 6,298 at
  `[62270,125420,30490]`, and 6,301 duplicate complete records. The frozen 1%
  threshold raises one observation-only warning; no corruption or cause is
  inferred.
- Added twelve canonical 1024×1024 CPU PNG diagnostics over three component
  pairs and four modes. Pixel bytes and lossless PNG bytes are independently
  hashed under the pinned
  `deterministic_cpu_triplanar_rgba_png_fflate_0_8_2_v1` profile.
- Replayed the exact real bundle three times. V8 facts SHA is `29da55f1…`,
  readiness `0106b768…`, checklist `234e6dc2…`; all artifact and preview
  identities repeated. One exact point-value unknown is resolved and nine V7
  unknowns remain.
- Added the responsive V8 diagnostic workbench without changing V7 public
  state/text/endpoints/download names. In-app Browser exercised all three
  planes, four modes, 2.5× zoom and reset at 1440×1000 and 390×844. There was
  no page-level overflow or browser warning/error, selected PNG identities
  matched the manifest, HTTP routes returned 200 and port 41776 closed.
- Frontend fidelity retained Georgia headings, teal authority labels, cream
  card borders, V7 rhythm/control sizing, responsive one-column collapse and
  muted epistemic copy. Final desktop/mobile evidence captures were inspected.
- Independent audits closed nested identity, offset/extrema, quantile,
  multiplicity, warning-boundary, preview-order, invalid-input, node-bound,
  attribute-range, buffer-ownership, zoom and accessibility gaps.
- Final focused gates: core V8 29/29; app V8 39/39; focused core/tool lint,
  core typecheck and `git diff --check` pass. Full core recorded 717 passed / one
  skipped plus two failures in unrelated shared untracked handoff-package work.
  Full tool recorded 623 passed plus one repeatable generic 500-file OBJ state-
  poll `ECONNRESET` before admission or Potree assertions; it appears outside
  the V8 preview path, but exact causal independence is not claimed. The current
  whole core typecheck is likewise qualified by the unrelated shared handoff
  file, and the tool build retains its pre-existing sibling-source packaging
  limitation.
- Cancellation is observed at pre-start and synchronous phase guards. A same-
  thread abort in the middle of decode or PNG compression is not promised.

Evidence:
`docs/reports/xgrids-potree-v2-point-values-v8-evidence-2026-07-18.json`;
report:
`docs/reports/xgrids-potree-v2-point-values-v8-report-2026-07-18.md`;
research:
`docs/reports/xgrids-potree-v2-point-values-v8-research-2026-07-18.md`.

T-531 is done only for this bounded slice. T-508 and the broader `/goal` remain
active. V8 diagnostics do not establish vendor semantics, units/frame/CRS,
physical bounds, completeness, accuracy, registration, provenance, rights,
viewer fidelity or authority. T-507/T-486 remain later authority/release
prerequisites only.

## T-532 · Local Photo Capture Quality Workbench V0

Completed a separate local pre-registration action under active T-508. No
cybersecurity, credential, identity-attestation, signing, cloud, deployment or
publication work was needed.

- Froze an authority-none report contract for receipt-verified JPEG/PNG pixels
  and the existing Reception 18-build/12-held-out naming protocol. The worker
  verifies source byte count/SHA again, decodes sequentially, records source
  megapixels, luminance/clipping, Tenengrad edge energy, mean colour and dHash,
  checks RAW companions and possible split leakage, and returns memory-only
  digest-bound WebP thumbnails without modifying originals.
- Added a strict controller and loopback start/status/cancel/report/thumbnail
  contract. Stop refuses to claim success when an active worker does not settle.
  Fresh request IDs and monotonic run revisions reject late-state rollback;
  submitted roles survive refresh/cross-tab state.
- Added a responsive workbench with live split counts, filename-specific role
  labels, coarse accessible progress, actionable protocol gaps, named similarity
  pairs, contact cards and human-review guidance. It is not Source Facts V9 and
  does not reconstruct or claim recovered detail.
- Exercised four exact copies of repository web photographs as a controlled
  implementation fixture: 2 build, 2 held-out, 16/10 missing slots and four
  expected retake findings for low resolution/missing RAW; two also triggered
  the frozen shadow-clipping heuristic. This is not the real Reception capture.
- In-app Browser desktop and 390×844 mobile QA rendered all four thumbnails,
  had no horizontal overflow or console warning/error, and a fresh tab retained
  locked roles `build, build, heldout, heldout`.
- Final gates: core 62 files / 741 passed / 1 skipped plus lint, typecheck and
  build; local app/CLI 39 files / 635 passed / 1 todo with one skipped file plus
  lint and typecheck. Focused lifecycle/browser/controller/HTTP regression is
  51/51; focused core photo contract/worker is 8/8.

Evidence:
`docs/reports/reception-room-photo-capture-quality-workbench-v0-evidence-2026-07-18.json`;
report:
`docs/reports/reception-room-photo-capture-quality-workbench-v0-report-2026-07-18.md`.

T-532 is done only for this bounded implementation slice. T-508 and the broader
`/goal` remain active. The next real input is an owned folder containing the
actual 18 build and 12 held-out captures plus matching untouched RAW files or
explicit session notes for missing RAW counterparts.

## G4 Slice 4 (T-522) — the consumers; the programme completes

- Change history now renders in the Evidence lens from the real audit
  read model (claim-safe by construction: operator-clock times formatted
  from the ISO string and labelled as recorded; origins never certified;
  fold summaries plain about compression). replayActions reconstructs a
  session from the trail via each action's own recorded inverse;
  window.__venReplay (DEV) diffs the reconstruction against the live
  document with honesty caveats. The AI adapter contract encodes 01 §12
  as a discriminated union — accepted ⟺ recorded operator acceptance in
  the TYPE, ghosts only from ai actors, transitions only out of proposed.
- Reviewer Block, fourth CRITICAL in the programme's recurring class: the
  history hook's stale-continuation guard keyed on config identity — an
  A→B→A round trip let a doubly-stale response replace the trail and
  regress the cursor; fixed with a request-generation token (advanced by
  every effect run and its cleanup), interleaving/unmount/enabled-flip
  reproductions pinned. Replay insertion now sorts like the live engine;
  the bridge reports wrong-document and truncation caveats.
- My-scope gates green: types 2144 + build; web 93 across eleven suites
  (pins included); zero typecheck errors in my files. Live-drawer
  walkthrough blocked by the shared dev stack's API (parallel-lane owned)
  — on the verification tail with the descending-paging param.

## T-537 · The Diary's command envelopes (Canon §9, the deferred mutation tail — Diary lane)

Three commits (096d29cf / ff477e92 / 88c8ff30), full record in
architecture doc §13. The headline: Diary mutations now travel as
server-authoritative command envelopes over /ws/diary when the channel is
live, with REST as the untouched fallback — one validation dialect, two
transports, proven end-to-end on the seeded stack (diary-live e2e 6/6,
including a new scenario asserting a drawer create fires ZERO REST
mutations, and the two-coordinator ink race arbitrated over ws).

- Extraction first: the create/update/transition cores left the route
  handlers verbatim for `services/booking-mutations.ts`; the route suite
  pinned the move (38/38, every status/code/error string identical) and
  the source-contract pins moved WITH the code plus two new invariants.
- `diary_commands` (0061): client-minted commandId pk recorded INSIDE the
  mutation's transaction — exactly-once; resends replay the recorded
  outcome. This commit also completed the timeline lane's in-flight 0060
  coherence (file+journal+tail together — the committed journal must stay
  contiguous) and STRUCTURALLY repaired the five Foundry migration-pin
  suites that have been red at HEAD since 0059 (tag-anchored lookups
  replacing tail-relative offsets; 48/48 against a journal LARGER than
  HEAD's, so appends can't break them again).
- The live e2e caught what units couldn't: a 23P01 aborts the wrapping
  command transaction even though the core returns a calm deny — the
  ledger write then died and the race's loser saw a generic failure. The
  core now runs in a nested savepoint; the loser gets the true
  INK_SLOT_TAKEN copy again. Environment note: the local API needs
  TWIN_PUBLIC_VENUE_SLUGS=trades-hall-glasgow for the public-enquiry
  scenario (the handler's opt-in allowlist defaults to "trades-hall"),
  and a TaskStop'd tsx API can orphan its child on 3011 — kill by PID
  before rebinding.
