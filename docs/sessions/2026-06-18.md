# 2026-06-18 Session

## T-467 Reception Room SPZ primary visual check

- Verified the local Reception Room export folders:
  `C:/Users/blake/Downloads/reception-room_xgrids_lcc2_spz_visual`,
  `C:/Users/blake/Downloads/reception-room_xgrids_lcc2_sog_visual`, and
  `C:/Users/blake/Downloads/reception-room_xgrids_lcc2_spz_with_mesh`.
- Confirmed `reception-room_xgrids_lcc2_spz_visual` contains seven room `.spz`
  chunks plus `env.spz`; the runtime registration script excludes `env.spz`.
- Confirmed `reception-room_xgrids_lcc2_sog_visual` remains the backup visual
  layer with seven room `.sog` chunks plus `env.sog`.
- Confirmed `reception-room_xgrids_lcc2_spz_with_mesh` is not referenced by the
  registration script and includes the extra
  `mesh-files/Reception Room Mobile.ply` proxy/collision/navigation candidate.
- Added `assets:register-reception-room-spz-runtime` to
  `packages/api/package.json`.
- Added script-source guards proving the SPZ registration path uses the visual
  export, registers `.spz` chunks as `usable` / `unverified`, keeps the package
  `internal_ready`, excludes `env.spz`, and does not reference `spz_with_mesh`
  or `textSplats`.
- Added a web runtime-resolution regression that accepts registered Reception
  Room `.spz` chunk sets and surfaces exact copy:
  `Runtime asset loaded, not yet verified/signed.`
- Ran a read-only query against the configured API database. Runtime package
  `71687e9e-c23d-4f51-b3dd-a6a82c97978d` is still
  `trades-hall/reception-room`, `internal_ready` / `unverified`, and points
  its primary visual asset to `0_0.spz` at
  `venues/trades-hall/rooms/reception-room/xgrids/2026-06-15/lcc2-result-spz/data/3dgs/0_0.spz`.
  The package resolves seven served `.spz` room chunks, excludes `env.spz`, and
  has no `spz_with_mesh` or `textSplats` path in the primary/served visual set.
- Updated current intake wording so the current package is described as serving
  seven SPZ room chunks directly; the SOG package remains provenance/backup.

Guardrails:

- No public exposure or public-room visual approval was added.
- No signed transform, coordinate-pair intake, capture-control promotion, or
  operational geometry was created.
- T-091 and T-091A remain open.
- Procedural fallback behaviour remains intact when no package or no safe URL
  is available.
