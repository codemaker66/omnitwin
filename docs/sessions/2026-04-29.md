# Session log: 2026-04-29

## Synthesized (afternoon, VSIR-0)

- Four deep research outputs commissioned for VSIR-0 concrete schema specification: Claude, ChatGPT-1, Gemini, ChatGPT-2.
- All four reviewed in sequence; synthesis performed across sources with cross-validation on every load-bearing decision.
- Three technical errors identified and corrected:
  - JCS RFC 8785 canonicalization (Gemini, ChatGPT-2 initial) — DSSE PAE eliminates the need
  - Polymorphic contextId anti-pattern (Gemini) — ChatGPT-2's controlled polymorphism in two specific columns is the fix
  - Full `prims jsonb` storage in database (Claude, Gemini) — ChatGPT-1/ChatGPT-2's "header in DB, body in file" pattern is the fix
- One external standards claim verified against official Khronos source: `KHR_gaussian_splatting` still Release Candidate as of Feb 3, 2026 announcement; Q2 2026 ratification target not yet confirmed. D-019 commits to "RC-aligned" wording, not "ratified standard."
- ADR D-019 committed as the four-source synthesis.
- Four deep research outputs archived at `docs/audits/2026-04-29-vsir-0-schema-deep-researches/` (placeholders; user pastes original text after).
- Two follow-up ADRs identified (D-020 qa_jobs orchestration queue, D-021 partition strategy) and tracked as T-111 and T-112.
- VSIR-0 implementation activation gated on T-080-T-099 closing AND T-091 (Trades Hall reality) completing AND ops baseline (T-054, T-060-T-072) in place.

## Fixed (afternoon, planner grouped movement)

- T-113 completed: fixed 2D/3D grouped table movement parity for table + chairs + cloth.
- Root cause: the 3D editor stores X/Z in render-space units, while the 2D blueprint uses real metres. The blueprint adapter and 2D drag write-back path were treating render-space as metres, causing 2D chair rings and 2D-to-3D moves to drift by `RENDER_SCALE`.
- Added explicit conversion helpers in `packages/web/src/lib/blueprint/adapt.ts`: editor render-space to blueprint metre-space, and blueprint metre-space back to editor render-space.
- Updated `packages/web/src/pages/BlueprintPage.tsx` so dragging a grouped table in 2D writes render-space deltas to every group member, keeping chairs and table cloth aligned when switching back to 3D.
- Regression coverage updated in `packages/web/src/lib/blueprint/__tests__/adapt.test.ts`.
- Verification: targeted Vitest suite passed (87 tests across adapter, editor store, table-group, placement-store); web typecheck passed; changed-file ESLint passed. Full web lint remains blocked by pre-existing T-081 (`grand-hall-textures.ts:342` restrict-template-expressions).

## Fixed (afternoon, T-081 lint)

- T-081 completed: fixed `@typescript-eslint/restrict-template-expressions` in `packages/web/src/lib/grand-hall-textures.ts:342`.
- Change: explicitly format the computed alpha value with `toFixed(4)` before interpolating into the CSS `rgba(...)` string.
- Verification: `pnpm --filter @omnitwin/web lint` now passes.

## Fixed (afternoon, T-080 Clerk CVEs)

- T-080 completed: upgraded Clerk auth packages and cleared the Clerk critical advisories from `pnpm audit`.
- API packages updated to patched `@clerk/backend` and `@clerk/fastify`; web migrated from deprecated `@clerk/clerk-react` to `@clerk/react`.
- Updated Clerk imports across web auth/dashboard/login/register code and Vite chunking. `UserMenu` now branches on the local auth store because `@clerk/react` does not export `SignedIn`/`SignedOut`.
- Raised vulnerable dependency floors/overrides for the remaining audit blockers so `pnpm audit --audit-level=moderate` is clean, including Happy DOM, Vite, Drizzle, Fastify, AWS SDK, Resend/Svix, and selected transitive build-tool packages.
- Added CI Security Audit job running `pnpm audit --audit-level=moderate`.
- Verification: `pnpm audit --audit-level=moderate`, `pnpm -r lint`, `pnpm -r typecheck`, `pnpm -r test`, and `pnpm -r build` pass.

## Triaged (afternoon, T-084 E2E failures)

- T-084 completed: ran the Playwright E2E suite and documented triage at `docs/audits/2026-04-27-e2e-triage.md`.
- Current result: 64 tests discovered, 32 passed, 29 failed, 3 skipped. The older audit note said 28 failures; current main has 29.
- Failure clusters: Hallkeeper null/progress handling (17 failures), editor Sign In toolbar overflow (3), staff dashboard role mismatch (3), stale planner submit fixtures (3), brittle lazy-load assertion (1), and two local-parallel catalogue timeouts that pass with `--workers=1`.
- First attempted run was blocked by a stale local Vite process returning HTTP 500 on `localhost:5173`; after restarting the local server, the suite ran normally.
- T-086 notes updated with the recommended fix order from the triage.

## Fixed (afternoon, T-086 E2E suite)

- T-086 completed: full web E2E suite now passes with 64 tests discovered, 61 passed, 3 skipped, 0 failed.
- Hallkeeper fixes: `/v2` status now wins over `/progress`, progress fetch failures no longer mask 403/404 states, optional instructions are schema-normalized, and legacy rows missing `positions`/`notes` no longer crash the page.
- Editor fixes: vertical toolbar scrolls so Sign In remains reachable; auth-modal tests target the named Sign In dialog; editor interaction tests run serially to avoid local WebGL contention.
- Review workflow fixes: `/dashboard` admits `staff`; `configuration-reviews.ts` Zod schemas now match the shared API client's unwrapped response payloads; planner submit and staff review E2E fixtures now use schema-valid UUIDs and complete snapshots.
- Performance test fix: lazy-load assertion now detects actual Hallkeeper/Dashboard page chunks instead of shared `packages/types` hallkeeper modules.
- Verification: `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, and `pnpm --filter @omnitwin/web e2e -- --reporter=list --workers=1` all pass.
