# 7 September 2026 — combined demo release and furniture handoff

## T-601 / T-603: selected Diary integrated for the authorized release

The founder rejected the still-live dark Diary and selected the light workspace and airy timetable references. The isolated Diary work is integrated as b5a0d824, 78268a69, dae2dec3, 8e164e1a and bf2d7707. It replaces the old surface with a seven-day summary, exact times, sourced room photos and a separately available precise timeline. It preserves actual booking/approval authority, movement confirmation, Undo, calendar tenant/range isolation and read-only hallkeeper inspection.

178 affected tests, scoped lint and web/E2E typecheck passed. Actual local API/browser checks covered save/reload/restore, a 15-minute timeline bar, hold/ink movement and Undo, historical/conflict focus, hallkeeper access and mobile/reduced motion. Final metadata is 12px with measured text contrast above 6:1. Busy multi-booking rows still require vertical scrolling; this is an internal reference adaptation pass, not founder sublime acceptance or measured device performance. Private evidence: D:/claude/venviewer-diary-redesign-evidence-20260907; [scoped report](../../design-qa.md).

The actual review rehearsal also exposed an empty-body JSON Content-Type header causing self-presence DELETE to fail with HTTP400. Root repair98bceb03 reproduced three failed regressions then passed39 tests and lint. Actual client against the disposable API/PostgreSQL returned200, cleared presence and preserved review/snapshot/email counts; receipt D:/claude/internal-demo-browser-20260906/fixed-client-leave-receipt.json. Quiet review's completed approval evidence remains separate.

Furniture batch badc0168 is integrated as e6921d1d. The combined candidate now has67 migrations through0068,33 canonical catalogue identities and no invented physical stock. T-601 owns final production build, exact-source migration preflight, Railway Linux build/boot, web publication and authenticated founder rehearsal. None is claimed complete merely by this integration.

Deferred existing defect: venue-admin enquiry listing falls through to user ownership at packages/api/src/routes/enquiries.ts:78 instead of the venue staff branch, hiding other-owner enquiries. Reproduced in an isolated fixture; no cross-tenant exposure demonstrated. The Diary fixture uses explicitly owned synthetic enquiries and does not mask this as an API repair.

## T-602 chair import handoff

Completed Blake's supplied Rodin chair import as distinct Burgess Turini 18/3,
UUID `7f1fb7a2-5210-57b1-9108-11255c059520`, in the inventory and editable planner.
The 7.44 MB PBR derivative has 52,986 triangles, source-equivalent 2K texture
pixels, a floor-centred pivot and a calibrated 0.42 × 0.88 × 0.58 m envelope.
Actual source colours are retained; generated geometry is not represented as a
measured scan. Stock counts remain unknown. Original source files are untouched.

Isolated branch: `codex/turini-chair-import`,
`D:/claude/venviewer-turini-chair-20260906`, base `06fa42be`. T-601 remains the sole
production release owner; no production DB operation, push or deployment was
performed here. [Full technical and verification handoff](../reports/burgess-turini-chair-import-2026-09-06.md).

Shared schemas now accept the versioned same-origin asset references. Migration
0067 adds only the new identity and preserves existing data on replay. The normal
seed path now reuses matching migration-registered identities atomically; fresh
66-migration tests reproduce the old duplicate failure and verify the correction.
T-601 owns the separate phase-test fixture's matching seed repair at integration.

Evidence: 164 affected shared-types tests; 46 GLTF lifecycle/instancing/fidelity
tests; 43 inventory/catalogue UI tests; 5 actual PostgreSQL migration checks;
21 fresh-chain canonical-seed/validation checks; types/web/API relevant lint and
typechecks. Final API build and web `--mode test` packaging passed. The latter is
not production Clerk configuration qualification.

Chrome against the actual disposable API verified desktop/mobile inventory,
chair-row placement, full PBR maps, model bounds/floor, selection/rotation,
successful Save and reload with the same UUID/pose, and a 144-chair imported
material batch. The final run had zero page/console errors and stock remained
null. This is synthetic-room correctness evidence on NVIDIA RTX 4090/ANGLE, not
a furnished Grand Hall or cross-device 60 fps claim. Browser/evidence scripts and
failed harness attempts are retained under
`D:/claude/venviewer-turini-evidence-20260906`.

Browser verification exposed an existing action-log client double-envelope bug.
Separate commit `b875578362543a0373d97d7961cae9ba4682b370` repairs POST/GET parsing
without weakening validation; four new regressions failed before correction and
60 related checks passed afterward. The final browser save rerun passed.

Internal release coordination confirmed that delivery of the optimized model and
preview is necessary to Blake's authorized inventory import. No original GLBs,
source photographs, brochure or private venue captures enter public assets. The
coordinated release task receives the exact commits and migration hash for its
combined checks and publication. Founder visual acceptance remains open.


## T-602 supplied furniture batch continuation

Blake supplied a bulk GLB folder and explicitly requested all finished progress live
for Elaine's demonstration. The actual source folder is E:/downloads/furniture_glbs;
18 source folders yield 17 new models plus a byte-identical reuse of the Turini.
Private branch codex/furniture-batch-import-20260907 starts at combined74348f9b.

Twelve distinct canonical identities and five presentation upgrades produce33
catalogue entries. New dimensions are visibly approximate until measured; quantities
remain unrecorded. Sources remain untouched. Runtime GLBs total59,512,608bytes and
1,144,994triangles, with1K maps and rendered thumbnails; three textureless exports
retain basic material. All17 outputs reproduce byte-for-byte from the reviewed
source hash manifest. The model folders record calibration and provenance.

Guarded0068 registration preserves existing physical rows, stock and saved FKs;
67-entry migration/seed, replay, rollback and concurrency checks pass on explicit
disposable native PostgreSQL. SQL hash:
10844538bafe5e4bef1d8d2f1bd8069429c4e5f0e408a0a097642ac9bd3da1f8.
592 affected web tests,51 API tests,17 shared catalogue tests, relevant types/lint,
API build and web --mode test build pass. Actual browser caught a service-table
chair-dialog path and obsolete procedural inspection controls; each was fixed
with failing-before/passing-after regressions. Final Chrome/RTX4090 browser run
passes inventory preview/unrecorded stock, actual chair/cake/divider drag, rotation,
save/reload,18-model batching and all18 saved identities.390px checks are responsive
viewport evidence, not a physical phone test. Page/console/HTTP errors: zero.

Failed harness evidence retained: oversized layout correctly rejected by floor-plan
validation, canvas-root reload race, and an aborted fire-and-forget audit request.
The final harness uses a20m synthetic room and waits for canvas/audit acknowledgement.
No validation was weakened. Evidence: D:/claude/furniture-batch-20260907.
Full report: docs/reports/furniture-batch-import-2026-09-07.md.

T-601's Build next-gen venue platform task confirms active combined qualification
and publication ownership. This task supplies the coherent tested furniture commit
and remains available for live URL/browser checks. No separate production release,
production data mutation or training spend was performed here. Founder aesthetic
acceptance, actual mobile performance and live publication are separate gates.

## T-601 demo browser write preflight repair — 2026-09-07

Production browser rehearsal found phase PATCH blocked before dispatch because the
API CORS registration inherited GET/HEAD/POST-only defaults. The real production-mode
server regression reproduced four missing verbs; explicit GET, HEAD, POST, PUT,
PATCH, DELETE and OPTIONS now cover application routes. All10 focused cases pass,
including unlisted-origin denial, unadvertised TRACE, credentials/exposed headers,
and the actual unauthenticated PATCH401 boundary. No database or production action
was taken by this fix. Red/green evidence: D:/claude/furniture-batch-20260907/cors-*.log.
The sole T-601 release owner integrates and publishes this narrow repair; local
verification does not claim live completion.

## Selected venue planner identity — 2026-09-07

Blake requested that the planning header show the selected venue instead of
VENVIEWER, with the supplied crest for Trade's Hall of Glasgow. The screenshot
belongs to ReferenceRoomHeader in current release 04ca4563, which is absent from
the older shared checkout. Implementation is isolated on
`codex/planner-venue-branding-20260907` in
`D:/claude/venviewer-planner-branding-20260907`; other task source was preserved.

The header resolves the editor's selected venue ID through the existing validated
getVenue adapter, hides prior venue data immediately on selection changes and
ignores obsolete responses. The canonical trades-hall-glasgow slug maps to the
requested display spelling and the exact unmodified 864 × 1000 transparent PNG
(SHA-256 bf6f406aa179de847169390cd3ec48e5c855d49201cda07f0919726fca59e043).
Other venues retain their own names/logos; unavailable identity and missing or
failed logos use neutral fallbacks. Long names wrap beside the existing room
subtitle and save action. No database records, authentication or save semantics
changed; no new mobile wordmark was introduced where none existed.

Verification: 17 tests pass across ReferenceRoomHeader and PlannerCockpit,
including the six new identity/failure cases and all retained save guards.
Full web/E2E typecheck, web lint and production compilation pass. Compilation
used the documented local-only Clerk validation placeholder, not an authenticated
integration. Independent source review found no material defects.
Actual headed Chromium rendered the normal planner with intercepted read-only
venue/configuration fixtures at 1440, 1024 and 960 × 900. The full name, supplied
crest, Grand Hall subtitle and save control are visible without overlap. A second
venue shows Riverside Assembly Rooms and no Trades crest. Screenshots and
reproduction scripts are in the isolated worktree's output/playwright directory.
The fixture omits timeline/capture services (retained 404 and missing splat
warnings), so this does not qualify room reconstruction or a production journey.
Initial browser harness failures (server startup and unavailable URL global) were
corrected before the successful rendered checks; product source remained frozen.

This is a completed local T-601 slice. The T-601 release owner must integrate the
branch and publish through the existing single release lane. No push, deployment,
production mutation or founder aesthetic acceptance is claimed.

## Build and ship standing instruction — 7 September 2026

Blake directed all future product work to include shipping after the earlier
planner fix stopped locally: “we build something we ship it.” Added the shared
.claude/conventions/shipping-changes.md delivery contract and routed AGENTS,
CLAUDE, GOAL, task protocol, active goal routing and release runbooks through it.
Product completion includes verification, coordinated integration, deployment
and checking the changed live flow. Release ownership serializes execution and
cannot justify an untracked handoff, redundant approval or local-only completion.
A newer explicit hold or actual unresolved gate stays explicit; source/data,
tenancy, spending and real operational approval boundaries remain intact.

Historical freezes and Tuesday-only instructions are identified as superseded,
without rewriting dated evidence about work that was previously only local.
The active local BG3/intelligent-platform goal cards and Docker gotcha were also
aligned. Research/design/local-only requests keep their stated deliverable;
documentation ships to the repository and does not require an unrelated app build.

Publication uses isolated codex/build-and-ship-policy-20260907 from f115dbdc;
this carries only the delivery-policy delta and preserves the parallel guidance
modernization and application changes in the shared tree. Independent policy
review, new-link resolution and diff checks qualify the documentation change.
Current execution tasks are notified to apply the same standing instruction.


## T-605: first platform integrity increment locally qualified

Goal12 remains active. Work is isolated at D:/claude/venviewer-intelligent-platform-20260907 from04ca4563. Commit94cc0756 removes obsolete persona/rulebook authorities and introduces canonical agent instructions plus the independently reviewed goal contract. Commit3c5b0c9a repairs concurrent event/booking edits, event/change/notification atomicity, referenced snapshot retention and own-venue admin quote/proposal permissions. Local PostgreSQL uses its explicit TCP port and the API closes its pool. A mandatory CI runner checks disposable target identity, all67 migration hashes/timestamps, inert replay and zero skipped required regressions. There is no schema delta to deploy.

Evidence under D:/claude/venviewer-platform-evidence-20260907: fullAPI172files/2902tests passed,5files/113tests skipped; separate requiredPostgreSQL6files/60tests passed with zero skips. API lint/typecheck/build, dependency audit and built-server HTTP live/readiness/version plus shutdown pass. The built-server smoke requires the existing omnitwin-dist condition; its initial missing-condition failure, initial lint/type-fixture errors and red behavioral regressions remain recorded. Skipped ordinary integration fixtures, Linux CI, production and model/GPU paths are not claimed from these local checks.

Current masterb5511c2b is being integrated privately, preserving5298f069's standing shipping policy, supplied venue branding and four-shard E2E. API/types/lock/Docker/Railway content remains identical to the qualified increment. T601 has the exact source/evidence and retains active release coordination; its current browser CI failures need resolution. Delivery will be followed through to exact API identity and an authenticated PATCH{} on the existing private DEMO ONLY event, preserving updatedAt/feed and approved evidence. No production write, deployment or paid compute has occurred in T605 at this checkpoint.

The full commercial-to-operations event/decision/release, evaluated AI/ranking, feedback, useful GPU work, second venue, recovery and performance gates remain open. The next integration needs a consistent inventory/phase/configuration lock order and pinned immutable commercial/layout sources; standalone mutation success does not prove a coherent release.

## T-602 extra furniture and planner defaults

Blake added black/white4ft cloth trestles and correctly questioned actual planner
usage. Fresh live11ad browser evidence shows144legacy banquet chairs and18generic
round tables loading no furniture GLBs. Existing selectable imports did not change
hard-coded new-layout defaults. The isolated follow-up fixes that authoring path,
adds guarded0069 registrations and preserves original/snapshot identities. Both
new source models are prepared and reproducible. Final checks pass 271 web/model,
60 API and 17 shared-catalogue tests, full web/API typecheck/build and scoped lint.
Actual browser banquet/theatre/quick defaults and both new table drag/rotation/save/
reopen flows pass. Wall reserve includes rotated corners; aisles use whole chair
groups; existing chair identities, scale and decoration survive editing. The release
owner selected a separate 10-table/80-chair DEMO ONLY draft, with qualified recipe
and approximate-dimension limits, leaving the approved original untouched.
Integration, migration, release and live verification remain pending at this commit.
Details: docs/reports/furniture-planner-followup-2026-09-07.md.
## T-606 live hallkeeper implementation
Founder explicitly requested deployment of the hallkeeper work to venviewer.com for the presentation. Isolated branch codex/hallkeeper-live-20260907 starts at b5511c2b. T-601 integrated ecfb449d,5106777c,7a32014c and4aea6143 into the serial release. The earlier run was interrupted before publication and missed the original presentation window.

Web7f2a701b4dade7d9d6048a82511ffa83cf4ff821 published at18:43:44UTC with Vercel success receipt6314446074. The release executor qualified934web tests, full types/build and Linux lint/types/build/audit. Actual signed-in Brave checks at18:45–18:51 confirm /hallkeeper→today, primary navigation, real room filtering, prepared-event links to current sheet and compiled handoff, approvalv1/162footprints/43rows, category navigation and PDF download. All shared checks remain0; no live domain writes were performed. The fictional six-stage walkthrough renders, simulated break-cover acceptance works and Restart clears that simulated action.

DayBoard's current active-booking projection does not show the prepared demo event; use the documented direct event/sheet links for presentation. The complete care/handover workflow remains a labelled design demonstration, distinct from real shared setup progress and approval. Local responsive/mock-API checks are separate from actual production read-only evidence. Details and direct links are in docs/design/hallkeeper-2026-09-07/live-verification.md and presentation-route.md. T-606's requested publication is complete; aesthetic acceptance remains Blake's.
## T-608 — customer onboarding and venue administrator access

Implementation in `codex/client-onboarding-20260907`, based on `b5511c2b`.

- Existing venues can be attached to a customer workspace without recreating rooms, bookings or stock. New venues use the same onboarding path.
- Platform administrators can record/renew/cancel exact-email invitations for venue administrators and other venue roles; platform authority is never granted by an invitation.
- Verified Clerk identity resolution now accepts invitations and activates memberships in one transaction, preserving an existing account's tenant and stronger administrator grant. The former acceptance ordering violated migration 0022's accepted-by constraint.
- Account pages wait for authoritative API access, show pending/error/retry states, and preserve safe internal destinations across sign-in and registration. Navigation helper sourced from task 01a07c28's 9978329e.
- Migration `0070_client_onboarding_access.sql` adds the venue-admin and lifecycle-audit values only. SHA-256: `98d22282662e99c508b30c1788d79c3c34b01d8a74e22410cfd6261f45cd72f0`. Integration must place its journal index after T-602 migration0069.

At 14:10 UTC the shared types build and seven contract tests passed. API/web/auth/real-PostgreSQL checks were still running under severe shared-machine memory and filesystem contention following a runtime reset; no release qualification is claimed in this checkpoint. Dependency recovery used the exact private lockfile virtual store and three immutable third-party dependencies from the authorized b551 install; every workspace dependency points at this checkout's own source/output. Actual browser and live checks remain required. T-601's current release executor owns serial integration and deployment.

The test PostgreSQL cluster is dedicated to this task, loopback port55478, database `venviewer_onboarding_test`. Tests require `VENVIEWER_ONBOARDING_TEST_DATABASE_URL=postgresql://postgres@127.0.0.1:55478/venviewer_onboarding_test`; run `pnpm --filter @omnitwin/api exec vitest run src/__tests__/onboarding-postgres.test.ts`. It never consumes ambient production DATABASE_URL. Fsync is disabled only on this disposable cluster; these tests demonstrate transaction/lock behavior, not crash durability.

No Elaine account was created, no production privilege was changed, and no email was sent. Elaine must create/sign in to her own account and complete email verification. The repeatable owner flow is Dashboard → Clients & access → existing venue or new venue → exact email + Venue administrator → save access → share the account link → check connected status after verified sign-in. One venue per account remains the current authorization model; account transfers and self-service billing are separate future work.

## T-601 presentation release integration — 2026-09-07

The live-site presentation request authorizes serial integration, migration, deployment and real browser rehearsal. T-601 handed exclusive release execution to the Deploy Venviewer to live site task. The isolated release checkout is D:/claude/venviewer-presentation-release-20260907; the shared working tree is preserved. The candidate combines the qualified API integrity increment, imported-furniture defaults and two 4ft tables, chair orientation, connected navigation, editable normal planner entry, Hallkeeper, client onboarding, unified working feedback and first-draw viewer arrival. Integration preserves the platform-administrator routing override before the Hallkeeper destination and appends migration0070 after0069.

A current production data/schema backup was created as Neon branch br-green-dust-ab01z4cx (pre-presentation-20260907-1400), parent br-super-feather-abjfwwhb in project misty-pine-24061977. Production preservation evidence at14:05 UTC records4events,12configurations,15bookings,0email sends and162objects in the original approved configuration. No operational records were changed for baseline rehearsal. The separately requested10-table/80-chair private demo draft remains a supported-API action after publication; the original approved sheet and snapshots are preserved.

Combined qualification, actual-target migration receipts, provider identities and final live checks are recorded under D:/claude/venviewer-presentation-readiness-20260907. The presentation-runbook.md there has tested signed-in links for Day Board, event operations, approved setup sheet, handoff, Diary and inventory. At this integration checkpoint the combined candidate is not deployed. Prior b551 browser CI contains56 failures and is not represented as green; the release must qualify changed behavior and record any remaining limits. No claim is made for the complete Goal12 programme, physical-device60fps, PSNR50, operational stock counts or founder aesthetic acceptance.

## T-601 / Elaine display-only showcase

Blake requested a spectacular showcase and presentation/PDF at /demo, including venue, operational and business stories. Implemented in isolated codex/elaine-showcase-20260907 with eight chapters and downloadable eight-page deck. Five focused regressions, strict changed-page types, affected lint and sixteen desktop/mobile rendered chapter checks pass. Poppler PDF visual review complete; no API writes. Full candidate gates and serial deployment are coordinated with Deploy Venviewer to live site. See [showcase evidence](../reports/elaine-showcase-2026-09-07.md). Local qualification is not deployment or aesthetic acceptance.

### Elaine showcase live closeout

Production 7f2a701b includes showcase 0a4bc509, Vercel receipt 6314446074. Fresh public /demo checks pass all 16 desktop/mobile chapter views, keyboard bounds, layout switching and full-screen enter/exit, with zero errors or write requests. Public PDF is 4,442,916 bytes, eight pages, and SHA-256 9d1bf556322be375adddd9411d137ffc124d907ba48912594e20387200ccfbe7 matches the reviewed deck. The release private dependency graph passed build and web type gates, with 934 selected web tests. Display-only showcase delivered; see the report for scope and remaining aesthetic/device distinctions.

### T-601 release execution checkpoint at18:34 UTC

Migration0069/0070 completed successfully at14:37:34 UTC on the verified active production target:69 ledger entries,40 asset definitions,4events,13configurations,15bookings,0emails and unchanged162 original objects, booking hash and approved configuration/snapshot hashes. The first preflight stopped before writes because the generic wrapper did not yet implement the existing verifier's historical CRLF/LF canonical comparison; the corrected wrapper preserved SQL/ledger bytes and retained both attempts. One additional empty configuration appeared before migration; current state was preserved rather than replaced with the earlier12-count baseline.

The coordinated run was interrupted before API/web publication; participating tasks reported usage-limit failures. On resuming at18:31, master remainedb5511c2b and API11ad2dbf, so this release did not meet the requested presentation window. Exact APIcandidatecedda2d7 had passed build, full APIlint/typecheck,180focused API tests (14optional database cases skipped),2233shared tests,60mandatory actual-PostgreSQL tests with zero skips,69migration/replay validation, and29actual onboarding database cases. Railway deployment220fa1be-ff22-49ee-b356-c5433c94f893 now builds that frozen API source. Final web integration also includes the eight-chapter public/demo showcase/PDF, qualified remaining activity work, account-arrival refinement and per-sheet offline queue drain. Frontend checks, production publication and final live rehearsal remain pending at this checkpoint. Evidence remains in D:/claude/venviewer-presentation-readiness-20260907.

## T-601 release closeout checkpoint and T-608 auth recovery

The approximately 14:45 UTC presentation deadline was missed during the
usage-limit interruption. Main web/API 7f2a701 publication resumed after the
deadline; web 2805f78e subsequently delivered final Activity, navigation,
onboarding contrast and unknown-capacity 2D corrections. Exact provider
identities, read-only original-data preservation and actual changed-flow evidence
are collected in the [release report](../reports/presentation-release-readiness-2026-09-07.md).
Historical checkpoint entries above remain dated evidence.

The imported 90-object private demonstration and separate 22-object QA draft
passed actual render/save/reopen, while the original 162-object approved plan and
its snapshots/bookings retained their hashes. Final 2805 Day Board date navigation
announced real loading and settled without alerts; the existing empty homepage
planner was editable and opened the catalogue. Showcase/PDF and actual
Diary/inventory/Hallkeeper/sheet/handoff routes are recorded in the external
presentation guide. No operational checklist, approval or notification was
changed during final read-only checks.

Auth recovery 3b1d3389 adds real Clerk account-settings/email-verification
guidance and obtains a fresh token before an explicit access retry. Its seven
auth files pass 132 tests, with full web/E2E types, lint and production build.
Web 3b1d3389 is now published at Vercel deployment 6315371782. The 20:00:53 UTC
receipt shows the new public entry and compatible ready API 7f2a701; the actual
authenticated provider UI independently confirms the production domain and exact
source. Actual signed-in reload passed against the new entry and corrected CSS.
A fresh anonymous in-app-browser registration check initially failed to load
Clerk, then one unchanged reload recovered the real registration form and Sign
in link to normal login. The initial transient provider-load failure is retained;
its cause was not conclusively captured, and DNS causation is not established.
The entry-referenced Clerk provider JavaScript returned HTTP 200 with the real
settings/verification guidance and fresh-token retry. No signup, verification
email or customer callback was submitted. Actual Elaine verified account/grant/
activation is not established and T-608 remains in progress.

T-594, T-602, T-603 and the existing T-606 publication scopes are delivered.
Newer founder direction keeps T-606 in progress for a compact room plan, current
stage, next action and six-room library, under the same owner.
The missing T-594 ledger row was reconciled from its owner evidence without
copying unrelated shared-tree changes. T-601's bounded final auth runtime checks
are complete; the release executor reviews and publishes this documentation,
then explicitly hands the serial release lane back. Linux CI
34156314598 for 2805 passed Test, Lint, Typecheck, Build and Security
Audit; E2E shards 1/2/4 failed, shard 3 was cancelled by the next push, and the run
is cancelled. New 3b1d run 34157607379 has passed Build/Typecheck/Security Audit/Lint
while Test/E2E remain running at this checkpoint. Full CI E2E is not green;
performance remains
inconclusive, founder/device acceptance is unclaimed, and Goal 12 remains active
under T-605 with its separate 0071 reservation. Copy tip 4789857, scaled-chair fix
997d980b, capture recovery 48022cad and further Hallkeeper design/copy work remain
separately owned follow-ups. T-601 resumes serial integration after an explicit
release-lane handback. No unrelated source, production grants, messages or compute
were included in documentation reconciliation.
