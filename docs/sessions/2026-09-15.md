# 15 September 2026

## T-610 — the Ship Friday plan (Fable session, overnight 14–15 September, revised Tuesday morning)

Blake asked for an ultra plan to make the platform shippable by Friday 18 September: an Apple-grade venue planner with drag-and-drop furniture at 60 fps on phones, a Matterport-class walk, a beautiful timetable, and a back office where hallkeepers, caterers, sales, managers, bookers and clients work in sync with instant messages and one-tap request buttons that flash on the timetable. The plan is [docs/plan/18](../plan/18-SHIP-FRIDAY-ULTRA-PLAN-2026-09-14.md).

Method: a clean worktree at `D:\claude\ship-friday-plan\repo` (the shared checkout is 141 commits behind origin and dirty, so it was not read); sixteen read-only readers, one per subsystem, returning structured maps with file:line evidence over production HEAD 91220e00 (`D:\claude\ship-friday-plan\understand-map.json`); a twenty-one-role veteran panel with a synthesis chair (`veteran-panel-full.json`, `veteran-synthesis.json`); a browser pass on live venviewer.com at desktop and 375 px (`live-observations-2026-09-14.md`); then six adversarial critics over the first draft (`critique-full.json`, `critique-digest.md`: 20 blockers, 53 majors, 32 changes), every refutation re-verified against `origin/master` 641f2667 before the revision. The readers' completeness critic died on a session limit and was replaced by the plan-level critique.

What the critique corrected, all applied: origin/master moved overnight to 641f2667 (18 Codex commits; 87aca5ca is already merged; the empty-enquiries defect is fixed; twin-cad is pushed except two commits); the API deploys from Railway's GitHub integration on any `packages/api` or `packages/types` push, so mid-week merges to master would have been production API releases before their migrations — the plan now uses a `release/r1` integration branch and one Friday merge; the twin is already live at twin.venviewer.com (the reader's INFERRED note was wrong and my own browser pass was right); the repository is public, so the private equipment `.docx` on `codex/inventory-selected-style` must never be pushed; migration 0071 is reserved by T-605 and the e2e specs by T-613, so tags are allocated serially and no lane edits specs; the capacity model was undefined and Tier A was re-cut to about 335 h across thirteen sessions with two checkpoints; four gate lines depended on Tier B and were demoted; §0 now says plainly what Friday will not have; §6 now leads with the register (ivory/forest/copper) and adds the phone tier, requests without staging, DMs to R2, the stock import and the checkout reset as founder decisions.

Findings that change what everyone should do this week: production web and API are both 641f2667 with CI red on that push and no gate anywhere; master CI has been red since 11 May so `deploy.yml` never migrates; production runs without Sentry, uptime monitoring or a rehearsed restore; iOS devices are classified into the desktop "high" splat tier and no mobile number has ever been measured; a finger cannot move furniture in the planner or move the visitor in the walk; production inventory had zero stock rows on 7 September (unverified since); there is no messaging or request model at all; the role vocabulary has five roles and only a platform admin can invite anyone; the readers' must-fix lists total 666 engineer-hours across 25 blockers and 113 majors.

Decisions recorded as proposals, not made: the seventeen in plan 18 §6 await Blake. The plan asserts one thing on its own authority: it will not claim 60 fps on mobile until a real device measures it.

Delivered: the plan, pointers in `docs/state/tasks.md` and `goals/README.md`, this log, on branch `claude/ship-friday-plan-2026-09-14` (rebased onto 641f2667) as a docs-only PR. Nothing was deployed, no production data was touched, no branch other than the plan branch was pushed. Lane 0 of the plan is the first executable work and is not started.
