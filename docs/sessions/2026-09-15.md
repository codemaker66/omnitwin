# 15 September 2026

## T-613 / Goal17 — browser release confidence

Blake asked “create a ./goal and run it”. Activated Goal17 with a runtime goal
and isolated `codex/browser-release-20260915` at54ebfc61. Audited all four failed
CI34919476716 shards against predecessor28209abc.84 failures were shared, with
114 passes,37 skips and116 cases never executed; this does not waive failures.

Reproduced and repaired real composer stacking, mobile catalogue/empty-schedule
placement and hallkeeper target-size defects. Updated obsolete test fixtures and
interaction paths, reviewed missing/changed Linux snapshots and retained original
failures. Capture fixtures use real published bytes checked against descriptor
hashes and lengths; they do not assert runtime review or venue truth.

Fast-forwarded the isolated checkout to the separately shipped641f2667 dependency
release and completed its frozen-lockfile install/postinstall. Final mobile8 and
focused43 units pass on these pins. The full browser gate, three currently failing
Linux Twin performance checks, hosted qualification and Goal17 delivery remain
open. T-601's serial lane is available after Goal16; no Goal17 release is claimed.

See the [working report](../reports/browser-release-confidence-2026-09-15.md) for
test receipts, exact limits and outstanding work. Existing source, production-data,
compute and other task boundaries remain; physical-device and aesthetic acceptance
are distinct from these technical checks.

### Integrated verification checkpoint

All 45 button-action cases pass. The combined admin/capture/tool run produced
52 passes, one pending-state race and one original skip; explicit CDP streaming
and DOM-ready navigation fixed the race, and its focused rerun passed. All 22
Linux state-visual cases pass with zero retries/skips and unchanged tolerances.
The web build and 25 configuration/composer units pass. Product and test repairs
are committed locally with explicit pathspecs; complete four-shard Linux and
hosted qualification remain open.

The strengthened Twin sample proves actual draw submissions and camera movement.
Identical source/pixels/limits pass all five cases on the existing RTX 4090, while
software Mesa llvmpipe passes two and fails three. This is a hardware dependency,
not a waived CI failure. A concrete GPU-check proposal is retained; Blake has been
asked to choose existing isolated hardware versus a separately capped rental.
No new worker, infrastructure, spend or gate replacement has been introduced.
