# 15 September 2026

## T-613 / Goal17 — browser release confidence

Blake asked “create a ./goal and run it”. Activated Goal17 with a runtime goal
and isolated `codex/browser-release-20260915` at54ebfc61. Audited all four failed
CI34919476716 shards against predecessor28209abc.84 failures were shared, with
114 passes,37 skips and116 cases never executed; this does not waive failures.

Reproduced and repaired real composer stacking, mobile catalogue/empty-schedule
placement and hallkeeper target-size defects. Updated obsolete test fixtures and
interaction paths, reviewed missing/changed Linux snapshots and retained original
failures. Capture fixtures use real published bytes checked against descriptor
hashes and lengths; they do not assert runtime review or venue truth.

Fast-forwarded the isolated checkout to the separately shipped641f2667 dependency
release and completed its frozen-lockfile install/postinstall. Final mobile8 and
focused43 units pass on these pins. The full browser gate, three currently failing
Linux Twin performance checks, hosted qualification and Goal17 delivery remain
open. T-601's serial lane is available after Goal16; no Goal17 release is claimed.

See the [working report](../reports/browser-release-confidence-2026-09-15.md) for
test receipts, exact limits and outstanding work. Existing source, production-data,
compute and other task boundaries remain; physical-device and aesthetic acceptance
are distinct from these technical checks.

### Integrated verification checkpoint

All 45 button-action cases pass. The combined admin/capture/tool run produced
52 passes, one pending-state race and one original skip; explicit CDP streaming
and DOM-ready navigation fixed the race, and its focused rerun passed. All 22
Linux state-visual cases pass with zero retries/skips and unchanged tolerances.
The web build and 25 configuration/composer units pass. Product and test repairs
are committed locally with explicit pathspecs. At that checkpoint the complete
Linux and hosted runs were still open; their later results are recorded below.

The strengthened Twin sample proves actual draw submissions and camera movement.
Identical source/pixels/limits pass all five cases on the existing RTX 4090, while
software Mesa llvmpipe passes two and fails three. This is a hardware dependency,
not a waived CI failure. A concrete GPU-check proposal is retained; Blake has been
asked to choose existing isolated hardware versus a separately capped rental.
No new worker, infrastructure, spend or gate replacement has been introduced.

### First hosted candidate and follow-up

Draft PR16 is pushed at `957f2b00563ab40113e52152601e5ac1dfc03cc2`. Hosted
CI34960484926 closed with seven passing and three failing jobs. All 353 browser
IDs reconcile: 298 ordinary passes, four executed expected failures, seven failures,
two flaky passes and 42 original skips. PostgreSQL and native-image checks pass.
No Goal17 release or aesthetic acceptance is claimed.

Two stale schedule copy assertions are corrected; their whole unit file passes
58/58. The complete immutable Linux baseline closed with 290 ordinary passes,
four executed expected failures, 17 failures and 42 original skips. All 353 cases
reconcile, with all 311 active cases executed once. After that run closed, reviewed
follow-up deltas passed all 18 focused Linux cases: ten narration viewports, two
large invitation views, three Reviews cases, two request boundaries and the full
camera-reference journey. Zero retries or snapshot updates; E2E types and scoped
lint also pass.

The passive registered Reception capture baseline failed on default SwiftShader
with seven real SOG files at 1440×900/DPR1. Its 53 returned renders had no probe
errors; the last two developing chunks remained at opacity 0.95349 near 68.46s and
0.97840 near 86.63s. The final page-screenshot step occupied 236.6s before the 300s
test budget expired (305.0s final summary). Font-network causation remains
unproven; returned calls are CPU/native submission evidence, not GPU presentation.
The native baseline-1 diagnostic retains its failed evidence.

The applied three-file dissolve candidate uses a monotonic timeline per opacity
channel. Its 45 focused units, 54 planner/lighting/SS++ regressions, app/E2E types
and scoped lint pass. Native browser comparison was pending at that checkpoint;
its later results follow. Required graphics timing and Goal17 delivery remain open.

### Capture timing and graphics comparison

Goal17's per-channel elapsed-time fade correction is committed locally as
`ea07e8b2`: 99 affected units, app/E2E types, lint and build pass. The same real
seven-chunk capture reaches exact full opacity on the corrected SwiftShader run,
but its viewport screenshot still times out. The explicitly selected RTX backend
passes the unchanged diagnostic in 19.5s overall, including a 355ms viewport
screenshot. Its actual adapter, source hashes and complete probe observations
are retained; the earlier failed hardware-selection attempt remains separate.
The software OpenGL comparison also passes in 40.2s. Commit `f78e190f` selects that
backend in the three capture specs, with passing types/lint. All seven active
capture cases pass, with one original skip, zero retries, no snapshot changes and
real cold downloads whose final hashes match. A clean Xvfb capture comparison
also passes in 39.8s; CI now prepares the same display wrapper. The unchanged Twin
suite still fails three software-renderer timing checks under Xvfb (two pass):
244ms longest hop task, 66.7ms pano RAF p95 and 50ms dollhouse RAF p95. Source
hashes match. The enforced GPU qualification choice remains pending. No timing/
image threshold, source assets or existing quarantine changed.
The screenshot was inspected for the stated capture/evidence-label contract;
legacy runtime alignment, aesthetic acceptance and physical-device targets remain
open. PR16 is still draft, production remains `641f2667`, and no Goal17 deployment
is claimed. See the working report for exact receipts and limitations.

### Second hosted candidate — complete accounting, performance dependency remains

PR16/head `61ccc5a601f6fa69d71aadae40f3d797a08740ff` is pushed. CI35008947128
tested merge `037fcc83c4244936b7d94914966b682b0525e362`; both trees match
`a8fe4125a4392e24e2ee8e2a0bc2982a050addad`. Nine jobs pass; only E2E4 fails.
All 353 cases reconcile: 304 ordinary passes, four executed expected failures,
three unchanged Twin timing failures, 42 original skips, no flaky outcomes and
no missing/extra/duplicate/unrun/interrupted case. Six retries belong only to
the timing failures. All seven active capture cases pass first attempt. The
platform/event-access PostgreSQL and native-image checks pass 60/53/12 cases.

The effective preview API target is localhost:3001: the protected Vercel preview
opens through the existing provider session, but Plan Grand Hall and ordinary
Retry fail on GET /venues with connection refused. No preview settings or venue
data changed. Captured actual browser evidence and all CI artifacts are retained
under `output/qualification/hosted-pr16-35008947128/` in the owned D: checkout.
The reconciler and a separate raw-artifact crosscheck agree on case identities,
attempts and outcomes; interruption regressions prevent a later pass from hiding
an interrupted attempt.

The required GPU worker arrangement remains pending. No compute purchase, worker
enrollment, branch-rule change, relaxed limit, merge or production deployment
was performed. Goal17 remains active with hardware qualification and live delivery
unfinished.
