# 15 September 2026

## T-610 — the Ship Friday plan (Fable session, overnight 14–15 September)

Blake asked for an ultra plan to make the platform shippable by Friday 18 September: an Apple-grade venue planner with drag-and-drop furniture at 60 fps on phones, a Matterport-class walk, a beautiful timetable, and a back office where hallkeepers, caterers, sales, managers, bookers and clients work in sync with instant messages and one-tap request buttons that flash on the timetable. The plan is [docs/plan/18](../plan/18-SHIP-FRIDAY-ULTRA-PLAN-2026-09-14.md).

Method: a clean worktree of `origin/master` 91220e00 at `D:\claude\ship-friday-plan\repo` (the shared checkout is 123 commits behind origin and dirty, so it was not read); sixteen read-only readers, one per subsystem, returning structured maps with file:line evidence (`D:\claude\ship-friday-plan\understand-map.json`); a twenty-one-role veteran panel with a synthesis chair (`veteran-panel-full.json`, `veteran-synthesis.json`); a browser pass on live venviewer.com at desktop and 375 px (`live-observations-2026-09-14.md`); then six adversarial critics over the plan itself. The readers' completeness critic died on a session limit and was replaced by the plan-level critique.

Findings that change what everyone should do this week: production web is `origin/master` and Vercel publishes every push with no gate; master CI has been red since 11 May so `deploy.yml` never migrates; the API at 7f2a701b was hand-deployed; production runs without Sentry, uptime monitoring or a rehearsed restore; eight Codex branches with real fixes exist only as local refs on this machine and the previous Claude session's fifteen twin commits are unpushed; iOS devices are classified into the desktop "high" splat tier and no mobile number has ever been measured; a finger cannot move furniture in the planner or move the visitor in the walk; production inventory has zero stock rows; there is no messaging or request model at all; the role vocabulary has five roles and only a platform admin can invite anyone; the readers' must-fix lists total 666 engineer-hours across 25 blockers and 113 majors.

Decisions recorded as proposals, not made: the eighteen in plan 18 §6 (canonical homepage, hiding /pricing and gating /demo, the role vocabulary, the flashing rule, the migration policy, the seed-week purge, go/no-go authority) await Blake. The plan asserts one thing on its own authority: it will not claim 60 fps on mobile until a real device measures it.

Delivered: the plan, pointers in `docs/state/tasks.md` and `goals/README.md`, this log, on branch `claude/ship-friday-plan-2026-09-14` as a docs-only PR. Nothing was deployed, no production data was touched, no branch other than the plan branch was pushed. Lane 0 of the plan (pushing the local-only branches, cherry-picks, resetting the shared checkout, CI green) is the first executable work and is not started.
