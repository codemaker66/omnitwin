# 2026-06-20

## T-469 route accessibility pass

- Continued T-469 with the requested route accessibility pass across dashboard, proposal share, supplier portal, and public Reception Room routes.
- Added landmark names:
  - `packages/web/src/components/dashboard/DashboardLayout.tsx` now labels the dashboard `<main>` as `Dashboard workspace`.
  - `packages/web/src/pages/ProposalPage.tsx` now uses a named `<main>` for loading, error, and ready proposal states, with the loading state exposed as polite status text.
  - `packages/web/src/pages/SupplierPortalPage.tsx` now labels loading, error, and ready supplier handoff `<main>` landmarks.
  - `packages/web/src/pages/RoomShowcasePage.tsx` now labels both missing-room and ready public showcase `<main>` landmarks.
- Hardened global keyboard/reduced-motion behavior in `packages/web/src/global.css`:
  - focus-visible now covers `[role="tab"]` and custom `[tabindex]` controls;
  - reduced motion now disables transitions globally and explicitly removes transition properties from focusable controls.
- Hardened planner reduced-motion behavior in `packages/web/src/App.css` by disabling planner-shell transitions under `prefers-reduced-motion: reduce`.
- Added `packages/web/e2e/accessibility-route-audit.spec.ts`, a serial Playwright audit that checks:
  - exactly one named visible `<main>` landmark;
  - named visible nav landmarks where present;
  - no unnamed visible controls;
  - keyboard `Tab` traversal with visible focus treatment;
  - no reduced-motion transition/animation offenders;
  - no WCAG contrast offenders for visible critical text/control surfaces;
  - no page errors or console errors.
- Artifact evidence:
  - `C:/Users/blake/omnitwin2/artifacts/t469-accessibility-route-audit-2026-06-20/report.json`
- Verification:
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web exec playwright test e2e/accessibility-route-audit.spec.ts --project=chromium --reporter=line`: 8/8 passed.
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web typecheck`: passed.
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web lint`: passed.
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web exec vitest run src/components/dashboard/__tests__/ExecutiveAnalyticsView.test.tsx --reporter=dot`: 3/3 passed.
- Direct standalone ESLint/typecheck for the E2E spec remains outside current project tooling: `eslint e2e/accessibility-route-audit.spec.ts` is blocked because the ESLint project service only includes `src`, and a one-off `tsc` invocation is blocked by missing Node ambient types for Playwright specs. The Playwright runtime compilation and the package source checks passed.
- Browser/computer-use note: this slice used Playwright rather than manual browser control because the requested checks need seeded auth, mocked public/private API states, reduced-motion emulation, deterministic keyboard traversal, and artifact output.
- T-091/T-091A remain open.
- T-453 remains the blocker for the real signed Reception Room public runtime package; no signed/public runtime claim was added.

## T-469 mobile/tablet accessibility and drawer internals

- Extended the route accessibility audit to desktop, tablet, and mobile viewport coverage for the same 8 route states:
  - dashboard analytics success/error;
  - proposal share success/unavailable;
  - supplier portal success/unavailable;
  - public Reception Room success/procedural fallback;
  - public room unavailable.
- Extracted reusable Playwright accessibility instrumentation to `packages/web/e2e/support/accessibility-audit.ts`:
  - landmark naming;
  - unnamed control detection;
  - focus-visible traversal;
  - reduced-motion transition/animation detection;
  - contrast sampling;
  - page/console error capture;
  - diagnostic selector output for motion offenders.
- Extended `packages/web/e2e/keyboard-focus-performance.spec.ts` so the existing seeded drawer/form/mobile flows now also record accessibility snapshots for:
  - admin create-venue dialog;
  - reviews request-changes dialog;
  - loadout create and delete dialogs;
  - proposal detail drawer and composer;
  - onboarding operator action forms;
  - event-day mobile ops board.
- Objective critique findings and fixes:
  - Route audit implementation initially used the shared helper while retaining stale inline audit functions; removed the duplicate block so one helper owns the accessibility contract.
  - Global `button, [role="button"]` hover animation in `index.html` overrode reduced-motion expectations; added a matching reduced-motion override and strengthened `global.css`.
  - Gradient-only primary buttons produced unreliable contrast surfaces; added solid fallback colors in Admin, Reviews, Loadouts, and Proposals actions.
  - Shared `FileUploader` carried nonessential progress/drop-zone transitions into operations drawers; removed them.
  - Onboarding light panels used gradient-only backgrounds, causing dark text to lack a stable readable surface; added solid panel fallbacks and removed a React shorthand-margin warning.
  - Proposal metadata timestamps were under contrast; raised timestamp color contrast.
- Artifact evidence:
  - `C:/Users/blake/omnitwin2/artifacts/t469-accessibility-route-audit-2026-06-20/report.json`: 24 route samples, zero accessibility problems.
  - `C:/Users/blake/omnitwin2/artifacts/t469-keyboard-focus-frame-accessibility-2026-06-19/report.json`: 6 keyboard/CDP samples passing and 7 internal accessibility snapshots, zero accessibility problems.
- Verification:
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web exec playwright test e2e/accessibility-route-audit.spec.ts --project=chromium --reporter=line`: 24/24 passed.
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web exec playwright test e2e/keyboard-focus-performance.spec.ts --project=chromium --reporter=line`: 6/6 passed.
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web exec vitest run src/components/dashboard/__tests__/AdminPanel.test.tsx src/components/dashboard/__tests__/OnboardingView.test.tsx src/components/dashboard/__tests__/LoadoutsView.test.tsx src/components/dashboard/__tests__/ReviewsView.test.tsx src/components/dashboard/__tests__/ProposalsView.test.tsx --reporter=dot`: 39/39 passed.
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web typecheck`: passed.
  - `pnpm --dir C:/Users/blake/omnitwin2/packages/web lint`: passed.
- Browser/computer-use note: Playwright remained the right verification tool for this slice because the requested coverage depends on viewport matrices, seeded auth roles, mocked API states, reduced-motion emulation, deterministic keyboard traversal, CDP frame sampling, and JSON artifact output.
- T-091/T-091A remain open.
- T-453 remains the blocker for the real signed Reception Room public runtime package; this pass still uses the honest public fallback/staged copy.

## T-469 production-preview parity and nested mutation audit

- Continued T-469 with the requested deployed-production parity pass by running the same accessibility, keyboard/focus, CDP frame-budget, and visual-regression matrix against a Vite production preview build.
- Added an explicit e2e-only auth bypass guard in `packages/web/src/lib/e2e-auth-bypass.ts` and wired it through `packages/web/src/main.tsx`, `packages/web/src/components/auth/ClerkRouteProvider.tsx`, and `packages/web/src/components/dashboard/DashboardLayout.tsx`.
  - The bypass only activates when `VITE_ENABLE_E2E_AUTH_BYPASS=true` and `window.__OMNITWIN_E2E__ === true`.
  - A normal production build without `VITE_ENABLE_E2E_AUTH_BYPASS` was also verified, so this does not create a public production auth bypass.
- Extended the nested drawer/form accessibility coverage beyond the previous 7 internal snapshots:
  - admin create-space, edit-space, delete-space, pricing-rule, and delete-venue dialogs;
  - reviews approval error drawer;
  - loadout caption error drawer;
  - executive analytics success and API failure;
  - supplier denied, admin denied, admin registry error, and hallkeeper mobile denied states;
  - supplier acknowledgement error, proposal-share comment error, event-day acknowledgement failure, onboarding project-gate failure, and admin create-venue failure.
- Hardened shared accessibility instrumentation in `packages/web/e2e/support/accessibility-audit.ts` so focus auditing handles SVG controls, first-tab body focus, visible focus treatment, reduced-motion, contrast, unnamed controls, and landmark naming consistently across the route and drawer harnesses.
- Fixed issues found by the audits:
  - `packages/web/src/components/dashboard/PolygonEditor.tsx` now renders the nested admin polygon editor with a dark graphite canvas and high-contrast gold/cyan controls instead of the low-contrast light treatment inside a dark modal.
  - `packages/web/src/components/dashboard/AdminPanel.css` now preserves readable primary-button hover/disabled contrast.
  - `packages/web/src/components/auth/ProtectedRoute.tsx` and `packages/web/src/pages/HallkeeperPage.tsx` now expose named `<main>` landmarks for loading, denied, unavailable, empty, and ready states.
  - `packages/web/src/pages/DashboardPage.tsx` now initializes directly into the requested permitted `?view=` for the current role, preventing production-preview first-paint fetches against an unrelated forbidden dashboard surface.
- Artifact evidence after the production-preview pass:
  - `artifacts/t469-accessibility-route-audit-2026-06-20/report.json`: 8 route states x 3 viewports = 24 accessibility samples.
  - `artifacts/t469-keyboard-focus-frame-accessibility-2026-06-19/report.json`: 8 frame-budget samples, 12 internal accessibility snapshots, zero failed frame samples, max idle p95 16.8ms, max interaction p95 16.8ms.
  - `artifacts/t469-dashboard-drawer-frame-visual-2026-06-19/report.json`: 8 dashboard drawer/state frame-budget samples, 8 accessibility snapshots, zero failed frame samples, max idle p95 16.8ms, max interaction p95 16.8ms.
  - `artifacts/t469-operational-state-frame-visual-2026-06-19/report.json`: 5 operational mutation-state frame-budget samples, 5 accessibility snapshots, zero failed frame samples, max idle p95 16.8ms, max interaction p95 16.8ms.
- Verification:
  - `VITE_ENABLE_E2E_AUTH_BYPASS=true pnpm --dir packages/web build`: passed.
  - `E2E_WEB_SERVER=preview E2E_BASE_URL=http://127.0.0.1:4176 pnpm --dir packages/web exec playwright test e2e/accessibility-route-audit.spec.ts e2e/keyboard-focus-performance.spec.ts e2e/dashboard-state-visual-performance.spec.ts e2e/operational-state-visual-performance.spec.ts --project=chromium`: 44/44 passed.
  - `pnpm --dir packages/web build`: passed with the e2e auth bypass disabled.
  - `pnpm --dir packages/web typecheck`: passed.
  - `pnpm --dir packages/web lint`: passed.
  - `pnpm --dir packages/web exec vitest run src/__tests__/dashboard.test.ts src/__tests__/auth-components.test.ts src/components/dashboard/__tests__/AdminPanel.test.tsx src/components/dashboard/__tests__/PolygonEditor.test.tsx`: 93/93 passed.
- T-469 remains in-progress: this pass covers the requested production-preview parity and the known remaining supplier/admin/executive nested drawers and mutation states that were outside the 7 prior internal snapshots, but full closure still requires continuing any newly discovered route-specific surfaces and rerunning the public Reception Room route against the real signed runtime package once T-453 public exposure evidence lands.
- T-091/T-091A remain open.
- T-453 remains the blocker for public signed Reception Room runtime exposure; no verified/signed public claim was added.

## T-469 protected ops handoff production-preview pass

- Continued T-469 with the next untested role-specific route surface: `/ops/handoff/:handoffPackId`.
- Hardened `packages/web/src/pages/OpsHandoffPage.tsx` with named `<main>` landmarks for loading, unavailable, and ready states:
  - `Operations handoff loading`;
  - `Operations handoff unavailable`;
  - `Operations handoff pack`.
- Rebuilt `packages/web/src/pages/OpsHandoffPage.css` from a pale back-office sheet into the graphite/gold/cyan operations-command treatment, preserving print/export behavior and adding local dark-mode overrides for the embedded AI draft panel.
- Added a unit guard in `packages/web/src/__tests__/OpsHandoffPage.test.tsx` for the ready and unavailable main landmarks.
- Extended `packages/web/e2e/operational-state-visual-performance.spec.ts` with:
  - a seeded staff-ready `/ops/handoff/:handoffPackId` state;
  - a seeded planner mobile unavailable/retry state;
  - honest AI status mocking with drafts disabled rather than pretending a provider is configured.
- Added visual-regression baselines:
  - `packages/web/e2e/operational-state-visual-performance.spec.ts-snapshots/desktop-ops-handoff-ready-chromium-win32.png`;
  - `packages/web/e2e/operational-state-visual-performance.spec.ts-snapshots/mobile-ops-handoff-unavailable-chromium-win32.png`.
- Artifact evidence:
  - `artifacts/t469-operational-state-frame-visual-2026-06-19/report.json`: now 7 frame-budget samples, 7 accessibility snapshots, zero failed frame samples, max idle p95 16.8ms, max interaction p95 16.8ms.
  - Preview screenshots inspected at `artifacts/t469-operational-state-frame-visual-2026-06-19/desktop-ops-handoff-ready.png` and `artifacts/t469-operational-state-frame-visual-2026-06-19/mobile-ops-handoff-unavailable.png`.
- Verification:
  - `pnpm --dir packages/web exec vitest run src/__tests__/OpsHandoffPage.test.tsx --reporter=dot`: 4/4 passed.
  - `pnpm --dir packages/web typecheck`: passed.
  - `pnpm --dir packages/web lint`: passed.
  - `VITE_ENABLE_E2E_AUTH_BYPASS=true pnpm --dir packages/web build`: passed.
  - `E2E_WEB_SERVER=preview E2E_BASE_URL=http://127.0.0.1:4176 pnpm --dir packages/web exec playwright test e2e/operational-state-visual-performance.spec.ts --project=chromium --update-snapshots`: 7/7 passed and wrote the two handoff baselines.
  - `E2E_WEB_SERVER=preview E2E_BASE_URL=http://127.0.0.1:4176 pnpm --dir packages/web exec playwright test e2e/accessibility-route-audit.spec.ts e2e/keyboard-focus-performance.spec.ts e2e/dashboard-state-visual-performance.spec.ts e2e/operational-state-visual-performance.spec.ts --project=chromium`: 46/46 passed.
  - `pnpm --dir packages/web build`: passed with the e2e auth bypass disabled.
- T-469 remains in-progress for additional route-specific surfaces as they are discovered, plus the real signed Reception Room runtime pass after T-453.
- T-091/T-091A remain open.
- T-453 remains the blocker for public signed Reception Room runtime exposure; no verified/signed public claim was added.

## T-469 protected asset registry production-preview pass

- Continued T-469 with the next protected admin/operator route surface: `/dev/assets/rooms`.
- Hardened `packages/web/src/pages/TradesHallAssetStatusPage.tsx`:
  - named the main landmark `Trades Hall asset registry`;
  - added a real `Retry asset registry` action for failed `/admin/assets/rooms` loads;
  - gave each internal room-view link a room-specific accessible label;
  - kept Reception Room copy honest as `Runtime asset loaded, not yet verified/signed.`
- Tightened `packages/web/src/pages/TradesHallAssetStatusPage.css` after screenshot review:
  - error/retry button now has graphite/gold styling and visible focus;
  - asset cards no longer stretch to the tallest card and leave dead blank space.
- Added unit coverage in `packages/web/src/__tests__/TradesHallAssetStatusPage.test.tsx` for the retry action and existing safe-copy/error behavior.
- Extended `packages/web/e2e/operational-state-visual-performance.spec.ts` with typed seeded `RoomAssetStatus` fixtures and three production-preview states:
  - admin success with Reception Room staged/internal, unverified evidence and coordinate-pair blocker copy;
  - admin delayed loading-to-error state with retry back to success;
  - planner mobile role-denied state through the real `ProtectedRoute` guard.
- Added visual-regression baselines:
  - `packages/web/e2e/operational-state-visual-performance.spec.ts-snapshots/desktop-asset-rooms-success-chromium-win32.png`;
  - `packages/web/e2e/operational-state-visual-performance.spec.ts-snapshots/tablet-asset-rooms-error-chromium-win32.png`;
  - `packages/web/e2e/operational-state-visual-performance.spec.ts-snapshots/mobile-asset-rooms-role-denied-chromium-win32.png`.
- Artifact evidence:
  - `artifacts/t469-operational-state-frame-visual-2026-06-19/report.json`: now 10 frame-budget samples, 10 accessibility snapshots, zero failed frame samples, max idle p95 16.8ms, max interaction p95 16.8ms.
  - Screenshot review covered `desktop-asset-rooms-success.png`, `tablet-asset-rooms-error.png`, and `mobile-asset-rooms-role-denied.png`; the first pass found and fixed the stretched-card defect.
- Verification:
  - `pnpm --dir packages/web exec vitest run src/__tests__/TradesHallAssetStatusPage.test.tsx`: 4/4 passed.
  - `pnpm --dir packages/web typecheck`: passed.
  - `pnpm --dir packages/web lint`: passed.
  - `VITE_ENABLE_E2E_AUTH_BYPASS=true pnpm --dir packages/web build`: passed.
  - `E2E_WEB_SERVER=preview E2E_BASE_URL=http://127.0.0.1:4176 pnpm --dir packages/web exec playwright test e2e/operational-state-visual-performance.spec.ts --project=chromium --update-snapshots`: 10/10 passed and refreshed the three asset-registry baselines.
  - `E2E_WEB_SERVER=preview E2E_BASE_URL=http://127.0.0.1:4176 pnpm --dir packages/web exec playwright test e2e/accessibility-route-audit.spec.ts e2e/keyboard-focus-performance.spec.ts e2e/dashboard-state-visual-performance.spec.ts e2e/operational-state-visual-performance.spec.ts --project=chromium`: 49/49 passed.
  - `pnpm --dir packages/web build`: passed with the e2e auth bypass disabled.
  - `git diff --check`: passed; Git reported only existing LF-to-CRLF warnings.
- T-469 remains in-progress for additional route-specific surfaces as they are discovered, plus the real signed Reception Room runtime pass after T-453.
- T-091/T-091A remain open.
- T-453 remains the blocker for public signed Reception Room runtime exposure; no verified/signed public claim, `spz_with_mesh` primary layer, or `textSplats` path was added.
