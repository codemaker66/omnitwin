# 18 September 2026

## T-627 — Native Three.js Gaussian rendering

Blake authorised implementation after the native/Spark baseline, followed by
visual inspection. Work is isolated in `codex/native-three-splats-20260918`,
based on production `ac338696d3e2b828a76d5c9a41c902640abba229`; the shared
checkout and Friday lane work remain preserved. T-627 supersedes the earlier
Spark renderer choice, not historical capture provenance or QA records.

The implementation removes `@sparkjsdev/spark` and its vendor patch. Three and
its types are pinned to r186; the first-party Gaussian addon renders one globally
sorted draw per scene. Cancellable native adapters retain SOG/PLY/SPZ/SPLAT/KSPLAT
assets, all SH bands, complete vendor levels, transforms and clipping. SOG WebP
decoding uses libwebp without browser colour/alpha conversion. A pinned Three
patch supplies the necessary lifecycle, orthographic, SH-direction, colour and
opacity hooks. A small Drei patch translates old update-range `offset` into
modern `start`, preserving instanced furniture under WebGPU. See
[the runtime note](../engineering/native-splats.md) for format, precision and
device limitations and [the baseline](../reports/native-splat-baseline-2026-09-18.md)
for the isolated comparison, which is not a product speedup claim.

Local qualification (not yet shipped or aesthetically accepted):

- Frozen offline pnpm install reproduced both patches. Shared/types and API
  typechecks passed; web source and E2E typechecks and full web lint passed.
- Integrated Vitest run: 474 files passed, 6,235 tests passed, 34 skipped. The
  sole failed suite was `playwright-config.test.ts`, whose Node subprocess hit
  its unchanged 15-second timeout while other work ran. Its isolated rerun
  passed all 16 cases in 4.52 seconds. Failed logs remain retained. Subsequent
  render-scope/opacity checks passed 34 cases, capture checks passed 39, preview
  checks passed 118, and the final snapshot-cache checks passed 16.
- Real Chrome/RTX 4090 WebGPU loaded the Grand Hall's 6,030,980 splats and
  162 furniture objects with no browser errors after the Drei fix. Inspection
  found and fixed an additional readiness bug: the native colour-output pass
  temporarily changes the render target. Readiness now checks the explicit
  render-entry scope, and waits for a successful main-canvas draw. Both WebGPU
  and WebGL displayed the complete room and furniture without browser errors.
- Same-device orthographic capture passed on both backends at 800×533: opaque
  PNG, all four orientation probes correct, 256-byte WebGPU row padding handled,
  zero new errors or adapter requests, and all camera/scene/renderer state
  restored. Cloth/flow TSL shaders rendered and animated on both backends.
- The same-device Reception poster hook produced complete 3200×2000 JPEGs on
  both backends with no new errors. The actual poster CLI also generated the
  Reception JPEG after removing a precheck for an unpublished progress field;
  the capture hook still rejects incomplete or failed rooms. A real WebGL
  context loss exposed an obscured retry button; the recovery panel now covers
  its canvas. An ordinary click created a new renderer, detached the old canvas
  and redrew all 61 fixture Gaussians without errors. The related 49 focused
  tests passed. Rendered recovery and poster screenshots were inspected.
- Production build initially rejected missing and then test-only Clerk keys.
  The public live key was subsequently identified from the deployed public
  bundle; no validation was weakened. The final production build passed in
  29.21 seconds; all four post-build bundle/isolation cases passed with no skips.
  Three's chunk was 1,722.96 kB raw / 473.55 kB gzip, with no Spark vendor chunk.
  Dependency audit reported no known vulnerabilities.
- Existing browser cases passed: native fixture 1, room resolution 3 (one
  existing FIXME skip), reduced motion 1, runtime defaults 3, and Trades Hall
  visual 3 (two existing opt-in skips). Native warnings are tolerated only as
  warnings after a ready canvas; errors remain failures except the existing
  mocked adjunct 401s, now matched to exact observed response URLs/statuses.
  Chrome 153 failed the old OpenGL test launch sequence after context disposal;
  an isolated API probe reproduced it without Three. Pinned Chromium 147 passed
  the same sequence and the tests without changing browser flags or thresholds.
- Three local native planner motion runs used the full 6,030,980-splat scene,
  162 furniture objects, 1600×1000 DPR 1 and the existing Showcase camera route.
  Mean RAF intervals were 9.2812 / 9.26193 / 9.27355 ms; p95 and p99 were 12.6 ms,
  with zero errors. These warm development-build measurements describe browser
  pacing/submissions, not physical presentation or a matched Spark product A/B.
- After full planner loading and explicit browser GC, CDP reported 1,679,963,215
  bytes of backing storage and 63,725,772 bytes of used JavaScript heap. The
  scene retained one complete compiled snapshot after releasing dominated
  partial snapshots. This excludes GPU allocations and does not demonstrate a
  memory improvement over Spark. Mobile and office-device qualification remain
  open; full-source fidelity and founder visual acceptance are not inferred.

Local evidence is retained under the owned worktree's
`output/playwright/native-migration/`. Implementation commit `f789c5d6` and the
release-branch CI-routing merge `c5af5631` are on
[PR 31](https://github.com/codemaker66/omnitwin/pull/31), targeting `release/r1`.
No product merge or production deployment has occurred. The required
exact-source CPU/GPU CI gate has no waiver. T-601/T-613 reported no
active deployment claim and returned the temporary serial release lane after
production `ac338696`. A fresh 02:43 UTC coordination check found no new active
claim or explicit hold. GitHub still lists a historical queued Deploy run
`30157671479` for July SHA `154c6894`, unchanged since 25 July with no jobs;
it is recorded separately from current execution. The deployment workflow's
`deploy-production` concurrency group serializes releases. This migration changes
API-consumed schema enums, so its eventual release must verify both web and API,
with no database migration delta.

### First hosted qualification and reproduced fixes

CI `35300794256`, attempt 1, tested PR merge checkout
`07a5651f3b27cae4c4d5ece24c268106fc495cb1`, whose tree was identical to feature
head `c5af5631`. Audit, lint, typecheck, build, API Native Image, the full workspace
test job, browser-gate tooling and CPU shards 1/2 passed. The full hosted test
pass supersedes the isolated local configuration-test timeout uncertainty.
The authenticated RTX 4090 worker passed all five existing GPU browser cases;
source checks before/after execution matched all 3,250 files. Publisher evidence
was accepted as GitHub status `54421177569`. Those existing cases exercise the
Twin panorama path; they do not measure native Gaussian performance.

Overall CI failed. CPU shard 3 had two layout-evidence failures; shard 4 had two
room-resolution capture failures and a flaky browser-Back case. All failed logs,
traces and retries remain in the run-specific evidence directory. No inventory,
skip, expected-failure or timing limit was relaxed.

- Both room-resolution tests reached the correct resolved state but hung while
  Playwright waited for an idle software-GL canvas to become screenshot-stable.
  The helper now performs a controlled redraw of the actual application scene
  and camera on its existing renderer, immediately followed by PNG encoding of
  that canvas. The original 15-second per-read limit, 15,000-byte threshold,
  single blank retry and 240-second case limits remain. Both unchanged workloads
  passed in 2.4 minutes under Linux llvmpipe/Xvfb with pinned Chromium 147 and
  the real five-file Reception capture. The retained PNGs were inspected. The
  same readback on Windows native WebGPU produced a 3200×2000, 7,453,545-byte PNG
  in 189.5 ms with no recorded errors. Readbacks are outside performance timing.
  The obsolete `preserveDrawingBuffer` option, ignored by native Three, was removed.
- The Twin failure was a real router race: an immediate Back could restore the
  URL while a deferred destination render left the local viewpoint stale.
  Explicit hop pushes now use `flushSync`; canonical URL replacements remain
  asynchronous. Deterministic teleport and animated-hop regressions failed
  before and pass after the fix. All 34 focused navigation tests, scoped static
  checks and the unchanged browser-Back E2E passed, with zero browser retries.
- The two layout-evidence failures reproduce with all 162 objects on software
  graphics. Removing duplicate test navigation is insufficient. Profiling found
  a first-draw shader/graphics wait; investigation identified that native shader
  warmup bypassed the clipping bridge used during rendering. The bridge now wraps
  the shared public `renderObject` method. That fix alone still failed both Linux
  cases; those failures remain retained. Warmup now runs after camera frame
  subscribers and includes procedural-shell visibility in its signature. The
  tests configure their full 162-object fixture before their single visit,
  removing only the prior unasserted one-object visit; test identities,
  assertions, timeouts and actual workload remain. Focused regression tests and
  independent source review passed. The next Linux run passed all three real
  Reception loading/walk cases and four one-object layout cases, with the
  original walk-exit skip. Both 162-object cases still exhausted 30 seconds:
  the welcome dialog covered the allegedly ready toolbar, and repeated CDP
  calls for each geometry read consumed the other case's remaining time.
  The placed-layout helper now enters through the dialog's supported native
  Escape action and asserts dismissal. All row/footer instant scrolls, element
  and text-Range bounds, and eight-pixel containment checks remain, batched in
  one browser evaluation. A separate helper probe passed valid rows and failed
  an oversized sixth row and missing footer. Final Linux/full CI qualification
  remains pending at this recorded point; earlier failed evidence is preserved.

The subsequent six-case run passed five placed-object cases; the remaining
162-object navigation/capacity case still exceeded 30 seconds. Batching its
concurrent panel bounds retained all eight clearance pairs and eight-pixel
limits, but it still exhausted the same deadline after its first screenshot.
A bounded same-source driver comparison then observed Chromium 147 native
WebGL2 on ANGLE Vulkan SwiftShader fail, while the existing OpenGL capture
profile selected Mesa llvmpipe (LLVM 20.1.2, 256 bits) and passed. Both arms
used 1493×1053, the same 162 objects, assertions, screenshots and 30-second
limit. Both passing screenshots were inspected. Both arms recorded two
connection-refused resource errors (URLs not captured) and no observed JS/GPU
validation errors; this is not a zero-network-error result. The retained
diagnostic is `output/playwright/native-migration/public-config-driver-diagnostic`.
The placed-object group now explicitly uses that existing OpenGL profile.
Independent gate review found no mandated CPU driver; the 353-case inventory,
original skips/expected failures and separate hardware profile remain intact.
This narrowly replaces default-SwiftShader coverage for this group; it does
not claim SwiftShader is fixed or establish physical-device performance.

Final scoped Linux qualification passed all six placed-object cases with zero
retries: the 162-object Evidence case took 11.574 seconds and the navigation/
capacity case 13.178 seconds within their unchanged 30-second limits. The
initial attempt to configure worker-scoped launch options inside `describe`
failed collection and remains retained; the final implementation extends the
test fixture at module scope and applies it only to these six cases. Lint and
E2E typecheck passed. Discovery retained all 353 identities and the 348/5 split;
list-only annotations do not execute the two dynamic skips or four expected
failures, so final 42/4 reconciliation remains the hosted execution gate's job.
All diagnostic browser/server processes were closed before hardware requalification.

A separate local smoke of the untouched production bundle loaded Reception on
native WebGPU: all four first-view sources drawn, 2,108,924 Gaussians and zero
console/page errors. It loaded emitted JS/worker/WASM assets, not development
modules. The Vercel PR preview is protected by login, so this local built-bundle
check is not represented as a hosted preview or live production inspection.

The final patched addon was also compared with Spark in nine serial focused
hardware captures (three per arm), using the identical hash-checked 6,019,684
SH3 PLY, camera/motion, 1600×900 DPR 1 and Chrome 153 on RTX 4090. Median FPS
was 158.723543 native, 134.131397 Spark with its earlier product settings and
130.879141 with additional sort/blur/cap controls. The native radius now matches
Spark's √8; improvement over the earlier product-settings arm was 18.334369%.
Root independently recomputed those medians and gain from the raw frame arrays.
All nine samples retained zero recorded errors and unchanged focus. A first
contended readiness attempt was retained and excluded; a separate unit run
fell between, not during, sampled timing windows. See the dated baseline-report
follow-up for remaining colour/SH/sort differences. This is an isolated addon
comparison, not complete-planner throughput, equal visual fidelity, startup or
memory qualification. Source/patch hashes and all raw samples are retained.

After the clipping/warmup changes, the real native WebGPU planner again loaded
all 6,030,980 Gaussians and 162 furniture objects at 1600×1000 DPR 1 with zero
recorded errors. The normal Evidence control opened successfully, and
`planner-final-webgpu-evidence.png` was captured and inspected. Existing Clock,
Windows power-preference and native PCF-shadow fallback warnings remain;
this is not a warning-free or aesthetically accepted result.

Release compatibility review confirmed no API implementation, database schema,
migration or journal delta. Shared schemas only widen renderer enums while
retaining historical Spark values. Deploy/verify the API before web publication;
if native-valued QA records are later registered, retain the widened schemas in
any API rollback. Both frozen dependency installations already copy the pinned
Three/Drei patches in Docker. Read-only provider checks still show production
`ac338696`: Railway `02bd48b7-5947-479b-a929-41de663308ec` SUCCESS and Vercel
`dpl_7qvsxsgEZt4QzRqiV2nRupvPpEMj` READY with the actual `venviewer.com` alias.

### Hosted startup state and release preparation

Follow-up commit `82ed7905` triggered CI `35305919004` against merge checkout
`f7c38e69ae3342124e471c2c44892f78cef27267` (tree
`1768f7735fc94424f140df4c48c496dfe02219c1`). The GPU job failed at 04:10:38 UTC
before producing any request or running hardware, reporting that its assigned
workflow was not active. The API had reported this run queued shortly after
startup; a subsequent response reports the same run/attempt/repository active,
updated at 04:10:47. The exact failed response was not retained by the old gate,
so delayed status propagation is plausible, not a proven historical response.

Preparation now waits at most 60 seconds for an identity-matching queued run to
be observed active, then creates the request. Every response must match run ID,
attempt and repository; terminal/unknown states, supersession and API errors
still fail immediately. The request's 25-minute lifetime, strict evidence wait,
publisher authentication and hardware profile are unchanged. Independent review
found no fail-open issue. All 169 JavaScript gate tests and 32 Python boundary
tests passed on Linux; the Windows Python attempt's unsupported `fcntl` import
failure remains retained. Syntax and diff checks passed. Failed hosted logs
remain retained. CI `35305919004` then completed with all four CPU browser
shards, workspace tests, lint, typecheck, audit, build and native API image
passing. Only GPU preparation and its dependent full-browser gate failed.
Fresh exact-source qualification of the gate fix remains required.

A fresh detached API ship checkout was prepared separately. The ignored
read-only deployment-preflight controller pins source, provider IDs and database
host/name, rejects ambient dotenv, and requires matching migration hashes and
timestamps with zero pending migrations. Its offline checks and independent
review passed. A targeted Railway variable read confirmed the intended existing
project/environment/service and returned only sanitized target identifiers;
credentials stayed in process memory. No database call, provider mutation or
deployment was made during this preparation.

### Cold native decoder discovery

CI `35306844407` on merge `0effe43b5de397ab686ce1a232f980998186c579`
accepted fresh five-case RTX evidence (status `54424261816`), with all 3,250
source files matching before/after. All application jobs and CPU shards returned
success, but complete browser reconciliation correctly rejected one retried
reduced-motion case. Its first attempt failed after 12.915 seconds when raw
`page.evaluate` lost its execution context during navigation; its retry passed
in 38.860 seconds. No gate waiver or production deployment followed.

A forced-cold Linux run reproduced Vite discovering four lazy native decoder
dependencies during capture decoding, then reloading the document. That run
passed because the reload did not intersect the raw assertion read; it reproduced
the trigger, not the exact timing of the hosted exception. Vite now includes
those four existing imports in initial dependency optimization. The original
reduced-motion spec is unchanged. A second forced-cold run passed on its first
attempt in 25.537 seconds without a late optimization/reload event, retaining
the real five-file capture, reduced motion, 1440×900, and every original deadline
and assertion. Original/fixed logs and traces are retained under
`output/playwright/native-migration/reduced-motion-navigation-{original,optimized}`.
Independent source and evidence review accompanies the new exact-source CI.
Trace comparison confirms two documents/Vite connections before and one after,
with byte-identical embedded test/fixture sources and five successful SOG
responses. The failed hosted first attempt has no trace, so attributing that
specific navigation to optimization remains an inference from the reproduced
mechanism. The shorter test duration is not a renderer performance benchmark.

The clean API checkout and all four Docker prerequisite/API builds passed. A
read-only production preflight against the explicitly pinned existing service
confirmed all 69 migration hashes/timestamps, zero pending and complete schema
prerequisites. No database mutation was attempted. Its receipt binds merge
`0effe43b`; the subsequent change is Vite development configuration and this
record, not API/schema source. A fresh hosted Reception preview of application
commit `82ed7905` also rendered native WebGPU at 2560×1440; normal camera drag
changed the inspected room view with zero console errors and ten known warnings.
Official temporary Vercel access was used without retaining credentials. These
are preview/preflight observations, not production publication or founder acceptance.

### Native loading, capture and cold-start qualification

CI `35308428255` checked merge `701f96a35fb213212cca3e5a67bae9500de1c0d5`
(tree `e6f5e6080682b198f26a79278f1a925fec592f57`). Application tests,
audit, lint, types, build and the Linux native API image passed. Fresh isolated
RTX evidence passed all five cases with 3,250 source files unchanged and was
accepted as status `54425115359`. The browser gate correctly rejected one
failed and three retried CPU cases. Independent artifact review accounted for
all 353 identities, the unchanged 42 skips and four expected failures; no
missing or extra cases were found. CPU2's furniture shortcut never reached its
action because the shell missed its five-second setup limit. CPU3 measured
10,078 ms against the unchanged strict ten-second load limit. CPU4 had one
retried and one persistently failing native screenshot case.

Untraced cold-server/cold-browser probes reproduced the shell failure and a
performance-first failure at 11,546 ms. Vite now warms only the planner's static
module graph at development-server startup. The original tests, stopwatches,
timeouts, contexts and normal HTTP-readiness condition are unchanged. Both
cases pass with the setting, including separate fresh-cache final runs. This
is development-server preparation, not a production startup speedup. The
successful performance assertion does not log its interval, so no exact faster
time or percentage is claimed. Strict typed config lint and independent review
pass; original probes and their limitations remain in the retained artifacts.

The capture investigation retained failed direct redraw, CDP redraw and early
compositor experiments. A hosted retry produced a valid image before an extra
Playwright trace snapshot pushed the host operation past 15 seconds; removing
that snapshot alone was insufficient. An instrumented fixed-camera software-GL
run also observed ink remaining nonzero until 26.207 seconds after the resolve
phase, with continued scene submissions. This does not attribute all of that
interval to ink: invalidation call stacks were not instrumented. Source and
deterministic RAF tests establish that its 100 ms elapsed-time cap prolonged
the fade across slow frames and scheduled an extra terminal callback.

The ink ease now uses full nonnegative elapsed time and snaps the computed
next value in the same callback. Its normal easing, fractional targets, reduced
motion, cancellation and final visible update remain. Two delayed-frame
regressions fail against the old code; all seven geometry/lifecycle tests pass
against the correction. Typed lint, web/E2E types and independent review pass.
The original screenshot helper then passed one staged run at 14.044 seconds,
but failed again in the combined run; that narrow success was not accepted as
a reliable capture repair.

The final screenshot transport crops the existing compositor surface through
CDP. It creates no graphics context and performs no additional scene draw.
It requires the actual application-owned canvas, unchanged DPR1 dimensions,
full containment within the existing viewport and a default render target;
viewport expansion is disabled. PNG signature and physical dimensions are
checked. The same 15-second host deadline includes session creation, bounds,
capture and cleanup; the original waits, size threshold, blank-image retry,
scene, camera, assertions and full trace policy remain. Small JSON attachments
retain backend, dimensions, camera, geometry counts and timing stages. These
are compositor images, which may include displayed DOM overlays, not a forced
render benchmark. Earlier fixed-ink compositor probes passed with captures of
0.497, 4.250 and 6.824 seconds, but inspection exposed a partially rendered
staged room despite its decoded-complete status; those passes alone did not
qualify final visual readiness.

Planner decoding now starts each chunk's reveal while completion follows its
actual native main-scene draw. Model view suppresses pending capture motion and
announcements while retaining terminal failure notices. The existing canvas
creation callback advances a renderer generation; the arrival hook clears old
completion for identical URLs and rejects previous-generation callbacks. Six
regressions fail against the preceding behavior; all 101 focused tests across
six files, strict lint and web source typecheck pass. Evidence is retained under
`35308428255-1/readiness-fix`. Independent review found no blocking issue.

The final three loading/capture cases passed under Linux llvmpipe with full
traces and zero retries in 2.6 minutes. All five pinned SOG files contribute
2,108,924 splats to both resolved images, including 3,626 environment splats;
the native WebGL2 backend, 1440×900 viewport/buffer and camera remain unchanged.
First canvas/phase appeared at 8.006 seconds, with five sources resolved at
28.268 seconds under the configured 50 Mbps transfer cap. These are local test
observations, not device-loading targets. The pending and two resolved capture
calls took 0.270, 13.789 and 14.085 seconds. The last two retain little margin
under the unchanged 15-second limit, so fresh full CI remains necessary.

Root image inspection found retained exit-caption text in the staged image.
Its trace shows `resolved`, `data-visible=false`, `aria-hidden=true` and no
working indicator before capture; the linework final image has no caption.
The staged image establishes complete captured geometry, not a fully departed
caption at that compositor sample. This presentation limitation and all raw
traces/PNG metadata remain in `final-drawn-compositor`. No production deployment,
reference update, database mutation or founder aesthetic acceptance is claimed
by these intermediate observations.

### Native publication regression and web rollback

PR31 qualified merge `5ea9b427794837a694591031d0e44b1e64b7000c` passed CI
`35314691261`: all 353 browser identities reconciled, zero retries, 6,286 web
unit tests passed, and fresh hardware evidence passed with unchanged sources.
The compatible API deployed successfully as Railway
`ee3cdebc-ae08-4732-90e9-2d2899feb53a`; its public version and readiness endpoints
verified the exact source. A read-only production preflight found 69 exact
migrations and zero pending. No production database migration was performed.
Both shared refs fast-forwarded atomically at 06:52:57 UTC. Vercel published
`dpl_7QtQAmn6oEDcKYWRBcH3zFzZSDin` to the production domains.

Subsequent branch CI did not remain clean. Master `35316833577` exhausted all
three staged screenshot attempts and retried the linework capture twice;
release `35316833858` passed its staged case only on retry. All failed captures
stalled inside `Page.captureScreenshot` at the unchanged 15-second deadline,
with 2,108,924 instances and the same 1440×900 canvas/camera. Identity and source
receipts remained valid; no other browser failures were found. The downstream
master Deploy workflow correctly skipped. These failures supersede the earlier
single-run screenshot reliability conclusion; original traces remain retained.

A fresh normal public Grand Hall visit also exposed missing wall and ceiling
surfaces, despite completing loading without console errors. Source inspection
identified a migration regression: old Spark layers used `editable:false`, which
excluded global clip edits. Native layers applied the existing unconditional
`RoomClipBox` for the first time. Its scan-derived extent is not a qualified
architectural containment volume. The previous full-wall appearance must be
preserved without changing measured room data or camera walk bounds.

At approximately 07:16 UTC, Vercel Instant Rollback restored the previous web
deployment `dpl_7qvsxsgEZt4QzRqiV2nRupvPpEMj`, source
`ac338696d3e2b828a76d5c9a41c902640abba229`. The authenticated dashboard and an
independent deployment lookup confirmed all three production aliases. The
compatible API remains deployed. Rollback suspends automatic production
promotion; the corrected native deployment requires explicit promotion after
qualification. Post-rollback public pixel inspection and both repairs remain
pending at this entry. T-627 remains in progress and the release coordinator
holds competing changes. Neither publication nor rollback is visual acceptance.

At 07:21 UTC a fresh normal headed Chrome visit confirmed the restored public
Grand Hall had continuous walls, frieze, portraits and panelling before and after
an ordinary camera drag. Loading finished with zero console errors and one
warning; viewport was 1600×1000, browser DPR1 and settled buffer 3200×2000. Both
actual PNGs were inspected and the browser closed. Retained images are
`output/playwright/native-migration/rollback-grand-hall-{before,after}.png`.

The bounded clipping correction removes the unconditional public-room clip,
retains camera movement bounds and leaves the separate dollhouse cutaway intact.
Its regression fails against the prior source; all 31 room-scene tests, scoped
lint and web/E2E typechecks pass. Fresh native WebGPU and WebGL2 views restore the
previously absent walls and ceiling, complete all 11 detail sources and respond
to ordinary dragging without console errors. This qualifies the clipping repair
only: side-by-side root inspection found conspicuous color speckling and streaks
in both native images relative to the clean restored Spark image. Native visual
fidelity remains a release blocker.

Independent comparison found the reviewed SOG rotation, scale, DC/alpha and SH
equations consistent with Spark. A bounded environment decode instead identified
a 1,432.115 m covariance-expanded bounding-sphere radius; metadata proves all 23
non-environment room files lie inside it. Native's 4,096 linear depth bins then
provide roughly 35 cm resolution from an interior view, unlike the clean PLY
benchmark that excludes the environment. This is a numerical mechanism to test,
not yet a proven browser fix; all environment splats must remain in any product
correction.

The screenshot transport A/B rejected omission of a redundant full-viewport clip
as a reliability repair: original 9.515 s versus candidate 13.953 s, both under
the unchanged limit but with inadequate demonstrated improvement. A subsequent
Chrome trace places most of a 10.827 s capture behind two full-scene WebGL flushes
(5.678 s and 5.134 s), while PNG encoding costs 0.358 s. The camera's extra damping
frames ended before splats loaded, so they do not explain that capture. The
trace also shows the separate ink RAF applying its final opacity after an already
scheduled scene draw. A correction that updates ink before rendering is under
test; no deadline, scene, image size or assertion is relaxed. Original artifacts
and failed fixture setup are retained under `capture-transport-ab`.
