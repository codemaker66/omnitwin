# Session Log — 2026-05-04

## Fixed (T-090 as-unknown-as cleanup)

- Removed all `as unknown as` matches from API, web, shared types, and training source/test paths.
- Added an ESLint `no-restricted-syntax` guard so `as unknown as` cannot re-enter unnoticed.
- Replaced Fastify request casts with module augmentation for `rawBody` and request timing metadata.
- Replaced Blueprint pointer-capture casts with typed helper functions.
- Replaced WebGL and router test double-casts with narrower test-facing interfaces/helpers.

## Fixed (T-353 post-ultrareview hardening)

- Removed `VITEST` as an auth/test-mode bypass signal; mock auth now keys off `NODE_ENV=test` only, with the production env guard still rejecting accidental `VITEST`.
- Moved auto-save WebSocket auth off URL query strings and onto a first-message `{ type: "auth", token }` protocol.
- Added a production Vite build guard requiring `VITE_CLERK_PUBLISHABLE_KEY`.
- Fixed the React StrictMode mounted-ref regression that left the guest enquiry Send flow disabled in development/E2E.
- Preserved the documented public-preview-only thumbnail behavior for guest enquiry sends.
- Expanded public claim guard coverage for certification/legal/fire/accessibility overclaim phrases.
- Aligned the root Node engine to `>=22.0.0`.
- Raised the API WebSocket server listener cap to remove the test close-path `MaxListenersExceededWarning`.

## Verification

- `pnpm --filter @omnitwin/api lint`
- `pnpm --filter @omnitwin/api typecheck`
- `pnpm --filter @omnitwin/api test -- auto-save env middleware/require-editable-config invitation-auth observability/event-bus services/pdf-prerender upload-policy`
- `pnpm --filter @omnitwin/api test` (53 files, 1579 tests)
- `pnpm --filter @omnitwin/api test:integration` (49 tests against Neon)
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web test -- public-claim-guard save-send-panel vite-config-env device-tier editor-store localstorage-cap`
- `pnpm --filter @omnitwin/web test` (77 files, 1601 tests)
- `pnpm --filter @omnitwin/web e2e -- e2e/public-config-flow.spec.ts --reporter=list --workers=1`
- `pnpm --filter @omnitwin/web e2e -- --reporter=list --workers=1` (79 passed, 3 skipped)
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types test` (31 files, 1255 tests)
- `pnpm audit --audit-level=moderate`
- `VITE_CLERK_PUBLISHABLE_KEY=pk_test_local_build_check pnpm -r build`

## Notes

- No T-091/T-091A runtime loader work was started.
- No fake assets, public copy changes, package renames, or product-runtime claims were added.

## Fixed (T-354 landing planner fit + room picker)

- Fixed the desktop landing hero so the embedded 2D planner fits inside its preview frame without clipping.
- Restored the Trades Hall room picker/gallery with Grand Hall, Saloon, Robert Adam Room, and Reception Room photography.
- Wired the landing 2D room selector so room changes update the hero photo, preview floorplan, proof chip, and `/plan?space=...` 3D planner links together.
- Expanded LandingPage unit coverage and responsive Playwright coverage for the room picker, room selector, desktop planner bounds, and restored room assets.

## Verification (T-354)

- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web test -- LandingPage`
- `pnpm --filter @omnitwin/web e2e -- e2e/landing-mobile-planner.spec.ts --reporter=list --workers=1`
- Local browser screenshot/pixel-bounds checks at 2048x1000 for the embedded planner and 1440x1000 for the restored room cards.
