# Session log - 2026-05-04

## In progress: T-088 invitation-before-user creation

- Morning implementation driver chose T-088 because F-001 is already present at HEAD and T-088 is the first unfinished item in the automation priority list.
- Added a shared Clerk onboarding gate so local user resolution now allows only:
  - an existing linked local user,
  - a verified Clerk email that matches a pre-provisioned local user row,
  - or a verified email whose domain is explicitly configured in `CLERK_APPROVED_EMAIL_DOMAINS`.
- Removed the old synthetic `${clerkId}@clerk.user` fallback from HTTP and WebSocket auth paths.
- Routed Clerk `user.created` webhooks through the same gate; approved-domain auto-creation always creates a lowest-privilege planner row, while invited rows preserve their pre-provisioned role and venue.
- Added `onboarding_audit_events` schema/migration to record allowed first-link/create decisions and denied onboarding attempts.
- Added focused regression tests for arbitrary Clerk user bypass, invited-user linking, approved-domain creation, missing email, unverified email, and conflicting email linkage.
- Verification passed:
  - `tsc --noEmit` for `packages/api`
  - `tsc --project tsconfig.build.json` for `packages/api`
  - `eslint src/` for `packages/api`
  - direct Node harness against compiled `dist/services/clerk-onboarding.js`
  - `git diff --check`
- Verification blocked:
  - targeted Vitest onboarding test failed before execution with `spawn EPERM` while Vite/esbuild loaded config in this sandbox.
  - `pnpm audit --audit-level=moderate` failed with registry `ECONNREFUSED 127.0.0.1:9`.
- T-088 remains `in-progress`, not `done`, until normal Vitest and audit can run outside these environment blocks.
