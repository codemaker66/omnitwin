# Session log — 2026-08-26

## T-554 local acceptance workflow

- Added a local-only, fail-closed workflow for converting complete qualified
  human Grand Hall scope decisions into four immutable, cross-bound artifacts
  plus a self-contained local evidence bundle: room membership, all-eight
  interface decisions, an invisible closed selection volume, an exact panorama
  mask set, preserved formal reviews, exact mask bytes, and a receipt-last file
  inventory.
- Strengthened the human gate so a qualified reviewer must explicitly accept
  exact MatterPak room 9, inspect both `Window` and `Mirror` source cleanup
  classes with no architectural authority, and disposition all 148 supplied
  panorama JPEGs rather than treating the 98 outside the initial 50-candidate
  set as automatically ineligible.
- Pending-template generation deliberately leaves room 9, both cleanup
  inspections, all 50 candidate panorama decisions, all 98 remaining panorama
  decisions, and all eight interface decisions unresolved; it leaves the
  volume empty, records `authority: "none"`, and creates no geometry or mask.
- Generated and retained that exact pending template pair under
  `docs/operations/grand-hall-t554-acceptance-template`, bound to root review
  pack `sha256:8a6bef9c3b9e5c27e4c1f62994d7d8d01a82b4afee9515d39f2513c36dcac3aa`.
- Generated the separate source-bound review supplement for all 98 panoramas
  outside the initial 1–50 set: seven pages plus one manifest, eight files
  total. Exact regeneration passed with
  manifest
  `sha256:fd1c53ef54db0ab3c34d1fc879ce22ce9a8c9947bc7aca1ade311ebe77d468a0`.
  Corrected non-authoritative visual inspection identifies sweeps 051–075 as
  camera stations in a distinct adjacent memorial/timber room, not the Grand
  Hall. Exact sweep 051 nevertheless contains narrow doorway-visible Grand Hall
  pixels at the equirectangular seam; sweeps 148–149 are circulation stations
  containing only doorway-visible Grand Hall pixels. These pixels require
  human confirmation and a T-550/review-pack rebuild before acceptance.
- Added a `bind-masks` preparation mode that validates the exact mask inventory
  and PNG pixels and writes SHA-256, byte length, included-pixel count, and
  excluded-pixel count into `human-decisions.json` in a new no-replace
  directory. It deliberately keeps `maskReviewed: false`; only a human
  inspecting those exact bytes may change that field.
- The acceptance path is designed to re-verify the immutable T-554 review pack,
  all 148 exact source JPEG identities and full decodes, every required
  8192×4096 binary grayscale mask against its reviewed hash/length/count
  binding, the complete human decision record, and the reviewed non-convex
  closed volume before writing a new no-replace output directory.
- A complete accepted output preserves the review pack, formal decisions,
  formal closed-volume review, and exact reviewed masks alongside the four
  derived artifacts. `publication-receipt.json` is written last. An interrupted
  directory without that complete receipt is explicitly incomplete and grants
  no authority; the workflow makes no filesystem-wide atomicity claim.
- Documented a baby-step operator runbook, linked it from the review-pack README
  and checklist, and clarified that the Markdown checklist is a decision aid
  rather than the formal acceptance record.
- No real room-9 acceptance, cleanup acceptance, 148-panorama decision set,
  accepted decision record, selection volume, panorama mask, complete
  receipt-last bundle, or scope artifact was generated. Human authority remains
  pending and T-554 remains blocked.
- The workflow cannot authorize reconstruction or runtime admission. Any
  eventual output still reports `productionTrust: null`,
  `runtimeAdmissionAuthorized: false`, and
  `reconstructionAuthorized: false`. No staging, deployment, provider,
  credential, database, bucket, browser-session, source-data, or production
  change occurred.
- Final adversarial hardening made a complete publication receipt mandatory
  before local `human_accepted` scope authority can be returned; rejects
  Windows DOS-device aliases including superscript `COM`/`LPT` forms; and
  re-verifies the exact physical identity and SHA-256 snapshot of the receipt,
  T-557 artifacts, bitset, all 148 panoramas, reviewed masks, and every XGRIDS
  member at the end of accepted-bundle verification.
- Final local gates passed: 35 reconstruction-foundry test files / 569 tests,
  package typecheck, lint, build, full-workspace typecheck, exact pending-template
  regeneration, and exact seven-page panorama-supplement regeneration. Two
  independent post-fix reviews found no remaining concrete code or security
  blocker.

## Remaining gates

- A venue owner or authorized domain reviewer must accept or reject exact
  MatterPak room 9, inspect both source cleanup classes, disposition all 148
  supplied panoramas, and resolve every exact-source interface without
  accepting the diagnostic panorama-to-E57 sequence as camera geometry.
- If any of the 98 panoramas outside the initial candidate set contains possible
  Grand Hall pixels, the current review set must be rebuilt; camera location
  cannot force the whole image into an outside-room exclusion. Sweeps 051–075
  are a distinct adjacent-room camera sequence, with narrow doorway-visible
  Grand Hall pixels in exact sweep 051. Sweeps 148–149 are circulation stations
  with only doorway-visible Grand Hall pixels. The stop condition remains
  active pending authorized original-resolution review.
- A qualified operator must author the source-evidence-derived non-convex
  invisible volume and original-grid masks, run exact mask-binding preparation,
  and have an authorized human inspect those bound bytes before the local
  acceptance gate may be run.
- T-555, T-557, T-556, and T-558 remain separate later gates for qualified LCC
  reconstruction, reviewed registration/output masking, bake-off, and
  byte-verified API/intake admission respectively.

## T-559 native E57 Image2D byte-lineage evidence pack

- Added strict local extraction/check tooling at
  `tools/twin-forge/e57-scripts/e57_image2d_evidence.py` and
  `tools/twin-forge/e57-scripts/extract_e57_image2d_evidence.py`, plus focused
  adversarial tests and the operator runbook at
  `docs/operations/grand-hall-e57-image2d-evidence-runbook.md`.
- The strict source profile binds the exact 20,518,437,888-byte E57 at SHA-256
  `975039d11fc04ca681f038e499f358124bbcab178ad5ce6324fa912212729cdd`,
  149 native `data3D` records, 894 `Image2D` records, six 4096×4096 JPEG
  faces per scan, and exactly 2,927,438,001 aggregate native JPEG bytes.
- Extraction writes the untouched embedded JPEG bytes, fully decodes them for
  integrity, retains native vector order, binds every exact
  `associatedData3DGuid`, hashes the complete source before and after, and
  publishes only through a no-replace hidden stage with
  `publication-receipt.json` written last.
- Check mode repeats both full source hashes, re-derives every native E57
  record and JPEG blob, and strictly verifies canonical JSON, stable before/after
  snapshots of the complete 896-file inventory, all native/published JPEG
  hashes and decodes, every GUID/face/path/intrinsic relation, the aggregate
  byte count, and the terminal receipt. The combined E57 suite ran 35 tests:
  34 passed and one pre-existing privilege-dependent symlink test skipped.
- The pack contract retains `authority: none`, writes no generated content, and
  does not read or use stored image poses. It grants no panorama correspondence,
  camera-orientation, room, mask, transform, training, reconstruction,
  structural, collision, export, public, runtime, staging, publication, or
  production-trust authority.
- Completed the real no-replace extraction at a disjoint D: evidence root and
  then completed a separate hardened strict `--check` pass. Both operations independently
  hashed the full E57 before and after their work and printed the exact
  149-scan / 894-JPEG / 2,927,438,001-byte success result.
- The hardened real check re-derived and compared all native E57 metadata and
  JPEG bytes; independent adversarial reruns rejected forged source metadata,
  a forged valid replacement JPEG plus recomputed receipt, concurrent extra or
  changed files, noncanonical face names, unsafe Windows path spellings, and a
  raced destination.
- The completed pack contains 896 files / 2,928,101,752 bytes. Its canonical
  manifest is 663,151 bytes at
  `sha256:fd13da9638d1a1e194fb0c1acaedbe07dea15e65d9c16353d29f6542ce3ad344`;
  its 600-byte terminal receipt is
  `sha256:a19b4058ab6006744184101d0b8287f14a64390065743dc5ff63fb73fa882415`
  and is preserved at
  `docs/operations/grand-hall-e57-image2d-evidence-v1.json`.
- External `sweep_*.jpg` mapping remains unresolved future T-550 work; T-554
  remains blocked on authorized scope review and exact original-grid masks.

## T-560 authority-none panorama/Image2D candidate-crosswalk method

- Froze the local deterministic method at commit
  `b04b4d2ae5093f1e6b2016b684c69187404571f3`. It compares all 148 exact
  external panorama identities with all 149 native T-559 `Data3D` identities,
  emits the complete 22,052-pair retrieval matrix, and performs frozen
  shortlist-only spherical verification.
- The method does not use filenames, numeric sequence, GPano metadata, stored
  image poses, room labels, or generated content as correspondence evidence.
  Every output remains `authority: none` and human-pending.
- Added a pinned CPython 3.12.12 / NumPy 1.26.4 / OpenCV 4.10.0.84 dependency
  lock, exact archive/wheel/native/installed-tree attestations, wheel-derived
  Python import allowlisting, existing-path write holds, and repeated
  end-of-run attestation.
- The worker requires `-I -S -B -X pycache_prefix=NUL`, an absent
  `PYTHONPATH`, exact standard import containers/machinery, explicit reviewed
  local-module loading, one Python thread during binding, one OpenCV thread,
  and OpenCL disabled. Tests use only the isolated test entry point.
- Final method gates passed: direct isolated worker launch, 15/15 focused
  startup/import/seal tests, 27/27 core custody/schema/ranking/publication
  tests, 40/40 real sealed NumPy/OpenCV builder tests, AST parsing, the
  50-line production-function policy, strict lock-schema checks, and diff
  checks. The final dependency-safe combined isolated command also passed
  67/67. Independent reviewers found no remaining blocker under the declared
  trusted same-user local-run model.
- The security boundary is precise: this is not a hostile-user sandbox or a
  completely read-only tree. Existing dependency paths are held against
  write/delete, new children can still be created, Python imports are
  allowlisted, persistent drift is re-attested, native DLL/config races are not
  comprehensively isolated, and network is unused rather than OS-denied.
- Added the operator runbook and method audit. As of this chronological entry,
  the real frozen build/check pair has not run and the target output is absent.
- No source data was changed. No crosswalk output, source, branch, or package
  was pushed, uploaded, merged, deployed, registered, staged, activated, or
  promoted. The method cannot establish room membership, pose, transform,
  masks, training eligibility, reconstruction truth, runtime admission, or
  production trust. T-554 through T-558 remain unchanged.
