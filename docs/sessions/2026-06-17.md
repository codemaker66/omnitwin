# 2026-06-17 Session

## T-466 mobile/tablet CDP frame-budget pass

- Ran the requested production-preview mobile/tablet matrix on
  `http://127.0.0.1:5181` using Playwright plus Chrome CDP tracing/performance
  metrics.
- Profiled 390x844@2 mobile and 768x1024@2 tablet viewports on
  `/plan/cfg-perf-grand-hall?space=grand-hall`,
  `/dev/trades-hall-visual`, and
  `/venues/trades-hall/rooms/reception-room`.
- Fixed the measured narrow-viewport frame sources instead of guessing:
  planner and internal visual canvases now use lean mobile/tablet budgets,
  tablet DPR is capped at `0.75`, mobile/tablet MSAA is disabled, desktop
  mouse-only camera drags are disabled on narrow canvases, planner cockpit
  overlays are omitted on mobile/tablet, circulation/constraint marker work is
  capped or skipped, and the planner canvas always suppresses native
  right-click context menu work.
- Final production-preview results:
  `/plan/cfg-perf-grand-hall?space=grand-hall` mobile idle p95 16.7ms,
  mobile interaction p95 16.8ms, tablet idle p95 16.8ms, tablet interaction
  p95 16.8ms; tablet camera and object-drag p95 both 16.8ms.
  `/dev/trades-hall-visual` mobile/tablet idle and interaction p95 16.8ms.
  `/venues/trades-hall/rooms/reception-room` mobile/tablet idle p95 16.7ms;
  interaction was not applicable because the public route exposed no runtime
  canvas.
- Evidence written to
  `C:/tmp/venviewer-frame-budget-mobile-tablet-final-2026-06-17.json`.
- Residual limits: 16.7-16.8ms is one 60Hz frame tick at Chrome timer
  precision, not a cross-device guarantee. The public Reception Room route used
  the fallback/no-canvas shell because T-453 signed public runtime exposure
  evidence is still unavailable; repeat this pass against the real signed
  Reception Room runtime package once T-453 evidence lands.

Verification:

- `pnpm --filter @omnitwin/web test -- src/__tests__/PlacedFurniture.test.ts src/__tests__/room-mesh.test.ts src/components/editor/__tests__/PlannerScene.test.tsx src/components/__tests__/CameraRig.test.ts src/components/__tests__/CirculationOverlay.test.tsx src/components/__tests__/ConstraintViolationSkin.test.ts src/__tests__/TradesHallVisualPage.test.tsx`
  passed: 7 files, 61 tests.
- `pnpm --filter @omnitwin/web build` passed.

## T-465 measured route frame-budget pass

- Ran a production-preview frame-budget pass on `http://127.0.0.1:5181` using
  Playwright plus Chrome CDP tracing/performance metrics. Browser plugin and
  Chrome DevTools MCP were unavailable, so the route pass used system Chrome
  via Playwright/CDP.
- Profiled `/plan/cfg-perf-grand-hall`, `/dev/trades-hall-visual`, and
  `/venues/trades-hall/rooms/reception-room` with local API mocks and route
  interactions.
- Fixed the measured eager-load sources instead of guessing:
  `CockpitSplatLayer` and `TradesHallVisualPage` lazy-load Spark only when
  splat URLs exist; Clerk moved behind `ClerkRouteProvider` and the planner
  sign-in modal now loads Clerk only on demand; Vite manual chunking now pins
  React, React DOM, React Router, scheduler, Zustand, and the Vite preload
  helper into `react-vendor` so public routes do not import `three`.
- Final production bundle `index.html` preloads only `react-vendor` plus CSS.
  No initial modulepreload for Spark, Clerk, or Three.
- Final route profile:
  `/plan/cfg-perf-grand-hall` idle p95 7.0ms, interaction p95 7.0ms, max
  interaction 13.9ms, zero sampled frames over 16.7ms, Spark false, Clerk
  false, Three true.
  `/dev/trades-hall-visual` idle p95 7.0ms, interaction p95 7.0ms, max
  interaction 7.2ms, zero sampled frames over 16.7ms, Spark false, Clerk
  false, Three true.
  `/venues/trades-hall/rooms/reception-room` idle p95 7.1ms, interaction p95
  7.0ms, max interaction 7.8ms, zero sampled frames over 16.7ms, Spark false,
  Clerk false, Three false.
- Screenshots saved at `C:/tmp/venviewer-frame-budget-final-plan.png`,
  `C:/tmp/venviewer-frame-budget-final-trades-hall-visual.png`, and
  `C:/tmp/venviewer-frame-budget-final-public-room.png`.
- No page errors, console warnings/errors, failed requests, or bad responses
  were observed in the final profiled pass.

Verification:

- `pnpm --filter @omnitwin/web test -- src/__tests__/bundle-splitting.test.ts src/__tests__/sspp-performance-budget.test.ts src/__tests__/role-aware-redirect.test.tsx src/__tests__/App.test.tsx src/__tests__/TradesHallVisualPage.test.tsx src/components/showcase/__tests__/PublicRoomRuntimeCanvas.test.tsx`
  passed: 6 files, 54 tests.
- `pnpm --filter @omnitwin/web exec eslint src/main.tsx src/router.tsx src/components/auth/ClerkRouteProvider.tsx src/components/editor/VerticalToolbox.tsx src/pages/TradesHallVisualPage.tsx src/components/editor/CockpitSplatLayer.tsx src/__tests__/bundle-splitting.test.ts src/__tests__/sspp-performance-budget.test.ts src/__tests__/TradesHallVisualPage.test.tsx src/components/showcase/PublicRoomRuntimeCanvas.tsx src/components/showcase/__tests__/PublicRoomRuntimeCanvas.test.tsx`
  passed.
- `pnpm --filter @omnitwin/web run typecheck` passed after the final Vite
  chunk adjustment, and `pnpm --filter @omnitwin/web run build` passed.
- Residual limits: the route profile used headless Chrome on this workstation,
  not target phones/tablets; the public room route measured fallback-safe
  exposure because no approved public runtime visual is available; real signed
  runtime packages still belong to T-453/T-091 evidence work.

## T-464 runtime canvas demand-rendering hardening

- Added demand rendering to the internal Trades Hall visual command canvas in
  `packages/web/src/pages/TradesHallVisualPage.tsx`.
- Added demand rendering and an explicit high-performance WebGL context
  preference to `packages/web/src/components/showcase/PublicRoomRuntimeCanvas.tsx`.
- Preserved the existing mobile-safe DPR caps: `[1, 2]` on the internal visual
  canvas and `[1, 1.5]` on the public runtime canvas.
- Extended `packages/web/src/__tests__/TradesHallVisualPage.test.tsx` to guard
  the internal canvas `frameloop`, DPR cap, and GPU preference.
- Added
  `packages/web/src/components/showcase/__tests__/PublicRoomRuntimeCanvas.test.tsx`
  to guard public runtime canvas demand rendering, DPR cap, high-performance
  context preference, alpha mode, and orbit-control constraints.
- No claim was made that the whole app now holds 60fps. Chrome DevTools MCP
  frame tracing was unavailable in this session, and the rendered smoke still
  produced two WebGL `ReadPixels` stall warnings on the internal visual route.

Verification for this performance slice:

- `pnpm --filter @omnitwin/web test -- src/__tests__/TradesHallVisualPage.test.tsx src/components/showcase/__tests__/PublicRoomRuntimeCanvas.test.tsx`
  passed: 2 files, 24 tests.
- `pnpm --filter @omnitwin/web exec eslint src/pages/TradesHallVisualPage.tsx src/__tests__/TradesHallVisualPage.test.tsx src/components/showcase/PublicRoomRuntimeCanvas.tsx src/components/showcase/__tests__/PublicRoomRuntimeCanvas.test.tsx`
  passed.
- `pnpm --filter @omnitwin/web run typecheck` passed.
- `pnpm --filter @omnitwin/web run build` passed.
- Browser plugin and Chrome DevTools MCP frame tracing were unavailable, so
  Playwright was used against `http://127.0.0.1:5180` with local API mocks.
- Playwright smoke rendered `/dev/trades-hall-visual` and
  `/venues/trades-hall/rooms/reception-room` with no Vite overlays, no page
  errors, and no console errors. Screenshots:
  `C:/tmp/venviewer-perf-smoke-dev-trades-hall-visual.png` and
  `C:/tmp/venviewer-perf-smoke-venues-trades-hall-rooms-reception-room.png`.
