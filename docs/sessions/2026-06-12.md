# 2026-06-12 Session

## T-430/T-431 Truth Mode and Evidence Runtime v0

- Added shared evidence runtime contracts in `packages/types/src/evidence-runtime.ts`, including evidence items, check results, assumptions, review gates, claim states, evidence packs, stale evidence events, audit log payloads, Truth Mode summaries, and the unsafe public/client claim guard.
- Added database schema and migration `0028_evidence_runtime.sql` for evidence runtime tables.
- Added evidence pack generator/API routes for approved layout snapshots, evidence item listing, review-gate decisions, and Truth Mode summary lookup.
- Wired `/dev/trades-hall-visual` Truth Mode panel to selection-aware table, route, room, runtime asset, and review gate targets with runtime summary loading and labelled internal fallback planning copy.
- Verified:
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/evidence-runtime.test.ts && pnpm --filter @omnitwin/types typecheck`
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/evidence-runtime.test.ts src/__tests__/schema.test.ts src/__tests__/routes-inventory.test.ts && pnpm --filter @omnitwin/api typecheck`
  - `pnpm --filter @omnitwin/web exec vitest run src/__tests__/TradesHallVisualPage.test.tsx && pnpm --filter @omnitwin/web typecheck`
  - Product-file unsafe wording scan for the new evidence runtime API/service/UI files.

Notes:

- No simulation engine was added.
- No public/customer claims were expanded.
- No real-world approval, certification, or signed review is claimed.

## T-107 Ops Compiler v1

- Added shared Ops Compiler contracts in `packages/types/src/ops-compiler.ts` for handoff packs, task groups, ops tasks, pick lists, suppliers, supplier instructions, sequences, room flip plans, BEO documents, and snapshot diffs.
- Added database schema and migration `0029_ops_compiler.sql` for internal handoff artifacts compiled from approved configuration sheet snapshots.
- Added deterministic compiler service and `/ops/handoff-packs/*` API routes for compiling and reading handoff packs.
- Added protected `/ops/handoff/:handoffPackId` dashboard with overview, pick list, setup tasks, room flip tasks, supplier notes, what changed, BEO body, load-in/breakdown sequences, and print/export.
- Verified:
  - `pnpm --filter @omnitwin/types build`
  - `pnpm --filter @omnitwin/types typecheck`
  - `pnpm --filter @omnitwin/api typecheck`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/ops-compiler.test.ts`
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/ops-compiler.test.ts src/__tests__/schema.test.ts src/__tests__/routes-inventory.test.ts`
  - `pnpm --filter @omnitwin/web exec vitest run src/__tests__/OpsHandoffPage.test.tsx`
  - Product-file unsafe wording scan for the new Ops Compiler API/service/UI files.

Notes:

- No event-day live board was added.
- No supplier portal was added.
- Handoff output is internal operations planning data from approved snapshots, not a safety or compliance determination.

## T-436 Event-Day Mobile Ops v0

- Added shared event-day ops contracts in `packages/types/src/event-day-ops.ts` for mobile board payloads, event-day issues, task assignments, completion events, status updates, supplier arrivals, changes since handoff, and idempotent task status updates.
- Added database schema and migration `0030_event_day_ops.sql` for `event_day_issues`, `task_assignments`, `task_completion_events`, and `ops_status_updates`.
- Added `/events/:id/ops-board`, `/ops-tasks/:id/status`, `/events/:id/issues`, `/events/:id/issues/:issueId`, and `/events/:id/changes-since-last-handoff` API support.
- Added protected `/ops/events/:eventId` mobile/tablet board with phase timeline, setup progress, checklist actions, issue reporting, changes, supplier arrivals, escalation notes, and explicit sync status.
- Added IndexedDB-backed event-day offline queue for task status and issue creation, reusing the hallkeeper queue posture: idempotency keys, last-write-wins task status, and retained issue reports.
- Verified:
  - `pnpm --filter @omnitwin/types typecheck`
  - `pnpm --filter @omnitwin/types build`
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/event-day-ops.test.ts`
  - `pnpm --filter @omnitwin/api typecheck`
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/event-day-ops.test.ts src/__tests__/schema.test.ts src/__tests__/routes-inventory.test.ts`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web exec vitest run src/__tests__/EventDayOpsPage.test.tsx src/lib/__tests__/event-day-offline-queue.test.ts`
  - Product-file unsafe wording scan for the new event-day API/service/UI files.
  - `git diff --check`

Notes:

- No supplier portal was added.
- No event-day live collaboration/socket board was added.
- Event-day output is internal operational execution state, not a safety or compliance determination.

## T-438 Guest Flow Replay v0

- Added shared Guest Flow Replay contracts and deterministic v0 engine in `packages/types/src/guest-flow-replay.ts`, covering layout input, room polygon, obstacles, entrances/exits, destinations, phase, assumptions, seed, trajectories, density heatmap, route conflicts, queue zones, staff lanes, metrics, hashes, and simulated planning-support disclosure.
- Added database schema and migration `0031_guest_flow_replay.sql` for `guest_flow_replays`, `agent_trajectories`, `density_heatmaps`, `route_conflicts`, `queue_zones`, and `staff_lanes`.
- Added API service boundary in `packages/api/src/services/guest-flow-replay.ts` for generating replay artifacts without creating safety, compliance, evacuation, or approval claims.
- Wired `/dev/trades-hall-visual` to show replay-derived ghost agents, cyan flow lines, density cells, route conflict markers, phase metrics, and the label `Simulated guest flow - planning support`.
- Verified:
  - `pnpm --filter @omnitwin/types typecheck`
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/guest-flow-replay.test.ts`
  - `pnpm --filter @omnitwin/types build`
  - `pnpm --filter @omnitwin/api typecheck`
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/guest-flow-replay.test.ts src/__tests__/schema.test.ts`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web exec vitest run src/__tests__/TradesHallVisualPage.test.tsx`
  - Product-file unsafe wording scan for the new Guest Flow Replay API/service/UI files.
  - `git diff --check`

Notes:

- No advanced physics engine was added.
- No external simulator integration was added.
- Replay output is simulated planning support and requires human review before operational reliance.

## T-447 Revenue and Executive Analytics v0

- Added shared revenue analytics contracts in `packages/types/src/revenue-analytics.ts`, covering revenue scenarios, pricing assumptions, comfort constraints, scenario comparisons, analytics snapshots, event revenue summary, pipeline summary, room utilisation, and venue dashboard payloads.
- Added database schema and migration `0032_revenue_analytics.sql` for `revenue_scenarios`, `pricing_assumptions`, `comfort_constraints`, `scenario_comparisons`, and `analytics_snapshots`, including exact margin and comfort/review guardrails.
- Added API support for `POST /revenue-scenarios`, `GET /events/:id/revenue-summary`, `GET /analytics/venue-dashboard`, `GET /analytics/pipeline-summary`, and `GET /analytics/room-utilisation`.
- Added the dashboard `Executive Analytics` view with pipeline value, enquiry conversion, proposal status, room utilisation, revenue scenario, comfort floor warnings, and review bottlenecks.
- Added a planner HUD revenue readiness card that updates from the live layout without inventing quote values.
- Verified:
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/revenue-analytics.test.ts`
  - `pnpm --filter @omnitwin/types typecheck`
  - `pnpm --filter @omnitwin/types build`
  - `pnpm --filter @omnitwin/api typecheck`
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/revenue-analytics.test.ts src/__tests__/schema.test.ts`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web exec vitest run src/components/dashboard/__tests__/ExecutiveAnalyticsView.test.tsx src/components/editor/__tests__/PlannerSpatialHud.test.tsx`
  - Product-surface unsafe wording scan for the new revenue API/service/UI files.
  - `git diff --check`

Notes:

- No revenue optimizer or solver was added.
- No AI or automatic commercial recommendation authority was added.
- Exact revenue remains tied to linked quote/event data; comfort and review constraints stay visible.

## T-450 AI Assistant Foundation With Strict Claim Guard

- Added shared AI draft contracts in `packages/types/src/ai-assistant.ts`, including use-case enums, draft status metadata, unsafe wording scanning, safe-language replacement, and deterministic draft digests.
- Added env-gated API AI adapter boundary in `packages/api/src/services/ai-assistant.ts`; the HTTP adapter is disabled unless `AI_ASSISTANT_ENABLED=true` and provider/model/base URL/API key are all configured.
- Added authenticated `GET /ai/status` and `POST /ai/drafts` routes. Disabled or incomplete configuration returns a fail-closed disabled state instead of attempting generation.
- Added reusable internal `AIDraftPanel` UI and wired draft-only controls into enquiry detail, ops handoff, and Trades Hall Truth Mode. The client proposal share-link page remains AI-free.
- Verified:
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/ai-assistant.test.ts`
  - `pnpm --filter @omnitwin/types typecheck`
  - `pnpm --filter @omnitwin/types build`
  - `pnpm --filter @omnitwin/api typecheck`
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/ai-assistant.test.ts`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web exec vitest run src/components/ai/__tests__/AIDraftPanel.test.tsx`
  - Product-source unsafe wording scan for AI API/service/UI files; only test assertions contained blocked phrases.
  - `git diff --check`

Notes:

- AI output is always draft-only, `ai_generated`, `unverified`, and human-review-required.
- No AI output is auto-sent or auto-approved.
- No live provider dependency is required without explicit environment configuration.

## T-451 Integration Layer v0

- Added shared integration contracts in `packages/types/src/integration-layer.ts`, covering integration connections, webhook endpoints, external calendar links, website embed configs, email templates, integration events, redacted public connection output, client-safe embed output, and webhook test result stubs.
- Added database schema and migration `0033_integration_layer.sql` for `integration_connections`, `webhook_endpoints`, `external_calendar_links`, `website_embed_configs`, `email_templates`, and `integration_events`.
- Added authenticated API support for `GET /integrations`, `POST /integrations`, `PATCH /integrations/:id`, `POST /webhooks/outbound/test`, `GET /embed-configs`, and `POST /embed-configs`.
- Added managed email-template metadata seeds based on existing transactional templates; no live template editing/sending path was added.
- Verified:
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/integration-layer.test.ts`
  - `pnpm --filter @omnitwin/types typecheck`
  - `pnpm --filter @omnitwin/types build`
  - `pnpm --filter @omnitwin/api typecheck`
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/integration-layer.test.ts src/__tests__/schema.test.ts`
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/routes-inventory.test.ts`
  - Product-source unsafe wording scan for integration contracts/routes/services.
  - `git diff --check`

Notes:

- No live Cvent, Salesforce, calendar, accounting, e-sign, email, or webhook delivery integration was added.
- Credential values are not stored or returned; connection output exposes only whether a credential reference is configured.
- Website embed output is client-safe, read-only, safe-mode-only, and analytics-stub-only.

## T-452 SS++ Hardening and Product Polish

- Added public-safe API observability status at `GET /health/observability`, reporting Sentry and metrics as `configured` or `missing_env` without exposing DSNs, tokens, or provider secrets.
- Added focused health and route-inventory tests for the observability probe.
- Made the public claim-safety guard an explicit CI step and removed unsupported root package metadata wording.
- Added operations docs:
  - `docs/operations/production-monitoring-checklist.md`
  - `docs/operations/backup-restore-drill.md`
  - `docs/operations/status-page-entry.md`
  - `docs/operations/accessibility-audit-2026-06-12.md`
  - `docs/operations/performance-budgets.md`
- Updated `docs/RUNBOOK.md` with the SS++ reliability proof-pack entry points and an explicit reminder that docs alone do not close Sentry, uptime, or backup verification tasks.
- Added Playwright screenshot smoke coverage for `/plan`, `/dev/trades-hall-visual`, the room showcase, client proposal, and dashboard analytics with deterministic mocked data.
- Added keyboard coverage for the landing primary CTA and event-day mobile task status activation.
- Added source-level performance-budget guardrails for documented budgets, Spark lazy isolation, chunk warning policy, and the hardening screenshot spec.

Notes:

- No external Sentry project, uptime monitor, or backup restore drill was configured or claimed complete.
- No production deployment or push was performed.
- No new product workflow was added.

## Research Ingestion and SS++ Strategy Consolidation

- Updated `docs/strategy/ss-plus-plus-platform-execution-plan.md` into the master SS++ build programme incorporating Blake's four research reports and the codebase gap report.
- Reframed the programme around Venviewer as a proof-carrying venue reality operating system rather than a scanner viewer, floorplan app, or CRM-only tool.
- Added the requested P0-P3 build order, explicit dependencies, room asset plan, safe language rules, GREEN definitions, top build priorities, and first 30 implementation tasks.
- Added not-started backlog rows T-453 through T-462 for missing SS++ programme work:
  - first real room runtime package
  - XGRIDS/AWS G6e processing lane
  - public room showcase
  - organisation/workspace onboarding
  - supplier portal
  - navmesh Guest Flow Replay upgrade
  - contract/e-sign/payment/accounting hooks
  - OpenUSD/streaming lane
  - AR/localisation research
  - enterprise analytics plan
- Updated `docs/diagrams/task-graph.md` with a separate SS++ programme subgraph for T-453 through T-462.

Notes:

- No product code was implemented.
- No task was marked done.
- T-091 and T-091A remain open.
- No new public, legal, fire, accessibility, occupancy, or measurement-certainty claim was added.

## T-453 P0 Room-Agnostic Runtime Asset Foundation

- Added shared seven-room Trades Hall runtime registry metadata for `grand-hall`, `reception-room`, `robert-adam-room`, `saloon`, `lady-convenors-room`, `north-gallery`, and `south-gallery`.
- Extended runtime asset schemas so Trades Hall capture sessions, asset versions, runtime package manifests, runtime package registration, room manifest queries, and latest-package lookup reject unsupported Trades Hall room slugs.
- Kept the existing runtime asset tables/migration path; no new migration was required because `0024_runtime_assets.sql` already covers `capture_sessions`, `asset_versions`, `room_manifests`, `runtime_packages`, and `processing_jobs`.
- Removed manual `splatUrl` loading from `/dev/trades-hall-visual`; Spark now loads only from the latest registered runtime package for the selected room.
- Added internal `/dev/assets/rooms` admin route/page showing room-by-room capture status, runtime package status, evidence status, safe copy, next action, and links into the internal room visual route.
- Updated the XGRIDS/AWS G6e runbook with all seven room slugs, external-splat R2 copy/registration guidance, and the internal asset status page.

Verified:

- `pnpm --filter @omnitwin/types test -- asset-version`
- `pnpm --filter @omnitwin/types build`
- `pnpm --filter @omnitwin/api lint`
- `pnpm --filter @omnitwin/api typecheck`
- `pnpm --filter @omnitwin/api test`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web test -- TradesHallVisualPage runtime-visual-asset spark-stack bundle-splitting`
- `pnpm --filter @omnitwin/web test -- TradesHallAssetStatusPage`
- `pnpm --filter @omnitwin/web build`
- Focused unsafe wording scan of changed production surfaces; hits were limited to deny-list scanner terms, not displayed copy.

Notes:

- No real external splat URL, R2 object, hash, or byte size was provided or registered.
- No RuntimePackage row was created.
- `/dev/trades-hall-visual` still shows the honest no-real-asset state until a registered loadable package exists.
- T-091 and T-091A remain open.

## T-427 phase 4 — staff proposal authoring in the dashboard

- Added `packages/web/src/lib/money-input.ts` — exact pounds-input parsing:
  `parsePoundsToMinor` splits the string and uses integer arithmetic only
  (the `parseFloat("0.29") * 100 === 28.999…` trap is test-pinned), plus a
  display-only `formatMinorAsCurrency`.
- Extended `packages/web/src/api/proposals.ts` with the staff client:
  list/get/create/patch/transition/history for proposals, version create +
  latest, quote create. Row schemas follow the dashboard house pattern
  (lenient `z.string()` statuses per enquiries.ts) while version payloads
  keep the STRICT @omnitwin/types schema so the claim guard holds at this
  boundary too.
- Added `packages/web/src/components/dashboard/ProposalsView.tsx` — create a
  draft → compose a version (client message, capacity note labelled planning
  estimate wording, optional quote lines) → send → track responses. Quote
  totals are never computed client-side: lines go to `POST /quotes` and the
  payload snapshot reuses the server's exact-engine results verbatim. The
  types-level claim guard runs client-side before anything posts. Send stays
  disabled until a version exists (mirrors the API 422). Share link renders
  once minted; history marks share-link client responses (`changedBy: null`).
- Wiring: `DashboardLayout.tsx` gains the `proposals` view + a `staffOnly`
  nav flag (API grants create/mutate to staff/admin only — the nav mirrors
  that instead of offering a tab that would only 403); `DashboardPage.tsx`
  renders the view.
- Tests: `lib/__tests__/money-input.test.ts` (6) and
  `components/dashboard/__tests__/ProposalsView.test.tsx` (9 — list/empty,
  venue-scoped create, send gating both ways, inline claim-guard rejection
  with zero API calls, SAFE save without quote, exact-minor-unit quote build
  asserting the server snapshot is reused, malformed-price rejection before
  any API call, concluded-proposal action surface). One fixture bug caught
  mid-run: the payload schema validates `quote.quoteId` as a UUID, so the
  mock quote id needed a real UUID shape.

Verified (all green): web typecheck clean, lint clean, full suite 123 files /
2092 tests, production build. During the chain the public-claim-guard test
correctly failed on the parallel strand's in-flight RoomShowcasePage /
TradesHallAssetStatusPage copy ("fire approved", "approved for occupancy",
"survey-grade", …); the strand removed the wording during this session's
verification and the guard then passed — the gate working as designed; no
strand files were modified by this session.

T-427 status: phases 1–4 done; stays in-progress. Remaining: layout visuals
on the client page, a real client comments thread (beyond the single
request-changes note), capacity intelligence surfacing (T-429), share-link
E2E. T-091/T-091A untouched.

## T-427 phase 5 — capacity intelligence in proposals (T-429) + share-link E2E

Planning-grade capacity now flows from the planner's engine into the
commercial loop. SAFE language end-to-end; no schema change — the generated
note travels through the existing claim-guarded `capacityNote` field.

- `packages/web/src/lib/proposal-capacity-note.ts` — reuses
  `layout-capacity.ts` (`computeCapacityIntelligence`, `comfortBandLabel`):
  `buildProposalCapacityGuidance` + `buildProposalCapacityNote`, which always
  appends "Planning estimate only, human review required — not a legal
  occupancy or fire-capacity figure" and is claim-guard-safe by construction
  (test-asserted for every layout style via `findUnsupportedProposalClaim`).
- `packages/web/src/components/dashboard/ProposalsView.tsx` — capacity
  guidance block in the composer: room select (venue spaces via `listSpaces`,
  failure falls back silently to a hand-written note), guest count, layout
  style → comfortable capacity + comfort band from the bounding-box floor
  area (the same planning-grade basis as the planner HUD), one click inserts
  the generated note. The client page renders it under its existing
  "Capacity guidance — planning estimate" label — no client-page change
  needed.
- `packages/web/e2e/proposal-share-link.spec.ts` — 4 Playwright tests against
  route-mocked API responses: client-safe render (quote from integer minor
  units, capacity guidance wording, SAFE footer disclosure, zero console
  errors), approve flow ("Approve proposal" → accepted banner, button gone),
  note-gated request-changes (disabled until note text; posted body
  asserted), and the plain-English unavailable state. Two initial failures
  were the spec asserting the old "Accept proposal" label after the strand's
  rename to "Approve proposal" — spec updated to the shipped copy.
- Tests: `proposal-capacity-note.test.ts` (7 — math, wording, claim-guard
  sweep across all styles, 500-char limit), ProposalsView +2 (guidance
  compute/insert with 21×10 Grand Hall → 140 comfortable; hidden when rooms
  fail to load). One lint fix: testing-library generic instead of an
  unnecessary type assertion.

Verified: web typecheck clean, lint clean, full suite 126 files / 2108 tests,
production build green, share-link E2E 4/4 passed. tasks.md updated:
T-427 phase 5 noted; T-429 → in-progress (proposal context shipped; landing
and enquiry contexts remain).
