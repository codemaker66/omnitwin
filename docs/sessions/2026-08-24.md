# Session log — 2026-08-24

## Scope and authority

- Continued T-540/T-541 on `codex/grand-hall-exact-runtime`.
- The owner selected and authorized the dedicated staging target ID
  `trades-hall-grand-hall-staging`, a fresh Railway staging API/database, a new
  private R2 bucket, staging migrations, exact Grand Hall intake, and
  authenticated staging-only browser/WebGL QA.
- The controlling instruction for this session was to commit the current
  Grand Hall vertical slice without merging, pushing, deploying, provisioning
  provider resources, running migrations/intake, or performing authenticated
  staging QA. No production authority was granted.

## Security closure

- Added `RUNTIME_PROFILE_INTAKE_R2_SESSION_TOKEN` to the validated intake
  configuration and made the temporary writer credential an inseparable
  access-key/secret/session-token triple.
- The read-only serving client is built independently and never receives the
  writer session token. Only the conditional-create intake writer receives the
  temporary triple.
- The runbook now requires a dedicated write-capable parent kept only in the
  trusted local operator environment. It locally signs a shortest-workable-
  lifetime child restricted to `PutObject` and the immutable prefix
  `venues/trades-hall/rooms/grand-hall/xgrids/grand-hall-big-model-sog-fine-v1/`.
- Of the writer material, Railway receives only the child triple; it separately
  receives the read-only serving credential. Intake cleanup removes all five
  target/deployment/temporary-credential fields and disables intake before
  redeployment. Immediate invalidation revokes the dedicated writer parent,
  never the read-only serving credential.

## Evidence boundary

- The owner supplied the literal placeholder `[accept/reject]`; no visual
  acceptance or rejection was inferred.
- The quoted SOG/SPZ hashes
  `2b44f8a43599085119c945d459b351a7ceec56753441f32ff4cdc0830183bea8`
  and `ca5ec391e71ca4f75132f569ab5c991fd9af1daf8d732e76fa9e04079160a5b5`
  identify the older v0 exterior-camera diagnostics.
- Corrected v1 diagnostic hashes remain
  `83bc8c80a0ce0e00d31a8b05c52473cc5292e17c51f2f86ee7edd9b907527a59`
  (SOG) and
  `da894224fddabc4aaf04d152fe6bdb4e9900e4899950eae726ab30cf867f2fa3`
  (SPZ). They remain `not_reviewed` and are not deployment, package, or public
  evidence.

## External state

- No Railway service, database, domain, variable, migration, or deployment was
  created or changed.
- No R2 bucket, credential, object, or public-access setting was created or
  changed.
- No admin token was requested, stored, printed, or used.
- No branch was pushed and no package was merged, promoted, or published.

## Verification

- Focused temporary-credential/API boundary: 4 files / 77 tests passed.
- Types: strict typecheck and lint passed; full suite 97 files / 2,156 tests
  passed with no type errors; build passed.
- API: strict typecheck and lint passed; full suite 160 files / 2,871 tests
  passed with 5 intentional skips; build passed.
- Web: source/E2E typecheck and lint passed; full suite 334 files / 4,142 tests
  passed; development-mode build passed.
- Independent security review found no actionable credential-isolation,
  response-redaction, or logging issue.
- The resulting local commit SHA is reported in the session handoff rather
  than written self-referentially into this commit.

## Final vertical-slice hardening

- Bound staging builds and API startup to the reviewed branch/SHA, exact
  Railway origin, isolated Clerk development instance, dedicated target,
  staging-only database identity, and staging-only telemetry posture. The
  runtime keeps the staging boundary after intake is disabled.
- Split the permanent bucket-scoped read credential from the short-lived,
  prefix-scoped conditional-PUT writer. Added local R2 signing, Windows DPAPI
  custody, a hash-pinned Railway CLI stdin handoff, and rejection of every
  operator-only secret in the deployed environment schema.
- Added the local encrypted browser-to-process Clerk relay. It acquires a
  fresh short-lived admin token immediately before each request and does not
  place the token in Railway, chat, `.env`, screenshots, clipboard, shell
  arguments, or ordinary process environment.
- Made migration and admin bootstrap staging-only, reviewed-clean-checkout
  operations. Database name/user and platform-admin identity are checked and
  recorded without accepting direct provider credentials on command lines.
- Hardened exact intake with bounded/cancelled response reads, immutable
  conditional PUT plus read-back, live corruption rehearsal, operator identity
  matching, closed failure classes, and repeatable collision-free apply
  reconciliation that still requires a subsequent exact idempotency proof.
- Kept the frontier to the supplied Grand Hall only. No neighbouring room,
  facade, generated fill, semantic mesh, collision asset, or point-cloud
  substitution was added.

## Final local verification

- Exact source manifest SHA-256 rechecked as
  `927a92699de222e99d2684ca2567a35ab1e523a036461e6e01236b7b77b7f659`.
- Focused security regression suite: 5 files / 266 tests passed.
- API: strict typecheck and lint passed; full suite 164 files / 3,018 tests
  passed with 5 intentional skips; production build passed.
- Web: strict typecheck and lint passed; full suite 334 files / 4,166 tests
  passed; both isolated staging and production-fixture builds passed.
- All 12 PowerShell runbook fences and the DPAPI/Railway helper parsed without
  errors. `git diff --check` passed apart from informational Windows line-ending
  notices.
- Independent code/security review found no remaining P0, P1, or P2 issue.
- No provider call, provider-state mutation, secret entry, upload, migration,
  deployment, push, merge, package publication, or authenticated browser QA
  occurred during this local commit-only phase.
