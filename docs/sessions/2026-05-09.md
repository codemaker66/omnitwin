# 2026-05-09

## T-385 — Splat Transform Post-Training Bridge Feasibility

- Researched current `@playcanvas/splat-transform` package posture from official npm/GitHub sources. Latest observed package version is `2.1.0`, with MIT license metadata, Node >=18 engine, CLI binary `splat-transform`, and GPU/WebGPU paths for floater/cluster filtering, voxel output, and collision mesh generation.
- Added `docs/architecture/splat-transform-post-training-bridge.md` as `SPLAT-XFORM-001`. Verdict is green for an offline post-training spike after a real RunPod `scene.ply` exists.
- Documented the proposed bridge: `scene.ply -> cleaned scene -> summary stats -> voxel/collision proxy -> optional diagnostic viewer/export -> Truth Mode labelled derived operational proxy`.
- Added future task T-386 to run the first real Trades Hall `scene.ply` fixture. T-386 remains blocked on T-091 producing an actual scene; T-091 and T-091A remain not done.
- Updated the License & IP Compliance Ledger with a conservative PlayCanvas splat-transform candidate row: MIT observed from official sources, but production/evidence use remains pending Venviewer fixture review, version pinning, hashes, attribution handling, and generated-output provenance.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` for T-385/T-386.

No package install, production runtime change, Spark visual route change, public copy change, legal/fire/accessibility/survey-grade claim, or package rename.

## T-387 — PlayCanvas Post-Training Diagnostic Toolchain Feasibility

- Researched current PlayCanvas package posture from official npm/GitHub sources for `@playcanvas/splat-transform`, SuperSplat, `@playcanvas/supersplat-viewer`, `playcanvas`, and `@playcanvas/react`.
- Added `docs/architecture/playcanvas-toolchain-post-training-spike.md` as `PLAYCANVAS-TOOLCHAIN-001`. Verdict is green for an offline diagnostic/toolchain lane after a real RunPod `scene.ply` exists.
- Documented the recommended split: keep Spark as Venviewer's runtime, use `@playcanvas/splat-transform` for post-training cleanup/statistics/SOG/LOD/voxel/collision outputs, use SuperSplat for internal visual QA, and use SuperSplat Viewer for internal static diagnostics.
- Added T-388 for a future SuperSplat diagnostic viewer fixture and T-389 for a future voxel/collision proxy evaluation against operational geometry needs. Both remain blocked on real T-091 output and T-386 fixture evidence.
- Updated the License & IP Compliance Ledger with conservative PlayCanvas rows for SuperSplat, SuperSplat Viewer, PlayCanvas Engine, and PlayCanvas React. All remain pending review/research-only before production or evidence use.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` for T-387/T-388/T-389.

No package install, production dependency, Spark route change, `/dev/trades-hall-visual` behavior change, public copy change, T-091/T-091A completion, legal/fire/accessibility/survey-grade claim, or package rename.

## T-390 — Table/Chair Group Integrity

- Fixed the labelled seating regression where moving a table could leave part of its labelled chair ring behind.
- Added `expandIdsToGroupMembers` in `packages/web/src/lib/placement.ts` and routed remove/group/ungroup actions through it so partial selections do not silently split a table ring.
- Seeded drag movement in `packages/web/src/components/SelectionSystem.tsx` from the actively dragged item's group before reading the current selection, avoiding pointer-frame selection lag.
- Added placement-store regression tests for labelled chairs moving with their table, whole-ring ungrouping, and regrouping a table without leaving chairs behind.

Verified `pnpm --filter @omnitwin/web test -- placement-store`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`, and `pnpm --filter @omnitwin/web build`.

## T-391 — Category-First Furniture Catalogue Navigation

- Added immediate category-jump controls to the desktop furniture panel in `packages/web/src/components/editor/VerticalToolbox.tsx`, so furniture sections are visible up front instead of hidden behind a long scroll.
- Added editor E2E coverage proving the category controls render and can jump to the Staging section.
- Added T-392 as the explicit follow-up for chair row/block placement brush, richer multi-select/group workflow, and the broader premium planner visual redesign.

Verified with the same web checks listed for T-390.

## T-393 — First-Person Saved POV Controls

- Fixed saved camera-reference POV controls so entering a chair/table/floor POV switches from planner orbit to human eye-point look behavior.
- Right-drag now turns the camera around its current eye point while the saved POV is active, instead of orbiting around the room target.
- Escape now cancels or exits the POV, restores the pre-POV planner camera pose, clears the active POV height switch, and returns normal planner controls.
- Added pure tests for the human POV look math and expanded editor E2E coverage through creating a POV, right-dragging inside it, and exiting with Escape.

Verified `pnpm --filter @omnitwin/web test -- human-pov-camera CameraRig`, `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, and `pnpm --filter @omnitwin/web build`.

## T-394 — Drag-From-Catalogue Furniture Placement

- Added polished League/TFT-style catalogue dragging to the desktop furniture panel.
- Catalogue rows still support click-to-place, but an intentional pull now starts drag mode, shows a cursor-following glass/gold placement token, and hands off to the existing 3D placement ghost/drop pipeline when the pointer crosses the scene.
- Hardened placement so dropping over toolbar/panel chrome does not place furniture through the UI, and drag release clears catalogue/ghost/guide state.
- Added editor E2E coverage for the lifted placement token and successful drag release.

Verified `pnpm --filter @omnitwin/web test -- catalogue-store PlacementGhost.helpers`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, and `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`.

## T-395 — Permissive Precise Furniture Placement

- Fixed the clunky furniture move loop by preserving the pointer grab offset during drag, so tables/platforms no longer jump their centres under the cursor.
- Removed hard blocking for soft placement violations. Furniture can now be moved or placed into overlap/out-of-room states, while the scene displays a holographic red-cross constraint skin over violating objects.
- Added smart alignment snapping against nearby furniture centres and edges, reusing the existing orange floor guides so top-down composition is easier to make visually clean.
- Tightened placement validity to use the rotated footprint against room bounds, not just the item centre point.
- Replaced floating chair/table label sprites with integrated muted-gold tape plates on top of the object; table labels animate gently as a premium planning cue.
- Preserved table/chair group movement, drag-from-catalogue placement, no-highlight planner chrome, and current public-copy/evidence boundaries.

Verified `pnpm --filter @omnitwin/web test -- placement snap-guide ConstraintViolationSkin`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`, and `pnpm --filter @omnitwin/web build`.

## T-396 — Selected-Table Dressing Actions

- Added white table cloth and dinner place-setting catalogue assets alongside the existing black cloth, with UUID-backed catalogue icons and table-dressing helpers.
- Dropping a cloth or dinner-setting item now applies to all selected tables first, or to the nearest hovered table when no table is selected, instead of creating loose decor objects.
- Reused the existing cloth drape animation for black/white cloth changes and added table-bound dinner place settings with plates, gold rims, glasses, and cutlery.
- Persisted `clothStyle` and `tableSetting` through editor store, scene bridge, anonymous draft recovery, web API parsing, WebSocket autosave metadata, and Hallkeeper instruction metadata.
- Preserved black vs ivory linen intent in the 2D blueprint adapter.

Verified `pnpm --filter @omnitwin/web test -- PlacementGhost.helpers table-dressing placement-store editor-roundtrip editor-store anonymous-planner-draft PlacedFurniture table-group placement adapt`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, `pnpm --filter @omnitwin/web build`, `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`, `pnpm --filter @omnitwin/types test`, `pnpm --filter @omnitwin/types lint`, `pnpm --filter @omnitwin/types typecheck`, `pnpm --filter @omnitwin/types build`, `pnpm --filter @omnitwin/api lint`, `pnpm --filter @omnitwin/api typecheck`, and `pnpm --filter @omnitwin/api test -- auto-save`.

No public copy, backend schema migration, T-091/T-091A loader, auth/upload/security logic, or package rename.

## T-397 — Laser Diagram Markup

- Added a first-class Laser Diagram tool to the 3D planner toolbar and mobile dock.
- Added `packages/web/src/stores/markup-store.ts` for colour/thickness/draft/commit/undo/clear state plus safe local serialization.
- Added `packages/web/src/components/MarkupLayer.tsx` to draw luminous floor-anchored tube strokes inside the R3F scene, and `packages/web/src/components/MarkupPersistence.tsx` to persist local planning markups per configuration in the browser.
- Wired `SelectionSystem` to stand down while markup mode is active, preventing diagram drawing from accidentally moving furniture, selecting objects, opening camera POVs, or toggling walls.
- Added polished controls for gold/ivory/ruby/cyan laser ink, stroke weight, undo, and clear, plus copy that keeps the feature scoped as local planning notes rather than venue truth/evidence.
- Tightened the first-run furniture coachmark so it does not overlap the drawing panel.

Verified `pnpm --filter @omnitwin/web test -- markup-store`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`, and `pnpm --filter @omnitwin/web build`. The editor E2E covers drawing persistence, Undo, toolbar presence, and the no-overlap coachmark guard.

No public copy, backend schema migration, T-091/T-091A loader, auth/upload/security logic, or package rename.

## T-398 — Cinematic Planner Shell

- Added a shared cinematic planner shell in `packages/web/src/App.css` and mounted the 3D canvas inside `.planner-canvas-stage` in `packages/web/src/App.tsx`.
- Added warm canvas grading, depth vignette, reduced-grain finish, and a subtle R3F fog background so the editor reads less like a bare WebGL surface and more like a staged planning environment.
- Refined the desktop toolbar, furniture catalogue panel, category chips, mobile dock, and toolbar hover/focus states in `packages/web/src/components/editor/VerticalToolbox.tsx` with a consistent black/gold material system.
- Added App unit coverage that the canvas is mounted inside the cinematic planner shell.
- Captured a Chromium screenshot smoke; the local visual smoke mounted the canvas/chrome while the local API server was offline, so two expected `localhost:3001` resource errors were present.

Verified `pnpm --filter @omnitwin/web test -- App`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`, and `pnpm --filter @omnitwin/web build`.

No public copy, public claims, backend behavior, T-091/T-091A loader state, auth/upload/security logic, or package rename changed.

## T-399 — Transitive Audit Floor Refresh

- Added conservative root `pnpm.overrides` floors for `fast-uri` 3.1.2 and `fast-xml-builder` 1.1.7 after `pnpm audit --audit-level=moderate` reported new transitive advisories through Fastify/AJV and AWS SDK XML tooling.
- Regenerated the lockfile and local install so the patched dependency floors are what the repo actually resolves.

Verified `pnpm install`, `pnpm audit --audit-level=moderate`, `pnpm --filter @omnitwin/api test`, and `pnpm --filter @omnitwin/web build`.

No source behavior, public copy, auth/upload/security logic, package rename, or T-091/T-091A loader state changed.
