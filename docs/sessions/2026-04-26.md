# Session log: 2026-04-26

## Decided

- Committed the pending PRODUCTION.md cleanup left over from the workflow infrastructure prompt set: drop the intro "Scary words explained" glossary, drop the Phase 8 TOC entry, normalize blank-line spacing throughout.
- Phase 8 section content was preserved — only the TOC entry was removed. The earlier uncommitted state had also deleted the full Phase 8 body (pricing tiers + Trades Hall at-cost rate + free trial mechanics + revenue math) and added a stray `wai` typo at the end of the file. Both were unflagged in the previous handoff and treated as accidental, so Phase 8 was restored from `git show HEAD` and `wai` was dropped before commit.
- Also restored an unflagged deletion of the section 1.3 "Alternative: Fly.io (free tier)" blockquote, which was lost in the same uncommitted state but not part of the intended cleanup.

## Built

- One commit on `master` landing only the three intended PRODUCTION.md changes (intro glossary removed, Phase 8 TOC entry removed, whitespace).

## Tried

(none)

## Next

- Pricing content (public pricing ladder, Trades Hall partner-rate options A/B/C, free-trial mechanics, revenue math) currently lives in `docs/PRODUCTION.md` Phase 8. If we want to move it — to a memory file, to a separate `PRICING.md`, or to an ADR — that should be a deliberate, separately-committed change so the deletion intent is explicit in the commit log.
- Land the unified RunPod training pipeline implementation per the next prompt in the queue.

---

## Decided (RunPod pipeline session, later same day)

Persona: Mr. Genjutsu (rendering architecture lead) for the ADR work; Mr. Scaffold (filesystem and infrastructure scaffolding) for the implementation files.

- D-014 (Venue Artifact Factory) promoted from `proposed` to `accepted`. Added a new `## Implementation contract` section that pins the bundle file list, the `manifest.json` schema, the v1 placeholder signature posture (with the v1 → v2 → v3 Ed25519 → Sigstore migration path called out), and the trust boundary statement (pods write candidate bundles; backend ingestion T-053 is the trust gate).
- D-016 (RunPod-canonical training environment) created as `accepted`. Locks RunPod A100 80GB Community Cloud as the default training surface; Secure Cloud for canonical bake-off runs; spot/interruptible deferred until checkpoint resume is proven end-to-end.
- ADR README gains a new `## Operational decisions (accepted 2026-04-26)` section above the workflow infrastructure section, with D-016 as the first entry. D-014's row updated to `Accepted` for consistency with the status promotion.
- Implemented the unified RunPod training pipeline derived from synthesis of two deep research outputs (Claude + ChatGPT, 2026-04-26). Single-trainer (gsplat 1.5.3), single-GPU (A100 80GB), pre-built wheel + pinned `pycolmap` rmbrualla fork, no nerfstudio, sparse UV+depth `.npz` priors, PLY canonical with SPZ derived later, $0.20 mip-NeRF garden smoke gate before any production run.
- Added a Shepherd Protocol section to `docs/state/tasks.md` to make task-list maintenance non-negotiable per session, plus a one-line reference to it in `CLAUDE.md`.
- Task list updates: T-001 → `in-progress` (smoke gate is step 1 to close it); T-002 → `done` (runbook landed); T-019 → `done` (E57 depth supervision script landed); new T-053 in Tier 2 (backend ingestion), `deferred` until T-018 lands.

## Built (RunPod pipeline session)

- ADR work: `docs/architecture/adr/D-014.md` updated; `docs/architecture/adr/D-016.md` created; `docs/architecture/adr/README.md` updated.
- `infra/runpod/`: `Dockerfile`, `healthcheck.py`, `bootstrap.sh`, `verify_build.sh`, `write_rclone_conf.sh`, `run_training.sh`, `runpod-template.json`, `RUNBOOK.md`.
- `venviewer_training/`: `__init__.py`, `simple_trainer_depth.py`, `colmap_depth_dataset.py`, `ssim_fallback.py`, `project_e57_depth.py`, `eval_holdout.py`, `make_manifest.py`, `webgl_fps.ts`.
- `configs/training/`: `config_b.yaml`, `config_smoke.yaml`.
- `docs/specs/runpod-training-contract.md` — bundle format spec referenced from D-014 and D-016.
- `docs/state/tasks.md` — T-001..T-005 + T-019 status changes, new T-053 row in Tier 2, Shepherd Protocol section appended.
- `docs/diagrams/task-graph.md` — regenerated. T-001 now sage (in-progress), T-002 + T-019 gold (done), T-053 charcoal (deferred). Tier 2 surfaced-by-status-change subgraph added; `T-018 → T-053 unblocks` edge labels the activation trigger.
- `CLAUDE.md` — single-line task-list-shepherding reference added directly after the session-logs line in the architecture/decision-log section (CLAUDE.md has no literal "Operating principles" heading; placement reflects the instruction's "after the line about session logs").

## Verified

- Both `config_b.yaml` and `config_smoke.yaml` parse with `yaml.safe_load`.
- All 8 Python files (7 in `venviewer_training/`, 1 in `infra/runpod/`) pass `ast.parse`.
- `infra/runpod/runpod-template.json` parses as valid JSON.
- All 4 bash scripts (`bootstrap.sh`, `verify_build.sh`, `write_rclone_conf.sh`, `run_training.sh`) pass `bash -n` syntax check.
- Cross-references between D-014, D-016, RUNBOOK.md, and runpod-training-contract.md are mutually consistent.
- No code in `apps/`, `packages/`, or `src/` was modified (constraint per the prompt).
- `task-graph.md` Mermaid block parses against `_theme.md` (init block + classDefs + node syntax all match the documented theme).

## Tries (notes for next session)

- `simple_trainer_depth.py` is a *thin wrapper* around a vendored copy of gsplat 1.5.3's `examples/simple_trainer.py` — it monkey-patches the two import sites (`Dataset` → our depth-augmented subclass; `fused_ssim` → fallback), then loads the vendored upstream from `_upstream_simple_trainer.py` and re-exports its `main()`. The vendored upstream file is NOT shipped in this commit. Operators run the documented `curl` + `sha256sum` commands inside `venviewer_training/` per the `VENDORING` block in the file before first pod use. This avoids reproducing thousands of lines of upstream code we have no business owning, while keeping fork drift detectable via the recorded SHA.
- `colmap_depth_dataset.py` similarly assumes a vendored `_upstream_colmap.py` (gsplat's `examples/datasets/colmap.py`) sits next to it.
- The runbook's "Things to verify on first real run" checklist (12 items, mirrors Claude Research §9 open questions) is the gate that closes T-001. Until those tick, T-001 stays `in-progress`.

## Unverified — please check on first pod boot

- Dockerfile actually builds in a real Docker daemon (not testable from Claude Code; must be built and pushed before any pod boots).
- `gsplat==1.5.3+pt24cu124` wheel still resolves at `https://docs.gsplat.studio/whl/pt24cu124`.
- `pycolmap` rmbrualla fork at commit `cc7ea4b73` still installs (commit hasn't been deleted/rewritten upstream).
- The exact upstream gsplat `simple_trainer.py` SHA — recorded in the `_upstream_simple_trainer.sha256` file at vendoring time per the `VENDORING` block, not now.
- Trainer's actual flag names for depth supervision (`--depth_loss --external_depth_dir`) match what gsplat 1.5.3's example trainer expects. If they don't, `colmap_depth_dataset.Dataset.__init__` keyword name needs to match gsplat's CLI surface.

## Next

- Operator path: build and push the trainer image (`docker build / docker push`), configure RunPod template + secrets per `infra/runpod/RUNBOOK.md`, run the $0.20 smoke test, verify pass criteria, mark T-001 `done`, then run Config B on Trades Hall (T-003).
- Architecture path (parallel): VSIR-0 schema implementation (T-018) once the deep research lands; bake-off harness (T-022) once the deep research lands. Both independent of T-001 closing. Recommend operator path before architecture work — get a working pipeline before adding schema or bake-off complexity.
