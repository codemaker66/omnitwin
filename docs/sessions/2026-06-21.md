# 2026-06-21

## T-469 public acquisition production-preview pass

- Checked live `https://venviewer.com/` visually in the in-app browser.
- Live public landing rendered without console errors and served the current optimized venue image files:
  - `/images/venue/grand-hall-room.jpg`
  - `/images/venue/reception-room.jpg`
  - `/images/venue/robert-adam-room.jpg`
  - `/images/venue/saloon-room.jpg`
  - `/images/venue/trades-hall-exterior.jpg`
- Live image hashes matched the repo assets, so the current “old photos” report is most likely stable filenames/browser cache plus the older light public landing layout, not missing image deployment.
- Added `packages/web/e2e/public-acquisition-visual-performance.spec.ts` for production-preview visual, accessibility, no-overflow, and RAF/CDP frame-budget coverage on:
  - landing desktop with updated room photo switching
  - landing mobile
  - pricing desktop with monthly/annual CTA rewiring
  - pricing mobile
- Added stable visual baselines under `packages/web/e2e/public-acquisition-visual-performance.spec.ts-snapshots/`.
- Expanded `packages/web/e2e/accessibility-route-audit.spec.ts` from 8 route states to 10 route states by adding public `/` and `/pricing`; final matrix is 30 samples across desktop, tablet, and mobile.

## Fixes

- Added named main landmarks to `LandingPage` and `PricingPage`.
- Added a dark caption backplate over the landing hero room image so the selected Reception Room caption clears contrast checks.
- Raised muted pricing text contrast.
- Raised executive analytics label/disclosure/action contrast.
- Hardened `packages/web/e2e/support/accessibility-audit.ts` so contrast checks parse modern `rgb()`, legacy comma `rgba()`, `oklab()`, `oklch()`, and layered gradients without over-brightening faint overlay backgrounds.

## Verification

- `pnpm --dir packages/web typecheck`: passed.
- `pnpm --dir packages/web lint`: passed.
- `pnpm --dir packages/web test -- src/__tests__/LandingPage.test.tsx src/components/dashboard/__tests__/ExecutiveAnalyticsView.test.tsx`: 24/24 passed.
- `pnpm --dir packages/web build`: passed with normal production settings.
- `E2E_WEB_SERVER=preview pnpm --dir packages/web e2e -- e2e/public-acquisition-visual-performance.spec.ts --reporter=list --update-snapshots --workers=1`: 4/4 passed.
- `VITE_ENABLE_E2E_AUTH_BYPASS=true pnpm --dir packages/web build`, then `E2E_WEB_SERVER=preview pnpm --dir packages/web e2e -- e2e/accessibility-route-audit.spec.ts --reporter=list --workers=1`: 30/30 passed.
- Public frame artifact: `artifacts/t469-public-acquisition-frame-visual-2026-06-21/report.json`, all samples passed, p95/max around 16.7-16.8ms, zero pass-budget breaches.
- Route accessibility artifact: `artifacts/t469-accessibility-route-audit-2026-06-20/report.json`, 30 route/viewport samples, zero named-control, landmark, focus-visible, reduced-motion, contrast, page-error, or console-error findings.

## Boundaries

- The protected-route production-preview accessibility run uses the existing e2e-only auth bypass guarded by `VITE_ENABLE_E2E_AUTH_BYPASS=true` and `window.__OMNITWIN_E2E__ === true`; normal production build remains verified without that flag.
- T-453 still blocks public signed Reception Room runtime exposure.
- No T-091/T-091A completion, `spz_with_mesh`, `textSplats`, public approval, or unsafe room-accuracy claim was added.

## T-469 cinematic public landing replacement

- Replaced the remaining old light acquisition page with a graphite/gold/cyan cinematic room showcase matching the current Venviewer direction.
- Added functional hero room selector controls for Grand Hall, Saloon, Robert Adam Room, and Reception Room.
- Kept the public Reception Room route honest: the public landing can show the updated room photo, but the runtime copy still says the Reception Room visual is staged internally and unverified until T-453 evidence exists.
- Added platform signal panels for visual package, evidence state, client flow, and performance budget without claiming signed runtime readiness.
- Visual QA found the mobile hero CTA could paint outside the viewport even when `document.scrollWidth` did not report overflow; fixed the hero grid/CTA sizing and added a Playwright assertion that checks the actual hero/action bounding boxes.
- Updated public acquisition visual baselines for the cinematic landing desktop/mobile states.

### Verification

- `pnpm --dir packages/web test -- src/__tests__/LandingPage.test.tsx src/__tests__/RoomShowcasePage.test.tsx`: 37/37 passed.
- `pnpm --dir packages/web typecheck`: passed.
- `pnpm --dir packages/web lint`: passed.
- `pnpm --dir packages/web build`: passed with normal production settings.
- `E2E_WEB_SERVER=preview pnpm --dir packages/web e2e -- e2e/public-acquisition-visual-performance.spec.ts --reporter=list --update-snapshots --workers=1`: 4/4 passed.
- `VITE_ENABLE_E2E_AUTH_BYPASS=true pnpm --dir packages/web build`, then `E2E_WEB_SERVER=preview pnpm --dir packages/web e2e -- e2e/accessibility-route-audit.spec.ts --reporter=list --workers=1`: 30/30 passed.
- Rebuilt normally after the e2e auth-bypass run.
- `E2E_WEB_SERVER=preview pnpm --dir packages/web e2e -- e2e/landing-mobile-planner.spec.ts --reporter=list --workers=1`: 10/10 passed.

### Artifacts

- Public frame/visual report: `artifacts/t469-public-acquisition-frame-visual-2026-06-21/report.json`.
- Fixed mobile CTA screenshot: `C:/tmp/venviewer-mobile-cta-fixed.png`.
- Local desktop cinematic screenshot: `C:/tmp/venviewer-cinematic-landing-local-ready-2.png`.
- Local Reception Room state screenshot: `C:/tmp/venviewer-cinematic-landing-reception-local.png`.

### Boundaries

- No public signed Reception Room runtime was exposed.
- No T-453 bypass was added.
- No T-091/T-091A completion, `spz_with_mesh`, `textSplats`, public approval, or unsafe verification claim was added.

## T-469 production-live route pass

- Added `packages/web/e2e/production-live-route-pass.spec.ts` for a live `https://venviewer.com` route pass covering:
  - public room pages: Grand Hall, Reception Room, Robert Adam Room, Saloon
  - proposal share functional client response/approval flow using production frontend plus non-mutating network fixture
  - supplier portal clarification/acknowledgement flow using production frontend plus non-mutating network fixture
  - mobile unavailable proposal/supplier share states
  - protected dashboard drawer URLs (`reviews`, `loadouts`, `proposals`, `admin`, `analytics`) failing closed to the auth shell
- The harness records live screenshots, visible-control/dead-button audits, accessibility audits, action proofs, CDP metrics, and RAF frame-budget summaries to `artifacts/t469-production-live-route-pass-2026-06-21/report.json`.
- Tightened screenshot capture to reset scroll before visual artifacts, so public room screenshots show the first viewport rather than inherited scroll positions.

## Fixes

- Rebuilt `LoginPage` and `RegisterPage` from the old light auth shell into a graphite/gold/cyan Venviewer auth shell via `AuthPage.css`.
- Fixed issues found by the production-live protected route pass:
  - undersized Clerk visible controls
  - missing named `main` landmark on auth pages
  - low-contrast Sign up action in the Clerk footer
- Kept third-party Clerk auth visible and honest; did not conceal Clerk's environment banner.

## Verification

- `pnpm --dir packages/web typecheck`: passed.
- `pnpm --dir packages/web lint`: passed.
- `pnpm --dir packages/web build`: passed with normal production settings.
- `E2E_WEB_SERVER=preview pnpm --dir packages/web e2e -- e2e/production-live-route-pass.spec.ts -g "protected dashboard" --reporter=list --workers=1`: 1/1 passed.
- Vercel production deployment for commit `c0539b0c4dc79af56eef06e4c7bdabef5dff8827` reached `READY`.
- `E2E_START_SERVER=false E2E_BASE_URL=https://venviewer.com FRAME_BUDGET_SAMPLE_MS=1200 pnpm --dir packages/web e2e -- e2e/production-live-route-pass.spec.ts --reporter=list --workers=1`: 5/5 passed.
- Final live report: 13 route samples, 13 accessibility samples, 13 control samples, max p95 `16.8ms`, zero sustained pass-budget misses, zero failed samples.

## Artifacts

- Report: `artifacts/t469-production-live-route-pass-2026-06-21/report.json`.
- Representative screenshots:
  - `artifacts/t469-production-live-route-pass-2026-06-21/desktop-public-room-reception-room.png`
  - `artifacts/t469-production-live-route-pass-2026-06-21/desktop-proposal-share-functional.png`
  - `artifacts/t469-production-live-route-pass-2026-06-21/desktop-supplier-portal-functional.png`
  - `artifacts/t469-production-live-route-pass-2026-06-21/desktop-protected-dashboard-auth-reviews.png`

## Boundaries

- Protected drawer internals on live production were not opened because there is no production-safe signed staff/admin session in this run; live scope verified fail-closed auth redirects, while drawer internals remain covered by the production-preview bypass matrix.
- Live auth screenshots show Clerk `Development mode`; opened T-470 as blocked pending real production Clerk keys/environment values. Do not hide the banner with CSS.
- Browser plugin was attempted earlier in the pass, but screenshot capture timed out there; Playwright was used for the browser evidence loop.
- No public signed Reception Room runtime was exposed.
- No T-453 bypass was added.
- No T-091/T-091A completion, `spz_with_mesh`, `textSplats`, public approval, or unsafe verification claim was added.
