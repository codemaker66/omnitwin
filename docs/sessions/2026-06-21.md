# 2026-06-21

## T-469 public acquisition production-preview pass

- Checked live `https://venviewer.com/` visually in the in-app browser.
- Live public landing rendered without console errors and served the current optimized venue image files:
  - `/images/venue/grand-hall-room.jpg`
  - `/images/venue/reception-room.jpg`
  - `/images/venue/robert-adam-room.jpg`
  - `/images/venue/saloon-room.jpg`
  - `/images/venue/trades-hall-exterior.jpg`
- Live image hashes matched the repo assets, so the current “old photos” report is most likely stable filenames/browser cache plus the older light public landing layout, not missing image deployment.
- Added `packages/web/e2e/public-acquisition-visual-performance.spec.ts` for production-preview visual, accessibility, no-overflow, and RAF/CDP frame-budget coverage on:
  - landing desktop with updated room photo switching
  - landing mobile
  - pricing desktop with monthly/annual CTA rewiring
  - pricing mobile
- Added stable visual baselines under `packages/web/e2e/public-acquisition-visual-performance.spec.ts-snapshots/`.
- Expanded `packages/web/e2e/accessibility-route-audit.spec.ts` from 8 route states to 10 route states by adding public `/` and `/pricing`; final matrix is 30 samples across desktop, tablet, and mobile.

## Fixes

- Added named main landmarks to `LandingPage` and `PricingPage`.
- Added a dark caption backplate over the landing hero room image so the selected Reception Room caption clears contrast checks.
- Raised muted pricing text contrast.
- Raised executive analytics label/disclosure/action contrast.
- Hardened `packages/web/e2e/support/accessibility-audit.ts` so contrast checks parse modern `rgb()`, legacy comma `rgba()`, `oklab()`, `oklch()`, and layered gradients without over-brightening faint overlay backgrounds.

## Verification

- `pnpm --dir packages/web typecheck`: passed.
- `pnpm --dir packages/web lint`: passed.
- `pnpm --dir packages/web test -- src/__tests__/LandingPage.test.tsx src/components/dashboard/__tests__/ExecutiveAnalyticsView.test.tsx`: 24/24 passed.
- `pnpm --dir packages/web build`: passed with normal production settings.
- `E2E_WEB_SERVER=preview pnpm --dir packages/web e2e -- e2e/public-acquisition-visual-performance.spec.ts --reporter=list --update-snapshots --workers=1`: 4/4 passed.
- `VITE_ENABLE_E2E_AUTH_BYPASS=true pnpm --dir packages/web build`, then `E2E_WEB_SERVER=preview pnpm --dir packages/web e2e -- e2e/accessibility-route-audit.spec.ts --reporter=list --workers=1`: 30/30 passed.
- Public frame artifact: `artifacts/t469-public-acquisition-frame-visual-2026-06-21/report.json`, all samples passed, p95/max around 16.7-16.8ms, zero pass-budget breaches.
- Route accessibility artifact: `artifacts/t469-accessibility-route-audit-2026-06-20/report.json`, 30 route/viewport samples, zero named-control, landmark, focus-visible, reduced-motion, contrast, page-error, or console-error findings.

## Boundaries

- The protected-route production-preview accessibility run uses the existing e2e-only auth bypass guarded by `VITE_ENABLE_E2E_AUTH_BYPASS=true` and `window.__OMNITWIN_E2E__ === true`; normal production build remains verified without that flag.
- T-453 still blocks public signed Reception Room runtime exposure.
- No T-091/T-091A completion, `spz_with_mesh`, `textSplats`, public approval, or unsafe room-accuracy claim was added.
