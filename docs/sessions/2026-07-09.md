# Session log — 2026-07-09

## Whole-repository hardening sweep

User request: inspect the complete codebase, fix every actionable code defect,
and then offer three ambitious next features. Work was tracked as T-472 through
T-479 and closed without discarding the repository's unrelated dirty work.

### Backend authority and data integrity

- WebSocket autosave now applies the same platform/venue authority, review
  locks, expected-revision CAS, revision history, and coordinate write nonce as
  HTTP writers. Buffered flushes coalesce in flight, retain failed work, and do
  not enter conflict/lock retry loops.
- `0044_placed_objects_render_to_real.sql` now locks every coordinate-bearing
  table, transforms only legacy live placed-object X/Z values, records explicit
  coordinate-space provenance for immutable evidence, and blocks old writers
  that omit a fresh nonce. Read boundaries normalize legacy Hallkeeper/sheet/
  Ops data; unknown coordinate spaces fail closed.
- Event scenarios reject phases from another event in both route logic and the
  `0045_event_scenario_phase_scope.sql` composite foreign key.
- Clerk webhooks are runtime-validated, idempotent, and return a retryable 503
  when processing fails instead of acknowledging lost work.
- Public enquiry validation now comes from the shared XOR schema, eliminating
  API/type drift between `configurationId` and `venueSlug` submissions.

### Twin and Twin Forge

- Signed negative floor bands are preserved; automatic navigation edges remain
  on one storey and reviewed overrides are the only cross-storey mechanism.
- Forge now preflights sources, stages complete bundles, verifies exact
  files/content/hashes and the 8 MiB mesh budget, and promotes by atomic rename
  with bounded Windows lock retries plus rollback. `--refresh-manifest` safely
  updates pose/navigation metadata without silently regenerating tiles.
- Real local Trades Hall refresh: 149 nodes, 65 on floor -1, 84 on floor 0,
  349 edges, zero automatic cross-floor edges, 448 hashes, and a 7,158,232-byte
  mesh. The gitignored bundle promoted cleanly with no staging residue.
- The runtime shares reference-counted pano textures, aborts obsolete streams,
  closes ImageBitmaps deterministically, prewarms neighbouring GPU textures,
  and batches/culls the parallax shell. These runtime changes are present in
  Blake's user-owned HEAD commit `1ebbc365`, which was preserved when HEAD
  advanced during the sweep.

### Frontend reliability and accessibility

- Enquiry list/history/detail requests abort and ignore stale responses;
  navigation clears obsolete history and refreshes deterministically.
- Dashboard result cards are native keyboard controls. The Spotlight hero is a
  named main landmark. React mounts before optional Sentry loading.
- The planner no longer mounts a transient/stale canvas before the requested
  configuration owns the store. Public planner E2E waits on the exact config
  and interactive toolbar; the WebGL-heavy file runs serially so Chromium's
  shared GPU process cannot starve eight simultaneous R3F boots.
- Floating planner widgets now avoid the Truth/lens right dock, and spatial
  regressions assert genuine two-dimensional clearance instead of a stale
  fixed left/right ordering.
- Vitest can no longer pass an empty suite, and dependency overrides clear the
  audited Babel/ESLint advisories.

### Verification

- Workspace typecheck: pass (types, API, web, Twin Forge).
- Workspace lint: pass (types, API, web, Twin Forge).
- Full unit matrix: 6,604 tests pass — types 1,711; API 2,011; web 2,857;
  Twin Forge 25.
- Final focused UI regressions: 45/45 pass.
- Focused API hardening matrix: 119/119 pass.
- Focused Playwright matrix: 56 pass, 3 intentionally skipped, 0 failures.
  It covers the full accessibility route/viewport matrix, public planner flow,
  and Twin walkthrough/deep-link/responsive behavior.
- Real in-app browser pass at `/venues/trades-hall/twin?node=scan_000`: all 149
  viewpoints load; floor -1/0 switching and Walk/Plan/Dollhouse work; WebGL
  canvas is 1920×1080 for a 1280×720 viewport; no console warnings/errors.
- `pnpm audit --audit-level low`: zero known vulnerabilities.
- Types/API builds and `vite build --mode development`: pass. The normal local
  production build correctly refuses the checked-in Clerk `pk_test_` key; it
  was not bypassed with a fake live credential.

### External blockers left honest

- T-001: RunPod template/secrets and smoke gate require owner-side console work.
- T-060/T-092: production Sentry DSNs, upload token, and alert rules are external.
- T-453: reviewed ARF↔CVF coordinate pairs and a signed transform still require
  human evidence; public runtime exposure cannot be invented.
- T-469: the exhaustive interaction audit remains blocked on the signed real
  Reception Room runtime path tracked by T-453.
- T-470: Clerk dashboard verification and a real signed-in production staff/
  admin session remain owner-side checks. The code correctly rejects `pk_test_`
  for production.

No package-wide `omnitwin` rename, fake integration, public evidence claim, or
unsigned room-runtime exposure was introduced.
