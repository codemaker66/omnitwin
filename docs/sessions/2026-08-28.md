# Session log — 2026-08-28

## T-554 authority-none native review replay checkpoint

- Added typed v2 coordinator, source-child, mask-child, and scoped domain
  events; pure deterministic replay; durable append-only child journals;
  explicit intent/commit/recovery-abort transitions; exclusive session-owner
  leases; exact root inventory checks; and crash-resume reconstruction.
- Successful tile delivery is credited only after the trusted Node response
  lifecycle emits `finish` and the first-delivery event is durably appended.
  Browser claims, aborted responses, ambiguous closes, stale generations, and
  cross-crash elapsed time receive no credit.
- Added exact spatial mask-state digests, deterministic mask replay, durable
  freeze/invalidation lineage, and session-store verification. These are
  human-pending review mechanics only; they do not accept a room, interface,
  panorama, mask, or selection volume.
- Added an authority-none compiled implementation-pack builder and verifier
  for the closed ESM core, response-lifecycle adapter, static assets, decoder
  probe, Sharp/libvips closure, and native runtime inspector. The output stays
  outside the Git workspace, is published no-replace, and now reports cleanup
  residue rather than hiding an incomplete cleanup after a failed build.
- Added a Windows x64 Node-API runtime inspector with exact UTF-16 local-path
  handling, reparse/UNC/device rejection, one active DLL directory per loaded
  image, retained directory identity, pre/post pathname revalidation, exact
  loaded-module inventories, cross-copy handle rejection, and fail-stop
  teardown semantics.
- Repaired the native-build custody boundary after audit found that the first
  candidate embedded a local Cargo path and did not prove which linker/SDK
  inputs Cargo actually used. The reviewed build now binds SHA-pinned Rust,
  Node, MSVC linker, and dumpbin executables; fixes MSVC/UCRT/UM library
  inventories and PATH/LIB/INCLUDE; remaps host/workspace paths; rejects host,
  username, workspace, and Cargo-registry text in the PE; and performs two
  isolated clean builds. The replacement 304,128-byte candidate is
  `sha256:e6feb1e3266da498aab4417d356da26c83160bed7be24aef7bc0ab4f5455929b`.
- Runtime attestation runs in a fresh environment-cleared child, restricts
  CommonJS resolution to built-ins and the two exact reviewed native add-ons,
  records only the selected network entry points it actually patches, binds
  dynamic ESM imports to exact pack members, verifies the decoder probe, and
  re-verifies the entire pack after import. It remains a diagnostic candidate;
  production trust, a production pack minter, runtime authority, browser
  launch, and an HTTP listener are deliberately unavailable.
- Removed an orphaned source-custody manager whose valid success path could not
  be exercised while the production pack/runtime brands correctly have no
  minter. Its persisted binding data remains covered by the typed event and
  replay contracts; dormant unverified execution code was not retained.
- No room decision, landmark, registration, transform, geometry, mask,
  reconstruction, generated content, API/provider action, upload, push,
  deployment, browser QA, staging mutation, or production change occurred.
  T-554 remains blocked and human-pending.
- Final gates passed: the current native build (fmt, 8 Rust tests, strict
  Clippy, two clean release builds, PE export/dependency inspection, host-path
  scans, and Node probe); the final 15-file v2/native TypeScript suite with
  222 tests passed and one intentional platform skip; an independent broader
  16-file audit suite with 240 tests passed and one intentional skip; Foundry
  typecheck, lint, and build; and diff/whitespace checks. Independent native,
  runtime-attestation, coordinate-boundary, and whole-checkpoint audits found
  no remaining P0/P1 blocker for a local commit.
- A process-global per-root serial-lane map remains acceptable only because no
  long-lived production factory/server exists. It must gain close/eviction
  semantics before any future server may open unbounded session roots.

## T-557 coordinate-pair intake groundwork

- Added a strict authority-none ARF↔CVF intake schema with explicit raw XGRIDS
  XBIN, historical SOG, processed BIG OBJ, MatterPak OBJ, room-9 boundary,
  interface-atlas, and E57 metadata bindings. The initial artifact must be
  revision-1 `nomination_only`; later successors are immutable and may be
  rejected without smuggling coordinate data into the root lineage.
- Later human-recorded controls require distinct visual nominations, exact OBJ
  face ordinals and ordered triangle vertices, integer barycentric Q9 weights,
  8 fit plus 6 held-out points, disjoint anchors, non-coplanarity, and floor,
  wall, high-detail, and room-depth distribution evidence.
- Added a deferred OBJ-byte verifier with private ordinary-buffer snapshots,
  same/cross-realm `SharedArrayBuffer` rejection, strict decimal/exponent
  parsing, exact BigInt Q9 half-away-from-zero arithmetic, exact topology and
  barycentric recomputation, room-9 inventory checks, and shared-interface
  anchor rejection.
- The five-file slice passed 13 schema tests, 15 OBJ-verifier tests, both
  package typechecks, the types build, and scoped lint. Synthetic coordinates
  exist only in tests. Cleanup-marker faces and E57 point support remain
  explicitly unverified, and accepted output inventory remains `null`.
- Added a read-only processed BIG inventory at
  `docs/operations/grand-hall-processed-big-inventory-v1.json`: 399 files, 60
  directories, 5,056,057,926 bytes, and 95 unique content hashes. Inventory
  digest `sha256:1369a3e897e8c6509abc69605ec87de7378fe0a7c38777c24eba95862cbb63fd`
  proves packaging/member identity only and grants no room or architectural
  authority.
- Preserved a historical ICP replay proof from two separate operating-system
  processes. Each child also reproduced the full unvalidated receipt twice
  in-process. The runner validates the exact reviewed BIG and MatterPak input
  bytes before launch, reverifies them after both children exit, and persists
  no process or machine identifiers. Last-fit input used 8,294
  correspondences; the separate post-fit mutual audit found 8,290. Exactness is
  classified only under reported worker versions and explicit child launch
  controls: the interpreter binary, environment lock, dependency trees, native
  closure, operating system, and CPU identity remain unverified. Two complete
  real production invocations emitted identical 3,464-byte canonical proof
  bytes (`sha256:c9d23fa354e1415dd69fed28ebe3e3e2aa638be75283f1a93b707ed5cfd57c62`).
- Persisted the source-bound historical registration seed at
  `docs/operations/grand-hall-authority-none-registration-seed-v1.json`
  (semantic self-digest
  `sha256:786fe2d4f2e24209d440aefc6d2496337e13b37d9bcba77eafe39a2cf0856c78`).
  Its matrix is a nomination aid only, is not a measurement or solver input,
  claims no convergence, and is not an accepted transform.
- Persisted the exact 5,093-byte empty coordinate-free root at
  `docs/operations/grand-hall-authority-none-coordinate-free-nomination-checkpoint-v1.json`
  (semantic self-digest
  `sha256:f0794f63a0545d852829355d59c779168fdc92770324f2a2a3972a0ac7e74b3b`).
  It is revision 1, `nomination_only`, `authority: none`, has zero nominations
  and coordinate pairs, and retains `productionTrust: null` with every
  mutation, generation, solve, transform, E57-authority, runtime, and public
  guardrail false.
- The coordinate-free verifier remains offline and is not exported as an
  operator command or wired to the API or runtime. It projects the frozen
  source identities and room-9 counts from the reviewed seed while processing
  zero anchors; it does not itself reopen the OBJs. A separate local run of the
  existing exact OBJ verifier reopened the supplied BIG and MatterPak bytes and
  reverified 119,564 room-9 faces, 174 shared vertices, and 292 interface
  faces. These checks establish byte binding only.
- Final affected-scope verification passed: types 106 files / 2,303 tests;
  Reconstruction Foundry 68 files / 965 passed and 3 optional skips; Python
  39/39; strict typecheck, lint, and build for both changed TypeScript packages;
  the exact 399-file / 5,056,057,926-byte processed BIG source regeneration;
  environment-lock byte verification; canonical one-LF/path-redacted artifact
  checks; and the separate exact two-OBJ verification above.
- The recursive workspace typecheck still cannot be treated as a clean-room
  gate in this worktree: `packages/web/node_modules` is a junction into the
  separate main checkout, whose stale `@omnitwin/types` workspace target lacks
  symbols already present at this branch's committed HEAD. It reproduced only
  web errors, and `packages/web` has no diff. The junction was not rewired
  because doing so would mutate the other checkout's dependency installation.
- No non-empty nomination, landmark, real coordinate, accepted transform,
  solver output, E57 point authority, accepted output inventory, output mask,
  generative fill, push, staging action, deployment, publication, or provider
  mutation was created. The checkpoint boundary is recorded in
  `docs/operations/grand-hall-authority-none-registration-checkpoint.md`.
  T-557 remains blocked on T-554, T-555, and qualified source-derived human
  control review.
