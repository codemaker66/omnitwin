# 2026-08-09

## T-539 continuation — timeline truth and presentation-only hall stand-in

This continuation stayed in the dedicated
`C:\Users\blake\omnitwin2-event-layout-runtime` worktree. The dirty user checkout
remained read-only. No deploy or merge was attempted.

### Integrated hardening

- Day/Week preview state now keeps the active frame, historical runtime, frozen
  furniture, minimap, phase, figures, URL, and capture endpoint synchronized at
  the same transition midpoint.
- Dense furniture morphs preserve physical continuity when interrupted while
  keeping orthographic evidence pinned to immutable endpoint layouts. Demand
  rendering and the minimap now receive a trailing redraw when scrubbing stops
  between throttle ticks.
- Back/Forward cancels stale animation generations. Missing, unknown, and
  no-phase history entries hydrate reload-equivalently, and freeze refresh keeps
  the phase that was just frozen.
- Compatible, distinct historical runtime resources blend directly from
  timeline progress. Same-resource endpoints remain a single fully opaque
  resource, and stable physical ordering avoids a midpoint alpha-order pop.
- Cross-tenant freeze targets return `404`, while globally read-only roles still
  receive the deliberate early `403`.
- Panorama-informed procedural dressing is confined to the visibly labelled
  `/dev/trades-hall-visual` review route. It is tagged generated,
  presentation-only, non-exportable, and non-interactive. Operational planner,
  measurement, and handoff paths exclude it and fail closed if excluded content
  reaches capture.
- Panorama/E57 pixels were inspected locally only. None were sent to ImageGen,
  img2threejs, or another external model, and no reference media, derived pixels,
  or hashes enter the intended diff.

### Fresh isolated PostgreSQL proof

- Started the dedicated PostgreSQL 16.6 cluster at `127.0.0.1:54339` and created
  disposable database
  `omnitwin_timeline_0060_20260809_pgverify_1645`.
- `pnpm --filter @omnitwin/api db:migrate` applied the full 62-entry journal
  through `0063`.
- Migration `0060_phase_layout_snapshot_lineage.sql` matched SHA-256
  `370C7EEE2A0DE06FF06FB26655B366E43CC84A67DD4C3268535092CEDBCE1F7F`.
- With the guarded local WebSocket bridge on `127.0.0.1:54331`, the PostgreSQL
  phase-layout suite passed **18/18**. It covered freeze/query/reload, tenant and
  room isolation, canonical concurrency, private delivery, immutability,
  figures, DST, and retry behavior.
- The disposable database, bridge, and isolated cluster were stopped and
  removed after verification. Ports `54331` and `54339` no longer listen; no
  shared or Neon database was touched.

The database rehearsal is contract evidence only. Its two available snapshots
both point to the same 36-byte `synthetic-grand-hall.sog` fixture with
`synthetic_contract_only` rights. It does not prove two real historical Trades
Hall runtimes or a visual crossfade.

### Verification

- Web focused suite: **16 files / 288 tests passed**.
- API focused suite: **8 files / 57 tests passed**.
- Isolated PostgreSQL suite: **1 file / 18 tests passed**.
- Web app + E2E TypeScript and API TypeScript: passed.
- Web and API ESLint: passed.
- Final compact quarantine recheck after adversarial review: shared/API/web +
  E2E TypeScript passed; shared binding **5/5**, focused API **36/36**, and
  renderer/store/cache **40/40** tests passed; changed-surface ESLint passed.
- API production build: passed.
- Web development-mode Vite bundle: passed, 3,296 modules. The production build
  correctly refused to run without a real `pk_live_` Clerk publishable key; no
  fake production credential was introduced.
- `git diff --check` passed. No untracked media, binary diff entries, or private
  reference artifacts remain.
- Independent UI audit found no open code P0/P1 after the fixes. Independent
  provenance audit confirmed the presentation-only route confinement and clean
  intended diff.

### Honest remaining gates

- Adversarial provenance review found that the existing v1 presentation-rights
  evidence cannot support a production approval. A proposed v2 database graph
  was deliberately removed before migration because signature trust, exact
  object-version/privacy evidence, reviewed-profile authority, parsed Scene
  coverage, DB-clock actor separation, and a separate execution attestation
  were not yet complete. No `0064` migration or mutation/status route exists.
- The draft v2 schema and inert browser-evidence harness were not retained:
  without a database authority graph, live status endpoint, or browser driver,
  they would be unreachable skeleton code. The production timeline parser emits
  unavailable state, and authenticated member delivery returns `404` before any
  database or private object read until T-541 lands an independently
  authenticated activation.
- T-541 must define the exact EXIF/orientation, decoder, ICC-to-sRGB, alpha,
  frame-selection, and row-layout rules for normalized panorama pixels. Current
  blocked pixels were not read to populate a new identity, and none of the 149
  existing Matterport/E57 panoramas qualifies as future direct-camera evidence.

- T-539 remains in progress because there are not yet two distinct,
  rights-cleared, human-reviewed Trades Hall runtime packages. The current E2E
  fixture deliberately binds `null` historical runtimes, so exact real-package
  selection, private object delivery, visual crossfade, abort recovery, and
  two-runtime desktop/mobile browser proof remain unverified.
- No fresh browser performance number was recorded. An unrelated orphaned
  Playwright/Edge/SwiftShader workload contaminated the performance lane. The
  current 500-object test samples page `requestAnimationFrame`; it does not prove
  60 fps canvas delivery, detailed hall draw calls, Spark heap stability, or
  real historical-runtime retention.
- Pre-existing
  `packages/web/public/images/venue/grand-hall-facade-3.jpg` remains a deployment
  blocker: embedded Arcaid rights metadata requires prior permission and/or
  fees. This session did not introduce or modify that file. Document permission
  or remove/replace it before any deployment.
