# 2026-08-09

## T-539 continuation — timeline truth and presentation-only hall stand-in

This continuation stayed in the dedicated
`C:\Users\blake\omnitwin2-event-layout-runtime` worktree. The dirty user checkout
remained read-only. No deploy or merge was attempted.

### Integrated hardening

- Day/Week preview state now keeps the active frame, historical runtime, frozen
  furniture, minimap, phase, figures, URL, and capture endpoint synchronized at
  the same transition midpoint.
- Dense furniture morphs preserve physical continuity when interrupted while
  keeping orthographic evidence pinned to immutable endpoint layouts. Demand
  rendering and the minimap now receive a trailing redraw when scrubbing stops
  between throttle ticks.
- Back/Forward cancels stale animation generations. Missing, unknown, and
  no-phase history entries hydrate reload-equivalently, and freeze refresh keeps
  the phase that was just frozen.
- Compatible, distinct historical runtime resources blend directly from
  timeline progress. Same-resource endpoints remain a single fully opaque
  resource, and stable physical ordering avoids a midpoint alpha-order pop.
- Cross-tenant freeze targets return `404`, while globally read-only roles still
  receive the deliberate early `403`.
- Panorama-informed procedural dressing is confined to the visibly labelled
  `/dev/trades-hall-visual` review route. It is tagged generated,
  presentation-only, non-exportable, and non-interactive. Operational planner,
  measurement, and handoff paths exclude it and fail closed if excluded content
  reaches capture.
- Panorama/E57 pixels were inspected locally only. None were sent to ImageGen,
  img2threejs, or another external model, and no reference media, derived pixels,
  or hashes enter the intended diff.

### Fresh isolated PostgreSQL proof

- Started the dedicated PostgreSQL 16.6 cluster at `127.0.0.1:54339` and created
  disposable database
  `omnitwin_timeline_0060_20260809_pgverify_1645`.
- `pnpm --filter @omnitwin/api db:migrate` applied the full 62-entry journal
  through `0063`.
- Migration `0060_phase_layout_snapshot_lineage.sql` matched SHA-256
  `370C7EEE2A0DE06FF06FB26655B366E43CC84A67DD4C3268535092CEDBCE1F7F`.
- With the guarded local WebSocket bridge on `127.0.0.1:54331`, the PostgreSQL
  phase-layout suite passed **18/18**. It covered freeze/query/reload, tenant and
  room isolation, canonical concurrency, private delivery, immutability,
  figures, DST, and retry behavior.
- The disposable database, bridge, and isolated cluster were stopped and
  removed after verification. Ports `54331` and `54339` no longer listen; no
  shared or Neon database was touched.

The database rehearsal is contract evidence only. Its two available snapshots
both point to the same 36-byte `synthetic-grand-hall.sog` fixture with
`synthetic_contract_only` rights. It does not prove two real historical Trades
Hall runtimes or a visual crossfade.

### Verification

- Web focused suite: **16 files / 288 tests passed**.
- API focused suite: **8 files / 57 tests passed**.
- Isolated PostgreSQL suite: **1 file / 18 tests passed**.
- Web app + E2E TypeScript and API TypeScript: passed.
- Web and API ESLint: passed.
- Final compact quarantine recheck after adversarial review: shared/API/web +
  E2E TypeScript passed; shared binding **5/5**, focused API **36/36**, and
  renderer/store/cache **40/40** tests passed; changed-surface ESLint passed.
- API production build: passed.
- Web development-mode Vite bundle: passed, 3,296 modules. The production build
  correctly refused to run without a real `pk_live_` Clerk publishable key; no
  fake production credential was introduced.
- `git diff --check` passed. No untracked media, binary diff entries, or private
  reference artifacts remain.
- Independent UI audit found no open code P0/P1 after the fixes. Independent
  provenance audit confirmed the presentation-only route confinement and clean
  intended diff.

### Honest remaining gates

- Adversarial provenance review found that the existing v1 presentation-rights
  evidence cannot support a production approval. A proposed v2 database graph
  was deliberately removed before migration because signature trust, exact
  object-version/privacy evidence, reviewed-profile authority, parsed Scene
  coverage, DB-clock actor separation, and a separate execution attestation
  were not yet complete. No `0064` migration or mutation/status route exists.
- The draft v2 schema and inert browser-evidence harness were not retained:
  without a database authority graph, live status endpoint, or browser driver,
  they would be unreachable skeleton code. The production timeline parser emits
  unavailable state, and authenticated member delivery returns `404` before any
  database or private object read until T-541 lands an independently
  authenticated activation.
- T-541 must define the exact EXIF/orientation, decoder, ICC-to-sRGB, alpha,
  frame-selection, and row-layout rules for normalized panorama pixels. Current
  blocked pixels were not read to populate a new identity, and none of the 149
  existing Matterport/E57 panoramas qualifies as future direct-camera evidence.

- T-539 remains in progress because there are not yet two distinct,
  rights-cleared, human-reviewed Trades Hall runtime packages. The current E2E
  fixture deliberately binds `null` historical runtimes, so exact real-package
  selection, private object delivery, visual crossfade, abort recovery, and
  two-runtime desktop/mobile browser proof remain unverified.
- No fresh browser performance number was recorded. An unrelated orphaned
  Playwright/Edge/SwiftShader workload contaminated the performance lane. The
  current 500-object test samples page `requestAnimationFrame`; it does not prove
  60 fps canvas delivery, detailed hall draw calls, Spark heap stability, or
  real historical-runtime retention.
- Pre-existing
  `packages/web/public/images/venue/grand-hall-facade-3.jpg` remains a deployment
  blocker: embedded Arcaid rights metadata requires prior permission and/or
  fees. This session did not introduce or modify that file. Document permission
  or remove/replace it before any deployment.

## T-486 continuation — universal Foundry contracts, not reconstruction completion

This continuation used the clean
`C:\Users\blake\omnitwin2-universal-foundry` worktree on
`codex/universal-foundry`; the dirty main checkout remained untouched. No
migration, database write, upload, publication, production-pointer change,
merge, push, or deployment was performed.

### Implemented and reachable boundaries

- Universal Source Facts V6 preserves and re-verifies the complete
  self-digested intake receipt and exact V5 artifact, then adds bounded
  ordinary point-cloud PLY header/payload-extent facts from bytes retained
  during the full-file hash. It does not decode coordinates or establish
  units, frame, accuracy, rights, registration, authority, or movable-object
  classification. The guided local app still shows V5; the advanced
  `inspect-source-facts` CLI reaches V6.
- The multi-root capture-bundle composer deterministically namespaces two or
  more complete admission results, embeds each original admission and exact ID
  map, preserves the strictest legal state, rejects duplicate bytes mounted
  from separate roots, and grants no execution capability. It performs no
  source read, alignment, scale resolution, registration, or fusion.
- The adapter assessment exhaustively compares an exact manifest, explicit
  pinned host inventory, and repository implementation truth. It distinguishes
  structural inspection, exact-asset compatibility, worker, dependency,
  vendor-export, rights, and activation blockers without probing the host,
  reading asset bytes, or starting a process.
- A bounded deterministic metric-registration library contract fits a proper
  source-to-target 3D similarity from exact root/frame/unit/digest-bound
  correspondences with fixed fit and held-out partitions. It records residuals
  and conditioning, but reports overlap `not_computed`, creates no reviewed
  TransformArtifact, and grants no placement, measurement, export, or runtime
  authority. Independent adversarial review returned GO for this bounded
  proposal boundary only; no operator CLI/UI or real Grand Hall correspondence
  run exists.
- The Room Reality Package assembler can form only an authority-none,
  release-blocked local candidate. Reference-catalog identity, exact derived
  members, and captured movable-object classification remain unverified;
  signing, publication, export authority, RuntimePackage registration, and
  activation stay disabled.
- The candidate-metadata path in the loopback `/room-review` page accepts
  candidate/evidence JSON and creates
  a digest-bound decision draft across source comparison, alignment, scale,
  crop, completeness, privacy, and movable objects. Strict transform, Scene
  Authority, and QA bodies remain caller-supplied and unauthenticated. The page
  reads no source pixels, decodes no geometry, applies no correction, and
  cannot approve or release the package.
- The same page now accepts generated `FoundryE57GeometryCropV0` JSON locally,
  capped at 12 MiB and 50,000 points per artifact, with an optional second
  distinct compatible overlay. It requires explicit decisions for all seven
  review dimensions. Raw E57 and source images are not read, nothing is
  uploaded, authority is `none`, and execution, correction, TransformArtifact
  and Scene Authority creation, QA, export, and activation are not authorized.
- A separate exported durable GLB preview core now binds one verified stage,
  exact JobSpec and derivative-rights evidence, a signed one-run permit,
  host-key HMAC-authenticated state records, a fenced writer lease, checkpoint
  resume, fresh output verification, and create-only private review output. It
  remains test-reachable rather than operator CLI/API-reachable, supports only
  the reviewed static-geometry GLB normalization subset, has authority `none`,
  and is not a reconstruction worker. Its built-in durable backend is
  Linux-only; it uses pinned private roots, no-follow handles, kernel-serialized
  lease mutations, monotonic fences, and crash-safe no-overwrite publication.
  Windows and macOS refuse before touching roots.
- A separate deterministic E57 geometry crop seam now source-replays resume
  checkpoints, detaches callback state, normalizes explicit poses, bounds
  memory/work/process time, and has exercised pye57 0.4.19 against a genuine
  tiny ASTM E57 fixture without decoding or extracting images. Its 256 MiB,
  1,000,000-point and 64-scan envelope explicitly excludes the recorded
  965.52-million-point Grand Hall E57; it is authority-none core evidence, not
  Grand Hall reconstruction.
- Day/Week historical-runtime cache coverage now proves that independent
  immutable snapshot bindings with the same ordered captured-room visual bytes
  and transform authorize separately but reuse one fetch/decode and opaque room
  resource while furniture changes. Package metadata and member aliases cannot
  justify a room crossfade. T-541 still prevents authenticated historical-runtime
  availability and private byte delivery, so this is unit evidence rather than
  a real Grand Hall browser demonstration.

### Browser review performance boundary

Actual browser QA opened two distinct compatible generated test crops at the
50,000-point limit each (100,000 overlaid points). Canvas render timing was
55.1 ms average / 85.7 ms maximum on mobile and 58.45 ms average / 69.8 ms
maximum on desktop. This is an honest P2 result and not 60 fps. It is also not
raw Grand Hall E57 processing, a TransformArtifact/QA/activation result, or the
timeline's 500-object/Spark browser proof.

### Grand Hall preflight boundary

`docs/operations/grand-hall-universal-foundry-preflight-2026-08-09.md`
reproduces a pure assessment of the existing frozen ingest manifest and an
explicit host inventory. It did not open, copy, decode, transform, upload,
train on, or freshly hash Matterport, E57, panorama, XGRIDS/PortalCam, OBJ/GLB,
COLMAP, PLY, SOG, or SPZ media.

The preflight found 310 assets: 302 with a structural inspector, zero with a
deterministic local processing worker for the exact asset, zero production
ready, 309 requiring rights review, and one legally blocked. The result is
authority `none`, execution `not_authorized`, and blocked. Structural
inspection is not conversion or reconstruction evidence.

### Documentation truth correction

The Foundry architecture and runbook now remove earlier unsupported claims
that the current browser accepts capture drag/drop, that a general local
compute path exists, that **Dashboard → Runtime Foundry** is a verified
operator surface, or that the production browser resolves and hashes an active
release. Repository candidate/release contracts and migration 0049 remain in
the tree, but this continuation did not prove an applied release system or a
raw-capture-to-production-`RuntimePackage` bridge.

### Honest remaining gates

- Record purpose-specific rights for one exact Grand Hall processing bundle.
- Obtain a reviewed official XGRIDS/PortalCam export or SDK; XBIN remains an
  opaque stop.
- Add production execution/fencing and a reviewed deployment backend. The
  durable preview backend is Linux-only; Windows/macOS remain unavailable.
  Replace the bounded scan-replaying E57 seam with a Grand Hall-scale streaming
  worker.
- Obtain real, independently reviewed correspondences or metric controls for
  compatible derived roots, run the bounded registration proposal, review
  held-out residuals and source overlap, then implement and review fusion.
- Independently review scale, axes, origin, crop, completeness, privacy,
  TransformArtifact, QA, exact member identities, movable-object masks, and
  Scene Authority.
- Implement T-541's authenticated activation and version-bound private member
  delivery before timeline binding.
- Run real-rights-cleared desktop/mobile browser, interruption/reload, and
  500-object performance evidence. A room crossfade also needs a genuinely
  distinct second captured room revision.

T-486 and T-539 therefore remain in progress. No real Grand Hall Room Reality
Package, production activation, or two-package crossfade is claimed.
