# Session Log - 2026-05-05

## Fixed (T-357 right-click camera POV references)

- Added a right-click camera point-of-reference flow for the 3D planner floor/grid and placed furniture objects.
- Added an Add POV composer with name, sitting, standing, and custom eye-height controls.
- Saved POVs now use the existing camera bookmark system and appear in the Camera tool instead of creating a parallel camera model.
- Activating a saved reference shows a compact POV height switch so the user can jump between sitting and standing eye height from the same camera reference.
- Camera bookmark navigation now invalidates the demand-mode R3F loop when a bookmark request is queued, so camera jumps run reliably from UI state.
- Preserved right-drag orbit by only opening the composer on stationary right-click/right-button release.

## Verification (T-357)

- `pnpm --filter @omnitwin/web test -- camera-animation bookmark-store camera-reference-store`
- `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --workers=1`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web test` (78 files, 1617 tests)
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`
- Browser automation verified floor-grid POV creation and furniture-object POV creation from a placed round table.

## Notes (T-357)

- The production build still reports the existing large `three` and `spark` chunk warnings.
- The full web unit run still emits existing mocked-R3F/Three DOM warnings.
- No public copy, T-091A loader, fake assets, package rename, or persistence beyond the current planner session was added.

## Fixed (T-358 3D planner autosave validation)

- Fixed the recurring `Couldn't save — Validation failed` toast in the 3D planner.
- Root cause: grouped table chairs still emitted the legacy `banquet-chair` slug while the API save schema requires canonical UUID catalogue IDs.
- Added a second save-boundary fix so freshly created scene objects use `local-` IDs until the API returns real row IDs, preventing the batch adapter from treating new objects as updates.
- Added save adapter canonicalization so legacy slug state is normalized before posting.

## Verification (T-358)

- `pnpm --filter @omnitwin/web test -- editor-store editor-roundtrip table-group placement placement-store`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `git diff --check`
- `pnpm --filter @omnitwin/web test`
- `pnpm audit --audit-level=moderate`

## Notes (T-358)

- The production build still reports the existing large `three` and `spark` chunk warnings.
- The targeted web test run still emits the existing Three.js multiple-instance warning from mocked test imports.
- No public copy, T-091A loader, fake asset, package rename, or API schema change was made.

## Fixed (T-359 landing Planrise visual cleanup)

- Removed the repeated Grand Hall raster photo from the landing page's Planrise/workflow section.
- Replaced the awkward overlapping collage with a cleaner product-native planning visual: status card, centered 2D plan, code-native 3D-mode panel, handoff rail, and timeline.
- Added unit and responsive E2E guards that the `.planrise` section contains no `<img>` so the duplicated hall photo cannot quietly return.

## Verification (T-359)

- `pnpm --filter @omnitwin/web test -- LandingPage`
- `pnpm --filter @omnitwin/web e2e -- e2e/landing-mobile-planner.spec.ts --workers=1`
- Browser screenshot inspection at 1228px desktop for `.planrise-stage`; verified no Planrise images, mode card visible, and no horizontal overflow.
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`

## Notes (T-359)

- The production build still reports the existing large `three` and `spark` chunk warnings.
- No public claims, T-091A loader, fake splat/runtime asset, package rename, or planner behavior change was made.

## Fixed (T-360 landing embedded 2D planner refit)

- Moved the embedded landing 2D planner back into the hero product column so desktop users see the planning surface on load instead of a delayed below-fold module.
- Rebuilt the embedded preview as a fitted 2D command surface: room selector, layout selector, Select/Add/Rotate/Measure/Camera/Snap/Walls/Review rail, live tool status, compact inspector, and full-width stage with no central blank editor space.
- Added real preview-side snap behavior for drops and moves, a camera-point overlay for the Camera tool, and a wall-outline toggle without adding fake runtime assets or changing backend/runtime planner data.
- Kept the restored Trades Hall room photography and room-picker behavior intact.

## Verification (T-360)

- `pnpm --filter @omnitwin/web test -- LandingPage`
- `pnpm --filter @omnitwin/web e2e -- e2e/landing-mobile-planner.spec.ts --workers=1`
- Browser screenshot inspection at 2048px, 1228px, 768px tablet, and 390px phone; verified no central 2D planner blank space and no horizontal overflow.
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes (T-360)

- The production build still reports the existing large `three` and `spark` chunk warnings.
- The landing preview now exposes the core 3D-planner affordances as interactive preview controls, while the full persistent planner remains the `/plan` route.
- No public claims, T-091A loader, fake splat/runtime asset, package rename, or auth/upload/security behavior changed.

## Fixed (T-361 landing 2D planner delete control)

- Added an explicit Trash/Delete action to the landing 2D planner's main tool rail.
- Added the same delete affordance to the compact in-canvas floorplan toolbar and selected-item inspector so users do not have to discover keyboard Delete/Backspace.
- Deleting the selected object now moves selection to the next nearby furniture item instead of leaving the inspector stranded.
- Kept the existing keyboard Delete/Backspace support aligned with the visible control.

## Verification (T-361)

- `pnpm --filter @omnitwin/web test -- LandingPage`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web e2e -- e2e/landing-mobile-planner.spec.ts --workers=1`
- Browser screenshot inspection at 1440px desktop for the embedded planner; verified Delete appears in the tool rail, quick toolbar, and inspector without crowding the stage.
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes (T-361)

- The production build still reports the existing large `three` and `spark` chunk warnings.
- No runtime loader, public claims, backend save behavior, auth/upload/security logic, fake asset, or package rename changed.

## Fixed (T-362 Grand Hall window wall panel cleanup)

- Removed dark raised wainscot panel blocks from the procedural Grand Hall arched-window wall.
- Kept dark timber panel blocks on the opposite portrait wall only.
- Raised the placeholder arched-window sill above the lower-wall panelling so the windows no longer visually intersect with black squares.
- Added regression helpers/tests for window-wall panel exclusion and sill height.

## Verification (T-362)

- `pnpm --filter @omnitwin/web test -- GrandHallRoom`
- Browser screenshot inspection of `/plan/e2e-config-001` with mocked public-config API responses at 1680x900; verified the arched-window wall no longer has black square blockers and the opposite portrait wall still has dark panel detail.
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes (T-362)

- The targeted GrandHallRoom test still emits existing mocked-Three DOM warnings.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No T-091A loader, fake splat/runtime asset, public copy, auth/upload/security logic, or package rename changed.

## Fixed (T-363 ornamented wall disassemble continuity)

- Fixed the wall-click visual branch where the upgraded Grand Hall wall dressing snapped off immediately and exposed the older plain brick wall during disassembly.
- Shared the wall-build threshold and 5s click-animation duration between `BrickWall` and ornament visibility logic.
- Added click-locked wall opacity stepping to `SurfaceVisibilityGroup` so friezes, pilasters, windows, trim, and wall dressing remain on the same animation timeline as the base wall.
- Preserved camera-side auto-hide behavior; non-click wall fades still follow the existing wall opacity path.

## Verification (T-363)

- `pnpm --filter @omnitwin/web test -- SurfaceVisibilityGroup`
- `pnpm --filter @omnitwin/web test -- BrickWall`
- `pnpm --filter @omnitwin/web test -- GrandHallRoom`
- Browser screenshot inspection of `/plan/e2e-config-001` with mocked public-config API responses at 1680x900; clicked the window wall and verified the upgraded wall dressing remains visible during the first second of disassembly instead of snapping to a plain wall.
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `pnpm --filter @omnitwin/web e2e -- e2e/editor-interactions.spec.ts --workers=1`
- `pnpm audit --audit-level=moderate`

## Notes (T-363)

- The targeted GrandHallRoom test still emits existing mocked-Three DOM warnings.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No T-091A loader, fake splat/runtime asset, public copy, auth/upload/security logic, or package rename changed.

## Fixed (T-364 Grand Hall wall door assemblies)

- Replaced the mistaken camera-facing short-end dark square blocks with three clear timber door assemblies.
- Added door frames, double leaves, raised panels, top rails, and brass handles so the camera-facing wall reads as three doors from the 3D planner camera.
- Removed that wall's square raised-panel clutter that made it read as a row of odd dark blocks.
- Preserved the opposite short-end fireplace cue while replacing the wall in the screenshot.
- Added regression coverage for the three-door count and for keeping the old paired short-end block marker out of the procedural source.

## Verification (T-364)

- `pnpm --filter @omnitwin/web test -- GrandHallRoom`
- Browser screenshot inspection of `/plan/e2e-config-001` with mocked public-config API responses at 1680x900; verified the camera-facing short-end wall reads as three doors rather than square plaques.
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes (T-364)

- The targeted GrandHallRoom test still emits existing mocked-Three DOM warnings.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No T-091A loader, fake splat/runtime asset, public copy, auth/upload/security logic, or package rename changed.

## Fixed (T-365 Grand Hall fireplace orientation)

- Moved the procedural fireplace/portrait composition to the far short end of the Grand Hall placeholder scene.
- Moved the three-door short-end cue to the opposite end, preserving the door assembly detail from T-364.
- Suppressed short-end square raised-panel clutter on both ends so the fireplace and door cues read cleanly from the planning camera.
- Added regression coverage for the corrected end-wall orientation.

## Verification (T-365)

- `pnpm --filter @omnitwin/web test -- GrandHallRoom`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- Browser screenshot inspection of `/plan/e2e-t365-config` with mocked public-config API responses at 1680x900; verified the fireplace cue appears at the far short end in the default planning view.
- Canvas data-read sanity check on the rendered planner canvas.
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`

## Notes (T-365)

- The targeted GrandHallRoom test still emits existing mocked-Three DOM warnings.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No T-091A loader, fake splat/runtime asset, public copy, auth/upload/security logic, or package rename changed.

## Fixed (T-366 planner dialog keyboard lock)

- Added a camera keyboard-input guard so WASD/arrow panning ignores events from inputs, textareas, selects, content-editable regions, and dialog descendants.
- Clear any held pan key while editable UI is focused, preventing a stuck camera move if focus changes mid-keypress.
- Focus and select the Add POV name field when the dialog opens, so typing a chair POV name replaces the suggestion without moving the scene.
- Added CameraRig regression coverage for text fields, dialog descendants, content-editable regions, and ordinary scene targets.

## Verification (T-366)

- `pnpm --filter @omnitwin/web test -- CameraRig`
- Browser check of `/plan/e2e-t366-config3` with mocked public-config API responses at 1280x800: right-clicked a Banquet Chair, confirmed the Add POV dialog opened for "Banquet Chair viewpoint", held `A` in the name field for 650ms, confirmed the input value changed to `a`, and confirmed the canvas data URL was unchanged before/after.
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`

## Notes (T-366)

- The targeted CameraRig test still emits the existing "Multiple instances of Three.js" warning.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No public copy, T-091A loader, fake splat/runtime asset, auth/upload/security logic, or package rename changed.

## Fixed (T-367 Trades Hall banquet chair mesh)

- Replaced the old generic chair primitive made from flat red boxes with a more specific Trades Hall-style banquet chair placeholder.
- Added a red padded cushion with darker underside, dark rounded back insert, red rim/piping, and black tubular legs/frame to match the supplied chair reference more closely.
- Kept the same catalogue item, placement footprint, selection behavior, grouping behavior, and save semantics; this is a procedural visual upgrade only, not a real captured asset.
- Added visual-spec regression coverage for footprint compatibility, multi-material palette, placement ghost colour override, rounded back shape, and the red backrest rim cut-out.

## Verification (T-367)

- `pnpm --filter @omnitwin/web test -- ChairMesh`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- Browser screenshot inspection of `/plan/e2e-t367-chair` with mocked public-config API responses and multiple Banquet Chair objects; verified the chairs render with red cushions, dark rounded backs, red edging, and black frame/legs instead of the old flat red block primitive.
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes (T-367)

- The targeted ChairMesh test still emits the existing "Multiple instances of Three.js" warning.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No T-091A loader, real asset claim, public copy, backend save semantics, auth/upload/security logic, or package rename changed.

## Fixed (T-368 folding 6ft round table mesh)

- Replaced the old central-pedestal round-table primitive with a procedural folding plastic 6ft round table.
- Added a pale plastic circular top, subtle lighter top surface, grey rim, underside disc, dark perimeter ring, four splayed dark metal legs, diagonal folding braces, and black rubber feet.
- Kept the same catalogue item, dimensions, placement footprint, grouping behavior, collision assumptions, and save semantics.
- Added visual-spec regression coverage for perimeter legs/braces, material palette, placement ghost colour override, and angled cylinder transforms.

## Verification (T-368)

- `pnpm --filter @omnitwin/web test -- RoundTableMesh`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- Browser screenshot inspection of `/plan/e2e-t368-round-table-final` with mocked public-config API responses and multiple 6ft Round Table objects; verified pale circular tops, dark folding legs/braces, and no central pedestal base.
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes (T-368)

- The targeted RoundTableMesh test still emits the existing "Multiple instances of Three.js" warning.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No T-091A loader, real asset claim, public copy, backend save semantics, grouping/collision behavior, auth/upload/security logic, or package rename changed.

## Fixed (T-369 refined folding 6ft round table)

- Tightened the 6ft round table mesh against Blake's repeated folding-table reference after the first procedural pass did not read strongly enough.
- Brightened the molded white plastic top, thickened the grey skirt/bead, reduced the underside disc, moved legs to front-left/front-right folding-table positions, added small visual overhang, doubled the diagonal braces, and added a center support, hinge collars, and larger black rubber feet.
- Kept the same catalogue item, dimensions, placement footprint intent, grouping behavior, collision assumptions, and save semantics.
- Expanded visual-spec regression coverage for doubled braces, center support, hinge points, molded rim proportions, front-leg readability, and slight leg overhang.

## Verification (T-369)

- `pnpm --filter @omnitwin/web test -- RoundTableMesh`
- Browser screenshot inspection of `/plan/e2e-t369-round-table-midfield` with mocked public-config API responses; verified the table reads as a white folding round with dark angled legs/braces at normal planner distance.
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes (T-369)

- The targeted RoundTableMesh test still emits the existing "Multiple instances of Three.js" warning.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No T-091A loader, real asset claim, public copy, backend save semantics, grouping/collision behavior, auth/upload/security logic, or package rename changed.

## Fixed (T-370 anonymous planner refresh persistence)

- Added browser-local draft persistence for dirty public-preview 3D planner scene objects keyed by configuration ID.
- Restored anonymous edits after refresh even when the API still returns stale or empty objects.
- Kept claimed/private configurations out of local draft persistence, and clear the anonymous draft after a successful public save.
- Covered added, moved, rotated, and deleted furniture, table groups, cloth state, and object notes through the shared editor object model.

## Verification (T-370)

- `pnpm --filter @omnitwin/web test -- anonymous-planner-draft`
- `pnpm --filter @omnitwin/web test -- editor-store`
- `pnpm --filter @omnitwin/web typecheck`
- `pnpm --filter @omnitwin/web lint`
- Browser refresh check of `/plan/e2e-t370-anon-draft` with mocked public-config API responses: seeded a dirty anonymous draft in localStorage, refreshed the page while the API still returned an empty object list, and confirmed the table remained visible after reload.
- `pnpm --filter @omnitwin/web test`
- `pnpm --filter @omnitwin/web build`
- `pnpm audit --audit-level=moderate`
- `git diff --check`

## Notes (T-370)

- Full web tests still emit existing React/Three DOM warnings, including the known "Multiple instances of Three.js" warning.
- The production build still reports the existing large `three` and `spark` chunk warnings.
- No public copy, backend schema, auth/upload/security logic, T-091A loader, fake asset/runtime claim, or package rename changed.
