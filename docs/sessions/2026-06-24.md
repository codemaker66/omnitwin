# 2026-06-24

## T-470 Clerk DNS Records Visible And Live Matrix Pass

- User showed the Clerk Production Domains page with five unverified DNS records.
- Role: Deployer/Tester.
- Access posture:
  - Domain under configuration is known: `venviewer.com`.
  - DNS provider is Cloudflare: `eleanor.ns.cloudflare.com` and `gordon.ns.cloudflare.com`.
  - I do not have Cloudflare DNS edit credentials in this environment (`CLOUDFLARE_API_TOKEN`, `CF_API_TOKEN`, `CLOUDFLARE_ZONE_ID`, and equivalents are missing).
  - I did not directly modify DNS.

### DNS verification

- All five Clerk CNAME records now resolve correctly from Cloudflare authoritative nameservers:
  - `accounts.venviewer.com -> accounts.clerk.services`
  - `clerk.venviewer.com -> frontend-api.clerk.services`
  - `clk._domainkey.venviewer.com -> dkim1.18z6okdt0io3.clerk.services`
  - `clk2._domainkey.venviewer.com -> dkim2.18z6okdt0io3.clerk.services`
  - `clkmail.venviewer.com -> mail.18z6okdt0io3.clerk.services`
- `Resolve-DnsName` via `1.1.1.1` also returns the same five targets.
- Missing records: none from this runner.
- Wrong-value records: none from this runner.
- Clerk JS endpoint:
  - `https://clerk.venviewer.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`
  - Result: HTTP 200, `application/javascript`.

### Browser verification

- In-app browser `/login`:
  - URL: `https://venviewer.com/login?t470-dns-visible=<timestamp>`.
  - Title: `Sign in - Venviewer`.
  - Clerk form mounted.
  - Visible Clerk title: `Sign in to Venviewer`.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
  - `Loading secure sign-in`: absent.
  - Console errors/warnings: none in the checked log set.

### Production-live matrix

- Command:
  - `$env:E2E_BASE_URL='https://venviewer.com'; $env:E2E_START_SERVER='false'; pnpm --filter @omnitwin/web exec playwright test e2e/production-live-route-pass.spec.ts --project=chromium --reporter=list`
- Result:
  - 5 passed.
- Fresh report:
  - `artifacts/t469-production-live-route-pass-2026-06-21/report.json`.
  - 13 route samples.
  - 13 accessibility samples.
  - 13 control samples.
  - 0 control issues.
  - 0 accessibility issues.
  - Max idle p95: `16.8ms`.
  - Max interaction p95: `16.8ms`.

### Status

- T-470 is no longer DNS-blocked.
- Updated T-470 from `blocked` to `in-progress`.
- Not marked fully done because I have not clicked Clerk's dashboard `Verify configuration` and have not verified a real signed-in production staff/admin session through to protected dashboard/API data.

## T-470 Rerun After User-Reported Authoritative DNS Fix

- User asked: `Rerun T-470 now that nslookup clerk.venviewer.com eleanor.ns.cloudflare.com and gordon.ns.cloudflare.com both return a real Clerk DNS record`.
- Role: Deployer/Tester.
- Definition of Done:
  - Verify authoritative DNS from both named Cloudflare nameservers.
  - Verify Clerk JS reachability.
  - Verify live `/login` and protected dashboard behavior in the in-app browser.
  - Rerun the production-live Playwright matrix.
  - Record the exact blocker if the live auth path still fails.

### DNS

- `Resolve-DnsName clerk.venviewer.com`: DNS name does not exist.
- `Resolve-DnsName clerk.venviewer.com -Server 1.1.1.1`: DNS name does not exist.
- `Resolve-DnsName clerk.venviewer.com -Server 8.8.8.8`: DNS name does not exist.
- `nslookup clerk.venviewer.com eleanor.ns.cloudflare.com`: non-existent domain.
- `nslookup clerk.venviewer.com gordon.ns.cloudflare.com`: non-existent domain.
- `nslookup -type=CNAME clerk.venviewer.com eleanor.ns.cloudflare.com`: non-existent domain.
- `nslookup -type=A clerk.venviewer.com eleanor.ns.cloudflare.com`: non-existent domain.
- `nslookup -type=CNAME clerk.venviewer.com gordon.ns.cloudflare.com`: non-existent domain.
- `nslookup -type=A clerk.venviewer.com gordon.ns.cloudflare.com`: non-existent domain.
- Cloudflare DoH CNAME check: `Status: 3` / NXDOMAIN.
- Google DoH CNAME check: `Status: 3` / NXDOMAIN.
- Clerk JS endpoint:
  - `https://clerk.venviewer.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`
  - Result: `No such host is known`.

### Production deployment/env

- `npx vercel inspect https://venviewer.com`:
  - Deployment: `dpl_Awzjc7AEg4KisZNsWMLou8HGLrur`.
  - Target: production.
  - Status: Ready.
  - Aliases include `https://venviewer.com`, `https://www.venviewer.com`, and `https://omnitwin-web.vercel.app`.
- `npx vercel pull --yes --environment=production` completed for `codemaker66s-projects/omnitwin-web`.
- Prefix-only env inspection:
  - `VITE_CLERK_PUBLISHABLE_KEY prefix=pk_live`.
  - No secret value recorded.

### Browser verification

- In-app browser `/login`:
  - URL: `https://venviewer.com/login?t470-rerun=<timestamp>`.
  - Title: `Sign in - Venviewer`.
  - Venviewer-branded shell rendered.
  - No framework error overlay.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
  - Stuck state: `Loading secure sign-in`.
- In-app browser protected dashboard:
  - URL: `https://venviewer.com/dashboard?view=reviews&t470-rerun=<timestamp>`.
  - Title: `Venviewer — Trades Hall Glasgow`.
  - Screen remains on `Opening your workspace`.
  - No framework error overlay.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
  - Console error: `Clerk: Failed to load Clerk JS ... https://clerk.venviewer.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`.

### Production-live matrix

- Command:
  - `$env:E2E_BASE_URL='https://venviewer.com'; $env:E2E_START_SERVER='false'; pnpm --filter @omnitwin/web exec playwright test e2e/production-live-route-pass.spec.ts --project=chromium --reporter=list`
- Result:
  - 4 passed.
  - 1 failed.
- Passed:
  - Public room pages stay live, accessible, controlled, and within frame budget.
  - Proposal share controls work against the live frontend bundle without production mutation.
  - Supplier portal acknowledgement controls work against the live frontend bundle without production mutation.
  - Live unavailable share links stay polished, accessible, and smooth on mobile.
- Failed:
  - `live protected dashboard drawer routes fail closed to auth without console errors`.
  - Expected `/dashboard?view=reviews...` to redirect to `/login`.
  - Actual URL stayed `https://venviewer.com/dashboard?view=reviews&live-route-pass=<timestamp>`.
  - Failure screenshot: `packages/web/test-results/production-live-route-pass-65712-auth-without-console-errors-chromium/test-failed-1.png`.
- Fresh report:
  - `artifacts/t469-production-live-route-pass-2026-06-21/report.json`.
  - 8 completed successful route samples.
  - 8 accessibility samples.
  - 8 control samples.
  - 0 control issues.
  - 0 accessibility issues.
  - Max idle p95: `16.8ms`.
  - Max interaction p95: `16.8ms`.

### Status

- T-470 remains blocked.
- This is not a Venviewer branding regression: `OMNITWIN` and Clerk `Development mode` are gone.
- This is still a Clerk Production Frontend API DNS/configuration blocker: the live app cannot load Clerk JS while `clerk.venviewer.com` is NXDOMAIN from this runner.
- Do not hide the loading state with CSS or claim auth fixed until Clerk JS resolves and the protected-dashboard matrix passes.

## T-470 Authoritative Nameserver Rerun

- User asked: `rerun T-470 after clerk.venviewer.com resolves from eleanor.ns.cloudflare.com and gordon.ns.cloudflare.com`.
- Role: Deployer/Tester.
- Definition of Done:
  - Confirm `clerk.venviewer.com` resolves from both named authoritative Cloudflare nameservers.
  - If true, verify Clerk JS, browser auth, and full live matrix.
  - If false, record the exact blocker and do not claim the matrix can pass.

### DNS

- `Resolve-DnsName clerk.venviewer.com`: DNS name does not exist.
- `Resolve-DnsName clerk.venviewer.com -Server 1.1.1.1`: DNS name does not exist.
- `Resolve-DnsName clerk.venviewer.com -Server 8.8.8.8`: DNS name does not exist.
- `nslookup clerk.venviewer.com eleanor.ns.cloudflare.com`: non-existent domain.
- `nslookup clerk.venviewer.com gordon.ns.cloudflare.com`: non-existent domain.
- Cloudflare DoH CNAME: `Status: 3` / NXDOMAIN.
- Google DoH CNAME: `Status: 3` / NXDOMAIN.
- Clerk JS endpoint:
  - `https://clerk.venviewer.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`
  - Result: no such host.

### Production deployment/env

- `npx vercel inspect https://venviewer.com`:
  - Deployment: `dpl_Awzjc7AEg4KisZNsWMLou8HGLrur`
  - Target: production.
  - Status: Ready.
  - Aliases include `https://venviewer.com`, `https://www.venviewer.com`, and `https://omnitwin-web.vercel.app`.
- `npx vercel pull --yes --environment=production` completed for `codemaker66s-projects/omnitwin-web`.
- Prefix-only env inspection:
  - `VITE_CLERK_PUBLISHABLE_KEY prefix=pk_live`
  - No secret value recorded.

### Browser verification

- In-app browser `/login`:
  - URL: `https://venviewer.com/login?t470-authoritative-rerun=2026-06-24`
  - Title: `Sign in - Venviewer`.
  - Meaningful Venviewer shell rendered.
  - No framework error overlay.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
  - Stuck state: `Loading secure sign-in`.
  - Console error: `Clerk: Failed to load Clerk JS ... https://clerk.venviewer.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`.
- In-app browser protected dashboard:
  - URL: `https://venviewer.com/dashboard?view=reviews&t470-authoritative-rerun=2026-06-24`
  - Title: `Venviewer — Trades Hall Glasgow`.
  - Screen remains on `Opening your workspace`.
  - No framework error overlay.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
  - Same Clerk JS failure.

### Matrix decision

- Full live matrix was not rerun because the user-specified prerequisite is still false: both `eleanor.ns.cloudflare.com` and `gordon.ns.cloudflare.com` return NXDOMAIN for `clerk.venviewer.com`.
- Last full matrix remains the current state:
  - 4 passed, 1 failed.
  - Protected dashboard auth fails because Clerk JS cannot load.

### Status

- T-470 remains blocked on authoritative Cloudflare DNS for the Clerk Production Frontend API host.

## T-470 Rerun After Reported Authoritative DNS Resolution

- User asked: `rerun T-470 after Cloudflare authoritative DNS resolves clerk.venviewer.com`.
- Role: Deployer/Tester.
- Definition of Done:
  - Confirm whether authoritative DNS actually resolves.
  - If it does, rerun live auth/dashboard checks and the full live matrix.
  - If it does not, record the exact blocker and avoid claiming a pass.

### DNS preflight

- `Resolve-DnsName clerk.venviewer.com`: DNS name does not exist.
- `Resolve-DnsName clerk.venviewer.com -Server 1.1.1.1`: DNS name does not exist.
- `Resolve-DnsName clerk.venviewer.com -Server 8.8.8.8`: DNS name does not exist.
- `nslookup clerk.venviewer.com eleanor.ns.cloudflare.com`: non-existent domain.
- `nslookup clerk.venviewer.com gordon.ns.cloudflare.com`: non-existent domain.
- Cloudflare DoH CNAME: `Status: 3` / NXDOMAIN.
- Google DoH CNAME: `Status: 3` / NXDOMAIN.
- Clerk JS endpoint:
  - `https://clerk.venviewer.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`
  - Result: no such host.

### Production deployment/env

- `npx vercel inspect https://venviewer.com`:
  - Deployment: `dpl_Awzjc7AEg4KisZNsWMLou8HGLrur`
  - Target: production.
  - Status: Ready.
  - Aliases include `https://venviewer.com`, `https://www.venviewer.com`, and `https://omnitwin-web.vercel.app`.
- `npx vercel pull --yes --environment=production` completed for `codemaker66s-projects/omnitwin-web`.
- Prefix-only env inspection:
  - `VITE_CLERK_PUBLISHABLE_KEY prefix=pk_live`
  - No secret value recorded.

### Browser/auth rerun

- In-app browser `/login`:
  - URL: `https://venviewer.com/login?t470-rerun=2026-06-24`
  - Title: `Sign in - Venviewer`.
  - Meaningful Venviewer shell rendered.
  - No framework error overlay.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
  - Clerk form remains at `Loading secure sign-in`.
- In-app browser protected dashboard:
  - URL: `https://venviewer.com/dashboard?view=reviews&t470-rerun=2026-06-24`
  - Title: `Venviewer — Trades Hall Glasgow`.
  - Screen remains on `Opening your workspace`.
  - No framework error overlay.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
- Direct Playwright network probe:
  - `/login` and `/dashboard?view=reviews` each recorded six failed requests to `https://clerk.venviewer.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`.
  - Browser error: `net::ERR_NAME_NOT_RESOLVED`.
  - Page error: `Clerk: Failed to load Clerk JS ... code="failed_to_load_clerk_js"`.
  - Local bundled Clerk app chunk `https://venviewer.com/assets/clerk-B461Hjpw.js` loaded with HTTP `200`; the remote Clerk frontend API script did not.

### Matrix decision

- The full live Playwright matrix was not rerun after this preflight because the required authoritative DNS condition is still false.
- The last full live matrix remains the valid state:
  - 4 passed, 1 failed.
  - Protected dashboard auth fails because Clerk JS cannot load from `clerk.venviewer.com`.

### Status

- T-470 remains blocked on the exact Clerk-provided Production Frontend API DNS record becoming visible from the authoritative Cloudflare nameservers.

## T-470 Live Auth / Dashboard Continuation

- User asked to continue after the prior T-470 DNS/auth blocker.
- Role: Deployer/Tester.
- Definition of Done:
  - Recheck authoritative DNS for `clerk.venviewer.com`.
  - Verify live `/login` and protected `/dashboard` behavior.
  - Rerun the live production route matrix when appropriate.
  - Update the task ledger with the exact current blocker.

### DNS

- `Resolve-DnsName clerk.venviewer.com`: DNS name does not exist.
- `Resolve-DnsName clerk.venviewer.com -Server 1.1.1.1`: DNS name does not exist.
- `Resolve-DnsName clerk.venviewer.com -Server 8.8.8.8`: DNS name does not exist.
- `nslookup clerk.venviewer.com eleanor.ns.cloudflare.com`: non-existent domain.
- `nslookup clerk.venviewer.com gordon.ns.cloudflare.com`: non-existent domain.
- Cloudflare DoH CNAME: `Status: 3` / NXDOMAIN.
- Google DoH CNAME: `Status: 3` / NXDOMAIN.
- Clerk JS endpoint:
  - `https://clerk.venviewer.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`
  - Result: no such host.

### Production deployment/env

- `npx vercel inspect https://venviewer.com`:
  - Deployment: `dpl_Awzjc7AEg4KisZNsWMLou8HGLrur`
  - Target: production.
  - Status: Ready.
  - Aliases include `https://venviewer.com`, `https://www.venviewer.com`, and `https://omnitwin-web.vercel.app`.
- `npx vercel pull --yes --environment=production` completed for `codemaker66s-projects/omnitwin-web`.
- Prefix-only env inspection:
  - `VITE_CLERK_PUBLISHABLE_KEY prefix=pk_live`
  - No secret value recorded.

### Browser verification

- In-app browser `/login`:
  - URL: `https://venviewer.com/login?t470-dom=2026-06-24`
  - Title: `Sign in - Venviewer`
  - Meaningful Venviewer shell rendered.
  - No framework error overlay.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
  - Clerk sign-in UI did not render because Clerk JS failed to load.
  - Console error: `Clerk: Failed to load Clerk JS ... code="failed_to_load_clerk_js"` from `assets/clerk-B461Hjpw.js`.
- In-app browser protected dashboard:
  - URL: `https://venviewer.com/dashboard?view=reviews&t470-dom=2026-06-24`
  - Title: `Venviewer — Trades Hall Glasgow`
  - Screen remains on `Opening your workspace`.
  - No framework error overlay.
  - `OMNITWIN`: absent.
  - `Development mode`: absent.
  - Same Clerk JS load failure.
- Browser screenshot capture timed out in the in-app browser, so persisted visual evidence comes from Playwright.

### Live Playwright matrix

- Ran:
  - `E2E_START_SERVER=false E2E_BASE_URL=https://venviewer.com FRAME_BUDGET_SAMPLE_MS=1200 pnpm --dir packages/web e2e -- e2e/production-live-route-pass.spec.ts --reporter=list --workers=1`
- Result:
  - 4 passed, 1 failed.
  - Passed:
    - public room pages
    - proposal share controls
    - supplier portal controls
    - unavailable proposal/supplier mobile states
  - Failed:
    - protected dashboard auth routes
- Failure:
  - `/dashboard?view=reviews&live-route-pass=...` stayed on the protected dashboard URL instead of redirecting to `/login` within the 5s assertion window.
- Failure screenshot:
  - `packages/web/test-results/production-live-route-pass-65712-auth-without-console-errors-chromium/test-failed-1.png`
- Failure context:
  - `packages/web/test-results/production-live-route-pass-65712-auth-without-console-errors-chromium/error-context.md`
- Report:
  - `artifacts/t469-production-live-route-pass-2026-06-21/report.json`
  - 8 completed route samples.
  - 8 accessibility samples, 0 problems.
  - 8 visible-control samples, 0 problems.
  - Max idle p95: `16.8ms`.
  - Max interaction p95: `16.8ms`.

### Status

- T-470 is not blocked by Vercel Production env anymore; `pk_live` is confirmed.
- T-470 is not blocked by deployment aliasing; `venviewer.com` points to the expected Ready production deployment.
- T-470 remains blocked by missing/unpropagated Clerk Production Frontend API DNS for `clerk.venviewer.com`.
- Required next external action:
  - In the authoritative Cloudflare DNS zone for `venviewer.com`, add/repair the exact Clerk-provided DNS record for `clerk.venviewer.com`, then wait for `eleanor.ns.cloudflare.com` and `gordon.ns.cloudflare.com` to answer with that record.

## T-471 SaaS Account Architecture And Bootstrap

- User asked to implement SaaS account architecture: separate Venviewer platform admin from venue-scoped workspace owner/admin, add tests, and create a safe bootstrap flow for the first production admin account.
- Role: Auth/platform implementer.
- Definition of Done:
  - Platform authority is separate from venue/customer workspace authority.
  - Global Venviewer-only routes fail closed unless `platformRole="admin"`.
  - Venue admins remain venue-scoped and do not become Venviewer-company admins.
  - The frontend resolves database-authoritative platform role after Clerk sign-in.
  - The first production platform admin can be bootstrapped safely without a public endpoint.
  - Regression tests prove the separation.

### Implementation

- Added shared platform role contract in `packages/types/src/user.ts`:
  - `PlatformRoleSchema`
  - `PlatformRole`
  - `PLATFORM_ROLES`
  - `UserSchema.platformRole`
  - `CreateUserSchema.platformRole`
- Added migration `packages/api/drizzle/0043_platform_admin_scope.sql`:
  - `users.platform_role varchar(20) not null default 'none'`
  - intentionally does not backfill existing `users.role='admin'` rows, so legacy venue admins are not silently promoted to Venviewer platform admins
  - check constraint for `none | operator | admin`
  - platform-role index
- Added database schema field `users.platformRole` in `packages/api/src/db/schema.ts`.
- Hardened auth middleware in `packages/api/src/middleware/auth.ts`:
  - JWT/mock token shape includes `platformRole` and `name`
  - real Clerk-resolved user returns DB-authoritative `platformRole`
  - newly created invited/domain users default to `platformRole='none'`
  - `isPlatformAdmin()`
  - `authorizePlatformAdmin()`
- Added authenticated `/auth/me` in `packages/api/src/routes/auth.ts` and registered it in `packages/api/src/index.ts`.
- Converted Venviewer-global API surfaces to platform-admin authorization:
  - `/admin/*`
  - onboarding/operator deployment routes
  - `/admin/assets/*`
  - venue create/delete
  - cross-venue/global branches in commercial, proposal, quote, enquiry, analytics, upload, lifecycle, and runtime/admin routes
- Preserved venue-scoped admin behavior where correct:
  - venue admins can manage their own venue
  - staff/hallkeeper role checks remain venue-bound
  - platform admin can cross venues only through explicit `platformRole='admin'`
- Added safe bootstrap CLI:
  - script: `packages/api/src/scripts/bootstrap-platform-admin.ts`
  - package command: `pnpm --filter @omnitwin/api platform-admin:bootstrap`
  - requires `VENVIEWER_PLATFORM_ADMIN_BOOTSTRAP_SECRET` to be set to a 32+ character secret
  - requires the caller to pass the exact matching `--secret`
  - creates or updates a DB user by email with `role='admin'`, `platformRole='admin'`, and `venueId=null`
  - does not expose a public HTTP route
- Updated web auth model:
  - `AuthUser.platformRole`
  - `/auth/me` API client in `packages/web/src/api/auth.ts`
  - `ClerkAuthBridge` seeds a temporary Clerk metadata fallback, then overwrites it with DB-authoritative `/auth/me`
  - E2E seed user carries `platformRole`
- Updated dashboard gating:
  - platform admin surfaces (`admin`, `onboarding`) require `platformRole='admin'`
  - venue admin/staff still reach venue staff surfaces
  - no-venue platform admin dashboard label becomes `Venviewer Platform`

### Verification

- `pnpm --filter @omnitwin/types test -- src/__tests__/user.test.ts`: 1 file, 66 tests passed.
- `pnpm --filter @omnitwin/types typecheck`: passed.
- `pnpm --filter @omnitwin/types build`: passed.
- `pnpm --filter @omnitwin/api typecheck`: passed.
- `pnpm --filter @omnitwin/web typecheck`: passed.
- Focused API regressions:
  - `auth.test.ts`
  - `invitation-auth.test.ts`
  - `onboarding-routes.test.ts`
  - `platform-admin-schema.test.ts`
  - `platform-admin-bootstrap.test.ts`
  - `utils/query.test.ts`
  - `upload-policy.test.ts`
  - Result: 7 files, 93 tests passed.
- Focused web regressions:
  - `auth-store.test.ts`
  - `dashboard.test.ts`
  - `role-aware-redirect.test.tsx`
  - `auth-components.test.ts`
  - `EditorPage.venue-routing.test.tsx`
  - Result: 5 files, 96 tests passed.
- Full API test suite:
  - `pnpm --filter @omnitwin/api test`
  - 92 files, 1985 tests passed.
- Full web test suite:
  - `pnpm --filter @omnitwin/web test`
  - 196 files, 2562 tests passed.
  - Test output included an expected mocked `401` network log from a Truth Mode request; suite passed.
- Workspace typecheck:
  - `pnpm typecheck`
  - passed.
- Lint:
  - `pnpm --filter @omnitwin/api lint`
  - `pnpm --filter @omnitwin/web lint`
  - `pnpm --filter @omnitwin/types lint`
  - all passed.

### First production platform admin bootstrap

- Required environment:
  - `DATABASE_URL` for the production API database.
  - `VENVIEWER_PLATFORM_ADMIN_BOOTSTRAP_SECRET` set to a random 32+ character value in the runtime where the command is executed.
- Command shape:
  - `pnpm --filter @omnitwin/api platform-admin:bootstrap -- --email <your-production-clerk-email> --name "Blake Faraway" --secret <same-bootstrap-secret>`
- Result:
  - If a DB user already exists for that email, it is upgraded to `platformRole='admin'`.
  - If no DB user exists for that email, it is pre-provisioned with `clerkId=null`; the first Clerk sign-in with the same verified email links the Clerk ID.
- Security note:
  - Rotate or remove `VENVIEWER_PLATFORM_ADMIN_BOOTSTRAP_SECRET` after bootstrapping.
  - Do not use workspace/venue owner accounts as Venviewer platform admins unless they are meant to administer the Venviewer company platform itself.
