# Session log — 2026-08-31

## T-562 visible-first fixed-camera checkpoint

- Re-audited the existing source-only SOG, SPZ, PLY, and vendor-editor
  diagnostics. The dated 2026-08-30 browser bundle contains only one capture
  per representation and therefore does not prove the specified cold plus
  three-warm schedule or fresh-process isolation.
- Added one digest-bound inspection-camera profile,
  `source-pose-19890-interior-v1`, shared by the browser and native lanes. Its
  target is explicitly inspection-only rather than recovered source optics;
  the profile grants no room, transform, architectural, runtime, staging, or
  production authority.
- Added a sequential browser orchestrator that launches SOG, SPZ, and
  structural-only PLY in three distinct Playwright/Chromium operating-system
  processes. Each lane must publish one cold and three warm 1600×900 captures,
  prove warm HTTP-cache hits, retain 120 warm-up plus 600 timed frames, bind
  exact source/camera/screenshot receipts, and refuse a dirty or changing
  worktree.
- Repaired the first-party native XGRIDS lane to load the canonical GH_1 LCC2
  explicitly through `ILCCSceneManager.LoadScene(string, Action)` after event
  subscription. It locks all 60 files / 214,350,601 bytes, excludes `env.sog`
  for browser-frontier parity, and labels the result as vendor-renderer parity
  over the same canonical SOG payload family rather than an independent
  reconstruction.
- Added a crash-recoverable per-user feature-toggle lease that adds only
  `com.venviewer.native_capture`, uses the vendor's public encryption utility,
  refuses stock-ID changes, and restores exact original bytes, pre-read
  timestamps, and attributes only after all LCCEditor processes have exited.
  The live toggle remained SHA-256
  `8ff16cac30f3f49a71be9a06d486b1bb9b682e0ccf1c5c35869a251d98313531`
  with no lease residue after self-test.
- Native verification passed against the exact canonical package and the
  890-file / 1,402,172,819-byte disposable-editor runtime closure. Module
  SHA-256 is
  `b3cb9d5d52d31a3b24feb84197af4b26eb82dc698d223d6a9108c0003aba8bb9`;
  camera-profile SHA-256 is
  `9eca9b6582b7301ec1c059b1a5be699e5a4983773afecb2beea46c2668305922`.
- Browser typecheck, scoped lint, five focused files / 16 tests, and the
  three-lane Playwright collection check passed. The real native and browser
  captures remain pending until this harness checkpoint is committed and the
  worktree is clean.

### T-562 browser residency correction

- Correction to the historical cold/warm HTTP-cache claim above: the real
  `2026-08-31-visible-first-hardware-v2` attempt completed its cold SOG capture,
  then the first reload increased bound source requests from 11 to 22. The
  attempt is rejected and incomplete; it has no valid resident sequence, SPZ
  or PLY lane, or final receipt.
- Each representation receives one cold source navigation/load and four total captures from one live fixture runtime. The following three resident captures perform no navigation, source fetch, decode, or scene attachment. They measure visual and frame-time stability of the long-lived decoded runtime; they do not claim HTTP-cache reload performance.
- The corrected labels are `cold-load-1`, `resident-capture-1`,
  `resident-capture-2`, and `resident-capture-3`. Each record binds
  `VENVIEWER_BROWSER_SOURCE_RESIDENCY_V1` with exact process scope
  `one_representation_one_cold_load_plus_three_resident_captures`; the completed
  bundle uses `venviewer.grand-hall.visible-first-browser-bakeoff.v3`.
- `2026-08-31-visible-first-hardware-v3` is the fresh successor target. The
  rejected hardware-v2 partial directory must not be treated as completed
  evidence or silently continued.

## GSFixer diagnostic evaluation

- Preserved the genuine official GSFixer-base Grand Hall result as
  `GENERATED_CINEMATIC`. It is a single-frame broad repaint and cannot become
  captured truth or the captured master.
- Generated a receipt-last authority-none review pack at
  `F:\venviewer-provider-cache\gsfix3d\evaluations\grand-hall-gsfixer-v2-forbidden-architecture-evidence-20260831T0800Z`.
  It binds the exact source and candidate and records 589,793 changed pixels
  outside the empty generated region. The semantic state remains deliberately
  `not_evaluated`; no automated or human architectural clearance is claimed.
- No source capture, SOG, SPZ, LCC2, mesh, panorama, E57, production system,
  staging system, vendor binary, stock feature toggle, or captured-truth layer
  was modified. No upload, push, deployment, publication, or paid provider run
  occurred.

## T-562 hardware-v3 and symmetric radiance comparison

- Completed the clean local hardware-v3 browser successor at producer commit
  `8a71c1b7d6e7abd07cae4479e642905dd8ba16a3`. SOG, SPZ, and structural-only
  PLY ran sequentially in distinct Playwright/browser processes on Chrome
  151 / ANGLE D3D11 / NVIDIA GeForce RTX 4090 / WebGL2 with no context loss.
- Each representation performed one source load and four total captures from
  one live runtime. SOG and SPZ stayed at 11 served source requests after the
  cold load; PLY stayed at one. Each capture advanced exactly 720 controlled
  frames and every lane's four PNGs remained byte-identical.
- Preserved the complete v3 bundle under
  `docs/evidence/grand-hall-lineage/2026-08-31-visible-first-hardware-v3`.
  SOG PNG SHA-256 is `72f4c376...d3aae`, SPZ is `02740825...13d20`, and PLY
  is `1678e636...38f60`. These match their 2026-08-30 pixels; hardware-v3 adds
  process, source-residency, and hardware proof rather than new appearance.
- Added and locally committed the strict create-only/check-only symmetric
  comparator as `09034f8f`. It pins the canonical camera SHA-256, verifies the
  full v3 receipt and exact inventory, cross-binds camera/renderer/Spark/source
  state, validates PLY as structural-only, rejects resident drift, and uses
  identity-checked receipt-last atomic publication.
- Generated and independently checked
  `2026-08-31-visible-first-hardware-v3-comparison`. Side-by-side SHA-256 is
  `8dced4d88ec65a884f63b02e6646ce797a1d90825c0c26167292cff869516add`;
  ×8 difference SHA-256 is
  `68e65209e737748f0fa5ac608d018ddfaefd01ef34745510dfaf0078916a9a7d`;
  canonical receipt SHA-256 is
  `b82b428b309368cf9a9ab129a683e2296bc9edd6b677ba5eba64e2925c2e1776`.
- The symmetric RGB-byte metrics are MAE `1.8918046296296296`, RMSE
  `2.5220771495234136`, PSNR `40.095636261322205 dB`, and maximum channel
  delta `34`. They cannot select a source-fidelity or beauty winner because
  neither input is independent ground truth.
- Codex inspected the side-by-side and amplified difference images. In this
  fixed view, both show the empty Grand Hall with a continuous timber floor
  and no facade, neighbouring room, or false dark central floor. This is not
  whole-room architectural acceptance; `winner` remains null and
  `visualAcceptance` remains `not_reviewed`.
- Comparator verification passed 11 focused tests, strict TypeScript, full
  package lint, package build, real-v3 in-memory regeneration, output write,
  and independent check. A broader package-suite probe still hits the existing
  Windows/pnpm junction rejection for the separately pinned LPIPS AlexNet
  weight in `grand-hall-protected-region-metrics`; it is unrelated to this
  comparator and was not weakened.
- No source, staging, deployment, production, push, upload, paid provider run,
  or runtime admission was performed.

## T-562 native exact-target v5 rejection

- Hardened the native module to v1.2.5 with a first-party RGB24 readback of the
  exact retained 1600×900 vendor render target. The route proves target
  assignment, Unity liveness and native instance identity, active-target
  binding/restoration, readable no-mipmap RGB24 transfer, vendor/first-party
  texture non-aliasing, cooperative stop/timeout cleanup, and a pixel gate
  before PNG encoding or publication.
- Full build and verification passed against the canonical 60-file package and
  the locked 890-file disposable-editor closure. The module SHA-256 is
  `7066c7ac4a2fe9865a41cf2a036b976e7c512a8fc8d30c9c5ba36945f6ddb8a2`.
- Ran one bounded native attempt. It proved 1,785 readiness frames over
  15.0053208 seconds, Ultra/render-all state, exact 1600×900 target assignment,
  a three-frame vendor callback window, first-party readback, and exact
  feature-toggle restoration. The readback was pure black: zero non-black
  pixels, one RGB value, zero channel range, and RGB24 SHA-256
  `2c4bd5e75b50567dd5a3cb4111a7df207ce8b453d73c916a0ee00c5c88129e5a`.
- The raster gate correctly rejected the attempt before PNG encoding or
  publication. This narrows the defect: the locked XGRIDS custom LCC renderer
  does not draw its visible frame into `Camera.targetTexture`; the PNG and
  first-party transfer path are not the remaining problem.
- Preserved a compact hash-bound rejection receipt under
  `docs/evidence/grand-hall-lineage/2026-08-31-native-lcc-exact-target-v5-rejection`.
  The next bounded route is XGRIDS' public
  `LCCCore.SnapFrameCaptureFeature.FrameRT`, refreshed only through a reversible
  exact-camera projection cycle. No source, vendor binary, production system,
  staging system, push, upload, deployment, publication, or paid provider run
  occurred.

## T-563 raw-to-processed XGRIDS/LCC pose lineage

- Added a strict authority-none adapter that stable-reads and binds the raw
  42,850-row XGRIDS `poses.csv`, the exact processed BIG inventory, and all nine
  processed pose/report sidecar pairs without persisting private absolute
  source paths.
- Proved that all nine processed packages share the same 21,417-pose sidecar
  and report bytes. Every processed timestamp pairs to a strictly increasing
  raw row; raw-index increments are 1 for five pairs, 2 for 21,409 pairs, and 3
  for two pairs. Absolute timestamp delta has median 22,552,044 ns, p95
  47,226,050 ns, and maximum 51,429,034 ns.
- Evaluated all 24 quaternion-component permutations using normalized,
  sign-invariant geodesic error. `wxyz` is the uniquely best component-order
  candidate, with a 28.520149415-degree mean-angle margin over `xwzy`. This is
  a tuple-order candidate only, not an accepted optical-camera orientation.
- Retained the raw/processed tuple pair corresponding to processed pose 19890.
  A predeclared 80/20 Horn similarity diagnostic reports scale
  `0.993092104965`; held-out RMSE is `0.576024118` and held-out p95 is
  `1.247584192` in unaccepted source-coordinate units. These residuals forbid
  use as a metric transform.
- Preserved the receipt at
  `docs/operations/grand-hall-xgrids-lcc-pose-lineage-authority-none-v1.json`.
  Bundle digest is
  `sha256:11d5540cf2a22cc8e3c3c2386cd5236cf1d115b2276b0eddc1a58ede2f5f2aec`;
  repository receipt SHA-256 is
  `de6f1b248e2f6d741ec990364c47838a1457f79a5c75ae632b794b1d57fad2b9`.
- Focused tests, strict typecheck, affected lint, package build, exact real
  regeneration, path-redaction checks, and diff check passed. Tuple semantics,
  body-to-camera extrinsic, pose direction, handedness, axes, intrinsics, FOV,
  metric units, E57 registration, and room scope remain unaccepted. No source
  mutation, reconstruction, provider run, browser, upload, staging, deployment,
  publication, or production-trust action occurred.

## T-562 native v14 success and native-inclusive comparison

- Replaced the rejected black-target routes with an owned URP
  `SingleCameraRequest` against XGRIDS' exact retained 1600×900 visible surface.
  The route requests and observes Gamma `R8G8B8A8_UNorm`, disables sRGB target
  conversion, suppresses and restores the vendor self/avatar spawn-marker
  renderers, preserves the exact camera/render/scene-dirty state, and directly
  reads the resulting display-code raster without a blit, copy, manual submit,
  `Camera.Render`, renderer mutation, or vendor-binary modification.
- Rebuilt and independently verified module 1.7.0 against the locked 890-file
  vendor closure. Build receipt SHA-256 is
  `b3fb90a93e0e8f3da81301557d0c706998d0cedd230ba3921739b039c7ebb08a`;
  module SHA-256 is
  `2b7de5c1d6475f7c34ad4ee8c2bbf4a96ec2c454d5b4e094410cbdc7ee954e19`;
  plugin SHA-256 is
  `453c74eab820f2b70db9101b777b41af9171e077a00e79b7a0781c25262ac55b`.
- The bounded v14 operator run completed successfully against the exact
  60-file / 214,350,601-byte canonical GH_1 LCC2 package. It observed 1,783
  readiness frames over 15.0061873 seconds and produced three byte-identical,
  non-degenerate capture attempts. Native receipt SHA-256 is
  `a97006c8facd90b8e4e8d4914acc72ea63ffbb967754ac9aaca4132c99369f90`;
  operator receipt SHA-256 is
  `a37fa98ee31abbd14a96a91462e571c2aed2b4b6e8b51d4918cda39efe7e314e`.
- Preserved the 1600×900 PNG8 at
  `docs/evidence/grand-hall-lineage/2026-08-31-native-lcc-fixed-camera-v14`.
  Its SHA-256 is
  `c08e5f2074792e852635b3ae6b48d1cdc114c440efd6f604f31f9ed1827c13e7`.
  Independent decoding proves it is the exact vertically flipped mapping of
  all 4,320,000 lower-left-origin raw RGB24 bytes. A direct PNG chunk parser
  also proves every PNG16 sample is exactly the corresponding PNG8 value
  multiplied by 257; it adds no captured precision. The compact evidence
  receipt SHA-256 is
  `83c2575cae99ccc9a30dc0b091437e0c60671c41210ce1d7370f12ba642534a6`.
- The feature-toggle lease restored exact bytes and metadata at original
  SHA-256
  `8ff16cac30f3f49a71be9a06d486b1bb9b682e0ccf1c5c35869a251d98313531`
  after every editor process exited. The uniquely bound 67,735-byte Player.log
  matched the exact clean-shutdown profile: one declared startup DBuffer
  error, one declared Windows Media Foundation warning, zero unexpected error
  lines or exception diagnostics, and one terminal Input System shutdown
  marker at EOF.
- Added a strict native-inclusive comparison of SOG, SPZ, and native LCC, with
  PLY retained as structural-only and excluded from radiance metrics. The
  preserved receipt SHA-256 is
  `68889dd7d73b6c3e7525501f9f0984507c83223bc0dc81d6bf93084b8b4a96f4`.
  SOG/native measures MAE 3.1683013889 bytes, RMSE 4.8384189720, PSNR
  34.4367341606 dB and maximum delta 97; SPZ/native measures MAE
  3.5498884259, RMSE 5.1773407660, PSNR 33.8486685960 dB and maximum delta
  98. The existing SOG/SPZ values remain MAE 1.8918046296, RMSE 2.5220771495,
  PSNR 40.0956362613 dB and maximum delta 34.
- Two independent full-resolution agent audits verified every image binding,
  the exact side-by-side panel composition, and all three difference formulas.
  They found no marker or watermark overlay, false dark central floor,
  neighbouring room, facade, generated fill, native-only aperture, crop, flip,
  or evident geometric shift in this fixed view. Zero-shift luminance
  correlation is 0.9960 for SOG/native and 0.9956 for SPZ/native and
  outperformed every tested ±4-pixel shift. Existing windows, doors, floor,
  wall panelling and ceiling features appear in all three source-derived lanes.
- These are agent diagnostic observations, not qualified human or whole-room
  acceptance. Native is vendor-renderer parity over the same canonical SOG
  payload family, not an independent reconstruction, and no lane is ground
  truth for the synthetic inspection camera. Winner remains null. A difficult
  oblique, qualified human scope review, metric authority, captured-master
  selection, runtime admission, staging, deployment and production trust all
  remain pending. No source, production or staging mutation, push, upload,
  deployment, publication, paid provider run, or generative enhancement
  occurred.
