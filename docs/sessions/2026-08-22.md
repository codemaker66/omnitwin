# Session log — 2026-08-22

## T-540 · Exact capture-only Grand Hall runtime and intake

The owner supplied a newer XGRIDS export set in an operator-controlled local drop, confirmed Venviewer's authority to use the XGRIDS and Matterport data, and corrected the product direction: the current scope is the individual Trades Hall Grand Hall, and its architecture must be 100% source-derived. Invented windows, doorways, a dark central floor, neighbouring-room layouts, procedural reconstruction, and generative repair are not acceptable. The previously created exterior facade remains a separate venue-presentation asset rather than part of the room runtime.

Work is isolated in `C:\Users\blake\omnitwin2-grand-hall-exact-runtime` on `codex/grand-hall-exact-runtime`; the dirty primary worktree was not reset or overwritten.

## Canonical source decision

The supplied `scans_BIG_MODEL_TH_GH_1/lcc2-result/Grand_Hall.lcc2` source was inspected against the pinned receipt:

- hierarchy SHA-256 `927a92699de222e99d2684ca2567a35ab1e523a036461e6e01236b7b77b7f659`;
- frontier receipt `sha256:8e7514e75aa19345dda1955f2cee3f9369339c553c2711c084cd04be4c9c1352`;
- 11 ordered depth-5 authoritative leaf SOG members;
- 6,019,684 Gaussians and 106,479,738 compressed bytes; and
- `env.sog`, all ancestor LODs, SPZ, OBJ, and PLY excluded from visible architecture.

All supplied export families were inventoried. `_GH_1` through `_GH_3` have byte-identical LCC2/SOG contents, with `_GH_2` and `_GH_3` differing only by added OBJ/PLY mesh exports. `_GH_4` through `_GH_6` are the byte-identical SPZ family with the same optional-mesh pattern; `_GH_7` through `_GH_9` are the separate LCC/mesh exports. This is why `_GH_1` is the minimal canonical carrier of the selected SOG family and not an arbitrary omission of a different captured room.

The exact contract is recorded in `docs/operations/grand-hall-captured-runtime-contract-2026-08-21.md`. No capture-session identity or capture date was independently established. Asset registration therefore keeps `captureSessionId: null`; the owner's usage authority is not treated as capture-session provenance or documentary evidence embedded in the package.

## Runtime implementation present in the worktree

- The Grand Hall scene is capture-only. Eleven verified/decoded splats reveal atomically only after the full decoded count matches; a partial or mismatched set is disposed and leaves architecture blank.
- The room uses the source-derived axis conversion, scale, centring, and camera/framing contract. Procedural/generated floors, walls, doors, windows, architectural ink, furniture, planning overlays, and facade imagery have no architectural authority.
- The verified-room policy also removes the generic minimap, furniture markers, route conflicts, capacity/circulation estimates, layout quantities, simulations, and editing controls from the Grand Hall cockpit. Direct blueprint entry, WebGL-to-blueprint fallback, keyboard history, notes, save/share/review actions, guest-flow replay/demo fixtures, generic phase metrics, clipping, x-ray, procedural fog, and canvas colour grading are blocked. A source-only notice replaces those tools until reviewed room-local alignment and collision evidence exist; pending or unavailable room identity uses separate neutral copy.
- Exact-runtime status is keyed to the current room and immutable package: it remains pending until all eleven members are attached, reports terminal verification/decode failure, clears when the Canvas detaches, restarts pending before same-package retry, resets on room/package change, and rejects stale callbacks. Locked layer chrome shows only captured-source or neutral identity status, never Mesh/Hybrid alternatives.
- One ten-minute absolute browser-load clock now spans authentication, all private preview fetch/body/hash work, all eleven Spark decodes, exact-count validation, and atomic attachment. Timeout aborts transport, disposes invisible partial/decoded meshes, leaves architecture blank, and fails only the current keyed lifecycle. The layer is remounted by the canonical `spaceId` + `venueId` + package identity, so same-package room/venue changes also dispose and detach the old resource; retry or identity change cancels the old clock and rejects stale timeout/completion callbacks.
- Authenticated discovery and private preview share the canonical eleven-member predicate. A package with changed order, receipt, composition, storage identity, evidence state, or nonvisual asset references is rejected.
- The exact Grand Hall namespace is rejected by generic asset/package registration and the legacy runtime-assets route, closing alternate registration and delivery paths.

## Same-server intake implementation present in the worktree

The platform-admin-only capability under `/admin/assets/grand-hall-frontier-intake` is disabled by default and has three stages:

1. preflight fully reads/hashes all existing private objects and returns only API-relative capabilities for missing canonical members;
2. member PUT hashes the binary request, performs a fixed-key conditional create through a distinct put-only R2 principal, and fully reads it back through the read-only serving principal; and
3. commit reopens and rehashes all 11 objects before the first database write, then one advisory-locked transaction exactly creates/reuses 11 AssetVersions and one immutable ordered `internal_ready` RuntimePackage.

The target binding covers the selected target, API origin, actual Docker-stamped build Git SHA, intentional intake deployment SHA, database connection identity, private storage identity/prefix, frontier receipt, and binding secret. Intake-enabled configuration fails closed when the build stamp is missing, `dev`, malformed, or differs from the intentional SHA; preflight, upload capabilities, commit, and the secret-free receipt carry the actual build SHA. Storage identifiers and credentials are not returned to the operator.

The CLI supports exactly one of `--apply` or the staging-only `--rehearse-conditional-put` mode and obtains its existing platform-admin token only from `RUNTIME_PROFILE_INTAKE_ADMIN_TOKEN`. Before any network request it anchors Git inspection to the executing script's real repository rather than the ambient shell checkout, proves the supplied commit exists and is exact clean `HEAD` including untracked files, re-derives the canonical frontier, and reads/hash-verifies all 11 apply members or both rehearsal members into bounded buffers. Every HTTP fetch and response body has an absolute abort deadline. The rehearsal performs exact member-0 create/reuse, rejects a wiped corrupt-copy of still-missing member 1, verifies state, and cannot commit/register.

Implementation and adversarial coverage are under:

- `packages/api/src/lib/grand-hall-frontier-contract.ts`;
- `packages/api/src/routes/grand-hall-runtime-intake.ts`;
- `packages/api/src/services/grand-hall-runtime-intake.ts`;
- `packages/api/src/scripts/intake-grand-hall-big-model-frontier.ts`;
- the Grand Hall API route/service/registration/R2/CLI and bypass regression suites; and
- the existing exact Grand Hall web renderer, receipt, camera, and runtime-access suites.

The operator procedure is `docs/operations/grand-hall-frontier-intake-runbook.md`. It requires a real staging conditional-PUT/idempotency rehearsal before production; unit mocks are not accepted as proof of the selected storage service.

## External actions deliberately not performed

No target environment was supplied or authorized for mutation. Consequently this session did not:

- upload any of the 11 members;
- write an AssetVersion, RuntimePackage, or external audit entry;
- deploy or activate the code/package in any environment;
- create, rotate, or change R2 credentials;
- invoke any generative-modelling, image, or video provider; or
- run authenticated browser/WebGL QA.

No generative API keys are required for the architectural layer. Browser automation remains outside current authority until Blake explicitly permits it.

## Remaining gates and requested inputs

T-540 remains `in-progress`. To continue safely, the operator must provide or select:

- either the discovered production-linked clean HTTPS API-origin candidate or a dedicated staging API target, plus an explicit stable intake target ID (the linked Vercel project ID is not an intake-target authorization);
- the exact reviewed Git commit deployed through the image's `BUILD_GIT_SHA` stamp and selected by matching `RUNTIME_PROFILE_INTAKE_DEPLOYED_GIT_SHA` configuration;
- an existing platform-admin bearer token, or an authorized operator who will run the intake;
- complete dedicated private-runtime read credentials and a distinct same-bucket put-only R2 key/secret if those principals are not already provisioned; and
- explicit permission for the final authenticated browser/WebGL visual QA.

Secret-safe local discovery found no selected `RUNTIME_PROFILE_INTAKE_TARGET_ID`, operator token, dedicated `RUNTIME_PROFILE_R2_*` read principal, or `RUNTIME_PROFILE_INTAKE_R2_*` write principal. Older generic R2 variables are not accepted as proof of the required split credentials. The locally linked Railway configuration does not identify a remote API project or environment, so no API deployment target can be inferred from the repository.

Before production, deploy the reviewed code to a dedicated staging target, prove conditional create with an exact duplicate member PUT, prove an idempotent full rerun, disable/remove the intake write capability, and retain the safe evidence named by the runbook. Production upload/registration may occur only after the production target is separately and explicitly selected.

## Verification status

Local implementation verification is complete:

- direct offline LCC2 frontier inspection passed: the manifest, all 11 ordered depth-5 members, all embedded Gaussian counts, the 106,479,738-byte total, the 6,019,684-Gaussian total, exclusion of `env.sog`, and receipt `sha256:8e7514e75aa19345dda1955f2cee3f9369339c553c2711c084cd04be4c9c1352` were re-derived from the supplied files with no network access or source writes;
- `pnpm --filter @omnitwin/api typecheck` passed;
- `pnpm --filter @omnitwin/api lint` passed;
- `pnpm --filter @omnitwin/api build` passed;
- `pnpm --filter @omnitwin/api test` passed 160 files and 2,870 tests, with 5 intentional skips and the package's integration test excluded by its standard script;
- the focused intake CLI, route, environment, Git/build binding, local-admission, receipt, rehearsal, registration, R2 contract/deadline, service, and runtime-access matrix passed 188/188 tests across 9 files;
- `pnpm --filter @omnitwin/web typecheck` passed both application and E2E TypeScript projects;
- `pnpm --filter @omnitwin/web lint` passed;
- `pnpm --filter @omnitwin/web build` passed with the already-linked Vercel production web project's genuine browser-public configuration;
- `pnpm --filter @omnitwin/web test` passed 329 files and 4,117 tests after the final source-only entry, fallback, visual-route, absolute-deadline, and keyed lifecycle gates landed;
- the integrated exact-renderer, editor-entry, WebGL-boundary, visual-route, lifecycle, and cockpit regression bundle passed 168/168 tests;
- the final exact-load deadline and canonical remount bundle passed 105/105 tests across transport, decoder/attachment, keyed lifecycle, Canvas failure, captured-source admission, and package resolution; fake-timer cases cover stalled auth token, fetch, response body, digest, Spark initialization, same-package room/venue remount, and stale retry completion; and
- `git diff --check` passed, with only the repository's Windows LF-to-CRLF notices.

The isolated worktree contains no active environment file. Its first production-build attempt correctly rejected the linked primary workspace's development-only Clerk key. Read-only Vercel production-env pulls supplied the genuine `VITE_CLERK_PUBLISHABLE_KEY` and clean remote `VITE_API_URL` to one-use local build files; the final build after the absolute-deadline and keyed-remount closure passed. Every one-use file was deleted immediately, no values were printed or retained, and no deployment or remote configuration change occurred. This verifies the production bundle only and does not select or authorize that API origin as the intake target.

These results do not claim deployment, live R2 conditional-create behaviour, database mutation, browser rendering, WebGL acceptance, operational geometry, or public evidence. T-540 remains `in-progress` while environment activation and permitted visual QA remain outstanding.
