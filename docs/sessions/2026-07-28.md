# 2026-07-28

## Foundry moonshot `/goal` · external evidence refresh (authority-none)

Re-ran the moonshot's external-research round against live primary sources and
reconciled the results with the repository's existing investigation state,
which is substantially ahead of the prompt's assumed baseline (root
investigation cutoff 2026-07-14; licence matrix maintained through ≥2026-07-18;
T-507 frozen; T-514 `contract_valid_runtime_blocked`; T-508 active; 2026-07-25
Brush-splat discovery recorded).

- Verified environment and claims from disk before writing anything: branch
  `feature/diary-p0-slice-3`; `state/training_runs.jsonl` still 0 bytes (no
  recorded real training run); `packages/web` pins `@sparkjsdev/spark` 2.0.0
  and `three` 0.180.0; `configs/training/config_b.yaml` +
  `venviewer_training/gsplat-v1.5.3.source-lock.json` present; ten PortalCam
  raw projects on `F:\gaussian splat -- xgrids\model` (each with
  `control_points.csv`, `poses.csv`, `gnss.csv`, `log/data.ulg`,
  `log/lixel.zip`, `model/{octree,hierarchy}.bin` and one `.xbin`);
  `F:\E57\cloud_0.e57` (20,518,437,888 bytes, 2026-03-01) with the
  `colmap_v2` workspace and twenty Brush checkpoints. No raw file was
  modified; no hashes of multi-GB originals were recomputed this session.
- Ran five parallel research agents (XGRIDS/Lixel; open geometry licences;
  Matterport/E57/capture hardware; splat runtime/formats/renderers;
  generative repair + semantics + provenance), primary sources only, every
  claim classed fact/inference/hypothesis/could-not-verify with URLs.
- Wrote the dossier annex, synthesis note and machine-readable register:
  `docs/reports/evidence/foundry-external-dossiers-2026-07-28.md`,
  `docs/reports/omnitwin-foundry-evidence-refresh-2026-07-28.md`,
  `docs/reports/omnitwin-foundry-evidence-refresh-2026-07-28.json`. The
  root investigation header now points at the refresh; a dated note was
  added under `docs/state/tasks.md` → Latest task notes.
- Net verdict movement: none of the standing verdicts weakened. PortalCam raw
  access re-verified blocked (recovery remains partial; the official
  SDK/export/rights letter is still the decisive test). New evidence
  strengthens the build side: Spark 2.0 `.RAD` LoD streaming + published
  device budgets (we already ship Spark 2.0.0), Cesium 3D Tiles splats and
  Streamed SOG, KHR splat extension at Release Candidate, a quantified API
  delivery ceiling (2048px cube faces → nominal ~8192×4096 / ~33.6 MP)
  that does not apply to the E57's 4096px faces (nominal ~16384×8192 /
  ~134.2 MP). Owned photography remains a separately controlled
  quality/provenance option, not the only route past the API ceiling; a
  licence-clean enhancement toolbox (NVIDIA Fixer commercial; SeedVR2,
  FLUX.2-klein-4B, LaMa,
  SAM 2.1 + Wan2.1-VACE, TRELLIS.2), permissive capture-rig SLAM (GLIM MIT,
  OKVIS2-X BSD-3), Apache feed-forward geometry checkpoints (MapAnything,
  Depth Anything 3), SpatialLM excluded (CC-BY-NC SceneScript encoder), and
  C2PA/ISO-22144 context confirming no 3D provenance standard exists.
- Flagged for the single counsel bundle: Matterport 2026-03-01 AI-training
  prohibition (also governs the discovered Grand Hall Brush checkpoints) and
  the XGRIDS LCC/LCC2 whitepaper "no competing AI training" clause (gates any
  training that consumes LCC-derived inputs).
- Explicit non-actions: no rights evaluation, activation, custody change,
  training, worker execution, paid or remote compute, provider contact,
  `.xbin`/`lixel.zip` parsing or decryption, raw mutation, signing,
  registration, promotion or publication. T-508 remains the exact next task,
  unchanged.

## T-508 continuation · activation/custody slice (authority-none)

Executed the audits' own "next admissible step" rather than touching any SQL:
the joint nine-artifact review, the 0059-collision disposition, and the first
static slice of the focused-0058-evidence gap. 0058 stays disabled; 0059 (any
ordinal) stays prohibited; every standing gate preserved.

- Scope was mapped from primary sources first (tasks.md T-508 row L273, the
  three 2026-07-14 activation audits, the nine V1 spec artifacts, and the
  current code state). Notable code-state facts: the 0058 substrate is fully
  orphaned from TypeScript (no Drizzle tables, services or routes reference
  any `fdv1_` object); `migration-tail-readiness` parity stops at 0057; and
  ordinal 0059 is consumed by the committed planner `0059_action_log`.
- An independent, non-author, read-only reviewer session produced the joint
  freeze-readiness audit, saved verbatim at
  `docs/reports/omnitwin-foundry-activation-v1-nine-artifact-joint-audit-2026-07-28.md`.
  All sixteen recomputed hashes match the 2026-07-14/15 records exactly.
  Verdicts: joint freeze **GO conditional** on one byte-preserving custody
  commit (P1-1 — eight of the nine artifacts, the parent, and the hash record
  itself are untracked); SQL-repair **authorship** GO after that commit;
  spec-complete 0058 NO-GO confirmed; 0059/enable NO-GO confirmed and now
  additionally unsatisfiable as written (P2-1). The six gap closures trace
  coherently across the artifact set; the pg_policy/pg_attribute catalog
  carry-over is closed at design level with the vacuous case explicit.
- The 0059 collision disposition is recorded as a proposal only:
  `docs/proposals/omnitwin-foundry-activation-enable-migration-rekeying-2026-07-28.md`
  (re-key the enable migration to content identity + intervening-chain hash
  binding; renumbering committed history ruled inadmissible). Parent contract
  untouched; decision owner Blake at the third-amendment audit.
- Focused 0058 evidence, static slice:
  `packages/api/src/__tests__/foundry-derivative-activation-0058-static-evidence.test.ts`
  freezes 0053–0058 bytes in CI, index-pins journal 51–57, proves no
  enable-shaped migration exists at any index, asserts the single
  disabled-sentinel epoch INSERT and two-read-helper grant surface, records
  the zero-policy/zero-RLS state as an asserted absence, and runs the
  63-byte identifier/truncation-collision sweep across the pg_class,
  pg_proc, pg_trigger, pg_constraint and per-table pg_attribute namespaces
  (24 tables / 53 functions / 16 triggers / 14 indexes / 163 constraints /
  578 columns; max identifier 61 bytes; zero collisions).
- Verification: 7/7 assertions pass against byte-identical copies under a
  minimal Node 22 strip-types harness with the workspace's real zod (the
  Windows-installed pnpm junctions are unreadable over the Linux mount, so
  in-workspace vitest cannot launch here). Outstanding Windows receipts:
  `pnpm --filter @omnitwin/api test -- src/__tests__/foundry-derivative-activation-0058-static-evidence.test.ts`,
  `... typecheck`, `... lint`.
- Machine-readable slice record:
  `docs/reports/omnitwin-foundry-t508-activation-slice-evidence-2026-07-28.json`.
- Remaining T-508 activation work, unchanged in kind: the P1-1 custody
  commit (Blake-gated), then repaired-SQL/privilege-delta authorship with the
  expanded `expectedCatalog` instance and semantic verifier, live semantic /
  Drizzle-parity / concurrency / adversarial evidence on pinned networkless
  PostgreSQL 16.14, and the fresh independent final audit against one exact
  migration hash.
