# 19 September 2026

## T-628 — public Gaussian splat hold

Blake asked for Gaussian splats to be inaccessible on the live site, with
“Work in progress”, and allowed the panorama/mesh replacement tour with the same
clear qualification. This is a temporary publication decision, not a reversal
of the native renderer migration. Local development remains available.

Work is isolated from production source `da944981` on
`codex/splats-work-in-progress`. T-601 returned the serial release lane and
confirmed no current hold or competing deployment. Shared dirty source and the
pending offline-tool/documentation consolidation are preserved.

The production policy closes room, showcase, Living Hall and internal capture
routes, removes the homepage embedded splat, prevents planner registry/staged
sources, disables capture controls and guards the common renderer. The planner
retains its mesh fallback. Same-origin `/splats/*` and the shipped native fixture
redirect to the WIP page before static/proxy access. The panorama/mesh tour stays
available with a persistent WIP strip inside its fullscreen root. Its current
live data comes from `https://twin.venviewer.com`; the missing default local
manifest and old homepage-disabled flag were stale availability evidence.

The scope is site access. Separate public storage URLs and previously downloaded
or cached files are not revoked by this change; source assets are retained.

Verification so far: 221 focused tests passed, web source/E2E types passed,
independent source review found no actionable issues. Production build, complete
web lint and actual rendered local/live checks remain in progress.
