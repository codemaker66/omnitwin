# 19 September 2026

## T-628 — public Gaussian splat hold

Blake asked for Gaussian splats to be inaccessible on the live site, with
“Work in progress”, and allowed the panorama/mesh replacement tour with the same
clear qualification. This is a temporary publication decision, not a reversal
of the native renderer migration. Local development remains available.

Work is isolated from production source `da944981` on
`codex/splats-work-in-progress`. T-601 returned the serial release lane and
confirmed no current hold or competing deployment. Shared dirty source and the
pending offline-tool/documentation consolidation are preserved.

T-601 confirmed PR 33 should follow the existing master deployment path only.
`release/r1` stays at `da944981`; its later owner must incorporate current master
and preserve this public hold before promoting that branch. Its separate CI run
is not a prerequisite for this bounded web change.

The production policy closes room, showcase, Living Hall and internal capture
routes, removes the homepage embedded splat, prevents planner registry/staged
sources, disables capture controls and guards the common renderer. The planner
retains its mesh fallback. Same-origin `/splats/*` and the shipped native fixture
redirect to the WIP page before static/proxy access. The panorama/mesh tour stays
available with a persistent WIP strip inside its fullscreen root. Its current
live data comes from `https://twin.venviewer.com`; the missing default local
manifest and old homepage-disabled flag were stale availability evidence.

The scope is site access. Separate public storage URLs and previously downloaded
or cached files are not revoked by this change; source assets are retained.

Verification before release: 233 focused tests, web source/E2E types, complete
web lint and the production build passed. Independent source review found no
actionable issues. The actual local production bundle showed all seven tested
closed routes with no canvas and no Gaussian asset requests. The real tour data
rendered panoramas and dollhouse mode, retaining its notice in fullscreen and on
narrow screens. A fullscreen-to-narrow intrinsic grid-width issue was reproduced
and fixed in `45c66f84`.

Hosted run `35438386241/1` passed the full workspace tests (including dedicated
PostgreSQL regressions), typecheck, lint, build, image/security/tooling checks and
CPU browser shards 1–3. Shard 4 exposed a short-landscape dossier/quick-action
overlap caused by the new notice and twelve intended visual-baseline changes.
The failed evidence is retained. A content-height budget corrects the overlap
without relaxing its assertion; all 18 responsive browser cases pass. Inspection
of the updated references also found a stale Windows disclaimer baseline: its
current copy was already changed by `63257478a` on 7 September, so those pixels
are a baseline reconciliation, not a new product-copy change. The GPU
gate uses the existing explicitly invoked local WSL/RTX 4090 worker; no unattended
receiver or rented compute is being introduced. Publication and live planner
verification remain pending. Production API CORS correctly excludes the local
preview origin, so that integration must be exercised on the deployed site.

All 24 refreshed visual references were inspected. Comparisons with snapshot
updates disabled passed 12/12 on Linux and 12/12 on Windows. The corrected
production build also passed. Hosted preview navigation independently confirmed
both a tracked `/splats/reception/0_0.sog` file and the native fixture redirect to
the WIP page before asset access.
