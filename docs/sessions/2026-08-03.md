# 2026-08-03

## T-539 — immutable historical runtime timeline

- Started from clean `feature/event-layout-timeline` commit `86430a15` in dedicated integration/API/UI/verification worktrees; the dirty user checkout is read-only.
- Mapped the user-directed goal to T-539 and deployed independent Data/API, Runtime/UI, and Performance/Provenance reviewers before implementation.
- Evidence gate at session start: `state/asset_versions.json` has no admitted assets, T-091 remains open, and the discovered Grand Hall Brush output is not an eligible runtime because provenance, review, and rights clearance are missing. Infrastructure work can proceed, but completion still requires two genuinely distinct reviewed Trades Hall RuntimePackages.
- Planned contract: append-only snapshot-to-runtime binding in the freeze transaction; private authenticated resolution; digest verification before paint; cancellation-safe bounded playback; honest historical-runtime-unavailable state.
- Shared contract and migration baseline committed as `53b0f88c`: migration `0063` adds an immutable available/unavailable decision to each new frozen phase-layout row; the response contract carries exact package/member/transform/provenance identities and rejects binding/snapshot drift. Focused types/API schema tests, strict typechecks, and types/API/web lint passed.
- Independent review found PostgreSQL nullable `CHECK` predicates could accept malformed JSON through SQL `NULL`. Commit `21b2ca29` wraps binding coherence, JSON identity, and package identity predicates in `IS TRUE`; focused tests and API lint pass. The same review identified package/QA/transform composite-scope and trusted-admission blockers that the Data/API slice must close before an available binding is accepted.
- Created a dedicated PostgreSQL 16 cluster `venviewer_timeline` on guarded local port `54329`, plus disposable database `omnitwin_timeline_0060_historical_runtime`; no configured project/production database was used. `pnpm --filter @omnitwin/api db:migrate` applied the complete journal through `0063` successfully.
- Ran the pre-existing real PostgreSQL freeze/timeline rehearsal against that fresh database. Sixteen of seventeen cases passed, including real create/select/freeze/query/reload, isolation, concurrency, immutability, and failure paths. The sole failure was the expected stale ledger assertion whose four-entry tail ended at `0062`; the actual tail correctly included `0063` at timestamp `1785758400000`. This is migration/startup evidence, not runtime-asset admission evidence.

### Integrated implementation

- `7a814cd3` lands the core Data/API slice: normalized append-only presentation admissions and rights evidence; exact package, member, QA, transform, and Scene Authority identities; immutable freeze-time binding; same-transaction admission resolution; timeline revalidation; and an authenticated venue/space/binding/member route with lineage, SHA-256, byte-length, tenant, venue, and room checks. Current real rows remain honestly unavailable with `provenance_incomplete`.
- `2df82523` lands the core Runtime/UI slice: verified private member fetches, exact digest checks before decode, a two-entry active/adjacent cache, serialized fetch/decode, abort generations, bounded byte/splat/member budgets, direct reviewed transforms, compatible crossfades, geometry/room hard switches, idle LRU thumbnails, synchronized viewer/minimap/figures/metrics, and explicit unavailable/error states.
- `dedee1e3` bounds API delivery to four concurrent transfers with upstream abort propagation and a 60-second deadline, and closes cache/header/schema issues found by API↔UI cross-review.
- `dc438d59` closes the independently reproduced adjacent-decode TTL race. Selected fetch starts before adjacent fetch, an active decode cannot be evicted by TTL, and adversarial cache tests pass 17/17.
- `0ddfefab` closes two final browser defects. Timeline shortcuts now use one stable listener with layout-synchronized current callbacks. Spark shutdown quiesces updates, LOD, pager work and timers; requires two idle turns; synchronously disposes Spark plus material and geometry after the final idle observation; bounds retirement; and trips a page-lifetime quarantine/unavailable latch instead of force-killing a busy worker or admitting more renderers.

### Verification evidence

- Fresh isolated PostgreSQL 16 final rehearsal used local cluster `venviewer_timeline` on port `54329`, WebSocket bridge `54331`, and guarded disposable database `omnitwin_timeline_0060_root_final`. Migrations `0000` through `0063` applied cleanly and the PostgreSQL-backed phase-layout contract suite passed 18/18. Its two available snapshots both bind the same 36-byte `synthetic-grand-hall.sog` admission whose rights basis is `synthetic_contract_only`; they are contract fixtures, not reviewed Trades Hall proof. A deliberately misnamed disposable database was safely refused by the test guard.
- API full suite: 157 files passed, 1 skipped; 2,761 tests passed, 23 skipped. Focused private-delivery/binding tests, API typecheck, lint, and build also pass.
- Final web full suite after renderer-deadline hardening at `e5ac5a6a`: 298/298 files and 3,499/3,499 tests pass. Web application+E2E TypeScript checks and full `src/`+`e2e/` ESLint pass. Development-mode Vite build passes with 3,293 modules; no fake production credential was introduced.
- Final combined Chromium run passes 5/5: the full three-test Day/Week timeline spec plus the two Spark fixture tests, including one real Reception Room SOG teardown. The shortcut-heavy happy path also passes 5/5 serial repeats after the stable-listener fix.
- The checked-in real local SOG fixture passes 20/20 across ten repeats, including 10/10 teardown cases. An independent five-cycle probe against the 10,279,160-byte Reception Room SOG observed renderer/material/geometry disposal 5/5/5, 10/10 renderer workers terminated, one expected bounded pooled loader worker retained, idle queues/messages at every disposal, and zero page/console errors. This is Spark lifecycle evidence only; it is not Trades Hall historical-runtime evidence.
- The 500-object Chromium budget remains within the existing threshold: idle p95 16.7 ms, max 16.8 ms, zero sustained misses; morph p95 16.8 ms, max 33.2 ms, one allowed sustained miss.
- Follow-up `e5ac5a6a` closes the lifecycle review's deadline P2: retirement now uses monotonic `performance.now()` and caps the last wait to the exact remaining interval. The new 23 ms/10 ms regression proves waits of `[10, 10, 3]`; independent review reports P0/P1/P2 none, and the real-SOG Chromium fixture passes 2/2 after the change. Host scheduler lateness can still delay a callback, but the loop no longer adds its own 250 ms overshoot. Constructor-failure/StrictMode handoff remains reviewed without a direct React regression.
- A fresh read-only audit of the configured Neon database used a read-only transaction and rollback. It is still at migration `0061`, so the `0063` rights/admission tables and runtime-binding columns do not exist there. It has one legacy/unverified Reception package, 16 unverified asset rows, and zero QA records, transform artifacts, Scene Authority evidence, phase snapshots, or reviewed historical runtime packages. No migration or write was attempted; applying `0062`/`0063` to a shared target remains outside this no-deploy session.

### Visual fidelity ledger

Reference: `C:\Users\blake\Downloads\09be6fcd-7894-43f4-af4c-47298b181ad0.png`.

Local git-ignored synthetic contract captures (not runtime-asset proof):

- `artifacts/event-layout-runtime/timeline-desktop-synthetic.png` — SHA-256 `AAF16F87DFC3CDD78DD8494BAA92DEAA1E51B4F800426BB50A7C70CF19952AC5`
- `artifacts/event-layout-runtime/timeline-mobile-synthetic.png` — SHA-256 `BD5C6E7DB47DE955AE8508C9C80554D0C1302C9D004AD5054AD79D4BC95D842C`

| Area | Concept target | Verified implementation state | Health |
|---|---|---|---|
| Viewer/timeline hierarchy | Large room viewer with the time control directly beneath it. | The central planner viewer, phase strip, ruler, filmstrip, and metrics read as one coordinated surface. | Good |
| Day/Week navigation | Swap temporal scope without losing the selected moment. | Day/Week controls, date window, active phase, slider, viewer, minimap, figures, and metrics share one selection source. | Good |
| Event progression | Scroll milestone-to-milestone and see the arrangement unfold. | Click, scrub, keyboard, playback, reverse movement, retarget, reload, and back/forward paths are covered; furniture morphs and runtime transitions use the same frame. | Good |
| Filmstrip previews | Preview each moment inside the slider. | The moving window mounts at most seven previews; raster work is proof-keyed and idle-queued in a separately bounded 24-entry LRU, and never invents a runtime image. | Good |
| Number motion | On-screen figures tumble to the selected amounts. | Guest/capacity/phase figures update with the active timeline frame and respect reduced motion. | Good |
| Truthful gaps/failures | Never show an old or wrong room. | Loading, unavailable, schedule gaps, digest mismatch, abort, stale generation, and renderer quarantine suppress the historical layer and present honest status. | Good |
| Desktop density | Information-rich command-centre composition. | Desktop preserves the functional viewer/timeline/metric hierarchy without obscuring editor controls, but the honest unavailable outline is sparse and the concept's richer venue/event side panels are not present in this capture. | Watch |
| Mobile reflow | Usable narrow layout rather than a scaled desktop. | Active phase and metrics remain legible and operable, although the compact date/control row is denser than the concept. | Watch |
| Photographic Trades Hall history | Distinct photoreal room captures for multiple milestones. | Intentionally absent: the viewer shows the frozen plan/unavailable treatment because no eligible reviewed Trades Hall RuntimePackages exist. | Blocked by evidence |

### Honest remaining gates

- T-539 remains `in-progress`. There are still no two genuinely distinct, rights-cleared, human-reviewed Trades Hall RuntimePackages, so the required two-runtime browser happy path and visual comparison cannot be run or claimed.
- Spark 2.0 has no cancellable async disposal API. If an upstream worker remains permanently busy, Venviewer retains that still-busy renderer and the page-global latch refuses subsequent renderer admission until reload; actual recovery without reload requires a pinned/upstream cancellation and `finally` fix. The per-canvas admission gate permits distinct canvases that were already active concurrently, so this is an accumulation circuit breaker rather than proof that only one renderer can ever be quarantined.
- A production-mode web build was not run because no real production Clerk publishable key was available; the development-mode Vite bundle is green and no fake credential was used.
- No deployment, merge into the user's branch, public bucket shortcut, fake URL, or synthetic runtime substitution was performed. The dirty `C:\Users\blake\omnitwin2` checkout remained read-only.
