# 6 September 2026 — isolated demo release integration

Created `codex/trades-hall-demo-release` from exact `fffae64d`, then integrated the
three inventory domain/API commits, the reviewed application subset of the
selected inventory style, and both Docker prerequisites. The demo's composed
Spark patch, lockfile and viewer remain unchanged. Inventory SQL is appended as
0065/0066 after manual-layout evidence 0064, with strictly increasing timestamps;
existing 0062/0063 remain unchanged for the actual production tail through 0061.

The original private inventory branch retains raw venue source material and
detailed reconciliation. Those documents are excluded from this public-release
history. Application source matches the previously verified selected-style
implementation; combined checks, fresh migration proof and production delivery
are still owned by the coordinated release. No source is edited during those
checks. [Exact composition and remaining boundaries](../reports/trades-hall-demo-release-integration-2026-09-06.md).
