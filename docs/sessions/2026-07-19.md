# Session log — 2026-07-19

## T-533 · Reception Room Envelope Review and fit-only registration seed

Continued the active OmniTwin Foundry `/goal` as ordinary local reconstruction
and product engineering. The prior **Goal blocked** banner was treated as a
product-category mismatch associated with cybersecurity-adjacent wording, not
as evidence that the product needs that work. Cybersecurity, penetration
testing, credentials,
identity attestation, signing, cloud deployment and publication were excluded.

- Froze `omnitwin.foundry.room-envelope-review.v0`, bound to the exact intake
  receipt, V8 artifact, Potree bundle/member identities and one exact preview
  for each canonical component plane. Only a 3–64 vertex simple intrinsic-
  pixel polygon is accepted. Eligibility requires a human
  `accepted_as_fit_seed` decision and at least 512 selected records.
- Added the read-only room-review worker. It reopens and re-hashes the exact
  three-member bundle, rejects path escape/symlinks/source drift, decodes the
  same 14-byte records, applies the frozen V8 raster mapping and records exact
  inside/outside counts, selected decoder-coordinate bounds and inverse-mapped
  polygon. Source mutation, network use and validation-input use remain false.
- Added the serialized loopback API and responsive workbench: exact-preview
  marks, proposed horizontal plane, click or numeric polygon editing, reviewer
  metadata, conservative `needs_revision` default, count/eligibility result and
  digest-bound canonical JSON download. The browser never supplies the source
  path.
- Added a separate fit-only Python consumer. It accepts only an eligible
  accepted artifact pinned to the exact Reception bundle, requests exactly the
  fit scans, compares proper/mirror fits, inverse-maps/crops/refits and can emit
  only an authority-none create-only candidate or refusal. It never requests
  validation/test scans and never creates an approved TransformArtifact.
- Replayed the unchanged Reception Potree source. The canonical automated full-
  frame proposal selected 175,237 records and excluded 0, with digest
  `1721c649…`; it remains `needs_revision` / `not_eligible` and cannot authorize
  fitting. The Python accepted-only gate rejected it before E57 access/output.
- In-app Browser QA marked all three record-density previews and repeated the
  full-frame count at 175,237 / 0, `not_eligible`, `authority:none`. Desktop
  1440×1000 and mobile 390×844 captures were inspected; mobile client/scroll
  widths were both 375 and there were zero browser warnings/errors. The local
  listener closed after the run.
- Verification: focused core 5/5; focused local app 66/66; fit-only Python 8/8;
  existing Python diagnostic 12 passed/1 expected Windows symlink skip; full
  core 63 files / 754 passed / 1 skipped; full CLI 43 files / 680 passed. Both
  packages pass lint and typecheck; core build passes. The CLI build retains
  its existing sibling-source `rootDir` packaging failure plus an unrelated
  trusted-Windows helper-bridge build error.

Evidence:
`docs/reports/reception-room-envelope-review-workbench-v0-evidence-2026-07-19.json`;
report:
`docs/reports/reception-room-envelope-review-workbench-v0-report-2026-07-19.md`;
canonical ineligible proposal:
`docs/reports/reception-room-envelope-review-proposal-v0-2026-07-19.json`.

T-533 and the broader `/goal` remain active. The next required input is a human
review of all three exact projections and the actual Reception room-only
boundary. Rights, independent survey control and later validation remain
separate non-cybersecurity gates.

## T-534 · Purpose-scoped HD super-app licence evidence refresh

Continued the active OmniTwin Foundry `/goal` without treating cybersecurity,
identity attestation, credentials, signing, deployment or publication as
ordinary super-app dependencies.

- Revalidated the 2026-07-14 technology/licence matrix against current official
  repository licence files, releases, model cards, dataset cards,
  specification terms and vendor/service agreements. The structured V1 covers
  48 supplied or production-relevant reference families with 125
  primary-source citations (119 distinct URLs), while keeping internal use,
  shipped code, weights/data/assets, services and independent-study candidates
  distinct.
- Confirmed the checked-in browser runtime remains exactly Three.js 0.180.0,
  React Three Fiber 8.18.0 and Spark 2.0.0, all MIT code at their exact pins.
  Upstream Spark 2.1.0, Three r185 and R3F 9 are upgrade candidates only.
- Selected the initial conditional reconstruction path for a new local
  manifest: pye57 0.4.19/libE57Format 3.1.1, PDAL/Open3D 0.19, COLMAP
  4.1.1/global mapper, curated hloc and gsplat 1.5.3. Narrow SPZ,
  SplatTransform, PlayCanvas and Basis code may be reviewed at exact packages;
  no PlayCanvas package is currently integrated.
- Found and explicitly excluded the existing `infra/runpod/Dockerfile` from
  that decision. It pins pye57 0.4.16/Open3D 0.18.0/gsplat 1.5.3+pt24cu124 but
  acquires SPZ and DN-Splatter from moving `main`, so it is not a reproducible,
  reviewed worker image and was not built, run or deployed.
- Kept raw XGRIDS/XBIN and default LCC/LCC2 product ingestion behind official
  export or written permission. The public SDK repositories still do not
  provide an affirmative root redistribution grant; Unreal SDK is now 3.0.0.
- Excluded graphdeco/Mip code, released ArtiFixer/MeshCoder/ScaRF default
  weights and the complete WorldMesh pipeline from the commercial lane.
  CL-Splats, ReAct-GS and WildGaussians whole repositories are also research
  only; Cross-Temporal 3DGS, GaussianUpdate, SimFoundry and NeuWorld are
  paper/citation candidates without a usable licensed release. The unresolved
  YouTube reference is cite/link only; its PPISP subject was separately screened
  as an Apache-2.0 conditional candidate. AI output stays a separate opt-in
  visual derivative.
- Corrected material drift and hidden obligations: SplatTransform 3.1.3,
  SuperSplat 2.31.1, COLMAP 4.1.1, KTX-Software's Ericsson ETC exception,
  Splat Analyzer's Apple AGPL lane and 3DGRUT's GPL `plyfile` dependency.
- Recorded that code-stack uncertainty no longer blocks the integrated browser
  stack or newly written local product engineering; the legacy RunPod image
  remains excluded. Real Matterport/venue/XGRIDS source rights and any exact
  generative checkpoint/data chain remain purpose-specific external evidence
  before cloud upload, commercial training, redistribution or authority claims.

Evidence:
`docs/reports/omnitwin-hd-stack-license-evidence-v1-2026-07-19.json`;
report:
`docs/reports/omnitwin-hd-stack-license-evidence-v1-2026-07-19.md`;
detailed matrix:
`docs/reports/omnitwin-foundry-technology-license-matrix.md`.

No package was installed, no source capture was parsed, no credential or cloud
account was used, no provider/storage call or spend occurred, and nothing was
signed, published or promoted. T-534 is complete only for this bounded
engineering screen; T-533, T-508, T-486 and the broader `/goal` remain active.

## T-535 · Local HD Worker Manifest V0 and workbench readiness

Continued the active OmniTwin Foundry `/goal` as ordinary local product and
reconstruction engineering. Cybersecurity work, credentials, cloud execution,
signing and publication were not needed.

- Added a strict V0 manifest, JSON Schema and build-owned generated TypeScript
  payload. The canonical payload digest is
  `fbfd7c6c51c8be06f9bb411f4833fbf0fd0daba45d09512ed70bf63420b9f436`.
  It says exactly `planned_not_installed_not_execution_ready`, with no current
  source/plan binding, no installation, no runtime verification, execution
  disabled and authority none.
- Pinned eight root identities and legal-text evidence: pye57 0.4.19,
  libE57Format 3.1.1, Xerces-C++ 3.2.3 at publisher tag `v3.2.3`, Open3D
  0.19.0, COLMAP 4.1.1, hloc 1.4, gsplat 1.5.3 and PPISP 1.2.1. All eight
  dependency/notice closures remain open; none is presented as an installable
  worker.
- Enforced five exact, disjoint lane contracts. E57, geometry QA and
  camera registration are local Windows candidates. Gaussian training and
  photometric compensation are separately reviewed GPU-worker-only lanes;
  gsplat or PPISP cannot be aliased into a local lane, and local Windows
  Gaussian training remains disabled under D-016.
- Kept hloc weight-free and download-free, deferred PDAL 2.10.2 explicitly,
  excluded raw XBIN/LCC/LCC2 decoding, unlisted models/data and AI substitution
  for captured truth, and retained the `infra/runpod/Dockerfile`
  `do_not_build_run_or_deploy` refusal.
- Added a nontechnical panel to the existing loopback workbench through the
  existing `/api/state` response. It renders every one of the five lanes,
  eight roots and six exclusions under “Pinned plan — not installed or ready
  to run.” No install, build, worker or execution route/control was added.
- Hardened the browser boundary to exact top-level and nested keys, safe unique
  IDs, exact identity sets and counts, fixed disabled activity flags and the
  remote-only Gaussian contract. Executable adversarial tests reject unknown
  fields, duplicate/missing identities and a locally relabelled Gaussian lane.
- Made the Windows x64 release graph require the build-owned generated manifest
  source, with an omission rejection test. This prevents a stale or commented
  import from satisfying release evidence.
- Two independent read-only audits found and closed the Xerces tag spelling,
  mutable gsplat legal-text URL, lane-alias, misleading `local-only` next-action,
  browser-parser and release-graph weaknesses before the evidence freeze.
- Verification: focused core 5/5, workbench 45/45 and Windows release graph
  27/27; serial whole-package runs are 768 passed/1 intentional skip across 64
  core files and 711/711 across 45 tool files. Both package typechecks, core
  build and targeted lint pass. Desktop 1440×1000 and mobile 320×800 layout
  inspection found no horizontal overflow and zero browser warnings/errors.

Evidence:
`docs/reports/omnitwin-local-hd-worker-manifest-v0-evidence-2026-07-19.json`.

No package was installed, no worker was built or run, no venue/source bytes
were touched, no model optimisation or provider call occurred, and no
credential, spend, signing or publication was used. T-535 is done only for
this bounded planning/readiness slice. T-533 still awaits a human room-envelope
review; T-508, T-486 and the broader `/goal` remain active.

## T-536 · Exact Local E57 Intake Environment V0

Continued the active OmniTwin Foundry `/goal` as ordinary local reconstruction
and super-app engineering. Cybersecurity, credentials, signing, deployment and
publication were not needed.

- Replaced only the historical T-535 E57 runtime candidate with a strict
  E57-only CPython 3.13.14 profile. Open3D 0.19 remains an independent runtime
  lane; the parent manifest and its digest were preserved as lineage.
- Pinned the exact CPython 3.13.14 embeddable archive, pye57 0.4.19 cp313 wheel,
  NumPy 2.5.1 cp313 wheel and pyquaternion 0.9.9 wheel. The canonical child
  digest is `34ad3f54ea5a5afcca908c66f48ab039381d6910b2372afbafee0c1f8545ea1e`
  and binds parent `fbfd7c6c51c8be06f9bb411f4833fbf0fd0daba45d09512ed70bf63420b9f436`.
- Froze all 13 ordered pye57 member path/size/hash receipts, four exact native
  lineage/disposition records and 29 legal identities/receipts under separate
  inventory digests. Artifact metadata, native binaries and the pye57 archive
  licence now cross-link to their exact member receipts.
- Recorded the real activity accurately: no package installer or system install
  was used, and no user-provided or venue source was read. A temporary unpacked
  CPython/pye57/NumPy/pyquaternion environment did run imports, a quaternion
  assertion and one synthetic three-point E57 write/read round trip. The 4,096
  byte cp313 fixture hashed to `91f2b9a0…db31`. The run depended on host
  `MSVCP140.dll`, was not a future bundle test and retained no repository log.
- Kept the result fail-closed. The application bundle and legal pack are not
  materialized, clean-host behavior is not verified, exact pybind11 build
  provenance remains unresolved and the existing aggregate-only adapter is not
  wired to this environment. Execution stays disabled and authority is none.
- Made the generator verify and carry the reviewed digest rather than silently
  recompiling a payload. Readiness verifies the complete generated document,
  server construction rejects a parent/child mismatch and the Windows release
  audit requires the generated E57 source to contribute positive bytes to
  `foundry.mjs`; omission and tree-shaken inclusion are regression-pinned.
- Added the nested no-control workbench section “Exact artifacts recorded —
  application bundle still open,” rendering all four artifacts and six gates.
  The old CPython 3.10 lane card is now explicitly labelled historical and
  superseded for E57. Final Playwright QA at 1440×1000 and 320×800 found no
  horizontal overflow, no overflowing cards, no controls and zero console
  warning/error. The loopback listener then stopped and its verified-empty QA
  source folder was removed.
- Verification: focused core 6/6 and focused tool 95/95; serial package suites
  65 files / 774 passed / 1 intentional skip core and 46 files / 753 passed
  tool. Both package typechecks, both lints and core build pass. The tool build
  retains its existing cross-package `rootDir`/TS6307 packaging failure and
  emitted no T-536-specific diagnostic.

Evidence:
`docs/reports/omnitwin-local-e57-intake-environment-v0-evidence-2026-07-19.json`.

T-536 is done only for this bounded exact-environment/readiness slice. The next
E57 closure must select the exact Microsoft C++ runtime disposition and
pybind11 notice, materialize one deterministic application-local bundle plus
legal pack, prove it on a clean synthetic-only Windows host and only then bind
the immutable bundle receipt into the aggregate-only adapter. T-533 still needs
the human Reception room-envelope review; T-508, T-486 and the broader `/goal`
remain active.

## Foundry phase 1 · operator sampling recorded, pilot manifest + diagnostic reproduced

The decade-30 operator sampling gap closed; release-grade identity
authentication, venue authority and release approval did not. The bounded pilot
branch executed end to end in four commits:

- **e6465aca** — Blake reviewed the decade-30 contact sheet (60 byte-pinned
  cubefaces) and confirmed all ten sampled positions as Grand Hall; the verdict
  was transcribed verbatim with evidence-index hash and scope. This ordinary
  operator record closed only the decade sampling gap. It is not cryptographic
  authentication, venue authority or release approval, and sweep 49 remains
  excluded per decision B.
- **e3b632b9** — pilot ingest manifest: deterministic tested builder (6/6) +
  the real FoundryIngestManifestV0 at
  `docs/operations/grand-hall-pilot-ingest-manifest-2026-07-19.json` —
  310 assets / 308 producing edges, every input hashed read-only (full
  20.5 GB E57 = sha256 975039d1…), honest rights postures (Matterport
  requires_review, benchmark prohibited), manifestSha256 63516c0b….
- **e1e011c9** — deterministic E57/COLMAP inspection as tested code (COLMAP
  cameras/images.bin parsers, camera centres −RᵀT, E57 paged logical reads,
  data3D pose extraction) plus a Horn closed-form similarity diagnostic. The
  first runner/report admitted excluded sweep 49 and shifted the frozen
  holdout from 44 to 45; the 2026-07-22 correction below supersedes those
  diagnostic numbers and frame wording.
- **c9a4076b** — evidence-run discipline caught the recorded
  e57ScanTranslations=1043 (149 scans + 894 images2D camera poses matched by
  an unscoped pattern); extraction now scoped to data3D, pinned by a
  discriminating fixture, re-run byte-identical residuals and exactly 149 —
  also closing e57-main's decisive next test from the manifest.

Correction — 2026-07-22: the diagnostic runner now fails closed onto the exact
T-507 scope 0–48, excludes the discovered sweep 49 and uses the frozen holdout
`[5, 15, 25, 35, 44]`. The corrected 49-correspondence result has scale
1.7362021512; its 44-point fit is 6.336 mm median / 11.021 mm RMSE / 16.961 mm
p95 / 45.905 mm max, and its five-point holdout is 3.831 mm median / 5.757 mm
RMSE / 9.679 mm p95 and max. The proposed edge is now explicitly
`colmap-sfm`→`e57-global-diagnostic`: an authority-none, same-lineage scan-pose
diagnostic, not independent survey control and not a governing release
transform. The full E57 and COLMAP inputs were rehashed before regenerating
only the residual report and proposed transform. T-507 and the T-486 r2
preflight remain unchanged; independent survey, rights and release review
remain external gates.

Earlier same goal: truth-hardening b9dadbcc (monotonicity over lineage,
transitive stage rights + blocked legal review, generated-content evidence
exclusion; catalog provenance was already closed in-tree).

Open (owner-side): Bright_Walls PLY/SOG export location; the intake's
release-grade sections (fit controls + blind checks, release scope,
Matterport/identity-reference rights); the XGRIDS official request. Next
engineering lane opened: local CPU mesh baselines — pye57 present, Open3D
absent on Python 3.13 (no wheel) → installing under the machine's 3.12.
