# Session log — 2026-06-08

## T-301 — Operational geometry data sufficiency checks

Request: continue the S+ engineering sweep with the next unblocked foundation
item.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add shared schema-level operational geometry data-sufficiency checks.
- Cover missing door-width evidence, unverified furniture footprints,
  unavailable connector graph data, unsupported route-discovery requests, and
  geometry/policy gaps.
- Return explicit Data Sufficiency Contract outcomes:
  `not_checked`, `degraded_evidence`, `unsupported_request`, or
  `requires_human_review`.
- Do not implement a compiler, solver, simulation engine, runtime UI, public
  copy, package rename, or T-091/T-091A state change.

### Fix

- Added `OPERATIONAL_GEOMETRY_DATA_SUFFICIENCY_CHECK_IDS`,
  `OPERATIONAL_GEOMETRY_ROUTE_DISCOVERY_MODES`,
  `OperationalGeometryDataSufficiencyCheckRequestSchema`,
  `OperationalGeometryDataSufficiencyFindingSchema`, and
  `operationalGeometryDataSufficiencyFindings` in
  `packages/types/src/operational-geometry.ts`.
- Exported the new checker surface from `packages/types/src/index.ts`.
- Extended `packages/types/src/__tests__/operational-geometry.test.ts` with
  regressions for:
  - missing door-width evidence;
  - explicit door-width evidence producing no finding;
  - unsupported/unverified furniture footprints;
  - unavailable connector graph data;
  - connector links to missing features;
  - unsupported arbitrary route discovery;
  - missing submitted-route input;
  - rejection of contradictory submitted-route references;
  - missing policy refs and required feature classes;
  - collection-level insufficient data propagation;
  - rejection of review-role leakage on non-human-review outcomes;
  - rejection of sufficiency message keys on sufficient outcomes;
  - rejection of pass/fail-style outcomes.
- Marked T-301 done in `docs/state/tasks.md` and updated
  `docs/diagrams/task-graph.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- operational-geometry`
  — 1 file / 25 tests passed.
- `pnpm --filter @omnitwin/types test -- operational-geometry data-sufficiency review-gate geometry-approximation`
  — 4 files / 42 tests passed.
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types build`
- `pnpm --filter @omnitwin/types test`
  — 54 files / 1460 tests passed.

Sandbox note: the first sandboxed runs of Vitest, ESLint, and TypeScript failed
before executing with Windows `EPERM` while opening tool binaries under
`node_modules/.pnpm`. The same commands passed when run outside the sandbox.

### Remaining work

- T-298 walkable-area compiler spike remains not-started.
- T-300 polygon-validity tests remain blocked on T-298.
- T-305 and T-320 remain blocked on T-300.
- T-091 / T-091A remain not-started: no real captured room asset is loaded in
  runtime and no evidence claim was changed.

## T-204 — Capture control source vocabulary

Request: continue the S+ engineering sweep with the next unblocked foundation
item after T-116.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add metadata-only shared vocabulary for Capture Control Network source
  classes, pose authority levels, alignment methods, QA statuses,
  staleness triggers, and reference types per CCN-001.
- Add small source-record guardrails so low-control sources cannot silently
  claim stronger authority.
- Do not implement E57 extraction, Matterport integration, fiducial detection,
  marker protocols, database schema, runtime UI, public copy, package rename,
  or T-091/T-091A state changes.

### Fix

- Added `packages/types/src/capture-control.ts` with typed vocabularies for:
  raw structured E57 poses, Matterport API/SDK poses, COLMAP poses, AprilTags,
  ChArUco boards, manual landmarks, control distances,
  artist/Blender alignment references, and known-pose COLMAP models.
- Added pose authority levels that keep measured control, validated fiducial
  control, manual landmark control, known-pose COLMAP, unconstrained COLMAP,
  and visual alignment separate.
- Added `CaptureControlSourceRecordSchema` with compatibility checks for
  source class to authority level and source class to alignment method.
- Required reviewer roles for accepted, rejected, contested, human-reviewed,
  and human-review-required QA states while rejecting reviewer-role leakage on
  machine-only states.
- Exported the new contract from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/capture-control.test.ts` covering pinned
  vocabularies, helper mappings, review-role requirements, and rejection of
  visual placement or unconstrained COLMAP authority inflation.
- Marked T-204 done in `docs/state/tasks.md` and updated
  `docs/diagrams/task-graph.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- capture-control`
  — 1 file / 12 tests passed.
- `pnpm --filter @omnitwin/types test`
  — 55 files / 1476 tests passed.

### Remaining work

- T-205 E57 pose inspection remains not-started and is the next real-data
  proof step for deciding whether raw E57 poses can be T-091B Pose Authority.
- T-208 Control QA report remains deferred until capture-control source
  records and real T-091/T-091B artifacts exist.
- T-091 / T-091A remain not-started: no real captured room asset is loaded in
  runtime and no evidence claim was changed.

## T-116 — TransformArtifactV0 runtime manifest surface

Request: continue the S+ engineering sweep with the next unblocked foundation
item after T-301.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Extend the RuntimeVenueManifestV0 shared type surface with typed
  TransformArtifactV0 records per D-024.
- Include explicit source/target frames, units, Matrix4d, alignment method,
  residual RMSE, landmark correspondences, D-012 provenance state, creator,
  reviewer, and date.
- Enforce that non-identity runtime layer transforms reference a declared
  TransformArtifactV0.
- Do not implement database schema, reconstruction pipeline, runtime loading,
  public copy, package rename, or T-091/T-091A state change.

### Fix

- Added transform frame/method/provenance/reference/actor vocabularies,
  `RuntimeTransformReferenceSchema`, `RuntimeTransformProvenanceSchema`,
  `RuntimeTransformActorSchema`, `RuntimeTransformLandmarkPairSchema`, and
  `TransformArtifactV0Schema` in
  `packages/types/src/runtime-venue-manifest.ts`.
- Added Matrix4d similarity validation for transform artifacts using the
  existing coordinate-frame helper policy.
- Added optional layer `transformArtifactId` and manifest-level
  `transformArtifacts`, with cross-reference checks and a non-identity
  transform guard.
- Tightened landmark-solve evidence so transform artifacts with landmark
  residuals need per-landmark residuals, per-landmark provenance, and aggregate
  residual RMSE.
- Required TransformArtifactV0 reviewer accountability to point at a human
  actor with an explicit review role while still allowing pipeline/tool
  creators.
- Rejected measured provenance claims for visual-only and unconstrained COLMAP
  alignments so low-control transforms cannot silently become metric authority.
- Exported the new schemas/types from `packages/types/src/index.ts`.
- Extended `packages/types/src/__tests__/runtime-venue-manifest.test.ts` with
  coverage for the vocabulary, default empty transform list, valid referenced
  transform artifacts, missing references, duplicate IDs, same-frame transforms,
  missing landmark residual evidence, invalid matrices, and negative residual
  metrics.
- Marked T-116 done, surfaced newly unblocked T-117, T-121, T-204, T-210,
  T-232, T-298, and T-335, and updated `docs/diagrams/task-graph.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- runtime-venue-manifest`
  — 1 file / 46 tests passed.
- `pnpm --filter @omnitwin/types typecheck`
- `pnpm --filter @omnitwin/types lint`
- `pnpm --filter @omnitwin/types test`
  — 54 files / 1464 tests passed.
- `pnpm --filter @omnitwin/types build`
- `git diff --check`

### Remaining work

- T-117, T-121, T-232, T-298, and T-335 are now ready to start
  when capacity allows.
- T-091 / T-091A remain not-started: no real captured room asset is loaded in
  runtime and no evidence claim was changed.

## T-210 — Exposure metadata in manifests

Request: continue the S+ engineering sweep with the next unblocked foundation
item after T-204.

Squad role: Architect + Tester.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed.

### Scope

- Add typed exposure metadata for future shareable manifests and export
  descriptors, starting with the RuntimeVenueManifestV0 shared contract.
- Carry artifact type, exposure tier, venue/client scope, allowed audience,
  expiry, claim review status, approval references, source artifact references,
  and export-safety metadata.
- Default omitted exposure metadata to `internal_only`.
- Do not implement access control, route changes, object-storage migration, DB
  schema, runtime loader behavior, public copy, package rename, or
  T-091/T-091A state changes.

### Fix

- Added `packages/types/src/exposure-metadata.ts` with
  `ExposureMetadataV0Schema`, allowed-audience vocabulary, claim-review status
  vocabulary, and an `internalOnlyExposureMetadata` helper.
- The exposure schema rejects non-public tiers with public audience, requires
  public-facing metadata to be public-safe and claim-reviewed, requires
  published case-study approval refs, requires partner-preview expiry, and
  requires authenticated-client venue/client scope.
- Added `RuntimeManifestExposureMetadataSchema` to
  `packages/types/src/runtime-venue-manifest.ts`; RuntimeVenueManifestV0 now
  defaults missing exposure to internal-only runtime-package metadata.
- Added runtime-manifest checks so client and case-study runtime exposure must
  be scoped to the manifest venue, and ownerVenueId cannot drift from venueId.
- Exported the exposure schemas/types from `packages/types/src/index.ts`.
- Added `packages/types/src/__tests__/exposure-metadata.test.ts` and extended
  `packages/types/src/__tests__/runtime-venue-manifest.test.ts` with exposure
  defaults, valid authenticated-client exposure, non-runtime artifact rejection,
  venue-scope mismatch rejection, and public runtime exposure rejection.
- Marked T-210 done in `docs/state/tasks.md` and updated
  `docs/diagrams/task-graph.md`.

### Verified

- `pnpm --filter @omnitwin/types test -- exposure-metadata runtime-venue-manifest`
  — 2 files / 60 tests passed.
- `pnpm --filter @omnitwin/types test`
  — 56 files / 1490 tests passed.

### Remaining work

- T-211 deploy-time public artifact scanner remains not-started; it can now
  consume typed exposure metadata instead of route-name heuristics.
- T-213 public claim guard integration with exposure tier remains deferred.
- T-091 / T-091A remain not-started: no real captured room asset is loaded in
  runtime and no evidence claim was changed.
