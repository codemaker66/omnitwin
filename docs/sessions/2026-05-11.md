# 2026-05-11

## T-408 live planner command shell and readable labels

- Completed T-408 as a focused live `/plan` visual alignment pass.
- Replaced the small desktop planner status card with a full-width dark command-center status bar inspired by the supplied references while keeping the `/plan` opening state blank.
- Enlarged in-scene seat/table labels into camera-facing black/gold nameplates so hallkeeper-facing names are readable from normal planning distance.
- Moved the desktop Send action below the new top bar so it no longer collides with the command shell.
- Verified `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, focused web unit tests, full `packages/web/e2e/editor.spec.ts`, and browser screenshots at 1600px/1400px desktop.
- No public evidence claims, fake T-091/T-091A assets, backend/auth/upload logic changes, package renames, or public marketing copy changes.

## T-409 hidden heart easter egg

- Completed T-409 as a small global web-app easter egg.
- Added a hidden `jackielarkin` key sequence that renders a giant CSS love heart, pulses seven times, then disappears.
- The listener ignores editable fields, contenteditable regions, and modifier shortcuts so chair/table naming, search, and planner controls are not affected.
- Verified `pnpm --filter @omnitwin/web test -- JackieLarkinHeart`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, and `pnpm --filter @omnitwin/web build`.

## T-410 premium table/seat callouts

- Completed T-410 after visual feedback that the table label still looked like a tiny strip compared with the command-center reference.
- Reworked renderer-native furniture labels into larger black/gold command callouts with a readable title, grouped-seat count, planner-label context, optional camera POV state, and an anchor dot.
- Offset table labels beside the object instead of drawing the card directly across the tabletop.
- Verified `pnpm --filter @omnitwin/web test -- PlacedFurniture`, `pnpm --filter @omnitwin/web lint`, `pnpm --filter @omnitwin/web typecheck`, `pnpm --filter @omnitwin/web build`, and local Chromium screenshot inspection.
