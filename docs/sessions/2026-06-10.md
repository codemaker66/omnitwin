# Session log — 2026-06-10

## T-447 — Unified command-sourced undo/redo (verification + bookkeeping)

Request: `claude --continue` — resume the session interrupted on 2026-06-09/10.
The undo/redo implementation was complete in the working tree but had never
been verified, given a task ID, or logged.

Squad role: Tester + Shepherd.
Branch: `fix/hallkeeper-offline-sync-dataloss`. Not pushed, not committed.

### State found on resume

- Implementation complete and fully wired:
  - `packages/web/src/lib/editor-history.ts` — pure invertible-delta undo/redo
    engine (epoch coalescing, identity-entry dropping, 100-entry cap, 2 MB
    budget, whole-timeline ID remapping, `local-` ID healing for resurrected
    server rows).
  - `packages/web/src/hooks/use-undo-redo-shortcuts.ts` — global Ctrl/Cmd+Z,
    Ctrl+Shift+Z, Ctrl+Y; inert in text fields and while laser markup owns
    stroke undo.
  - `packages/web/src/stores/editor-store.ts` — one shared timeline across 3D
    scene and 2D blueprint; undo/redo actions, 800 ms interaction epochs,
    catalogue-name labels, selection capture/restore, autosave requester.
  - `packages/web/src/components/editor/EditorBridge.tsx` — scene mutations
    routed through `replaceObjectsFromScene`; debounced auto-save registered
    so undo/redo persists.
  - Legacy snapshot undo removed from
    `packages/web/src/stores/placement-store.ts`; superseded
    `packages/web/src/components/ActionBar.tsx` deleted (no references remain).
  - Undo/redo surfaced in PlannerCommandDeck, VerticalToolbox, BlueprintPage;
    shortcuts mounted in EditorPage.
  - New/updated tests: `editor-history`, `editor-store-history`,
    `use-undo-redo-shortcuts`, `VerticalToolbox`, `blueprint-from-store`, plus
    editor-bridge/placement-store/command-deck/HUD/composer/circulation suites.
- The 2026-06-08 types work (T-301, T-204, T-116, T-210) was already done,
  verified, and logged; statuses were already updated in `tasks.md` and
  `task-graph.md`.

### Done this session

- Full verification chain (all green):
  - `pnpm --filter @omnitwin/types build` — clean.
  - `pnpm --filter @omnitwin/types test` — 56 files / 1490 tests, no type
    errors.
  - `pnpm --filter @omnitwin/web typecheck` — clean.
  - `pnpm --filter @omnitwin/web lint` — clean.
  - `pnpm --filter @omnitwin/web test` — 112 files / 2028 tests passed.
  - `pnpm --filter @omnitwin/web build` — production build OK.
  - `pnpm --filter @omnitwin/api typecheck` — clean (types changes are
    additive; API unaffected).
  - `pnpm --filter @omnitwin/api test` — 55 files / 1636 tests passed.
- Added T-447 (done) to `docs/state/tasks.md`; bumped `Updated:` to
  2026-06-10.
- Added T447 node, dependency edges (T-358, T-370, T-390, T-397 → T-447), and
  done-class entry to `docs/diagrams/task-graph.md`.

### Not run this session

- E2E suites at the time of the verification chain above — unit/build
  verification only. Closed later the same session; see the editor E2E
  section below.

### Editor E2E verification (after Blake asked to commit + run the suite)

- First run failed wholesale: every spec mocking `GET
  /public/configurations/:id` was rejected by the api-client Zod boundary
  ("unexpected response shape"). Root cause is **pre-existing fixture rot**,
  not T-447: committed `8e4f1f5` (2026-06-07, revision optimistic
  concurrency) made `revision` required in `ConfigurationResponseSchema` and
  changed the batch-save response to `{ objects, revision }`, updating every
  unit fixture but **zero** E2E fixtures. The editor E2E suite has been red
  on HEAD since that commit.
- Two further failures after the fixture repair, attributed by experiment
  (stash strand → run on clean HEAD → restore):
  - `all core toolbar buttons are present` — strict-mode violation, **caused
    by T-447**: the command deck now also exposes Undo/Redo, so the
    page-wide `getByRole("button", { name: "Undo" })` matched two elements.
    Fixed by scoping the assertion to `planner-toolbar`.
  - `auth modal closes when Escape is pressed` — **pre-existing product
    bug**, exposed as a timing race: `.venviewer-planner-shell` declares
    `isolation: isolate`, trapping the AuthModal's zIndex 200 inside the
    shell's stacking context while the status header (z46) and 3D/2D
    view-mode pill (z31) sit outside it in the root context — planner
    chrome painted above (and was clickable on top of) an open modal.
    Fixed by portalling AuthModal to `document.body`. (A single-sample HEAD
    pass during attribution was Clerk-iframe expansion timing; the stacking
    defect is structural and present on HEAD.)
- All fixture/spec/product repairs are tracked as T-448 and committed
  separately from the T-447 feature strand.
- Result: editor suite (editor, editor-interactions, editor-mobile-shell)
  **38/38 passed** serialized.

## T-448 — Editor E2E suite repair (fixtures, modal stacking, Undo scoping)

Request: same Blake instruction ("commit the two strands as separate commits
and run the editor E2E suite") — the suite run surfaced these defects.

Squad role: Tester + Debugger.

### Fix

- `revision: 1` added to every mocked configuration payload, and batch-save
  mocks converted to the `{ objects, revision }` envelope, across
  `packages/web/e2e/{editor,editor-interactions,editor-mobile-shell,navigation,performance,planner-submit,public-config-flow}.spec.ts`.
- `packages/web/src/components/editor/AuthModal.tsx` now portals to
  `document.body` so the overlay's zIndex 200 escapes the planner shell's
  `isolation: isolate` stacking context and genuinely covers planner chrome.
- `editor.spec.ts` toolbar-presence assertion scoped to `planner-toolbar`
  (the command deck legitimately exposes a second Undo/Redo since T-447).

### Verified

- Serialized editor suite: 38/38 passed.
- Full serialized web E2E: **95 passed / 1 failed / 4 skipped in 4.9 min.**
  The single failure is `trades-hall-visual.spec.ts` ("loads the empty
  internal command shell without runtime errors"): `TradesHallVisualPage`
  unconditionally fetches `GET /assets/runtime-packages/latest` on mount, and
  with no local API on :3001 the browser logs `net::ERR_CONNECTION_REFUSED`,
  tripping the spec's zero-console-error guard even though the page falls
  back gracefully. Pre-existing environment coupling — the page, api client,
  and spec are untouched by T-447/T-448 — filed as **T-449** (not-started).
- Web typecheck, lint, and the AuthModal unit contract suite
  (save-send-panel, 30 tests) after the portal change.

### Remaining work

- T-449: decouple the trades-hall-visual console guard from the local API
  (mock the runtime-package fetch in the spec, or scope the guard).
- T-091 / T-091A remain not-started: no real captured room asset is loaded in
  runtime and no evidence claim was changed.
