# 2026-06-15

## Reception Room runtime camera arrival

- Continued the registered Reception Room camera-control pass from T-091/T-091A runtime QA work.
- Added an explicit bounded camera arrival profile for the Reception Room runtime view: arrival position `[0.25, 7.15, 14.1]`, arrival target `[0, 1.2, -4]`, duration `1400ms`, settling into position `[0.2, 6.2, 13.4]` and target `[0, 0.9, -4.15]`.
- Updated the R3F runtime camera rig so the arrival uses preallocated vectors, clamps throughout the move, and cancels immediately when OrbitControls starts a user interaction.
- Extended focused regression coverage for the arrival pose, bounds, and user-interaction cancel hook.
- Updated the Reception Room runtime intake note with the current arrival values and the transform limitation reminder.
- Verified the visual route on system Edge with Playwright fallback because the in-app Browser was unavailable as `iab`; screenshots are `output/playwright/reception-room-camera-arrival-settled.png` and `output/playwright/reception-room-camera-arrival-after-drag.png`.

T-091 and T-091A remain open. This improves runtime QA presentation and navigation only; the Reception Room transform remains approximate until signed room-local alignment, LCC2/LOD strategy, visual QA record, and exposure review are complete.

## Task source-of-truth repair: CRM and revenue analytics

- Audited the platform goal surface against the current repo and confirmed these are real, implemented surfaces rather than empty claims: dashboard route, commercial pipeline, proposals/quotes, revenue analytics, evidence runtime/Truth Mode, AI draft assistant, event-day ops, guest-flow replay, integrations, and runtime package status.
- Corrected `docs/state/tasks.md` so T-428 is `done` with concrete artifact paths for the commercial spine (`commercial-spine.ts`, migration `0034_commercial_spine.sql`, `/crm` and `/opportunities` routes, `CommercialPipelineView`, API client, and regression tests).
- Fixed a duplicated task ID: revenue analytics had been logged as `T-447`, colliding with the earlier unified undo/redo task. Revenue analytics is now `T-463`; `T-447` remains undo/redo. Updated `docs/sessions/2026-06-12.md` with an explicit correction note.
- Updated `docs/diagrams/task-graph.md` so T-456 depends on T-428 + T-451, and T-462 depends on T-463 + T-451 instead of the undo/redo task.
- Verified:
  - duplicate task-ID PowerShell check: no duplicate task IDs in `docs/state/tasks.md`;
  - stale revenue-as-T-447 grep: no stale dependency/source references;
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/commercial-spine-routes.test.ts src/__tests__/commercial-spine-schema.test.ts src/__tests__/revenue-analytics.test.ts` — 3 files / 26 tests passed;
  - `pnpm --filter @omnitwin/web exec vitest run src/components/dashboard/__tests__/CommercialPipelineView.test.tsx src/components/dashboard/__tests__/ExecutiveAnalyticsView.test.tsx` — 2 files / 5 tests passed;
  - `git diff --check` passed with CRLF warnings only.

No runtime product code changed. Existing uncommitted cockpit/E2E edits were left untouched.

## T-456 organisation/workspace onboarding foundation

- Added the concrete onboarding foundation for managed venue rollout:
  organisation/workspace contracts, workspace memberships, venue-scoped owner
  and staff invitations, onboarding project/review state, entitlement state, and
  audit events.
- Implemented shared contracts in `packages/types/src/onboarding.ts`, exported
  through `@omnitwin/types`, with provider-verification rules that reject access
  enforcement unless real provider evidence is present.
- Added migration `packages/api/drizzle/0037_onboarding_entitlements.sql` and
  Drizzle schema tables for `organisations`, `workspaces`,
  `workspace_memberships`, `onboarding_projects`, `workspace_entitlements`, and
  `onboarding_audit_events`. Venue remains the v1 authorization boundary.
- Added admin-only `/onboarding` API routes for summary, managed workspace
  creation, staff invitations, onboarding project updates, and provider
  verification updates. Customer owner/admin is a workspace membership plus a
  venue-scoped invitation, not the platform-wide `admin` role.
- Added the dashboard onboarding view and typed web client:
  `packages/web/src/components/dashboard/OnboardingView.tsx`,
  `packages/web/src/api/onboarding.ts`, and admin nav/page wiring.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` so T-456 is
  marked done with concrete artifacts.
- Verified:
  - `pnpm --filter @omnitwin/types run build`;
  - `pnpm --filter @omnitwin/types run typecheck`;
  - `pnpm --filter @omnitwin/api run typecheck`;
  - `pnpm --filter @omnitwin/web run typecheck`;
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/onboarding.test.ts` — 1 file / 5 tests passed;
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/onboarding-schema.test.ts src/__tests__/onboarding-routes.test.ts` — 2 files / 12 tests passed;
  - `pnpm --filter @omnitwin/web exec vitest run src/components/dashboard/__tests__/OnboardingView.test.tsx` — 1 file / 2 tests passed;
  - `pnpm --filter @omnitwin/web exec vitest run src/__tests__/dashboard.test.ts` — 1 file / 57 tests passed;
  - browser smoke fallback with Playwright CLI against `http://localhost:5174/dashboard`: protected dashboard route served, then redirected to `/login` as expected for an unauthenticated session. Local console noise was limited to missing favicon and Clerk development telemetry/CORS warnings.

Remaining platform gaps after T-456: no live Stripe/checkout reconciliation,
no supplier portal, no enterprise multi-venue analytics UI, no client e-sign
flow, and T-091 real Trades Hall runtime evidence remains open.

## T-435 supplier coordination pack foundation

- Added shared supplier coordination contracts in
  `packages/types/src/supplier-coordination.ts`, exported through
  `@omnitwin/types`: supplier-safe pack state, assigned requirement items,
  hashed share-token records, acknowledgements, public supplier view payloads,
  and deterministic source digests tied to handoff pack + snapshot hash.
- Added migration `packages/api/drizzle/0038_supplier_coordination.sql` and
  Drizzle schema tables for `supplier_coordination_packs`,
  `supplier_coordination_pack_items`, `supplier_coordination_share_tokens`, and
  `supplier_acknowledgements`.
- Added authenticated internal API routes under `/supplier-coordination` to
  create packs from frozen ops handoff supplier instructions, view pack bundles,
  and generate one-time raw share links while persisting SHA-256 token hashes
  only.
- Added capability-scoped public routes under `/supplier-share` for supplier
  pack viewing and acknowledgement capture. Public output is deliberately
  smaller than the internal pack: supplier-scoped requirements, arrival-window
  fields, contact details, source label/version/hash prefix/digest, and
  acknowledgement state only.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` so T-435 is
  done and visible as a T-457 supplier portal prerequisite.
- Verified:
  - `pnpm --filter @omnitwin/types run build`;
  - `pnpm --filter @omnitwin/types run typecheck`;
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/supplier-coordination.test.ts` — 1 file / 6 tests passed;
  - `pnpm --filter @omnitwin/api run typecheck`;
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/supplier-coordination-schema.test.ts src/__tests__/supplier-coordination-routes.test.ts` — 2 files / 14 tests passed;
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/routes-inventory.test.ts src/__tests__/ops-compiler.test.ts` — 2 files / 20 tests passed;
  - `pnpm --filter @omnitwin/web run typecheck`;
  - `git diff --check` passed with CRLF warnings only.

Remaining after T-435: T-457 still needs the actual supplier portal UI/workflow
over this foundation; T-459 still needs live contract/e-sign/payment/accounting
integration hooks; T-091 real Trades Hall runtime evidence remains open.

## Cockpit typecheck unblock

- Fixed the stale cockpit overlay projection test fixture so `sampleTrajectoryAtProgress` receives the current plain replay-point shape `{ x, y }` instead of the timed trajectory shape `{ x, y, t }`.
- Confirmed the cockpit scene-overlay model module resolves and the model selectors/layer rules typecheck.
- Tightened the overlay model so static heritage-only overlays do not wake the replay worker, and wired density patches to the replay artifact's actual `cellSizeM`.
- Reviewed the adjacent minimap/focus path and added a guard/test so the camera focus frame loop does not crash before OrbitControls is registered.
- Restored full `@omnitwin/web` typecheck.

Note: the cockpit scene overlay component, evidence beam, camera focus, minimap, replay hook, projection/model helpers, and their focused tests are currently untracked in this working tree. Include them intentionally with the cockpit overlay work before committing.

## Planner cockpit Phase 3 — overlays, evidence beam, minimap radar (shipped)

Promoted the cockpit's spatial layer from the dev page's percentage-positioned
2D overlays to real, world-anchored R3F geometry living inside the live `/plan`
canvas, so it pins to the floor and tracks the camera under orbit/pan/zoom.

- Pure, unit-tested mappers in `lib/`: `cockpit-overlay-projection` (replay
  metre-frame → scene render-space, Y→Z flip, trajectory polylines, mote
  sampling, density extents), `cockpit-scene-overlay-model` (lens + Layers-toggle
  rules, replay selectors, tones), `cockpit-minimap-model` (plan projection +
  click inverse).
- `useCockpitReplay` binds the overlays to the guest-flow replay artifact
  (worker → fallback), gated to the Flow/Evidence lenses and cached.
- In-canvas R3F: `CockpitSceneOverlays` (flow paths, glowing agent motes,
  density patches, route-conflict markers, heritage/wall buffer band, lighting
  probe placeholder), `CockpitEvidenceBeam` (evidence→scene gold light column
  raised on marker hover/focus), `CockpitCameraFocus` (eased recentre via the
  default OrbitControls). Mounted in `PlannerScene`.
- `CockpitMinimap` (cockpit stage): live top-down plan of real placed furniture,
  click-to-recentre. Extended into an **evidence radar** — in the Flow/Evidence
  lenses it projects the simulated route-conflict/review markers onto the plan
  (pulsing, severity-coloured, reduced-motion safe).
- Lens behaviour: Design stays clean for editing; Flow lights up flow family;
  Evidence shows review markers + heritage; Lighting shows the probe grid. The
  Phase-2 Layers menu now drives real geometry.

SAFE throughout: everything labelled *simulated / planning evidence / human
review required*; the heritage band is an explicit "confirm with venue" guide
and the lighting probes an explicit placeholder — no surveyed/certified claims,
no fabricated evidence→object mapping.

Verified green and committed to `master` (then pushed): types build + 1579
tests; api typecheck + 1794 tests; web typecheck + lint + 2241 tests + production
build. The earlier "untracked" note above is superseded — these files are now on
`master` (`df71321` overlays/minimap, `3f44a3b` minimap evidence radar).

Still unverified: a live browser pass of the cockpit (the Playwright-extension
navigation was unavailable this session). The underlying splat render is already
proven on `/dev/trades-hall-visual?room=reception-room`.

## T-457 supplier portal v1

- Built the public supplier portal at `/supplier-share/:token` with typed web
  client `packages/web/src/api/supplier-coordination.ts`, route wiring in
  `packages/web/src/router.tsx`, and the supplier-safe page/CSS in
  `packages/web/src/pages/SupplierPortalPage.{tsx,css}`.
- Extended `SupplierSafePackViewSchema` with
  `changesSincePreviousHandoff`, and updated the public supplier-share API to
  resolve change-summary counts from `snapshot_diffs` while preserving the
  existing source snapshot label/version/hash prefix/digest references.
- The portal displays only supplier-scoped handoff information: assigned
  requirements, arrival windows, supplier contact note, source references,
  previous responses, and acknowledgement state. It supports acknowledge and
  clarification-request submissions; clarification requests require a note.
- Added regression coverage in
  `packages/web/src/__tests__/SupplierPortalPage.test.tsx` for safe rendering,
  no internal leakage, acknowledgement posting/refetch, note-gated
  clarification requests, already-acknowledged state, unavailable state, and
  schema boundary validation.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` so T-457 is
  marked done.
- Verified:
  - `pnpm --filter @omnitwin/types run build`;
  - `pnpm --filter @omnitwin/types exec vitest run src/__tests__/supplier-coordination.test.ts` - 1 file / 6 tests passed;
  - `pnpm --filter @omnitwin/api run typecheck`;
  - `pnpm --filter @omnitwin/api exec vitest run src/__tests__/supplier-coordination-schema.test.ts src/__tests__/supplier-coordination-routes.test.ts` - 2 files / 14 tests passed;
  - `pnpm --filter @omnitwin/web run typecheck`;
  - `pnpm --filter @omnitwin/web exec vitest run src/__tests__/SupplierPortalPage.test.tsx` - 1 file / 7 tests passed;
  - source scan for `as unknown`, `any`, TODO/FIXME, and stray `console.log`
    across the touched supplier portal files returned no matches;
  - Browser MCP was unavailable (`Browser is not available: iab`), so
    Playwright was used against the local Vite server at
    `http://127.0.0.1:5175/supplier-share/supplier-token` with intercepted
    supplier-safe API responses. Desktop, mobile, and acknowledged-state visual
    smoke passed with no console/page errors and no internal-leak marker.

Screenshots captured for local review:

- `C:\Users\blake\AppData\Local\Temp\venviewer-supplier-portal-desktop.png`
- `C:\Users\blake\AppData\Local\Temp\venviewer-supplier-portal-acknowledged.png`
- `C:\Users\blake\AppData\Local\Temp\venviewer-supplier-portal-mobile.png`

Remaining platform gaps after T-457: staff-side supplier pack authoring can be
polished further on top of the T-435 API foundation; T-459 still needs live
contract/e-sign/payment/accounting hooks; T-437 post-event learning and T-462
enterprise analytics remain open; T-091 real Trades Hall runtime evidence
remains open.

## T-454 AWS G6e XGRIDS processing lane dry run

- Tightened the existing operator runbook
  `docs/operations/aws-g6e-xgrids-processing-runbook.md` with a current dry-run
  pointer and updated date.
- Added `docs/operations/aws-g6e-xgrids-dry-run-2026-06-15.md` as the dated
  dry-run record for the high-RAM XGRIDS / PortalCam processing lane.
- The dry run validated the lane at the documentation and registration-contract
  boundary: inputs, staging directories, output artifacts, hashes, R2 paths,
  AssetVersion/RuntimePackage registration steps, internal runtime QA URLs,
  shutdown/cost controls, failure gates, and claim boundaries.
- Cross-checked the runbook routes against the current implementation and tests:
  `/admin/assets/capture-session`, `/admin/assets/register-version`,
  `/admin/assets/register-runtime-package`,
  `/assets/runtime-packages/latest`, and `/admin/assets/rooms`.
- No AWS instance was launched, no XGRIDS/Lixel processing was run, no R2 object
  was uploaded/listed, and no room asset was registered or promoted by this dry
  run.
- Updated `docs/state/tasks.md` and `docs/diagrams/task-graph.md` so T-454 is
  marked done. T-460 is still not ready because T-453 remains in progress.

Verification:

- `pnpm --filter @omnitwin/api exec vitest run src/__tests__/assets.test.ts`
  passed - 1 file / 25 tests.
- `pnpm --filter @omnitwin/api run typecheck` passed.
- `rg` checks confirmed every route named in the dry-run record exists in
  `packages/api/src/routes/assets.ts` or the matching web API client.
- `git diff --check` passed with CRLF warnings only.

Remaining T-091/T-453 work: live G6e console checks and room processing are
still operator actions; Reception Room still needs signed room-local transform,
visual QA review, exposure approval, and review records before any public
runtime claim.

## T-453 Reception Room runtime QA record

- Added shared runtime QA evidence contracts in
  `packages/types/src/runtime-qa-record.ts`, exported through
  `@omnitwin/types`. The schema records package id, room, source bundle hash,
  Spark load posture, view transform posture, bounded camera profile, required
  QA checks, limitations, and public exposure decision.
- Added `packages/types/src/__tests__/runtime-qa-record.test.ts` covering the
  current Reception Room QA record, required check coverage, passed-check
  evidence refs, unsafe wording rejection, approximate-transform boundaries,
  and the stricter path required before public exposure can be allowed.
- Added `docs/operations/reception-room-runtime-qa-record-2026-06-15.md` as the
  dated operations record for runtime package
  `71687e9e-c23d-4f51-b3dd-a6a82c97978d`: source hash, `3,491,322` splats,
  seven served room chunks, `env.sog` excluded, approximate view transform,
  camera profile, current QA checks, and blocked public exposure.
- Updated `docs/operations/reception-room-runtime-intake-2026-06-13.md`,
  `docs/state/tasks.md`, and `docs/diagrams/task-graph.md` so T-453 reflects
  the new QA record while remaining in progress.

Verification:

- `pnpm --filter @omnitwin/types run typecheck` passed.
- `pnpm --filter @omnitwin/types exec vitest run src/__tests__/runtime-qa-record.test.ts`
  passed.
- Source scan over the touched runtime QA TypeScript files returned no `any`,
  `as unknown`, TODO/FIXME, or `console.log` matches.

Remaining T-453 work: the current Reception Room transform is still an
approximate view transform. T-453 cannot close until signed room-local transform
evidence, formal visual QA review, LCC2/LOD loader decision, and exposure review
are recorded.
