# 2026-06-15

## Reception Room runtime camera arrival

- Continued the registered Reception Room camera-control pass from T-091/T-091A runtime QA work.
- Added an explicit bounded camera arrival profile for the Reception Room runtime view: arrival position `[0.25, 7.15, 14.1]`, arrival target `[0, 1.2, -4]`, duration `1400ms`, settling into position `[0.2, 6.2, 13.4]` and target `[0, 0.9, -4.15]`.
- Updated the R3F runtime camera rig so the arrival uses preallocated vectors, clamps throughout the move, and cancels immediately when OrbitControls starts a user interaction.
- Extended focused regression coverage for the arrival pose, bounds, and user-interaction cancel hook.
- Updated the Reception Room runtime intake note with the current arrival values and the transform limitation reminder.
- Verified the visual route on system Edge with Playwright fallback because the in-app Browser was unavailable as `iab`; screenshots are `output/playwright/reception-room-camera-arrival-settled.png` and `output/playwright/reception-room-camera-arrival-after-drag.png`.

T-091 and T-091A remain open. This improves runtime QA presentation and navigation only; the Reception Room transform remains approximate until signed room-local alignment, LCC2/LOD strategy, visual QA record, and exposure review are complete.

## Cockpit typecheck unblock

- Fixed the stale cockpit overlay projection test fixture so `sampleTrajectoryAtProgress` receives the current plain replay-point shape `{ x, y }` instead of the timed trajectory shape `{ x, y, t }`.
- Confirmed the cockpit scene-overlay model module resolves and the model selectors/layer rules typecheck.
- Tightened the overlay model so static heritage-only overlays do not wake the replay worker, and wired density patches to the replay artifact's actual `cellSizeM`.
- Reviewed the adjacent minimap/focus path and added a guard/test so the camera focus frame loop does not crash before OrbitControls is registered.
- Restored full `@omnitwin/web` typecheck.

Note: the cockpit scene overlay component, evidence beam, camera focus, minimap, replay hook, projection/model helpers, and their focused tests are currently untracked in this working tree. Include them intentionally with the cockpit overlay work before committing.

## Planner cockpit Phase 3 — overlays, evidence beam, minimap radar (shipped)

Promoted the cockpit's spatial layer from the dev page's percentage-positioned
2D overlays to real, world-anchored R3F geometry living inside the live `/plan`
canvas, so it pins to the floor and tracks the camera under orbit/pan/zoom.

- Pure, unit-tested mappers in `lib/`: `cockpit-overlay-projection` (replay
  metre-frame → scene render-space, Y→Z flip, trajectory polylines, mote
  sampling, density extents), `cockpit-scene-overlay-model` (lens + Layers-toggle
  rules, replay selectors, tones), `cockpit-minimap-model` (plan projection +
  click inverse).
- `useCockpitReplay` binds the overlays to the guest-flow replay artifact
  (worker → fallback), gated to the Flow/Evidence lenses and cached.
- In-canvas R3F: `CockpitSceneOverlays` (flow paths, glowing agent motes,
  density patches, route-conflict markers, heritage/wall buffer band, lighting
  probe placeholder), `CockpitEvidenceBeam` (evidence→scene gold light column
  raised on marker hover/focus), `CockpitCameraFocus` (eased recentre via the
  default OrbitControls). Mounted in `PlannerScene`.
- `CockpitMinimap` (cockpit stage): live top-down plan of real placed furniture,
  click-to-recentre. Extended into an **evidence radar** — in the Flow/Evidence
  lenses it projects the simulated route-conflict/review markers onto the plan
  (pulsing, severity-coloured, reduced-motion safe).
- Lens behaviour: Design stays clean for editing; Flow lights up flow family;
  Evidence shows review markers + heritage; Lighting shows the probe grid. The
  Phase-2 Layers menu now drives real geometry.

SAFE throughout: everything labelled *simulated / planning evidence / human
review required*; the heritage band is an explicit "confirm with venue" guide
and the lighting probes an explicit placeholder — no surveyed/certified claims,
no fabricated evidence→object mapping.

Verified green and committed to `master` (then pushed): types build + 1579
tests; api typecheck + 1794 tests; web typecheck + lint + 2241 tests + production
build. The earlier "untracked" note above is superseded — these files are now on
`master` (`df71321` overlays/minimap, `3f44a3b` minimap evidence radar).

Still unverified: a live browser pass of the cockpit (the Playwright-extension
navigation was unavailable this session). The underlying splat render is already
proven on `/dev/trades-hall-visual?room=reception-room`.
