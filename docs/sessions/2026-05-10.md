# 2026-05-10

## T-404 planner cinematic command/performance deep dive pass

- Completed T-404 as a focused planner UX/performance slice rather than a broad product rewrite.
- Added a desktop cinematic `PlannerCommandDeck` that derives its state from the existing catalogue, placement, selection, markup, and camera-reference stores.
- Routed command-deck catalogue/markup/select requests through `planner-toolbar-events` so `VerticalToolbox` remains the single owner of panel/tool state.
- Added contextual actions for empty planning, active catalogue placement, laser markup, human POV, selected furniture, grouping/ungrouping, deletion, table cloths, and dinner table settings.
- Added focused unit coverage and editor E2E coverage for the new command surface.
- Verified web lint, typecheck, production build, focused unit tests, serialized editor E2E, and a local browser screenshot smoke.

No public copy, public photoreal claim, T-091/T-091A runtime asset state, backend behavior, auth/upload/security logic, or package naming changed.

## T-406 blank fast planner hotfix

- Completed T-406 as an immediate correction to the T-405 live feedback.
- Removed the automatic 132-item starter proposal from new public Grand Hall drafts so `/plan` opens as an empty editable hall again.
- Deleted the now-unused starter proposal generator and its test.
- Kept the lightweight spatial HUD/camera opening pose, but it now reports the actual empty state until users place furniture.
- Updated the public-config E2E path to assert `/plan` creates a blank hall and does not attach the Send panel until furniture exists.
- Verified focused editor-store/HUD tests, web lint, typecheck, production build, focused public-config E2E, and audit.

No public copy, T-091/T-091A runtime asset state, backend behavior, auth/upload/security logic, or package naming changed.

## T-405 cinematic planner starter proposal and event HUD pass

- Completed T-405 as the next visible `/plan` opening-state pass toward Blake's high-end planner reference.
- Added a real editable Grand Hall starter proposal for newly-created public Grand Hall drafts: 8 dressed dining rounds, a dressed central banquet row, 116 chairs, and 132 placed items total.
- Added a desktop right-side `PlannerSpatialHud` that derives spaces/capacity/dressed-table counts from the actual placement store instead of static mock data.
- Lifted the default desktop camera into a fuller top-down planning-board composition so a fresh layout opens closer to a complete room overview.
- Added focused unit coverage for starter proposal generation, HUD stats, editor-store seeding, and CameraRig framing, plus public-config E2E coverage for the `/plan` auto-create path.
- Verified web lint, typecheck, production build, focused unit tests, public-config E2E, audit, and a local Chromium screenshot smoke with mocked API responses and no console/request failures.

No public copy, public photoreal claim, T-091/T-091A runtime asset state, backend behavior, auth/upload/security logic, or package naming changed.
