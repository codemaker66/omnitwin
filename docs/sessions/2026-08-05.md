# Session log — 2026-08-05

## T-500/T-505 · Autonomous follow-up (Blake AFK — no-GUI items only)

Resumed conversation (T-499/T-500 lineage). Executed everything that needs no
owner at the screen; full detail in
`docs/operations/portalcam-raw-audit-2026-07-16.md` §8.

- **Spark knob pin (read-only; no code edits — runtime files belong to the
  parallel workstream):** production planner renders at **DPR 0.75 on every
  device tier** (`DESKTOP/TABLET/PHONE_PLANNER_DPR = 0.75`) and passes **no
  Spark render profile** — `/plan` runs Spark 2.0 defaults for
  maxSh/blur/LOD/sorting. The only pinned profile in the tree
  (`reception-fixed-fine-review-v1`) is the correct template for our data:
  its `blurAmount 0.3 / preBlurAmount 0` pairing matches the tiles'
  `antialias:false`. Four-view protocol amended: capture view D at stock AND
  at DPR `[1,2]` to separate the 0.75× pipeline from the asset ceiling.
- **Frame-cache hunt:** `F:\gaussian splat -- xgrids\Trades_Hall` is an
  empty LCC *Editor* project shell (app 1.13.1, 2026-06-04) — USDA stubs,
  empty cache/history/temp. The real LCC Studio reconstruction workspace
  (336.7 MB built model, any frame cache) is on C:; needs an owner-approved
  mount or the manual check in audit doc §6.6.
- **T-505 review pack staged:**
  `F:\VenviewerReconstructionWork\reception-preview-cloud-2026-08-05\` —
  `reception-preview.ply` (175,237 pts; decode of the open potree v2
  preview verified byte-exact against metadata bounds), top-down /
  wall-band / two elevation PNGs, `provenance.json` (SHA-256 sources,
  authority: none). One human confirmation of the room outline satisfies
  the T-505 human-review blocker; the Foundry lane can then run the E57
  crop/ICP from the PLY.
- Ledger: T-500 and T-505 rows appended additively (both still owned by
  their existing statuses: in-progress / blocked). No push, no code
  changes, no T-091 movement.
- Date-hygiene note: the review pack was first written with the previous
  turn's date (07-16); caught via file mtimes against the environment
  clock and regenerated same run as 2026-08-05 — the
  "resumed-conversation date drift" rule held because the clock was
  checked before any repo dating.

**Still needs Blake (the complete list):** (1) LCC Studio at the PC — license
tier + where the "$800/yr" quote came from, quality-scale ceiling, master-PLY
export; (2) four-view screenshots (or grant screen control and do it
guided); (3) approve a C: mount so the LCC workspace frame-cache hunt can run;
(4) 60 seconds: look at the two top-down PNGs in the review pack and confirm
"yes, that's the Reception Room" — that unblocks T-505's review requirement;
(5) standing: the T-001 RunPod console hour.

## T-505 · Human review recorded; floor-boundary polygon derived (later same day)

- Blake reviewed the two projection PNGs from his phone and confirmed the
  Reception Room outline (item 4 above — done). Recorded with honest
  provenance in `review-record-2026-08-05.json` (relayed verdict; scope =
  room identity only — explicitly not scale, alignment, contact, controls,
  calibration, rights, or authority; SHA-256 of both reviewed PNGs pinned).
- Derived the second accepted unblock artifact form:
  `floor-boundary-polygon.json` **v2** — floor-coverage footprint method
  (floor slice → 5cm occupancy → morphological close → max-enclosed-area
  contour → RDP 7cm), 108 vertices, 107.7 m², visually verified via
  `floor-boundary-overlay.png`. **v1 (wall-band contour) traced interior
  clutter, failed the visual check, and was rejected** — the rejection is
  documented in the polygon's `method` field per Law 1.
- Ledger: **T-505 `blocked → not-started`** — the row's stated unblock
  condition ("human-reviewed room-only 3D crop or floor-boundary polygon")
  is met in both forms. All standing Foundry-lane constraints restated in
  the row: validation held back, scans 126/129/141 frozen, verified
  controls/calibration/rights before any authority claim, T-506
  format/source-rights gate, T-502 still gated on a reviewed
  TransformArtifactV0.
- Deliberately NOT done here: the E57-side crop/ICP — the sandbox has no
  pye57/open3d, the 20.5 GB E57 belongs on the workstation, and the
  frozen-scan discipline is the Foundry lane's to execute.

## T-505 · E57 crop/ICP executed (Foundry lane, later same day)

Blake's relayed room-identity confirmation landed (PNG hashes re-verified
against `review-record-2026-08-05.json`), unblocking the §4 workflow. The
"no pye57/open3d" blocker above was overcome without network: an own
ASTM-E2807 reader (numpy+stdlib; paged CRC logical stream, XML tail,
CompressedVector packet walk; decoded counts verified against XML) plus a
gravity-aligned 4-DoF FFT global search standing in for the unavailable
Open3D FPFH+RANSAC — both deviations declared in the receipt with a
workstation confirmation step left open.

- Global registration resolved the floor ambiguity by evidence (venue floor
  z=-3.60 at 167.8°: 91.2% matched vs 35.9% for the runner-up).
- Station-in-polygon selection yielded E57 sweeps 122–144 — independently
  matching the July contact sheet; **frozen 126/129/141 never decoded,
  fitted, or included**.
- Crop: 20 sweeps decoded full-res (5.2M valid pts each), bbox+0.5m crop,
  1 cm voxel merge → `reception-e57-crop-1cm.ply` (10,706,822 pts,
  sha256 `d6b8a6df…6848`; full-res regeneration recipe in the receipt).
- Final fit (rigid; no scale solved): forward median **9.0 mm**, RMSE
  **14.4 mm**, p95 **29.6 mm**, 76.9% within 50 mm, 77.0% matched @30 cm —
  vs the July baseline's 319.6/334.5 mm RMSE at 14.5/18.4% within 50 mm.
  Reverse median 37.1 mm, bounded by preview point spacing; the July
  mirror symptom is absent. Three fixed-view overlays rendered; wall
  traces coincide visually.
- Outputs in `…\reception-preview-cloud-2026-08-05\e57-crop-icp-2026-08-05\`:
  crop PLY, three overlays, `crop-icp-result-2026-08-05.json` (full method,
  hashes, deviations, gates), `proposed-transform-artifact-v0-draft.json`
  (schema-adjacent; the TransformArtifactV0 schema requires a human
  reviewer, so promotion awaits Blake's transform review). Repo report:
  `docs/reports/reception-room-e57-crop-icp-2026-08-05.md`.
- Gates intact: candidate fit only, authority none; control-point column
  pending T-480; T-502 still gated on the reviewed TransformArtifactV0;
  no rights/calibration claims. Separately: the T-508 **P1-1 custody
  commit remains HELD** per Blake's instruction — not performed.
