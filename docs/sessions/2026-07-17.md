# Session log — 2026-07-17

## T-524 · e2e static-analysis coverage (Slice-6 review Finding 2)

Executed just past midnight, continuing the Slice-6 session. Commit
`0bf09f48`; ledger row T-524 done.

- **The gate itself:** new `packages/web/e2e/tsconfig.json` (strict base
  inherited, DOM lib for evaluate closures, `node` + `vite/client` types —
  specs import src modules that use `import.meta.env` — noEmit,
  `playwright.config.ts` included). Package scripts now enforce it:
  `typecheck` chains `tsc -p e2e --noEmit` (separate process per the
  Windows heap gotcha), `lint` covers `e2e/`. 31 previously-unchecked
  files (28 specs + 3 support) now under both gates.
- **Zero rule weakening.** The config-protection hook blocked an
  eslint-config e2e block — the right outcome: every strictTypeChecked
  rule (incl. no-floating-promises, all no-unsafe-*) now holds on e2e,
  stricter than the unit-test relaxation block. The only accommodations
  are six inline `no-console` disables, each with a reason, on deliberate
  runner-output logs (smoke version/cleanup reports, CARD-A1/A2 timing
  evidence).
- **18 findings fixed in source:** typed `declare global` Window
  augmentation for the exposeFunction bridge (button-audit); dead
  `snapshotEnvelopeFixture` deleted + real null-narrow replacing a
  non-narrowing `expect().not.toBeNull()` (keyboard-focus); `"placeholder" in
  element` narrow + readonly-spread + six `String()` template wraps
  (production-live-route-pass); four route handlers now `await`
  `route.fulfill()`/`fallback()` instead of `void` (navigation, route-pass
  ×2, sspp-hardening — Playwright-recommended form); proven-dead `?? ""`
  dropped (accessibility-audit).
- **BONUS — latent CI breaker repaired:** the committed api manifest has
  carried `pg@^8.22.0` (Slice-4's drizzle-kit TCP driver) with NO lockfile
  entries — `pnpm install --frozen-lockfile` failed on every fresh checkout
  of the branch (`ERR_PNPM_OUTDATED_LOCKFILE` reproduced in a detached
  HEAD worktree). Because the shared worktree's lockfile also carries other
  sessions' uncommitted resolution changes, the committed lockfile was
  regenerated CLEAN-ROOM: detached `git worktree` at HEAD + only the web
  manifest change + `pnpm install --lockfile-only`, then frozen-verified
  and staged via hash-object. **Reusable trap: a manifest committed without
  its lockfile breaks frozen CI; in a shared tree, repair via clean-room
  regeneration, never by committing the union lockfile.**
- **Verification:** `tsc -p e2e` 0 errors · `eslint e2e/` 0 problems · web
  units 3218/3218 · navigation e2e 7/7 warm (one cold-compile flake passed
  in isolation and on re-run; the test exercising the awaited-fulfill path
  green both runs) · production build green (under the resolver
  workstream's new in-flight VITE_API_URL assert, var supplied).
- **Foreign in-flight flags (attributed, untouched):** `pnpm lint` shows
  one error in `src/stores/editor-store.ts` inside the G4 session's
  uncommitted hunk (`actionEmitter` — zero hits in HEAD); the
  `vite.config.ts` production build assert is the resolver workstream's
  uncommitted change.
- **Deploy checklist updated:** master drifted 3 commits past the
  reconciliation within hours (walk-the-room, room dossiers, image
  ladder — all shipped through Vercel's no-CI-wait path; master's pins
  still lack cockpitImport, CI still red). §2 now mandates a drift check +
  re-merge before §4's push; §4 notes the lockfile repair as part of the
  first green CI.
- Housekeeping: my Playwright-spawned Vite on 5173 killed (PID 35444);
  Slice 7 (first-live-week operations) remains parked on Blake's owner
  deploy.
