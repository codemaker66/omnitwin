# Session log — 2026-07-17

## T-524 · e2e static-analysis coverage (Slice-6 review Finding 2)

Executed just past midnight, continuing the Slice-6 session. Commit
`0bf09f48`; ledger row T-524 done.

- **The gate itself:** new `packages/web/e2e/tsconfig.json` (strict base
  inherited, DOM lib for evaluate closures, `node` + `vite/client` types —
  specs import src modules that use `import.meta.env` — noEmit,
  `playwright.config.ts` included). Package scripts now enforce it:
  `typecheck` chains `tsc -p e2e --noEmit` (separate process per the
  Windows heap gotcha), `lint` covers `e2e/`. 31 previously-unchecked
  files (28 specs + 3 support) now under both gates.
- **Zero rule weakening.** The config-protection hook blocked an
  eslint-config e2e block — the right outcome: every strictTypeChecked
  rule (incl. no-floating-promises, all no-unsafe-*) now holds on e2e,
  stricter than the unit-test relaxation block. The only accommodations
  are six inline `no-console` disables, each with a reason, on deliberate
  runner-output logs (smoke version/cleanup reports, CARD-A1/A2 timing
  evidence).
- **18 findings fixed in source:** typed `declare global` Window
  augmentation for the exposeFunction bridge (button-audit); dead
  `snapshotEnvelopeFixture` deleted + real null-narrow replacing a
  non-narrowing `expect().not.toBeNull()` (keyboard-focus); `"placeholder" in
  element` narrow + readonly-spread + six `String()` template wraps
  (production-live-route-pass); four route handlers now `await`
  `route.fulfill()`/`fallback()` instead of `void` (navigation, route-pass
  ×2, sspp-hardening — Playwright-recommended form); proven-dead `?? ""`
  dropped (accessibility-audit).
- **BONUS — latent CI breaker repaired:** the committed api manifest has
  carried `pg@^8.22.0` (Slice-4's drizzle-kit TCP driver) with NO lockfile
  entries — `pnpm install --frozen-lockfile` failed on every fresh checkout
  of the branch (`ERR_PNPM_OUTDATED_LOCKFILE` reproduced in a detached
  HEAD worktree). Because the shared worktree's lockfile also carries other
  sessions' uncommitted resolution changes, the committed lockfile was
  regenerated CLEAN-ROOM: detached `git worktree` at HEAD + only the web
  manifest change + `pnpm install --lockfile-only`, then frozen-verified
  and staged via hash-object. **Reusable trap: a manifest committed without
  its lockfile breaks frozen CI; in a shared tree, repair via clean-room
  regeneration, never by committing the union lockfile.**
- **Verification:** `tsc -p e2e` 0 errors · `eslint e2e/` 0 problems · web
  units 3218/3218 · navigation e2e 7/7 warm (one cold-compile flake passed
  in isolation and on re-run; the test exercising the awaited-fulfill path
  green both runs) · production build green (under the resolver
  workstream's new in-flight VITE_API_URL assert, var supplied).
- **Foreign in-flight flags (attributed, untouched):** `pnpm lint` shows
  one error in `src/stores/editor-store.ts` inside the G4 session's
  uncommitted hunk (`actionEmitter` — zero hits in HEAD); the
  `vite.config.ts` production build assert is the resolver workstream's
  uncommitted change.
- **Deploy checklist updated:** master drifted 3 commits past the
  reconciliation within hours (walk-the-room, room dossiers, image
  ladder — all shipped through Vercel's no-CI-wait path; master's pins
  still lack cockpitImport, CI still red). §2 now mandates a drift check +
  re-merge before §4's push; §4 notes the lockfile repair as part of the
  first green CI.
- Housekeeping: my Playwright-spawned Vite on 5173 killed (PID 35444);
  Slice 7 (first-live-week operations) remains parked on Blake's owner
  deploy.
## G4 Slice 1 (T-522) — Action envelope + log core (shipped)

- See docs/plan/g4-action-log-program.md "Slice 1 — SHIPPED" for the full
  record: schema, seq-based gesture sealing, append-only store, wiring.
- The review cycle earned its keep twice: the reviewer's two CRITICALs
  (id-remap identity churn; cap-time evict masking) forced the structural
  seq redesign, and our own integration test then caught the seq-restart
  defect at config boundaries — fixed via beginActionLogForConfig.
- Zero behaviour change proven: editor-store-history + editor-history pinned
  suites untouched and green throughout; 89 tests across the slice's five
  suites; both packages' full suites + typecheck + lint green.

## T-525 · Re-reconcile with master's fresh-landing series (Diary lane)

Merges `eb127615` + `53541ad0` — the branch again contains master's head
(`eb2f7431`; master moved TWICE during the operation: retrigger commit
mid-fetch, dispose-churn fix mid-merge). Zero conflicts (master's 40 files
are all pages/fresh/* + image ladder + one gotcha doc; no overlap with
Diary files or any dirty worktree path — verified by set intersection
before merging).

- **Gates on the twice-merged branch, all green:** web typecheck (src +
  e2e projects) · web lint (src/ e2e/ — fully clean; G4's editor-store
  error cleared when their slice-1 commit landed) · web units 3233/3233 ·
  api units 2622 (+5 skipped) · web production build 21.2s · diary live
  e2e 5/5 · production smoke 4/4.
- **Clean-room discovery (the big one):** the COMMITTED api tree does not
  build from a fresh checkout. `routes/reconstruction-foundry.ts` and
  `services/reconstruction-foundry-integrations.ts` were swept into commit
  `8af609ea` (Diary Slice-1 "gate unblocks" — our lane's own sweep), but
  they import `services/reconstruction-foundry.ts` (STILL UNTRACKED) and
  seven `FOUNDRY_*` env fields that live only in the Foundry session's
  uncommitted env.ts hunk. Every worktree build masked it for six days;
  the detached-worktree build (T-524's technique) exposed it. CI on any
  push fails at api build until the Foundry workstream commits the module
  + env block together — their running session has been messaged with the
  exact fix + self-check recipe; the deploy checklist §2 now carries it
  as a pre-push BLOCKER.
- Also flagged to them: their new untracked verify-foundry script +
  tools/reconstruction-foundry/local-app.ts fail tsc under the repo's
  current @types/node resolution ('once' on ChildProcess Pick types) —
  pin @types/node in the tools package when committing.
- Union-tree api build fails only in those untracked foreign files;
  committed-content api errors are exactly the coherence set above. The
  merge itself touched no api file.

## T-508 · SPZ Source Facts V2 local super-app slice

Completed the next immutable source-understanding profile without widening V1
and without any cybersecurity, cloud, deployment or publication dependency.

- Added Universal Source Facts V2, Source Readiness V2 and Operator Evidence
  Checklist V2 with distinct schema versions and digest domains. V1 now has a
  literal-byte, fixed-time golden proving the exact Source Facts → Readiness →
  Checklist serialization/digest chain remains unchanged when SPZ stays
  outside V1.
- Added same-handle SPZ inspection for legacy v1-v3 gzip and current v4. Legacy
  covers exact per-version layouts, one gzip member, CRC/ISIZE, EOF and declared
  bounded ILV extensions. V4 covers the plaintext header, bounded extensions,
  exact TOC, five/six independent Zstandard ranges and exact compressed-input
  consumption. Gaussian values are never decoded.
- Published caps: 64 GiB source/decompressed, 1 MiB gzip header, 16 MiB and 256
  extension records, v4 compression plausibility 1024. Oversized gzip headers
  return a resource-limit code. A namespace feature test keeps package-root and
  legacy use working on Node versions without `createZstdDecompress`; v4
  returns `SPZ_V4_ZSTD_RUNTIME_UNAVAILABLE`.
- Closed final audits: state-neutral unknown wording on failed inspections,
  deep-frozen SPZ registries, versioned V2 evidence-code aliases, V2 issuer and
  caller-supplied collector removed from the package root, collector-level
  cancellation fail-close, max-u64 TOC taxonomy, concatenated-frame rejection,
  legal gzip optional-header fixtures and a mocked minimum-runtime smoke. Final
  re-audits found no actionable P0-P3.
- Migrated the local app to V2 and proved canonical downloads. A real v4 fixture
  with two extensions, SH degree 2 and six Zstandard streams survives the
  nested state/download path exactly.
- Reran the clean eight-file Reception set read-only: 52,911,630 source bytes,
  3,459,703 declared Gaussians, 69,194,188 decompressed bytes, eight unique
  source fingerprints. Receipt stayed `3fa54eba75cbb22d4fcc947027641f156472df546d553012b297e32bde875a58`;
  final facts/readiness/checklist are `c665eb1350d0a39197d425167c8e3e3f0557c5186f194132699ed1280b3cb615`,
  `07132d0589254211312cd72b2780b11a815c5a0afcfae05dbdc287b29e68976f`
  and `94bf31b16fe4c812f07637077d98e85acc5ce85a52fa18e89930ec5e9ef94de4`.
- Refreshed and visually checked 1280×720 and 320×844 captures. Both rendered
  all eight Source Facts cards with no page overflow or console warning/error;
  hashes are `d6025ef1…` and `8e2c3de2…`.
- Final gates: SPZ contract 52/52; core 459 passed/1 skipped across 34 files;
  local app 172/172 across 14 files; both complete lints, typechecks and builds
  passed.

Evidence is in
`docs/reports/reception-room-spz-source-facts-v2-evidence-2026-07-17.json`.
T-508 remains active. The next bounded profile is Gaussian PLY; image/video
container facts and capture-role classification follow as a separate slice.

## G4 Slice 2 (T-522) — coverage widening

- Markup strokes, event details, and the lighting rig now emit Actions with
  named intents and restoring inverses through one seam (surface-actions
  builder + logPlannerAction wiring; actor context unified with slice 1).
  Full record incl. the intent table, two Blake-Clause audit corrections
  (object notes were already engine-covered; the 42-case blueprint reducer
  serves the unpersisted demo sandbox), and the review cycle in
  docs/plan/g4-action-log-program.md "Slice 2 — SHIPPED".
- The reviewer's Block earned its keep again: a cross-config save race
  that would fabricate a record in the wrong config's log, and an
  unisolated audit channel that could misreport "Failed to save" — both
  fixed with reproductions pinned, plus a latent updater-atomicity
  regression and a guard-pattern deviation.
- Zero behaviour change: every surface's pre-existing suite ran alongside
  as the pin; web full suite + typecheck + lint green.

## T-508 · Gaussian PLY Source Facts V3 local super-app slice

Completed the next immutable local source-understanding profile. This is
ordinary offline reconstruction-product engineering; no cybersecurity,
credential, cloud, deployment, publication or source-mutation work was used.

- Added distinct Universal Source Facts V3, Source Readiness Map V3 and
  Operator Evidence Checklist V3 schemas and digest domains. V1/V2 remain
  byte-frozen, and the V2 chain golden stays green.
- Added a same-open-handle classic Gaussian PLY inspector for case-sensitive
  PLY 1.0 binary little-endian, exactly one fixed-width vertex element,
  required float32 Gaussian property families in arbitrary declared order,
  optional all-or-none `nx/ny/nz`, complete SH0–4 `f_rest` sets, bounded fixed
  scalar extras and exact header-plus-count-times-stride EOF arithmetic.
- The profile does not decode values. Eleven immutable unknowns retain decoded
  attributes, encoding semantics, physical bounds, units, frame, renderer
  compatibility, fidelity, provenance, accuracy, registration and rights.
  XBIN remains an atomic no-partial-evidence stop.
- Hardened format discrimination after independent review: PlayCanvas packed
  PLY needs its complete legacy/current signature; an isolated `packed_*`
  scalar stays a legal extra; encoding/scalar tokens are case-sensitive; and
  property names accept printable non-whitespace ASCII.
- Reproduced three real read-only chains: Reception LCC SH3+normals at
  496,504,970 bytes / 2,002,028 rows / 248-byte stride; Reception LCC
  SH0+normals at 134,589,707 / 1,979,247 / 68; and Brush SH3 without normals,
  in lexicographic property order, at 11,859,606 / 50,246 / 236.
- The Brush chain fingerprints are receipt `96229acc…`, Source Facts
  `2305a6ce…`, Source Readiness `7920416a…` and Checklist `f63dbdb2…`.
  Each readiness artifact has one represented lane and 12 gaps; each checklist
  has 12 requests in two groups.
- In-app browser QA exposed all 59 declared Brush properties and byte offsets
  at 1280×720 and 320×844. Both widths had no page-level horizontal overflow;
  the table remained locally scrollable; console warnings/errors were empty.
  Receipt, facts, readiness and checklist endpoints all returned exact 200
  JSON attachment responses, and the interactive download controls completed.
- Final audit hardening preserves semantic `nx/ny/nz` offsets when normal
  declarations are shuffled and replaces the receipt's substring heuristic
  with case-sensitive bounded PLY grammar. A valid header beyond the 64 KiB
  receipt probe is inspected and refined only after established same-handle
  structure; failed ordinary PLY remains outside V3.
- Gates passed: classifier 18/18; inspector 21/21; combined
  inspector/pipeline/readiness/checklist 35/35; V3 plus frozen V2 golden 48/48;
  shared types 2,110/2,110; full Reconstruction Foundry 494 passed/1 skipped
  across 37 files; local app 174/174 across 14 files; every lint, typecheck and
  build passed. Two independent final audits found no actionable P0-P3.

The durable manifest is
`docs/reports/reception-room-gaussian-ply-source-facts-v3-evidence-2026-07-17.json`.
T-508 and the broad offline super-app goal remain active. The next bounded
slice is a new immutable image/video container-facts profile with capture-role
classification kept separate from file validity.

## T-508 · Image/video Container Source Facts V4 local super-app slice

Completed the next immutable local source-understanding profile. This was
ordinary offline reconstruction-product engineering; no cybersecurity,
credential, cloud, deployment, publication or source-mutation work was used.

- Added distinct Universal Source Facts V4, Source Readiness Map V4 and
  Operator Evidence Checklist V4 schemas and digest domains; V1-V3 remain
  immutable.
- Added same-open-handle bounded inspectors for SOF0/SOF2 eight-bit Huffman
  JPEG, static PNG with CRC/order validation, and ISO-BMFF movie/video-track
  declarations. The later facts pass binds complete SHA-256, byte count,
  up-to-128-byte magic prefix and canonical media candidates back to the exact
  receipt identity before issuance.
- Container validity remains separate from DSLR, phone, Matterport, panorama,
  drone or generic role and captured, enhanced-captured, generated or concept
  provenance. Ten media unknowns carry through readiness and checklist
  artifacts.
- Real read-only replay established one 2,998,854-byte 4096×4096 baseline JPEG
  and one 15,177,808-byte 4096×4096 static PNG. The PNG replay also corrected a
  malformed earlier note by re-establishing the complete readiness digest
  `d271015fe5277dcec8a0d58f469cff82b3b2b20beb5f07cc5f71697430c040db`.
- ISO-BMFF proof is fixture-only. Its profile establishes complete top-level
  tiling plus selected movie and video-track structure, not sample-table
  completeness, `mdat` binding, sample decode or decoder compatibility.
- XBIN remains an atomic no-partial-evidence stop. The package root does not
  expose caller-supplied V4 issuance, and every artifact remains authority
  none with execution unauthorized.
- In-app browser QA on the real JPEG verified the exact V4 truth labels at
  1280×720 and 320×844 with no page horizontal overflow or console
  warning/error. All four download controls completed and their HTTP endpoints
  returned 200 canonical JSON attachments. QA caught the stale receipt
  filename; it is now regression-pinned as
  `foundry-universal-intake-receipt-v0.json`.
- Gates passed: focused V4 31/31; Reconstruction Foundry 539 passed/1 skipped
  across 41 files; local Foundry CLI 214/214 across 16 files; focused local app
  44/44; shared types 2,110/2,110; all lint, typecheck and build gates passed.
  The final independent audit found no actionable P0-P3.

The durable manifest is
`docs/reports/reception-room-image-video-container-source-facts-v4-evidence-2026-07-17.json`.
T-508 and the broad offline super-app goal remain active. The earlier
source-understanding roadmap's next bounded slice is calibration and trajectory
facts.

## T-508 · Calibration / Trajectory Source Facts V5 local super-app slice

Completed the next immutable local source-understanding profile. This was
ordinary offline reconstruction-product engineering. Cybersecurity,
credentials, cloud deployment, publication and source mutation were out of
scope and not required.

- Added distinct Universal Source Facts V5, Source Readiness Map V5 and
  Operator Evidence Checklist V5 schemas/digest domains while preserving
  immutable V1-V4 meanings.
- Added a bounded same-open-handle UTF-8 CSV/JSON inspector. Established facts
  bind complete byte length and SHA-256 and cover complete CSV record structure
  or duplicate-key-safe bounded JSON syntax/tree shape only.
- Kept field/key meanings, timestamps, cadence, frame/CRS/units, transform and
  quaternion conventions, calibration applicability, synchronization,
  accuracy/drift, provenance, rights, registration and authority as fixed
  unresolved facts. Numeric values remain exact source lexemes rather than
  interpreted measurements.
- Hardened every reviewed boundary: aggregate CSV/JSON consistency, decimal
  ordering including signed zero, exact size/digest round trips,
  format-specific failure registries, cancellation non-issuance, V1-V5
  precedence collisions, ordinary-PLY no-fallthrough, canonical limit
  comparison, bounded UI preview and panel-local download status. XBIN remains
  atomic.
- Final real read-only replay established Grand Hall `poses.csv` at 3,659,287
  bytes, 42,850 records and 342,800 fields; LCC2 `poses.json` at 542,394 bytes
  and 54,351 values; and two exact E57 `poses.json` files at 39,717 and 37,780
  bytes. The E57 files share one bounded shape digest but retain different byte
  and complete artifact-chain digests; no semantic equivalence is claimed.
- Calibration remains fixture-only. The real `transforms.json` inventory item
  is outside this frozen one-candidate target, and encrypted XGRIDS calibration
  was not decrypted, renamed or treated as evidence.
- In-app browser QA covered the Grand Hall V5 view at 1280×720 and 390×844
  without page overflow or console warning/error. The three V5 downloads were
  confirmed on disk and the local listener shut down. Final preview/status and
  schema hardening is regression-covered; no second rendered-browser or saved
  screenshot claim is made.
- Gates passed: focused V5 28/28; Reconstruction Foundry 567 passed/1 existing
  skip across 44 files; local Foundry CLI/app 240/240 across 17 files; shared
  types 2,110/2,110 across 91 files; all lint/typecheck/build gates passed at
  the V5 checkpoint.
  Independent contract and parser reviews found no remaining actionable defect.
- A later concurrent workstream created the unrelated untracked
  `local-offline-normalization-preview-container-preflight.ts` after that clean
  checkpoint. Its five current ESLint findings were preserved rather than
  folded into V5; CLI typecheck/build, 240/240 tests and targeted lint for all
  V5-touched CLI files remain green.

The durable machine-readable evidence and compact report are:

- `docs/reports/calibration-trajectory-source-facts-v5-evidence-2026-07-17.json`
- `docs/reports/calibration-trajectory-source-facts-v5-report-2026-07-17.md`

T-508 and the broad OmniTwin Foundry `/goal` remain active. The next ordinary
offline source-understanding slice should inventory and freeze a distinct V6
profile for open non-Gaussian point geometry, beginning with ordinary PLY and
adding LAS/LAZ or XYZ only where real inputs and bounded contracts support it.
Do not infer units, CRS, frame, semantics, accuracy, rights or authority.

## T-526 · Coherence closure complete + drift merges (Diary lane, continued into 07-18)

Five commits across two sessions closed every committed-tree coherence
break; the branch now installs (frozen), builds and tests green from a
TRUE fresh checkout.

- **Diary lane:** 481bc2e3 (api half — foundry service module verbatim
  after byte-stability proof; seven FOUNDRY_* env fields + refines,
  excluding the production-required RUNTIME_PROFILE/PUBLIC_API_ORIGIN
  additions; 0049.sql + journal entry, retiring the hand-apply caveat)
  and 6717e373 (web half — FoundryReleaseDetail's three missing deps
  committed; the two Living Hall preview routes DE-REFERENCED from the
  committed router per the a0830f72 precedent — their feature still needs
  uncommitted LivingHallPage prop changes + two more modules, so
  completing it was not coherence surgery; routes return with their
  pages).
- **Foundry workstream** (responding to the two session messages):
  8cd027fa (their full closure — schema/routes/tests, the tools package
  with the @types/node@25 pin I flagged twice, lockfile, env examples),
  790074c5 (T-506 docs corpus), 6353a9fe (`packages/api/drizzle/*.sql
  -text` byte-pin attribute + CLI repair).
- **Verification upgraded twice:** junctioned-node_modules clean rooms
  are valid ONLY while manifests are unchanged (their 8cd027fa manifest
  changes invalidated mine — caught, redone with a real
  `pnpm install --frozen-lockfile`, 1m16s, PASSED); and deep temp-path
  worktrees break vitest startup on Windows (package-imports resolution
  vs path case) — short-path sibling worktrees for fresh-install
  verification from now on.
- **Final fresh-checkout gates at 6353a9fe:** web typecheck 0 · lint
  clean · units 3176/3176 · build green; api build 0 errors · suite
  green (the last four failures were Windows autocrlf rewriting
  byte-pinned migration sqls on checkout — LF-normalized hashes matched
  the pins exactly; their -text rule cures any fresh Windows clone;
  Linux CI was never affected).
- Drift merges: master@f4e6eadc (walk hand-feel, one file, no overlap)
  after the earlier eb2f7431 pair. Deploy checklist §2 blocker RESOLVED;
  remaining pre-push steps = drift re-check + Blake's owner actions.
