# Session log — 2026-07-13

## T-508 · Grand Hall T-486 preflight and HD Foundry foundation

- The existing T-507 artifact records an earlier decision B for Grand Hall
  anchors 000/010/020/040 with sweep 049 excluded. The bare `b` supplied during
  this continuation was not accepted as a new authenticated attestation. It
  confers no geometry, rights, release, signing or publication authority.
- Audited T-507 against independent control. No usable local Grand Hall survey
  exists. Matterport/E57/COLMAP/OBJ/GLB are one lineage; `th.nwc` has unknown
  provenance; XGRIDS control/GNSS evidence is empty/zero and the wrong room;
  the web floor plan has no survey provenance and conflicts with the current
  venue page at 10.5 m versus 10 m room width.
- Reproduced a new cubeface-rig contradiction from frozen T-507 JSON: 230
  registered candidate faces, every sweep missing at least one face, mean
  11.0598 mm, median 9.5237 mm, P95 21.1934 mm, maximum 95.6822 mm and 105
  faces over 10 mm after candidate scaling. Leave-one-sweep-out RMSE is 11.2942
  mm with 194.87 ppm scale span. These remain same-lineage diagnostics and
  block an independent accuracy claim.
- Audited `output/foundry/trades-hall-prepared`: exact release digest
  `e3525acfd76bdd89d621c6eefaae8494f9c459e52702d351545f685655a917d8`,
  QA digest `9eb006631370ba228f230018f4b05c9ad95204f681f8faf78e38b54029a8e586`,
  449 files / 498,035,687 bytes / 149 nodes. The release is already E57-native,
  all room slugs are null, and it has no evidence-role files; the T-507
  COLMAP→E57 diagnostic is not its load-bearing transform.
- Verified current official-source technology/licence status. Notable changes:
  COLMAP 4.1.0; SplatTransform 3.0.0; SPZ library v3 versus format v4; gsplat
  main changes unreleased beyond v1.5.3; 3DGRUT v1.1.0 is the latest official
  release despite v2-era README text; XGRIDS custom terms remain blocked
  pending written clearance; ArtiFixer/MeshCoder released weights remain
  noncommercial; Three Meshlets is an experimental MIT fork sample, not an
  upstream Three.js release.
- Added `omnitwin.foundry.offline-review-package.v0`: strict sorted,
  digest-bound and tamper-evident artifact index, domain-separated digest,
  canonical venue/room slugs, fixed authority `none`, offline evidence-review
  readiness, permanently non-online public/signing postures, and negative
  parsing tests against TransformArtifact, T-486 review, evidence-registration
  and signing schemas. It makes no physical-storage immutability claim.
- Added a Capture Factory `grand-hall-offline-review` command. It validates the
  exact 13-file T-507 tree, indexed byte hashes, schemas/cross-digests, three
  probe envelopes, prepared release/QA epoch, exact prepared source manifest,
  PNG fixed view and audit/intake posture. It cross-binds the bundle,
  standalones, audit, intake and release epochs and revalidates the copied tree
  before manifest creation, before promotion and after promotion.
- The initial 20-artifact package (`sha256:f829ca31…`) was superseded after
  independent review found a copied-source TOCTOU gap, incomplete epoch
  cross-binding, an absent prepared source manifest and an overstated evidence
  readiness label. Produced the replacement 21-artifact dossier at
  `C:/Users/blake/Documents/Codex/2026-07-12/new-chat/outputs/grand-hall-t486-offline-preflight-2026-07-13-r2`,
  package digest
  `sha256:d2411e3f5a0ab3206c1dc174c71005130e5cd26df6369ddd2ec6e10b5eb9a85b`.
  It includes exact prepared source manifest `fa00cecb…`; state is evidence
  review blocked on withheld complete identity evidence/rights, public approval
  not ready offline, signing not ready unsigned, authority none.
- Added control audit/report/evidence, review-gate intake, offline-package spec,
  current licence matrix and HD bake-off. Recommended stack: independent
  E57/control metric scaffold, rigid camera rigs, LiDAR-constrained MCMC/3DGUT
  captured radiance, explicit structural/hero meshes, captured hero sidecars,
  temporal deltas and strictly isolated generated derivatives.
- Verification: offline-package types 12/12, offline builder 7/7, complete
  Capture Factory suite 30/30 and complete types suite 1,880/1,880. Types build,
  Capture Factory typecheck/full lint and Foundry-focused types lint pass;
  production-path fixture and real r2 builder pass. The 22-file tree, all 21
  indexed hashes/lengths, package digest, exact source-manifest hash and
  negative T-486 parses were independently rechecked. The broad types
  `pnpm typecheck`/full lint are currently red only in unrelated, pre-existing
  dirty `runtime-composition-decision` v1 work (two test narrowing errors and
  one template-literal lint error); those user-owned edits were not changed. No
  raw mutation, training, paid compute, proprietary parsing/decryption,
  evidence registration, T-486 review mutation, signing, publication or
  promotion.
- T-508 remains in progress. No operator UI is built before an explicit A/B/C
  visual-direction selection. Independent survey, legal/rights and
  authenticated-review gates remain owner-blocked. At this checkpoint the
  durable executor and attempt ledger were still gaps; the later control-plane
  slice below closes those two foundations, not the actual reconstruction or
  enhancement workers.

## T-508 · Executable intake, plan-only routing and candidate verification

- Added a universal deterministic intake receipt for files or directories,
  including bounded signature evidence for E57, OBJ, GLB/glTF, PLY, images,
  video, COLMAP, XGRIDS containers and related inputs. Fixed a reproduced
  65,536-byte retained-header versus 64,000-character schema crash by bounding
  detector text explicitly. Detection remains classification evidence, not
  format validity, rights clearance or processing authority.
- Added an all-path, self-digested admission review/result contract. Every
  receipt path is admitted or excluded; every admitted byte count/hash binds
  the receipt; non-detector classification needs an evidence-backed override;
  raw XGRIDS `.xbin` remains technically and legally blocked. The result is a
  deterministic draft `FoundryIngestManifestV0` with authority `none`.
- Added verified atomic local staging. It repeats the complete source scan,
  copies only admitted regular files through mutation-detecting handles,
  writes exact receipt/review/result/exclusion/manifest evidence, atomically
  promotes to a nonexistent output and re-verifies root identity, canonical
  roles, exact tree, all hashes and recompiled admission. Symlinks/junctions,
  hard aliases, overlap, overwrite, source drift and post-stage tampering fail
  closed.
- Added a deterministic recipe/DAG and routing dossier for local CPU/CUDA and
  remote candidates. It checks exact manifest identity, purpose-aware rights,
  resource capacity, stale provider estimates, budget caps and immutable image
  digests. Every emitted JobSpec is literal `plan_only`; confirmation
  consumption, provider SDKs, credentials, object-store mutation and process
  execution are absent. D-016 now rejects local model-training execution even
  with otherwise plausible approval material.
- Disabled the historical RunPod shell runner before any run ID, directory,
  network, trainer or billing action. It exits 78 without an environment or
  boolean bypass because it predates JobSpec, rights, single-use confirmation,
  compute approval, cost/kill enforcement and a durable attempt ledger.
- Added strict local D-014 extracted-candidate verification. It requires the
  exact root file set and placeholder signature; rechecks every size/hash;
  parses bounded fatal-UTF-8 duplicate-key-safe JSON; requires complete,
  monotonic terminal metrics and held-out consistency; validates exact gsplat
  float32 PLY property order from `sh_degree`, vertex count against `cap_max`,
  exact stride/payload length, finite streamed values and nonzero rotations;
  rejects hard links; and performs a second exact full-digest tree pass.
  Bilateral-grid candidates fail closed because D-014 does not yet specify the
  view count, 12-channel layout, dtype/endian and serialization required to
  validate the tensor.
- A passing D-014 result is still `valid_untrusted_training_candidate`,
  release-ineligible, authority `none`. Legacy v0 lacks exact ingest-manifest,
  JobSpec, provider-plan, immutable attempt-ledger, quality-contract and
  trusted-signature bindings; execution, training, cloud dispatch, object
  storage, quality approval, evidence/AssetVersion registration, runtime use,
  signing, publication and promotion remain explicitly unauthorized.
- Added local CLI commands for receipt, admission draft, verified staging,
  plan-only routing and D-014 candidate verification. None requires cloud
  credentials. The CLI rejects unknown execution flags.
- Independent adversarial review found and closed dossier-schema forgery,
  JSONL duplicate-key, PLY terminator/mixed-newline, metric-overflow,
  whitespace-placeholder, bounded-read path-identity and host-locale ordering
  gaps. The final review reported no high-severity issue; the last crafted
  CRLF/LF PLY bypass now has a passing regression.
- Final verification: complete Reconstruction Foundry suite 13 files / 100
  tests; CLI 2 files / 13 tests; focused Foundry/offline-review types 2 files /
  29 tests with no type errors; types, Reconstruction Foundry and CLI builds;
  focused package/CLI typechecks and lint all pass. No training run or paid
  compute was launched; `state/training_runs.jsonl` remains unchanged.

## T-508 · Durable execution control and provider-result custody

- Added migration `0053_foundry_execution_control.sql` and Drizzle parity for
  immutable execution policies, rights approvals, confirmation and compute
  authority, executions, fenced attempts, provider-command claims, event
  history, cost observations/application, kill and stop intent, verified
  checkpoints and append-only terminal state. The database remains the final
  arbiter for the live claim, fence, rights, deadline, kill state and cost
  headroom immediately before provider invocation.
- Added an exact-binding provider-command executor and PostgreSQL store. Claim
  selection is deployment/profile specific, invocation-start evidence is
  irreversible, completion is command-kind validated before SQL, and retryable
  completion acknowledgement never causes a second provider call. Local and
  RunPod Pods REST adapters use injected clients and remain unwired; the old
  `execution-dispatch.ts` path and legacy RunPod runner remain quarantined.
- Added append-only provider-result observations and terminal classifications.
  A conclusive adapter response is retained against the exact command claim and
  invocation before or atomically with completion. Classification is evidence
  only (`late_eligible`, `already_authoritative`, `terminal_conflict` or
  `not_eligible`): it cannot mutate projections, events or checkpoints. A
  bounded database-only scanner classifies held observations after terminal
  state appears and performs no provider I/O.
- Observation replay is exact and conflict detecting. Poll, checkpoint and stop
  success/failure outcomes now require the exact non-null provider reference;
  checkpoint evidence uses its own canonical domain digest and rejects invalid
  calendar instants including year zero. Timely custody has three explicit
  transient retries. Timeout losers and dual observation/completion failures
  use a non-rejecting 72-attempt 250 ms-to-5 s retry horizon, ending at the
  earlier of `max(result arrival, claim expiry) + 30 s` or the operational cap.
  Provider invocation remains exactly once.
- Migration SHA-256 is
  `6950a3a230dd0d2181f33296dac3bb5ff42ebbe070e5bdcd4d651f8c737ffa34`.
  Fresh PostgreSQL 16 and 18.4 databases applied all 52 migrations. PostgreSQL
  18 `plpgsql_check` covered 68 functions / 96 relation-aware invocations with
  zero errors and warnings; four existing performance cast notes remained.
  Live rollback-only probes on both versions returned `observed`, exact
  `replayed`, `already_authoritative` and stable classification replay, with
  recovery selecting only unclassified work and final row counts `0/0/0`.
- Final verification: focused execution-control/adapter suite 5 files / 116
  tests; broad Foundry API suite 13 / 217; API shard 1 of 4, 31 / 413; complete
  Reconstruction Foundry suite 19 / 158; focused execution/stop contract suite
  2 / 92 with no type errors. API typecheck, targeted lint and API build pass.
  Independent audit found no remaining P0/P1/P2 defect in this slice.
- No provider, credential, upload, training, paid-compute, object-store,
  evidence-registration, signing, publication, promotion or production-DB
  action occurred. The remaining custody boundary is process death after a
  provider response reaches memory but before its first durable observation;
  closing it requires a provider callback or external durable spool. Actual
  reconstruction/enhancement workers, runtime wiring/scheduling, the separate
  A/B/C UI decision and external survey/rights/review gates remain open.

## T-508 · Sealed staged-source inspection worker

- Hardened verified intake staging so the staging index and every indexed file
  must remain regular, singly linked files. Verification now binds identity
  before and after bounded reads and full hashes; regressions cover external
  hardlink aliases to both staged payloads and the staging index.
- Added a sealed in-process `inspect_sources/v0` worker. It accepts only the
  exact fixed operation identity and one zero-cost, local-CPU, no-network
  JobSpec; binds the complete admitted manifest, admission and staging
  evidence; reruns purpose-aware static rights checks; hashes each exact file
  through retained handles; and performs bounded header detection without
  payload decode, reconstruction, inference, training or child processes.
- Outputs are canonical, self-digested `source-inspection.json` and
  `artifact-index.json`, both authority `none`. The report explicitly records
  that caller-bound execution identity is not authorization, worker-profile
  allowlisting and live control-plane approval are not established, and no
  registry, object-store, signing, publication or promotion capability exists.
- The output directory and both output file handles are exclusively reserved
  and identity-checked before any source-derived bytes are read. Report content
  is fsynced before index content, which is the last commit marker; existing
  paths are never replaced, tree/file/depth and JSON sizes are bounded, and an
  interrupted reservation is left incomplete rather than recursively removed.
- Production execution fails closed on Windows before filesystem access because
  portable Node APIs cannot prove a private Windows ACL. The platform-neutral
  core is reachable only through a `NODE_ENV=test` entrypoint that is absent
  from the package root. A reviewed Windows ACL or OS-sandbox backend is still
  required before local production execution there; no CLI, API, scheduler or
  provider adapter invokes this worker yet.
- Verification: focused staging/worker suite 2 files / 20 passed / 1 skipped;
  complete Reconstruction Foundry suite 20 files / 169 passed / 1 skipped;
  package typecheck, lint and build pass. Independent security re-audit found
  no remaining P0/P1 issue in this slice. This establishes exact byte custody
  and classification only; it creates no geometry, visual detail, quality win,
  metric authority or publishable runtime package.

## T-508 · Lossless GLB normalization proof core

- Corrected the reconstruction recipe authority boundary: `normalize_mesh`
  outputs remain captured-derived but now have
  `mayEnterMeasuredGeometry: false`. The schema rejects a re-digested recipe
  that restores eligibility before registration and control QA; input-role
  scoping was not changed because the current generic stage covers GLB/glTF,
  OBJ, FBX, CAD/BIM and OpenUSD and gives every stage the full asset set.
- Added a sealed `normalize_mesh_glb/v0` pure proof for one exact size/hash-bound
  binary GLB. V0 deliberately accepts only extension-free, self-contained,
  indexed static TRIANGLES with identity transforms, float32 POSITION and
  uint16/uint32 indices, one tightly packed accessor per view, no orphan or
  overlapping storage, and no material, texture, image, camera, animation,
  skin, morph, sparse, quantized, custom-attribute or URI semantics.
- Pinned glTF Transform core/extensions 4.3.0 and retained meshoptimizer 1.2.0.
  The core uses a no-URI `PlatformIO`, registers only
  `EXT_meshopt_compression`, and invokes the encoder directly with filter NONE.
  It never imports the `meshopt()` convenience transform or calls quantize,
  reorder, simplify, weld, deduplicate, prune or texture recoding. Dependency
  and transitive versions/licences are recorded in the canonical report.
- The proof independently decodes every output view, compares exact float/index
  logical bytes and rooted topology through a domain-separated semantic digest,
  derives and verifies accessor bounds, checks meshopt mode/count/stride, and
  requires zero Khronos Validator errors and warnings before and after. The
  public verifier recomputes source/report/invocation bindings, semantic counts,
  bounds, output hashes and validator versions/results under bounded allocation.
- Two adversarial reviews closed unsafe optional-extension loss, 8-bit index,
  tiny-transform, output-expansion, source-binding, forged-count/validator and
  derived-bound gaps. Final review found no remaining P0/P1 correctness defect
  in this production-disabled V0 slice.
- Production execution fails closed on every platform before reading options or
  touching bytes/files. The test-only Buffer harness is guarded by
  `NODE_ENV=test` and omitted from root exports. Enabling execution still needs
  one-GLB-per-job recipe scoping plus exact verified-stage, manifest, admission,
  JobSpec, live fence, trusted worker profile and purpose-aware derivative-rights
  binding in a reviewed sandbox. No CLI/API/scheduler/provider path invokes it.
- Verification: normalization proof 18/18; combined staging/inspection/recipe/
  normalization focus 41 passed / 1 skipped; complete Reconstruction Foundry
  suite 21 files / 188 passed / 1 skipped; package typecheck, lint and build
  pass. This changes container compression only and claims no geometry or visual
  quality improvement, measured authority, registration, signing or publication.

## T-508 · Derivative-rights evidence contract and registry

- Added a separate, strict V0 derivative-rights contract for the exact
  `normalize_mesh_glb/v0` operation. It binds one GLB, the immutable full input
  asset snapshot, the exact JobSpec and ingest-manifest subjects, the sealed
  no-network command, `commercial_internal_use`, the terms-evidence artifact,
  every ordered restriction and disposition, policy generation and TTL, and
  whole-job prohibitions on training, redistribution and public release.
  Parsing and hashing remain evidence preflight only: they do not authenticate
  a registry, consume an approval or grant dispatch authority.
- Added additive migration `0054_foundry_derivative_rights.sql` with three
  append-only registries for immutable policy versions, multi-record policy
  revocations and exact approvals. Every table is constrained to authority
  `none`; no table or trigger joins or alters the 0053 execution, admission,
  provider-command or generic-rights authority paths. Shared advisory locking
  serializes policy generation, approval and revocation visibility, and an
  approval may not use a stale generation or outlive the earliest revocation.
- The database admits an intentionally narrow source-only manifest subset for
  this V0: exact source roots and canonical strings, ASCII paths for identical
  JavaScript/PostgreSQL case folding, no coordinate frames, transforms,
  provenance edges or generated regions, and no derived, generated or
  frame-bound assets. The stored original manifest and canonical subject
  digests are unchanged; a temporary validation copy only normalizes the HTTPS
  scheme to match the frozen case-insensitive TypeScript rule.
- Adversarial review closed fail-open nested-manifest acceptance, uppercase
  HTTPS snapshot mismatch and two binary64 cost-parity faults. The final cost
  guard applies sign, ordering, integer-micro and safe-integer checks entirely
  after conversion to binary64. A 25,000-pair JavaScript-serialized randomized
  comparison produced 5,692 valid cases and zero SQL/TypeScript mismatches;
  live full-trigger probes accepted the valid seven-decimal regression and
  rejected unsafe rounding, fractional micros, negatives and over-budget jobs.
- Frozen SHA-256 values are
  `EF6DA3C51824071A750E12B5931C3FFDF0DC8DF9D9B11E01FAE11978B74E249E`
  for the TypeScript contract,
  `3FBAA37C9189AA189BF68E15CE020CD9712F59A8C6CADC8F470A320D3940AF50`
  for its focused test,
  `05E80543A52978C9B01AB277257CA7EED7BD8A2F37DBEDFDEEE06FA1C730BDB4`
  for migration 0054 and
  `1696904FFF26642929F7846BB9AEC13E7F127DD85BE7F6B5BCF2CE0AA5679985`
  for the dedicated migration test. Migration 0053 remains byte-identical at
  `6950A3A230DD0D2181F33296DAC3BB5FF42EBBE070E5BDCD4D651F8C737FFA34`.
- Final verification: fresh PostgreSQL 16 replay of all 53 SQL files, yielding
  exactly 3 derivative tables / 12 non-internal triggers / 7 derivative
  functions; focused API migration suite 41/41; derivative/foundry type suite
  3 files / 144 tests with no type errors; complete API suite 123 files / 2,436
  tests; API typecheck, targeted lint and build. Independent final audit found
  no remaining P0/P1. Extreme decimal exponents supplied directly as raw JSONB
  remain inherited fail-closed PostgreSQL behavior; canonical JavaScript
  serialization is clean. Concurrent-session stress and exhaustive PL/pgSQL
  negative-branch testing remain open.
- This registry is not production authority. Writer authentication, tenant and
  project authorization, custody and verification of the actual terms bytes,
  future atomic candidate-to-execution activation, V1 execution/provider/worker/
  report binding, revocation containment and downstream restriction propagation
  remain prerequisites. No provider, production database, object store,
  training, paid compute, evidence registration, signing, publication or
  promotion action occurred.

## T-508 · Authenticated derivative terms custody and review V1

- Added a strict V1 contract for bounded terms-evidence custody and
  registry-attestation review. Public upload metadata cannot assert hash, size,
  actor or time; public review input cannot assert the immutable approval
  digest, reviewer, time, authority or execution eligibility. All four request
  and receipt identities use closed, versioned, domain-separated canonical
  JSON. PostgreSQL and TypeScript match the same frozen four-digest golden
  vector. Text rejects padding, NUL and invalid Unicode scalar sequences.
- Added additive migration `0055_foundry_derivative_rights_custody.sql` with
  exact inline `bytea` custody bounded to 1–4 MiB and an immutable review table.
  PostgreSQL recomputes raw-byte length and SHA-256, assigns millisecond custody
  and review times, derives both receipts and receipt digests, verifies a locked
  current platform-admin actor, and denies update, delete and truncate. Both
  tables are permanently `authority='none'` and `execution_eligible=false`.
- Accepted review binds the exact 0054 approval digest and exact custodied
  receipt/bytes. Under the shared policy-version advisory lock it samples DB
  time only after lock acquisition, then rejects a superseded generation,
  expiry or effective revocation. Rejection receipts remain recordable. An
  idempotent retry returns historical evidence only; it is not current authority.
- Added authenticated platform-admin custody, exact-byte attachment retrieval
  and review endpoints. Retrieval is `application/octet-stream`, attachment,
  `private, no-store`, `nosniff` and CSP `sandbox`. Read-side checks rehash bytes
  and rebind request JSON, receipt JSON/digests, row metadata, actor and DB time.
  No 0055 table is consumed by execution, provider, worker, release,
  publication or signing code.
- Fresh PostgreSQL 16 replayed all 54 SQL files. Live probes proved DB-derived
  receipt replacement, byte hash/size and actor rejection, artifact rebinding
  denial, strict idempotency, metadata-swap denial and append-only enforcement.
  Stale-generation, expired and revoked approvals could not be accepted while
  rejection receipts succeeded. A two-session test held the policy lock,
  inserted a future-effective revocation, and made the review wait 2.794 s;
  after re-sampling DB time it rejected and inserted zero review rows.
- Final verification: shared types 89 files / 2,064 tests; Reconstruction
  Foundry 21 files / 188 passed / 1 skipped; API 126 files / 2,451 tests;
  focused custody/service/route/type and production-normalizer gates; package
  typechecks, full lint and builds. Independent final audit found no P0/P1.
  Migration 0055 SHA-256 is
  `47602CF4EF2973A5E8588759519CA6CEF3C7D7DD2F52C463CEEA3EB4667DB2E7`;
  immutable 0053/0054 remain
  `6950A3A230DD0D2181F33296DAC3BB5FF42EBBE070E5BDCD4D651F8C737FFA34`
  and `05E80543A52978C9B01AB277257CA7EED7BD8A2F37DBEDFDEEE06FA1C730BDB4`.
- This is still evidence custody, not a live registry attestation or execution
  capability. Mandatory next work was the V1 subject/binding set and an
  authority-none candidate reservation; future live work still requires atomic
  candidate-to-execution activation, current-authority checks at admission,
  prepare, claim, invocation and output commit, authenticated worker/report/
  output custody, revocation containment and downstream restriction lineage.
  Production normalization remains hard-disabled. No production database,
  provider, paid compute, object store, signing, publication or promotion was
  touched.

## Immutable runtime-package revision contract

- Added migration `0052_runtime_package_revisions`. It assigns deterministic
  per-room revision numbers to surviving historical rows, labels them
  `legacy`, and leaves their content digest null because the old mutable write
  path cannot prove what bytes were originally registered.
- New rows are append-only `content_sha256` identities. PostgreSQL now enforces
  positive monotonic `(venue, room, revision)` values, unique non-null
  `(venue, room, content digest)` values, a per-room advisory lock, and denial
  of update/delete/truncate operations.
- Added deterministic canonical SHA-256 creation logic with server-assigned
  next revisions, duplicate-digest idempotency, explicit requested-revision
  conflicts, one bounded retry for named database races, and integrity checks
  against a digest resolving to different content.
- Independent adversarial review closed three boundary holes before handoff:
  content identities now require an explicit non-null digest in SQL; the insert
  trigger rejects any new row labeled `legacy`; and exact API retries resolve
  their digest before mutable asset-status/URL admission checks.
- Added authenticated `POST /admin/assets/runtime-package-revisions`. It
  returns the real package id, revision, digest and created/idempotent flag.
  The old authenticated `/register-runtime-package` path now returns 410 and
  performs no database work. Runtime readers select the latest eligible row by
  revision instead of mutable timestamps.
- The Reception Quality fine-frontier manifest now carries its fixed decision
  id, hierarchy SHA-256, SOG/fine level, fixed-frontier policy, Gaussian count
  and fixed `generatedAt`. Dry-run shows only its expected digest; `--apply`
  can call only the authenticated create endpoint and reports package id and
  revision only from the server receipt. The old SPZ upsert script fails closed.
- Verification: shared types 85 files / 1,888 tests; API 113 files / 2,234
  tests; focused immutable-revision, route, migration and operator-script tests
  91/91; types and API lint pass; API typecheck passes; migration-tail preflight
  is read-only and passes exact-prefix/hash checks with 0050–0052 pending.
  `safeToApplyProduction` remains false. No migration or live `--apply` was run.
