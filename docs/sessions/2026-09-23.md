# 23 September 2026

## T-628 — continue public Gaussian-splat hold delivery

Blake resumed the requested live-site restriction. The isolated candidate and
PR 33 remain based on `da944981`; refreshed remote state shows master and
release/r1 still at that source. T-601's master-only release coordination is
being refreshed before publication. The shared dirty checkout is preserved.

The final layout correction keeps the canvas and measurement projection at full
viewer size beneath the persistent WIP strip. The HUD retains its reviewed
placement; short-landscape dossier and completed measurement panels remain
clear of controls. Actual real-mesh picks on the local production bundle aligned
with their markers; the completed panel left 8.38 CSS pixels below Fullscreen at
the observed approximately 844×390 viewport. Fullscreen entry/exit retained the
notice, and measurement dismissal worked.

Recovered 19 September verification: all 484 web test files passed, with 6,388
passing tests and 16 existing skips. Windows and Linux each passed 18 responsive
cases plus 12 visual comparisons with updates disabled. After the final panel
cap, the three affected rendered cases passed again on both platforms. The
production build passed. One remaining lint error in a nullable test-message
label was corrected on continuation. Fresh complete web/E2E types and scoped
lint passed. Exact-source hosted/GPU qualification precedes integration and
live verification. T-601 refreshed the release handback at 16:46 UTC: no newer
hold or competing release, and release/r1 remains unchanged.

The earlier failed browser/GPU evidence and diagnosis remain recorded in the
[19 September session](2026-09-19.md). No renderer cutaway, performance budget,
fixture geometry, source asset, API or production data is changed by this hold.

### Qualification and retained startup finding

CI 35891205197 attempt 1 passed its upstream component jobs, but the final browser
gate correctly rejected one retried planner startup case: 10,403ms against the
unchanged 10,000ms limit. The original failure has a report/screenshot, not a
trace. Its retry trace showed renderer/main-thread delay after network loading;
fonts were not the dominant delay in that captured retry. The earlier aaf791
run passed this same, unchanged planner case on its first attempt.

Bounded local diagnostics retained both passes and failures. An observational
baseline/candidate pair ran the original test unchanged on default Chromium
147.0.7727.15 / SwiftShader WebGL2, 1280×720, with fresh HOME/install/Vite/browser
context, no trace and no retries. Baseline da944981 failed at 12,709ms and the
candidate at 10,068ms. First default-framebuffer draw submissions were 7.732s and
5.733s respectively; longest main-thread tasks were 3.897s and 3.436s. This shows
main-thread blocking and gaps between canvas visibility, draw submission and
waiter completion. It does not establish a reliable source performance difference
or the cause of the original CI failure. Observer overhead and distinct clock
spans remain explicit; draw/RAF
observations do not establish physical paint. No renderer, fixture, test budget,
test assertion, skip or gate was changed in response. Broader startup work remains
an open T-613 finding, separate from this public access hold.

One fresh complete qualification, CI 35891205197 attempt 2, passed all 13 jobs.
The startup case passed on its first attempt. PR33 merged normally as
`d7eb71bf05be135d564d0cfa0c90545285743540`; its tree
`e6b2eccd6fc27080244928ad157a72586220bde3` is identical to the qualified synthetic
merge 2b095356. Fresh master CI 35896124136 also passed all 13 jobs, including the
startup case without retry and a new authenticated 5/5 GPU result. Both complete
gates reconcile 353 cases: 307 ordinary passes, 4 executed expected failures and 42
original skips. All scheduled non-skipped cases executed with no unexpected
failures, flaky results, retries or missing cases.
Automatic Deploy 35897612771 passed. release/r1 intentionally stays at da944981;
its next promotion must incorporate this master hold.

### Production and closeout

Authenticated Vercel lookup of `venviewer.com` resolves to Ready production
`dpl_BqsV3YkkmVZa3LoR4433Guv76jnf`, source d7eb71bf. Actual rendered live checks:

- Seven direct room/capture/viewer routes show **Work in progress**, no canvas,
  and a tour link. The complete 49-request capture contains no Gaussian request.
- Direct GETs for the tracked `/splats/reception/0_0.sog` and
  `/fixtures/native-gaussians.splat` return 307 to `/work-in-progress`; no asset
  body was read. Actual redirect caching is `public, must-revalidate, max-age=0`.
- The homepage closes the room-walk links and labels both the hold and available
  tour. `/fresh` has a static captured-room notice and labelled tour links.
- The actual 149-viewpoint panorama/mesh tour renders in Walk, Dollhouse and Plan.
  Its WIP notice remains visible in fullscreen, at 375×812 and at 844×390.
  Room selection works; the landscape dossier and completed measurement panel
  stay clear of controls. Two live mesh picks align visibly with the markers;
  the panel leaves 7.72 CSS pixels below Fullscreen and dismisses normally.
  This verifies interaction, not survey accuracy. A separate complete 185-request
  tour navigation capture contains no Gaussian request; an earlier truncated
  combined capture is retained but is not used as absence evidence.
- The existing private saved planner returns “Public preview configuration not
  found” in this signed-out browser. No new draft was created. Authenticated live
  planner rendering remains unverified in this session; automated/local checks
  cover production source selection, stale capture state and renderer denial.

API health remains Ready on production da944981, with no API/schema source delta.
The unchanged public smoke wrapper passed 15/15. Its exact passing candidate
replaced `final-config.json` with a dated backup, SHA256
`886f75e40b4ca78a45bbbfdbaa389a5887da6a3f87ce74381dd0d9dd895913ad`.
The existing VenviewerSmokeMON definition is unchanged and Ready. Browser viewport
overrides were reset and the live WIP page left open. No production fixture/data
mutation or new compute service was requested.

Local evidence is under `C:/Users/blake/.codex/visualizations/2026/09/23/`:
`splats-wip-production-d7eb71bf`, `startup-observation`, the retained failed/prior/
qualified CI artifacts, and `splats-wip-master-35896124136-gate`. Fresh GPU results
and smoke-rotation receipts remain under the 19 September task evidence directory
`01a0b936-fe82-7812-9d34-d5daa29c2de8`. The production receipt links the actual
provider response, rendered-flow record and direct-file checks. Task tool history
retains the live screenshots and accessibility observations.

T-628's requested live-site scope is done. This closes Venviewer entry points;
separate public object-storage URLs and previously cached/downloaded files are
not revoked. There is no broader device-performance or founder aesthetic
acceptance claim. This closeout is documentation only for the owner branch and
the next coordinated documentation consolidation; it does not require another
application release.
