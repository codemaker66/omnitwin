# Session log — 2026-08-25

## Scope and authority

- Continued the Grand Hall-only fidelity programme on
  `codex/grand-hall-exact-runtime` and registered T-550.
- The owner supplied the raw XGRIDS capture, Matterport panorama export,
  MatterPak OBJ/MTL, and full-building E57 as source evidence.
- The controlling boundary remains source-accurate Grand Hall content only:
  no neighbouring rooms, facade, invented architecture, generated fill, or
  capture-artifact substitution.
- This session was read-only against every supplied source. No push, merge,
  deployment, provider mutation, source mutation, reconstruction, upload, or
  package promotion occurred.

## Source audit

- Bound the 41,095,196,672-byte XGRIDS XBIN at SHA-256
  `42aac50bea3e4fb526536101d140af379c0c0cb87094e3a25379e6cf617bbfe0`
  and independently bound `project_data/poses.csv` at SHA-256
  `b86bc45d15b8b5a84d61160afe3e16e7659e195557a2b8c6567039bb74d83127`.
  The raw project remains unclassified and is not training- or runtime-eligible
  until a reviewed LCC point-cloud preview and Creator Data export exist.
- Bound the 20,518,437,888-byte full-building E57 at SHA-256
  `975039d11fc04ca681f038e499f358124bbcab178ad5ce6324fa912212729cdd`.
  It contains 149 scans and 894 embedded pinhole images and is not room-isolated.
- Bound the whole-building MatterPak OBJ at SHA-256
  `cf7247b5343fe719dc0f1aaf6b64c667d238c69133b71c44ccd9f5c67b5878c7`
  and its MTL at SHA-256
  `8e43085c90e40e2e76b7e221038c13bd65f17893a3d097eb12ffea5445f85d7a`.
  Its common Matterport lineage means it is boundary reference, not an
  independent accuracy control.
- Audited the first 50 of 148 panorama sweeps as provisional mask-authoring
  candidates. Visual inspection classifies scans 001–016 and 019–047 as
  visually consistent with the Grand Hall, scans 000/017/048 as mixed portal
  views, and scans 018/049 as adjacent-space views. This is explicitly not a
  geometric camera-centre membership claim and remains human-review pending.

## T-550 evidence contract

- Added a strict, authority-free, canonical-digest schema and the corresponding
  JSON artifact. It preserves the historical T-507 pilot unchanged and does
  not reinterpret that diagnostic manifest as room membership.
- Fifty scan records bind exact preview and candidate-panorama hashes. Forty-four
  panorama correspondences remain sequence hypotheses; six boundary pairs carry
  reproducible normalized cyclic-correlation diagnostics and explicit human-
  confirmation and E57-byte-lineage `false` states.
- The contract requires a reviewed non-convex closed selection volume and exact
  8192×4096 single-channel 8-bit grayscale PNG exclusion masks before any
  training/runtime authority. A later bit-exact output mask must bind the real
  ordered XGRIDS/LCC inventory after it exists. The panorama-mask `{0,255}`
  values, polarity, pixel origin, seam behavior, coordinate space, metadata
  restrictions, no-resampling declaration, reason union, and whole-frame
  exclusions are pinned.
- Procedural or synthesized pixel replacement is forbidden. In particular, the
  existing `PanoStage` nadir crown is not permitted for this exact Grand Hall
  path; masked and unknown regions must remain transparent or unknown rather
  than becoming a false dark floor.
- Membership artifact SHA-256:
  `sha256:e2822de20e28bbeeb7ca81c8aad96214852e39bdc206e3d378d37d80c2904c68`.
- Nested panorama-crosswalk evidence SHA-256:
  `sha256:aecf6168948d66dbde4d6e302c682a72cef323106fb3eaf52e20587c9844ca7f`.

## Verification

- Focused T-550 suite: 1 file / 11 tests passed with no type errors.
- Full shared-types suite: 98 files / 2,167 tests passed with no type errors.
- Shared-types strict typecheck, lint, and production build passed.
- `git diff --check` passed apart from informational Windows line-ending notices.
- An independent evidence-integrity re-review found no remaining issue after the
  mask representation and nested/enclosing digest regressions were tightened.

## Remaining gates

- Human review and acceptance of the visual room-scope classifications.
- Author and approve the non-convex Grand Hall selection volume plus panorama
  masks, then later review the XGRIDS/LCC registration and exact output mask.
- Run the raw XGRIDS project through LCC Studio 2.3-or-newer Point Cloud Preview
  and Creator Data on a machine with 128 GB RAM and verified fast local storage;
  this workstation has approximately 32 GB RAM and must not start the long bake.
- Only after those evidence gates may controlled reconstruction bake-offs and
  source-faithful browser/runtime QA begin. T-550 therefore remains in progress.

## T-552 fail-closed runtime admission slice

- Added an immutable shared v2 room-only evidence contract binding a separately
  accepted closed selection volume, every interface decision, panorama masks,
  a reviewed XGRIDS/LCC-output-to-MatterPak/E57 transform, an exact ordered
  output-inventory bitset, human review, no-fill policy, and the exact derived
  SOG inventory.
- Production has no accepted evidence trust root. Synthetic two-member v2
  fixtures are test-only and do not assert that a real cropped artifact exists.
- Registration, canonical/latest discovery, authenticated preview, the exact
  loader, and exact scene metadata all use the accepted v2 inventory. Legacy
  v1, missing/pending/wrong-scope evidence, and any renamed/reordered/subset or
  mixed legacy member fail closed before object reads.
- The exact Trades Hall Grand Hall path exposes neither generic/legacy bytes nor
  procedural architecture, neighbouring rooms, facade assets, generated fill,
  or PanoStage nadir replacement. Unknown content remains blank/transparent.
- Shared browser safety ceilings now bound each member to 16 MiB, the atomic
  package to 128 MiB / 32 members, and decode hints to 1,000,000 Gaussians per
  member / 8,000,000 total. They retain headroom above the real source while
  blocking manifest-driven allocation and request-fan-out failures.
- Terminal exact-loader failure is wired into the standalone page before
  admission and during transfer, so metadata and mid-transfer faults cannot
  leave Truth Mode pending. RoomScene labels both current visual layers
  `unregistered`; it declares no reviewed transform artifact and carries no
  dangling inspection-transform reference.
- Retired the old v1 mint, secret writer, DPAPI handoff, Railway stage, and
  intake entry points before environment, credential, file, token, or network
  access. Replaced the old executable runbook with a **STOP** record; the
  read-only source validator remains diagnostic and reports
  `runtimeAdmissible: false`.
- No real v2 output was fabricated, no trust root was set, and no provider,
  credential, staging, deployment, object, database, or production state was
  changed.

## T-551 MatterPak/E57 room-9 source-boundary receipt

- Delivered a deterministic, path-redacted, authority-none source receipt at
  `docs/operations/grand-hall-room9-source-boundary-evidence-v1.json`.
  Receipt digest:
  `sha256:0d331b5193f345ad5a127372b691ae02d2049fecdcfd0bc92b7f7cc27166997b`;
  artifact self digest:
  `sha256:7ab3490a55f67d700a8ab84581e53c69e66b3dc831256bc9b70350d43f8b41c4`.
- The colour plan identifies MatterPak room 9 as the Grand Hall. The room-9
  selection contains 43 OBJ groups, 119,564 faces, 59,049 vertices, 42
  materials, and 90 connected components. Candidate interfaces with rooms 13
  and 14 remain unresolved portal decisions.
- E57 camera-centre first hits classify scans 000-017 and 019-047 as room 9,
  scan 018 as room 11, scan 048 as room 14, and scan 049 onward outside this
  provisional room-9 set. This geometric diagnostic does not by itself accept
  panorama pixels or authorize reconstruction.
- The receipt proves the E57 root GUID matches the MatterPak OBJ stem and binds
  the canonical pose export, staged inputs, probe, and generation manifest. A
  final `--check-artifact` pass rehashed all 20,518,437,888 E57 bytes and
  passed. Room 9 is explicitly not watertight and the receipt grants no closed
  volume, transform, training, runtime, structure, collision, or export
  authority.
- Verification: 23 focused boundary/file tests, strict typecheck, lint, and
  build passed. Tests cover staged-E57 drift before and during hashing, GUID
  mismatch, pose/probe/manifest drift, linked-path escape, cross-runtime float
  canonicalisation, and artifact self-digest integrity.

## T-553 raw XGRIDS/LCC read-only preflight

- Added the Grand Hall LCC preflight command and operator runbook. It binds the
  exact 4-directory / 12-file / 41,296,996,984-byte raw project at canonical
  inventory digest
  `sha256:6e6fe18c4944cb5a0e68a69c3bc9dbb808835be6293465f50652d47e8df68236`.
  The 41,095,196,672-byte XBIN has the expected `XBAG` signature and SHA-256
  `42aac50bea3e4fb526536101d140af379c0c0cb87094e3a25379e6cf617bbfe0`.
- The corrected combined receipt is
  `sha256:dc2259089043ae4a1d95663f251d4bd94699124cd49baa3b8958a0d668389b8a`.
  It observes one RTX 4090 with 24,564 MiB VRAM, driver 596.49, and compute
  capability 8.9. GPU eligibility passes.
- The workstation remains blocked on exactly four gates:
  `RAM_128_GIB_REQUIRED`, `SCRATCH_500_GIB_FREE_REQUIRED`,
  `SCRATCH_EMPTY_DIRECTORY_REQUIRED`, and
  `LCC_2_3_OR_NEWER_REVIEW_REQUIRED`. The installed program reports internal
  version `0.15.0.7`; no unsupported mapping to the public 2.3 label was made.
- Verification: 13 focused tests plus the full reconstruction-foundry suite
  (25 files / 366 tests), strict typecheck, lint, and build passed. The run
  performed no source write, network operation, LCC launch, reconstruction,
  upload, staging, deployment, or publication.

## T-554 authority-none human review pack

- Added a canonical root descriptor binding the T-550/T-551/T-553 evidence,
  the complete 148-file panorama inventory, all 50 scan-index 000–049 review
  candidates, the other 98 explicitly ineligible sources, the boundary-review
  manifest, and the panorama-review manifest. Root descriptor digest:
  `sha256:a0b39dae80cec0724f68ebced0b4662223304e82bff1233b8c477b23f0088c24`.
- Added source-derived room-9 XY and Z-slice diagrams plus separate room-9↔13
  and room-9↔14 point/residual diagnostics. The room plan exposes all eight
  exact shared-index interface IDs as pending; the portal diagnostics are
  explicitly not closure geometry. Boundary manifest digest:
  `sha256:6d0f6a230053ccc85275a80260c7b27cfd612ee5c7ca9964bc0ca8653b84de27`.
- Added a 50-candidate panorama contact sheet and a separate six-pair
  crosswalk sheet, each regenerated from and checked against exact source JPEG
  bytes. Panorama manifest digest:
  `sha256:c2d74ee55b27be9b4641d3b94968591d37735d353987d30adca4fc785b3636ef`;
  ordered panorama inventory digest:
  `sha256:949f4cbf365f33d47c5e75f46b881aff857695fbbb70879e27c4f23f4b2af176`.
- Browser inspection confirmed the plan, portal diagnostics, panorama overview,
  and crosswalk are legible and retain their authority-none warnings. No room
  decision, interface decision, panorama mask, selection volume, transform,
  XGRIDS/LCC output mask, or human acceptance was authored by an agent.
- The blank human checklist makes every scan and all eight interface decisions
  explicit. `UNSURE` remains fail-closed, and every current machine/agent label
  is reversible by the venue owner or another qualified human reviewer.

## T-558 concrete-file verifier groundwork

- Added a strict future accepted-scope bundle verifier for the concrete
  authority-none T-554 root descriptor plus all six human-accepted artifacts.
  It validates canonical/fatal-UTF-8 JSON and self-digests; proves the accepted
  artifacts descend from the exact T-550/T-551/T-553 XGRIDS, MatterPak/E57,
  panorama, and eight-interface lineage rather than a phantom shared digest;
  checks interface-to-boundary semantics; fully decodes every exact source JPEG;
  and requires every exact source-grid panorama mask to be binary grayscale8
  PNG containing only `IHDR`/`IDAT`/`IEND` chunks.
- File access is fail-closed against links, path escape, inventory drift,
  malformed encodings, and same-path replacement while hashing. XGRIDS/LCC
  members are streamed once through both hashing and a required format-aware
  inspector; the inspector must corroborate native order plus actual record kind
  and count before the output bitset's length, LSB order, padding, digest, and
  population can pass.
- This is generic integrity groundwork, not a real LCC format implementation or
  runtime admission. The function explicitly returns
  `productionTrustActivated: false` and `runtimeAdmissionAuthorized: false`.
  T-558 remains blocked until a real audited member inspector and API/intake
  wiring operate on the eventual human-accepted artifacts and T-556 candidate.
- Corrected a provenance conflation found during final review: the immutable
  legacy SOG-frontier receipt (`8e7514…c1352`) remains separate from the raw
  XGRIDS/LCC project receipt (`dc2259…89b8a`). The reviewed transform and output
  mask bind the raw-project receipt to each other; the runtime evidence retains
  the legacy frontier receipt as distinct historical lineage.

## Local closure state

- T-551, T-552, and T-553 are locally complete. T-550 remains in progress
  because visual membership is human-pending. T-554 through T-558 remain
  blocked on owner/domain review, a sealed closed selection volume and panorama
  masks, qualified hardware, reviewed LCC compatibility/settings,
  reconstruction, reviewed registration and output mask, the final
  matched-camera source-faithful bake-off, and byte-level API/intake admission.
- The earlier v1 intake path is superseded and retired. Production's accepted
  v2 evidence digest remains `null`, so the exact Grand Hall route fails closed
  instead of rendering old bytes or an invented fallback.
- Final integrated verification passed: shared types 100 files / 2,192 tests
  with no type errors; API 164 files / 2,878 passed and 5 skipped; web 334 files
  / 4,175 tests; reconstruction-foundry 29 files / 420 tests. Strict
  typecheck, lint, and production builds passed for all four packages. The web
  build used the repository's reviewed local-only boundary, production API
  origin, and a non-secret syntactic Clerk publishable-key placeholder; it did
  not validate a real sign-in or provider deployment.
- A final independent read-only audit found no remaining concrete blocker in
  the v2 limit enforcement, pre-read/pre-request refusal, terminal failure
  handling, unregistered RoomScene provenance, trust root, or fallback/legacy
  boundaries. `git diff --check` passed with informational Windows line-ending
  notices only.
- Authenticated browser/WebGL QA remains intentionally unperformed: there is no
  real accepted room-only v2 artifact and the production trust root is still
  `null`. Activating a synthetic fixture merely to obtain a frame would defeat
  the fidelity gate.
