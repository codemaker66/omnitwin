# 2026-06-14

## Reception Room runtime QA framing

- Adjusted the registered Reception Room XGRIDS/SOG visual QA framing so `/dev/trades-hall-visual?venue=trades-hall&room=reception-room` starts from a closer elevated interior camera instead of an outside dollhouse-scale overview.
- Increased the runtime splat fit target and added room-specific OrbitControls speed/distance tuning for registered Reception Room assets.
- Documented that the transform and camera pose remain approximate visual QA aids, not signed room-local operational alignment.
- Verified with focused web unit tests, web typecheck, web lint, the default Trades Hall visual Playwright spec, and the opt-in registered Reception Room package E2E on system Edge.
- Browser plugin was unavailable as `iab`; used Playwright/Edge fallback for rendered verification.

T-091 and T-091A remain open. The Reception Room package is still internal-only/unverified until signed transform, LCC2/LOD strategy, visual QA record, and exposure review are complete.

## Reception Room runtime camera controls

- Replaced the registered Reception Room route's loose runtime OrbitControls path with a bounded camera-control rig that applies runtime FOV, damping, polar limits, slower input speeds, and room-local camera/target clamps.
- Tuned the Reception Room camera profile to start from a higher composed interior shot: position `[0.2, 6.2, 13.4]`, target `[0, 0.9, -4.15]`, FOV `48`, distance `1.2-13.5`.
- Added regression coverage for the bounded camera contract and for the page wiring that passes the registered Reception Room controls into OrbitControls.
- Updated the Reception Room runtime intake note with the current camera bounds and the transform limitation reminder.
- Browser-verified the final start view and a small drag/orbit interaction on system Edge; screenshots are `output/playwright/reception-room-camera-tuned-start.png` and `output/playwright/reception-room-camera-tuned-after-drag.png`.

Still not complete for T-091/T-091A: the camera rig improves QA navigation only. The transform remains approximate until signed room-local alignment and human review are complete.
