# 2026-05-03

## T-088 invitation-before-user creation

- Chose T-088 because F-001 is already closed through T-083 and T-097, while T-088 was the first unfinished item in the daily priority order.
- Marked T-088 `in-progress`; not marked done because normal Vitest and `pnpm audit` could not complete in this sandbox.
- Added a backend onboarding gate so a Clerk identity only resolves to a local user when it is already linked, matches a pre-provisioned user row by verified email, or uses a configured approved email domain.
- Removed the `${clerkId}@clerk.user` missing-email fallback from HTTP and WebSocket auth.
- Routed Clerk `user.created` webhooks through the same onboarding gate and stopped trusting `public_metadata` role/venue for new approved-domain user creation.
- Added `user_onboarding_audit` migration/schema entries to record allowed and denied onboarding decisions.
- Added focused regression coverage in `packages/api/src/__tests__/user-onboarding.test.ts`.

Verification:

- Passed API typecheck with `tsc --noEmit`.
- Passed API lint with `eslint src/`.
- Passed API build with `tsc --project tsconfig.build.json`.
- Passed a direct compiled-runtime validation of invited-user, approved-domain, arbitrary-user, and unverified-email paths via `node C:\tmp\validate-user-onboarding.mjs`.

Blocked verification:

- Vitest could not run in this sandbox because Vite/esbuild needs Node child-process spawn and fails with `spawn EPERM`.
- `pnpm audit --audit-level=moderate` could not reach the npm audit endpoint; registry traffic is refused at `127.0.0.1:9`.

Next step:

- Run the normal API Vitest target and `pnpm audit --audit-level=moderate` in an environment that allows Node child-process spawn and npm registry access; if they pass, T-088 can be reviewed for completion.
