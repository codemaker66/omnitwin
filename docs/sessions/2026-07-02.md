# Session log — 2026-07-02

## Twin Phase 1 — The Walk (shipped locally, R2 pending)

Program spec `docs/superpowers/specs/2026-07-02-twin-program-design.md`
(decisions: R2, public-from-day-one, XGrids LCC first-class, name = Twin).
Plan `docs/superpowers/plans/2026-07-02-twin-phase1-walk.md` — 12 tasks,
executed subagent-driven (fresh agent per task batch, orchestrator review
between; ~6 subagents + reviewer).

### What exists now

- **`twin/0` schemas** in @omnitwin/types (capture-source union incl. xgrids-lcc).
- **twin-forge** (`tools/twin-forge`): poses.json + cubemaps → WebP tiles
  (256/1024), KNN nav graph (+ per-venue overrides file), SHA-256 hashes,
  schema-validated manifest. Real run: **149 nodes, 357 edges, 1,788 tiles,
  136 MB, 0 missing** → `packages/web/public/twin/trades-hall` (gitignored).
- **Viewer** at `/venues/:venueSlug/twin` (public route): cube-shader
  PanoStage with LOD streaming + zenith crown, spring WalkControls
  (drag/pinch/wheel), gold nav rings with 4px drag-guard, spring hops with
  crossfade + `?node=` URL sync (back button walks), minimap listbox
  (keyboard + teleport), claim-safe copy, Rite-voiced states.
- **Calibration** (visual, against `scan_000.jpg`): FACE_TO_CUBE gained
  `rotateQuarters`; front=1, back=3, up=1, down=3 quarter-turns — the first
  "good" render was luck (the hall sat on the right face). Table pinned by
  table-derived tests.
- **Gates**: twin e2e 11/11 + landing 6/6; chunk budget (TwinPage ~16 KB raw
  vs 250 KB gz budget; LandingPage chunk asserted free of twin modules);
  visual harness ok across forward/right/back/zenith/nadir/hop/minimap/mobile;
  typescript-reviewer approve (P0 none; P1s fixed same-day: AbortController
  in useTwinManifest, TwinViewer keyed by venueSlug; P2: CLI --tier validated
  via schema enum).

### Verified (committed state)

typecheck/lint clean (types, web, twin-forge) · types 1,699 green (incl. the
repaired @clerk/react pin — was red at HEAD since 2026-06-25) · twin-forge
6/6 · **full web suite 2,665 green in a clean worktree at twin HEAD** ·
production build clean.

### Known context, not mine to fix silently

`App.test.tsx` "caps DPR at [1,1]" fails ONLY with the prior session's
uncommitted planner-perf modifications in the tree (App/AdaptiveResolution
et al. — receives [0.75, 0.75]). Proven clean at the same HEAD in a fresh
worktree. Blake: that in-flight work needs finishing or stashing.

### Gotchas recorded

- Parallel subagents share one git index → commit with explicit pathspec
  (one sweep happened; boundaries repaired same session; memorized).
- twin-forge CLI paths are package-cwd-relative → always pass absolute.
- Tuple-typed matrices + strictTypeChecked: `?? 0` on literal tuple indices
  is a lint error; unroll math instead of casting.

### Remaining for full Phase-1 public production

1. Blake: create R2 bucket `venviewer-twin` + custom domain
   `twin.venviewer.com` + API token (steps in `tools/twin-forge/README.md`).
2. `rclone copy packages/web/public/twin r2:venviewer-twin`.
3. Vercel env `VITE_TWIN_ASSET_BASE=https://twin.venviewer.com`, redeploy.
   Until then production shows the graceful "twin is being prepared" state.
4. Follow-ups parked: minimap SVG-ARIA screen-reader smoke test; walk the
   full 149-node graph and hand-edit nav overrides for doorways/stairwells.
