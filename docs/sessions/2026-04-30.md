# Session log - 2026-04-30

## Completed: T-114 Grand Hall procedural visual fidelity

- Improved the current procedural 3D viewer toward the Trades Hall Grand Hall reference while the photoreal/splat runtime remains future work.
- Updated the real loaded planner path (`RoomMesh`) as well as the standalone `GrandHallRoom` fallback so the visual pass appears when a Grand Hall space is loaded.
- Added/retuned visual cues: warm avodire timber ceiling field, raised coffer beams, five chandeliers, five arched window bays with curtains, gold trade frieze, portrait/honour-board walls, darker wainscot panelling, fireplace focal point, warmer lighting, and subtler planning grid opacity.
- Adjusted the narrow/mobile camera framing so the planner opens inside the room instead of looking at the outside of a wall, and added CameraRig regression coverage for that pose.
- Verified with:
  - `pnpm --filter @omnitwin/web lint`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web test -- src/components/__tests__/GrandHallRoom.test.tsx`
  - `pnpm --filter @omnitwin/web test -- src/components/__tests__/CameraRig.test.ts`
  - Desktop browser canvas screenshot/pixel check on `/plan/visual-grand-hall` with mocked Grand Hall config and space data: 9/9 non-background samples, 9 unique colors, no console errors, no failed requests
  - Mobile browser canvas screenshot/pixel check on `/plan/visual-grand-hall` with mocked Grand Hall config and space data: 8/8 non-background samples, 8 unique colors, no console errors, no failed requests
  - `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts --reporter=list --workers=1`
- Local Vite dev server was stopped after browser verification; port 5173 confirmed closed.

## Completed: T-115 Grand Hall reference correction

- Corrected the T-114 procedural stand-in against the actual Grand Hall floorplan and reference photos.
- Set the canonical Grand Hall footprint to 21m x 10.5m across shared types, web geometry, API seed fixtures, and related tests.
- Reoriented the procedural ornament layer to the correct long-axis model: one arched-window long wall, portrait/honour-board opposite wall, short-end fireplace/door cues, three chandeliers, central dome, wall-chair rows, and balcony/door wall cue.
- Fixed the remaining 2D/3D room-frame mismatch by making the blueprint adapter prefer the same hand-authored Grand Hall geometry as the 3D viewer, even when older API space dimensions still say 10m length.
- Updated 2D drag-back-to-3D movement to use the blueprint scene room frame, so grouped table/chair moves use the same dimensions when returning to 3D.
- Updated floor-plan thumbnail capture to use the active room-dimensions store rather than raw API dimensions.
- Verified with:
  - `pnpm --filter @omnitwin/web lint`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web test -- src/lib/blueprint/__tests__/adapt.test.ts src/__tests__/room-geometries.test.ts src/components/__tests__/GrandHallRoom.test.tsx src/components/__tests__/CameraRig.test.ts src/components/__tests__/BookmarkPanel.test.ts src/stores/__tests__/bookmark-store.test.ts src/lib/__tests__/camera-animation.test.ts`
  - `pnpm --filter @omnitwin/types test -- src/__tests__/space.test.ts src/__tests__/hallkeeper-v2.test.ts src/__tests__/configuration-sheet-snapshot.test.ts src/__tests__/integration.test.ts`
  - `pnpm --filter @omnitwin/api test -- src/__tests__/seed.test.ts src/__tests__/manifest-generator-v2.test.ts src/__tests__/spatial-classifier-v2.test.ts src/__tests__/services/event-sheet-extractor.test.ts src/__tests__/services/hallkeeper-sheet-v2-data.test.ts src/__tests__/services/pdf-prerender.test.ts src/__tests__/hallkeeper-pdf-v2.test.ts`
  - `pnpm --filter @omnitwin/api typecheck`
  - `pnpm --filter @omnitwin/web build`
  - Production-preview browser screenshots for desktop, 2D, and mobile with mocked public config and stale 10m API length to verify the new geometry override path.

## Completed: T-087 Three.js 0.180 + Spark runtime gate

- Reviewed Claude's read-only verification prompt and treated it as triage, not a read-only constraint, because Blake's current instruction was to fix/build the appropriate work.
- Static verification found T-087 not started, T-088 not started/partial, T-089 partial, T-093 partial/not-started, and T-096 not started. T-087 was the correct first code task because it is bounded, unblocks renderer/Truth Mode work, and does not consume the VSIR migration numbers reserved by D-019.
- Upgraded the web renderer compatibility unit to `three` ^0.180.0, `@types/three` ^0.180.0, `@react-three/fiber` ^8.18.0, and `@react-three/drei` ^9.122.0.
- Added `@sparkjsdev/spark` 2.0.0 and a lazy `/dev/splat-fixture` route that imports Spark directly rather than using drei's older `<Splat />` path.
- Kept Spark out of the regular Three.js chunk by adding a separate lazy `spark` manual chunk in Vite.
- Added source-level regression tests for the Spark stack and route, and a Playwright E2E smoke test so CI exercises the fixture route.
- Marked T-087 and superseded T-052 done; noted that T-098 is now unblocked.
- Verified with:
  - `pnpm --filter @omnitwin/web lint`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web test -- src/__tests__/spark-stack.test.ts src/__tests__/bundle-splitting.test.ts src/__tests__/save-send-panel.test.ts`
  - `pnpm --filter @omnitwin/web build`
  - `pnpm --filter @omnitwin/web e2e -- e2e/splat-fixture.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web e2e -- --reporter=list --workers=1` (62 passed, 3 skipped)
  - `pnpm audit --audit-level=moderate`

## Planned: D-024 reconstruction strategy and transform artifacts

- Read the current VSIR/truth/artifact ADR context (D-009, D-011, D-012, D-014, D-019), `docs/state/tasks.md`, `state/asset_versions.json`, and the existing runtime manifest/type surface.
- Added D-024, committing the Scene Authority Map extension for per-region/fixture-type representation routing.
- D-024 records the authority-map fields: geometry, appearance, lighting, physics, semantic, interaction, export, truth status, confidence tier, provenance references, reconstruction strategy, and transform artifact reference.
- D-024 records the allowed strategies: MatterPak original, E57 Poisson, E57 BPA, E57 dual meshing, RealityScan hybrid, PGSR mesh extraction, 2DGS mesh extraction, neural surface reconstruction, manual artist proxy, Geometry Nodes parametric, kitbash proxy, and procedural runtime.
- D-024 records the production policy: deterministic E57-to-GLB room shell by default; deterministic geometry first for ornate plaster/dome/frieze; manual or artist proxy expected for chandelier/crystal/stained glass/mirror/polished-metal regions; Gaussian splats remain appearance/radiance/lighting authority where appropriate.
- D-024 records Hero Region Specialist production/fallback policy for chandeliers, crystal pendants, stained glass, mirrors, polished metal, ornate plasterwork, dome/frieze, carved wood, and marble/fireplace regions.
- D-024 defines CVF/ARF/RRF coordinate frames and requires nontrivial assets to persist `ARF -> CVF` and/or `CVF -> RRF` transform artifacts.
- Added follow-up planning tasks:
  - T-116: `TransformArtifactV0` runtime manifest support
  - T-117: Hero Region asset pipeline spike
  - T-118: E57 deterministic meshing spike
  - T-119: RealityScan/PGSR/2DGS/neural comparison spike
  - T-120: Chandelier/stained-glass proxy asset workflow
  - T-121: Truth Mode v0 authority display for raw/enhanced/proxy/splat regions
- Updated `docs/diagrams/task-graph.md` and the task source legend.
- No reconstruction pipeline, DB schema, package rename, or public claim changes were made.

## Completed: T-122 Device-class UX doctrine and Trades Hall responsive planner standards

- Added `docs/architecture/device-class-ux-doctrine.md` as the current phone/tablet/desktop UX doctrine for Venviewer.
- Defined phone portrait, phone landscape, tablet portrait, tablet landscape, and desktop classes, with the no-dead-end rule, touch-native expectations, full-screen mobile planner pattern, tablet planning/presentation surface, and desktop power-editing surface.
- Refined the Trades Hall landing planner responsive behavior:
  - phone keeps the fitted embedded preview and opens the full-screen planner for CTA/tap/edit interactions
  - tablet portrait keeps the plan first while exposing controls and inspector instead of using phone-only launch chrome
  - tablet landscape uses a compact three-column planning surface
  - desktop preview behavior remains intact
- Expanded Playwright coverage for 320x568, 375x812, 390x844, 430x932, 768x1024, 1024x768, and 1280x800.
- Added T-122 to `docs/state/tasks.md` and `docs/diagrams/task-graph.md`.

## Completed: T-123 Truth Mode doctrine

- Incorporated the two Truth Mode research reports into `docs/architecture/truth-mode-doctrine.md`.
- Defined Truth Mode as Venviewer's trust interface across trust inspection, trust workflow, and trust infrastructure, not merely a debug heatmap.
- Documented progressive disclosure levels L1-L4: persistent trust indicator, popover summary, provenance drawer/evidence chain, and raw manifest/signed artifact/audit bundle.
- Documented persona presets for Planner Lite, Hallkeeper Verification, Developer / QA Debug, and Client Real-vs-Proposed.
- Committed the multi-axis truth model: source/evidence, verification, confidence, staleness/freshness, Scene Authority Map references, provenance references, and known-unknown vs measured-empty distinction.
- Added evidence/source vocabulary and verification vocabulary, including `known_unknown`, `measured_empty`, `suppressed`, `contested`, and `expired`.
- Documented visual encoding rules, semantic truth-token requirements, non-color accessibility requirements, verification decay policy, and false-confidence guardrails.
- Updated backlog:
  - T-121 revised as the T-091C Truth Mode v0 overlay task.
  - T-124 through T-130 added for tokens, provenance event log, popover, persistent indicator, hallkeeper worklist, expiry, and audit bundle stub.
  - T-131 through T-133 added for C2PA-style export, per-Gaussian uncertainty, and HVET research.
  - T-134 added for hallkeeper/planner think-aloud validation.
- Updated T-029 and T-101 to treat Truth Heatmap as part of broader Truth Mode.
- Updated `docs/diagrams/task-graph.md` and the task source legend with `TRUTH-DR-2026-04-30`.
- No runtime UI, C2PA integration, per-Gaussian metadata storage, production rendering code, public copy, or package rename was changed.

## Completed: T-124 Truth state design tokens

- Added typed Truth Mode vocabulary in `packages/types/src/truth-mode.ts` for evidence/source state, verification state, confidence tier, staleness state, persona presets, disclosure levels, and semantic token categories.
- Added shared Truth Mode visual tokens in `packages/types/src/design-tokens.ts` for observed, fused, inferred, ai-generated, human-edited, artist-proxy, verified, contested, stale, and known-unknown.
- Each token includes color roles plus non-color hooks (`hatch`, `stipple`, `outline`, `badge`, `label`) so future Truth Mode surfaces do not rely on color alone.
- Exported the vocabulary and tokens from `packages/types/src/index.ts`.
- Added contract tests in `packages/types/src/__tests__/truth-mode.test.ts` and expanded `packages/types/src/__tests__/design-tokens.test.ts`.
- Verified:
  - `pnpm --filter @omnitwin/types lint`
  - `pnpm --filter @omnitwin/types typecheck`
  - `pnpm --filter @omnitwin/types test -- src/__tests__/truth-mode.test.ts src/__tests__/design-tokens.test.ts`
  - `pnpm --filter @omnitwin/types test`
  - `pnpm --filter @omnitwin/types build`
- No runtime UI, C2PA integration, provenance graph, per-Gaussian metadata, public copy, package rename, or production rendering behavior was changed.

## Completed: T-127 Truth Mode L1/L2 foundation

- Added a gated persistent Truth Mode indicator in `packages/web/src/components/truth/TruthModeIndicator.tsx`.
- Added `packages/web/src/lib/truth-mode-summary.ts` to derive the current procedural-scene posture without inventing provenance data.
- Mounted the indicator in `EditorPage` and the Spark fixture only when enabled by development mode or a `?truth=1` / `?truthMode=1` query parameter.
- L1 shows current mode/status, whether procedural content is present, whether measured runtime assets are loaded, and known issue count.
- L2 popover summarizes evidence/source, verification availability, confidence availability, freshness availability, known issues, and a disabled future provenance drawer.
- The UI states plainly that current procedural/Spark views do not have measured runtime assets, confidence bands, review records, or signed QA certificates loaded.
- Added tests:
  - `packages/web/src/components/truth/__tests__/TruthModeIndicator.test.tsx`
  - `packages/web/e2e/truth-mode-indicator.spec.ts`
- Verified:
  - `pnpm --filter @omnitwin/web lint`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web test -- src/components/truth/__tests__/TruthModeIndicator.test.tsx src/__tests__/spark-stack.test.ts`
  - `pnpm --filter @omnitwin/web e2e -- e2e/truth-mode-indicator.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web e2e -- e2e/splat-fixture.spec.ts e2e/truth-mode-indicator.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web build`
- No full heatmap, C2PA, database change, provenance graph, per-Gaussian metadata, package rename, or public copy change was made.

## Completed: T-135 3D planner mobile shell

- Audited the 3D planner mobile failure points:
  - Desktop command/help overlay with `Click`, `Q`, `E`, and `Esc` came from `PlacementHint`, `MeasurementOverlay`, and `VerticalToolbox` tooltip shortcuts.
  - `Send to Events Team` came from `SaveSendPanel`.
  - The bottom toolbar came from `VerticalToolbox`.
  - The clipped `Don't show again` coachmark came from `PlacementHint`.
  - Save/autosave state is owned by `editor-store`, with the debounce/flush path in `EditorBridge`; the toolbar previously flashed success after a failed save because `saveToServer()` swallowed the error and resolved.
  - Mobile layout previously used a narrow-width bottom rail and several fixed overlays; `EditorPage` used `100vh`, not a safe-area-aware `100dvh` shell.
- Implemented the smallest 3D mobile shell fix:
  - `EditorPage` now wraps the planner in a `100dvh`, safe-area-aware, no-horizontal-overflow shell.
  - `VerticalToolbox` now suppresses desktop shortcut tooltips on touch/narrow viewports, shows compact touch labels, keeps Save visible early in the mobile toolbar, and derives save copy from real save state.
  - `SaveSendPanel` moves the send CTA into a compact top-safe action on mobile instead of floating over the scene content.
  - `PlacementHint` uses touch-native copy (`Tap to place`, `Drag to move`, `Rotate`, `Cancel`) and a viewport-bounded mobile card with a non-clipped `Hide` action.
  - `MeasurementOverlay` uses tap-oriented copy and hides keyboard-only cancellation hints on touch/narrow viewports.
  - `editor-store.saveToServer()` now returns success/failure so UI code does not flash `Saved` after a failed request.
- Added `packages/web/src/lib/editor-save-status.ts` plus tests for explicit save-state labels.
- Added `packages/web/e2e/editor-mobile-shell.spec.ts`, covering:
  - 320x568, 375x812, 390x844, 430x932 phones
  - 768x1024 tablet portrait
  - 1024x768 tablet landscape
  - 1280x800 desktop baseline
- Verified:
  - `pnpm --filter @omnitwin/web lint`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web test -- src/lib/__tests__/editor-save-status.test.ts src/__tests__/editor-store.test.ts src/components/__tests__/PlacementHint.test.ts`
  - `pnpm --filter @omnitwin/web e2e -- e2e/editor-mobile-shell.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web e2e -- e2e/public-config-flow.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts e2e/editor-interactions.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web test`
  - `pnpm --filter @omnitwin/web build`

## Completed: T-136 scene-first mobile 3D planner shell

- Follow-up to T-135 after the first mobile safety pass still left a compressed desktop toolbar on phones.
- Added `packages/web/src/components/editor/MobilePlannerTopBar.tsx`:
  - mobile top bar with room, layout state, honest save state, 2D/3D segment, and compact Send action
  - offline state reports browser-local changes rather than pretending the server has saved
- Refactored send preparation into `packages/web/src/components/editor/send-layout-flow.ts` so desktop `SaveSendPanel` and mobile top bar share the same flush/capture/upload path.
- Reworked `packages/web/src/components/editor/VerticalToolbox.tsx`:
  - mobile now uses a stateful touch dock instead of the desktop rail squeezed horizontally
  - idle mode shows Select/Add/View/More
  - placing mode shows a bottom sheet with `Tap to place` guidance plus Rotate/Cancel/View controls
  - selected mode has a compact object sheet with Done/Rotate/Delete/View controls
  - desktop keyboard shortcuts remain desktop-only
- Hid desktop/debug-like `SectionSlider` and wall controls on mobile/touch chrome; desktop keeps them.
- `PlacementHint` is now desktop-only; mobile placement guidance lives in the bottom sheet, avoiding clipped `Don't show again` coachmarks.
- Expanded `packages/web/e2e/editor-mobile-shell.spec.ts`:
  - 320, 375, 390, 430 phone widths
  - 768 portrait tablet, 1024 landscape tablet
  - 1280 desktop baseline
  - asserts no horizontal overflow, no mobile desktop hints, CTA/topbar/dock viewport bounds, CTA outside the scene focal zone, no clipped placement coachmark, and desktop shortcut preservation.
- Updated source-grep tests for the shared send preparation helper.
- Verified:
  - `pnpm --filter @omnitwin/web lint`
  - `pnpm --filter @omnitwin/web typecheck`
  - `pnpm --filter @omnitwin/web test -- src/lib/__tests__/editor-save-status.test.ts src/components/__tests__/PlacementHint.test.ts`
  - `pnpm --filter @omnitwin/web e2e -- e2e/editor-mobile-shell.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web e2e -- e2e/public-config-flow.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web e2e -- e2e/editor.spec.ts e2e/editor-interactions.spec.ts --reporter=list --workers=1`
  - `pnpm --filter @omnitwin/web build`
  - `pnpm --filter @omnitwin/web test`

## Completed: T-137 Residual Radiance Layer doctrine

- Incorporated two learned radiance residual research reports into `docs/architecture/residual-radiance-layer.md`.
- Documented Residual Radiance Layer as a research track, not a T-091A blocker.
- Reaffirmed the production T-091 path: Spark/SPZ splat, structural mesh, Truth Mode, and Hero Regions.
- Committed the core doctrine:
  - semantic/PBR mesh remains authoritative for geometry, semantics, collisions, layout constraints, editing, measurement, and exports
  - residual is subordinate and appearance-only unless a future ADR explicitly grants another authority
  - residual should be surface-bound where possible and inherit semantic identity from the mesh
  - residual must be explainable in Truth Mode and droppable under performance pressure
- Defined binding strategies: mesh triangle ID, barycentric coordinate, UV coordinate, local tangent frame, region ID, semantic class, and approved free-space splat for special classes only.
- Defined additive-first composition, alpha-over special cases, first prototype scope, research candidates, browser/runtime posture, metrics, failure gates, and fallback ladder.
- Added Tier 6 research tasks:
  - T-138 fixed-light zone prototype plan
  - T-139 MILo / Gaussian Frosting / Spark feasibility spike
  - T-140 semantic PBR mesh / NVDIFFRECMC experiment
  - T-141 object insertion probe-grid residual demo
  - T-142 residual metrics suite
  - T-143 Spark custom residual shader feasibility
  - T-144 neural texture / deferred shader research branch
  - T-145 limited lighting-state residual experiment
  - T-146 stained-glass/chandelier special-class residual policy
- No runtime code, dependencies, production rendering, public marketing copy, or package rename changed.
