/**
 * Architectural model colour palette — clean, professional, premium.
 *
 * Designed for a venue planning tool: bright, open, and easy to read.
 * Grid lines and furniture will layer on top of these neutral surfaces.
 */

/** Light warm grey — clean floor surface with subtle warmth. */
export const FLOOR_COLOR = "#e8e0d8";

/** Clean white with hint of cream — upper wall panels. */
export const WALL_COLOR = "#f5f2ee";

/** Slightly darker than walls — provides ceiling depth. */
export const CEILING_COLOR = "#e0ddd8";

/** Warm medium grey — wainscoting / lower wall accent. */
export const WAINSCOT_COLOR = "#c8c0b8";

/** Height in meters of the wainscoting panels. */
export const WAINSCOT_HEIGHT = 2.5;

/** Floor grid line colour — subtle, non-distracting. */
export const GRID_COLOR = "#c0b8b0";

/** Floor grid secondary line colour — even more subtle. */
export const GRID_COLOR_CENTER = "#a89888";

/** Dome ceiling — slightly cooler than the flat ceiling to differentiate. */
export const DOME_COLOR = "#d5d8de";

/** Human reference figure — neutral medium grey, visible but not distracting. */
export const HUMAN_FIGURE_COLOR = "#8090a0";
