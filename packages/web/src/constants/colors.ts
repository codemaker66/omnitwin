/**
 * Room surface colours — flat hex values for meshBasicMaterial.
 * Chosen for clear visual distinction when viewing geometry without lighting.
 */

/** Dark grey floor — grounds the room visually. */
export const FLOOR_COLOR = "#3a3a3a";

/** Warm cream walls — period-appropriate for Trades Hall Glasgow. */
export const WALL_COLOR = "#f5e6c8";

/** Off-white ceiling — subtle separation from walls. */
export const CEILING_COLOR = "#f0f0f0";
