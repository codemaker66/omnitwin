{
  "name": "@omnitwin/api",
  "version": "0.0.1",
  "private": true,
  "description": "OMNITWIN API — Fastify + Drizzle ORM + Neon PostgreSQL",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "node --max-old-space-size=4096 -e \"require('child_process').execSync('tsc --noEmit', {stdio:'inherit'})\"",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "lint": "eslint src/",
    "clean": "rm -rf dist",
    "dev": "tsx watch src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "tsx src/db/seed.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/jwt": "^9.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@neondatabase/serverless": "^0.10.4",
    "@omnitwin/types": "workspace:*",
    "argon2": "^0.41.1",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.38.4",
    "fastify": "^5.2.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "drizzle-kit": "^0.30.4",
    "eslint": "^9.19.0",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.22.0",
    "vitest": "^3.0.5"
  }
}
