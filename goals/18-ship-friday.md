# 18 · Ship Friday — Release 1, executed by Opus worker sessions

Written Tuesday 15 September 2026 by the Fable session at Blake's request ("construct a ./goal for opus to follow"). This card is the runnable form of [plan 18](../docs/plan/18-SHIP-FRIDAY-ULTRA-PLAN-2026-09-14.md). The plan holds the evidence, the file:line anchors and the reasoning; this card holds what a worker session needs to act without re-deriving any of it. Where the two disagree, the plan's §4.0 rules win and the worker records the disagreement in the session log.

## The /goal block (paste into an Opus session)

```text
/goal Read goals/18-ship-friday.md and execute it. You are one of up to thirteen worker sessions delivering Release 1 of docs/plan/18-SHIP-FRIDAY-ULTRA-PLAN-2026-09-14.md by Friday 18 September 2026, 16:00 BST. First read goals/EXECUTION.md under the heading "Ship Friday": if no captain row exists, you are the captain — record yourself, then do Slice 0. Otherwise claim the first unclaimed lane in the queue order in this card, record the claim, and do that lane's Tier A only. Work only in your own worktree cut from release/r1. Never merge to master, never write to the production database, never edit an e2e spec or playwright.config.ts, never push codex/inventory-selected-style, never reset C:\Users\blake\omnitwin2. Verify every item with the lane's commands before recording it as done; record local, committed, merged and verified as different states. Stop and record a blocker rather than guess when a Blake decision your lane needs is unanswered.
```

## Outcome, in Blake's words

"i want it to shippable by this friday … a beautiful looking venue planner where we can drag and drop furniture into with 60fps on mobile devices … a replacement to matterport's venue viewing capability … the behind the scenes work area … for hallkeepers, caterers, venue salesmen, venue managers, venue bookers and clients … fully operational and in sync … buttons they can press for the usual help … a notication flashing on their event … everything must look beautiful and designed to be there and perfect, nothing must look vibe coded or placeholder." (14 September 2026.) And: "construct a ./goal for opus to follow as you are quite expensive as a model to use for everything." (15 September 2026.)

Release 1 is the public-ready floor for Trades Hall, defined by the thirty gate lines in plan 18 §2. Direct messages, caterer surfaces, the client page and the walk's measure/share tools are Release 2 and later (plan 18 §3); do not add them this week.

## Where we are (verified 15 September 09:40 BST; re-verify the sha before you cut a worktree)

- Production web and API are both `origin/master` 641f2667. Vercel publishes every master push with no gate; Railway rebuilds the API from any master push that touches `packages/api` or `packages/types`; migrations are applied only by hand through the guarded helper. **This is why nothing merges to master before Friday.**
- Master CI has been red since 11 May; the four E2E shards fail on every run. Codex's T-613 (`origin/codex/browser-release-20260915`) owns the repair; no worker in this goal edits a spec.
- Migration 0071 is reserved by T-605. This goal uses 0072 (Lane 10), 0073 (Lane 10, Friday only) and 0074 (Lane 9). Only the migration owner touches `packages/api/drizzle/meta/_journal.json`.
- The repository is public. The branch `codex/inventory-selected-style` and commit d044dec6 carry a retained private venue document; they are never pushed and never cherry-picked.
- The shared checkout `C:\Users\blake\omnitwin2` is 141 commits behind origin, dirty, and the common `.git` for twenty worktrees. It is not production and it is never reset without Blake's confirmation (decision 15).
- The twin is live at `https://twin.venviewer.com` (149 scans). The Grand Hall walk is live for five rooms but look-only on touch. The planner cannot move furniture by touch. Production inventory had zero stock rows on 7 September. There is no messaging or request model. Full detail: plan 18 §1.
- Blake's answers to the seventeen decisions in plan 18 §6 are recorded, when they arrive, in `docs/sessions/2026-09-15.md` (or the day's log). Until an answer exists, the rules in "Decided" below apply.

## Decided (the operating contract; plan 18 §4.0 in full)

- **One captain.** The session that records the first "Ship Friday" captain row in `goals/EXECUTION.md` holds the T-601 release lane for the week. It alone merges into `release/r1` and master, allocates migration tags, runs the Friday sequence, and is the only writer of `docs/state/tasks.md`. It owns no lane. Every other worker owns exactly one lane at a time.
- **One integration branch.** `release/r1` is tagged from `origin/master` by the captain at the start of Slice 0 and its sha is written in `goals/EXECUTION.md`. Every worker cuts `git worktree add D:/claude/venviewer-lane<N>-20260915 -b codex/ship-friday-lane<N> release/r1`. Every PR targets `release/r1`. Master receives one merge, on Friday, by the captain, in this order: Neon backup branch id recorded → migrations through the guarded helper against the exact `release/r1` sha → push `release/r1` to master → Railway and Vercel build the same sha → `/health/version` shows it. `railway up` only if the git build does not fire, and only from a checkout that `railway status` confirms is linked to the existing service.
- **Merge windows** into `release/r1`: Wednesday 12:00 (Lanes 8, 10, the T-581 rebase, 1, 7, the twin-cad merge), Wednesday 18:00 (Lanes 5, 6, 2), Thursday 08:00 (Lanes 4, 3, 9, 11). Feature freeze Thursday 14:00. Gate Thursday 15:00. Go/no-go Thursday 18:00; no-go means Monday 21 September.
- **File ownership.** `index.html` head = Lane 2, its `<style>` globals = Lane 1. `router.tsx` = Lanes 2 and 8 only, one-line patches from anyone else, diff-stat under 60 lines, never reformatted (CRLF). `DashboardLayout.tsx` and `NAV_ITEMS` = Lane 8. `NotificationCenter.tsx` = Lane 9. `DayBoardPage.tsx` = Lane 6 until window 2, exposing a `<SlotRequests>` mount point. `PlannerScene.tsx` = Lane 4. `RoomSplatScene.tsx` = Lane 3 after the T-581 rebase lands. `utils/query.ts` and every role set = Lane 8. `seed.ts` = Lane 10 until window 1. E2E specs, `playwright.config.ts`, visual baselines = T-613 (Codex); report failures to it via `goals/EXECUTION.md`.
- **Decisions and defaults.** Lane 1 does not start until decision 1 (the register) is answered. Lanes 8 and 10 do only fan-out inventory and the stub PR until decision 6 (roles) is answered. Decisions 10 (seed purge), 14 (stock import) and 15 (checkout reset) require an explicit yes in the session log before any action; there is no default. Every other decision's default is its recommendation in plan 18 §6. Adopted without a decision: one pulse then a steady dot on the request slab; R1 water requests route to the hallkeeper; legacy proposal links redirect for thirty days.
- **Register.** Ivory, forest green, copper and sage (`.claude/conventions/product-experience.md`), dark graphite only inside the 3D viewport; the golden cursor, gold scrollbar and brass accent go. The Trades House campaign pages keep their own brand.
- **Test ink.** Every production check uses a booking, enquiry or request titled "VENVIEWER TEST — delete" in a named window; the Friday 17:30 release note deletes them by id.
- **Honesty.** No 60 fps claim until a real device measures it; unmeasured devices are listed as unmeasured. Local, committed, merged to `release/r1`, on the preview, on production and accepted by Blake are six different states; record which one you reached.

## The work, in slices (queue order; Tier A only unless your Tier A is green and it is before Thursday 14:00)

Each slice names its hours, its plan-18 §2 gate lines, its Tier A items in one line each (the plan's Lane section has the file:line anchors), its verify commands and its owned files. Claim a slice by adding a row under "Ship Friday" in `goals/EXECUTION.md`: `| Lane N | <session name> | codex/ship-friday-laneN | D:/claude/venviewer-laneN-20260915 | §2 lines … | claimed <time> |`. Create the heading if it does not exist.

### Slice 0 — The captain (Lane 0; 14 h, plus an ops session of 5 h; Tuesday)

Gate line 28. Do in order: (1) `git fetch`; record the available sessions and the current `origin/master` sha under "Ship Friday" in `goals/EXECUTION.md`; tag `release/r1` at that sha and push it; widen `.github/workflows/ci.yml` so `release/**` gets the check. (2) Push the eight local-only `codex/*` branches except `codex/inventory-selected-style`, and the two unpushed `worktree-twin-cad` commits. (3) Cherry-pick 997d980b as the first PR into `release/r1`. (4) Rescue the shared checkout's five landing commits to `rescue/shared-checkout-landing-2026-09-15` and its dirty files to `rescue/shared-checkout-dirty-2026-09-15` with explicit pathspecs; push both; do not reset the checkout. (5) Integrate `origin/codex/browser-release-20260915` (T-613) into `release/r1` first, editing no spec yourself; new specs from other lanes run in a separate `e2e-new` job with a 20-minute budget. (6) Vercel gate: the Ignored Build Step skips master (`[ "$VERCEL_GIT_COMMIT_REF" != "master" ]`); add a `deploy-web` job to `deploy.yml` after `migrate`; enable branch protection on master only after Blake has added himself as required reviewer on the GitHub `production` environment and `gh api repos/:owner/:repo/environments` shows it. (7) Publish the queue and the migration tags (0072 Lane 10, 0073 Lane 10 Friday only, 0074 Lane 9) in `goals/EXECUTION.md`. Ops session, separately: Sentry on both sides with one verified scrubbed event; uptime monitors on `/health/live`, `/health/ready`, `/health/observability` and the site with an alert route; the Neon restore drill on a disposable branch, recorded; `cleanup.yml` gains a dry-run input and a receipt.
Verify: CI green on `release/r1`; `gh api repos/:owner/:repo/branches/master/protection` returns 200; the rows in `docs/operations/production-monitoring-checklist.md` carry ids and timestamps.
Owns: `.github/workflows/*`, `packages/web/vercel.json`, `goals/EXECUTION.md`, `docs/state/tasks.md`, `docs/operations/*`.

### Slice 1 — Roles (Lane 8; 22 h; Tuesday–Wednesday; first in window 1)

Gate lines 25, 26. Stub PR by Tuesday 16:00: `utils/query.ts` exports `canAdministerVenue`, `canManageCommercial`, `canReadInventory`, `canWriteInventory`. After decision 6: add `caterer`, `sales`, `manager` to `USER_ROLES` in `packages/types/src/user.ts` and fan out to `auth.ts`, `env.ts`, `types/onboarding.ts`, `role-routing.ts`, `DashboardLayout.tsx` (delete the `executive` and `supplier` branches), `booking-mutations.ts` `DIARY_WRITE_ROLES`, `ws/diary-live.ts` `DIARY_READ_ROLES`, `enquiries.ts`, every `router.tsx` `allowedRoles` literal; make the `EventPlanAudienceRoleSchema.parse(request.user.role)` sites safe parses; hallkeepers lose edit on venue, spaces and pricing but keep inventory read and room-state write. Collapse the remaining per-route helpers into `utils/query.ts`. The public dead end: uninvited signed-in identities reach a "Request access" form; denial screens get "Use another account" and "Back to Venviewer".
Verify: `pnpm --filter @omnitwin/api exec vitest run src/__tests__/auth.test.ts src/__tests__/invitation-auth.test.ts src/__tests__/proposal-permissions-postgres.test.ts src/__tests__/quote-permissions-postgres.test.ts` plus new 401/403/400 tests on every touched route; `pnpm --filter @omnitwin/types test`.
Owns: `packages/types/src/user.ts`, `packages/api/src/utils/query.ts`, every role set, `DashboardLayout.tsx`, `router.tsx` `allowedRoles`.

### Slice 2 — Inventory and data (Lane 10; 22.5 h + 4 h Friday if decision 14; Tuesday read-only)

Gate line 27. Tuesday, read-only: `SELECT count(*)` on the stock and `asset_definitions` tables; compare production bookings for w/c 7 and w/c 14 September against the seed titles (Mackenzie–Ross, MacLeod, Robertson, Kerr, Nairn, Sinclair); record both in the session log. Extract only `docs/data/trades-hall-equipment-2026-09-05.json` from d044dec6 with `git show` into your worktree; never cherry-pick the commit. Migration 0072 (window 1): CHECK constraints for `users.role` (Lane 8's list), `configurations.state/review_status/visibility/layout_style`, `enquiries.state`, the `EVENT_PLAN_AUDIENCE_ROLES` CHECK widening, plus indexes `events(venue_id, starts_at)`, `events(venue_id, ends_at)`, `pricing_rules(venue_id, space_id) WHERE deleted_at IS NULL`, `placed_objects(asset_definition_id)`. Migration 0073 (applied Friday only): retire or rename the 8 legacy `asset_definitions` rows and add the 3 missing canonical items; add Chiavari, pink, red/gold gallery chairs, highchairs, staging, TVs, mics to `asset-catalogue.ts`. Copy pass: remove "check dimensions"/"Approx.", the "Add catalogue items" dead end, digest hashes in the UI; replace the invented accessories in `hallkeeper-accessories.ts` with the venue's real linen or remove them. Read-only inventory for hallkeeper, staff and planner via Lane 8's `canReadInventory`. The seed purge only after decision 10 and a fresh Neon backup branch (Thursday after go). The stock import only after decision 14, Friday 16:30, through the audited stock-adjustment command with Blake's admin account as actor.
Verify: `pnpm --filter @omnitwin/api exec vitest run src/__tests__/migration-tail-readiness.test.ts src/__tests__/venue-inventory-postgres.test.ts src/__tests__/inventory-reservations-postgres.test.ts src/__tests__/route-atomicity-postgres.test.ts` against the disposable Postgres; `SELECT` counts before and after.
Owns: `packages/api/drizzle/0072*`, `0073*`, `_journal.json` (with Lane 9 for 0074), `seed.ts` until window 1, `asset-catalogue.ts`, `hallkeeper-accessories.ts`, the inventory components.

### Slice 3 — One register (Lane 1; 20 h; starts after decision 1)

Gate lines 3 (signed-in count), 7 (theme colour). Remove the `index.html` globals (golden cursor, gold scrollbar, universal button pop). Load Inter 700 (one-line patch to Lane 2 for `router.tsx`) and replace the 178 rules requesting weights 720–950 with 600/700. Extend `styles/house-tokens.css` with `--vv-ivory`, `--vv-forest`, `--vv-copper`, `--vv-sage`; define or rename the four undefined House tokens; add a test that every consumed `var(--house-*)` resolves. Move `ToastContainer`, `StatusBadge`, `ConfirmModal` and the five `aria-modal` dialogs without a focus trap onto the tokens with traps and Escape/return-focus; retire the brass accent; replace the twelve ASCII "..." with "…". Raise every rule at 9 px or below to 11 px on live surfaces. Restore text selection in planner inputs (`App.css`). `<main>` landmarks on the eleven pages without one. `theme-color` aligned with the page ground. One focus-ring style.
Verify: `pnpm --filter @omnitwin/web exec vitest run src/__tests__/house-tokens.test.ts`; the a11y route audit; before/after screenshots at 390 and 1366 of Diary, dashboard, planner, home.
Owns: `index.html` `<style>`, `styles/house-tokens.css`, `components/shared/*`, `App.css`, `global.css`.

### Slice 4 — The back office (Lane 7; 28 h)

Gate lines 4 (email), 23. `crm.ts` and `opportunities.ts` stop returning 403 to venue admins (import `canManageCommercial` from Lane 8; the nav-gating change goes to Lane 8 as a patch). Newest-first with real pagination on enquiries, proposals, opportunities and follow-ups. Analytics truthfulness: `venueId` or a venue picker for platform admins; one definition of pipeline value shared with the Pipeline tab; utilisation from bookings; drop the permanently empty scenario cards (fallback: hide the wrong numbers). Notifications that fire on a new public enquiry and on client accept/request-changes regardless of `configurationId`, to staff and sales, with the unread count on the visible nav. `EMAIL_FROM` set on Railway to the venue's name with a monitored reply-to; the client acknowledgement template in the venue's voice with the organiser's name and an en-GB date. Reviewer email deep link fixed (`/dashboard?view=reviews&config=:id`). Remove or dev-gate the "DEMO ONLY" copy; retire `/proposal/:shareCode` behind a thirty-day redirect to the token page.
Verify: `pnpm --filter @omnitwin/api exec vitest run src/__tests__/enquiries src/__tests__/crm src/__tests__/opportunities src/__tests__/revenue-analytics`; a signed-in production check as Blake's admin on Friday; the received acknowledgement email's From and greeting read.
Owns: `packages/api/src/routes/{crm,opportunities,enquiries,proposals,revenue-analytics,public-enquiries,configuration-reviews}.ts`, the email templates, the dashboard views except `DashboardLayout.tsx` and `NotificationCenter.tsx`.

### Slice 5 — The timetable (Lane 5; 31 h)

Gate lines 17, 18, 19. The enquiry tray requests `status=submitted` and `under_review` with a full page and stops refetching everything on every board change. Create-in-context: New booking seeds the viewed day and room; clicking an empty overview cell or day-lane position opens the drawer prefilled. Touch: long-press lifts blocks and slips, `touch-action: pan-x pan-y` until lifted, the immediate `preventDefault` on slips removed. The edit drawer shows and edits notes, allows room change, shows the owner's name and the linked client. Global shortcuts guarded while a drawer or `<select>` has focus; self excluded from the presence count. Hold reminders on a GitHub Actions or Railway cron hitting `POST /admin/diary/hold-reminders` with a service token and one dry-run receipt. `SELECT` from `turnaround_rules` on production and find why `GET /calendar` reports "not checked"; no migration. Stale strings in the two diary e2e specs reported to T-613. The hidden month view retired and the T-603 residue swept.
Verify: `pnpm --filter @omnitwin/web exec vitest run src/pages/diary`; `pnpm --filter @omnitwin/web exec playwright test e2e/diary*.spec.ts --workers 1` on the disposable stack; a signed-in production check on Friday with a labelled test booking on a phone and a laptop; the cron log.
Owns: `packages/web/src/pages/diary/**`, `packages/api/src/routes/{bookings,calendar}.ts`, `services/hold-reminders.ts`.

### Slice 6 — The hallkeeper's day (Lane 6; 28 h)

Gate lines 20, 21. Reconcile T-606 first (done on master; rebase, do not re-skin what it published). The Day Board → setup sheet corridor without a compiled handoff (`booking.eventId` → event configuration link → approved configuration → `/hallkeeper/:configId`), with an honest "no sheet yet" state and a `<SlotRequests>` mount point on the slot for Lane 9. PDF and sheet times pinned to `data.venue.timezone` and derived from the linked Diary booking, never an 18:00 UTC default. The fictional walkthrough removed from production navigation and moved under `/dev`. A token, type and copy pass across the six hallkeeper pages (Day Board, sheet, room-plans library, event-day board, handoff, mission control); no "4D Mission Control / Spatial command map" copy. Live sync on the event-day board (reuse `useDiaryLive` or a guarded 10 s poll; refetch after every mutation; acknowledgements read from Lane 11's route). Issues can be resolved, closed and assigned. Mission Control's incident form and task grid hidden when the ops board's controls are shown. Supplier arrivals shown only when captured; compiler notes labelled as notes. Venue timezone instead of the hard-coded "Europe/London".
Verify: `pnpm --filter @omnitwin/api exec vitest run src/__tests__/hallkeeper-pdf-v2.test.ts src/__tests__/hallkeeper-floor-plan.test.ts`; `pnpm --filter @omnitwin/web exec vitest run src/pages/hallkeeper src/pages/__tests__/HallkeeperPage.test.tsx`; one booking followed phone → sheet → PDF on the disposable stack with the PDF opened; screenshots of the six pages.
Owns: `packages/web/src/pages/hallkeeper/**`, `HallkeeperPage.tsx`, `EventDayOpsPage.tsx`, `OpsHandoffPage.tsx`, `EventMissionControl.tsx`, `hallkeeper-pdf-v2.ts`, `hallkeeper-sheet-v2-data.ts`, `ops-compiler.ts` arrivals.

### Slice 7 — The front door (Lane 2; 36.5 h; the six-session variant keeps 19 h by promoting `/fresh` to `/`)

Gate lines 1–7. One canonical home: merge `FreshPage`'s photography, capacities, wedding rates and enquiry composer into `RoomsHomePage`; the photographic Grand Hall (WebP ladder ≤300 KB, `fetchpriority=high`) is the hero; capacities worded as envelopes from `lib/trades-hall-venue-truth.ts`. Staff links out of the public nav on `/`, `/fresh` and `/room/:slug`; pinned tests updated. Enquire reaches the composer from every public page; every mailto replaced (rite, living hall, quiz, leaflet page, legal contact, `/fresh` footer); the composer posts `TRADES_HALL_ENQUIRY_VENUE_SLUG` from `@omnitwin/types` with a route test asserting 201. `/pricing` out of nav, sitemap and robots; `/demo` behind the admin route guard. Redirect `/landing`, `/welcome`, `/living-hall`, `/editor` to `/`; retire or 301 `/venues/trades-hall/rooms/:slug`; kill the dead anchors; a designed not-found page replacing the `*` redirect. Trades House leaflet: remove the review note or de-list the page; replace the quiz mailto. WebP ladders for the hero and the seven stills; drop the two unused font families. Apple-touch-icon, manifest, per-route `<title>`, `viewport-fit=cover`; fix the stale `index.html` comment.
Verify: a new `packages/web/src/__tests__/public-copy-sweep.test.ts` (renders every public route, asserts none of the plan's forbidden strings, greps `dist/` after `pnpm --filter @omnitwin/web build`); `pnpm --filter @omnitwin/web exec vitest run src/pages/__tests__/RoomsHomePage.test.tsx`; `curl -I` each retired route on the preview; a real enquiry on production Friday.
Owns: `index.html` head, `router.tsx` routes and redirects, `RoomsHomePage.*`, `pages/fresh/**`, the retired landing pages, `public/` (manifest, icons, robots, sitemap, security.txt), `TradesHouse*` pages.

### Slice 8 — The planner on touch (Lane 4; 31 h)

Gate lines 12 (planner), 13, 14, 16, 1 (`/blueprint`). The T-581 ladder rebase (6dad07fe) as its own PR in window 1, rebased onto the `release/r1` sha, four conflicts resolved, the `RoomSplatScene.tsx` deletion reviewed by the room session; this gives the planner the device profile and the coarse-first ladder. Touch drag-to-move: in `SelectionSystem.tsx` a primary touch on the selected item (or any placed item after one tap) enters the existing move gesture with pointer capture; two fingers stay camera; check the overnight touch-chrome commits (68172fc0, 713cad08, 3ee0e5be) first. DPR clamped to 2 and 1.5 while the camera moves via `AdaptiveResolution` or `regress`. WebGL context loss handled on the planner canvas. `/blueprint` gated with `withPlannerAuth` as the WebGL-failure fallback only. Real configuration and space names in `MobilePlannerTopBar.tsx`. A `?perf=1` frame-interval sampler writing to `window.__venPerf`; run Reception Room + 100 chairs on the 4090 and on every device Blake lends; publish the JSON to `D:\claude\device-matrix\` with unmeasured devices listed as unmeasured.
Verify: `pnpm --filter @omnitwin/web exec vitest run src/components/__tests__/SelectionSystem.test.ts src/components/editor/__tests__/PlannerScene.test.tsx src/components/editor/__tests__/MobilePlannerTopBar.test.tsx`; Lane 12's touch e2e at 390×844 and 1024×768.
Owns: `PlannerScene.tsx`, `SelectionSystem.tsx`, `CockpitSplatLayer.tsx`, `use-room-runtime-splat.ts`, `planner-resolution-policy.ts`, `CameraRig.tsx`, `MobilePlannerTopBar.tsx`, `EditorPage.tsx`.

### Slice 9 — The room: walk and twin (Lane 3; 38 h)

Gate lines 8–12 (walk). Verify `/tour` and `/venues/trades-hall/twin` from a fresh profile on desktop and phone; flip `FRESH_TOUR_ENABLED` so the front door links to the twin. Merge `worktree-twin-cad` in window 1 (merge-tree clean; the branch changes the forge, so re-forge the Trades Hall bundle with the new nav graph, `rclone` the delta to the twin bucket, verify manifest edges through the domain; fallback by Thursday 14:00: ship the 11 July bundle). Touch locomotion in `InteriorCamera.tsx`: tap the floor to glide (raycast y=0, clamp with `containPosition`), hold-to-walk, a second pointer as pinch-to-move-forward and never as look (fallback: tap-to-glide only). A real mobile tier: touch-primary "Apple GPU"/Adreno/Mali classified below "high" by UA + `pointer: coarse` + `maxTouchPoints` + `deviceMemory`, with `lod: true`, a 1.0–1.5 M motion budget and settled DPR 1.5; fix `lib/__tests__/device-tier.test.ts:44`. Serve the vendor's coarser level (Grand Hall level 4, 2.95 M / 51.6 MB) as the sharp layer for weak tiers via `roomSplatLadder(profile)`, labelled interim. Public walk chrome: no staff links, `100dvh` and safe-area insets, a fullscreen control, an honest status pill that never sticks at 0 % (the T-582 poller, after the T-581 rebase lands). WebGL context loss handled on the walk canvas.
Verify: `pnpm --filter @omnitwin/web exec vitest run src/lib/__tests__/device-tier.test.ts src/components/rooms/__tests__/InteriorCamera.test.tsx`; `window.__roomWalk` ledger on the 4090 and every device available; the network log on a real iPhone shows the ≤52 MB level; `curl -sI https://twin.venviewer.com/trades-hall/manifest.json` returns 200 application/json.
Owns: `InteriorCamera.tsx`, `RoomWalkPage.*`, `RoomSplatScene.tsx` (after the rebase), `device-tier.ts`, `splat-runtime-profile.ts`, `twin/**`, `tools/twin-forge/**`, `fresh-copy.ts` tour flag.

### Slice 10 — Requests and the flashing slot (Lane 9; 30 h; Tuesday–Friday)

Gate line 22. Requires decisions 12 and 13 (default yes). Migration 0074: `requests` (venue id, booking or slot, room, kind `refreshments | temperature | cleaning | av | access | other`, quantity, urgency `routine | soon | now`, free text, requested-by actor, owner, state `sent | acknowledged | accepted | resolved` with outcome and note, idempotency key) plus `request_status_history`; Zod in `@omnitwin/types`; routes gated by venue tenancy; the audience fixed at creation. Chairs and tables are R2. Live delivery: `notification.created` and `request.changed` on the bus `EventMap`, emitted after commit, fanned out over `/ws/diary` to the venue's connected staff; reconnect replays. The request slab: `day-board-state.ts` (pure, unit-tested) gains a requests input keyed by booking; until window 2 touch only that file and the API, then fill Lane 6's `<SlotRequests>` mount point: one slab per open request, one pulse then a steady dot, accept and resolve from the slot; the Diary drawer shows the same request. Staff create requests from the slot. Unread count on the visible nav. Escalation: an unacknowledged "now" request escalates to the venue admin by in-product notification plus Resend email after the window read from a `venue_settings` row seeded for Trades Hall (default 3 minutes), never a code constant.
Verify: `pnpm --filter @omnitwin/api exec vitest run src/__tests__/requests` (401/403/400, audience immutability, idempotency replay returns the same request); `pnpm --filter @omnitwin/web exec vitest run src/pages/hallkeeper/lib/__tests__/day-board-state.test.ts`; three signed-in identities on the disposable stack over the real websocket; Friday 16:30 on production, one labelled test booking: a request pressed on one phone appears on a second within a second.
Owns: `packages/api/drizzle/0074*`, `packages/api/src/routes/requests.ts`, `packages/types/src/requests.ts`, `ws/diary-live.ts` fan-out, `observability/event-bus.ts`, `day-board-state.ts`, `NotificationCenter.tsx`.

### Slice 11 — The event chain (Lane 11; 12 h; Wednesday–Thursday)

Gate lines 20 (corridor half), 21 (acknowledgements). Pass the linked `eventId` from `?eventId` into `compileOpsHandoffPack` in `OpsLensPanel.tsx` and create the `event_configuration_links` row when the corridor opens, so the Event Day board stops saying "missing handoff". Persist change acknowledgements (`acknowledgedBy`/`acknowledgedAt` or `GET /events/:id/change-acknowledgements`) for Lane 6 to read. Hide `AIDraftPanel` when `/ai/status` is not configured; reword the Event Architect hero; hide its nav link for R1 (a `NAV_ITEMS` patch sent to Lane 8).
Verify: `pnpm --filter @omnitwin/api exec vitest run src/__tests__/event-day-ops src/__tests__/events.test.ts src/__tests__/ops-handoff`; `pnpm --filter @omnitwin/web exec vitest run src/components/editor/cockpit/__tests__/OpsLensPanel.test.tsx src/__tests__/EventDayOpsPage.test.tsx`; open `/ops/events/<id>` on the preview and confirm the board no longer says "missing handoff".
Owns: `OpsLensPanel.tsx`, `event-day-ops.ts` acknowledgements, `AIDraftPanel.tsx`, `EventArchitectPage.tsx` hero.

### Slice 12 — Verification and release (Lane 12; 13 h; Wednesday–Friday; never the captain)

Gate lines 13 (e2e), 28 (rehearsal evidence), 29 (touch). A touch drag-and-drop e2e for the planner at iPhone and iPad viewports using `hasTouch`/`page.touchscreen`, in the `e2e-new` job, branched from `release/r1` after window 1 so it uses Lane 10's seed; Clerk test keys from Blake by Wednesday 08:00. Dead test code and machine-bound paths removed with T-613's agreement (`public-config-flow.spec.ts` skipped blocks, the `fixme` in `plan-room-resolve.spec.ts`, the six `C:/Users/blake` artifact paths, `demo-smoke.mjs`'s import). The release rehearsal on the disposable stack and a Vercel preview in the §4.0 order; `railway status` confirms the link before any `railway up`. The production smoke after the Friday deploy.
Verify: `pnpm --filter @omnitwin/web exec playwright test e2e/touch*.spec.ts --workers 1`; the CI job URL for `e2e-new`; the rehearsal's ids and timestamps in `docs/sessions/2026-09-18.md`.
Owns: `e2e/touch*.spec.ts` (new), `packages/web/scripts/demo-smoke.mjs`, the rehearsal record.

## Checkpoints and the kill order (the captain decides; a lane worker proposes)

Checkpoint 1, Tuesday 18:00: is `release/r1` cut with T-613 integrated and CI running on it? Checkpoint 2, Wednesday 18:00: is every Tier A lane green in its worktree? If behind at either, cut in this order, Tier A first: (1) the twin-cad merge → R2, ship the 11 July bundle on the current viewer; (2) Lane 9 → staff-created requests only, escalation email dropped; (3) Lane 2's canonical-home merge → `/fresh` promoted to `/`; (4) Lane 6's token pass → walkthrough removal, corridor, times and live sync only; (5) Lane 7's analytics → hide the wrong numbers; (6) Lane 3's touch locomotion → tap-to-glide only; (7) Lane 4's ladder rebase → DPR cap and touch drag only; (8) Lane 11 → R2; (9) Lane 9 → R2 entirely. Never cut: Slice 0, Lane 1, the reduced Lane 2 (staff nav, Enquire and its slug guard, pricing off, `/demo` gated, the landing cull, dead anchors, images, SEO/PWA), Lane 10's seed purge and 0072, gate lines 1–5 and 7 once decisions 2–4 are yes, and the CI, branch-protection and deploy-web half of line 28. If the gate is not green at Thursday 18:00 the release is Monday 21 September; say so in the log rather than shipping a half-painted door.

## Done when

Every gate line in plan 18 §2 that your lane lists is true on the Vercel preview of `release/r1` against the disposable stack, verified by a session other than yours, and recorded in `docs/sessions/2026-09-1x.md` with the command output or screenshot path. For the captain: every Tier A lane merged into `release/r1`, the §2 gate run at Thursday 15:00 with a second session verifying, the go/no-go recorded at 18:00, and on Friday the release sequence completed with `/health/version` showing the pushed sha, the live checks done with labelled test ink and that ink deleted, and the release note written. Local, committed, merged, on the preview, on production and accepted by Blake are recorded as six different states; do not collapse them.

## Verify (every lane, before any PR)

```
pnpm -r typecheck
pnpm -r lint
pnpm --filter @omnitwin/web build
git diff --check -- <your changed paths>
```

then the lane's own commands above. Windows-green is not Linux-green: the CI run on your PR is the truth. Do not edit web source while an e2e or harness run is in progress.

## Forbidden

- Merging anything into master before the captain's Friday sequence; pushing to master at all if you are not the captain.
- Any write to the production database except the captain's Friday migrate step and the two explicitly authorised data commands (seed purge after decision 10, stock import after decision 14), each after a fresh Neon backup branch.
- Editing an e2e spec, `playwright.config.ts` or a visual baseline (T-613's), or deleting twin baselines before the twin-cad merge.
- Pushing `codex/inventory-selected-style` or cherry-picking d044dec6; committing the `.docx`, the jpeg, or the raw equipment JSON unless decision 16 says yes.
- Resetting, checking out or stashing in `C:\Users\blake\omnitwin2`; cutting a worktree from anything but `release/r1`.
- `railway up` from a directory that `railway status` does not show as linked to the existing service (it creates a new project).
- Touching a file another lane owns without sending a patch to that lane's owner; reformatting `router.tsx`; a PR that touches `router.tsx` with a diff-stat over 60 lines.
- Writing to `docs/state/tasks.md` if you are not the captain; editing `goals/EXECUTION.md` beyond your own claim row.
- Claiming 60 fps, "measured", "verified live" or "accepted" without the evidence named in the lane's Verify; describing a merged PR as shipped.
- Starting Tier B before your Tier A is green and before Thursday 14:00; taking a second lane before your first is merged.
- Any confirmation dialog, strobe, siren or sound in a room in use; any generated image posing as the venue; any new visual language outside the register.

## Human inputs (Blake; the answers are recorded in the day's session log)

Decisions, one word each, the first four by Tuesday 12:00: plan 18 §6 items 1 (the register), 2 (homepage), 3 (pricing off), 4 (closed-room copy), 5 (planner control system), 6a/6b/6c (roles), 7 (hallkeeper execution model), 8 (Event Architect hidden), 9 (production environment reviewer), 10 (seed purge), 11 (phone tier), 12 (requests without staging), 13 (DMs in R2), 14 (stock import), 15 (checkout reset), 16 (publish the JSON — recommend no), 17 (go/no-go authority and the weekend hold).

Actions: devices lent or the one-command harness run (`D:\claude\device-matrix\`); confirmation that nothing of Blake's is open in the shared checkout and that the captain may `railway link`/`railway up` and set Vercel and Railway variables; Blake added as required reviewer on the GitHub `production` environment by Tuesday 16:00; Sentry and uptime accounts; Clerk test keys as GitHub secrets by Wednesday 08:00; equipment photographs by Wednesday; a real booking for the Friday walk; three evening judging sessions on his phone and iPad (this is gate line 30 and nothing else satisfies it).

## How to resume or hand off

Read, in this order: `goals/EXECUTION.md` under "Ship Friday" (who is the captain, which lanes are claimed, the `release/r1` sha, the migration tags); the latest `docs/sessions/2026-09-1x.md` (Blake's answers, blockers, what merged); plan 18 §4.0 and your lane's section; then `git fetch` and `git log --oneline origin/release/r1 -20`. If the captain's row is older than a day with no update, follow the shipping contract's "named owner unavailable" clause: record the handback in `goals/EXECUTION.md`, take the captain role, and continue from the last recorded step. When you stop, write one paragraph in the session log: the state you reached (local, committed, merged, on the preview), the exact next step, and any blocker with the decision or input it waits on.

## Unlocks

Release 2 on 25 September (plan 18 §3): threads and direct messages with requests folded in, the client composer and per-button escalation from venue configuration, the supplier pass, the kitchen sheet, People & access, the 3–4 MB first rung for the walk, and the goal-01 sublime rebuild on the surfaces Blake judged.
